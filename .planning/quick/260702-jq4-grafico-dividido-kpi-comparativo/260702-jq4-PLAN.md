---
phase: quick-260702-jq4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/precoMcoSeries.ts
  - src/lib/precoMcoSeries.test.ts
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
autonomous: true
requirements: [QUICK-260702-jq4]
must_haves:
  truths:
    - "A página /analise-precos mostra DOIS gráficos empilhados: um principal (preço + break-even + colchão de margem + MCO%) e um menor de barras com as unidades vendidas por bucket"
    - "O gráfico principal NÃO tem mais a linha de unidades vendidas nem o terceiro eixo Y oculto — só os dois eixos R$/un (esq) e MCO% (dir)"
    - "Os dois gráficos usam o MESMO chartData e as mesmas margens/larguras de eixo, alinhando os buckets no eixo X"
    - "Cada KPI card mostra o comparativo vs período anterior de mesma duração (% para preço/break-even/MCO R$/qtd/receita; p.p. para MCO %)"
    - "Quando o período anterior não tem dados, o comparativo mostra '—' em vez de número"
    - "A lógica de janela anterior, agregação de KPIs e cálculo de delta vive em util puro com testes; suíte vitest verde e build limpo"
  artifacts:
    - path: src/lib/precoMcoSeries.ts
      provides: "computePreviousWindow, computePriceKpis, percentDelta, pointDelta (utils puros do comparativo)"
      contains: "export function computePreviousWindow"
    - path: src/lib/precoMcoSeries.test.ts
      provides: "testes das 4 funções novas"
      contains: "computePreviousWindow"
    - path: src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
      provides: "dois gráficos (ComposedChart principal + BarChart de unidades) e comparativo nos KPIs"
      contains: "BarChart"
  key_links:
    - from: src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
      to: src/lib/precoMcoSeries.ts
      via: "computePreviousWindow(fromDate,toDate) define a janela anterior; computePriceKpis agrega ambas as janelas; percentDelta/pointDelta produzem o comparativo"
      pattern: "computePreviousWindow|computePriceKpis"
---

<objective>
Feedback round 2 do Wesley sobre o gráfico de `/analise-precos`
(`PrecoPraticadoReport.tsx`, entregue na Phase 79 + quick 260702-i8u):

1. **Dividir o gráfico único em DOIS** empilhados verticalmente e alinhados ao
   mesmo período/granularidade: (A) principal = preço praticado + break-even
   tracejado + colchão de margem (Areas) no eixo esquerdo R$/un + linha MCO% no
   eixo direito, REMOVENDO a linha de unidades vendidas e o 3º YAxis oculto que o
   quick i8u adicionou; (B) menor logo abaixo = SÓ unidades vendidas por bucket
   como BARRAS, eixo X alinhado ao do A.

2. **KPI cards com comparativo vs período anterior** de mesma duração
   (imediatamente antes do período selecionado): Preço médio, Break-even médio,
   MCO R$, Qtd, Receita em %; **MCO % em pontos percentuais (p.p.)**. Sem dados no
   período anterior → "—".

A lógica pura (janela anterior, agregação de KPIs, deltas) vai para o util
`src/lib/precoMcoSeries.ts` com testes (padrão TDD do projeto).

Purpose: separar a leitura de margem/preço da leitura de volume (dois gráficos
mais legíveis) e dar contexto temporal aos KPIs (variação vs período anterior).
Output: dois gráficos + comparativo nos 6 KPIs + 4 funções puras testadas.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Componente alvo, util puro e seus testes
@src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
@src/lib/precoMcoSeries.ts
@src/lib/precoMcoSeries.test.ts

# Card usado nos KPIs (props subtitleNode) e helper de MCO reusado
@src/components/dashboard/KPICard.tsx
@src/lib/mco.ts
</context>

<tasks>

<!-- planner-discipline-allow: yAxisId="qtd" -->

<task type="auto" tdd="true">
  <name>Task 1: Utils puros do comparativo — janela anterior, agregação de KPIs e deltas (TDD)</name>
  <files>src/lib/precoMcoSeries.ts, src/lib/precoMcoSeries.test.ts</files>
  <behavior>
    - computePreviousWindow: dado from="2026-06-08", to="2026-06-14" (7 dias
      inclusivos), retorna { from: "2026-06-01", to: "2026-06-07" } — 7 dias
      imediatamente antes. Um único dia (from===to) → janela anterior de 1 dia
      (o dia anterior). from ou to null → retorna null.
    - computePriceKpis: para o row base (qtd 10, total 1000, cmv 300, comissao
      120, frete 80, impostos 60) com adsDaily spend 40 no mesmo dia e
      incluirAds=true → precoMedio 100, breakevenMedio 60, mco 400, mcoPct 40,
      qtd 10, receita 1000 (reconcilia com a lógica atual do componente). qtd=0
      / total=0 → precoMedio 0, breakevenMedio 0, mcoPct null (sem NaN).
    - percentDelta: (110, 100) → 10; (90, 100) → -10; previous 0 → null.
    - pointDelta: (41.3, 40) → ~1.3; qualquer argumento null → null.
  </behavior>
  <action>
    Todas as adições são em `src/lib/precoMcoSeries.ts` (funções puras novas,
    exportadas) e os testes correspondentes em `src/lib/precoMcoSeries.test.ts`.
    NÃO altere `computePrecoMcoSeries`, `bucketKeyForDate`, as interfaces
    existentes nem a propagação de `qtd` (que o Task 2 usa nas barras).

    (A) Imports date-fns: ao import existente de date-fns no topo do arquivo,
        adicione `differenceInCalendarDays` e `subDays` (ambos existem no pacote;
        já se importa `format`, `parseISO`, `startOfMonth`, `startOfWeek`).

    (B) `computePreviousWindow(from: string | null, to: string | null):
        { from: string; to: string } | null` — computa a janela imediatamente
        anterior de MESMA duração:
        - se `from` ou `to` for falsy, retorne `null`;
        - `start = parseISO(from)`, `end = parseISO(to)`;
        - `durationDays = differenceInCalendarDays(end, start) + 1` (inclusivo);
          se `durationDays <= 0`, retorne `null`;
        - `prevEnd = subDays(start, 1)`; `prevFrom = subDays(prevEnd, durationDays - 1)`;
        - retorne `{ from: format(prevFrom, "yyyy-MM-dd"), to: format(prevEnd, "yyyy-MM-dd") }`.

    (C) `interface PriceKpis { qtd: number; receita: number; precoMedio: number;
        breakevenMedio: number; mco: number; mcoPct: number | null; }` e
        `computePriceKpis(rows: PrecoSeriesRow[], opts: ComputePrecoMcoSeriesOpts):
        PriceKpis` — replica EXATAMENTE a agregação hoje inline no componente
        (bloco `kpis` do PrecoPraticadoReport, exceto os avisos qtdSemCusto /
        temImpostoAusente, que ficam no componente):
        - chame `computePrecoMcoSeries(rows, opts)` para obter a série e some
          `p.ads` de todos os pontos em `adsBucket` (reconcilia ads com o gráfico);
        - some `qtd`, `total`, `cmv`, `comissao`, `frete`, `impostos` das rows;
        - `precoMedio = qtd > 0 ? receita/qtd : 0`;
        - `breakevenMedio = qtd > 0 ? (cmv+comissao+frete+adsBucket+impostos)/qtd : 0`;
        - `{ mco, pct } = computeMco({ grossRevenue: receita, cmv, platformCost:
          comissao + frete, ads: adsBucket, tax: impostos })` (REUSE `computeMco`
          já importado — não duplique a fórmula);
        - retorne `{ qtd, receita, precoMedio, breakevenMedio, mco, mcoPct: pct }`.

    (D) `percentDelta(current: number, previous: number): number | null` —
        `previous === 0 ? null : ((current - previous) / Math.abs(previous)) * 100`.
        O denominador com `Math.abs` mantém o sinal correto quando o valor
        anterior é negativo (ex.: MCO R$ que era negativo).

    (E) `pointDelta(current: number | null, previous: number | null): number | null`
        — se qualquer um for null, retorne `null`; senão `current - previous`
        (usado para o delta de MCO % em pontos percentuais).

    (F) Testes em `src/lib/precoMcoSeries.test.ts`: adicione três novos blocos
        `describe` (reuse o helper `row()` já existente do arquivo):
        - `describe("computePreviousWindow", ...)`: cobre a janela de 7 dias
          (2026-06-08..2026-06-14 → 2026-06-01..2026-06-07), o caso de 1 dia
          (from===to → dia anterior) e o caso `null` (from ou to null → null).
        - `describe("computePriceKpis", ...)`: caso típico com o row base + ads 40
          (precoMedio 100, breakevenMedio 60, mco 400, mcoPct 40 via toBeCloseTo)
          e o caso qtd=0/total=0 (precoMedio 0, breakevenMedio 0, mcoPct null).
        - `describe("percentDelta / pointDelta", ...)`: percentDelta(110,100)=10,
          (90,100)=-10, previous 0 → null; pointDelta(41.3,40)≈1.3 (toBeCloseTo),
          pointDelta(null,40)=null, pointDelta(40,null)=null.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run src/lib/precoMcoSeries.test.ts</automated>
  </verify>
  <done>
    `grep -c "export function computePreviousWindow" src/lib/precoMcoSeries.ts` == 1;
    `grep -c "export function computePriceKpis" src/lib/precoMcoSeries.ts` == 1;
    `grep -c "export function percentDelta" src/lib/precoMcoSeries.ts` == 1;
    `grep -c "export function pointDelta" src/lib/precoMcoSeries.ts` == 1;
    o teste `precoMcoSeries.test.ts` passa incluindo os 3 novos describe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dividir em dois gráficos — principal (sem unidades) + BarChart de unidades alinhado</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <action>
    Todas as mudanças são no bloco `hasData` do render (o gráfico) e nos imports
    do recharts. NÃO toque nos useEffects de dados, nas KPIs nem no util. O
    `chartData` já traz `qtd` por bucket (propagado no quick i8u) — as barras o
    consomem direto, sem buscar nada.

    (A) Import recharts: adicione `BarChart` e `Bar` ao import existente (que já
        traz `ComposedChart, Area, Line, XAxis, YAxis, CartesianGrid, Tooltip as
        RechartsTooltip, ResponsiveContainer, Legend`).

    (B) Gráfico principal (o `ComposedChart` atual) — LIMPAR as adições de
        unidades do quick i8u:
        - REMOVA o terceiro eixo (o `<YAxis>` com `yAxisId="qtd"` e `hide`);
        - REMOVA a `<Line>` de unidades vendidas (a que usa `yAxisId="qtd"` /
          `dataKey="qtd"` com stroke muted fino);
        - No `payload` do `<Legend>`, REMOVA o item de unidades vendidas (o
          objeto cujo `id` é `"qtd"`), deixando 4 itens: Preço praticado,
          Break-even, MCO %, e o item de colchão "Margem". Os demais (Areas
          base/gainBand/lossBand, linhas de preço/break-even/mcoPct) ficam
          intactos. Ao final, NENHUMA referência a `yAxisId="qtd"` deve restar no
          `ComposedChart`.
        - Mantenha a altura do `ResponsiveContainer` do principal em 380 (ou 360)
          e as margens `{ left: 4, right: 8, top: 12, bottom: 0 }` — elas definem
          o alinhamento com o gráfico de barras.

    (C) Gráfico de barras (novo, menor, LOGO ABAIXO do principal, dentro do MESMO
        `<CardContent>`, separado por um espaçamento pequeno ex. `mt-1`):
        - `ResponsiveContainer width="100%" height={140}` envolvendo um
          `<BarChart data={chartData} margin={{ left: 4, right: 8, top: 4, bottom: 0 }}>`
          (mesmas margens laterais do principal para os buckets baterem).
        - `<CartesianGrid strokeDasharray="3 3" stroke="hsl(var(--border))" vertical={false} />`.
        - `<XAxis dataKey="label" fontSize={11} tick={{ fill: "hsl(var(--muted-foreground))" }} tickMargin={8} />`
          (idêntico ao do principal, para o eixo X alinhar).
        - Eixo esquerdo de unidades: `<YAxis fontSize={11} tick={{ fill: "hsl(var(--muted-foreground))" }} allowDecimals={false} width={56} />`
          — `width={56}` casa com o YAxis esquerdo R$/un do principal.
        - Espaçador direito invisível para casar com o eixo MCO% (width 44) do
          principal: `<YAxis orientation="right" width={44} hide />`.
        - Tooltip simples só de unidades: um componente local `BarTooltip`
          (dentro do arquivo, ao lado de `ChartTooltip`) que renderiza o `label`
          e uma linha "Unidades: {intFmt(payload[0].payload.qtd)}" no mesmo estilo
          de card do `ChartTooltip` (reuse `intFmt` já definido). Ligue via
          `<RechartsTooltip content={<BarTooltip />} cursor={{ fill: "hsl(var(--muted-foreground))", fillOpacity: 0.08 }} />`.
        - Barra: `<Bar dataKey="qtd" name="Unidades vendidas" fill="hsl(var(--primary))" fillOpacity={0.55} radius={[3,3,0,0]} isAnimationActive={false} />`.
          Após esta task, `dataKey="qtd"` deve existir EXATAMENTE 1 vez no arquivo
          (só nesta barra — a Line de unidades do principal foi removida).
        - Opcional (legibilidade): um rótulo pequeno acima das barras, ex.
          `<p className="mt-2 mb-0.5 text-[10px] uppercase tracking-wider text-muted-foreground text-center">Unidades vendidas por período</p>`
          antes do `ResponsiveContainer` das barras.

    (D) Estados de loading / vazio: mantenha o comportamento atual (o bloco de
        barras só é renderizado dentro do ramo `hasData`, junto do principal).
        Rodapé de transparência e avisos de dado ausente permanecem como estão.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run && npm run build</automated>
  </verify>
  <done>
    No `PrecoPraticadoReport.tsx`:
    - `grep -c "<BarChart" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 1;
    - `grep -c "<Bar " src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 1;
    - `grep -c 'dataKey="qtd"' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` == 1 (só a barra);
    - `grep -c 'yAxisId="qtd"' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` == 0 (3º eixo e Line de unidades removidos do principal);
    - `grep -c "BarTooltip" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 2 (definição + uso);
    - `npx vitest run` verde e `npm run build` limpo.
  </done>
</task>

<task type="auto">
  <name>Task 3: Comparativo vs período anterior nos KPI cards</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <action>
    Reúna os dados do período anterior (mesma RPC + mesma tabela de ads, janela de
    mesma duração imediatamente antes) e exiba a variação em cada KPI card como
    texto secundário pequeno colorido. Use os utils puros do Task 1.

    (A) Import: de `@/lib/precoMcoSeries`, adicione `computePreviousWindow`,
        `computePriceKpis`, `percentDelta`, `pointDelta` (e o tipo `PriceKpis` se
        precisar anotar). Do `@/lib/utils` já se importa `cn`.

    (B) Estado novo: `const [prevRows, setPrevRows] = useState<PrecoSeriesRow[] | null>(null);`
        e `const [prevAdsDaily, setPrevAdsDaily] = useState<AdsDailyRow[]>([]);`.

    (C) Busca das rows (período atual + anterior em paralelo): no useEffect que
        hoje busca `orders_price_timeseries`, extraia um helper local
        `mapRows(data): PrecoSeriesRow[]` com o mesmo `.map` de hoje (bucket, qtd,
        total, cmv, comissao, frete, qtd_sem_custo, impostos, qtd_sem_imposto).
        Compute `const prev = computePreviousWindow(fromDate, toDate);` e dispare
        as duas chamadas em `Promise.all`: a atual (`_from: fromDate, _to: toDate`)
        e, se `prev` não for null, a anterior (`_from: prev.from, _to: prev.to`) —
        mesmos `_item_id`, `_ml_user_ids`, `_granularity`. Sob erro/cancelamento
        mantenha a mesma proteção `cancelled` de hoje. Set `setRows(mapRows(cur))`
        e `setPrevRows(prev ? mapRows(prv) : null)`. Zere ambos quando `!selectedId`.

    (D) Busca dos ads (período atual + anterior): no useEffect que hoje busca
        `ml_ads_products_cache`, extraia um helper local `mapAds(data): AdsDailyRow[]`
        (filtro `date != null` + map `{ date, spend }` como hoje) e uma função
        local que monta a query para um par (from,to) com `.range(0, 4999)`,
        `.eq("item_id", selectedId)`, `.in("ml_user_id", mlUserIds)` quando houver,
        e `.gte("date", from)/.lte("date", to)`. Rode a atual e, quando
        `computePreviousWindow(fromDate,toDate)` não for null, a anterior em
        `Promise.all`; `setAdsDaily(mapAds(cur))` e `setPrevAdsDaily(prev ? mapAds(prv) : [])`.

    (E) KPIs com util: substitua o cálculo inline de `kpis` por
        `computePriceKpis(rows ?? [], { adsDaily, incluirAds, granularity })`
        espalhado, MANTENDO `qtdSemCusto` e `temImpostoAusente` calculados inline a
        partir de `rows` (esses dois avisos não estão no util). Compute também
        `const prevKpis = useMemo(() => (prevRows && prevRows.length > 0) ?
        computePriceKpis(prevRows, { adsDaily: prevAdsDaily, incluirAds, granularity })
        : null, [prevRows, prevAdsDaily, incluirAds, granularity]);`.

    (F) Deltas: `const deltas = useMemo(() => { if (!prevKpis) return null; return {
        precoMedio: percentDelta(kpis.precoMedio, prevKpis.precoMedio),
        breakevenMedio: percentDelta(kpis.breakevenMedio, prevKpis.breakevenMedio),
        mco: percentDelta(kpis.mco, prevKpis.mco),
        qtd: percentDelta(kpis.qtd, prevKpis.qtd),
        receita: percentDelta(kpis.receita, prevKpis.receita),
        mcoPp: pointDelta(kpis.mcoPct, prevKpis.mcoPct),
      }; }, [kpis, prevKpis]);`.

    (G) Renderização do comparativo: crie um helper local `comparativoNode(delta:
        number | null, unidade: "pct" | "pp", cor: "direcional" | "neutra")` que
        retorna um `ReactNode`:
        - `delta == null` → `<span className="text-[10px] text-muted-foreground">— vs período anterior</span>`;
        - senão formate `sign = delta > 0 ? "+" : ""`; valor =
          `delta.toLocaleString("pt-BR", { maximumFractionDigits: 1 })`; sufixo =
          `unidade === "pp" ? " p.p." : "%"`; texto = `${sign}${valor}${sufixo} vs período anterior`;
        - classe de cor: `cor === "neutra"` → `text-muted-foreground`; senão
          `delta > 0 ? "text-success" : delta < 0 ? "text-destructive" : "text-muted-foreground"`;
        - retorne `<span className={cn("text-[10px]", classeCor)}>{texto}</span>`.
        Passe o resultado via prop `subtitleNode` de cada `KPICard`:
        - Preço médio → `comparativoNode(deltas?.precoMedio ?? null, "pct", "neutra")`
          (aumento de preço é ambíguo — cor neutra com sinal);
        - Break-even médio → `comparativoNode(deltas?.breakevenMedio ?? null, "pct", "neutra")`
          (aumento de break-even é ruim — cor neutra, não verde);
        - MCO (R$) → `comparativoNode(deltas?.mco ?? null, "pct", "direcional")`;
        - MCO % → `comparativoNode(deltas?.mcoPp ?? null, "pp", "direcional")` (p.p.);
        - Qtd vendida → `comparativoNode(deltas?.qtd ?? null, "pct", "direcional")`;
        - Receita → `comparativoNode(deltas?.receita ?? null, "pct", "direcional")`.

    NÃO altere a RPC nem a tabela (ambas já em prod, RLS org-first). Toda a nova
    leitura reusa exatamente os mesmos campos já autorizados, só com outras datas.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run && npm run build</automated>
  </verify>
  <done>
    No `PrecoPraticadoReport.tsx`:
    - `grep -c "computePreviousWindow" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 1;
    - `grep -c "computePriceKpis" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 1;
    - `grep -c "prevKpis" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 1;
    - `grep -c "subtitleNode" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 6 (um por KPI);
    - `grep -c "vs período anterior" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 1;
    - `grep -c "p.p." src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 1;
    - `npx vitest run` verde e `npm run build` limpo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RPC `orders_price_timeseries` / tabela `ml_ads_products_cache` | Já existente. A única mudança é uma SEGUNDA chamada às MESMAS RPC/tabela com datas da janela anterior — nenhum campo, filtro de escopo (item_id + ml_user_id) ou pacote novo. RLS org-first isola. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-jq4-01 | Information Disclosure | 2ª busca (período anterior) via `orders_price_timeseries` e `ml_ads_products_cache` | accept | Mesma RPC SECURITY INVOKER e mesma tabela sob RLS org-first já em prod; mesmos filtros `_item_id`/`_ml_user_ids`/`item_id`/`ml_user_id`; só as datas mudam. Não expõe dado de outra org nem de outro item. |
| T-jq4-02 | Denial of Service | dobro de queries por render | accept | 2 chamadas em `Promise.all` por effect (atual + anterior), disparadas só na troca de anúncio/período/granularidade; `.range(0,4999)` já limita ads. Custo desprezível. |
</threat_model>

<verification>
- `npx vitest run` — suíte inteira verde, incluindo os 3 novos describe de
  `precoMcoSeries.test.ts` (computePreviousWindow, computePriceKpis, percentDelta/pointDelta).
- `npm run build` — build Vite limpo (tsc sem erro; `PriceKpis` e helpers tipados).
- Inspeção visual (checkpoint humano do orquestrador): dois gráficos empilhados e
  alinhados no eixo X; principal sem a linha de unidades; barras de unidades
  abaixo; cada KPI com "±X% vs período anterior" (MCO % em p.p.); "—" quando o
  período anterior não tem dados.
</verification>

<success_criteria>
- Gráfico dividido em dois: `ComposedChart` principal (sem unidades / sem 3º eixo)
  + `BarChart` de unidades alinhado (mesmas margens e larguras de eixo).
- `dataKey="qtd"` aparece só na barra; nenhum `yAxisId="qtd"` restante.
- 6 KPI cards com comparativo vs período anterior (% nos 5, p.p. no MCO %),
  "—" sem dados anteriores.
- 4 funções puras novas testadas; vitest verde + build limpo.
</success_criteria>

<output>
Create `.planning/quick/260702-jq4-grafico-dividido-kpi-comparativo/260702-jq4-SUMMARY.md` when done
</output>
