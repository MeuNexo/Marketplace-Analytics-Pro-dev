---
phase: quick-260618-1sl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mco.ts
  - src/lib/mco.test.ts
  - src/lib/kpi-glossary.ts
  - src/components/mercadolivre/MLMcoStrip.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements: [MCO-STRIP-01]
must_haves:
  truths:
    - "O lojista vê uma faixa slim no topo do /vendas (entre o ConsultorCard e o MLKPIGrid) com o MCO do período em R$ e %."
    - "O marcador ● e o % ficam verdes quando MCO ≥ 0 e vermelhos quando MCO < 0 (tokens kpi.positive/kpi.negative)."
    - "O rótulo é 'MCO do dia' quando o período selecionado é hoje, e 'MCO do período' caso contrário."
    - "O (i) abre um popover (hover + tap) com o breakdown Receita − CMV − Custo Op. − Impostos = MCO."
    - "Com receita = 0 no período a faixa mostra — sem NaN/divisão por zero."
    - "Ads é contabilizado exatamente uma vez (custo_plataforma = frete+comissão, sem ads; ads somado à parte)."
    - "Nenhum KPI do MLKPIGrid é removido ou alterado (mudança 100% aditiva)."
  artifacts:
    - path: "src/lib/mco.ts"
      provides: "Helper puro computeMco({grossRevenue,cmv,platformCost,ads,tax}) => {mco,pct}"
      exports: ["computeMco"]
      min_lines: 15
    - path: "src/lib/mco.test.ts"
      provides: "Teste unitário vitest do computeMco (R$/%, sinal negativo, receita 0 → pct null, ads contado uma vez)"
    - path: "src/components/mercadolivre/MLMcoStrip.tsx"
      provides: "Faixa de apresentação MCO (props: mco, pct, label, loading, empty)"
      exports: ["MLMcoStrip"]
      min_lines: 40
    - path: "src/lib/kpi-glossary.ts"
      provides: "Nova entrada 'mco' no glossário central"
      contains: "mco:"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/lib/mco.ts"
      via: "import computeMco + montagem do input a partir de valores derivados"
      pattern: "computeMco"
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/components/mercadolivre/MLMcoStrip.tsx"
      via: "render <MLMcoStrip> entre ConsultorCard e MLKPIGrid"
      pattern: "MLMcoStrip"
    - from: "src/components/mercadolivre/MLMcoStrip.tsx"
      to: "src/lib/kpi-glossary.ts"
      via: "tooltip do (i) lê KPI_GLOSSARY.mco"
      pattern: "KPI_GLOSSARY"
---

<objective>
Implementar a faixa "MCO do dia" no topo da página `/vendas` (`src/pages/MercadoLivre.tsx`), exatamente conforme o design aprovado em `docs/superpowers/specs/2026-06-18-mco-strip-vendas-design.md`.

A faixa mostra a Margem de Contribuição Operacional (MCO) do período em R$ e %, posicionada entre o `ConsultorCard` e o `MLKPIGrid`. Mudança 100% ADITIVA: nenhum KPI existente do `MLKPIGrid` é removido ou alterado.

Propósito: dar ao lojista o "número-resultado" da operação logo no início da página, reusando valores já derivados (sem novo fetch), com tooltip explicativo de glossário e consistência de cálculo com o DRE/`MLCostCard`.

Output: helper puro `computeMco` (`src/lib/mco.ts`) + teste vitest, componente de apresentação `MLMcoStrip.tsx`, entrada `mco` no glossário central, e o wiring na página.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@docs/superpowers/specs/2026-06-18-mco-strip-vendas-design.md
@CLAUDE.md
@src/pages/MercadoLivre.tsx
@src/components/mercadolivre/MLKPIGrid.tsx
@src/components/mercadolivre/MLCostCard.tsx
@src/components/dashboard/KPICard.tsx
@src/lib/kpi-glossary.ts
@src/hooks/useMLKPISummary.ts
</context>

<artifacts_this_phase_produces>
- `src/lib/mco.ts` — exporta `computeMco(input: McoInput): McoResult`, com tipos `McoInput`/`McoResult`.
- `src/lib/mco.test.ts` — suíte vitest do `computeMco`.
- `src/components/mercadolivre/MLMcoStrip.tsx` — exporta `MLMcoStrip` (componente de apresentação puro).
- `src/lib/kpi-glossary.ts` — nova chave `mco` em `GlossaryKey` + entrada em `KPI_GLOSSARY`.
- `src/pages/MercadoLivre.tsx` — import + montagem do input + render de `<MLMcoStrip>` entre o `ConsultorCard` e o bloco `kpi_grid`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper puro computeMco + teste vitest + entrada 'mco' no glossário</name>
  <files>src/lib/mco.ts, src/lib/mco.test.ts, src/lib/kpi-glossary.ts</files>
  <read_first>
    - src/lib/kpi-glossary.ts (formato de `GlossaryKey` união de strings + `KPI_GLOSSARY` Record; entradas `custo_operacional`/`margem_operacional` como modelo de redação leiga)
    - src/components/mercadolivre/MLCostCard.tsx (linhas 67–74: `lucro = receitaMes − totalTarifas − (cmvMes ?? 0) − (impostosMes ?? 0)`; o helper deve reconciliar com este cálculo, mas separando ads do platformCost)
    - vitest.config.ts (já lido: globals:true, jsdom, include src/**/*.test.ts — `describe/it/expect` globais disponíveis)
  </read_first>
  <behavior>
    - computeMco({ grossRevenue: 10000, cmv: 3000, platformCost: 2000, ads: 1000, tax: 500 }) → { mco: 3500, pct: 35 }
    - Sinal negativo: custos > receita → mco < 0 e pct < 0 (ex: grossRevenue 1000, cmv 800, platformCost 300, ads 100, tax 50 → mco -250, pct -25).
    - grossRevenue = 0 → pct = null (sem NaN, sem divisão por zero); mco = -(cmv+platformCost+ads+tax).
    - Ads contado exatamente uma vez: dois inputs idênticos exceto ads (0 vs N) diferem no mco exatamente por N — nunca 2N.
    - pct usa grossRevenue como base: pct = grossRevenue > 0 ? (mco/grossRevenue)*100 : null.
  </behavior>
  <action>
    Crie `src/lib/mco.ts` com tipos e helper puro (sem React, sem imports de runtime):
    - `export interface McoInput { grossRevenue: number; cmv: number; platformCost: number; ads: number; tax: number; }`
    - `export interface McoResult { mco: number; pct: number | null; }`
    - `export function computeMco(input: McoInput): McoResult` retornando `mco = grossRevenue - cmv - platformCost - ads - tax` e `pct = grossRevenue > 0 ? (mco / grossRevenue) * 100 : null`.
    Documente em comentário (1 linha) que `platformCost` é frete+comissão SEM ads, e que ads é subtraído exatamente uma vez — reconciliando com o "Lucro do mês" do MLCostCard (que já trata PADS dentro das tarifas).

    Crie `src/lib/mco.test.ts` cobrindo os 4 casos do bloco <behavior> usando `describe`/`it`/`expect` globais (vitest globals já habilitado). Inclua um caso que isola a contabilização única de ads (diff exato = N).

    Edite `src/lib/kpi-glossary.ts`: adicione `"mco"` na união `GlossaryKey` (após `margem_pos_ads`) e a entrada `mco` em `KPI_GLOSSARY` com `term: "MCO"`, `definition` em linguagem leiga (ex: "O que sobra da receita do período depois de descontar o custo do produto, as taxas do Mercado Livre, a publicidade e os impostos.") e `example` com o breakdown "Receita − CMV − Custo Operacional − Impostos = MCO". Texto plano (sem HTML).
  </action>
  <verify>
    <automated>npx vitest run src/lib/mco.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/mco.test.ts` passa com todos os casos verdes.
    - `npx tsc --noEmit` não acusa erro novo (tipos exportados corretos; `mco` é membro válido de `GlossaryKey`).
    - `grep -q "mco:" src/lib/kpi-glossary.ts` e a união `GlossaryKey` contém `"mco"`.
  </acceptance_criteria>
  <done>computeMco existe, testado e verde; entrada `mco` no glossário com breakdown; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Componente de apresentação MLMcoStrip (faixa slim + popover glossário)</name>
  <files>src/components/mercadolivre/MLMcoStrip.tsx</files>
  <read_first>
    - src/components/dashboard/KPICard.tsx (linhas 95–119: padrão Popover + PopoverTrigger/PopoverContent com `open`/`onOpenChange`, `onMouseEnter/onMouseLeave` para hover, `onClick stopPropagation` para tap, `HelpCircle` da lucide — REUSAR este padrão exato para o (i))
    - src/components/mercadolivre/MLCostCard.tsx (uso de `text-kpi-positive`/`text-kpi-negative`, `tabular-nums`, motion wrapper opcional, `Card`/`CardContent` — para estilo consistente)
    - src/lib/kpi-glossary.ts (após Task 1 ter a entrada `mco`; ler `KPI_GLOSSARY.mco.definition`/`.example` para o popover)
    - CLAUDE.md (named exports; props interface inline acima do componente; design tokens via classes Tailwind; TS strict)
  </read_first>
  <action>
    Crie `src/components/mercadolivre/MLMcoStrip.tsx` como componente de apresentação PURO (named export `MLMcoStrip`, sem fetch, sem lógica de cálculo — recebe valores prontos via props).

    Interface inline acima do componente:
    `interface MLMcoStripProps { mco: number; pct: number | null; label: string; loading?: boolean; empty?: boolean; }`
    onde `label` é "MCO do dia" ou "MCO do período" (decidido pelo pai), e `empty` indica receita = 0 no período.

    Layout: faixa slim de largura total (uma linha), conteúdo `● MCO do dia · R$ X · Y% · (i)`:
    - `●` (marcador) e o `%` usam `text-kpi-positive` quando `mco >= 0`, senão `text-kpi-negative`. Demais textos em `text-foreground`/`text-muted-foreground` (neutro).
    - Formate R$ com `toLocaleString("pt-BR", { style: "currency", currency: "BRL" })` e % com `pct.toFixed(1)` + "%". Use `tabular-nums`.
    - Separadores `·` em `text-muted-foreground`.
    - Estado `empty` (ou `pct == null`): mostre `—` no lugar de R$ e %, marcador em neutro, sem divisão por zero.
    - Estado `loading`: skeleton slim (ex: `<div className="h-6 ... animate-pulse" />`) na largura da faixa.
    - Responsivo: tudo em uma linha que colapsa graciosamente em mobile (use `flex items-center gap-2 flex-wrap` e tamanhos `text-sm`); sem tabela.

    `(i)`: REUSE o padrão exato do KPICard (linhas 95–119): `Popover` controlado por `useState` (`open`/`onOpenChange`), `PopoverTrigger asChild` com `<button>` contendo `<HelpCircle className="w-3.5 h-3.5" />`, `onMouseEnter`/`onMouseLeave` para hover, `onClick` com `stopPropagation` + toggle para tap, `aria-label="Ver definição"`. `PopoverContent side="top" align="start"` exibindo `KPI_GLOSSARY.mco.definition` + (em nova linha) o breakdown `Receita − CMV − Custo Op. − Impostos = MCO` (de `KPI_GLOSSARY.mco.example`). Renderize como texto plano — NUNCA `dangerouslySetInnerHTML`.

    Não introduza dark-mode toggle (fora de escopo). Não remova nem dependa de nenhum KPI do MLKPIGrid.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "MLMcoStrip" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/mercadolivre/MLMcoStrip.tsx` existe com `export function MLMcoStrip`.
    - Usa Popover/PopoverTrigger/PopoverContent de `@/components/ui/popover` e `HelpCircle` de lucide-react (mesmo padrão do KPICard).
    - Aplica `text-kpi-positive`/`text-kpi-negative` no `●` e no `%` conforme sinal do mco.
    - Trata `empty`/`pct == null` mostrando `—` sem NaN.
    - Sem `dangerouslySetInnerHTML`. `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>MLMcoStrip renderiza a faixa com cor por sinal, popover de glossário e estados loading/empty, sem alterar KPIs existentes.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring em MercadoLivre.tsx — montar input do computeMco e renderizar a faixa</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <read_first>
    - src/pages/MercadoLivre.tsx (já lido): valores derivados disponíveis — `kpiSummary` (gross_revenue, custo_plataforma=frete+comissão, total_tax, has_tax_data, cmv, cmv_has_cost), `adsSummary.total_spend` (=`adsTotalForPeriod`), `monthlyCostWaterfall` (base do fallback por % mensal), `currentFrom`/`currentTo`, `filters.singleDayRange`. Render: ConsultorCard nas linhas 652–661; bloco `widget.id === "kpi_grid"` nas linhas 676–688.
    - src/hooks/useMLFilters.ts (já lido): `todayUTC()` import já presente na página (linha 14); "hoje" = `currentFrom === currentTo && currentTo === todayUTC()` (ou `singleDayRange === todayUTC()` / `period === 0`).
    - src/components/mercadolivre/MLCostCard.tsx (filosofia de fallback: quando CMV/imposto do período ausentes, estimar por % do waterfall mensal aplicado à receita do período — manter consistência com o DRE).
  </read_first>
  <action>
    Em `src/pages/MercadoLivre.tsx`:

    1. Importe `MLMcoStrip` de `@/components/mercadolivre/MLMcoStrip` e `computeMco` de `@/lib/mco` (siga a ordem de imports existente).

    2. Monte o input do MCO via `useMemo` (depende de `kpiSummary`, `adsSummary.total_spend`, `monthlyCostWaterfall`):
       - `grossRevenue = kpiSummary?.gross_revenue ?? 0`
       - `platformCost = kpiSummary?.custo_plataforma ?? 0` (frete+comissão, JÁ exclui ads — confirmado em useMLKPISummary)
       - `ads = adsSummary.total_spend` (= `adsTotalForPeriod`; somado UMA vez)
       - `cmv`: usar `kpiSummary.cmv` quando `kpiSummary.cmv_has_cost`; senão FALLBACK por % mensal = `grossRevenue * (cmvMensal / receitaMensal)` usando `monthlyCostWaterfall` (`has_cmv ? cmv : 0` / `paid_revenue`), com guarda de receita mensal > 0; senão 0. Mesma fonte/fallback que o MLCostCard usa.
       - `tax`: usar `kpiSummary.total_tax` quando `kpiSummary.has_tax_data`; senão FALLBACK por % mensal análogo via `monthlyCostWaterfall` (`has_tax_data ? total_tax : 0` / `paid_revenue`); senão 0.
       Comente que ads entra exatamente uma vez e que platformCost não inclui ads (anti-duplicação).

    3. Calcule `const { mco, pct } = computeMco(mcoInput)` (useMemo).

    4. Rótulo dinâmico via `useMemo`: `isToday = (filters.singleDayRange ? filters.singleDayRange === todayUTC() : (currentFrom === currentTo && currentTo === todayUTC()))`; `const mcoLabel = isToday ? "MCO do dia" : "MCO do período"`.

    5. Estado empty: `const mcoEmpty = (kpiSummary?.gross_revenue ?? 0) <= 0`. Loading: reuse `kpiSummaryLoading || isRecalcing || effectiveLoading`.

    6. Renderize `<MLMcoStrip mco={mco} pct={pct} label={mcoLabel} loading={...} empty={mcoEmpty} />` EXATAMENTE entre o bloco do `ConsultorCard` (fecha na linha ~661) e o início do `widgets.map(...)` que renderiza o `kpi_grid` — ou seja, após o `{connected && (<ConsultorCard .../>)}` e antes do bloco de "Nenhum dado no cache"/`widgets.map`. A faixa deve aparecer apenas quando `connected` (mesma condição do conteúdo de dados). NÃO toque na renderização do `MLKPIGrid` nem em nenhuma prop dele.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpo e `npm run build` conclui sem erro.
    - `<MLMcoStrip` aparece exatamente uma vez, posicionado entre o ConsultorCard e o `MLKPIGrid` (grep de ordem no arquivo).
    - O input do computeMco usa `kpiSummary.custo_plataforma` (sem ads) + `adsSummary.total_spend` (uma vez) — nenhuma duplicação de ads.
    - O `MLKPIGrid` e suas props permanecem inalterados (diff não altera o bloco `kpi_grid`).
    - Rótulo alterna entre "MCO do dia" (hoje) e "MCO do período".
  </acceptance_criteria>
  <done>A faixa MCO renderiza entre o ConsultorCard e o MLKPIGrid no /vendas, com cálculo reusando valores derivados, rótulo dinâmico e fallback consistente com o DRE; build e tsc limpos; nenhum KPI removido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova fronteira de confiança é introduzida. A faixa consome valores já derivados na página (`kpiSummary`, `adsSummary`, `monthlyCostWaterfall`) — provenientes de fluxos existentes (RPC `get_kpi_summary` e queries já em produção), sem novo fetch, sem nova entrada de usuário, sem nova edge function.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-mco-01 | Information Disclosure | MLMcoStrip / kpi-glossary | accept | Apenas re-exibe agregados financeiros já mostrados em outros cards do /vendas (mesma RLS org-first). Nenhum dado novo exposto. |
| T-mco-02 | Tampering (XSS) | MLMcoStrip popover / glossário | mitigate | Todo texto (faixa + glossário) renderizado como texto plano via JSX; proibido `dangerouslySetInnerHTML`. Sem risco novo de injeção. |

Sem instalação de pacotes (npm/pip/cargo) — não se aplica o gate de legitimidade de pacote nem `T-{phase}-SC`.
</threat_model>

<verification>
- `npx vitest run src/lib/mco.test.ts` — todos os casos do computeMco verdes.
- `npx tsc --noEmit` — sem erros de tipo (inclui `mco` como `GlossaryKey` válido).
- `npm run build` — build de produção conclui sem erro.
- Inspeção: `<MLMcoStrip>` posicionado entre o ConsultorCard e o `MLKPIGrid`; bloco `kpi_grid` inalterado.
- Anti-duplicação de ads: `platformCost` = `kpiSummary.custo_plataforma` (frete+comissão, sem ads) + `ads` somado uma única vez.
- Checkpoint visual de Wesley (preview Vercel): faixa renderiza, cor por sinal, (i) abre breakdown, segue o seletor de período (rótulo "do dia"/"do período").
</verification>

<success_criteria>
- Faixa "MCO do dia/período" visível no topo do /vendas, entre ConsultorCard e MLKPIGrid, em R$ e %.
- `●` e `%` coloridos por sinal (kpi.positive/negative).
- (i) hover+tap abre breakdown Receita − CMV − Custo Op. − Impostos = MCO.
- receita = 0 → `—`, sem NaN.
- ads contado exatamente uma vez; reconcilia com "Lucro do mês" do MLCostCard.
- 100% aditivo: nenhum KPI do MLKPIGrid removido/alterado.
- tsc, build e vitest limpos.
</success_criteria>

<output>
Create `.planning/quick/260618-1sl-faixa-mco-do-dia-no-vendas/260618-1sl-SUMMARY.md` when done.
</output>
