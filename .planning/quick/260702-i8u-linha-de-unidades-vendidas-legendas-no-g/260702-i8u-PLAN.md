---
phase: quick-260702-i8u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/precoMcoSeries.ts
  - src/lib/precoMcoSeries.test.ts
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
autonomous: true
requirements: [QUICK-260702-i8u]
must_haves:
  truths:
    - "O gráfico de /analise-precos mostra uma linha discreta de unidades vendidas por bucket"
    - "O gráfico tem uma legenda que identifica cada série visível (preço, break-even, MCO %, unidades) e o colchão como 'Margem'"
    - "A legenda NÃO lista as Areas técnicas (base transparente, gainBand, lossBand) como itens separados"
    - "O tooltip mostra a quantidade de unidades vendidas do bucket"
    - "Suíte vitest verde e build limpo"
  artifacts:
    - path: src/lib/precoMcoSeries.ts
      provides: "campo qtd propagado no McoSeriesPoint"
      contains: "qtd: r.qtd"
    - path: src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
      provides: "linha de unidades (3º eixo Y oculto) + Legend filtrada + tooltip com unidades"
      contains: "yAxisId=\"qtd\""
  key_links:
    - from: src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
      to: src/lib/precoMcoSeries.ts
      via: "chartData[].qtd consumido pela Line yAxisId=qtd e pelo tooltip"
      pattern: "dataKey=\"qtd\""
---

<objective>
Feedback do Wesley pós-preview da Phase 79 no gráfico de `/analise-precos`
(`PrecoPraticadoReport.tsx`): (1) adicionar uma linha de **unidades vendidas por
bucket** usando um TERCEIRO eixo Y oculto para não poluir os dois eixos visíveis
(R$/un à esquerda, MCO % à direita); (2) adicionar uma **legenda** do recharts que
identifica as séries visíveis, FILTRANDO as Areas técnicas do colchão (base /
gainBand / lossBand) para aparecerem como um único item "Margem". Atualizar o
tooltip para incluir as unidades. Manter paridade mobile (legenda com wrap).

Purpose: tornar o gráfico auto-explicativo (legenda) e adicionar a leitura de
volume por bucket sem sobrecarregar os eixos existentes.
Output: linha de unidades discreta + Legend custom + tooltip com unidades, com
o campo `qtd` propagado do util para o ponto da série.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Componente a modificar e o util que alimenta a série
@src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
@src/lib/precoMcoSeries.ts
@src/lib/precoMcoSeries.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagar `qtd` do PrecoSeriesRow para o McoSeriesPoint (pré-requisito de dados)</name>
  <files>src/lib/precoMcoSeries.ts, src/lib/precoMcoSeries.test.ts</files>
  <behavior>
    - Dado um row com `qtd: 10`, o McoSeriesPoint resultante deve ter `qtd === 10`.
    - Os campos já existentes (precoUnit, breakevenUnit, ads, mco, mcoPct, base,
      gainBand, lossBand, custoAusente, impostoAusente) permanecem inalterados.
  </behavior>
  <action>
    O campo `qtd` HOJE NÃO existe no output do util: a interface `McoSeriesPoint`
    não o declara e o `.map((r) => {...})` de `computePrecoMcoSeries` não o inclui
    (só usa `const qtd = r.qtd` internamente para as divisões). A Task 2 precisa de
    `qtd` por bucket no `chartData`, então propague-o aqui.

    1. Na interface `McoSeriesPoint` (arquivo `src/lib/precoMcoSeries.ts`),
       adicione o campo `qtd: number;` — documente como "unidades vendidas no
       bucket (r.qtd)". Coloque-o logo após `bucket` para leitura natural.
    2. No objeto retornado pelo `.map((r) => ({ ... }))`, adicione `qtd: r.qtd,`
       (a variável local `qtd` já é `r.qtd`; use `r.qtd` explicitamente para
       clareza). Nenhuma outra linha muda.
    3. No teste `src/lib/precoMcoSeries.test.ts`, dentro do caso que já valida o
       ponto base ("qtd 10, total 1000, ..."), adicione uma asserção
       `expect(p.qtd).toBe(10);` provando a propagação. O teste usa asserções
       por-campo (`toBe`/`toBeCloseTo`), NÃO `toEqual` estrito, então adicionar o
       campo é seguro e não quebra os demais casos.

    Não altere a fórmula de MCO, as bandas do colchão, nem a bucketização de ads.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run src/lib/precoMcoSeries.test.ts</automated>
  </verify>
  <done>
    `grep -n "qtd: r.qtd" src/lib/precoMcoSeries.ts` retorna 1 linha; a interface
    `McoSeriesPoint` declara `qtd: number`; o teste de precoMcoSeries passa incluindo
    a nova asserção `expect(p.qtd).toBe(10)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Linha de unidades (3º eixo Y oculto) + Legend filtrada + tooltip com unidades</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <action>
    Todas as mudanças são no `ComposedChart` (bloco `hasData` renderizado) e no
    componente `ChartTooltip`. O `chartData` já será `McoSeriesPoint & { label }`
    com `qtd` após a Task 1 — nada a buscar do backend.

    (A) Import: adicione `Legend` ao import do recharts (linha que já importa
        `ComposedChart, Area, Line, XAxis, YAxis, CartesianGrid, Tooltip as
        RechartsTooltip, ResponsiveContainer`).

    (B) Terceiro eixo Y OCULTO para as unidades — coloque logo após o YAxis
        `yAxisId="mco"`: um `<YAxis yAxisId="qtd" hide />`. `hide` garante que a
        escala das unidades NÃO desenha ticks nem eixo (os dois eixos visíveis
        R$/un e MCO % permanecem intocados).

    (C) Linha de unidades DISCRETA — adicione uma `<Line>` (após a linha do
        `mcoPct`, para ficar por cima ou por baixo conforme z-order desejado;
        pode ficar como última série). Props:
        - `yAxisId="qtd"`, `dataKey="qtd"`, `name="Unidades vendidas"`
        - `type="monotone"`
        - `stroke="hsl(var(--muted-foreground))"`, `strokeWidth={1}`,
          `strokeOpacity={0.5}` (estilo discreto — cor muted, traço fino)
        - `dot={false}`, sem `strokeDasharray` (para NÃO confundir com a linha
          tracejada de break-even, que já usa muted-foreground dashed).

    (D) Legenda do recharts FILTRADA — adicione um `<Legend>` como filho do
        `ComposedChart` (antes ou depois das séries; a ordem no JSX não afeta a
        legenda quando `payload` é explícito). Use `payload` CUSTOM para listar SÓ
        os itens desejados e excluir as Areas técnicas (base/gainBand/lossBand):
        - `verticalAlign="bottom"`
        - `wrapperStyle={{ fontSize: 11, paddingTop: 8 }}` (wrap responsivo p/
          paridade mobile — a legenda quebra em várias linhas quando estreita)
        - `payload={[...]}` com exatamente 5 entradas:
          1. `{ value: "Preço praticado", type: "line", id: "precoUnit", color: "hsl(var(--accent))" }`
          2. `{ value: "Break-even", type: "line", id: "breakevenUnit", color: "hsl(var(--muted-foreground))" }`
          3. `{ value: "MCO %", type: "line", id: "mcoPct", color: "hsl(var(--primary))" }`
          4. `{ value: "Unidades vendidas", type: "line", id: "qtd", color: "hsl(var(--muted-foreground))" }`
          5. `{ value: "Margem (verde=positiva, vermelho=negativa)", type: "rect", id: "band", color: "hsl(var(--success))" }`
        O `payload` explícito é a forma determinística de esconder as 3 Areas do
        colchão da legenda e representá-las como um único item "Margem".

        Ajuste o espaço vertical: a legenda ocupa a base do gráfico. Aumente a
        altura do `ResponsiveContainer` de `340` para `380` (ou adicione
        `bottom: 8` no `margin` do ComposedChart) para a legenda não sobrepor o
        eixo X. Escolha o que mantiver o plot legível.

    (E) Tooltip com unidades — em `ChartTooltip`, o objeto `d` agora tem `d.qtd`
        (o tipo já é `McoSeriesPoint & { label }`). Adicione uma linha
        `<Row k="Unidades" v={intFmt(d.qtd)} />` logo após o cabeçalho `{d.label}`
        e ANTES de "Preço" (ou entre "MCO %" e o bloco "Por unidade" — escolha o
        que ler melhor). Reuse o helper `intFmt` já definido no arquivo.

    NÃO toque na lógica de dados (useEffect/RPC/util), nas KPIs, nos avisos de
    dado ausente, nem no rodapé de transparência — o rodapé pode permanecer como
    está (a Legend passa a ser a identificação primária das séries).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run && npm run build</automated>
  </verify>
  <done>
    No `PrecoPraticadoReport.tsx`:
    - `grep -c "yAxisId=\"qtd\"" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 2 (YAxis oculto + Line);
    - `grep -n "<YAxis yAxisId=\"qtd\" hide" ...` retorna 1;
    - `grep -n "Legend" ...` retorna o import E o uso (>= 2 ocorrências);
    - `grep -n "Unidades vendidas" ...` retorna 1 (na Legend);
    - `grep -n "Margem (verde=positiva" ...` retorna 1 (item de colchão na Legend);
    - `grep -n "intFmt(d.qtd)" ...` retorna 1 (tooltip com unidades);
    - `npx vitest run` verde e `npm run build` limpo (tsc sem erro).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Mudança puramente presencial (render client-side de dados já autorizados via RLS org-first pela RPC `orders_price_timeseries` e `ml_ads_products_cache`). Sem nova entrada de usuário, sem nova query, sem novo pacote. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-i8u-01 | Information Disclosure | Legend/tooltip de /analise-precos | accept | Renderiza apenas dados já buscados e já exibidos no mesmo gráfico (preço/break-even/MCO/unidades do próprio anúncio, sob RLS org-first). Nenhum dado novo é exposto. |
</threat_model>

<verification>
- `npx vitest run` — suíte inteira verde (inclui precoMcoSeries.test.ts com a nova asserção de `qtd`).
- `npm run build` — build Vite limpo (tsc sem erros de tipo; `d.qtd` tipado via McoSeriesPoint).
- Inspeção visual (checkpoint humano do orquestrador, fora deste plano): linha discreta de unidades visível, legenda com 5 itens (sem as Areas técnicas), tooltip mostrando "Unidades", wrap correto no mobile.
</verification>

<success_criteria>
- Campo `qtd` propagado no `McoSeriesPoint` e consumido pelo `chartData`.
- Linha de unidades desenhada em 3º eixo Y oculto (`yAxisId="qtd" hide`), estilo muted/fino.
- `<Legend>` com payload custom de 5 itens; Areas base/gainBand/lossBand ausentes da legenda.
- Tooltip inclui as unidades do bucket.
- vitest verde + build limpo.
</success_criteria>

<output>
Create `.planning/quick/260702-i8u-linha-de-unidades-vendidas-legendas-no-g/260702-i8u-SUMMARY.md` when done
</output>
