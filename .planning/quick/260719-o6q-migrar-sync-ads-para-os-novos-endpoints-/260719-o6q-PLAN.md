---
phase: quick-260719-o6q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sync-ads/index.ts
autonomous: true
requirements:
  - ADS-MIGRATE-01
must_haves:
  truths:
    - "sync-ads chama os novos endpoints Product Ads do ML (path com {SITE_ID}) e não retorna mais 404"
    - "site_id é capturado da resposta de /advertising/advertisers?product_id=PADS e usado na montagem das URLs"
    - "campanhas leem o campo budget novo com fallback para nomes antigos"
  artifacts:
    - path: "supabase/functions/sync-ads/index.ts"
      provides: "sync-ads apontando para os endpoints /advertising/{SITE_ID}/advertisers/{ADVERTISER_ID}/product_ads/ads/search e /product_ads/campaigns/search"
      contains: "product_ads/ads/search"
  key_links:
    - from: "supabase/functions/sync-ads/index.ts (syncUser)"
      to: "api.mercadolibre.com/advertising/{SITE_ID}/advertisers/.../product_ads/ads/search"
      via: "mlGet com URL construída a partir de siteId + advertiserId"
      pattern: "product_ads/ads/search"
---

<objective>
Migrar `supabase/functions/sync-ads/index.ts` dos endpoints Product Ads do ML descontinuados em 26/02/2026 (retornam 404) para os novos endpoints oficiais, capturando o `site_id` do advertiser e ajustando as URLs de items e campanhas.

Purpose: O endpoint antigo `/advertising/advertisers/{id}/product_ads/items` foi permanentemente descontinuado pelo ML e retorna 404; a quick task anterior (260719-nov) parou de mascarar a falha, expondo o erro real. Sem a migração, o cache de ads fica sem dados novos.
Output: Edge function `sync-ads` atualizada e deployada em prod (`ckcdevcxgvueywivefgx`) preservando `verify_jwt=false`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@supabase/functions/sync-ads/index.ts

# Escopo é SÓ este arquivo. `normalizeMetrics()`, a lógica de paginação e os
# nomes dos campos de métrica extraídos (m.prints, m.clicks, m.cost,
# m.units_quantity, m.total_amount, m.direct_amount) JÁ são compatíveis com o
# schema novo — NÃO alterar. Não mexer em process-sync-job (já corrigido na
# quick anterior). Sem suíte de teste Deno neste projeto.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar URLs do sync-ads para os novos endpoints Product Ads (+ capturar site_id)</name>
  <files>supabase/functions/sync-ads/index.ts</files>
  <action>
Na função `syncUser` (por volta da linha 163), a chamada `mlGet(ML_API + "/advertising/advertisers?product_id=PADS", token)` já retorna `advData`. Além do `advertiser_id` já capturado em `advData?.advertisers?.[0]?.advertiser_id`, capturar também o site_id numa constante `siteId = advData?.advertisers?.[0]?.site_id`. Logo após o guard `if (!advertiserId) throw new Error("No advertiser_id for ml_user_id=" + mlUserId);`, adicionar um guard idêntico para o site_id: `if (!siteId) throw new Error("No site_id for ml_user_id=" + mlUserId);`.

Migração de URL 1 — busca de items (por volta da linha 191-194). Trocar o path descontinuado
`ML_API + "/advertising/advertisers/" + advertiserId + "/product_ads/items?" + itemsQs`
para o novo
`ML_API + "/advertising/" + siteId + "/advertisers/" + advertiserId + "/product_ads/ads/search?" + itemsQs`.
Manter `itemsQs` (o `URLSearchParams` com date_from/date_to/metrics/metrics_summary/limit/offset) exatamente como está — os query params não mudam.

Migração de URL 2 — busca de campanhas (por volta da linha 285-288). Trocar
`ML_API + "/advertising/advertisers/" + advertiserId + "/product_ads/campaigns?limit=50&offset=" + campOff`
para
`ML_API + "/advertising/" + siteId + "/advertisers/" + advertiserId + "/product_ads/campaigns/search?limit=50&offset=" + campOff`.
Não adicionar date_from/date_to/metrics (não pedimos métrica de campanha).

Ajuste de campo de budget (por volta da linha 302). O novo endpoint expõe o orçamento no campo `budget` (não mais `budget_amount`/`daily_budget`). Alterar o mapeamento de `campRows` para incluir `c.budget` como primeira opção do fallback: `daily_budget: Number(c.budget ?? c.budget_amount ?? c.daily_budget ?? 0)`.

NÃO alterar: `normalizeMetrics()`, a extração de métricas (m.prints/m.clicks/m.cost/m.units_quantity/m.total_amount/m.direct_amount), a lógica de paginação (`offset += items.length; if (items.length === 0 || offset >= total) break;`), nem o mapeamento de `campaign_id: String(c.id ?? c.campaign_id ?? "")` — `c.id` já é o campo novo e já funciona.
  </action>
  <verify>
    <automated>grep -n "product_ads/ads/search" supabase/functions/sync-ads/index.ts && grep -n "product_ads/campaigns/search" supabase/functions/sync-ads/index.ts && grep -n "site_id" supabase/functions/sync-ads/index.ts && grep -c "product_ads/items" supabase/functions/sync-ads/index.ts | grep -qx 0 && echo OK</automated>
  </verify>
  <done>
Arquivo usa os novos paths `/advertising/{siteId}/advertisers/{advertiserId}/product_ads/ads/search` e `/product_ads/campaigns/search`; `siteId` é capturado de `advData.advertisers[0].site_id` com guard de erro; `daily_budget` usa `c.budget` como primeira opção; nenhuma referência restante a `product_ads/items` (path antigo). `normalizeMetrics`, paginação e extração de métricas inalterados.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deployar sync-ads em prod preservando verify_jwt=false</name>
  <files>supabase/functions/sync-ads/index.ts</files>
  <action>
Deployar a função `sync-ads` atualizada no projeto Supabase `ckcdevcxgvueywivefgx` via MCP Supabase (`deploy_edge_function`), preservando `verify_jwt=false` (a função usa `requireServiceRole` interno, não JWT do Supabase).

Se a ferramenta MCP Supabase NÃO estiver acessível ao executor (executor gsd sem MCP de deploy), NÃO tentar contornar: documentar no SUMMARY.md a pendência de deploy manual com o comando/parâmetros exatos (projeto `ckcdevcxgvueywivefgx`, função `sync-ads`, `verify_jwt=false`) para o orquestrador executar o deploy via MCP/CLI com token.
  </action>
  <verify>
    <automated>MISSING — deploy via MCP não tem verificação automatizada local; confirmar no SUMMARY.md se o deploy foi executado ou registrado como pendência para o orquestrador</automated>
  </verify>
  <done>
Função `sync-ads` deployada em `ckcdevcxgvueywivefgx` com `verify_jwt=false` E confirmada via resposta do MCP; OU, se MCP indisponível, pendência de deploy manual documentada no SUMMARY.md com projeto/função/verify_jwt.
  </done>
</task>

</tasks>

<verification>
- `grep` confirma novos paths presentes e path antigo `product_ads/items` ausente
- `site_id` capturado e com guard de erro
- Deploy executado em prod OU pendência documentada no SUMMARY.md
</verification>

<success_criteria>
sync-ads chama os endpoints novos de Product Ads (não retorna mais 404), captura o `site_id` do advertiser, lê `budget` das campanhas, e a função está deployada em `ckcdevcxgvueywivefgx` (ou a pendência de deploy está registrada para o orquestrador).
</success_criteria>

<output>
Create `.planning/quick/260719-o6q-migrar-sync-ads-para-os-novos-endpoints-/260719-o6q-SUMMARY.md` when done
</output>
