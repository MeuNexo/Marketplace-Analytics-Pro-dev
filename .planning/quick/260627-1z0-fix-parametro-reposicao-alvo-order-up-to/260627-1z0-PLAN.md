---
phase: quick-260627-1z0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260668000300_get_replenishment_by_sku_alvo_order_up_to.sql
autonomous: true
requirements: [REPL-ALVO-01]
must_haves:
  truths:
    - "O alvo (order-up-to) de cada SKU nunca é menor que o ponto de reposição: alvo usa GREATEST(meta_cobertura_dias, lead_time_dias + 7) no termo de dias."
    - "O cálculo de compra usa o MESMO termo de dias do alvo (coerência alvo↔compra)."
    - "Nada além do alvo/compra mudou em relação à 20260668000200 (ponto, gatilho, venda_dia, motor, colunas, SECURITY INVOKER)."
  artifacts:
    - path: "supabase/migrations/20260668000300_get_replenishment_by_sku_alvo_order_up_to.sql"
      provides: "CREATE OR REPLACE de get_replenishment_by_sku com alvo order-up-to = lead_time + ciclo de revisão (7) + safety"
      contains: "GREATEST(b.meta_cobertura_dias, b.lead_time_dias + 7)"
  key_links: []
---

<objective>
Corrigir o parâmetro de reposição na RPC `get_replenishment_by_sku`: o alvo (order-up-to) deve ser no mínimo `lead_time + ciclo de revisão (7 dias) + safety`. Hoje, quando `meta_cobertura_dias` (ex.: 45) é menor que `lead_time_dias` (70 Pralana, até 85 em outras), o alvo cai abaixo do `ponto_reposicao` → quando estoque+a_caminho fica entre o alvo e o ponto, o gatilho dispara mas a compra colapsa no MOQ.

Purpose: garantir que a compra sugerida reponha o suficiente para cobrir o lead time + o próximo ciclo de revisão, eliminando o caso degenerado alvo < ponto.

Output: UMA migration nova que faz CREATE OR REPLACE da função idêntica à `20260668000200`, mudando APENAS o termo de dias do `alvo` (e do `compra`, para mantê-los coerentes).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@supabase/migrations/20260668000200_get_replenishment_by_sku_engine_max_confidence.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration do alvo order-up-to (lead_time + 7 + safety)</name>
  <files>supabase/migrations/20260668000300_get_replenishment_by_sku_alvo_order_up_to.sql</files>
  <action>
Crie o arquivo `supabase/migrations/20260668000300_get_replenishment_by_sku_alvo_order_up_to.sql` copiando INTEGRALMENTE o conteúdo de `20260668000200_get_replenishment_by_sku_engine_max_confidence.sql` (mesmo DROP FUNCTION da assinatura 4-arg `(UUID, INTEGER, NUMERIC, BOOLEAN)`, mesmo CREATE OR REPLACE, mesma RETURNS TABLE com as 35 colunas incluindo `venda_simples`/`venda_inteligente`, mesmo corpo, mesmo SECURITY INVOKER, mesmo SET search_path='public', mesmo `#variable_conflict use_column`, e os mesmos REVOKE/GRANT finais).

A ÚNICA mudança é no CTE `calc`, no termo de dias do alvo, em DOIS pontos:

1. Linha do `alvo_v`: trocar o termo de dias `(b.meta_cobertura_dias + b.safety_days)::NUMERIC` por `(GREATEST(b.meta_cobertura_dias, b.lead_time_dias + 7) + b.safety_days)::NUMERIC`. A expressão completa passa de `(b.venda_base * p_demand_multiplier * (b.meta_cobertura_dias + b.safety_days)::NUMERIC) AS alvo_v` para `(b.venda_base * p_demand_multiplier * (GREATEST(b.meta_cobertura_dias, b.lead_time_dias + 7) + b.safety_days)::NUMERIC) AS alvo_v`.

2. Dentro do CASE do `compra` (mesmo CTE `calc`), o ramo ELSE repete o mesmo termo de dias no cálculo da quantidade faltante. Trocar `b.venda_base * p_demand_multiplier * (b.meta_cobertura_dias + b.safety_days)::NUMERIC - (b.sku_stock + b.qtd_a_caminho)::NUMERIC` por `b.venda_base * p_demand_multiplier * (GREATEST(b.meta_cobertura_dias, b.lead_time_dias + 7) + b.safety_days)::NUMERIC - (b.sku_stock + b.qtd_a_caminho)::NUMERIC`, mantendo intactos o GREATEST(0, ...), CEIL, NULLIF(b.pack_multiple, 0), `* b.pack_multiple` e o `GREATEST(..., b.moq)::INTEGER`.

NÃO altere mais NADA: `vd`, `ponto` (continua `(b.lead_time_dias + b.safety_days)`), a condição `WHEN (b.sku_stock + b.qtd_a_caminho) > ponto THEN 0`, o gatilho (`<= c.ponto`), `venda_dia`, `cobertura_atual`, `venda_dia_origem`, `lead_time_origem`, `tendencia`, `fator_sazonal`, `lead_time_real`, `venda_simples`, `venda_inteligente`, todos os CTEs anteriores (inventory_by_sku, params_lookup, costs_*, row_sales, canon, sales_by_sku, incoming_by_sku, fornecedor_by_sku, ewma_sales, seasonal_index, lead_time_by_fornecedor, sales_smart, params, base), o ORDER BY, ou os GRANT/REVOKE.

Atualize APENAS o bloco de comentário do cabeçalho para descrever esta mudança: documente que a constante `7` = ciclo de revisão (reposição a cada 7 dias) e que o alvo agora é o maior entre `meta_cobertura_dias` e `lead_time_dias + 7`, garantindo alvo >= ponto. Pode preservar o resto do comentário herdado da 200.

IMPORTANTE: não rode `supabase db push`, `supabase migration up`, nem aplique via MCP. O executor APENAS cria o arquivo e commita. O deploy da migration é feito pelo orquestrador via MCP após aprovação do Wesley (com comparação antes/depois). Não há mudança de frontend.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260668000300_get_replenishment_by_sku_alvo_order_up_to.sql && grep -c 'GREATEST(b.meta_cobertura_dias, b.lead_time_dias + 7)' supabase/migrations/20260668000300_get_replenishment_by_sku_alvo_order_up_to.sql | grep -qx 2 && echo OK_2_OCORRENCIAS</automated>
  </verify>
  <done>
Arquivo `20260668000300_*.sql` existe; contém EXATAMENTE 2 ocorrências de `GREATEST(b.meta_cobertura_dias, b.lead_time_dias + 7)` (uma no `alvo_v`, uma no `compra`); o `ponto` permanece `(b.lead_time_dias + b.safety_days)` inalterado; DROP FUNCTION 4-arg + CREATE OR REPLACE + REVOKE/GRANT presentes e idênticos à 200; diff contra a 200 mostra mudança SOMENTE no termo de dias de alvo_v/compra e no comentário de cabeçalho. Nenhum deploy executado.
  </done>
</task>

</tasks>

<verification>
- `diff` entre `20260668000300` e `20260668000200` mostra alterações apenas: (a) comentário de cabeçalho, (b) termo de dias do `alvo_v`, (c) termo de dias do `compra`. Nenhuma outra linha (assinatura, RETURNS TABLE, CTEs, ponto, gatilho, ORDER BY, REVOKE/GRANT) muda.
- `grep -c 'GREATEST(b.meta_cobertura_dias, b.lead_time_dias + 7)'` retorna 2.
- `grep '(b.lead_time_dias + b.safety_days)'` ainda presente no `ponto` (não tocado).
</verification>

<success_criteria>
Migration criada e commitada; alvo order-up-to = `GREATEST(meta_cobertura_dias, lead_time_dias + 7) + safety_days` no alvo e na compra; tudo o mais intacto; sem deploy.
</success_criteria>

<output>
Create `.planning/quick/260627-1z0-fix-parametro-reposicao-alvo-order-up-to/260627-1z0-SUMMARY.md` when done.
</output>
