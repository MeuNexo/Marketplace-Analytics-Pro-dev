---
phase: 102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pricing/mcoSimulation.ts
  - src/lib/pricing/mcoSimulation.test.ts
autonomous: true
requirements: [D-04]
must_haves:
  truths:
    - "computeSimulatedWaterfall retorna mcoPct = null (nunca NaN) quando precoUnit <= 0"
    - "comissaoUnit e impostoUnit são derivados de comissaoPct/impostoPct × precoUnit com guard de preço zero"
    - "a fórmula de MCO nunca é reimplementada — computeSimulatedWaterfall chama computeMco"
  artifacts:
    - path: "src/lib/pricing/mcoSimulation.ts"
      provides: "função pura computeSimulatedWaterfall + interfaces SimulatedInputs/SimulatedWaterfall"
      exports: ["computeSimulatedWaterfall", "SimulatedInputs", "SimulatedWaterfall"]
      contains: "computeMco"
    - path: "src/lib/pricing/mcoSimulation.test.ts"
      provides: "suite vitest da função pura (normal, preço zero, %-boundaries, passthrough de negativos)"
      min_lines: 30
  key_links:
    - from: "src/lib/pricing/mcoSimulation.ts"
      to: "src/lib/mco.ts"
      via: "import { computeMco } — fonte única da fórmula de MCO"
      pattern: "import.*computeMco.*from.*@/lib/mco"
---

<objective>
Criar a função pura `computeSimulatedWaterfall` (novo arquivo `src/lib/pricing/mcoSimulation.ts`, irmão de `mcoRecommendation.ts`) que recebe os valores por-unidade editados pelo usuário (preço/CMV/frete/ads em R$, comissão/imposto em %) e devolve o mesmo formato de números derivados que o `WaterfallCard` já expõe (mcUnit, mcoUnit, mcoPct), reusando `computeMco` como fonte única da fórmula. Mais a suíte de testes TDD.

Purpose: Este é o motor de cálculo do simulador (base de D-04 — recompute ao vivo do waterfall/semáforo). Isolado da UI, sem I/O, ele roda independente na Wave 1 e é consumido pela Wave 2. Sem ele, o componente não tem como recalcular o MCO simulado sem duplicar a fórmula canônica (violação da regra "computeMco é fonte única").

Output: `mcoSimulation.ts` (~30 linhas) + `mcoSimulation.test.ts`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-RESEARCH.md
@.planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-PATTERNS.md
</context>

<artifacts_produced>
Símbolos NOVOS criados por este plano (excluir de verificação de drift — não existem no repo antes desta phase):
- `computeSimulatedWaterfall` (função) — `src/lib/pricing/mcoSimulation.ts`
- `SimulatedInputs` (interface) — `src/lib/pricing/mcoSimulation.ts`
- `SimulatedWaterfall` (interface) — `src/lib/pricing/mcoSimulation.ts`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever a suíte RED de mcoSimulation.test.ts</name>
  <files>src/lib/pricing/mcoSimulation.test.ts</files>
  <read_first>
    - src/lib/mco.test.ts — estilo de teste da fórmula (describe/it, toBeCloseTo, caso pct=null quando grossRevenue=0)
    - src/lib/pricing/mcoRecommendation.test.ts — estilo de teste de "função-alavanca" pura irmã
    - src/lib/mco.ts — assinatura de computeMco e a garantia `pct: null` quando grossRevenue <= 0
    - .planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-RESEARCH.md — Pattern 1 (assinatura exata de SimulatedInputs/SimulatedWaterfall + corpo de referência de computeSimulatedWaterfall)
  </read_first>
  <behavior>
    - Caso normal: precoUnit=100, cmvUnit=30, comissaoPct=12, freteUnit=5, impostoPct=8, adsUnit=4 → comissaoUnit≈12, impostoUnit≈8, mcUnit≈100-30-12-5-8=45, mcoUnit≈41, mcoPct≈41 (usar toBeCloseTo).
    - precoUnit = 0 → mcoPct === null (nunca NaN/Infinity); comissaoUnit e impostoUnit === 0 (guard de divisão).
    - Fronteira %: comissaoPct=0 e impostoPct=0 → comissaoUnit=0, impostoUnit=0; comissaoPct=100 sobre precoUnit=100 → comissaoUnit=100.
    - Passthrough de negativo: a função pura NÃO rejeita valores negativos (validação é responsabilidade da UI em D-05) — cmvUnit negativo apenas propaga aritmeticamente, sem throw.
    - mcUnit exclui ads; mcoUnit inclui ads (mcUnit - adsUnit == mcoUnit).
  </behavior>
  <action>
    Criar `src/lib/pricing/mcoSimulation.test.ts` importando `computeSimulatedWaterfall` (ainda inexistente — RED) de `./mcoSimulation`. Escrever os casos do bloco `<behavior>` como `describe`/`it`, com comentários inline mostrando a aritmética e `toBeCloseTo` para floats, espelhando o estilo de `mco.test.ts` e `mcoRecommendation.test.ts`. Este commit é a fase RED do ciclo TDD: a suíte deve compilar contra as interfaces esperadas e FALHAR por o módulo ainda não existir. Commit: `test(102-01): add failing tests for computeSimulatedWaterfall`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pricing/mcoSimulation.test.ts 2>&1 | grep -Eq "Cannot find module|Failed to resolve|No test files|failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pricing/mcoSimulation.test.ts` existe e importa `computeSimulatedWaterfall` de `./mcoSimulation`.
    - A suíte cobre: caso normal, precoUnit=0→null, fronteiras %-0/100, passthrough de negativo, e a relação mcUnit-adsUnit==mcoUnit.
    - `npx vitest run src/lib/pricing/mcoSimulation.test.ts` FALHA (RED) porque `src/lib/pricing/mcoSimulation.ts` ainda não existe.
  </acceptance_criteria>
  <done>Arquivo de teste criado, RED comprovado (falha por módulo ausente), commitado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar computeSimulatedWaterfall (GREEN)</name>
  <files>src/lib/pricing/mcoSimulation.ts</files>
  <read_first>
    - src/lib/mco.ts — computeMco (assinatura McoInput → McoResult, guard pct null)
    - src/lib/pricing/mcoRecommendation.ts — docblock/tom + derivação R$↔% com guard `precoUnit > 0 ? ... : 0` (linhas 43-44)
    - src/lib/precoMcoSeries.ts — interface WaterfallCard (campos mcUnit/mcoUnit/mcoPct que o formato simulado deve espelhar para o componente reusar o mesmo Row)
    - .planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-RESEARCH.md — Pattern 1 (corpo de referência)
  </read_first>
  <behavior>
    - Ver bloco <behavior> da Task 1 — a implementação deve fazer TODOS aqueles testes passarem.
  </behavior>
  <action>
    Criar `src/lib/pricing/mcoSimulation.ts` com as interfaces `SimulatedInputs` (precoUnit, cmvUnit, comissaoPct, freteUnit, impostoPct, adsUnit) e `SimulatedWaterfall` (precoUnit, cmvUnit, comissaoUnit, freteUnit, impostoUnit, adsUnit, mcUnit, mcoUnit, mcoPct) e a função `computeSimulatedWaterfall(input)`. Derivar comissaoUnit e impostoUnit como `precoUnit > 0 ? (precoUnit * pct) / 100 : 0` (guard obrigatório contra preço zero — Pitfall 5). Chamar `computeMco({ grossRevenue: precoUnit, cmv: cmvUnit, platformCost: comissaoUnit + freteUnit, ads: adsUnit, tax: impostoUnit })` para obter mco/pct — NUNCA reescrever a fórmula. mcUnit = margem de contribuição ANTES de ads (precoUnit - cmvUnit - comissaoUnit - freteUnit - impostoUnit); mcoUnit = mco (pós-ads); mcoPct = pct. Docblock no tom de `mcoRecommendation.ts` explicando zero-I/O e reuso de computeMco. Rodar a suíte (GREEN). Commit: `feat(102-01): implement computeSimulatedWaterfall`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pricing/mcoSimulation.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/pricing/mcoSimulation.test.ts` passa 100% (GREEN).
    - `grep -Eq "import.*computeMco.*from.*[\"']@/lib/mco[\"']" src/lib/pricing/mcoSimulation.ts` — importa computeMco.
    - `grep -q "grossRevenue - cmv - platformCost - ads - tax" src/lib/pricing/mcoSimulation.ts` retorna vazio — a fórmula NÃO é reimplementada localmente (só `computeMco` a contém).
    - Guard `precoUnit > 0` presente antes de cada divisão por precoUnit.
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>Função implementada, todos os testes verdes, tsc limpo, commitado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Função pura, zero I/O, zero rede, zero persistência — não cruza nenhuma fronteira de confiança nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | computeSimulatedWaterfall (integridade numérica) | mitigate | Guard `precoUnit > 0 ? ... : 0` em toda divisão + `computeMco` retorna pct=null quando grossRevenue<=0 → nunca NaN/Infinity; coberto por teste dedicado |

Sem instalação de pacotes npm neste plano → sem checkpoint de legitimidade (T-102-SC não se aplica).
</threat_model>

<verification>
- `npx vitest run src/lib/pricing/mcoSimulation.test.ts` verde.
- `npx tsc --noEmit` sem erros.
- computeMco é a única fonte da fórmula (grep confirma import + ausência de reimplementação).
</verification>

<success_criteria>
- `computeSimulatedWaterfall` existe, é pura, reusa `computeMco`, e trata precoUnit<=0 devolvendo mcoPct=null.
- Suíte TDD verde (normal, preço zero, fronteiras %, passthrough de negativo).
- Ciclo RED→GREEN em commits atômicos separados.
</success_criteria>

<output>
Create `.planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-01-SUMMARY.md` when done
</output>
