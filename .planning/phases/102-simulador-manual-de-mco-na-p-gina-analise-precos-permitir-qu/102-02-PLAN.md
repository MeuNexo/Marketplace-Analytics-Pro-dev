---
phase: 102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.test.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]
must_haves:
  truths:
    - "Ligar o toggle 'Simular' transforma as 6 linhas do waterfall (Receita/CMV/Comissão/Frete/Impostos, e Ads só quando incluirAds) em campos editáveis dentro de um painel tingido"
    - "Ao digitar num campo, MC/un, MCO/un e o Badge do semáforo recalculam ao vivo (a cada tecla) usando os valores simulados"
    - "As duas linhas de recomendação (preço mínimo, ACOS-alvo) permanecem byte-idênticas antes/durante/depois da simulação — sempre calculadas sobre o waterfallCard REAL"
    - "Valor inválido (preço/CMV/frete/ads < 0; comissão%/imposto% fora de 0-100) no blur/Enter dispara toast.error e reverte o campo ao último valor válido"
    - "Botão 'Resetar' e troca de anúncio/variação (selectedId/selectedSku) voltam a simulação aos valores reais; troca de item também desliga o modo Simular"
    - "Nada é persistido — só useState de componente"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      provides: "toggle Simular + SimField + painel tingido + recompute ao vivo + âncora D-04 + reset D-03"
      contains: "computeSimulatedWaterfall"
    - path: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.test.tsx"
      provides: "bloco describe 'modo Simular' cobrindo D-03/D-04/D-05"
  key_links:
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/lib/pricing/mcoSimulation.ts"
      via: "import { computeSimulatedWaterfall } — motor do recompute simulado"
      pattern: "computeSimulatedWaterfall"
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/lib/pricing/mcoRecommendation.ts"
      via: "computeMcoRecommendation SEMPRE recebe waterfallCard real (invariante D-04 intocada)"
      pattern: "computeMcoRecommendation\\(waterfallCard, targetMcoPct\\)"
---

<objective>
Estender o card "Detalhamento de MCO" em `PrecoPraticadoReport.tsx` com o modo de simulação manual ("e se"): toggle "Simular" no header, painel tingido com as 6 linhas do waterfall viradas em campos editáveis (`SimField`), recompute ao vivo de MC/un + MCO/un + semáforo com os valores simulados (D-04), validação commit-time com toast+revert (D-05), botão "Resetar" + reset automático na troca de item/variação (D-03) — tudo client-side/efêmero, sem backend. E estender o teste do componente.

Purpose: É a entrega visível da phase. O invariante crítico é D-04: as recomendações (preço mínimo, ACOS-alvo) NUNCA reagem à simulação — continuam ancoradas no waterfallCard real. Comissão/imposto são editados em %, não R$, para acompanharem o preço simulado (Pitfall 2). O comportamento é 100% aditivo: com o toggle desligado, o card é idêntico à Phase 101.

Output: `PrecoPraticadoReport.tsx` estendido + `PrecoPraticadoReport.test.tsx` com o bloco "modo Simular".
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-RESEARCH.md
@.planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-UI-SPEC.md
@.planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-PATTERNS.md
@src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
@src/lib/pricing/mcoSimulation.ts
</context>

<artifacts_produced>
Símbolos NOVOS criados por este plano (excluir de verificação de drift):
- `SimField` (componente React local a PrecoPraticadoReport.tsx)
- estados locais: `simulating`, `simDraft`, e handlers `seedFromReal`/`handleToggleSimular`/`handleResetar`
- `simCard` (useMemo derivado) e `activeMcoPct` (seletor real-vs-simulado)
Consome de 102-01: `computeSimulatedWaterfall`, `SimulatedInputs`, `SimulatedWaterfall`.
Consome existentes (Phase 101/50/83): `computeMco`, `classifyMcoHealth`, `mcoHealthRole`, `parseNumber`, `Row`, `computeMcoRecommendation`, `computeWaterfallCard`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: SimField + estado de simulação + ciclo reset</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx — estado atual completo; especialmente linhas 264-292 (estados + effect de reset de selectedSku), 604-622 (commitMcoTargetEdit: padrão toast+revert), 712-722 (Switch+Label "Incluir publicidade")
    - src/lib/pricing/calculator.ts — parseNumber (linha 199): pt-BR "1.234,56"→1234.56, degrada inválido→0, nunca lança
    - src/lib/pricing/mcoSimulation.ts — SimulatedInputs/SimulatedWaterfall/computeSimulatedWaterfall (do 102-01)
    - .planning/phases/102-.../102-RESEARCH.md — Pattern 2 (SimField: onChange live + onBlur valida) e Pattern 3 (seedFromReal, handlers, useEffect de reset, simCard useMemo)
    - .planning/phases/102-.../102-UI-SPEC.md — "Interaction Contract" (classes do input, cópia dos toasts, teclado) e tabela de min/max por campo
  </read_first>
  <action>
    Em `PrecoPraticadoReport.tsx`, adicionar (sem tocar em nenhuma lógica existente):

    1. Import `computeSimulatedWaterfall` + tipos de `@/lib/pricing/mcoSimulation`; import `parseNumber` de `@/lib/pricing/calculator`; import do ícone `RotateCcw` de lucide-react (adicionar ao import existente na linha 5-8).

    2. Componente local `SimField` (fora do componente principal, escopo de módulo, ao lado do `Row`): props `value:number, min:number, max?:number, unit:"currency"|"percent", onLiveChange:(v:number)=>void, onReject:()=>void`. Estado interno `draft` string iniciado de `String(value)`. `onChange`: `setDraft(raw)` e `onLiveChange(parseNumber(raw))` a CADA tecla (recompute ao vivo — D-04, sem debounce, sem gate de blur). `onBlur`: parse do draft final; inválido = draft vazio OU parsed<min OU (max!=null e parsed>max) → `toast.error(...)` com a cópia da UI-SPEC (currency: "Valor precisa ser maior ou igual a zero"; percent: "Valor precisa estar entre 0% e 100%") + `setDraft(String(value))` + `onReject()`; senão `setDraft(String(parsed))`. `onKeyDown` Enter → `.blur()`. Input: `type="text" inputMode="decimal"`, classes exatas da UI-SPEC (`w-20 rounded border border-accent/40 bg-background px-1.5 py-0.5 text-right text-xs outline-none ring-1 ring-accent/30 tabular-nums`). Sufixo de unidade `%` (após) ou prefixo `R$` (antes) em `text-[10px] text-muted-foreground`. NÃO usar o shadcn `Input` — raw `<input>` como o campo Meta MCO%. IMPORTANTE: `draft` deve re-sincronizar quando `value` muda por fonte externa (Resetar/troca de item) — usar `useEffect([value])` que faz `setDraft(String(value))`, para o reseed refletir na tela.

    3. Estado no componente principal: `const [simulating, setSimulating] = useState(false)` e `const [simDraft, setSimDraft] = useState<SimulatedInputs | null>(null)`.

    4. `seedFromReal(card: WaterfallCard): SimulatedInputs` (useCallback) espelhando Pattern 3: precoUnit/cmvUnit/freteUnit direto; comissaoPct = `card.precoUnit > 0 ? (card.comissaoUnit / card.precoUnit) * 100 : 0`; impostoPct idem com impostoUnit; adsUnit = `incluirAds ? card.adsUnit : 0` (Pitfall 4 — nunca semear ads quando o toggle está off).

    5. Handlers: `handleToggleSimular(checked)` → `setSimulating(checked)`; se checked, `setSimDraft(seedFromReal(waterfallCard))` (reseed do card real ATUAL no momento do clique — nunca lazy initializer, Pitfall 3). `handleResetar()` → `setSimDraft(seedFromReal(waterfallCard))`.

    6. `useEffect(() => { setSimulating(false); setSimDraft(null); }, [selectedId, selectedSku])` — reset automático D-03 (some o modo simular ao trocar item/variação).

    7. `const simCard = useMemo(() => (simDraft ? computeSimulatedWaterfall(simDraft) : null), [simDraft])`.

    NÃO alterar ainda a renderização do waterfall/recomendação — isso é a Task 2. Este task só adiciona a infraestrutura de estado + o componente SimField.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "computeSimulatedWaterfall" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` — import/uso presente.
    - `grep -q "function SimField" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` — componente definido.
    - `grep -q "parseNumber" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` e `grep -q "RotateCcw" ...` — imports presentes.
    - Existe `useEffect` cujo array de deps é `[selectedId, selectedSku]` que faz `setSimulating(false)` — reset D-03.
    - `seedFromReal` deriva comissaoPct/impostoPct via `precoUnit > 0 ? ... : 0` (nunca R$ cru para comissão/imposto — D-01/Pitfall 2) e zera adsUnit quando `!incluirAds` (Pitfall 4).
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>SimField + estado de simulação + handlers + effect de reset adicionados; tsc limpo. Renderização ainda intocada.</done>
</task>

<task type="auto">
  <name>Task 2: Wire do waterfall editável, header (toggle/badge/Resetar), semáforo dinâmico e âncora D-04</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx — bloco do card (linhas 917-1058): header (932-946), waterfall block (948-972), Meta MCO% (974-1010), recomendação (1012-1036), warning footer (1038-1054); e as linhas 590-600 (waterfallCard/mcoRecommendation/mcoHealthValue/mcoRole)
    - .planning/phases/102-.../102-UI-SPEC.md — seções "Simulator Layout & Interaction" (header estendido, painel tingido, tabela de campos), "Color" (regra de não-colisão accent×semáforo), "Copywriting Contract" (todas as cópias)
    - .planning/phases/102-.../102-RESEARCH.md — bloco "Recommendation call site — must stay unchanged" e "Semáforo reuse"
  </read_first>
  <action>
    Alterar SÓ a renderização do card (o data-layer/fetch permanece intocado):

    1. Semáforo dinâmico (D-04): adicionar `const activeMcoPct = simulating && simCard ? simCard.mcoPct : waterfallCard.mcoPct;` e derivar `mcoHealthValue`/`mcoRole` de `activeMcoPct` (substituir o cálculo atual da linha 599-600 para usar `activeMcoPct` no lugar de `waterfallCard.mcoPct`). O Badge do header e as cores das linhas de subtotal passam a seguir o semáforo simulado quando `simulating`.

    2. INVARIANTE D-04 — NÃO tocar na chamada `computeMcoRecommendation(waterfallCard, targetMcoPct)` (linhas 595-598): ela continua recebendo o `waterfallCard` real, com deps `[waterfallCard, targetMcoPct]`. Nunca alimentar o card simulado na recomendação (seria circular). As duas linhas de recomendação (preço mínimo, ACOS-alvo) renderizam idênticas independente de `simulating`.

    3. Header (bloco 932-946): à direita do Badge de %, adicionar o par `Switch`+`Label` "Simular" (mesma forma visual do "Incluir publicidade": `Label` `text-xs text-muted-foreground cursor-pointer`, `onCheckedChange={handleToggleSimular}`). Quando `simulating===true`, adicionar inline, nesta ordem: (a) `Badge` variant `outline` accent-tinted `border-transparent bg-accent/15 text-accent` texto "Simulando"; (b) `Button` variant `ghost` size `sm` com `RotateCcw` (12px) + "Resetar" `text-xs` `onClick={handleResetar}`. Header vira `flex-wrap` para paridade mobile.

    4. Waterfall block (948-972): quando `simulating===true`, envolver o `<div className="space-y-2">` num painel tingido `rounded-lg bg-accent/5 border border-accent/20 p-2 -mx-2`. Cada uma das linhas editáveis troca o valor por um `SimField` (mantendo o rótulo `k` intacto), lendo/escrevendo em `simDraft` via `onLiveChange`/`onReject` (onReject reverte para o valor de `simDraft` corrente):
       - "Receita/un" ← precoUnit, currency, min 0
       - "(−) CMV" ← cmvUnit, currency, min 0
       - "(−) Comissão" ← comissaoPct, percent, min 0 max 100
       - "(−) Frete" ← freteUnit, currency, min 0
       - "(−) Impostos" ← impostoPct, percent, min 0 max 100
       - "(−) Ads" ← adsUnit, currency, min 0 — renderizado SÓ quando `incluirAds` (mesma condição da linha 963); quando `!incluirAds`, sem SimField de ads e `simDraft.adsUnit` fica 0 (Pitfall 4).
       As linhas de subtotal "= Margem de Contribuição/un" e "= MCO/un" continuam usando o `Row` existente, mas com `v`/`accent`/`danger` vindos de `simCard` quando `simulating` (ex.: `simulating && simCard ? simCard.mcUnit : waterfallCard.mcUnit`). Com o toggle OFF, renderiza exatamente o `Row` da Phase 101 (valores reais) — nada muda.
       Handlers de edição: cada `onLiveChange={(v)=>setSimDraft(d => d ? {...d, precoUnit: v} : d)}` (campo correspondente); `onReject={()=>{/* SimField já reverte o draft string; o valor no simDraft volta ao último válido */}}`. Para revert do valor computado, restaurar o campo em `simDraft` ao último valor válido — a forma mais simples: SimField reverte o próprio input e chama `onReject`, e como o `onLiveChange` já não foi chamado com o valor inválido no commit, o `simDraft` mantém o último válido; garantir que o revert visual e o valor computado batam (o `useEffect([value])` do SimField ressincroniza o draft string a partir do `value` que continua sendo o último válido de `simDraft`).

    5. Badge do header (linha 937-939): `pctFmt(activeMcoPct)` e classe `MCO_ROLE_BADGE_CLASS[mcoRole]` (agora dinâmico). A regra de não-colisão de cor: o chrome de "simulação" é accent; o semáforo é success/warning/destructive — nunca sobrepostos no mesmo elemento (painel accent-tingido contendo Badge de saúde colorido).

    6. Recomendação (1012-1036): adicionar UMA linha de legenda, SÓ quando `simulating`, abaixo das duas linhas, `text-[10px] text-muted-foreground`: "Preço mínimo e ACOS-alvo continuam calculados com os custos e preço reais — não mudam com a simulação". Números/ícones/layout pixel-idênticos à 101.

    7. Warning footer (1038-1054): permanece incondicional, baseado em `waterfallCard.custoAusente`/`impostoAusente` (dados reais), não muda com `simulating`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq "computeMcoRecommendation\(waterfallCard, targetMcoPct\)" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` — a chamada de recomendação segue com o card REAL (invariante D-04 preservada).
    - `grep -q "activeMcoPct" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` e o semáforo (`classifyMcoHealth`) usa `activeMcoPct`.
    - Header contém o `Switch` com `onCheckedChange={handleToggleSimular}` e, condicional a `simulating`, o Badge "Simulando" e o Button "Resetar" com `RotateCcw`.
    - Painel tingido `bg-accent/5 border border-accent/20` presente e condicionado a `simulating`.
    - A linha de Ads editável é gated pela mesma condição `incluirAds` (grep mostra `incluirAds &&` na vizinhança do SimField de ads).
    - Legenda-âncora da recomendação presente com a cópia exata da UI-SPEC, condicionada a `simulating`.
    - `npx tsc --noEmit` e `npm run build` sem erros.
  </acceptance_criteria>
  <done>Card estendido com toggle/painel/campos editáveis/semáforo dinâmico/legenda-âncora; recomendação intocada; tsc + build limpos.</done>
</task>

<task type="auto">
  <name>Task 3: Estender PrecoPraticadoReport.test.tsx com o bloco "modo Simular"</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.test.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.test.tsx — fixture/mocks existentes (SALES_ROW, rpcRows, mocks de supabase/MLInventoryContext/useMcoTargets) e os 2 testes atuais
    - .planning/phases/102-.../102-PATTERNS.md — seção do test file (5 asserts requeridos)
  </read_first>
  <action>
    Adicionar `describe("modo Simular", ...)` reusando a MESMA fixture/mocks já no arquivo. Testes (via `@testing-library/react` `fireEvent`/`userEvent`, aguardando o card montar com `waitFor`):
    1. D-02: ligar o toggle "Simular" faz as linhas do waterfall virarem `<input>` editáveis (ex.: o valor de "Receita/un" que era texto vira um input com o valor real semeado).
    2. D-04 (o mais importante): capturar o texto das duas linhas de recomendação (preço mínimo, ACOS-alvo) ANTES de simular; ligar Simular; alterar o campo de preço para um valor diferente; asseverar que os números de recomendação continuam BYTE-IDÊNTICOS (não reagem à simulação).
    3. D-04: alterar o campo de preço/CMV muda o texto do Badge de semáforo e/ou da linha "= MCO/un" (recompute ao vivo comprovado).
    4. D-05: digitar valor inválido (ex.: comissão% = "150") e disparar blur → `toast.error` é chamado (o mock de sonner registra a chamada) e o campo reverte ao valor anterior.
    5. D-03: mudar `selectedId` (re-render com outro produto pré-selecionado) reseta `simulating` para false (o painel tingido/Badge "Simulando" somem).
    Mockar `sonner`/`toast.error` no topo se ainda não estiver mockado (checar mocks existentes; se `toast` não é mockado hoje, adicionar `vi.mock("sonner", ...)` com `error: vi.fn()`).
  </action>
  <verify>
    <automated>npx vitest run src/components/mercadolivre/anuncios/PrecoPraticadoReport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Novo `describe("modo Simular"` presente no arquivo.
    - Os 5 asserts (D-02 inputs, D-04 recomendação byte-idêntica, D-04 recompute ao vivo, D-05 toast+revert, D-03 reset em troca de item) existem e passam.
    - `npx vitest run src/components/mercadolivre/anuncios/PrecoPraticadoReport.test.tsx` verde; os 2 testes originais da Phase 101 continuam verdes (sem regressão).
  </acceptance_criteria>
  <done>Bloco de teste "modo Simular" adicionado, todos os testes do arquivo verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entrada numérica do usuário → estado do componente | valores digitados nos SimField cruzam para o cálculo em memória (nunca para rede/DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02 | Tampering (integridade de dado) | SimField (validação de faixa) | mitigate | Validação commit-time (D-05): preço/CMV/frete/ads ≥ 0; comissão%/imposto% 0-100; inválido → toast.error + revert ao último válido; parseNumber degrada não-numérico a 0 sem lançar |
| T-102-03 | Tampering (XSS via render) | render dos valores simulados no DOM | accept | Todos os números são renderizados via text-node do React / templates `brl()`/`pctFmt()`, nunca `dangerouslySetInnerHTML` — sem superfície de injeção introduzida |
| T-102-04 | Information Disclosure | persistência acidental | mitigate | Zero persistência: só `useState`. Nenhuma tabela/RPC/localStorage/URL param — se surgir necessidade de gravar, é violação de escopo (CONTEXT.md), sinalizar não implementar |

Sem instalação de pacotes npm neste plano → T-102-SC não se aplica.
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run build` limpos.
- `npx vitest run src/components/mercadolivre/anuncios/PrecoPraticadoReport.test.tsx` verde (2 originais + bloco novo).
- Invariante D-04 comprovada por teste (recomendação byte-idêntica) E por grep (call site intocado).
- Toggle OFF = card idêntico à Phase 101 (nenhuma linha existente alterada no caminho não-simulando).
</verification>

<success_criteria>
- Toggle "Simular" liga edição inline das 6 linhas dentro do painel tingido (D-01/D-02).
- MC/un, MCO/un e semáforo recalculam ao vivo com os valores simulados (D-04).
- Recomendação (preço mínimo, ACOS-alvo) ancorada no real, byte-idêntica durante a simulação (D-04).
- Validação toast+revert em cada campo (D-05); Resetar e troca de item/variação resetam (D-03).
- Zero persistência; comportamento com toggle OFF idêntico à Phase 101.
</success_criteria>

<output>
Create `.planning/phases/102-simulador-manual-de-mco-na-p-gina-analise-precos-permitir-qu/102-02-SUMMARY.md` when done
</output>
