---
phase: 02-configuracao-fiscal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/roleAccess.ts
  - src/components/layout/routeMeta.ts
  - src/components/layout/ApiSidebar.tsx
autonomous: true
requirements:
  - FISCAL-01
must_haves:
  truths:
    - "/fiscal is listed in roleAccess as OWNER_ONLY"
    - "routeTitles has an entry for /fiscal with title and subtitle"
    - "ApiSidebar Configurações group has a Fiscal child after Integrações"
  artifacts:
    - path: "src/config/roleAccess.ts"
      provides: "Access control for /fiscal"
      contains: '"/fiscal": OWNER_ONLY'
    - path: "src/components/layout/routeMeta.ts"
      provides: "Page title and subtitle for /fiscal"
      contains: '"/fiscal"'
    - path: "src/components/layout/ApiSidebar.tsx"
      provides: "Sidebar navigation item for Fiscal"
      contains: '"Fiscal"'
  key_links:
    - from: "src/config/roleAccess.ts"
      to: "src/components/auth/RoleRoute"
      via: "roleAccess record lookup"
      pattern: '"/fiscal":\s*OWNER_ONLY'
    - from: "src/components/layout/ApiSidebar.tsx"
      to: "/fiscal route"
      via: "path property in Configurações children"
      pattern: 'path.*fiscal'
---

<objective>
Wire /fiscal into the access-control, route-meta, and sidebar infrastructure so that the page
is reachable and correctly restricted before the page component itself is written.

Purpose: Wave 1 tasks are pure config edits with zero UI dependencies — they unblock Wave 2 in
parallel and establish the role/nav contract that MLFiscal.tsx relies on.

Output: Three edited files; no new files.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc\src\config\roleAccess.ts
@C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc\src\components\layout\routeMeta.ts
@C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc\src\components\layout\ApiSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /fiscal to roleAccess (FISCAL-01)</name>
  <files>src/config/roleAccess.ts</files>
  <action>
    In the `roleAccess` record, add the entry `"/fiscal": OWNER_ONLY` on the line immediately
    after `"/integracoes": OWNER_ONLY` (currently line 25). The constant `OWNER_ONLY` is already
    defined at the top of the file — do not redefine it. Do not touch any other lines.

    Result after edit (lines 24-26 area):
      "/integracoes": OWNER_ONLY,
      "/fiscal": OWNER_ONLY,
      "/monitoramento": OWNER_ONLY,
  </action>
  <verify>
    <automated>npx tsc --noEmit --project C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc\tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>roleAccess record contains "/fiscal": OWNER_ONLY and tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add /fiscal to routeMeta (FISCAL-01)</name>
  <files>src/components/layout/routeMeta.ts</files>
  <action>
    In the `routeTitles` record, add the /fiscal entry immediately after the "/integracoes" entry
    (currently line 16). Use exactly:

      "/fiscal": { title: "Configuração Fiscal", subtitle: "Configure o regime tributário de cada conta Mercado Livre" },

    Do not modify any other lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc\tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>routeTitles has a "/fiscal" key with the correct title and subtitle strings.</done>
</task>

<task type="auto">
  <name>Task 3: Add Fiscal nav item to ApiSidebar (FISCAL-01)</name>
  <files>src/components/layout/ApiSidebar.tsx</files>
  <action>
    Two changes to ApiSidebar.tsx:

    1. Add `FileText` to the lucide-react import statement (line 1). The existing import ends with
       `Users,` — add `FileText,` to the same import block. Do not remove any existing imports.

    2. In the `apiSections` array, inside the "Configurações" group's `children` array
       (currently ends at `{ icon: Plug, label: "Integrações", path: "/integracoes" },`),
       append a new child entry on the next line:

         { icon: FileText, label: "Fiscal", path: "/fiscal" },

    The "Configurações" children array after the edit must be:
      { icon: Target,    label: "Metas",       path: "/metas"       },
      { icon: Users,     label: "Sellers",     path: "/sellers"     },
      { icon: Plug,      label: "Integrações", path: "/integracoes" },
      { icon: FileText,  label: "Fiscal",      path: "/fiscal"      },
  </action>
  <verify>
    <automated>npx tsc --noEmit --project C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc\tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>FileText is imported and the Configurações children array contains the Fiscal entry at the end. tsc exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar visibility → route access | Nav item visibility alone does not grant access; RoleRoute enforces server-side role check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | roleAccess /fiscal | mitigate | OWNER_ONLY array ensures only owners pass RoleRoute; non-owners are redirected |
| T-02-02 | Information Disclosure | ApiSidebar | accept | Sidebar item is UI hint only; actual gate is RoleRoute + roleAccess; seeing the link grants nothing |
</threat_model>

<verification>
After all three tasks complete:
- grep confirms `"/fiscal": OWNER_ONLY` in roleAccess.ts
- grep confirms `"/fiscal"` in routeMeta.ts
- grep confirms `FileText` import and `path: "/fiscal"` in ApiSidebar.tsx
- `tsc --noEmit` exits 0
</verification>

<success_criteria>
- roleAccess.ts: "/fiscal" mapped to OWNER_ONLY, positioned after "/integracoes"
- routeMeta.ts: "/fiscal" entry with title "Configuração Fiscal"
- ApiSidebar.tsx: FileText imported, Fiscal nav item appended to Configurações children
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuracao-fiscal/02-01-SUMMARY.md` summarising
what was changed in each file and confirm tsc passes.
</output>
