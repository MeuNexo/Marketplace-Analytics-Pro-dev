---
phase: 77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr
plan: 03
type: execute
wave: 3
depends_on: ["77-02"]
files_modified:
  - src/App.tsx
  - src/config/roleAccess.ts
  - src/components/layout/ApiSidebar.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "As rotas /produtos-vendidos e /analise-precos estão registradas em App.tsx (lazy + RoleRoute + ErrorBoundary) e navegam para as páginas reais"
    - "Ambas as rotas estão em roleAccess.ts com OPERATIONAL (owner/admin/member) — sem default-deny silencioso (lição Phase 54)"
    - "O grupo Dashboard do menu lateral mostra 'Produtos Vendidos' e 'Análise de Preços' como itens navegáveis"
  artifacts:
    - path: "src/App.tsx"
      provides: "rotas lazy /produtos-vendidos e /analise-precos"
      contains: "produtos-vendidos"
    - path: "src/config/roleAccess.ts"
      provides: "registro das duas rotas (default-deny evitado)"
      contains: "/analise-precos"
    - path: "src/components/layout/ApiSidebar.tsx"
      provides: "2 itens no grupo Dashboard"
      contains: "Análise de Preços"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/mercadolivre/MLProdutosVendidos.tsx"
      via: "React.lazy(() => import('./pages/mercadolivre/MLProdutosVendidos'))"
      pattern: "MLProdutosVendidos"
    - from: "src/components/layout/ApiSidebar.tsx"
      to: "/produtos-vendidos"
      via: "child do grupo Dashboard com path /produtos-vendidos"
      pattern: "/produtos-vendidos"
    - from: "src/config/roleAccess.ts"
      to: "/produtos-vendidos"
      via: "chave no mapa roleAccess"
      pattern: "produtos-vendidos"
---

<objective>
Fiar as duas páginas ao app: rotas em App.tsx, controle de acesso em roleAccess.ts e itens no menu lateral (ApiSidebar.tsx). Concentrado em UM plano para evitar que dois planos paralelos toquem os mesmos 3 arquivos de config. Depende do Plano 02 porque App.tsx faz `import` dos arquivos de página.

Purpose: Tornar as páginas acessíveis por rota e menu, sem cair no default-deny (pitfall Phase 54).
Output: 3 arquivos de config modificados.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md
@src/pages/mercadolivre/MLProdutosVendidos.tsx
@src/pages/mercadolivre/MLAnalisePrecos.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (Plano 03)
- **Rotas:** `/produtos-vendidos` e `/analise-precos` em `src/App.tsx` (lazy imports `MLProdutosVendidos`, `MLAnalisePrecos` + Routes com RoleRoute+ErrorBoundary)
- **Access control:** chaves `"/produtos-vendidos"` e `"/analise-precos"` = OPERATIONAL em `src/config/roleAccess.ts`
- **Menu:** 2 itens novos no grupo "Dashboard" de `src/components/layout/ApiSidebar.tsx`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Registrar rotas em App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (linhas 26-56 lazy imports; ~127-144 bloco de rotas protegidas com /compras como analog exato)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção App.tsx modificado — snippets)
  </read_first>
  <action>Adicionar 2 lazy imports junto dos existentes (padrão da linha `const MLCompras = React.lazy(() => import("./pages/mercadolivre/MLCompras"));`): `const MLProdutosVendidos = React.lazy(() => import("./pages/mercadolivre/MLProdutosVendidos"));` e `const MLAnalisePrecos = React.lazy(() => import("./pages/mercadolivre/MLAnalisePrecos"));`. Adicionar 2 Routes no bloco de rotas protegidas (junto de `/compras`, mesmo wrapper): `<Route path="/produtos-vendidos" element={<RoleRoute><ErrorBoundary fallbackTitle="Erro em Produtos Vendidos"><MLProdutosVendidos /></ErrorBoundary></RoleRoute>} />` e `<Route path="/analise-precos" element={<RoleRoute><ErrorBoundary fallbackTitle="Erro em Análise de Preços"><MLAnalisePrecos /></ErrorBoundary></RoleRoute>} />`.</action>
  <verify>
    <automated>grep -q "MLProdutosVendidos" src/App.tsx &amp;&amp; grep -q "MLAnalisePrecos" src/App.tsx &amp;&amp; grep -q 'path="/produtos-vendidos"' src/App.tsx &amp;&amp; grep -q 'path="/analise-precos"' src/App.tsx &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` contém `React.lazy(() => import("./pages/mercadolivre/MLProdutosVendidos"))`
    - `src/App.tsx` contém `React.lazy(() => import("./pages/mercadolivre/MLAnalisePrecos"))`
    - `src/App.tsx` contém `path="/produtos-vendidos"` e `path="/analise-precos"` com `<RoleRoute>` e `<ErrorBoundary`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Duas rotas lazy registradas com RoleRoute+ErrorBoundary.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar rotas em roleAccess.ts (evitar default-deny)</name>
  <files>src/config/roleAccess.ts</files>
  <read_first>
    - src/config/roleAccess.ts (mapa roleAccess; constante OPERATIONAL; entrada "/compras": OPERATIONAL como analog)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção roleAccess.ts modificado)
  </read_first>
  <action>No objeto `roleAccess`, adicionar (junto de `"/compras": OPERATIONAL`): `"/produtos-vendidos": OPERATIONAL,` e `"/analise-precos": OPERATIONAL,` — nível analítico, mesmo que /compras. CRÍTICO: sem este registro, `canAccess` retorna false para toda role e o usuário é redirecionado silenciosamente para /vendas (pitfall Phase 54 com /consultor). Não alterar VIEWER_ELIGIBLE_ROUTES (viewer permanece default-deny para estas rotas — coerente com /compras).</action>
  <verify>
    <automated>grep -q '"/produtos-vendidos": OPERATIONAL' src/config/roleAccess.ts &amp;&amp; grep -q '"/analise-precos": OPERATIONAL' src/config/roleAccess.ts &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/roleAccess.ts` contém `"/produtos-vendidos": OPERATIONAL`
    - `src/config/roleAccess.ts` contém `"/analise-precos": OPERATIONAL`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Rotas mapeadas em roleAccess — sem default-deny silencioso.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar 2 itens no grupo Dashboard do menu lateral</name>
  <files>src/components/layout/ApiSidebar.tsx</files>
  <read_first>
    - src/components/layout/ApiSidebar.tsx (import de lucide linhas 1-24; grupo "Dashboard" children linhas 34-39; grupo Operações usa ShoppingBag para "Anúncios")
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção ApiSidebar.tsx modificado)
  </read_first>
  <action>No array `children` do grupo "Dashboard" (após `{ icon: Receipt, label: "Margem", path: "/financeiro" }`), adicionar: `{ icon: PackageSearch, label: "Produtos Vendidos", path: "/produtos-vendidos" },` e `{ icon: LineChart, label: "Análise de Preços", path: "/analise-precos" },`. Adicionar `PackageSearch` e `LineChart` ao bloco de import de `lucide-react` no topo do arquivo (ShoppingBag já está em uso por "Anúncios" no grupo Operações — usar ícones distintos para não confundir). Se algum nome de ícone não existir no lucide-react instalado, usar alternativas válidas: `Activity` ou `BarChart2` para Análise de Preços, `ShoppingBag`/`Package` para Produtos Vendidos.</action>
  <verify>
    <automated>grep -q '"/produtos-vendidos"' src/components/layout/ApiSidebar.tsx &amp;&amp; grep -q 'Análise de Preços' src/components/layout/ApiSidebar.tsx &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/ApiSidebar.tsx` contém item com `path: "/produtos-vendidos"` e label `"Produtos Vendidos"`
    - `src/components/layout/ApiSidebar.tsx` contém item com `path: "/analise-precos"` e label `"Análise de Preços"`
    - Ícones usados estão no bloco de import de `lucide-react`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dois itens navegáveis no grupo Dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → rota SPA | Navegação para /produtos-vendidos e /analise-precos cruza RoleRoute + canAccess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-07 | Broken Access Control | roleAccess.ts | mitigate | Registrar ambas as rotas como OPERATIONAL — sem registro, default-deny redireciona silenciosamente (pitfall Phase 54) |
| T-77-08 | Elevation of Privilege | RoleRoute nas rotas novas | mitigate | Rotas envolvidas em `<RoleRoute>` (checa role da org) + ErrorBoundary — padrão consolidado do projeto |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — RESEARCH.md Package Legitimacy Audit confirma zero instalações |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo
- greps das 3 tasks retornam OK
- Navegação manual (verifier): clicar em "Produtos Vendidos" e "Análise de Preços" no menu abre as páginas (NÃO redireciona para /vendas)
</verification>

<success_criteria>
- Rotas registradas em App.tsx + roleAccess.ts; 2 itens no menu Dashboard.
- Nenhum redirecionamento default-deny ao navegar para as rotas novas.
- Zero pacote novo. NÃO mergear PR nem deployar em prod (entrega em branch).
</success_criteria>

<output>
Create `.planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-03-SUMMARY.md` when done.
</output>
