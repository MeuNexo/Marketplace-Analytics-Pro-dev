---
phase: 77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
  - src/pages/mercadolivre/MLProdutosVendidos.tsx
  - src/pages/mercadolivre/MLAnalisePrecos.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A página Produtos Vendidos mostra painel duplo: lista de marcas/categorias (receita+qtd) à esquerda e, ao selecionar um grupo, os anúncios vendidos daquele grupo à direita"
    - "A página Análise de Preços mostra a evolução do preço praticado (médio/mín/máx) por anúncio com volume sobreposto, granularidade dia/semana/mês, alimentada pela RPC orders_price_timeseries"
    - "Ambas as páginas funcionam nos dois layouts (mobile e desktop)"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      provides: "componente de série temporal de preço (portado do oficial)"
      exports: ["PrecoPraticadoReport", "PriceReportProduct"]
      min_lines: 150
    - path: "src/pages/mercadolivre/MLProdutosVendidos.tsx"
      provides: "página Produtos Vendidos (painel duplo marcas/categorias -> itens)"
      min_lines: 80
    - path: "src/pages/mercadolivre/MLAnalisePrecos.tsx"
      provides: "página Análise de Preços (wrapper de PrecoPraticadoReport)"
      min_lines: 40
  key_links:
    - from: "src/pages/mercadolivre/MLProdutosVendidos.tsx"
      to: "src/hooks/useMLSoldProducts.ts"
      via: "consome allRows do hook + aggregatePvGroups/aggregatePvItems"
      pattern: "useMLSoldProducts"
    - from: "src/pages/mercadolivre/MLAnalisePrecos.tsx"
      to: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      via: "renderiza <PrecoPraticadoReport products mlUserIds fromDate toDate />"
      pattern: "PrecoPraticadoReport"
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "orders_price_timeseries (RPC)"
      via: "supabase.rpc('orders_price_timeseries', ...)"
      pattern: "orders_price_timeseries"
---

<objective>
Criar as duas páginas independentes e portar o componente `PrecoPraticadoReport` do app oficial. Consome a foundation do Plano 01 (hook `useMLSoldProducts`, util `soldProductsAgg`, RPC `orders_price_timeseries`). Este plano NÃO toca App.tsx/roleAccess/ApiSidebar — a fiação fica no Plano 03 (evita conflito de arquivo).

Purpose: Entregar a UI das duas análises reusando padrões consolidados (MLCompras, Phase 73, PrecoPraticadoReport oficial).
Output: 3 arquivos novos em src (1 componente portado + 2 páginas).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-RESEARCH.md
@.planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md
@src/hooks/useMLSoldProducts.ts
@src/components/mercadolivre/anuncios/soldProductsAgg.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plano 02)
- **Componente:** `PrecoPraticadoReport` (+ tipo `PriceReportProduct`) em `src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` (portado do oficial)
- **Página:** `MLProdutosVendidos` (default export) em `src/pages/mercadolivre/MLProdutosVendidos.tsx` — painel duplo marcas/categorias
- **Página:** `MLAnalisePrecos` (default export) em `src/pages/mercadolivre/MLAnalisePrecos.tsx` — wrapper de PrecoPraticadoReport
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Portar PrecoPraticadoReport do app oficial</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <read_first>
    - /root/garment-glow-official/src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx (fonte a portar — porte quase direto)
    - src/components/dashboard/KPICard.tsx (confirmar path de import @/components/dashboard/KPICard)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção PrecoPraticadoReport — imports, Props, chamada RPC)
  </read_first>
  <behavior>
    - Props: { products: PriceReportProduct[]; mlUserIds: string[]; fromDate: string | null; toDate: string | null; request?: { itemId: string; nonce: number } | null }.
    - Seletor de anúncio via Command/Popover; default = request?.itemId ?? products[0]?.id.
    - Ao selecionar item + granularidade (day/week/month) chama supabase.rpc("orders_price_timeseries", { _item_id, _ml_user_ids, _from, _to, _granularity }) e desenha ComposedChart (linhas médio/mín/máx + barras de volume qtd|receita).
    - Toggle volume qtd/receita; 6 KPICards.
    - Cleanup anti-stale com flag cancelled.
  </behavior>
  <action>Copiar o conteúdo de `/root/garment-glow-official/src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` para `src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx`. Ajustes no porte: (1) confirmar que todos os imports resolvem no nosso projeto (`@/components/dashboard/KPICard`, `@/components/ui/command`, `@/components/ui/popover`, `@/components/ui/toggle-group`, `recharts`, `date-fns`, `lucide-react` — todos já instalados, ver RESEARCH.md Environment Availability); (2) manter a chamada `(supabase.rpc as any)("orders_price_timeseries", { _item_id, _ml_user_ids: mlUserIds?.length ? mlUserIds : null, _from: fromDate, _to: toDate, _granularity: granularity })` — passar `_from`/`_to` como strings "YYYY-MM-DD" ou null; (3) manter `SeriesRow.bucket` como string YYYY-MM-DD; (4) NÃO alterar a lógica de gráfico/KPIs. Exportar `PrecoPraticadoReport` e o tipo `PriceReportProduct`.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` exporta `PrecoPraticadoReport` e `PriceReportProduct`
    - Contém `orders_price_timeseries` e `ComposedChart`
    - Contém import `KPICard` de `@/components/dashboard/KPICard`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Componente portado e compilando; imports resolvem no nosso projeto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Página MLProdutosVendidos (painel duplo marcas/categorias)</name>
  <files>src/pages/mercadolivre/MLProdutosVendidos.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLCompras.tsx (analog de página: sticky header, MLPageHeader, layout, dual mobile/desktop)
    - src/components/mercadolivre/analise/AnaliseDashboard.tsx (wiring exato de useMLFilters + MLPeriodPicker: linhas 46-52 e 304-316)
    - src/components/mercadolivre/anuncios/soldProductsAgg.ts (aggregatePvGroups/aggregatePvItems + tipos)
    - src/hooks/useMLSoldProducts.ts (hook de dados)
    - src/contexts/MLInventoryContext.tsx (useMLInventory().items — cross-ref item_id -> category_id/title/thumbnail)
    - src/contexts/MLStoreContext.tsx (resolvedMLUserIds)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção MLProdutosVendidos.tsx — estrutura completa)
  </read_first>
  <behavior>
    - Estado: pvView "marca"|"categoria" (toggle), pvSelected string|null.
    - itemsMap = Map<item_id, inventoryItem> de useMLInventory().items.
    - pvGroups = aggregatePvGroups(allRows, pvView, itemsMap); pvItems = aggregatePvItems(allRows, pvSelected, pvView, itemsMap) quando pvSelected != null.
    - Selecionar um grupo (esquerda) preenche a tabela de anúncios (direita).
    - Loading state enquanto isLoading; estado vazio quando allRows vazio.
  </behavior>
  <action>Criar `MLProdutosVendidos.tsx` (default export `MLProdutosVendidos`). Header sticky no padrão MLCompras.tsx com `<MLPageHeader title="Produtos Vendidos" />` + `MLPeriodPicker` (wiring de `useMLFilters(30)` exatamente como AnaliseDashboard.tsx linhas 46-52/304-316) + ToggleGroup marca/categoria. Contextos: `const { resolvedMLUserIds } = useMLStore()`, `const { items: inventoryItems } = useMLInventory()`. Dados: `useMLSoldProducts({ fromDate: currentFrom, toDate: currentTo, resolvedMLUserIds })`. Montar `itemsMap` com `useMemo` (chave `i.id`). Derivar `pvGroups`/`pvItems` com `useMemo` chamando `aggregatePvGroups`/`aggregatePvItems`. Layout: `grid grid-cols-1 lg:grid-cols-[320px_1fr] gap-4`. Coluna esquerda = lista de grupos como `<button>` (funciona mobile nativo — lição Phase 71/dual-layout), cada botão mostra name + qty + revenue formatado BRL, destaca `pvSelected`. Coluna direita = anúncios do grupo: thumbnail (cross-ref itemsMap.thumbnail), título, vendidos (qty), receita (BRL), estoque (itemsMap.available_quantity), % participação (shareOfGroup). Dual-layout na coluna direita: tabela em `hidden lg:block` + cards em `lg:hidden` (ResponsiveTable NÃO existe no projeto — implementar o padrão de cards mobile manualmente). Categoria usa cross-ref com itemsMap.category_id; anúncio ausente do inventory cai em "Sem categoria".</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/mercadolivre/MLProdutosVendidos.tsx` tem `export default function MLProdutosVendidos` (ou `export default MLProdutosVendidos`)
    - Contém `useMLSoldProducts`, `aggregatePvGroups`, `aggregatePvItems`
    - Contém `<MLPageHeader title="Produtos Vendidos"` e `MLPeriodPicker`
    - Contém ramo mobile e desktop (ex.: `lg:hidden` e `hidden lg:block`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Página de painel duplo criada, consumindo a foundation do Plano 01, dual-layout mobile/desktop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Página MLAnalisePrecos (wrapper de PrecoPraticadoReport)</name>
  <files>src/pages/mercadolivre/MLAnalisePrecos.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLCompras.tsx (estrutura de página + sticky header)
    - src/components/mercadolivre/analise/AnaliseDashboard.tsx (wiring useMLFilters + MLPeriodPicker)
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx (Props que a página alimenta)
    - src/hooks/useMLSoldProducts.ts (reusar para derivar a lista de produtos com vendas)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção MLAnalisePrecos.tsx — estrutura mínima)
  </read_first>
  <behavior>
    - Deriva a lista `products` (id+title) deduplicada por item_id, ordenada por qty vendida desc, a partir de allRows de useMLSoldProducts.
    - Renderiza PrecoPraticadoReport com products, mlUserIds=resolvedMLUserIds, fromDate=currentFrom, toDate=currentTo.
    - request pode ser null (atalho deep-link é OPCIONAL — ver Deferred).
  </behavior>
  <action>Criar `MLAnalisePrecos.tsx` (default export `MLAnalisePrecos`). Header sticky com `<MLPageHeader title="Análise de Preços" />` + `MLPeriodPicker` (mesmo wiring de useMLFilters(30)). `const { resolvedMLUserIds } = useMLStore()`. Dados: `useMLSoldProducts({ fromDate: currentFrom, toDate: currentTo, resolvedMLUserIds })`. Derivar `products` com `useMemo`: dedup por `item_id`, `title = r.titulo ?? item_id`, somar qty, ordenar por qty desc, mapear para `{ id, title }`. Renderizar `<PrecoPraticadoReport products={products} mlUserIds={resolvedMLUserIds} fromDate={currentFrom} toDate={currentTo} request={null} />`. O atalho deep-link `?item=MLB...` via `useSearchParams()` é OPCIONAL — se implementar, derivar `request={ itemId, nonce }` de `searchParams.get("item")`; se não, `request={null}` e registrar como follow-up no SUMMARY (conforme Deferred do CONTEXT.md).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/mercadolivre/MLAnalisePrecos.tsx` tem `export default` de `MLAnalisePrecos`
    - Contém `<PrecoPraticadoReport` e `<MLPageHeader title="Análise de Preços"`
    - Contém `useMLSoldProducts` (derivação da lista products)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Página wrapper criada alimentando PrecoPraticadoReport com produtos do período.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RPC orders_price_timeseries | PrecoPraticadoReport chama a RPC; SECURITY INVOKER preserva RLS |
| client → orders (via useMLSoldProducts) | Query direta autenticada; RLS org-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04 | Elevation of Privilege (IDOR) | PrecoPraticadoReport RPC call | mitigate | RPC é SECURITY INVOKER (criada no Plano 01); passa `_ml_user_ids` das lojas da org; RLS de orders isola |
| T-77-05 | Information Disclosure | MLProdutosVendidos cross-ref inventory | accept | useMLInventory().items já é org-scoped (Context global); cross-ref em memória não expande escopo |
| T-77-06 | Broken Access Control | rotas /produtos-vendidos e /analise-precos | transfer | Registro em roleAccess.ts + App.tsx é responsabilidade do Plano 03 (Wave 3) — sem ele, default-deny |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — RESEARCH.md Package Legitimacy Audit confirma zero instalações |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo após os 3 arquivos
- PrecoPraticadoReport exporta e resolve todos os imports
- MLProdutosVendidos tem ramos mobile e desktop
- Ambas as páginas consomem a foundation do Plano 01 (hook/util/RPC)
</verification>

<success_criteria>
- Duas páginas + componente portado criados e compilando.
- Nenhum toque em App.tsx/roleAccess.ts/ApiSidebar.tsx (fiação no Plano 03) — sem conflito de arquivo.
- Zero pacote novo instalado.
</success_criteria>

<output>
Create `.planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-02-SUMMARY.md` when done. Registrar se o atalho deep-link `?item=` foi implementado ou deferido.
</output>
