---
phase: 77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mercadolivre/anuncios/soldProductsAgg.ts
  - src/components/mercadolivre/anuncios/soldProductsAgg.test.ts
  - src/hooks/useMLSoldProducts.ts
  - supabase/migrations/20260677000000_orders_price_timeseries.sql
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A RPC orders_price_timeseries existe no banco ckcdevcxgvueywivefgx e retorna série temporal de preço por item sem vazar dados de outra org (SECURITY INVOKER + RLS de orders)"
    - "O util soldProductsAgg agrupa pedidos por marca/categoria e por item com % de participação, testado isoladamente"
    - "O hook useMLSoldProducts busca pedidos pagos do período com paginação .range() e respeita guard de resolvedMLUserIds vazio"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/soldProductsAgg.ts"
      provides: "aggregatePvGroups + aggregatePvItems (agregação client-side pura)"
      exports: ["aggregatePvGroups", "aggregatePvItems", "SoldProductRow", "PvGroup", "PvItem"]
      min_lines: 60
    - path: "src/components/mercadolivre/anuncios/soldProductsAgg.test.ts"
      provides: "testes unitários do util de agregação"
      min_lines: 30
    - path: "src/hooks/useMLSoldProducts.ts"
      provides: "hook de query paginada de orders + guard de lojas"
      exports: ["useMLSoldProducts"]
      min_lines: 40
    - path: "supabase/migrations/20260677000000_orders_price_timeseries.sql"
      provides: "RPC orders_price_timeseries adaptada (data_pedido TEXT -> ::date)"
      contains: "CREATE OR REPLACE FUNCTION public.orders_price_timeseries"
  key_links:
    - from: "src/hooks/useMLSoldProducts.ts"
      to: "orders (Supabase table)"
      via: "supabase.from('orders').eq('status','paid') com .range() paginado"
      pattern: "from\\(\"orders\"\\)"
    - from: "supabase/migrations/20260677000000_orders_price_timeseries.sql"
      to: "orders (Supabase table)"
      via: "FROM orders o com o.data_pedido::date"
      pattern: "o\\.data_pedido::date"
---

<objective>
Criar a camada de dados compartilhada das duas páginas: (1) a migration+RPC `orders_price_timeseries` no banco real (portada do oficial, adaptada para `data_pedido` TEXT), aplicada e provada por smoke; (2) o util puro `soldProductsAgg` (com testes) e (3) o hook `useMLSoldProducts` que pagina `orders`. Sem UI, sem rotas — foundation reutilizada pelos Planos 02 e 03.

Purpose: Isolar a única dependência bloqueante (RPC inexistente no nosso projeto) e o util testável antes das páginas. Evita conflito de arquivos com os planos de UI.
Output: 3 arquivos novos em src + 1 migration aplicada em produção (ckcdevcxgvueywivefgx).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-RESEARCH.md
@.planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plano 01)
- **Util:** `aggregatePvGroups`, `aggregatePvItems` + tipos `SoldProductRow`, `PvGroup`, `PvItem` em `src/components/mercadolivre/anuncios/soldProductsAgg.ts`
- **Testes:** `src/components/mercadolivre/anuncios/soldProductsAgg.test.ts`
- **Hook:** `useMLSoldProducts` (+ tipos `UseMLSoldProductsParams`, `UseMLSoldProductsResult`) em `src/hooks/useMLSoldProducts.ts`
- **RPC/migration:** função `public.orders_price_timeseries(_item_id, _ml_user_ids, _from, _to, _granularity)` em `supabase/migrations/20260677000000_orders_price_timeseries.sql` (aplicada no banco `ckcdevcxgvueywivefgx`)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Util puro soldProductsAgg + testes</name>
  <files>src/components/mercadolivre/anuncios/soldProductsAgg.ts, src/components/mercadolivre/anuncios/soldProductsAgg.test.ts</files>
  <read_first>
    - src/components/mercadolivre/anuncios/listingSalesAgg.ts (analog exato — util pura de agregação client-side)
    - src/components/mercadolivre/anuncios/listingSalesAgg.test.ts (padrão de teste vitest do projeto)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção soldProductsAgg.ts — assinaturas completas)
  </read_first>
  <behavior>
    - aggregatePvGroups(rows, "marca", itemsMap): agrupa por row.marca; marca null vira chave "" com name "Sem marca"; soma qty e revenue; ordena por revenue desc.
    - aggregatePvGroups(rows, "categoria", itemsMap): usa itemsMap.get(item_id).category_id como chave; ausente vira "Sem categoria".
    - aggregatePvItems(rows, pvSelected, "marca", itemsMap): filtra rows do grupo, agrupa por item_id, calcula shareOfGroup = revenue/totalRevenue do grupo (0 quando totalRev=0); título preferindo itemsMap.title, fallback row.titulo, fallback item_id.
    - Caso vazio: rows=[] retorna [].
  </behavior>
  <action>Criar `soldProductsAgg.ts` exportando `SoldProductRow` (item_id, titulo, marca, quantidade, receita_bruta, data_pedido, ml_user_id), `PvGroup` (key, name, qty, revenue), `PvItem` (item_id, title, qty, revenue, shareOfGroup) e as funções puras `aggregatePvGroups(rows, pvView, itemsMap)` e `aggregatePvItems(rows, pvSelected, pvView, itemsMap)` conforme as assinaturas do 77-PATTERNS.md. `itemsMap` é `Map<string, { category_id?: string | null; title?: string; thumbnail?: string }>`. Zero import de React/Supabase/rede (util 100% pura). Nunca usar `new Date()` sobre `data_pedido`; se precisar da parte data, usar `slice(0,10)`. Criar `soldProductsAgg.test.ts` no padrão do `listingSalesAgg.test.ts`, cobrindo: agrupamento por marca, fallback "Sem marca", agrupamento por categoria via itemsMap, shareOfGroup somando 1 dentro do grupo, e caso vazio.</action>
  <verify>
    <automated>npx vitest run src/components/mercadolivre/anuncios/soldProductsAgg.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/mercadolivre/anuncios/soldProductsAgg.ts` exporta `aggregatePvGroups` e `aggregatePvItems`
    - `soldProductsAgg.ts` NÃO contém `import` de `react`, `@/integrations/supabase` nem `new Date(`
    - `npx vitest run src/components/mercadolivre/anuncios/soldProductsAgg.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Util puro criado e coberto por testes verdes; sem dependências de React/rede.</done>
</task>

<task type="auto">
  <name>Task 2: Hook useMLSoldProducts (query paginada de orders)</name>
  <files>src/hooks/useMLSoldProducts.ts</files>
  <read_first>
    - src/components/mercadolivre/anuncios/useMLListingSales.ts (analog exato — paginação .range(), guard, cleanup cancelled)
    - src/components/mercadolivre/anuncios/soldProductsAgg.ts (tipo SoldProductRow — reusar como shape do row)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção useMLSoldProducts.ts — loop completo)
  </read_first>
  <action>Criar `useMLSoldProducts({ fromDate, toDate, resolvedMLUserIds })` retornando `{ allRows, isLoading, error }`. Reusar o tipo `SoldProductRow` de `soldProductsAgg.ts`. Query: `supabase.from("orders").select("item_id, titulo, marca, quantidade, receita_bruta, data_pedido, ml_user_id").eq("status","paid").gte("data_pedido", fromDate).lte("data_pedido", toDate).in("ml_user_id", resolvedMLUserIds).range(offset, offset + PAGE - 1)` com `PAGE = 1000` e `MAX_ROWS = 50000`. `data_pedido` é TEXT — passar strings "YYYY-MM-DD" direto (NÃO objeto Date). Guard: se `!fromDate || !toDate || !resolvedMLUserIds.length` então zerar `allRows` e não buscar (evita `.in("ml_user_id", [])`). Loop de paginação acumula até `page.length < PAGE`. Cleanup anti-stale com flag `cancelled` e `return () => { cancelled = true; }`. Deps do useEffect: `[fromDate, toDate, resolvedMLUserIds]`.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useMLSoldProducts.ts` exporta `useMLSoldProducts`
    - Contém `.eq("status", "paid")` e `.range(` (paginação)
    - Contém guard `resolvedMLUserIds.length` antes do fetch e `cancelled` no cleanup
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Hook criado seguindo padrão Phase 73; tsc limpo.</done>
</task>

<task type="auto" gate="blocking-human">
  <name>Task 3 [BLOCKING]: Migration orders_price_timeseries — arquivo + push no banco real + smoke</name>
  <files>supabase/migrations/20260677000000_orders_price_timeseries.sql</files>
  <read_first>
    - /root/garment-glow-official/supabase/migrations/20260630170000_orders_price_timeseries.sql (SQL de referência a portar)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-RESEARCH.md (seção "Migration orders_price_timeseries" — SQL adaptado completo e validado)
    - .planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-PATTERNS.md (seção da migration — SQL final)
  </read_first>
  <action>Criar `supabase/migrations/20260677000000_orders_price_timeseries.sql` com a função `public.orders_price_timeseries(_item_id text, _ml_user_ids text[] DEFAULT NULL, _from date DEFAULT NULL, _to date DEFAULT NULL, _granularity text DEFAULT 'day')` LANGUAGE sql STABLE, retornando TABLE(bucket date, preco_medio numeric, preco_min numeric, preco_max numeric, qtd bigint, total numeric, orders bigint). ADAPTAÇÃO obrigatória vs. oficial: `o.data_pedido::date` (nosso schema usa TEXT), status IN ('paid','shipped','delivered'), GROUP BY 1 sobre date_trunc por granularidade (day/week/month). SECURITY INVOKER (NÃO DEFINER, sem parâmetro org — RLS de orders isola por organização; lição anti-IDOR Phase 63/69). Depois de escrever o arquivo, a APLICAÇÃO no banco `ckcdevcxgvueywivefgx` é feita pelo ORQUESTRADOR via MCP `mcp__claude_ai_Supabase__apply_migration` (o executor NÃO tem o token; sinalizar no SUMMARY que a migration precisa ser aplicada). Smoke pós-push (orquestrador via MCP `execute_sql`): `SELECT * FROM orders_price_timeseries('MLB0000000000', NULL, NULL, NULL, 'day') LIMIT 1;` deve retornar sem erro "function does not exist". NÃO aplicar via SQL Editor (proibido por feedback do projeto).</action>
  <verify>
    <automated>test -f supabase/migrations/20260677000000_orders_price_timeseries.sql &amp;&amp; grep -q "CREATE OR REPLACE FUNCTION public.orders_price_timeseries" supabase/migrations/20260677000000_orders_price_timeseries.sql &amp;&amp; grep -q "data_pedido::date" supabase/migrations/20260677000000_orders_price_timeseries.sql &amp;&amp; echo OK</automated>
    <human-check>Orquestrador confirma via MCP execute_sql que a RPC existe no banco ckcdevcxgvueywivefgx e responde sem erro "function does not exist".</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260677000000_orders_price_timeseries.sql` existe
    - Contém `CREATE OR REPLACE FUNCTION public.orders_price_timeseries`
    - Contém `o.data_pedido::date` (adaptação TEXT->date)
    - NÃO contém `SECURITY DEFINER` (must be INVOKER — default; sem parâmetro org)
    - Smoke MCP: `SELECT * FROM orders_price_timeseries('MLB0000000000', NULL, NULL, NULL, 'day') LIMIT 1;` executa sem erro de função inexistente
  </acceptance_criteria>
  <done>Arquivo de migration criado e função aplicada no banco real (via orquestrador MCP), provada por smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (orders) | Query direta autenticada cruza aqui; RLS de `orders` isola por organização |
| client → RPC orders_price_timeseries | Chamada RPC cruza aqui; SECURITY INVOKER preserva RLS do chamador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Elevation of Privilege (IDOR) | RPC orders_price_timeseries | mitigate | SECURITY INVOKER sem parâmetro org — RLS de `orders` filtra por organização do chamador (lição Phase 63/69); NUNCA DEFINER com org param |
| T-77-02 | Tampering (dados incompletos) | useMLSoldProducts | mitigate | Loop `.range()` obrigatório com MAX_ROWS=50000 — PostgREST trunca em 1000 sem aviso |
| T-77-03 | Information Disclosure | useMLSoldProducts guard | mitigate | Guard de `resolvedMLUserIds` vazio + `.in("ml_user_id", ...)` restringe às lojas da org |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — RESEARCH.md Package Legitimacy Audit confirma zero instalações |
</threat_model>

<verification>
- `npx vitest run src/components/mercadolivre/anuncios/soldProductsAgg.test.ts` verde
- `npx tsc --noEmit` limpo
- Arquivo de migration presente com a função e o cast `::date`
- Smoke MCP da RPC no banco real (orquestrador) sem erro de função inexistente
</verification>

<success_criteria>
- Util puro + testes verdes; hook criado; migration escrita e aplicada no banco real com smoke ok.
- Zero pacote novo instalado. Zero rota/UI tocada (sem conflito com Planos 02/03).
</success_criteria>

<output>
Create `.planning/phases/77-pagina-analise-de-anuncios-produtos-vendidos-e-analise-de-pr/77-01-SUMMARY.md` when done. No SUMMARY registrar explicitamente que a migration precisa ser (ou foi) aplicada via MCP apply_migration no projeto `ckcdevcxgvueywivefgx`.
</output>