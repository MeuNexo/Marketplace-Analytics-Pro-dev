---
phase: 41-veracidade-total
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - supabase/migrations/20260612140000_ml_billing_monthly.sql
  - supabase/functions/sync-ml-billing/index.ts
  - src/hooks/useMLBilling.ts
  - src/components/mercadolivre/MLCostCard.tsx
  - src/pages/MercadoLivre.tsx
  - src/integrations/supabase/types.ts
autonomous: false
requirements: [DATA-04]
user_setup: []
must_haves:
  truths:
    - "Linha 'Frete' no breakdown usa CFFE real da billing API quando disponivel para o periodo (DATA-04)"
    - "Nova linha 'Parcelamento (CFONPN)' existe no breakdown e mostra valor quando ha billing (DATA-04)"
    - "Indicador visual de fonte 'billing' vs 'estimado' aparece junto ao Frete (DATA-04)"
    - "Tabela ml_billing_monthly existe e e populada pela EF sync-ml-billing escopada por org+ml_user_id+period_month"
  artifacts:
    - path: "supabase/migrations/20260612140000_ml_billing_monthly.sql"
      provides: "Tabela ml_billing_monthly + RLS is_org_member"
      contains: "ml_billing_monthly"
    - path: "supabase/functions/sync-ml-billing/index.ts"
      provides: "EF que busca ML Billing API e upserta CFFE/CFONPN"
      contains: "fetchBillingPeriod"
    - path: "src/hooks/useMLBilling.ts"
      provides: "Hook useMLBilling(periodMonth) lendo ml_billing_monthly"
      exports: ["useMLBilling"]
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "Linha CFONPN + indicador billing/estimado"
      contains: "CFONPN"
  key_links:
    - from: "supabase/functions/sync-ml-billing/index.ts"
      to: "ml_billing_monthly"
      via: "upsert onConflict org+ml_user_id+period_month"
      pattern: "ml_billing_monthly"
    - from: "src/pages/MercadoLivre.tsx"
      to: "useMLBilling"
      via: "billingMonth = monthlyFrom.substring(0,7)"
      pattern: "useMLBilling\\("
    - from: "src/components/mercadolivre/MLCostCard.tsx"
      to: "billing props"
      via: "cfonpn + billingSource props"
      pattern: "billingSource"
---

<objective>
Entregar DATA-04: CFFE real (linha "Frete") e nova linha "Parcelamento (CFONPN)" no breakdown de custos de /vendas, vindos da ML Billing API real, com indicador de fonte "billing" vs "estimado".

Cria a tabela `ml_billing_monthly`, a edge function `sync-ml-billing` (fluxo de 2 chamadas confirmado em producao via nexo-mcp: monthly/periods -> period key -> summary/details -> charges[] CFFE/CFONPN), o hook `useMLBilling` e estende `MLCostCard` com a linha CFONPN e o indicador de fonte. CFFE substitui o frete estimado quando billing disponivel; fallback para frete de orders quando indisponivel (ex.: conta sem Full -> 404).

Purpose: Tornar visiveis ~R$15,9k/mes de CFONPN (hoje R$0) e ~R$2,5k/mes de extras CFFE que o frete de orders nao captura.
Output: tabela + EF deployadas em producao (ckcdevcxgvueywivefgx), hook + UI no MLCostCard.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-veracidade-total/41-CONTEXT.md
@.planning/phases/41-veracidade-total/41-RESEARCH.md
@.planning/phases/41-veracidade-total/41-PATTERNS.md
@supabase/functions/sync-ml-orders/index.ts
@src/hooks/useMLCostWaterfall.ts
@src/components/mercadolivre/MLCostCard.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (Plano 41-02)

- Tabela `public.ml_billing_monthly` (id, organization_id, ml_user_id, period_month, charges JSONB, resumo JSONB, synced_at; UNIQUE org+ml_user_id+period_month; RLS is_org_member)
- Edge function `sync-ml-billing` (body `{ ml_user_id, period_month }`; helper `fetchBillingPeriod`; upsert em ml_billing_monthly)
- Hook `useMLBilling(periodMonth: string)` em `src/hooks/useMLBilling.ts` (interface `MLBillingData`)
- MLCostCard props novas: `cfonpn?: number | null`, `billingSource?: boolean`; linha "Parcelamento (CFONPN)"; indicador "billing"/"estimado"; import `CreditCard` de lucide-react
- Tipos em `src/integrations/supabase/types.ts` para ml_billing_monthly (adicionados manualmente, padrao do projeto)
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration ml_billing_monthly + tipos + [BLOCKING] apply_migration</name>
  <files>supabase/migrations/20260612140000_ml_billing_monthly.sql, src/integrations/supabase/types.ts</files>
  <read_first>
    - .planning/phases/41-veracidade-total/41-PATTERNS.md (secao migration ml_billing_monthly — DDL + RLS is_org_member)
    - .planning/phases/41-veracidade-total/41-RESEARCH.md (secao "DATA-04: ML Billing API" — schema da tabela)
    - supabase/migrations/20260604130000_fix_batch_upsert_orders_seller_id_cast.sql (estrutura/formatacao de migration do projeto)
    - supabase/migrations/20260423153544_937751fe-2ac3-4e2e-ba69-b7639fc1475e.sql (linhas ~269-307 — padrao de policy is_org_member org-scoped)
    - .planning/phases/41-veracidade-total/41-CONTEXT.md (secao "Ambiente e deploy" — apply_migration via MCP no ckcdevcxgvueywivefgx, PROIBIDO supabase db push)
  </read_first>
  <action>
    Criar `supabase/migrations/20260612140000_ml_billing_monthly.sql` com:
    - `CREATE TABLE public.ml_billing_monthly` com colunas: `id UUID DEFAULT gen_random_uuid() PRIMARY KEY`, `organization_id UUID NOT NULL REFERENCES organizations(id)`, `ml_user_id TEXT NOT NULL`, `period_month TEXT NOT NULL` (formato YYYY-MM), `charges JSONB`, `resumo JSONB`, `synced_at TIMESTAMPTZ DEFAULT now()`, e `UNIQUE (organization_id, ml_user_id, period_month)`.
    - `ALTER TABLE public.ml_billing_monthly ENABLE ROW LEVEL SECURITY;`
    - Policy `org_member_billing` FOR ALL USING (`public.is_org_member(auth.uid(), organization_id)`) — assinatura posicional, identica aos analogs em 20260423153544.
    - Index opcional em (organization_id, ml_user_id, period_month) ja coberto pelo UNIQUE.
    Em seguida atualizar `src/integrations/supabase/types.ts` adicionando manualmente a entrada de `ml_billing_monthly` (Row/Insert/Update) — padrao do projeto (tipos adicionados a mao, nao regenerados).

    [BLOCKING] Apos escrever o arquivo .sql, aplicar a migration em producao via MCP Supabase `apply_migration` no project `ckcdevcxgvueywivefgx`, usando como nome o nome do arquivo sem timestamp (`ml_billing_monthly`). NUNCA usar `supabase db push` (CLI local linkado no projeto errado gionpsuunfkkzzjdubfy). Esta task NAO pode ser considerada concluida sem a migration aplicada na base live — build/tsc passam sem isso (tipos vem do arquivo, nao da base), gerando falso-positivo.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260612140000_ml_billing_monthly.sql && grep -q "ml_billing_monthly" supabase/migrations/20260612140000_ml_billing_monthly.sql && grep -q "is_org_member" supabase/migrations/20260612140000_ml_billing_monthly.sql && echo "migration file OK"</automated>
    <automated>grep -q "ml_billing_monthly" src/integrations/supabase/types.ts && echo "types OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo supabase/migrations/20260612140000_ml_billing_monthly.sql existe e contem CREATE TABLE ml_billing_monthly, ENABLE ROW LEVEL SECURITY e policy com is_org_member
    - Migration APLICADA em producao via MCP apply_migration no ckcdevcxgvueywivefgx (confirmar via MCP list_tables que a tabela ml_billing_monthly existe na base live)
    - src/integrations/supabase/types.ts contem entrada ml_billing_monthly
    - `npx tsc --noEmit` sai com codigo 0
    - NENHUM uso de `supabase db push`
  </acceptance_criteria>
  <done>Tabela ml_billing_monthly criada no arquivo e aplicada na base live ckcdevcxgvueywivefgx; tipos atualizados; tsc limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Edge function sync-ml-billing + deploy via MCP</name>
  <files>supabase/functions/sync-ml-billing/index.ts</files>
  <read_first>
    - supabase/functions/sync-ml-orders/index.ts (imports Deno linhas 1-3; corsHeaders; auth isServiceRole+JWT linhas 390-467; token lookup ml_tokens linhas 440-453; numeric seller_id /users/me linhas 474-475; mlFetch helper linhas 84-95)
    - .planning/phases/41-veracidade-total/41-PATTERNS.md (secao sync-ml-billing — todos os patterns: fetchBillingPeriod, upsert, error handling, serve entrypoint)
    - .planning/phases/41-veracidade-total/41-RESEARCH.md (secao "DATA-04: ML Billing API" — fluxo 2 calls, parse CFFE/CFONPN, Pitfalls 1/2/6/7)
    - .planning/phases/41-veracidade-total/41-CONTEXT.md (secao "Ambiente e deploy" — deploy EF via MCP deploy_edge_function ou npx --project-ref ckcdevcxgvueywivefgx)
  </read_first>
  <action>
    Criar `supabase/functions/sync-ml-billing/index.ts` replicando a estrutura de `sync-ml-orders`:
    - Imports Deno std@0.168.0, @supabase/supabase-js@2, zod@v3.22.4.
    - corsHeaders identicos ao sync-ml-orders.
    - Body schema zod: `{ ml_user_id: z.string().min(1), period_month: z.string().regex(/^\d{4}-\d{2}$/) }` — retornar 400 em parse invalido (mitiga Tampering T-41-02-03).
    - Auth: `isServiceRole = token === SUPABASE_SERVICE_ROLE_KEY`; se nao service role, validar JWT via `supabaseAdmin.auth.getUser(token)` (401 se invalido); checar `is_org_member(_user_id, _org_id)` quando ha organization_id (403 se nao membro); skip org check para service role.
    - Token lookup: `ml_tokens.select("access_token, organization_id, seller_id").eq("ml_user_id", ml_user_id).not("access_token","is",null).limit(1).maybeSingle()` -> 404 se sem token. Capturar `organizationId = tokenRow.organization_id`; se null, logar e pular upsert sem lancar erro fatal (Pitfall 6).
    - Numeric seller id: `mlFetch("/users/me", accessToken)` -> `String(mlUser.id)` (Pitfall 7).
    - Helper `fetchBillingPeriod(token, sellerId, periodMonth)` exatamente como no PATTERNS/RESEARCH: GET `/billing/integration/monthly/periods?seller_id=...&group=ML&document_type=BILL`; se !ok retornar null (404 = sem Full, Pitfall 2); achar period com matching `k.startsWith(month) || k.substring(0,7)===month || k.startsWith(monthCompact)` (Pitfall 1); GET `/billing/integration/periods/key/{key}/summary/details?seller_id=...&document_type=BILL`; parse `bill_includes.charges`; CFFE = soma de charges cujo `type` inclui "CFFE", CFONPN = soma cujo `type` inclui "CFONPN"; retornar `{ cffe, cfonpn, charges: charges.map(c => ({type, label, amount: Number(c.amount ?? 0)})) }`. Logar os `type` encontrados (mitiga risco A2 do research).
    - Se `fetchBillingPeriod` retornar null, upsert com charges=[] e resumo={cffe:0,cfonpn:0} OU pular upsert e retornar `{ success: true, billing: null }` — escolher pular upsert (nao sobrescrever billing anterior com zeros).
    - Upsert em `ml_billing_monthly` com onConflict `organization_id,ml_user_id,period_month`, gravando charges, resumo `{ cffe, cfonpn, synced_at }` e synced_at.
    - Token ML NUNCA deve aparecer em logs ou na resposta (mitiga T-41-02-02).
    - Top-level catch retornando 500 com `{ success:false, error: message }` (licao Phase 38 — nunca engolir erro).
    - Usar `AbortSignal.timeout(15_000)` nativo Deno (Pitfall 5).

    Deploy: via MCP `deploy_edge_function` no project `ckcdevcxgvueywivefgx` (ou `npx supabase functions deploy sync-ml-billing --project-ref ckcdevcxgvueywivefgx`). NAO setar verify_jwt=false no dashboard — a EF aceita service role E user JWT internamente.
  </action>
  <verify>
    <automated>test -f supabase/functions/sync-ml-billing/index.ts && grep -q "fetchBillingPeriod" supabase/functions/sync-ml-billing/index.ts && grep -q "ml_billing_monthly" supabase/functions/sync-ml-billing/index.ts && echo "EF file OK"</automated>
    <automated>grep -q "isServiceRole" supabase/functions/sync-ml-billing/index.ts && grep -q "is_org_member" supabase/functions/sync-ml-billing/index.ts && grep -q "billing/integration/monthly/periods" supabase/functions/sync-ml-billing/index.ts && echo "auth+endpoint OK"</automated>
    <automated>grep -vq "access_token" <(grep -i "console.log\|console.error" supabase/functions/sync-ml-billing/index.ts) && echo "no token in logs OK" || echo "REVISAR logs"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo supabase/functions/sync-ml-billing/index.ts existe com helper fetchBillingPeriod, auth dual-path (isServiceRole + JWT + is_org_member), body zod com regex YYYY-MM, upsert em ml_billing_monthly
    - EF deployada em ckcdevcxgvueywivefgx (confirmar via MCP que a function aparece ACTIVE)
    - Smoke: invocar EF com `{ ml_user_id: "<pe-vermeio>", period_month: "2026-04" }` retorna 200 e popula 1 linha em ml_billing_monthly (validar via MCP execute_sql: `SELECT resumo FROM ml_billing_monthly WHERE period_month='2026-04'`)
    - Nenhum `console.log`/`console.error` imprime access_token
    - verify_jwt NAO setado como false (auth tratada no codigo)
  </acceptance_criteria>
  <done>EF sync-ml-billing criada, deployada em producao e validada via smoke contra a conta Pe Vermeio para Abril/2026.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Hook useMLBilling + linha CFONPN/indicador no MLCostCard + wiring em MercadoLivre.tsx</name>
  <files>src/hooks/useMLBilling.ts, src/components/mercadolivre/MLCostCard.tsx, src/pages/MercadoLivre.tsx</files>
  <read_first>
    - src/hooks/useMLCostWaterfall.ts (padrao de hook: useQuery, queryKey com orgId+resolvedMLUserIds+periodo, enabled, staleTime)
    - src/components/mercadolivre/MLCostCard.tsx (interface CostWaterfallCardProps linhas 21-36; array lines linhas 49-97; linha "Frete" linhas 67-73)
    - src/pages/MercadoLivre.tsx (monthlyFrom linha ~160; uso de costWaterfallData/MLCostCard ~linha 580; props passadas ao MLCostCard)
    - .planning/phases/41-veracidade-total/41-PATTERNS.md (secoes useMLBilling, MLCostCard modify, MercadoLivre.tsx wiring — interface MLBillingData, linha CFONPN, indicador billing/estimado)
    - .planning/phases/41-veracidade-total/41-RESEARCH.md (Open Question 3 — CFFE como "Frete ML" quando disponivel, fallback total_frete)
  </read_first>
  <action>
    1. Criar `src/hooks/useMLBilling.ts` seguindo o padrao de useMLCostWaterfall: exporta interface `MLBillingData` (`cffe: number`, `cfonpn: number`, `charges: Array<{type,label,amount}>`, `resumo`, `synced_at`) e funcao `useMLBilling(periodMonth: string)` que usa useQuery com queryKey `["ml","billing", orgId, resolvedMLUserIds, periodMonth]`, le `ml_billing_monthly` filtrando por organization_id (currentOrg.id), `in(ml_user_id, resolvedMLUserIds)` e `eq(period_month, periodMonth)` com `.maybeSingle()`, retorna null quando sem dados, `enabled: !!orgId && resolvedMLUserIds.length>0 && !!periodMonth`, `staleTime: 30*60*1000`. Resolve cffe/cfonpn de `data.resumo`.
    2. Estender `MLCostCard`: adicionar props `cfonpn?: number | null` e `billingSource?: boolean` a `CostWaterfallCardProps`. Importar `CreditCard` de lucide-react (juntar a import existente linha 3). Apos a linha "Frete" no array `lines`, inserir condicionalmente (`cfonpn !== undefined ? [{...}] : []`) a linha "Parcelamento (CFONPN)" com icon CreditCard violet, value `cfonpn ?? null`, base gross_revenue. Adicionar `cfonpn` ao calculo de `operationalCosts` (somar `(cfonpn ?? 0)`). Na renderizacao da label "Frete", quando `billingSource` definido, anexar `<span className="text-[9px] text-muted-foreground/60 ml-1">{billingSource ? "billing" : "estimado"}</span>`.
    3. Wiring em MercadoLivre.tsx: computar `billingMonth = monthlyFrom.substring(0,7)` (YYYY-MM); `const { data: billingData } = useMLBilling(billingMonth);`. No `<MLCostCard ...>`, passar `frete={billingData?.cffe ?? costWaterfallData?.total_frete ?? <freteAtual>}`, `cfonpn={billingData?.cfonpn ?? null}` e `billingSource={!!billingData}`. Preservar o fallback existente de frete quando billingData ausente.
    4. `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/hooks/useMLBilling.ts && grep -q "useMLBilling" src/hooks/useMLBilling.ts && grep -q "ml_billing_monthly" src/hooks/useMLBilling.ts && echo "hook OK"</automated>
    <automated>grep -q "cfonpn" src/components/mercadolivre/MLCostCard.tsx && grep -q "billingSource" src/components/mercadolivre/MLCostCard.tsx && grep -q "CreditCard" src/components/mercadolivre/MLCostCard.tsx && echo "card OK"</automated>
    <automated>grep -q "useMLBilling" src/pages/MercadoLivre.tsx && grep -q "billingSource" src/pages/MercadoLivre.tsx && echo "wiring OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useMLBilling.ts exporta useMLBilling e MLBillingData, lendo ml_billing_monthly escopado por org+resolvedMLUserIds+period_month
    - MLCostCard tem props cfonpn e billingSource; linha "Parcelamento (CFONPN)" no array lines; cfonpn somado em operationalCosts; indicador "billing"/"estimado" junto ao Frete
    - MercadoLivre.tsx chama useMLBilling(billingMonth) e passa frete=CFFE-com-fallback, cfonpn e billingSource ao MLCostCard
    - `npx tsc --noEmit` sai com codigo 0
  </acceptance_criteria>
  <done>Hook useMLBilling criado, MLCostCard com linha CFONPN + indicador, MercadoLivre.tsx wirado; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 4: Commit + push do frontend de billing</name>
  <files>src/hooks/useMLBilling.ts, src/components/mercadolivre/MLCostCard.tsx, src/pages/MercadoLivre.tsx, src/integrations/supabase/types.ts, supabase/functions/sync-ml-billing/index.ts, supabase/migrations/20260612140000_ml_billing_monthly.sql</files>
  <read_first>
    - .planning/phases/41-veracidade-total/41-CONTEXT.md (secao "Ambiente e deploy")
  </read_first>
  <action>
    `git add` dos arquivos do plano (migration, EF, hook, MLCostCard, MercadoLivre.tsx, types.ts), commit `feat(41): DATA-04 billing CFFE/CFONPN real (tabela + EF sync-ml-billing + useMLBilling + MLCostCard)` e `git push origin main` para o Vercel auto-deployar o frontend. A migration ja foi aplicada na base (Task 1) e a EF ja foi deployada (Task 2); o push aqui e para o frontend.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -qi "DATA-04\|billing" && echo "commit OK"</automated>
  </verify>
  <acceptance_criteria>
    - Commit criado contendo os arquivos de DATA-04
    - `git push origin main` executado sem erro
  </acceptance_criteria>
  <done>Frontend de billing commitado e pushado; Vercel auto-deploy disparado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    DATA-04 completo: tabela ml_billing_monthly + EF sync-ml-billing (deployadas em ckcdevcxgvueywivefgx) + hook useMLBilling + MLCostCard com CFFE real na linha Frete, nova linha "Parcelamento (CFONPN)" e indicador "billing"/"estimado".
  </what-built>
  <how-to-verify>
    1. Garantir que a EF foi rodada para a conta Pe Vermeio nos meses corrente e anterior (apos Vercel deploy, abrir /vendas dispara leitura; se vazio, invocar a EF manualmente).
    2. Abrir /vendas no mes corrente -> card "Custos" deve mostrar a linha "Frete" com indicador "billing" (quando ha billing) e uma nova linha "Parcelamento (CFONPN)" com valor.
    3. Conferir contra referencia Nexo Abril/2026 (se filtrar Abril): CFFE ~R$40k, CFONPN ~R$15,9k para o seller 1639558873.
    4. Conta sem Full (427063369) deve cair em fallback "estimado" sem quebrar o card.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva divergencia (linha ausente, valor errado, fallback quebrado)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> EF sync-ml-billing | invocacao por usuario (JWT) ou cron (service role) — dual-path auth |
| EF -> ML Billing API | token ML server-side; nunca exposto ao cliente |
| browser -> ml_billing_monthly (read) | leitura escopada por organization_id via RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Information Disclosure | ml_billing_monthly cross-org read | mitigate | RLS policy org_member_billing com is_org_member(auth.uid(), organization_id); hook usa resolvedMLUserIds escopado |
| T-41-02-02 | Information Disclosure | token ML em logs/resposta | mitigate | EF usa service_role; token nunca vai ao frontend nem a logs; helper mlFetch nao loga Authorization |
| T-41-02-03 | Tampering | period_month malformado no body | mitigate | zod regex /^\d{4}-\d{2}$/ -> 400 em parse invalido |
| T-41-02-04 | Spoofing/Elevation | chamada nao autenticada a EF | mitigate | isServiceRole==serviceKey OU JWT valido via auth.getUser; is_org_member 403; verify_jwt nao desabilitado |
| T-41-02-05 | Denial of Service | rate limit ML Billing API | accept | research A1: 4 chamadas/sync; AbortSignal.timeout(15s); 404 retorna null sem retry agressivo |
| T-41-02-SC | Tampering | npm/pip/cargo installs | mitigate | nenhum pacote novo (research: Package Legitimacy Audit vazio) |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erros
- MCP list_tables confirma ml_billing_monthly na base live ckcdevcxgvueywivefgx
- Smoke EF: invocar com Pe Vermeio + 2026-04 retorna 200 e popula resumo.cffe/cfonpn
- Confirmacao visual de Wesley do card com CFFE billing + linha CFONPN + indicador (checkpoint)
</verification>

<success_criteria>
- Linha Frete usa CFFE real quando billing disponivel; fallback estimado quando ausente (DATA-04)
- Nova linha "Parcelamento (CFONPN)" com valor (DATA-04)
- Indicador "billing" vs "estimado" visivel (DATA-04)
- Tabela + EF deployadas e validadas em producao
- tsc limpo
</success_criteria>

<output>
Create `.planning/phases/41-veracidade-total/41-02-SUMMARY.md` when done
</output>
