---
phase: 41-veracidade-total
plan: 03
type: execute
wave: 3
depends_on: ["41-01", "41-02"]
files_modified:
  - src/pages/mercadolivre/MLAnuncios.tsx
autonomous: false
requirements: [DATA-05, DATA-06]
user_setup: []
must_haves:
  truths:
    - "Comissao em /anuncios vem da API ML real (commCache via listing_prices), nao de LISTING_TYPE_RATES hardcoded, quando o item esta no cache (DATA-05)"
    - "commCache e populado para itens filtrados independentemente de columnView (DATA-05)"
    - "KPIs de comissao/frete/CMV/impostos de /vendas, /financeiro e /anuncios batem entre si e com a referencia Nexo Abril/2026 (DATA-06)"
  artifacts:
    - path: "src/pages/mercadolivre/MLAnuncios.tsx"
      provides: "commCache prioritario sobre LISTING_TYPE_RATES; guard columnView removido na populacao"
      contains: "commCache"
  key_links:
    - from: "src/pages/mercadolivre/MLAnuncios.tsx"
      to: "ml-precos-custos (listing_prices)"
      via: "commCache useEffect sem guard columnView"
      pattern: "commCache"
---

<objective>
Entregar DATA-05 (comissao real por anuncio em /anuncios via API ML, fim do LISTING_TYPE_RATES como fonte) e DATA-06 (consistencia cruzada de KPIs entre /vendas, /financeiro e /anuncios, validada contra a referencia Nexo Abril/2026).

DATA-05 e cirurgico: o `commCache` (via EF ml-precos-custos -> listing_prices, sale_fee real) ja existe e ja e usado no call site, mas (a) so e populado quando `columnView === "financeiro"` e (b) `getCommissionRate()` ainda cai em LISTING_TYPE_RATES quando o cache nao tem o item. Ajustar para que o cache seja populado para todos os itens filtrados e que o call site prefira o cache. O objeto LISTING_TYPE_RATES PERMANECE (ainda usado por funcoes mock de graficos em financialMockData.ts — cleanup e Phase 46).

DATA-06 e auditoria: confirmar que /vendas e /financeiro ja usam useMLCostWaterfall como fonte unica (research confirma) e validar numericamente os totais contra a referencia Nexo (comissao R$39,2k, CFFE R$40k, CFONPN R$15,9k para Abril/2026, seller 1639558873).

Purpose: Eliminar a ultima fonte hardcoded de comissao e provar que as tres paginas contam a mesma historia financeira.
Output: MLAnuncios.tsx com commCache prioritario; relatorio de auditoria de consistencia no SUMMARY.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-veracidade-total/41-CONTEXT.md
@.planning/phases/41-veracidade-total/41-RESEARCH.md
@.planning/phases/41-veracidade-total/41-PATTERNS.md
@src/pages/mercadolivre/MLAnuncios.tsx
@src/pages/mercadolivre/MLFinanceiro.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (Plano 41-03)

- `src/pages/mercadolivre/MLAnuncios.tsx` — useEffect de populacao do commCache sem o guard `columnView === "financeiro"` (so `!filteredItemKey` permanece); call site usando commCache prioritario sobre getCommissionRate/LISTING_TYPE_RATES
- Relatorio de auditoria de consistencia DATA-06 (no SUMMARY): comparacao /vendas x /financeiro x /anuncios x referencia Nexo Abril/2026
- (LISTING_TYPE_RATES NAO removido — permanece em financialMockData.ts para graficos mock)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: DATA-05 — commCache prioritario sobre LISTING_TYPE_RATES em /anuncios</name>
  <files>src/pages/mercadolivre/MLAnuncios.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLAnuncios.tsx (getCommissionRate linhas 58-62; commCache state linha 590; useEffect de populacao linhas 820-843 com guard columnView; call sites linhas 1318-1321 e 1496-1497)
    - supabase/functions/ml-precos-custos/index.ts (endpoint listing_prices, resposta sale_fee_amount/percentage_fee)
    - .planning/phases/41-veracidade-total/41-PATTERNS.md (secao MLAnuncios.tsx modify — guard a remover, call site ja correto, "NAO deletar LISTING_TYPE_RATES")
    - .planning/phases/41-veracidade-total/41-RESEARCH.md (secao "DATA-05" e Pitfall 4 — nao deletar LISTING_TYPE_RATES; so trocar o uso em getCommissionRate via cache)
  </read_first>
  <action>
    1. No useEffect de populacao do commCache (linha ~820), remover o guard `columnView !== "financeiro"`, mantendo apenas `if (!filteredItemKey) return;`. Assim o commCache (comissao real via listing_prices) e populado para todos os itens filtrados, nao so na coluna financeiro — necessario para que a comissao exibida seja sempre a real (DATA-05) e consistente com /vendas (DATA-06).
    2. Garantir que os call sites (linhas ~1318-1321 e ~1496-1497) consultam `commCache.get(item.id)` PRIMEIRO e so caem em `getCommissionRate(item.listing_type_id)` (LISTING_TYPE_RATES) quando o cache ainda nao tem o item — research confirma que esse padrao ja esta correto no call site; apenas confirmar e nao quebrar.
    3. NAO remover o import nem o objeto `LISTING_TYPE_RATES` (linha 11) — ainda usado por `getFinancialDailyStats()`/`getListingTypeBreakdown()` em financialMockData.ts (Pitfall 4; cleanup e Phase 46).
    4. Verificar que a remocao do guard nao causa fetch excessivo: a populacao ja deduplica via `filtered.filter(i => !commCache.has(i.id))` (linha 821). Se a lista filtrada for muito grande, manter o batching existente.
    5. `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>grep -vq 'columnView !== "financeiro" || !filteredItemKey' src/pages/mercadolivre/MLAnuncios.tsx && echo "guard columnView removido OK"</automated>
    <automated>grep -q "LISTING_TYPE_RATES" src/pages/mercadolivre/MLAnuncios.tsx && grep -q "commCache.get(item.id)" src/pages/mercadolivre/MLAnuncios.tsx && echo "cache-first + LISTING_TYPE_RATES preservado OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - O useEffect de populacao do commCache NAO contem mais `columnView !== "financeiro"` no guard (somente `!filteredItemKey`)
    - LISTING_TYPE_RATES continua importado e usado como fallback em getCommissionRate (objeto NAO deletado)
    - Call sites consultam commCache antes do fallback
    - `npx tsc --noEmit` sai com codigo 0
  </acceptance_criteria>
  <done>commCache populado para todos os itens filtrados e prioritario sobre LISTING_TYPE_RATES; objeto mock preservado; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: DATA-06 — auditoria de consistencia cruzada + validacao contra referencia Nexo</name>
  <files>src/pages/mercadolivre/MLAnuncios.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFinanceiro.tsx (linhas 161-166 — KPIs de useMLCostWaterfall: total_comissao, total_frete)
    - src/pages/MercadoLivre.tsx (waterfall do periodo — total_comissao/total_frete/cmv/total_tax exibidos no MLCostCard)
    - .planning/phases/41-veracidade-total/41-RESEARCH.md (secao "DATA-06" — tabela de fonte por pagina; referencia Nexo Abril/2026)
    - .planning/phases/41-veracidade-total/41-CONTEXT.md (secao "Specific Ideas" — numeros de referencia)
  </read_first>
  <action>
    Auditoria (sem novo codigo de produto alem do que a Task 1 ja fez):
    1. Confirmar via leitura que /vendas (MercadoLivre.tsx) e /financeiro (MLFinanceiro.tsx) usam ambos `useMLCostWaterfall` para total_comissao/total_frete/cmv/total_tax para o mesmo periodo — research confirma; documentar no SUMMARY a fonte exata de cada metrica por pagina.
    2. Validar numericamente contra a referencia Nexo Abril/2026 (seller 1639558873) usando o MCP Supabase execute_sql no ckcdevcxgvueywivefgx:
       - Comissao: `SELECT SUM(comissao) FROM orders WHERE ml_user_id=<pevermeio> AND <abril> AND status IN (PAID_STATUSES)` deve aproximar R$39.170 (~11,15%).
       - CFFE: `SELECT (resumo->>'cffe')::numeric FROM ml_billing_monthly WHERE period_month='2026-04'` deve aproximar R$40.065.
       - CFONPN: idem `resumo->>'cfonpn'` deve aproximar R$15.902.
    3. Confirmar que o `total_comissao` exibido em /vendas e /financeiro para o mesmo periodo e identico (mesma RPC get_cost_waterfall, mesma fonte) — se divergir, investigar e registrar a causa.
    4. Registrar a tabela de auditoria no SUMMARY (metrica | /vendas | /financeiro | /anuncios | referencia Nexo | bate?). Esta task NAO altera codigo de produto exceto se a auditoria revelar um bug claro de fonte divergente — nesse caso, corrigir minimamente e documentar.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q "useMLCostWaterfall" src/pages/mercadolivre/MLFinanceiro.tsx && grep -q "useMLCostWaterfall" src/pages/MercadoLivre.tsx && echo "fonte unica confirmada nas duas paginas OK"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contem tabela de auditoria de consistencia (comissao/frete/CMV/impostos x /vendas x /financeiro x /anuncios x referencia Nexo)
    - Validacao SQL executada via MCP no ckcdevcxgvueywivefgx: comissao Abril ~R$39,2k, CFFE ~R$40k, CFONPN ~R$15,9k registradas com os valores reais obtidos
    - /vendas e /financeiro confirmados usando useMLCostWaterfall (mesma fonte) para os totais
    - `npx tsc --noEmit` sai com codigo 0
  </acceptance_criteria>
  <done>Auditoria de consistencia concluida e documentada; valores validados contra a referencia Nexo Abril/2026.</done>
</task>

<task type="auto">
  <name>Task 3: Commit + push de DATA-05/06</name>
  <files>src/pages/mercadolivre/MLAnuncios.tsx</files>
  <read_first>
    - .planning/phases/41-veracidade-total/41-CONTEXT.md (secao "Ambiente e deploy")
  </read_first>
  <action>
    `git add src/pages/mercadolivre/MLAnuncios.tsx` (e qualquer fix minimo de Task 2), commit `feat(41): DATA-05 comissao real /anuncios + DATA-06 auditoria consistencia` e `git push origin main` para o Vercel. Se a Task 2 nao alterou codigo, commitar apenas a mudanca da Task 1.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -qi "DATA-05\|comissao\|consistencia" && echo "commit OK"</automated>
  </verify>
  <acceptance_criteria>
    - Commit criado com as mudancas de DATA-05 (e fix de DATA-06 se houver)
    - `git push origin main` executado sem erro
  </acceptance_criteria>
  <done>Mudancas commitadas e pushadas; Vercel auto-deploy disparado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    DATA-05 (comissao real via commCache/listing_prices em /anuncios, LISTING_TYPE_RATES so como fallback) e DATA-06 (auditoria de consistencia entre /vendas, /financeiro, /anuncios validada contra referencia Nexo Abril/2026).
  </what-built>
  <how-to-verify>
    1. Abrir /anuncios -> a comissao por anuncio deve mostrar o percentual real (ex.: do listing_prices, variando por item) e nao um % fixo hardcoded; o badge de % junto ao valor deve refletir o sale_fee real.
    2. Comparar o total de comissao em /vendas e /financeiro para o mesmo periodo -> devem ser identicos.
    3. Filtrar Abril/2026 e conferir contra a referencia Nexo: comissao ~R$39,2k, CFFE ~R$40k (linha Frete billing), CFONPN ~R$15,9k.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva qual numero nao bate entre as paginas / referencia</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> EF ml-precos-custos | invocacao autenticada para sale_fee via listing_prices |
| browser -> orders/get_cost_waterfall | leitura financeira escopada por org/ml_user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Information Disclosure | leitura de comissao/orders cross-org | mitigate | RLS por organization_id ja vigente; hooks escopados por resolvedMLUserIds |
| T-41-03-02 | Denial of Service | fetch excessivo de listing_prices ao remover guard columnView | mitigate | dedupe `filter(i => !commCache.has(i.id))`; batching existente preservado; so itens filtrados |
| T-41-03-03 | Tampering | comissao exibida divergente da fonte unica | mitigate | call site cache-first; auditoria DATA-06 valida igualdade /vendas==/financeiro |
| T-41-03-SC | Tampering | npm/pip/cargo installs | mitigate | nenhum pacote novo (research: Package Legitimacy Audit vazio) |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erros
- Guard columnView removido na populacao do commCache; LISTING_TYPE_RATES preservado
- Auditoria SQL via MCP confirma comissao/CFFE/CFONPN proximos da referencia Nexo Abril/2026
- Confirmacao visual de Wesley (checkpoint)
</verification>

<success_criteria>
- Comissao /anuncios vem da API ML real quando item no cache; LISTING_TYPE_RATES so fallback (DATA-05)
- commCache populado para todos os itens filtrados (DATA-05)
- /vendas, /financeiro, /anuncios consistentes e batendo com referencia Nexo (DATA-06)
- tsc limpo
</success_criteria>

<output>
Create `.planning/phases/41-veracidade-total/41-03-SUMMARY.md` when done
</output>
