---
phase: 41-veracidade-total
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/MercadoLivre.tsx
  - src/pages/mercadolivre/MLPedidos.tsx
  - src/hooks/useMLCostWaterfall.ts
  - src/hooks/useMLQueries.ts
  - src/components/mercadolivre/GoalsCard.tsx
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "Card Custos em /vendas exibe CMV e Impostos com valores nao-nulos quando ha config cadastrada (DATA-01)"
    - "Filtro Hoje em /vendas carrega KPI cards via auto-recalc silencioso com skeleton, nunca '—' estatico (DATA-02)"
    - "Lucro Bruto mensal vem de useMLCostWaterfall como fonte unica, excluindo pedidos cancelados (DATA-03)"
    - "Lucro Bruto % no GoalsCard e igual ao mostrado no MLCostCard para o mesmo periodo"
  artifacts:
    - path: "src/hooks/useAutoRecalc.ts"
      provides: "Hook de recalc silencioso ja implementado (verificar, nao recriar)"
      contains: "useAutoRecalc"
    - path: "src/hooks/useMLCostWaterfall.ts"
      provides: "total_tax e has_tax_data no retorno"
      contains: "has_tax_data"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "useAutoRecalc"
      via: "chamada para waterfall do periodo e mensal"
      pattern: "useAutoRecalc\\("
    - from: "src/pages/mercadolivre/MLPedidos.tsx"
      to: "sync-ml-orders"
      via: "auto-sync de hoje quando 0 orders"
      pattern: "sync-ml-orders"
---

<objective>
Consolidar e validar a veracidade dos numeros ja entregue por fases anteriores: DATA-01 (CMV/Impostos nao-nulos no card Custos), DATA-02 (auto-recalc silencioso no filtro Hoje) e DATA-03 (Lucro Bruto mensal de fonte unica sem cancelados).

A fundacao destes tres requisitos JA ESTA construida no codigo (Phases 21/31/32/38). Este plano e majoritariamente VERIFICACAO + cleanup de dead code residual + confirmacao visual — NAO reconstrucao. DATA-01 ja teve backend aplicado em producao (commit fc090c46); aqui so se valida visualmente e corrige fix residual de frontend se houver.

Purpose: Garantir que os tres requisitos prontos estao de fato funcionando end-to-end antes de adicionar billing real (41-02) e comissao real (41-03).
Output: /vendas com CMV/Impostos visiveis, filtro Hoje com skeleton, Lucro Bruto mensal consistente entre GoalsCard e MLCostCard.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-veracidade-total/41-CONTEXT.md
@.planning/phases/41-veracidade-total/41-RESEARCH.md
@.planning/phases/41-veracidade-total/41-PATTERNS.md
@.planning/phases/31-auto-sync-cmv-impostos-pedidos-realtime/31-01-PLAN.md
@.planning/phases/21-lucro-cache/21-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plano 41-01)

- `src/hooks/useAutoRecalc.ts` — hook `useAutoRecalc` (JA EXISTE; verificar, nao recriar)
- `src/hooks/useMLCostWaterfall.ts` — campos `total_tax`, `has_tax_data` no retorno (JA EXISTEM; verificar)
- `src/pages/MercadoLivre.tsx` — wiring de `useAutoRecalc` (JA WIRED linhas 188-189); `monthlyCostWaterfall` como fonte do Lucro Bruto (JA WIRED)
- `src/pages/mercadolivre/MLPedidos.tsx` — auto-sync de hoje quando 0 orders (JA EXISTE linhas 757-769; verificar)
- `src/components/mercadolivre/GoalsCard.tsx` — prop `grossProfitRevenue` (JA EXISTE; verificar)
- Nenhuma nova migration (DATA-01 backend ja aplicado em producao via fc090c46)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Verificar e fechar DATA-02 (auto-recalc Hoje) e DATA-03 (Lucro Bruto mensal fonte unica)</name>
  <files>src/pages/MercadoLivre.tsx, src/pages/mercadolivre/MLPedidos.tsx, src/hooks/useMLCostWaterfall.ts, src/hooks/useMLQueries.ts, src/components/mercadolivre/GoalsCard.tsx, src/hooks/useAutoRecalc.ts</files>
  <read_first>
    - src/pages/MercadoLivre.tsx (linhas 140-200 e 340-360 e 580-590 — wiring de useAutoRecalc, monthlyCostWaterfall, estimationBase, GoalsCard)
    - src/hooks/useAutoRecalc.ts (hook completo — confirmar logica Caso 1 null+inclui hoje e Caso 2 sem CMV/imposto)
    - src/hooks/useMLCostWaterfall.ts (confirmar total_tax e has_tax_data no select e no retorno)
    - src/pages/mercadolivre/MLPedidos.tsx (linhas 690-770 — loadOrders retorna number e auto-sync de hoje)
    - src/components/mercadolivre/GoalsCard.tsx (prop grossProfitRevenue e calculo grossProfitPct)
    - .planning/phases/31-auto-sync-cmv-impostos-pedidos-realtime/31-01-PLAN.md (plano base DATA-02)
    - .planning/phases/21-lucro-cache/21-01-PLAN.md (plano base DATA-03)
    - .planning/phases/41-veracidade-total/41-RESEARCH.md (secoes "DATA-02: Estado Atual" e "DATA-03: Estado Atual")
  </read_first>
  <action>
    Esta task e de VERIFICACAO contra o codigo atual (research confirma que A/B do 31-01 e Task1/2 do 21-01 ja foram executados em fases anteriores). Para cada item abaixo, confirmar que existe; se faltar, implementar o gap exato descrito no plano base; se ja existir, nao reescrever.

    DATA-02 (base 31-01):
    1. Confirmar `useAutoRecalc` em src/hooks/useAutoRecalc.ts implementa: Caso 1 (costWaterfall === null E periodo inclui hoje -> sync + recalc) e Caso 2 (waterfall sem has_cmv OU sem has_tax_data -> recalc), com firedRef para nao re-disparar por periodo, retornando `{ isRecalcing }`.
    2. Confirmar em MercadoLivre.tsx que `useAutoRecalc` esta chamado para o periodo selecionado (com `costWaterfall`, currentFrom, currentTo) e para o mensal (`monthlyCostWaterfall`, monthlyFrom, monthlyTo), e que `isRecalcing` alimenta o loading do MLKPIGrid (`kpiSummaryLoading || isRecalcing`).
    3. Confirmar em MLPedidos.tsx que `loadOrders` retorna `Promise<number>` e que ha useEffect que, apos loadOrders() retornar 0 e `dateTo >= hoje`, dispara `sync-ml-orders` por ml_user_id com toast "Sincronizando dados de hoje…" e recarrega. Se ja existe (research confirma linhas 757-769), nao duplicar.
    4. Se algum dos itens 1-3 NAO existir, implementar conforme as Tasks 31-01-A/B/C do plano base — sem fenced code, seguindo os identificadores ja citados.

    DATA-03 (base 21-01):
    5. Confirmar que MercadoLivre.tsx usa `monthlyCostWaterfall = useMLCostWaterfall(monthlyFrom, monthlyTo)` como fonte do `currentGrossProfit` (paid_revenue menos cmv/comissao/frete/ads/tax, com has_cmv e has_tax_data como guards), e NAO usa `monthlyKpiSummary` para o Lucro Bruto mensal.
    6. Confirmar que `useMLCostWaterfall` expoe `total_tax` e `has_tax_data` (research confirma que ja existem). Se faltar, adicionar `tax_amount` ao select e somar para pedidos pagos conforme Task 1 do 21-01.
    7. Confirmar que `GoalsCard` recebe `grossProfitRevenue={monthlyCostWaterfall?.paid_revenue ?? 0}` e usa esse denominador para `grossProfitPct`.
    8. Cleanup: remover qualquer dead code residual de `monthlyKpiSummary`/`rangeSyncedRef` SE ainda existir e nao for mais usado (research indica que `rangeSyncedRef` ja foi removido). Nao remover `useMLKPISummary` se ainda for usado para o KPI do periodo selecionado (linha ~147).
    9. Rodar `npx tsc --noEmit` apos qualquer edicao.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q "useAutoRecalc" src/pages/MercadoLivre.tsx && grep -q "has_tax_data" src/hooks/useMLCostWaterfall.ts && grep -q "grossProfitRevenue" src/components/mercadolivre/GoalsCard.tsx && echo OK</automated>
    <automated>grep -c "rangeSyncedRef" src/pages/MercadoLivre.tsx | grep -qx 0 && echo "no rangeSyncedRef OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com codigo 0
    - src/pages/MercadoLivre.tsx contem `useAutoRecalc` chamado para periodo e mensal
    - src/hooks/useMLCostWaterfall.ts contem `has_tax_data` e `total_tax`
    - src/pages/mercadolivre/MLPedidos.tsx contem auto-sync de hoje invocando `sync-ml-orders` quando count === 0 e dateTo >= hoje
    - src/components/mercadolivre/GoalsCard.tsx contem prop `grossProfitRevenue` usada no calculo de `grossProfitPct`
    - Nenhuma ocorrencia de `rangeSyncedRef` em MercadoLivre.tsx (dead code removido)
    - Nenhuma nova migration criada (DATA-01 backend ja aplicado via fc090c46)
  </acceptance_criteria>
  <done>DATA-02 e DATA-03 confirmados no codigo (gaps fechados se houver); tsc limpo; sem dead code residual.</done>
</task>

<task type="auto">
  <name>Task 2: Commit + push das verificacoes/cleanup de DATA-02/03</name>
  <files>src/pages/MercadoLivre.tsx, src/pages/mercadolivre/MLPedidos.tsx, src/hooks/useMLCostWaterfall.ts, src/hooks/useMLQueries.ts, src/components/mercadolivre/GoalsCard.tsx</files>
  <read_first>
    - .planning/phases/41-veracidade-total/41-CONTEXT.md (secao "Ambiente e deploy" — Frontend deploy = commit + push origin main)
  </read_first>
  <action>
    Se a Task 1 produziu qualquer edicao em src/, fazer `git add` dos arquivos alterados, commit com mensagem `feat(41): consolidar DATA-02 auto-recalc + DATA-03 lucro bruto fonte unica` e `git push origin main` para o Vercel auto-deployar. Se a Task 1 nao alterou nenhum arquivo (tudo ja estava correto), pular commit e registrar no SUMMARY que nenhuma mudanca de codigo foi necessaria. Nao tocar em backend/migrations.
  </action>
  <verify>
    <automated>git status --porcelain src/ | grep -q . && echo "ha mudancas para commit" || echo "nada a commitar (tudo ja correto)"</automated>
  </verify>
  <acceptance_criteria>
    - Se houve edicoes em src/: commit criado e `git push origin main` executado com sucesso (sem erro de push)
    - Se nao houve edicoes: SUMMARY registra que DATA-02/03 ja estavam corretos e nenhum commit foi necessario
  </acceptance_criteria>
  <done>Mudancas (se houver) commitadas e pushadas para o Vercel; caso contrario, no-op documentado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    DATA-01 (backend ja em producao via fc090c46), DATA-02 (auto-recalc Hoje) e DATA-03 (Lucro Bruto mensal fonte unica) consolidados. Wesley precisa confirmar visualmente os tres no dashboard.
  </what-built>
  <how-to-verify>
    1. Abrir /vendas com periodo de 30 dias com pedidos -> card "Custos" deve mostrar CMV e Impostos com valores em R$ (nao "s/ custo"/"s/ config") quando ha config cadastrada (DATA-01).
    2. Trocar o filtro para "Hoje" -> os KPI cards devem mostrar skeleton/loading e carregar via auto-recalc silencioso, nunca "—" estatico permanente (DATA-02).
    3. Conferir que o Lucro Bruto % no GoalsCard e igual ao Lucro % mostrado no card "Custos" (MLCostCard) para o mes corrente, e que cancelamentos nao inflam o numero (DATA-03).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que esta divergente (qual card, qual valor)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Supabase RPC (get_cost_waterfall) | leitura de dados financeiros escopados por org/ml_user_id |
| browser -> EF recalc-order-costs / sync-ml-orders | invocacao autenticada via JWT do usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Information Disclosure | get_cost_waterfall / orders read | mitigate | RLS por organization_id ja vigente; hook usa resolvedMLUserIds escopado |
| T-41-01-02 | Tampering | auto-recalc dispara recalc-order-costs | accept | only_missing:true; EF valida org membership; impacto limitado a recalculo idempotente |
| T-41-01-03 | Denial of Service | auto-sync loop em /pedidos | mitigate | firedRef/guard de "0 orders + inclui hoje" evita re-disparo; sync 1x por abertura |
| T-41-01-SC | Tampering | npm/pip/cargo installs | mitigate | nenhum pacote novo instalado nesta fase (research: Package Legitimacy Audit vazio) |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erros
- `npx vitest run` verde (se houver testes de useAutoRecalc)
- Confirmacao visual de Wesley nos tres cards (checkpoint)
</verification>

<success_criteria>
- Card Custos exibe CMV e Impostos nao-nulos quando ha config (DATA-01)
- Filtro Hoje carrega via skeleton/auto-recalc, nunca "—" estatico (DATA-02)
- Lucro Bruto mensal de useMLCostWaterfall, sem cancelados, consistente GoalsCard==MLCostCard (DATA-03)
- tsc limpo; sem dead code rangeSyncedRef
</success_criteria>

<output>
Create `.planning/phases/41-veracidade-total/41-01-SUMMARY.md` when done
</output>
