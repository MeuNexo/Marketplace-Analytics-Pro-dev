---
phase: 94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha
plan: 03
type: execute
wave: 3
depends_on: ["94-02"]
files_modified:
  - src/pages/MercadoLivre.tsx
  - src/components/mercadolivre/MLCostCard.tsx
autonomous: false
requirements: [SC4, SC6]
must_haves:
  truths:
    - "The DRE card's cmvMes/impostosMes become regime-aware and are a no-op (byte-identical) while the month is open"
    - "The card shows a regime pill: amber 'Previsão' when open, emerald 'Apurado — baseado nas guias de MM/YYYY' when closed"
    - "An owner sees a button to 'marcar mês como apurado' (open) / 'reabrir mês' (closed); non-owners see no such button"
    - "A green empurrãozinho hint appears when the 3 guias left the placeholder — as a hint, never auto-closing"
    - "Wesley confirms junho/2026 in apuração reconciles (CMV cheio + guia ICMS de julho)"
  artifacts:
    - path: "src/pages/MercadoLivre.tsx"
      provides: "Regime wiring at the cmvMes/impostosMes injection point + hooks + owner handlers"
      contains: "resolveDreRegime"
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "Regime pill + owner-only close/reopen button + empurrãozinho hint"
      contains: "regime"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/lib/dreRegime.ts + useDreMonthClose + useImpostoGuiaReal"
      via: "resolveDreRegime replaces the legacy cmvMes/impostosMes assignment (lines ~262-264)"
      pattern: "resolveDreRegime\\("
    - from: "src/components/mercadolivre/MLCostCard.tsx"
      to: "owner gate"
      via: "canClose prop (orgRole==='owner') gates the close/reopen button; RLS is authoritative"
      pattern: "canClose"
---

<objective>
Wire the regime through the UI. At the `cmvMes`/`impostosMes` injection point in `MercadoLivre.tsx` (~lines 262-264), replace the legacy assignment with `resolveDreRegime(...)`, fed by the 94-02 hooks — provably a no-op while the month is open. Extend `MLCostCard` with a regime pill ("Previsão" amber / "Apurado — baseado nas guias de MM/YYYY" emerald), an owner-only "marcar mês como apurado" / "reabrir mês" button, and the green empurrãozinho hint — all cloning the card's existing badge/Tooltip idiom, zero new components/colors.

Purpose: Delivers the user-visible half of the phase (SC4) — the lojista sees an honest previsão while open and the real result once closed. The open-month path stays byte-identical to Phase 88 (SC6). Ends with a human-verify checkpoint on the junho/2026 reconciliation.

Output: Regime-aware `/vendas` DRE card with close/reopen + selo + hint.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha/94-CONTEXT.md
@src/pages/MercadoLivre.tsx
@src/components/mercadolivre/MLCostCard.tsx
@src/lib/dreRegime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regime wiring in MercadoLivre.tsx (no-op when open)</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <read_first>
    - src/pages/MercadoLivre.tsx lines 98 (useOrganization destructure), 186-238 (billingMonth/dreMonthOverride, useDreOperational instantiation), 260-312 (cmvMes/impostosMes/margemContribuicao/dreCascade), 798-817 (MLCostCard render).
    - src/lib/dreRegime.ts (from 94-02) — resolveDreRegime / shouldNudgeClose / monthPlusOne signatures. Note shouldNudgeClose takes `{ rows, targetCompetence }` (cash_outflows-shaped nudge rows), NOT the RPC guia rows.
    - src/hooks/useDreMonthClose.ts + src/hooks/useImpostoGuiaReal.ts (from 94-02) — the latter exports BOTH useImpostoGuiaReal (real tax) and useImpostoGuiaNudge (the [M,M+2) cash_outflows rows for the nudge).
  </read_first>
  <action>
    Add `orgRole` to the `useOrganization()` destructure (line ~98). Compute `const dreSaleMonth = billingMonthIsCurrentMonth ? monthlyFrom : billingMonthFrom;` (the same "YYYY-MM-01" already fed to useDreOperational). Instantiate `const monthClose = useDreMonthClose(dreSaleMonth);`, `const guiaReal = useImpostoGuiaReal(dreSaleMonth);` and `const guiaNudge = useImpostoGuiaNudge(dreSaleMonth);`. Replace the legacy lines 262-264 (`cmvMes`/`impostosMes`) with a single `const regimeResult = resolveDreRegime({ isClosed: monthClose.data?.isClosed ?? false, cmvMedio: dreWaterfall?.cmv ?? 0, hasCmv: !!dreWaterfall?.has_cmv, cmvCheio: dreWaterfall?.cmv_cheio ?? 0, hasCmvCheio: !!dreWaterfall?.has_cmv_cheio, totalTaxEstimado: dreWaterfall?.total_tax ?? 0, hasTaxData: !!dreWaterfall?.has_tax_data, guiaReal: guiaReal.data ?? null });` then `const cmvMes = regimeResult.cmvMes; const impostosMes = regimeResult.impostosMes;`. Everything downstream (margemContribuicao, buildDreCascade) consumes cmvMes/impostosMes unchanged. CRITICAL no-op guarantee: while open (no close row), regimeResult reproduces the exact legacy expression, so margemContribuicao/dreCascade are byte-identical to Phase 88. Compute `const nudgeClose = shouldNudgeClose({ rows: guiaNudge.data ?? [], targetCompetence: monthPlusOne(dreSaleMonth) });` (the full locked 3-signal OR — driven by the cash_outflows rows, NOT the RPC totals), `const canClose = orgRole === "owner";`, and a `guiaCompetenceLabel` = pt-BR "MM/YYYY" of `monthPlusOne(dreSaleMonth)`. Pass new props to MLCostCard (Task 2): `regime={regimeResult.regime}`, `mesClosed={monthClose.data?.isClosed ?? false}`, `guiaCompetenceLabel={guiaCompetenceLabel}`, `canClose={canClose}`, `nudgeClose={nudgeClose}`, `onClose={() => monthClose.close()}`, `onReopen={() => monthClose.reopen()}`, `closeBusy={monthClose.isClosing || monthClose.isReopening}`. Do not remove or alter any existing MLCostCard prop.
  </action>
  <verify>
    <automated>grep -q "resolveDreRegime(" src/pages/MercadoLivre.tsx && grep -q "useDreMonthClose(" src/pages/MercadoLivre.tsx && grep -q "useImpostoGuiaNudge(" src/pages/MercadoLivre.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - cmvMes/impostosMes now derive from resolveDreRegime; open-month values are byte-identical to the prior expression.
    - orgRole destructured; canClose=owner-only; guiaCompetenceLabel = MM/YYYY of M+1.
    - New MLCostCard props passed; no existing prop removed; tsc 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Regime pill + owner button + empurrãozinho in MLCostCard</name>
  <files>src/components/mercadolivre/MLCostCard.tsx</files>
  <read_first>
    - src/components/mercadolivre/MLCostCard.tsx lines 1-70 (imports + MLCostCardProps), the existing badge span pattern (fonte pill: emerald/blue/amber classes) and the TooltipProvider/Tooltip/HelpCircle explainer block.
    - 94-CONTEXT.md <specifics> — selo copy ("Previsão" / "Apurado — baseado nas guias de DD/MM"), empurrãozinho copy ("as 3 guias parecem lançadas — fechar mês?").
  </read_first>
  <action>
    Add optional props to MLCostCardProps: `regime?: "previsao" | "apuracao"`, `mesClosed?: boolean`, `guiaCompetenceLabel?: string`, `canClose?: boolean`, `nudgeClose?: boolean`, `onClose?: () => void`, `onReopen?: () => void`, `closeBusy?: boolean`. Near the existing `fonte` pill / mesLabel header, render a regime pill reusing the existing badge span idiom: when `regime === "apuracao"` → emerald classes, text `Apurado — guias de {guiaCompetenceLabel}`; else → amber classes, text `Previsão`. When `canClose` is true, render a small button (reuse existing button/ghost styling in the card; no new component): label `Marcar mês como apurado` when `!mesClosed`, `Reabrir mês` when `mesClosed`; onClick calls onClose/onReopen respectively; disabled while `closeBusy` (show Loader2 like the existing syncing spinner). When `nudgeClose && !mesClosed`, render a green 🟢 hint (reuse the Tooltip explainer idiom or an inline emerald text badge) with copy like `As 3 guias parecem lançadas — fechar mês?` — it is a hint only, it does NOT auto-call onClose. All new UI is additive; when the new props are undefined (any other caller), the card renders exactly as today.
  </action>
  <verify>
    <automated>grep -q "regime" src/components/mercadolivre/MLCostCard.tsx && grep -q "canClose" src/components/mercadolivre/MLCostCard.tsx && grep -q "nudgeClose" src/components/mercadolivre/MLCostCard.tsx && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Regime pill: amber "Previsão" (open) / emerald "Apurado — guias de MM/YYYY" (closed), cloning the existing badge idiom.
    - Owner-only button toggles marcar-apurado / reabrir via onClose/onReopen; disabled while closeBusy; hidden when !canClose.
    - Empurrãozinho green hint shows when nudgeClose && !mesClosed; is display-only (no auto-close).
    - New props all optional → other callers unchanged; tsc 0; full vitest green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify junho/2026 reconciliation on /vendas</name>
  <action>Wesley verifies the regime-aware card on /vendas: previsão unchanged, apuração reconciles for junho/2026, owner-only close/reopen, empurrãozinho is a hint only.</action>
  <what-built>Regime-aware DRE card on `/vendas`: pill + owner close/reopen + empurrãozinho, with previsão unchanged from Phase 88.</what-built>
  <how-to-verify>
    1. Open `/vendas` (prod build or preview) as owner of Pé Vermeio. Navigate the DRE card to **Junho/2026** via the ‹ › buttons.
    2. Confirm it opens in **Previsão** (amber pill) and the numbers match what Wesley validated 2026-07-10 (CMV médio, imposto estimado) — byte-identical to before this phase.
    3. Click **Marcar mês como apurado**. Confirm the pill flips to emerald **Apurado — guias de 07/2026**, CMV switches to cheio (~R$133.264,87) and imposto switches to the real guia (ICMS de julho R$5.151,56 + PIS/COFINS), NOT the June guia (R$4.793,21). Verify the resultado reconciles to ~histórico.
    4. Click **Reabrir mês** → confirm it returns to Previsão (row deleted).
    5. Confirm a non-owner (member) session sees the pill but NO close/reopen button.
    6. If the 3 guias for the displayed month left the placeholder, confirm the green 🟢 empurrãozinho hint shows and does NOT auto-close.
  </how-to-verify>
  <resume-signal>Type "approved" if junho reconciles in apuração and previsão is unchanged; otherwise describe the mismatch (expected vs shown).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner button → dre_month_close mutation | Frontend gate (orgRole) is UX-only; RLS from 94-01 is the real authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-06 | Elevation of Privilege | Non-owner triggering close/reopen by bypassing the hidden button | mitigate | canClose hides the button (UX) but dre_month_close INSERT/DELETE RLS (owner via get_org_role) is the enforcement — a crafted call still fails server-side |
| T-94-07 | Tampering | Empurrãozinho silently auto-closing a month | accept→mitigate | nudge is display-only; onClose is only ever invoked by the explicit owner button click, never by the hint |
| T-94-SC | Tampering | package installs | accept | No new npm packages — reuses existing shadcn Tooltip/Button/lucide |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx vitest run` full suite green (dreCascade junho fixture unchanged → previsão path intact).
- Open-month DRE numbers byte-identical to Phase 88 (visual + unchanged margemContribuicao path).
- Human-verify checkpoint: junho/2026 apuração reconciliation + owner-only button + reopen.
</verification>

<success_criteria>
- SC4: card shows regime selo + owner-only marcar/reabrir + green empurrãozinho.
- SC6: previsão (open month) unchanged vs Wesley's 2026-07-10 validation; full test suite green.
</success_criteria>

<output>
Create `.planning/phases/94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha/94-03-SUMMARY.md` when done. Record the junho reconciliation result from the human-verify checkpoint.
</output>
