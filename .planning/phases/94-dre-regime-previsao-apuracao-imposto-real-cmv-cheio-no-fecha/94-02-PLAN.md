---
phase: 94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - src/hooks/useMLCostWaterfall.ts
  - src/hooks/useDreMonthClose.ts
  - src/hooks/useImpostoGuiaReal.ts
  - src/lib/dreRegime.ts
  - src/lib/dreRegime.test.ts
autonomous: true
requirements: [SC2, SC3, SC6]
must_haves:
  truths:
    - "cmv_cheio flows from the live get_cost_waterfall RPC into the frontend alongside cmv (médio)"
    - "The real imposto for sale-month M is fetched from get_imposto_guia_by_competence at competence M+1 (the M+1 shift lives in the hook, not in the big DRE RPC)"
    - "A month with no dre_month_close row resolves to PREVISÃO; a month with a row resolves to APURAÇÃO"
    - "PREVISÃO output is byte-identical to the current Phase 88 expression (médio CMV + estimated tax)"
    - "PREVISÃO and APURAÇÃO bases never mix — médio-CMV never coexists with guia-real tax, and cheio-CMV never coexists with estimated tax"
    - "The empurrãozinho fires on the full locked 3-signal OR (vencimento≠21 OR status=paid OR valor≠anterior), driven by a direct RLS cash_outflows read across [M, M+2) — the early vencimento≠21 signal is not dropped"
  artifacts:
    - path: "src/lib/dreRegime.ts"
      provides: "Pure resolveDreRegime() + shouldNudgeClose() + DreRegime types"
      exports: ["resolveDreRegime", "shouldNudgeClose", "DreRegime", "monthPlusOne"]
    - path: "src/lib/dreRegime.test.ts"
      provides: "Never-mix proof, byte-identical previsão, junho/2026 reconciliation"
    - path: "src/hooks/useDreMonthClose.ts"
      provides: "Read presence + close()/reopen() mutations on dre_month_close (owner via RLS)"
      exports: ["useDreMonthClose"]
    - path: "src/hooks/useImpostoGuiaReal.ts"
      provides: "useImpostoGuiaReal (get_imposto_guia_by_competence M+1 real tax) + useImpostoGuiaNudge (direct RLS cash_outflows read [M,M+2) for the 3-signal nudge)"
      exports: ["useImpostoGuiaReal", "useImpostoGuiaNudge"]
    - path: "src/hooks/useMLCostWaterfall.ts"
      provides: "cmv_cheio + has_cmv_cheio threaded through CostWaterfallData"
      contains: "cmv_cheio"
  key_links:
    - from: "src/hooks/useImpostoGuiaReal.ts"
      to: "get_imposto_guia_by_competence RPC (live in prod)"
      via: "supabase.rpc with p_competence = M+1 of the displayed sale month"
      pattern: "get_imposto_guia_by_competence"
    - from: "src/hooks/useImpostoGuiaReal.ts"
      to: "cash_outflows (RLS org-scoped)"
      via: "direct read across [M, M+2) for the 3 imposto categories → nudge signals"
      pattern: "from\\(\"cash_outflows\"\\)"
    - from: "src/hooks/useDreMonthClose.ts"
      to: "public.dre_month_close (RLS)"
      via: "supabase.from('dre_month_close') select/insert/delete"
      pattern: "from\\(\"dre_month_close\"\\)"
    - from: "src/lib/dreRegime.ts"
      to: "src/hooks/useMLCostWaterfall.ts + useImpostoGuiaReal.ts"
      via: "resolveDreRegime consumes cmv/cmv_cheio + total_tax/guia-real to pick a coherent base"
      pattern: "resolveDreRegime"
---

<objective>
Build the pure regime logic and the data layer that feeds it, with zero UI change. This plan delivers: (1) `cmv_cheio` threaded through `useMLCostWaterfall`; (2) `useDreMonthClose` (presence read + owner close/reopen mutations against the RLS-guarded table from 94-01); (3) `useImpostoGuiaReal` (the M+1 shift — calls the already-live `get_imposto_guia_by_competence` at competence M+1); (4) `src/lib/dreRegime.ts` — a pure function that derives PREVISÃO vs APURAÇÃO from close-presence and picks ONE coherent base, never mixing médio-CMV with guia-real tax.

Purpose: The M+1 shift and the never-mix invariant are the reconciliation heart of the phase (SC2, SC3). Keeping the shift in the hook (per CONTEXT <db_reality> refined design) means `get_dre_operational_by_competence` is never touched — zero regression on the other 7 blocos and the DFC (SC6). The byte-identical previsão test proves Phase 88 is untouched behaviorally.

Output: New hooks + pure regime lib + tests. No component renders change yet (that's 94-03).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha/94-CONTEXT.md
@src/hooks/useMLCostWaterfall.ts
@src/hooks/useDreOperational.ts
@src/lib/dreCascade.ts
</context>

<db_contract>
The following are LIVE in prod `ckcdevcxgvueywivefgx` (verified by orchestrator — do NOT re-derive, do NOT create migrations for them):
- `get_cost_waterfall(p_org_id uuid, p_user_ids text[], p_from date, p_to date)` → TABLE(paid_revenue, cmv, total_comissao, total_frete, total_tax, orders_count, **cmv_cheio**). `cmv`=médio, `cmv_cheio`=cheio, `total_tax`=estimado.
- `get_imposto_guia_by_competence(p_org_id uuid, p_competence date)` → TABLE(category, total, status, n). The 3 categories are `Imposto Venda - ICMS` / `- PIS` / `- COFINS`. Call with `p_competence = M+1` (first-of-month) to get the real guias for sale-month M.
- `dre_month_close` (from 94-01) — presence of a row for (org, competence_month=sale-month "YYYY-MM-01") = closed.
- `cash_outflows` (existing table, org-scoped RLS already proven anti-IDOR for get_cashflow) — columns include `organization_id, category, amount, status, outflow_date, competence_date`. Used by a DIRECT RLS read for the nudge signals (the aggregated RPC returns no outflow_date/amount, so it cannot power vencimento≠21 / valor≠anterior). Do NOT modify this table or any RPC over it — read-only.
RPCs are invoked by name via `supabase.rpc(...)` (they are not in generated types — that is the established convention, confirmed in types.ts).
</db_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure regime resolver + tests (dreRegime.ts)</name>
  <files>src/lib/dreRegime.ts, src/lib/dreRegime.test.ts</files>
  <read_first>
    - src/hooks/useMLCostWaterfall.ts — current CostWaterfallData shape (cmv/has_cmv/total_tax/has_tax_data) and the exact previsão expression the resolver must reproduce byte-identically.
    - src/pages/MercadoLivre.tsx lines 261-264 — the legacy cmvMes/impostosMes assignment this resolver replaces: `cmvMes = (has_cmv ? cmv : null) ?? null`, `impostosMes = (has_tax_data ? total_tax : null) ?? null`.
    - 94-CONTEXT.md <decisions> "Regime é único" + <db_reality> refined design — the never-mix rule and M+1 semantics.
    - src/lib/dreCascade.test.ts (~line 115) — junho/2026 fixture style (toBeCloseTo, Phase 87 numbers) to mirror.
  </read_first>
  <behavior>
    - Test A (previsão byte-identical): regime=previsão when no close row; output cmvMes/impostosMes equal EXACTLY the legacy expression (médio CMV via has_cmv, estimated total_tax via has_tax_data). Prove with the same inputs the card uses today.
    - Test B (apuração base): regime=apuração when closed; cmvMes = cmv_cheio (via has_cmv_cheio, null if absent), impostosMes = sum of the 3 guia-real category totals.
    - Test C (never mix): assert that in previsão the output never equals the cheio value nor the guia-real sum; in apuração the output never equals the médio value nor the estimated total_tax. Médio+guia-real and cheio+estimado must be impossible to produce from the resolver.
    - Test D (junho/2026 reconciliation): apuração with cmv_cheio=133264.87 and guia ICMS 5151.56 (+ pis/cofins fixture) → cmvMes toBeCloseTo(133264.87, 2), impostosMes toBeCloseTo(sum, 2). Use toBeCloseTo (float precision, Pitfall 5).
    - Test E (nudge — 3-signal OR, LOCKED, display-only): with all 3 target-competence (M+1) categories present, shouldNudgeClose fires when ANY of the 3 locked signals holds — (1) `vencimento≠21`: a target guia whose outflow_date day-of-month !== 21; (2) `status='paid'`: any target guia paid; (3) `valor≠anterior`: a target guia whose amount differs from the same-category previous-competence (M) placeholder. Assert EACH signal fires the nudge INDEPENDENTLY (day≠21 alone; paid alone; amount-changed alone), and that NONE firing (all day=21, all pending, all amounts equal the M placeholder) → false; a missing category → false. It is a hint only, never gates the resolver nor calls the close mutation. Rationale (from CONTEXT): Wesley moves the vencimento to the real day AND updates the value BEFORE payment, so `status` stays 'pending' until ~day 21 — the `vencimento≠21` signal is the EARLY apuração signal; dropping it makes the nudge fire too late.
  </behavior>
  <action>
    Create `src/lib/dreRegime.ts` — a pure module (no React/Supabase). Export `type DreRegime = "previsao" | "apuracao"`. Export `monthPlusOne(firstOfMonth: string): string` that maps "YYYY-MM-01" → the next month's "YYYY-MM-01" (build from numeric year/month parts, never string-concat; always day 01 — Pitfall 3). Export `resolveDreRegime(input)` where input carries: `isClosed: boolean`, `cmvMedio: number`, `hasCmv: boolean`, `cmvCheio: number`, `hasCmvCheio: boolean`, `totalTaxEstimado: number`, `hasTaxData: boolean`, and `guiaReal: { category: string; total: number; status: string }[] | null`. Return `{ regime, cmvMes: number | null, impostosMes: number | null, apuracaoImpostoReal: number | null }`. Logic: if `!isClosed` → previsão: `cmvMes = (hasCmv ? cmvMedio : null) ?? null`, `impostosMes = (hasTaxData ? totalTaxEstimado : null) ?? null` (reproduce the legacy expression EXACTLY). If `isClosed` → apuração: `cmvMes = (hasCmvCheio ? cmvCheio : null) ?? null`, `impostosMes = sum of guiaReal totals for the 3 Imposto Venda categories` (null if guiaReal is null/empty). NEVER read cheio in the previsão branch nor estimado in the apuração branch — the two branches share no base variable.

    Export the pure `shouldNudgeClose(input)` where `input = { rows: { category: string; amount: number; status: string; outflowDate: string; competenceMonth: string }[]; targetCompetence: string }` (targetCompetence = the M+1 first-of-month; `competenceMonth` on each row = the row's competence first-of-month). Pure, NO fetch (rows come from useImpostoGuiaNudge in Task 3). Partition into `targetRows` (competenceMonth === targetCompetence, i.e. M+1) and `prevRows` (competenceMonth === the M competence). Return true ONLY when all 3 categories `Imposto Venda - ICMS` / `- PIS` / `- COFINS` are present in targetRows AND at least ONE of the 3 LOCKED signals fires (CONTEXT <specifics>, restored per checker): (1) `vencimento≠21` — any targetRow where `new Date(outflowDate).getUTCDate() !== 21` (parse the ISO date safely, do not rely on locale); (2) `status='paid'` — any targetRow with `status === 'paid'`; (3) `valor≠anterior` — any targetRow whose `amount` differs from the same-category prevRow `amount` (the recurring placeholder). It is display-only — it must NEVER gate resolveDreRegime nor call the close mutation. Add a header comment: this resolver is the never-mix guardrail (SC3); the M+1 real-tax total that feeds `guiaReal` comes from `get_imposto_guia_by_competence` (useImpostoGuiaReal), while the 3-signal nudge rows come from a direct RLS `cash_outflows` read (useImpostoGuiaNudge) — the RPC is NOT modified. Then write `src/lib/dreRegime.test.ts` covering behaviors A–E.
  </action>
  <verify>
    <automated>npx vitest run src/lib/dreRegime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - resolveDreRegime returns byte-identical previsão output vs the legacy expression (Test A green).
    - Never-mix proven: no branch produces médio+guia-real or cheio+estimado (Test C green).
    - junho/2026 apuração reconciliation green via toBeCloseTo (Test D).
    - shouldNudgeClose implements the full 3-signal OR (vencimento≠21 / paid / valor≠anterior); each signal fires independently, none → false, missing category → false (Test E). Pure hint — never gates the resolver.
    - No import of React or supabase in dreRegime.ts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Thread cmv_cheio through useMLCostWaterfall</name>
  <files>src/hooks/useMLCostWaterfall.ts</files>
  <read_first>
    - src/hooks/useMLCostWaterfall.ts (full) — CostWaterfallData interface + the r-mapping block that currently reads paid_revenue/cmv/total_tax.
    - <db_contract> above — live get_cost_waterfall now returns cmv_cheio.
  </read_first>
  <action>
    Add `cmv_cheio: number` and `has_cmv_cheio: boolean` to the `CostWaterfallData` interface (documented: cmv_cheio = CMV cheio da nota, base do regime APURAÇÃO). In the queryFn mapping, read `const cmv_cheio = Number(r.cmv_cheio ?? 0);` and return `cmv_cheio: Math.round(cmv_cheio * 100) / 100, has_cmv_cheio: cmv_cheio > 0`. Do NOT change any existing field, the `paid_revenue === 0 → null` guard, staleTime, or queryKey. Purely additive so previsão consumers are byte-identical.
  </action>
  <verify>
    <automated>grep -q "has_cmv_cheio" src/hooks/useMLCostWaterfall.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - CostWaterfallData exposes cmv_cheio + has_cmv_cheio, mapped from r.cmv_cheio with 2-decimal rounding.
    - No existing field/guard/queryKey altered; tsc 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: useDreMonthClose + useImpostoGuiaReal hooks</name>
  <files>src/hooks/useDreMonthClose.ts, src/hooks/useImpostoGuiaReal.ts</files>
  <read_first>
    - src/hooks/useDreOperational.ts — the exact hook idiom to clone (useOrganization → orgId, useQuery, "YYYY-MM-01" p_month, staleTime, anti-IDOR server-side note).
    - src/components/mercadolivre/ReplenishmentParamsDialog.tsx — the direct `supabase.from(...)` write pattern (no EF; RLS is the only guard) to mirror for close/reopen.
    - src/hooks/useDreOperational.ts + any existing cash_outflows reader — the direct RLS read idiom (`.eq("organization_id", orgId)` + range filters); cash_outflows RLS is already org-scoped (used by get_cashflow / get_dre_operational).
    - <db_contract> — dre_month_close columns + get_imposto_guia_by_competence signature + cash_outflows columns.
  </read_first>
  <action>
    `src/hooks/useDreMonthClose.ts`: export `useDreMonthClose(competenceMonth: string)` (competenceMonth = sale-month "YYYY-MM-01"). A useQuery keyed `["dre","month-close",orgId,competenceMonth]` that SELECTs `dre_month_close` where organization_id=orgId AND competence_month=competenceMonth, returning `{ isClosed: boolean, closedAt: string | null }`. Plus two mutations using `supabase.from("dre_month_close")`: `close()` = insert `{ organization_id: orgId, competence_month: competenceMonth, closed_by: <auth uid> }`; `reopen()` = delete matching row. Each mutation invalidates the query key on success. RLS (from 94-01) is the sole authority — do not re-check role in SQL; the frontend owner-gate in 94-03 is UX-only. Enabled only when orgId && competenceMonth.
    `src/hooks/useImpostoGuiaReal.ts`: export TWO hooks.
    (a) `useImpostoGuiaReal(saleMonth: string)` (saleMonth = "YYYY-MM-01") — the M+1 real-tax source for the resolver. Compute `p_competence = monthPlusOne(saleMonth)` (import from src/lib/dreRegime). useQuery keyed `["dre","imposto-guia",orgId,p_competence]` calling `supabase.rpc("get_imposto_guia_by_competence", { p_org_id: orgId, p_competence })`. Map rows to `{ category: string; total: number; status: string; n: number }[]` (Number/String coercion like useDreOperational). staleTime 5min, enabled when orgId && saleMonth. This is the LOCKED M+1 shift — the RPC is NOT modified (shared surface); the totals reconcile the real tax to the centavo (SC2).
    (b) `useImpostoGuiaNudge(saleMonth: string)` — the SIGNAL source for the empurrãozinho (the RPC cannot power vencimento≠21 / valor≠anterior — it returns no outflow_date/amount). Direct RLS read: `supabase.from("cash_outflows").select("category, amount, status, outflow_date, competence_date").eq("organization_id", orgId).in("category", ["Imposto Venda - ICMS","Imposto Venda - PIS","Imposto Venda - COFINS"]).gte("competence_date", saleMonth).lt("competence_date", monthPlusOne(monthPlusOne(saleMonth)))` — i.e. competence range `[M, M+2)`, covering both the previous competence M (the recurring placeholder) and the target M+1. Map to `{ category: string; amount: number; status: string; outflowDate: string; competenceMonth: string }[]` where `competenceMonth` = first-of-month string of competence_date. useQuery keyed `["dre","imposto-nudge",orgId,saleMonth]`, staleTime 5min, enabled when orgId && saleMonth. cash_outflows RLS (org-scoped, already anti-IDOR-proven for get_cashflow) is the sole guard — pass only the session orgId, no client re-filter beyond the `.eq`. These raw rows feed `shouldNudgeClose({ rows, targetCompetence: monthPlusOne(saleMonth) })` in 94-03. Note: imposto guias always carry a non-null competence_date, so filtering on competence_date is correct here.
  </action>
  <verify>
    <automated>grep -q 'from("dre_month_close")' src/hooks/useDreMonthClose.ts && grep -q "get_imposto_guia_by_competence" src/hooks/useImpostoGuiaReal.ts && grep -q 'from("cash_outflows")' src/hooks/useImpostoGuiaReal.ts && grep -q "useImpostoGuiaNudge" src/hooks/useImpostoGuiaReal.ts && grep -q "monthPlusOne" src/hooks/useImpostoGuiaReal.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - useDreMonthClose reads presence + exposes close()/reopen() via direct supabase.from (no EF), invalidating the query.
    - useImpostoGuiaReal calls get_imposto_guia_by_competence at monthPlusOne(saleMonth) (the M+1 shift, RPC unmodified), returns the 3-category totals with status for the resolver.
    - useImpostoGuiaNudge does a direct RLS `cash_outflows` read across [M, M+2) for the 3 imposto categories, returning category/amount/status/outflowDate/competenceMonth rows — the ingredients for all 3 nudge signals.
    - Both hooks org-scoped via useOrganization; cash_outflows read passes only session orgId (RLS anti-IDOR); tsc 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend hook → dre_month_close / RPCs | Session JWT crosses into RLS-guarded reads/writes and SECURITY INVOKER RPCs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | Information Disclosure | useImpostoGuiaReal / useDreMonthClose cross-org read | mitigate | Hooks pass only session org id; get_imposto_guia_by_competence is SECURITY INVOKER (proven anti-IDOR) and dre_month_close SELECT RLS is is_org_member (94-01) — no client-side re-filter trusted |
| T-94-08 | Information Disclosure | useImpostoGuiaNudge direct cash_outflows read across orgs | mitigate | cash_outflows RLS is org-scoped and already anti-IDOR-proven for get_cashflow/get_dre_operational; the read passes only the session orgId via .eq — a foreign org returns 0 rows |
| T-94-05 | Tampering | Mixing médio-CMV with guia-real tax (double-counted credit) | mitigate | resolveDreRegime shares no base variable between branches; Test C proves the mix is unreachable |
| T-94-SC | Tampering | package installs | accept | No new npm packages — pure TS + existing stack |
</threat_model>

<verification>
- `npx vitest run src/lib/dreRegime.test.ts` green (behaviors A–E, incl. all 3 nudge signals firing independently + none→false).
- `npx tsc --noEmit` clean.
- Full suite unaffected: `npx vitest run` (dreCascade junho fixture still green — no dreCascade.ts change in this plan → SC6 previsão path untouched).
</verification>

<success_criteria>
- SC2: M+1 shift implemented in useImpostoGuiaReal (competence = M+1); junho reconciliation proven in the resolver test.
- SC3: regime derived from close-presence; never-mix proven by Test C.
- SC6: no change to dreCascade.ts / get_dre_operational_by_competence / get_cashflow; previsão output byte-identical (Test A) and full suite green.
</success_criteria>

<output>
Create `.planning/phases/94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha/94-02-SUMMARY.md` when done. Note the never-mix test and the junho reconciliation numbers.
</output>
