---
phase: 94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260694000000_dre_month_close.sql
autonomous: false
requirements: [SC1, SC5, SC6]
must_haves:
  truths:
    - "Owner can persist a closed-month row for their org; presence of the row = month is in APURAÇÃO"
    - "Owner can delete the row to reopen the month back to PREVISÃO"
    - "A member (non-owner) can read close state but cannot INSERT/DELETE it"
    - "A JWT from org A cannot read, close, or reopen a month belonging to org B (anti-IDOR)"
    - "Phase 88 previsão math is unchanged — no existing RPC is modified by this plan"
  artifacts:
    - path: "supabase/migrations/20260694000000_dre_month_close.sql"
      provides: "dre_month_close table + org-first RLS (owner INSERT/DELETE, member SELECT)"
      contains: "create table public.dre_month_close"
  key_links:
    - from: "supabase/migrations/20260694000000_dre_month_close.sql"
      to: "public.get_org_role / public.is_org_member"
      via: "RLS policies clone ml_tax_config owner-only pattern"
      pattern: "get_org_role\\(auth.uid\\(\\), organization_id\\) = 'owner'"
---

<objective>
Create the `dre_month_close` table — the single source of truth for whether a sale-month is in PREVISÃO (no row) or APURAÇÃO (row present) — with org-first RLS cloned verbatim from the `ml_tax_config` owner-only template. The migration file is written into the repo (source of truth) and applied to prod `ckcdevcxgvueywivefgx` by the orchestrator via MCP at a blocking checkpoint, where advisors and anti-IDOR are proven.

Purpose: Locks the regime-switch authority in the database with RLS, so no frontend gate can be trusted for the close/reopen mutation (SC1, SC5). This plan modifies NO existing RPC — `get_dre_operational_by_competence`, `get_cost_waterfall`, and `get_cashflow` are left byte-identical, guaranteeing zero regression on Phase 88 previsão and the DFC (SC6).

Output: `dre_month_close` table live in prod with RLS + advisors clean + cross-org IDOR proof.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha/94-CONTEXT.md
</context>

<deploy_note>
**The gsd-executor CANNOT deploy** (no Supabase CLI token in this environment). The executor's job is to WRITE the migration file into `supabase/migrations/`. The migration is applied to prod `ckcdevcxgvueywivefgx` by the ORCHESTRATOR via the Supabase MCP `apply_migration` tool at the checkpoint task below. Never apply via SQL Editor.
</deploy_note>

<tasks>

<task type="auto">
  <name>Task 1: Write dre_month_close migration (table + org-first RLS)</name>
  <files>supabase/migrations/20260694000000_dre_month_close.sql</files>
  <read_first>
    - supabase/migrations/20260515120000_ml_tax_config.sql — EXACT RLS template to clone (owner INSERT/UPDATE/DELETE via get_org_role='owner'; member SELECT via is_org_member). Same fiscal-module domain, not a cross-domain borrow.
    - 94-CONTEXT.md <db_reality> — confirms is_org_member(_user_id,_org_id) and get_org_role(_user_id,_org_id) exist as SECURITY DEFINER helpers in prod; confirms the presence-of-row semantics (row = closed; delete = reopen).
  </read_first>
  <action>
    Create the migration creating `public.dre_month_close` with PK `(organization_id, competence_month)` where `organization_id uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE`, `competence_month date NOT NULL` (stores the SALE month S as "YYYY-MM-01", NEVER the guia's M+1 — this is the same value the frontend passes as p_month elsewhere), `closed_at timestamptz NOT NULL DEFAULT now()`, `closed_by uuid NULL` (no FK, per ml_claim_templates convention). Enable RLS. Add three policies cloning ml_tax_config verbatim, TO authenticated: SELECT USING `public.is_org_member(auth.uid(), organization_id)`; INSERT WITH CHECK `public.get_org_role(auth.uid(), organization_id) = 'owner'`; DELETE USING `public.get_org_role(auth.uid(), organization_id) = 'owner'`. Do NOT add an UPDATE policy — reopen is a DELETE (per D-locked "Mês SEM registro → previsão" presence semantics). The RLS enforcement is SECURITY-INVOKER-equivalent (row-level policies on a plain table). Add a header comment stating this migration was applied to prod ckcdevcxgvueywivefgx via MCP (repo-history reconciliation pattern, same as 20260692000000). No trigger, no function, no automatic close logic — the row is the only authority (per D-locked "Gatilho = clique manual, NUNCA automático").
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260694000000_dre_month_close.sql && grep -q "create table public.dre_month_close" supabase/migrations/20260694000000_dre_month_close.sql && grep -c "get_org_role(auth.uid(), organization_id) = 'owner'" supabase/migrations/20260694000000_dre_month_close.sql | grep -qE '^[2-9]' && ! grep -qi "for update" supabase/migrations/20260694000000_dre_month_close.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path with `create table public.dre_month_close` (PK organization_id + competence_month).
    - RLS enabled; exactly SELECT (is_org_member), INSERT (owner), DELETE (owner) policies; NO UPDATE policy.
    - No trigger/function/automatic-close logic present.
    - `closed_by uuid NULL` present without FK; `competence_month date` documented as sale-month "YYYY-MM-01".
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Orchestrator MCP apply + advisors + anti-IDOR proof</name>
  <action>ORCHESTRATOR-ONLY (Supabase MCP). Apply the migration to prod ckcdevcxgvueywivefgx via apply_migration, confirm the table + advisors, and run the cross-org IDOR proof. The gsd-executor cannot perform this step (no CLI token).</action>
  <what-built>Migration file `20260694000000_dre_month_close.sql` written into the repo, ready for MCP deploy.</what-built>
  <how-to-verify>
    ORCHESTRATOR (has Supabase MCP; the executor does not) applies and proves against prod `ckcdevcxgvueywivefgx`:
    1. `apply_migration` with the file body from Task 1. Expect success (no 42P13 / no policy-name collision).
    2. `list_tables` (or `execute_sql` on information_schema) → confirm `dre_month_close` exists with the 4 columns + PK.
    3. `get_advisors` (security + performance) → confirm NO new error introduced by this table (RLS enabled satisfies the "policy exists" advisor; a plain SELECT/INSERT/DELETE-only table is expected clean).
    4. ANTI-IDOR PROOF (SC5): impersonate a member of org Thales (`e4150d57...`) and attempt to SELECT / INSERT / DELETE a `dre_month_close` row with `organization_id` = Pé Vermeio (`7f615df7-7bac-45e5-8a93-827fb9ddeec7`). Expect: 0 rows on SELECT, permission-denied / 0 rows affected on INSERT+DELETE. Then prove own-org owner CAN insert+delete its own month.
    5. NO-REGRESSION SPOT-CHECK (SC6): confirm `get_dre_operational_by_competence` and `get_cost_waterfall` and `get_cashflow` function bodies are unchanged (this plan touched none).
  </how-to-verify>
  <resume-signal>Type "approved" once the table is live, advisors are clean, and the cross-org IDOR proof shows 0 rows for the foreign org. Report the anti-IDOR result verbatim in the SUMMARY.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT → dre_month_close (RLS) | Any org member's session can attempt reads/writes; RLS is the sole guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Information Disclosure | SELECT on dre_month_close | mitigate | RLS SELECT USING is_org_member(auth.uid(), organization_id) — a foreign org reads 0 rows |
| T-94-02 | Elevation of Privilege | INSERT/DELETE (close/reopen) by a non-owner member | mitigate | RLS INSERT WITH CHECK / DELETE USING get_org_role(...)='owner' — DB-enforced, frontend gate is UX-only |
| T-94-03 | Tampering | Owner of org A forging organization_id of org B on INSERT | mitigate | WITH CHECK re-validates get_org_role against the target organization_id, not the caller's default org |
| T-94-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this plan — SQL-only |
</threat_model>

<verification>
- Migration file present and structurally correct (Task 1 automated grep).
- Orchestrator MCP checkpoint: table live, advisors clean, cross-org IDOR = 0 rows for foreign org, own-org owner CRUD works.
- No existing RPC modified (git diff shows only the new migration file).
</verification>

<success_criteria>
- SC1: `dre_month_close` (PK org-first) live in prod with owner-only writes + member SELECT + reversible-by-DELETE; advisors clean.
- SC5: cross-org JWT proven to read/write 0 foreign-org rows.
- SC6: no existing RPC touched → Phase 88 previsão and get_cashflow/DFC unaffected.
</success_criteria>

<output>
Create `.planning/phases/94-dre-regime-previsao-apuracao-imposto-real-cmv-cheio-no-fecha/94-01-SUMMARY.md` when done. Record the anti-IDOR proof result verbatim and confirm no RPC was modified.
</output>
