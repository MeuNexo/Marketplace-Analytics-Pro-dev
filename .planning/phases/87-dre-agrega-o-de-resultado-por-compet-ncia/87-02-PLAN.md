---
phase: 87-dre-agrega-o-de-resultado-por-compet-ncia
plan: 02
type: execute
wave: 2
depends_on: [87-01]
files_modified: []
autonomous: false
requirements: [SC-4]
must_haves:
  truths:
    - "Reconciliação de total fechado passa em junho/2026: Σ(todos os blocos incl. outros_operacionais) + Σ(categorias excluídas) = Σ(todas as linhas de cash_outflows de junho, escopo competência)"
    - "Toda categoria viva de junho aparece em exatamente um bloco da RPC (nomeado ou outros_operacionais) com SUM batendo"
    - "Anti-IDOR provado: JWT/role da org Pé Vermeio chamando a RPC com p_org_id da org Thales retorna 0 linhas"
    - "A cobertura de competence_date de junho/2026 é medida e registrada junto ao resultado (não assumida como ≥90%)"
  artifacts: []
  key_links:
    - from: "reconciliação"
      to: "get_dre_operational_by_competence"
      via: "SELECT da RPC vs SELECT manual agregado direto de cash_outflows por competence_date"
      pattern: "get_dre_operational_by_competence"
---

<objective>
Provar que a RPC do plano 87-01 está correta e isolada por tenant, reconciliando um mês real fechado (junho/2026, org Pé Vermeio `7f615df7`) e validando o anti-IDOR. A reconciliação desta fase é um **check de total fechado** (nada some, tudo cai em um bloco) — independente de o "Resultado líquido" de junho já ser conhecido pelo Wesley, porque a margem vem de outra fonte (Phase 88 compõe).

Purpose: Fechar a Phase 87 com evidência de que os custos operacionais agregam corretamente e o isolamento org-first funciona, antes do frontend (Phase 88) consumir.
Output: relatório de reconciliação + anti-IDOR anexado ao SUMMARY (sem mudança de código).

DEPENDÊNCIA CRÍTICA: o backfill de competence_date da Phase 86 ainda estava drenando (cobertura baixa e subindo). A reconciliação de valores absolutos só é confiável com cobertura de junho ≥90%. Se estiver abaixo, executar o check de MECÂNICA (categoria→bloco, anti-IDOR, total fechado sobre as linhas JÁ com competência) e registrar a cobertura como pendência — NÃO apresentar o resultado como "Resultado líquido final" ao Wesley.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/87-dre-agrega-o-de-resultado-por-compet-ncia/87-CONTEXT.md
@.planning/phases/87-dre-agrega-o-de-resultado-por-compet-ncia/87-RESEARCH.md
@.planning/phases/87-dre-agrega-o-de-resultado-por-compet-ncia/87-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Medir cobertura de competence_date de junho/2026 (gate da reconciliação)</name>
  <read_first>
    - 87-RESEARCH.md → "Reconciliation Plan" → "Pre-condition — coverage check"
    - STATE / memória: backfill Phase 86 ainda drenando; não assumir ≥90%
  </read_first>
  <what-built>
    Nada de código. Mede quanto das linhas de cash_outflows de 2026 (e especificamente junho) já têm
    competence_date preenchido, para decidir se a reconciliação de valores absolutos é confiável ou se
    vale só como check direcional de mecânica.
  </what-built>
  <action>Orquestrador (com Supabase MCP) roda os dois SELECTs de cobertura descritos em how-to-verify e registra o pct; decide entre reconciliação de valores (≥90%) ou só mecânica (<90%). Gate da Task 2.</action>
  <how-to-verify>
    Orquestrador via Supabase MCP (ckcdevcxgvueywivefgx), org Pé Vermeio (7f615df7):
    1. SELECT count(*) FILTER (WHERE competence_date IS NOT NULL) AS com, count(*) AS total, round(100.0*count(*) FILTER (WHERE competence_date IS NOT NULL)/NULLIF(count(*),0),1) AS pct FROM cash_outflows WHERE organization_id = '<org uuid>' AND tiny_payable_id IS NOT NULL AND outflow_date >= '2026-01-01';
    2. Idem, restrito a junho: ... AND outflow_date >= '2026-06-01' AND outflow_date < '2026-07-01'.
    3. Registrar pct. Se junho ≥90% → reconciliação de valores é confiável. Se <90% → seguir só com o check de mecânica (Task 2) e marcar cobertura como pendência bloqueante do "número final".
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint MCP executado pelo orquestrador; evidência = os dois SELECTs de cobertura</automated>
  </verify>
  <acceptance_criteria>
    - Cobertura de competence_date de junho/2026 medida e registrada (pct explícito).
    - Decisão registrada: reconciliação de valores confiável (≥90%) OU só mecânica (<90%, cobertura pendente).
  </acceptance_criteria>
  <resume-signal>Cole os pct de cobertura (2026 e junho); digite "cobertura medida" para seguir</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Reconciliar total fechado + provar anti-IDOR</name>
  <read_first>
    - 87-RESEARCH.md → "Reconciliation Plan" (steps 1-4) e "Security Domain" (teste IDOR com p_org_id alheio)
    - memória feedback_rpc_rls_correlated_subquery_timeout / método de impersonação (SET LOCAL ROLE authenticated + set_config request.jwt.claims em transação ROLLBACK)
  </read_first>
  <what-built>
    Nada de código. Prova duas coisas sobre a RPC do 87-01: (a) reconciliação de total fechado —
    nenhuma categoria some, tudo bate; (b) isolamento de tenant (anti-IDOR) — org alheia = 0 linhas.
  </what-built>
  <action>Orquestrador (com Supabase MCP) executa os blocos A (reconciliação de total fechado + inspeção de outros_operacionais) e B (impersonação authenticated para o teste anti-IDOR cross-org) descritos em how-to-verify, e registra delta + prova own/cross-org. Gate de fechamento da Phase 87.</action>
  <how-to-verify>
    Orquestrador via Supabase MCP (ckcdevcxgvueywivefgx):

    A) RECONCILIAÇÃO DE TOTAL FECHADO (SC-4):
    1. Rodar a RPC: SELECT bloco, SUM(total) FROM get_dre_operational_by_competence('<Pé Vermeio uuid>', '2026-06-01') GROUP BY bloco ORDER BY 2 DESC;
    2. Agregado manual direto: SELECT category, SUM(amount) FROM cash_outflows WHERE organization_id = '<Pé Vermeio uuid>' AND competence_date = '2026-06-01' GROUP BY category ORDER BY 2 DESC;
    3. Cross-check: cada category do agregado manual aparece em EXATAMENTE um bloco da RPC (nomeado ou outros_operacionais), OU é uma das exclusões conhecidas. Somar: Σ(todos os blocos da RPC, incl. outros_operacionais) + Σ(categorias excluídas do manual) deve igualar Σ(todas as linhas de junho por competência). Registrar o delta (esperado R$0,00).
    4. Inspecionar outros_operacionais: se o valor for suspeito (alto, "cara de Estrutura/Pessoal"), é sinal de mismatch de string → voltar ao 87-01 Task 1/2. Caso contrário, listar as categorias que legitimamente caíram ali.
    5. Confirmar financeiro_bruto (Empréstimo) vem com financeiro_is_approximate=true e NÃO está somado ao operacional.

    B) ANTI-IDOR (SC-4):
    6. Impersonar um usuário membro SÓ da org Pé Vermeio (SET LOCAL ROLE authenticated + set_config('request.jwt.claims', jwt da Pé Vermeio, true) numa transação ROLLBACK) e chamar get_dre_operational_by_competence('<Thales e4150d57 uuid>', '2026-06-01') — DEVE retornar 0 linhas (RLS zera org alheia), sem erro.
    7. Na mesma sessão, chamar com o próprio org (Pé Vermeio) — DEVE retornar as linhas normais (own > 0). Contraste own>0 / cross-org=0 é a prova.
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint MCP executado pelo orquestrador; evidência = delta de reconciliação (=R$0,00) + prova own>0/cross-org=0</automated>
  </verify>
  <acceptance_criteria>
    - Total fechado reconcilia: Σ(blocos incl. outros_operacionais) + Σ(excluídas) = Σ(junho por competência), delta = R$0,00 (nas linhas com competência).
    - Toda categoria viva de junho mapeada a exatamente um bloco ou exclusão conhecida; outros_operacionais inspecionado e justificado.
    - Anti-IDOR provado: cross-org (Thales) = 0 linhas, own (Pé Vermeio) > 0, sem erro.
    - financeiro_bruto isolado com is_approximate=true.
    - Cobertura de junho (da Task 1) registrada junto ao veredito; se <90%, resultado marcado como direcional/pendente.
  </acceptance_criteria>
  <resume-signal>Cole o delta de reconciliação + a prova anti-IDOR (own vs cross-org); digite "reconciliado" para fechar a Phase 87</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → RPC | Chamada com p_org_id arbitrário; isolamento validado empiricamente aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04 | Information Disclosure / Elevation | get_dre_operational_by_competence chamada cross-org | mitigate | Teste empírico de impersonação (Task 2B): JWT Pé Vermeio + p_org_id Thales → 0 linhas. Confirma a mitigação T-87-01 do plano 87-01 em runtime. |
| T-87-05 | Tampering (reconciliação enganosa) | cobertura de backfill parcial | mitigate | Gate de cobertura (Task 1): resultado só é "final" com junho ≥90%; abaixo disso, marcado direcional/pendente para não apresentar número subcontado como Resultado líquido. |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência instalada (só queries de verificação). |
</threat_model>

<verification>
- Reconciliação de total fechado com delta R$0,00 (nas linhas com competência) para junho/2026.
- Anti-IDOR provado empiricamente (cross-org=0, own>0).
- Cobertura de junho medida e registrada; veredito qualificado por ela.
</verification>

<success_criteria>
- SC-4 satisfeito: anti-IDOR provado + reconciliação com mês real fechado (junho/2026) documentada.
- outros_operacionais inspecionado (sem mismatch de string oculto).
- Financeiro isolado/flagueado, fora do operacional.
</success_criteria>

<output>
Create `.planning/phases/87-dre-agrega-o-de-resultado-por-compet-ncia/87-02-SUMMARY.md` when done.
Registrar: delta de reconciliação, cobertura de junho, prova anti-IDOR, lista de categorias em outros_operacionais.
</output>
