---
phase: 87-dre-agrega-o-de-resultado-por-compet-ncia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]
must_haves:
  truths:
    - "get_dre_operational_by_competence classifies every cash_outflows.category into the exact 87-CONTEXT block map: impostos_venda / pessoal / estrutura / servicos / operacional / financeiro / excluido / nao_classificado"
    - "Rows with NULL competence_date fall into their outflow_date month via COALESCE(competence_date, date_trunc('month', outflow_date)::date) — no 2026 value is silently dropped (~8.7% of rows)"
    - "Cartão de crédito is classified as operacional (not excluido) AND flagged double_count_risk=true so the embedded ML-fatura double-count stays visible; it is never auto-netted"
    - "Empréstimo returns its full parcela total (juro+principal); no SAC split; the misleading approximate flag is gone"
    - "The RPC stays SECURITY INVOKER: a member of a foreign org passing Pé Vermeio's p_org_id gets 0 rows (anti-IDOR via RLS is_org_member)"
    - "Σ of all blocos for June/2026 reconciles to a same-source hand-check of cash_outflows June competence (fallback-adjusted) at R$0,00 delta"
  artifacts:
    - path: "supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql"
      provides: "DROP + CREATE OR REPLACE of get_dre_operational_by_competence reconciled to the current CONTEXT map, plus REVOKE/GRANT"
      contains: "double_count_risk"
  key_links:
    - from: "supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql"
      to: "public.cash_outflows"
      via: "COALESCE(competence_date, outflow_date month) window filter + category CASE classification"
      pattern: "COALESCE\\(co.competence_date"
    - from: "get_dre_operational_by_competence"
      to: "authenticated role"
      via: "REVOKE EXECUTE FROM PUBLIC, anon + GRANT EXECUTE TO authenticated in the same migration"
      pattern: "GRANT +EXECUTE ON FUNCTION public.get_dre_operational_by_competence"
---

<objective>
Reconcile the already-in-prod (via drift, unmerged branch) `get_dre_operational_by_competence(p_org_id uuid, p_month date)` RPC to Wesley's CURRENT locked decisions in 87-CONTEXT.md, and CODIFY that reconciliation as a tracked migration (same pattern as Phase 86: author migration file → orchestrator applies via Supabase MCP `apply_migration` → confirm/reconcile). This is NOT greenfield — the function exists; Phase 87 diffs the deployed body against the new map and re-ships it, then proves it as an `authenticated` user (anti-IDOR + timeout) and reconciles June/2026.

The five deltas vs the deployed body (per 87-CONTEXT):
1. `Cartão de crédito`: move `excluido` → `operacional`, and expose a `double_count_risk` flag so the embedded ML-fatura double-count is VISIBLE (never auto-netted — Wesley fixes at source going forward).
2. `competence_date` NULL fallback: `COALESCE(co.competence_date, date_trunc('month', co.outflow_date)::date)` in BOTH window bounds (currently NULL-competence rows are dropped).
3. `Empréstimo`: keep the FULL parcela sum (already correct) but drop the now-misleading `financeiro_is_approximate` column (nothing is approximated — no SAC split).
4. `Insumos`/`Itens do CD`: move `servicos` → `operacional`; add `Serviços gerais` to `servicos`; add `Impostos, taxas` + `Veículos, transportes` to `operacional`.
5. `pessoal` gains `Pró-labore`; `excluido` gains `Reembolso cliente`; the `ELSE` catch-all becomes a distinct visible `nao_classificado` bloco (Wesley's "não esconder" principle; folds today's 0-row `Outros`).

Purpose: Fase 2 of 3 of the "DRE de Resultado" milestone — the aggregation layer Phase 88's frontend consumes. Backend only; no frontend.
Output: `supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql`, applied and confirmed in prod `ckcdevcxgvueywivefgx`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-dre-agrega-o-de-resultado-por-compet-ncia/87-CONTEXT.md
@.planning/phases/87-dre-agrega-o-de-resultado-por-compet-ncia/87-RESEARCH.md
@.planning/phases/86-dre-compet-ncia-no-contas-a-pagar/86-01-PLAN.md
@supabase/migrations/20260618100000_cash_flow_tables.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the DRE-map reconciliation migration (DROP + CREATE OR REPLACE)</name>
  <files>supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql</files>
  <read_first>
    - 87-CONTEXT.md → "Mapa categoria → linha da DRE" (the authoritative category→bloco map) and the three LOCKED decisions (Cartão de crédito, competence fallback, Empréstimo full parcela). Implements D: Cartão→operacional+visible, COALESCE fallback, Empréstimo cheio.
    - 87-RESEARCH.md → "Pattern 1" (the verbatim deployed RPC body — this is your starting point to diff) and its 7-item "What must change for the CURRENT map" list; Pitfall 1 (migration numbering — never reuse 20260687*/20260690*), Pitfall 5 (amount is always positive).
    - 86-01-PLAN.md → the migration-file + REVOKE/GRANT + "apply via MCP only" header pattern to mirror.
    - supabase/migrations/20260618100000_cash_flow_tables.sql → `cash_outflows` schema (competence_date nullable, amount always positive, RLS cash_outflows_select via is_org_member) + index cash_outflows_org_competence_category_idx.
  </read_first>
  <action>
    Create the migration reconciling `public.get_dre_operational_by_competence(p_org_id uuid, p_month date)` to the current 87-CONTEXT map. Because the RETURNS TABLE column set changes (the `financeiro_is_approximate` column is replaced by `double_count_risk`), a plain CREATE OR REPLACE would raise 42P13 "cannot change return type" — so the file must FIRST `DROP FUNCTION IF EXISTS public.get_dre_operational_by_competence(uuid, date);` then CREATE. The DROP is safe: nothing in `main` consumes this function (Phase 88 frontend not built here; no view/RPC depends on it).

    The file, in order:
    (1) Header comment: reconciles the drift-deployed get_dre_operational_by_competence to 87-CONTEXT (2026-07-08). Apply ONLY via Supabase MCP `apply_migration` on `ckcdevcxgvueywivefgx` (NOT the CLAUDE.md id, NOT `supabase db push` — no CLI token). Timestamp 20260692000000 chosen to sit above the highest migration in either line (main max 20260690000100) per Pitfall 1.
    (2) `DROP FUNCTION IF EXISTS public.get_dre_operational_by_competence(uuid, date);`
    (3) `CREATE FUNCTION public.get_dre_operational_by_competence(p_org_id uuid, p_month date) RETURNS TABLE (bloco text, category text, total numeric, n integer, double_count_risk boolean) LANGUAGE sql STABLE SECURITY INVOKER SET search_path TO 'public' AS $function$ ... $function$;` — keep it a single `SELECT ... GROUP BY` (no correlated subquery / no LATERAL — T-87-03 timeout guard; the shape already satisfies this).
    (4) The CASE → bloco map, EXACTLY per 87-CONTEXT (implements SC-1/SC-2):
        - `impostos_venda`: `'Imposto Venda - ICMS','Imposto Venda - PIS','Imposto Venda - COFINS'`
        - `pessoal`: `'Salários','Pró-labore','Pessoal - INSS'`
        - `estrutura`: `'Aluguéis e condomínio','Água, luz','Telecomunicação, internet'`
        - `servicos`: `'Contabilidade','Serviços gerais'`
        - `operacional`: `'Insumos','Itens do CD','Impostos, taxas','Veículos, transportes','Cartão de crédito'`
        - `financeiro`: `'Empréstimo'`
        - `excluido`: `'Fornecedores','Previsões de compra','Aporte','ADS Mercado Livre','Prestação de serviço do Mercado Envios Full','ADS Shopee','Ads Magazine Luiza','Vendas Mercado Livre','Vendas Magalu','Reembolso cliente'`
        - `ELSE 'nao_classificado'` (distinct visible bucket — Claude's-discretion call per CONTEXT, chosen so a future uncategorized Tiny category surfaces instead of silently inflating operacional; folds today's 0-row `Outros`).
    (5) SELECT columns: `bloco`, `co.category`, `sum(co.amount) AS total`, `count(*)::integer AS n`, and `(co.category = 'Cartão de crédito') AS double_count_risk` (implements the Cartão visibility decision; Empréstimo full-parcela decision needs no flag — its total is already the full sum, and the old approximate flag is intentionally not carried over).
    (6) WHERE: `co.organization_id = p_org_id` AND the competence window applied to `COALESCE(co.competence_date, date_trunc('month', co.outflow_date)::date)` on BOTH bounds: `>= date_trunc('month', p_month)::date` and `< (date_trunc('month', p_month) + interval '1 month')::date` (implements the fallback decision — SC-1/SC-4 no-value-lost).
    (7) `GROUP BY 1, co.category` then `ORDER BY 1, sum(co.amount) DESC`. (double_count_risk is derived from the grouped co.category, so no extra GROUP BY term is required.)
    (8) `REVOKE EXECUTE ON FUNCTION public.get_dre_operational_by_competence(uuid, date) FROM PUBLIC, anon;` then `GRANT EXECUTE ON FUNCTION public.get_dre_operational_by_competence(uuid, date) TO authenticated;` (re-issued in this same migration — Postgres does not preserve prior grants across DROP/CREATE; T-87-02).

    Do NOT touch `get_cashflow` (Phase 60 DFC), `get_imposto_guia_by_competence` (leave the drift-deployed one untouched — the impostos_venda deduction comes from THIS RPC's impostos_venda bloco; the Phase 88 double-count question is a checkpoint item, not a change here), `cash_outflows` schema, or any cron. Amount is always positive (Pitfall 5) — `sum(co.amount)` is a positive expense total; do not negate.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && f=supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql && grep -q 'DROP FUNCTION IF EXISTS public.get_dre_operational_by_competence(uuid, date)' "$f" && grep -q 'SECURITY INVOKER' "$f" && grep -qF "COALESCE(co.competence_date, date_trunc('month', co.outflow_date)::date)" "$f" && grep -q 'double_count_risk' "$f" && grep -qF "'Pró-labore'" "$f" && grep -qF "'Serviços gerais'" "$f" && grep -qF "'nao_classificado'" "$f" && [ "$(grep -cF "'Cartão de crédito'" "$f")" -ge 2 ] && grep -Eq 'REVOKE EXECUTE ON FUNCTION public.get_dre_operational_by_competence.*FROM PUBLIC, anon' "$f" && grep -Eq 'GRANT +EXECUTE ON FUNCTION public.get_dre_operational_by_competence.*TO authenticated' "$f" && echo DRE_MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql` exists and starts with a `DROP FUNCTION IF EXISTS public.get_dre_operational_by_competence(uuid, date);` before the CREATE.
    - Function is `LANGUAGE sql STABLE SECURITY INVOKER SET search_path TO 'public'` and returns `(bloco, category, total, n, double_count_risk)` — the `financeiro_is_approximate` column is not carried over.
    - CASE map matches 87-CONTEXT exactly: `Cartão de crédito` under `operacional` (not `excluido`); `Insumos`/`Itens do CD` under `operacional`; `Serviços gerais` under `servicos`; `Pró-labore` under `pessoal`; `Reembolso cliente` under `excluido`; `ELSE 'nao_classificado'`.
    - Window filter uses `COALESCE(co.competence_date, date_trunc('month', co.outflow_date)::date)` on BOTH bounds.
    - `double_count_risk = (co.category = 'Cartão de crédito')` present (so `'Cartão de crédito'` appears ≥2×: operacional list + flag expression).
    - `REVOKE ... FROM PUBLIC, anon` and `GRANT ... TO authenticated` both re-issued for the function.
    - No correlated subquery / LATERAL; `get_cashflow`, `get_imposto_guia_by_competence`, cash_outflows schema and crons untouched.
    - Grep gate prints DRE_MIGRATION_OK.
  </acceptance_criteria>
  <done>Migration file authored, self-consistent, and diffs cleanly against the deployed body per the 5 CONTEXT deltas; grep gate prints DRE_MIGRATION_OK.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply via Supabase MCP, reconcile June/2026, prove anti-IDOR (orchestrator-only)</name>
  <what-built>The migration `20260692000000_dre_operational_reconcile_context_map.sql` (DROP + CREATE of `get_dre_operational_by_competence` reconciled to the current CONTEXT map + REVOKE/GRANT). The executor has no Supabase MCP; the orchestrator applies it to `ckcdevcxgvueywivefgx`, confirms the shape, reconciles June/2026, and proves anti-IDOR as a real `authenticated` role.</what-built>
  <read_first>
    - 87-RESEARCH.md → Pitfall 1 (verify prod state BEFORE apply — the function already exists via drift), Pitfall 2 (RLS-under-INVOKER cost is invisible as postgres — test as authenticated), Pitfall 3 (reconcile cash_outflows same-source, NOT against Tiny's "Custos e-commerce" screen), Pitfall 4 (execute_sql returns only the LAST result set — bundle checks via jsonb_build_object / one call per SELECT).
    - 87-CONTEXT.md → "Segurança" (SECURITY INVOKER + anti-IDOR by caller's org) and "Reconciliação" (junho/2026).
    - MEMORY: org `7f615df7` = Pé Vermeio (has costs, valid MCO); org `e4150d57` = Thales (foreign). Supabase id = `ckcdevcxgvueywivefgx`, NOT the CLAUDE.md id.
  </read_first>
  <how-to-verify>
    1. BASELINE (before apply), via MCP execute_sql on `ckcdevcxgvueywivefgx` — confirm the drift state Pitfall 1 warns about:
       - `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'get_dre_operational_by_competence';` — record the CURRENT (drifted) body; confirm it is the pre-reconciliation shape (financeiro_is_approximate present, Cartão in excluido, no COALESCE fallback).
       - Baseline advisors: MCP `get_advisors` (security + performance) — snapshot to diff after apply.
    2. APPLY via MCP `apply_migration` (name: dre_operational_reconcile_context_map, body = the migration file). NOT `supabase db push`.
    3. CONFIRM new shape (execute_sql; bundle via jsonb_build_object to dodge Pitfall 4):
       - `SELECT prosecdef, pg_get_functiondef(oid) FROM pg_proc WHERE proname='get_dre_operational_by_competence';` → `prosecdef=false` (INVOKER); body contains `double_count_risk`, `COALESCE(co.competence_date`, `'Cartão de crédito'` inside the operacional list, `'nao_classificado'`.
       - Privileges (T-87-02): `SELECT has_function_privilege('anon','public.get_dre_operational_by_competence(uuid,date)','EXECUTE') AS anon, has_function_privilege('authenticated','public.get_dre_operational_by_competence(uuid,date)','EXECUTE') AS auth;` → anon=false, auth=true.
    4. RECONCILE June/2026 (same-source, Pitfall 3), single result set:
       - `SELECT jsonb_build_object('rpc_all_blocos', (SELECT sum(total) FROM get_dre_operational_by_competence('7f615df7-...'::uuid,'2026-06-01'::date)), 'raw_cashflows', (SELECT sum(amount) FROM cash_outflows WHERE organization_id='7f615df7-...'::uuid AND COALESCE(competence_date, date_trunc('month', outflow_date)::date) >= '2026-06-01' AND COALESCE(competence_date, date_trunc('month', outflow_date)::date) < '2026-07-01'));` → the two totals must be EQUAL (R$0,00 delta — every row lands in exactly one bloco including excluido/nao_classificado).
       - Confirm the Cartão flag surfaced: `SELECT bloco, total, double_count_risk FROM get_dre_operational_by_competence('7f615df7-...'::uuid,'2026-06-01'::date) WHERE category='Cartão de crédito';` → bloco=`operacional`, double_count_risk=true (if June has a Cartão row).
    5. ANTI-IDOR proof (SC-4) — impersonate a real member of the FOREIGN org and call with Pé Vermeio's p_org_id:
       ```
       BEGIN;
       SET LOCAL statement_timeout = '8s';
       SET LOCAL ROLE authenticated;
       SELECT set_config('request.jwt.claims', '{"sub":"<real-e4150d57-member-uuid>","role":"authenticated"}', true);
       SELECT count(*) FROM get_dre_operational_by_competence('7f615df7-...'::uuid,'2026-06-01'::date);  -- expect 0
       EXPLAIN (ANALYZE, TIMING) SELECT * FROM get_dre_operational_by_competence('7f615df7-...'::uuid,'2026-06-01'::date);  -- runs well under 8s, no per-row SubPlan
       ROLLBACK;
       ```
       Foreign-org member → 0 rows; a real Pé Vermeio member → rows return and finish under the 8s authenticated timeout.
    6. NON-REGRESSION: MCP `get_advisors` shows no NEW security/performance issue vs baseline; confirm `get_cashflow` and `get_imposto_guia_by_competence` definitions are unchanged (DFC + imposto RPC untouched).
    7. FLAG to Wesley (open question, do not resolve here): Phase 88 must source the impostos-sobre-venda deduction from EITHER this RPC's `impostos_venda` bloco OR `get_imposto_guia_by_competence` — never both (double-count). Surface this as the one decision Phase 88 planning needs.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied via MCP `apply_migration` (not CLI).
    - Function is SECURITY INVOKER (`prosecdef=false`); body carries `double_count_risk`, the COALESCE fallback, Cartão under operacional, `nao_classificado` ELSE.
    - `anon` has no EXECUTE; `authenticated` has EXECUTE.
    - June/2026 reconciliation: Σ all blocos == Σ cash_outflows June competence (fallback-adjusted), R$0,00 delta.
    - Cartão de crédito (if present in June) returns bloco=operacional, double_count_risk=true.
    - Anti-IDOR: foreign-org member gets 0 rows; query completes under the 8s authenticated timeout with no per-row SubPlan.
    - No new advisor issue; `get_cashflow` and `get_imposto_guia_by_competence` unchanged.
    - Phase 88 impostos double-count question recorded for hand-off.
  </acceptance_criteria>
  <resume-signal>Type "applied" with the June reconciliation delta (expect R$0,00), the anti-IDOR result (expect 0 rows for the foreign org), and confirmation the Phase 88 impostos question is noted — or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated role → get_dre_operational_by_competence | untrusted caller passes an arbitrary `p_org_id`; RLS on cash_outflows is the real tenant boundary |
| DROP/CREATE of a public RPC | privilege re-grant surface — grants are lost on DROP and must be re-issued |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Information Disclosure / Elevation of Privilege | `get_dre_operational_by_competence` p_org_id parameter | mitigate | SECURITY INVOKER (never DEFINER) + RLS `cash_outflows_select` (is_org_member); proven by impersonating a foreign-org `authenticated` member → 0 rows (Task 2 step 5) |
| T-87-02 | Elevation of Privilege | EXECUTE grant after DROP/CREATE | mitigate | `REVOKE EXECUTE FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated` re-issued in the same migration; verified via has_function_privilege (anon=false, auth=true) |
| T-87-03 | Denial of Service | RLS-under-INVOKER statement_timeout | mitigate | single-table `WHERE org = p_org_id GROUP BY` — no correlated subquery / LATERAL; proven under the real `authenticated` role (8s timeout) with EXPLAIN ANALYZE, not just postgres (Pitfall 2) |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | this phase installs zero packages (pure SQL migration); no legitimacy audit required |
</threat_model>

<verification>
- Task 1 grep gate prints DRE_MIGRATION_OK (map, fallback, flag, revoke/grant all present; DROP precedes CREATE).
- MCP-confirmed: prosecdef=false (INVOKER); body contains double_count_risk + COALESCE fallback + Cartão under operacional + nao_classificado ELSE; anon has no EXECUTE.
- June/2026 same-source reconciliation delta = R$0,00.
- Anti-IDOR: foreign-org authenticated member → 0 rows; query under 8s.
- get_cashflow + get_imposto_guia_by_competence + cash_outflows schema + crons untouched; no new advisor issue.
</verification>

<success_criteria>
- SC-1: RPC aggregates by competence (with COALESCE fallback) and classifies into the CONTEXT blocks (impostos_venda/pessoal/estrutura/servicos/operacional/financeiro), Empréstimo full parcela.
- SC-2: EXCLUI Fornecedores/Previsões de compra (CMV), Aporte (capital), other-channel + ML-cost categories via the `excluido` bloco.
- SC-3: no IRPJ/CSLL/FGTS — the RPC never touches income-tax categories (none exist in cash_outflows).
- SC-4: anti-IDOR (SECURITY INVOKER + RLS) proven as a real authenticated role; June/2026 reconciled to a closed month at R$0,00.
- SC-5: output shape (bloco, category, total, n, double_count_risk) ready for Phase 88 to compose Receita → Margem → Resultado operacional → Resultado líquido.
</success_criteria>

<output>
Create `.planning/phases/87-dre-agrega-o-de-resultado-por-compet-ncia/87-01-SUMMARY.md` when done, recording: the June/2026 reconciliation delta, the anti-IDOR result, whether June had a Cartão de crédito row (double_count_risk), and the flagged Phase 88 impostos-source decision.
</output>
