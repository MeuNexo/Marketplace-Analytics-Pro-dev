---
phase: 78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/mercadolivre/MLDevolucoes.tsx
  - src/pages/org/OrgSettings.tsx
  - src/components/org/OrgMembersTab.tsx
  - src/components/org/OrgInvitesTab.tsx
  - src/pages/Sellers.tsx
  - src/pages/Integrations.tsx
  - src/pages/Profile.tsx
  - src/pages/mercadolivre/MLFiscal.tsx
  - src/pages/mercadolivre/MLPerguntas.tsx
  - src/pages/mercadolivre/MLMetas.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Em /organizacao, as abas (TabsList) são acessíveis no mobile e a linha de membro não esmaga botões de ação em 360px"
    - "Em /fiscal, o Dialog de configuração (Lucro Real, grid ICMS) cabe e é scrollável em 360px"
    - "Ações icon-only (Sellers, Integrações, Profile) são descobríveis por toque, não só por hover"
    - "A tabela de Devoluções tem largura mínima com scroll intencional em vez de comprimir colunas"
  artifacts:
    - path: "src/pages/org/OrgSettings.tsx"
      provides: "TabsList responsiva (wrap ou scroll) para as 4 abas em mobile"
      contains: "TabsList"
    - path: "src/pages/mercadolivre/MLFiscal.tsx"
      provides: "Grid ICMS colapsável no mobile (grid-cols-1 sm:grid-cols-3) + scroll interno garantido"
      contains: "sm:grid-cols-3"
  key_links:
    - from: "src/components/org/OrgMembersTab.tsx"
      to: "src/pages/Sellers.tsx"
      via: "padrão de ações por item (Dropdown/ícones) reutilizado do card de seller"
      pattern: "DropdownMenu|flex-wrap"
---

<objective>
Corrigir a responsividade das páginas de Pós-venda e Configurações: /devolucoes, /organizacao (abas + membros + convites), /sellers, /integracoes, /perfil, /fiscal, /perguntas e /metas.

Escopo (findings do audit Grupo C): C-03 (tabela Devoluções sem min-w), C-04 (OrgSettings TabsList cortada — MAJOR), C-05 (linha de membro esmaga 4 botões — MAJOR), C-11 (grid ICMS fiscal estoura dialog — MAJOR), C-06 (botão convite sem w-full), C-07 (botões Sellers < 40px + tooltip hover), C-08 (botões loja Integrações só title/hover), C-09 (upload avatar group-hover), C-10 (sticky header config offsets), C-12 (Dialog fiscal scroll interno), C-13 (textarea Perguntas sem autoFocus).

Purpose: Configurações e pós-venda contêm 3 MAJORs que quebram funções no mobile: abas de organização cortadas, linha de membro impraticável, e o Dialog fiscal com grid de 3 colunas estourando. As ações icon-only dependentes de hover são invisíveis ao toque — problema recorrente para o público leigo do projeto. Reusar o padrão de ações por item do card de /sellers (Tooltip shadcn / ícones) onde aplicável.
Output: 10 arquivos de Pós-venda/Config responsivos, sem regressão de testes/build.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-CONTEXT.md
@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-C.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /organizacao — TabsList responsiva (MAJOR) + linha de membro sem esmagar botões (MAJOR) + botão de convite</name>
  <files>src/pages/org/OrgSettings.tsx, src/components/org/OrgMembersTab.tsx, src/components/org/OrgInvitesTab.tsx</files>
  <read_first>
    - src/pages/org/OrgSettings.tsx (~37-43 TabsList com 4 abas)
    - src/components/org/OrgMembersTab.tsx (~186-272 linha de membro com Avatar + nome + Select + até 3 botões)
    - src/components/org/OrgInvitesTab.tsx (~104-122 formulário de convite)
    - src/pages/Sellers.tsx (~203-214 — padrão de ações por item com ícones/Tooltip, referência para agrupar ações do membro)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-C.md (findings C-04, C-05, C-06)
  </read_first>
  <action>
    [C-04] MAJOR — OrgSettings.tsx TabsList (~37) com 4 abas (Geral/Membros/Convites/Audit log) sem scroll/wrap é cortada em 360px. Tornar responsiva: ou `flex-wrap sm:flex-nowrap w-full` na TabsList, ou envolver num wrapper `overflow-x-auto -mx-4 px-4` para scroll horizontal controlado. Escolher a que preservar melhor o visual desktop (as 4 abas em linha em ≥sm).

    [C-05] MAJOR — OrgMembersTab.tsx (~186-272): a linha de membro empilha Avatar + nome + Select(w-28) + até 3 botões (Acesso/Transferir/Trash) numa única div flex sem wrap → ~360px só de botões, esmaga em 360px. Reorganizar: mover as ações de gerenciamento para uma segunda linha com flex-wrap, OU consolidar as ações num único DropdownMenu por item (trigger "..."), seguindo o padrão de ações por item de /sellers. Garantir shrink-0 nos botões e min-w-0 no nome. Comportamento das ações (mudar role, transferir, remover) inalterado.

    [C-06] MINOR — OrgInvitesTab.tsx (~104-122): o Button "Enviar convite" não tem largura responsiva. Adicionar className w-full md:w-auto ao botão para bom touch target na coluna mobile.
  </action>
  <verify>
    <automated>grep -Eq "flex-wrap|overflow-x-auto" src/pages/org/OrgSettings.tsx && grep -Eq "DropdownMenu|flex-wrap" src/components/org/OrgMembersTab.tsx && grep -q "w-full md:w-auto" src/components/org/OrgInvitesTab.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - OrgSettings TabsList responsiva (flex-wrap ou wrapper overflow-x-auto) — grep.
    - OrgMembersTab linha de membro com ações agrupadas (DropdownMenu) ou em segunda linha (flex-wrap) — grep; ações preservadas.
    - OrgInvitesTab botão com w-full md:w-auto — grep.
    - Lógica de gerenciamento de membros/convites inalterada.
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Em 360px: as 4 abas de organização acessíveis; linha de membro utilizável; botão de convite com bom alvo de toque.</done>
</task>

<task type="auto">
  <name>Task 2: /fiscal — grid ICMS colapsável (MAJOR) + scroll interno do Dialog + tabela Devoluções min-w</name>
  <files>src/pages/mercadolivre/MLFiscal.tsx, src/pages/mercadolivre/MLDevolucoes.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFiscal.tsx (~349-362 grid grid-cols-3 de ICMS por destino; ~562-637 Dialog de config com Tabs e LucroRealForm)
    - src/pages/mercadolivre/MLDevolucoes.tsx (~205-238 tabela de 6 colunas dentro de overflow-x-auto)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-C.md (findings C-11, C-12, C-03)
  </read_first>
  <action>
    [C-11] MAJOR — MLFiscal.tsx grid de ICMS por destino (~349) usa grid grid-cols-3 fixo dentro de um Dialog max-w-md → em 360px cada coluna cai a ~130px e os labels longos (Inter N/NE/CO/ES, Intra-estadual) truncam/estouram. Trocar para grid grid-cols-1 sm:grid-cols-3 (colunas colapsam no mobile). O grid grid-cols-2 vizinho (inputs LP) já está OK — não mexer.

    [C-12] MINOR — MLFiscal.tsx Dialog de config (~562-637): a aba Lucro Real (LucroRealForm ~10 inputs + ICMS) pode exceder a altura com teclado virtual. Adicionar overflow-y-auto max-h-[70dvh] ao container de conteúdo interno do Dialog (ex.: no wrapper dentro do TabsContent) para garantir scroll mesmo quando o DialogContent nativo não o fizer.

    [C-03] MAJOR — MLDevolucoes.tsx tabela (~205-238, 6 colunas): a coluna "Motivo" (texto livre) sem truncar comprime as demais em 360px. Adicionar min-w-[640px] à table (scroll horizontal intencional no wrapper overflow-x-auto existente) e aplicar max-w-[120px] truncate à coluna "Motivo" (espelhando o tratamento da coluna "Produto"). Dados inalterados.
  </action>
  <verify>
    <automated>grep -q "sm:grid-cols-3" src/pages/mercadolivre/MLFiscal.tsx && grep -Eq "max-h-\[70dvh\]|overflow-y-auto" src/pages/mercadolivre/MLFiscal.tsx && grep -q "min-w-\[640px\]" src/pages/mercadolivre/MLDevolucoes.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - MLFiscal grid ICMS usa grid-cols-1 sm:grid-cols-3 — grep; grid 2-col LP intocado.
    - MLFiscal Dialog interno com scroll garantido (max-h-[70dvh] overflow-y-auto) — grep.
    - MLDevolucoes table com min-w-[640px] e coluna Motivo truncada — grep min-w.
    - Zero mudança de cálculo fiscal / dados de devolução.
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Em 360px: config fiscal (Lucro Real + ICMS) cabe e rola; tabela de Devoluções com scroll lateral intencional.</done>
</task>

<task type="auto">
  <name>Task 3: Ações por toque — Sellers, Integrações, Profile + textarea Perguntas + sticky header config</name>
  <files>src/pages/Sellers.tsx, src/pages/Integrations.tsx, src/pages/Profile.tsx, src/pages/mercadolivre/MLPerguntas.tsx, src/pages/mercadolivre/MLMetas.tsx</files>
  <read_first>
    - src/pages/Sellers.tsx (~203-214 botões de ação h-7 w-7 com Tooltip hover)
    - src/pages/Integrations.tsx (~980-999 botões icon-only com title)
    - src/pages/Profile.tsx (~93-108 overlay de upload group-hover)
    - src/pages/mercadolivre/MLPerguntas.tsx (~321-332 Textarea de resposta)
    - src/pages/mercadolivre/MLMetas.tsx (~165 sticky header com offsets negativos)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-C.md (findings C-07, C-08, C-09, C-13, C-10)
  </read_first>
  <action>
    [C-07] MINOR — Sellers.tsx botões de ação (Power/Pencil/Trash) têm h-7 w-7 (28px, abaixo do mínimo touch ~40px) e a Tooltip só aparece no hover. Aumentar para h-9 w-9 (ou h-10 w-10). Manter os Tooltip. Adicionar sr-only label ao botão Power para leitores de tela.

    [C-08] MINOR — Integrations.tsx (~980-999): dois botões icon-only (X = resetar nome, Pencil = renomear) comunicados só por title (não dispara no touch). Envolver cada um num Tooltip do shadcn (padrão de /sellers) OU adicionar label de texto com hidden sm:inline junto ao ícone.

    [C-09] MINOR — Profile.tsx (~93-108): overlay de câmera usa opacity-0 group-hover:opacity-100 → invisível no touch. Adicionar group-active:opacity-100 (ou active:opacity-100), ou exibir o ícone Camera sempre visível como badge no canto do avatar. Resultado: no mobile o usuário vê que a foto é clicável.

    [C-13] MINOR — MLPerguntas.tsx (~321-332): Textarea de resposta sem autoFocus → botões abaixo ficam fora da tela com teclado virtual. Adicionar autoFocus ao Textarea (aciona scrollIntoView na maioria dos browsers mobile). Opcional: rows menor no mobile.

    [C-10] MINOR — MLMetas.tsx (~165) e demais sticky headers de config: os offsets negativos hardcoded (-top-4/-6/-8) podem sobrepor o Header nativo do app em browsers mobile com barra de endereço dinâmica. Adicionar scroll-mt para o topo (ex.: scroll-mt-[52px]) OU ajustar o top do sticky para descontar a altura do Header (~52px). Aplicar em MLMetas.tsx como representante; se trivial e sem conflito, os demais arquivos de config deste plano (Sellers, Integrations, Fiscal) já pertencem a este plano — o executor pode aplicar o mesmo scroll-mt neles ao tocá-los nas outras tasks. Documentar no SUMMARY quais páginas receberam o ajuste. (NÃO tocar arquivos fora deste plano — /organizacao e /perfil já estão neste plano; /vendas está no Plano 02, então não incluir.)
  </action>
  <verify>
    <automated>grep -Eq "h-9 w-9|h-10 w-10" src/pages/Sellers.tsx && grep -Eq "Tooltip|hidden sm:inline" src/pages/Integrations.tsx && grep -Eq "active:opacity-100|group-active:opacity-100" src/pages/Profile.tsx && grep -q "autoFocus" src/pages/mercadolivre/MLPerguntas.tsx && grep -q "scroll-mt-\[" src/pages/mercadolivre/MLMetas.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Sellers botões de ação com h-9/h-10 (grep) + sr-only no Power.
    - Integrations botões de loja com Tooltip ou label hidden sm:inline (grep).
    - Profile overlay com active/group-active:opacity-100 (grep).
    - MLPerguntas Textarea com autoFocus (grep).
    - MLMetas sticky header com scroll-mt-[...] (grep).
    - Ações/dados inalterados; só descoberta por toque e offsets.
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Ações icon-only descobríveis por toque; textarea de resposta rola para os botões; sticky header de config não sobrepõe o Header nativo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → nenhuma nova superfície | Mudanças de layout/CSS/aria/autoFocus. Nenhum fetch/RPC/EF/lógica de auth alterada; ações de membro/convite/seller usam os MESMOS handlers existentes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04 | Elevation of Privilege | Ações de membro (role/transferir/remover) movidas para DropdownMenu | accept | Apenas reposicionamento visual; os gates de permissão (canManage/isOwner) e handlers permanecem idênticos. Nenhuma ação nova exposta a papéis sem permissão. |
| T-78-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova instalada (apenas className/JSX/aria/autoFocus). Sem pacotes a auditar. |
</threat_model>

<verification>
- `npx vitest run` → 318+ testes verdes (exit 0).
- `npx vite build` → build limpo (exit 0).
- Grep gates das 3 tasks passam.
- Nenhuma mudança de comportamento de dados, permissões ou lógica fiscal.
</verification>

<success_criteria>
- /devolucoes, /organizacao, /sellers, /integracoes, /perfil, /fiscal, /perguntas e /metas utilizáveis em 360px.
- Build + testes verdes.
</success_criteria>

## Artifacts this phase produces (Plano 04)

- `OrgSettings.tsx`: TabsList responsiva.
- `OrgMembersTab.tsx`: ações de membro agrupadas (DropdownMenu) ou em segunda linha.
- `OrgInvitesTab.tsx`: botão w-full md:w-auto.
- `MLFiscal.tsx`: grid ICMS colapsável (sm:grid-cols-3) + scroll interno do Dialog.
- `MLDevolucoes.tsx`: table min-w-[640px] + coluna Motivo truncada.
- `Sellers.tsx` / `Integrations.tsx` / `Profile.tsx`: ações descobríveis por toque (h-9/h-10, Tooltip, active opacity).
- `MLPerguntas.tsx`: Textarea autoFocus.
- `MLMetas.tsx`: sticky header scroll-mt. Sem novas props públicas.

## Cobertura finding → task (Plano 04)

| Finding | Severidade | Arquivo | Task |
|---------|-----------|---------|------|
| C-04 — OrgSettings TabsList cortada | MAJOR | OrgSettings.tsx | Task 1 |
| C-05 — linha de membro esmaga 4 botões | MAJOR | OrgMembersTab.tsx | Task 1 |
| C-06 — botão convite sem w-full | MINOR | OrgInvitesTab.tsx | Task 1 |
| C-11 — grid ICMS fiscal estoura dialog | MAJOR | MLFiscal.tsx | Task 2 |
| C-12 — Dialog fiscal sem scroll interno | MINOR | MLFiscal.tsx | Task 2 |
| C-03 — tabela Devoluções sem min-w | MAJOR | MLDevolucoes.tsx | Task 2 |
| C-07 — botões Sellers <40px + tooltip hover | MINOR | Sellers.tsx | Task 3 |
| C-08 — botões loja Integrações só title/hover | MINOR | Integrations.tsx | Task 3 |
| C-09 — upload avatar group-hover | MINOR | Profile.tsx | Task 3 |
| C-13 — textarea Perguntas sem autoFocus | MINOR | MLPerguntas.tsx | Task 3 |
| C-10 — sticky header config offsets | MINOR | MLMetas.tsx (+ demais config do plano) | Task 3 |

<output>
Create `.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-04-SUMMARY.md` when done
</output>
