---
phase: 78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mercadolivre/MLPeriodPicker.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/LayoutShell.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "O seletor de período (MLPeriodPicker) abre e é totalmente utilizável em viewport de 360px — calendário cabe na tela e o botão de confirmar é alcançável"
    - "O usuário consegue trocar de organização no mobile (OrganizationSwitcher acessível abaixo de 640px)"
    - "Os botões de atalho de período (7d/30d/90d/etc.) quebram em múltiplas linhas quando não cabem, sem overflow"
  artifacts:
    - path: "src/components/mercadolivre/MLPeriodPicker.tsx"
      provides: "Calendário responsivo (1 mês no mobile, 2 no desktop) + PopoverContent com largura limitada ao viewport + quick ranges com flex-wrap"
      contains: "numberOfMonths"
    - path: "src/components/layout/Header.tsx"
      provides: "OrganizationSwitcher acessível no mobile (sem hidden sm:block bloqueando)"
      contains: "OrganizationSwitcher"
  key_links:
    - from: "src/components/mercadolivre/MLPeriodPicker.tsx"
      to: "src/hooks/use-mobile.tsx"
      via: "useIsMobile() controla numberOfMonths"
      pattern: "useIsMobile"
---

<objective>
Corrigir os componentes compartilhados de shell e seletor de período que quebram no mobile — os fixes de maior alavancagem da fase, pois afetam TODAS as páginas do app.

Escopo (findings do audit): A-01/C-02 (MLPeriodPicker calendário duplo estoura o viewport — BLOCKER), C-14 (quick ranges sem flex-wrap), C-01 (OrganizationSwitcher invisível no mobile — BLOCKER).

Purpose: O MLPeriodPicker é usado em quase todas as páginas de dados (Vendas, Financeiro, Pedidos, Devoluções, Fluxo de Caixa…); com o calendário de 2 meses estourando 360px, a seleção de período fica inacessível em todo o app. O OrganizationSwitcher escondido prende usuários multi-org na org ativa no celular. Corrigir esses dois componentes destrava a usabilidade mobile de forma transversal.
Output: MLPeriodPicker responsivo + OrganizationSwitcher acessível no mobile, sem regressão de testes/build.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-CONTEXT.md
@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-A.md
@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-C.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MLPeriodPicker responsivo — calendário 1 mês no mobile + popover limitado ao viewport + quick ranges com wrap</name>
  <files>src/components/mercadolivre/MLPeriodPicker.tsx</files>
  <read_first>
    - src/components/mercadolivre/MLPeriodPicker.tsx (arquivo alvo — ler inteiro; especial atenção às linhas ~72-103: PopoverContent, quick ranges, Calendar)
    - src/hooks/use-mobile.tsx (hook useIsMobile, breakpoint 768px — padrão do projeto)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-A.md (finding A-01) e 78-AUDIT-C.md (findings C-02, C-14)
  </read_first>
  <action>
    Corrige três findings no MLPeriodPicker, todos no mesmo arquivo:

    [A-01/C-02] BLOCKER — Calendário duplo (`numberOfMonths={2}`) estoura o viewport mobile. Importar `useIsMobile` de `@/hooks/use-mobile`, chamar `const isMobile = useIsMobile();` no topo do componente, e trocar o literal `numberOfMonths={2}` por `numberOfMonths={isMobile ? 1 : 2}`. No `PopoverContent`, adicionar limitação de largura ao viewport: manter as classes existentes e acrescentar `max-w-[calc(100vw-1rem)]` (ex.: className passa a conter `w-auto max-w-[calc(100vw-1rem)] p-3`). NÃO alterar a lógica de seleção de datas nem o range retornado.

    [C-14] MINOR — Botões de quick range (7d/30d/90d/Este mês) num `flex gap-1` sem wrap. Adicionar `flex-wrap` ao container dos QUICK_RANGES (o `<div className="flex gap-1 mb-3">` passa a `flex flex-wrap gap-1 mb-3`). Comportamento dos botões inalterado.

    Seguir o padrão do projeto: `useIsMobile` já é usado em MLPedidos/MLFinanceiro/MLAnuncios e no LayoutShell. NÃO introduzir novo hook nem checar `window.innerWidth` diretamente no render.
  </action>
  <verify>
    <automated>grep -q "numberOfMonths={isMobile ? 1 : 2}" src/components/mercadolivre/MLPeriodPicker.tsx && grep -q "useIsMobile" src/components/mercadolivre/MLPeriodPicker.tsx && grep -q "max-w-\[calc(100vw" src/components/mercadolivre/MLPeriodPicker.tsx && grep -q "flex-wrap" src/components/mercadolivre/MLPeriodPicker.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - MLPeriodPicker.tsx importa e chama `useIsMobile`.
    - `numberOfMonths={isMobile ? 1 : 2}` presente (grep exato).
    - PopoverContent contém `max-w-[calc(100vw-...` (grep).
    - Container de quick ranges contém `flex-wrap` (grep).
    - Nenhuma mudança na assinatura de props do componente nem na lógica de range (só layout).
    - `npx vitest run` exit 0 (318+ testes) e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Seletor de período totalmente utilizável em 360px: calendário de 1 mês, popover contido na tela, quick ranges quebram em linha quando necessário.</done>
</task>

<task type="auto">
  <name>Task 2: OrganizationSwitcher acessível no mobile (Header + fallback no drawer se necessário)</name>
  <files>src/components/layout/Header.tsx, src/components/layout/LayoutShell.tsx</files>
  <read_first>
    - src/components/layout/Header.tsx (linhas ~55-65: o wrapper `hidden sm:block` do OrganizationSwitcher)
    - src/components/layout/LayoutShell.tsx (Sheet/drawer do menu mobile ~linha 56; hook useIsMobile já importado aqui)
    - src/components/layout/OrganizationSwitcher.tsx (ler para entender a largura interna; o span interno já usa max-w/truncate)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-C.md (finding C-01)
  </read_first>
  <action>
    [C-01] BLOCKER — OrganizationSwitcher some no mobile por estar dentro de `<div className="hidden sm:block shrink-0">`.

    Abordagem preferida (mínima): no Header.tsx, remover o `hidden sm:block` do wrapper do OrganizationSwitcher e conter overflow com largura máxima no wrapper — ex.: `<div className="shrink-0 max-w-[140px] sm:max-w-none">` — de modo que o switcher fique visível em qualquer largura sem empurrar os outros itens do header. Ler o OrganizationSwitcher primeiro para confirmar que o texto interno já trunca (`truncate` / `max-w`); se não truncar bem em 140px, apoiar-se no `max-w-[140px] truncate` no wrapper.

    Se, ao ler o Header, o espaço horizontal em 360px não comportar o switcher junto do avatar/menu sem estourar (avaliar os outros elementos do header), então em vez de mostrar inline: montar o OrganizationSwitcher como PRIMEIRO item dentro do Sheet do menu mobile em LayoutShell.tsx (topo do drawer, antes da navegação), condicionado a `isMobile` (hook já importado no LayoutShell), e manter o switcher no header apenas em `sm:` (`hidden sm:block`). Escolher UMA das duas abordagens de forma que o resultado observável seja: em 360px existe UM caminho de toque para trocar de organização. Documentar no SUMMARY qual abordagem foi usada e por quê.

    NÃO alterar a lógica de troca de org (função/estado do OrganizationSwitcher permanece intocada) — apenas visibilidade/posicionamento.
  </action>
  <verify>
    <automated>test -z "$(grep -n 'hidden sm:block' src/components/layout/Header.tsx | grep -i organizationswitcher)" && grep -q "OrganizationSwitcher" src/components/layout/Header.tsx && echo HEADER_OK; grep -rq "OrganizationSwitcher" src/components/layout/Header.tsx src/components/layout/LayoutShell.tsx && echo SWITCHER_MOUNTED</automated>
  </verify>
  <acceptance_criteria>
    - O OrganizationSwitcher NÃO está mais gated por um wrapper `hidden sm:block` que o remove totalmente do mobile OU foi montado adicionalmente no drawer mobile do LayoutShell.
    - Existe pelo menos um ponto de acesso ao OrganizationSwitcher em viewport &lt; 640px (verificável lendo o resultado: sem `hidden sm:block` no wrapper do Header, ou presente no Sheet do LayoutShell sob `isMobile`).
    - Lógica de troca de organização inalterada (só visibilidade/posição).
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Usuário com múltiplas orgs consegue trocar de organização no celular por um caminho de toque real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → nenhuma nova superfície | Mudanças são exclusivamente de layout/CSS/visibilidade. Nenhum novo dado é lido, escrito ou exposto; OrganizationSwitcher já existente apenas muda de posição/visibilidade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Information Disclosure | OrganizationSwitcher exposto no mobile | accept | Componente já existente e já gated pela mesma lógica de auth/org; tornar visível no mobile não amplia o conjunto de orgs acessíveis (backend RLS org-first inalterado). Sem nova superfície de dados. |
| T-78-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova é instalada nesta fase (apenas mudanças de className/JSX). Sem pacotes a auditar. |
</threat_model>

<verification>
- `npx vitest run` → 318+ testes verdes (exit 0).
- `npx vite build` → build limpo (exit 0).
- Grep gates das duas tasks passam.
- Nenhuma mudança de comportamento de dados: só layout/visibilidade.
</verification>

<success_criteria>
- MLPeriodPicker utilizável em 360px (calendário 1 mês, popover contido, quick ranges com wrap).
- OrganizationSwitcher acessível no mobile.
- Build + testes verdes.
</success_criteria>

## Artifacts this phase produces (Plano 01)

- `MLPeriodPicker.tsx`: passa a usar `useIsMobile`; nova classe `max-w-[calc(100vw-1rem)]` no PopoverContent; `numberOfMonths` condicional. Sem novas props públicas.
- `Header.tsx` / `LayoutShell.tsx`: OrganizationSwitcher com novo posicionamento/visibilidade mobile. Sem novos símbolos exportados.

## Cobertura finding → task (Plano 01)

| Finding | Severidade | Arquivo | Task |
|---------|-----------|---------|------|
| A-01 / C-02 (dedupe) — MLPeriodPicker calendário duplo | BLOCKER | MLPeriodPicker.tsx | Task 1 |
| C-14 — quick ranges sem flex-wrap | MINOR | MLPeriodPicker.tsx | Task 1 |
| C-01 — OrganizationSwitcher invisível no mobile | BLOCKER | Header.tsx / LayoutShell.tsx | Task 2 |

<output>
Create `.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-01-SUMMARY.md` when done
</output>
