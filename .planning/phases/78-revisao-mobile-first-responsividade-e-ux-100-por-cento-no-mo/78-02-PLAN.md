---
phase: 78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/mercadolivre/MLPublicidade.tsx
  - src/pages/mercadolivre/MLFinanceiro.tsx
  - src/pages/mercadolivre/MLProdutosVendidos.tsx
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Em /publicidade, o rodapé de resumo das campanhas e as tabelas de campanhas/produtos patrocinados são utilizáveis em 360px (sem overflow do viewport; métricas-chave acessíveis)"
    - "Em /financeiro, as tabelas Lucro por Marca/Estado têm variante mobile e o gráfico de Composição não fica comprimido no mobile"
    - "Copy de UI não faz referência geográfica incorreta ('à esquerda') em layout empilhado mobile"
  artifacts:
    - path: "src/pages/mercadolivre/MLPublicidade.tsx"
      provides: "Rodapé de campanhas com flex-wrap + variante mobile (cards) das tabelas de campanhas e produtos patrocinados"
      contains: "flex-wrap"
    - path: "src/pages/mercadolivre/MLFinanceiro.tsx"
      provides: "Tabelas Marca/Estado com dual-layout isMobile + margem do gráfico condicional a isMobile"
      contains: "isMobile"
  key_links:
    - from: "src/pages/mercadolivre/MLFinanceiro.tsx"
      to: "src/hooks/use-mobile.tsx"
      via: "isMobile já importado — reusar para tabelas Marca/Estado e margem do chart"
      pattern: "isMobile"
---

<objective>
Corrigir a responsividade das páginas do Dashboard: /publicidade, /financeiro, /produtos-vendidos, /analise-precos e / (Vendas).

Escopo (findings do audit Grupo A): A-02 (rodapé campanhas overflow — BLOCKER), A-03 (tabela Produtos Patrocinados 15 col.), A-07 (tabela Campanhas 10 col.), A-04 (tabelas Marca/Estado sem mobile), A-05 (chart Composição comprimido), A-10 (KPI grid trunca), A-06 (copy à esquerda), A-08 (controles Análise de Preços quebram), A-09 (botões ícone-only).

Purpose: São as páginas de leitura financeira/ads mais consultadas. O rodapé de campanhas com gap-8 sem wrap esconde totais no mobile (BLOCKER); tabelas largas sem variante card e o chart comprimido tornam a análise inviável em 360px. Aplicar o padrão dual-layout já estabelecido na própria MLFinanceiro (tabela Lucro por Produto em ~linha 751) para consistência.
Output: 5 arquivos de página do Dashboard responsivos, sem regressão de testes/build.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-CONTEXT.md
@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-A.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /publicidade — rodapé de campanhas (BLOCKER) + variante mobile das tabelas Campanhas e Produtos Patrocinados</name>
  <files>src/pages/mercadolivre/MLPublicidade.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLPublicidade.tsx (arquivo alvo — ler inteiro; foco linhas ~641-708 tabela Campanhas + rodapé ~702, e ~752-879 tabela Produtos Patrocinados)
    - src/pages/mercadolivre/MLFinanceiro.tsx (~linha 751-775 — padrão de referência isMobile ternário: cards vs table)
    - src/hooks/use-mobile.tsx (useIsMobile)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-A.md (findings A-02, A-03, A-07)
  </read_first>
  <action>
    [A-02] BLOCKER — Rodapé de resumo das campanhas (div com quatro spans, hoje flex items-center gap-8) estoura em 360px sem wrap. Trocar para flex flex-wrap items-center gap-x-4 gap-y-1 mantendo text-xs text-muted-foreground e demais classes. Conteúdo/valores inalterados.

    [A-07] MAJOR — Tabela de Campanhas (10 colunas com whitespace-nowrap) exige scroll horizontal extenso. Importar/reusar useIsMobile; criar variante mobile em cards mostrando as colunas-chave (Campanha/Nome, Status, Gasto, ROAS) via ternário isMobile: cards no mobile, tabela no desktop, seguindo o padrão de MLFinanceiro.tsx:751. Manter a tabela completa em desktop dentro do wrapper overflow-x-auto existente. O card mobile lê os MESMOS dados de filteredCampaigns (sem novo fetch).

    [A-03] MAJOR — Tabela Produtos Patrocinados (15 colunas). Mesma abordagem: variante mobile em cards com as colunas-chave (Produto, Gasto, ROAS, ACoS, Estoque), tabela completa só no ramo desktop. Reusar isMobile.

    Regra dual-layout (lição Phase 71): se algum card/linha tiver interação por item (clique/menu), garantir que o gatilho exista no ramo mobile também. Aqui as tabelas são de leitura; se houver clique em linha, replicar no card.
  </action>
  <verify>
    <automated>grep -q "flex-wrap" src/pages/mercadolivre/MLPublicidade.tsx && grep -Eq "useIsMobile|isMobile" src/pages/mercadolivre/MLPublicidade.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Rodapé de campanhas contém flex-wrap (grep) e os 4 totais permanecem (nenhum removido).
    - MLPublicidade.tsx passa a usar isMobile para variante mobile das duas tabelas.
    - Ramo desktop das tabelas preservado (mesmas colunas/dados).
    - Zero mudança em cálculo/fetch de dados (só apresentação).
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Em 360px: totais das campanhas visíveis (quebram em linha) e ROAS/ACoS/Estoque acessíveis em cards sem scroll horizontal extenso.</done>
</task>

<task type="auto">
  <name>Task 2: /financeiro — tabelas Marca/Estado dual-layout + gráfico Composição sem compressão + KPI grid</name>
  <files>src/pages/mercadolivre/MLFinanceiro.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFinanceiro.tsx (arquivo alvo — ler inteiro; foco: ~751 padrão dual-layout de referência, ~923-1089 tabelas Marca/Estado, ~489 chart Composição, ~353 KPI grid)
    - src/hooks/use-mobile.tsx (useIsMobile — já importado nesta página, linha ~26/121)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-A.md (findings A-04, A-05, A-10)
  </read_first>
  <action>
    [A-04] MAJOR — Tabelas Lucro por Marca (9 col.) e Lucro por Estado não têm variante mobile, ao contrário da tabela Lucro por Produto (linha ~751) na MESMA página. Aplicar o MESMO padrão de ternário isMobile (cards no mobile, table no desktop) já existente na página. Card mobile de Marca: Marca, Receita, Lucro R$, Lucro %. Card mobile de Estado: Estado, Pedidos, Receita, Lucro %. Reusar o isMobile já disponível.

    [A-05] MAJOR — Gráfico Composição da Receita (ComposedChart ~linha 489) usa margem direita de 48 que comprime as barras em 360px. Trocar a margem para: top 4, right isMobile ? 4 : 48, left 0, bottom 0. Opcionalmente reduzir a width dos YAxis no mobile (ex.: width isMobile ? 40 : 54 no eixo BRL) — decisão do executor conforme o que couber, sem alterar os dados/escala do gráfico.

    [A-10] MINOR — KPI grid de 8 cards grid-cols-2 (~linha 353) pode truncar valores monetários longos em 360px. Verificar o comportamento do KPICard size compact; se valores longos truncarem, aplicar mitigação de baixo risco (ex.: manter grid-cols-2 com valor em text-sm, ou grid-cols-1 sm:grid-cols-2 ... lg:grid-cols-8). Escolher a opção que NÃO regride o layout desktop (lg:grid-cols-8 preservado). Se o KPICard já trata truncamento adequadamente, documentar no SUMMARY que nenhuma mudança foi necessária para A-10.
  </action>
  <verify>
    <automated>grep -q "isMobile ? 4 : 48" src/pages/mercadolivre/MLFinanceiro.tsx && test "$(grep -c isMobile src/pages/mercadolivre/MLFinanceiro.tsx)" -ge 3 && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Chart Composição usa margem direita condicional isMobile ? 4 : 48 (grep).
    - Tabelas Marca e Estado passam a ter ramo mobile (cards) além do desktop (table) — verificável por leitura do dual-layout.
    - isMobile referenciado ao menos 3 vezes na página (grep count) após os fixes.
    - Layout desktop preservado (colunas completas, lg:grid-cols-8 no KPI grid).
    - Zero mudança em cálculo de dados.
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Em 360px: gráfico de Composição legível (barras não comprimidas) e tabelas Marca/Estado consumíveis em cards.</done>
</task>

<task type="auto">
  <name>Task 3: Fixes MINOR de copy e controles — /produtos-vendidos, /analise-precos, / (Vendas)</name>
  <files>src/pages/mercadolivre/MLProdutosVendidos.tsx, src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx, src/pages/MercadoLivre.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLProdutosVendidos.tsx (~linha 209 — copy do estado vazio do painel de itens; grid grid-cols-1 lg:grid-cols-[320px_1fr])
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx (~linhas 193-253 — barra de controles com dois ToggleGroup, o último com ml-auto)
    - src/pages/MercadoLivre.tsx (~linhas 669, 679 — botões Atualizar/Personalizar com hidden sm:inline no label)
    - src/hooks/use-mobile.tsx (useIsMobile)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-A.md (findings A-06, A-08, A-09)
  </read_first>
  <action>
    [A-06] MINOR — MLProdutosVendidos.tsx: em layout empilhado mobile o painel de grupos fica ACIMA (não à esquerda). Substituir a referência geográfica lateral do estado vazio por uma neutra vertical ("acima"), correta em qualquer viewport abaixo de lg. Localizar a string de placeholder do estado vazio (linha ~209) e ajustá-la. Versão simples (texto único "acima") preferida; se a leitura indicar que a versão condicional isMobile é trivial, pode usá-la.

    [A-08] MINOR — PrecoPraticadoReport.tsx: na barra de controles flex flex-wrap, os dois ToggleGroup (granularidade e volume) podem se separar em linhas diferentes de forma desconexa. Agrupar os dois ToggleGroup num único div flex items-center gap-2 ml-auto (movendo o ml-auto para esse wrapper e removendo-o do ToggleGroup individual), para que fiquem sempre juntos quando quebrarem de linha. Comportamento dos toggles inalterado.

    [A-09] MINOR — MercadoLivre.tsx: os botões Atualizar/Personalizar mostram só ícone no mobile (hidden sm:inline no label). O audit conclui que é aceitável (aria-label presente). Ação mínima: garantir que ambos os botões tenham aria-label e title descritivos (tooltip nativo). NÃO forçar exibir o texto no mobile (economia de espaço é intencional). Se ambos já têm aria-label e title, documentar que nenhuma mudança foi necessária para A-09.
  </action>
  <verify>
    <automated>grep -q "acima" src/pages/mercadolivre/MLProdutosVendidos.tsx && grep -q "ml-auto" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - MLProdutosVendidos.tsx: estado vazio não referencia mais "à esquerda"; usa referência vertical ("acima").
    - PrecoPraticadoReport.tsx: os dois ToggleGroup agrupados num wrapper com ml-auto (grep ml-auto presente).
    - MercadoLivre.tsx: botões Atualizar/Personalizar com aria-label + title.
    - Zero mudança de comportamento de dados.
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Copy correta em mobile; controles de Análise de Preços agrupados; botões da barra de Vendas com rótulo acessível.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → nenhuma nova superfície | Mudanças exclusivamente de layout/CSS/copy. Nenhum fetch/RPC/EF alterado; variantes mobile leem os MESMOS dados já carregados. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-02 | Information Disclosure | Variantes mobile das tabelas | accept | Cards mobile renderizam subconjunto dos MESMOS dados já buscados e já exibidos no desktop; nenhum novo dado é lido ou exposto. |
| T-78-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova instalada (apenas className/JSX/copy). Sem pacotes a auditar. |
</threat_model>

<verification>
- `npx vitest run` → 318+ testes verdes (exit 0).
- `npx vite build` → build limpo (exit 0).
- Grep gates das 3 tasks passam.
- Nenhuma mudança de comportamento de dados: só layout/copy.
</verification>

<success_criteria>
- /publicidade, /financeiro, /produtos-vendidos, /analise-precos e / (Vendas) utilizáveis em 360px.
- Build + testes verdes.
</success_criteria>

## Artifacts this phase produces (Plano 02)

- `MLPublicidade.tsx`: passa a usar isMobile; variantes mobile (cards) das tabelas de Campanhas e Produtos Patrocinados; rodapé com flex-wrap.
- `MLFinanceiro.tsx`: tabelas Marca/Estado ganham ramo mobile; margem do chart Composição condicional a isMobile.
- `MLProdutosVendidos.tsx` / `PrecoPraticadoReport.tsx` / `MercadoLivre.tsx`: ajustes de copy/agrupamento/aria. Sem novas props públicas.

## Cobertura finding → task (Plano 02)

| Finding | Severidade | Arquivo | Task |
|---------|-----------|---------|------|
| A-02 — rodapé campanhas gap-8 sem wrap | BLOCKER | MLPublicidade.tsx | Task 1 |
| A-07 — tabela Campanhas 10 col. sem mobile | MAJOR | MLPublicidade.tsx | Task 1 |
| A-03 — tabela Produtos Patrocinados 15 col. sem mobile | MAJOR | MLPublicidade.tsx | Task 1 |
| A-04 — tabelas Marca/Estado sem mobile | MAJOR | MLFinanceiro.tsx | Task 2 |
| A-05 — chart Composição margin right:48 | MAJOR | MLFinanceiro.tsx | Task 2 |
| A-10 — KPI 8-cards trunca valores longos | MINOR | MLFinanceiro.tsx | Task 2 |
| A-06 — copy "à esquerda" incorreto no mobile | MINOR | MLProdutosVendidos.tsx | Task 3 |
| A-08 — controles Análise de Preços quebra estranha | MINOR | PrecoPraticadoReport.tsx | Task 3 |
| A-09 — botões Atualizar/Personalizar ícone-only | MINOR | MercadoLivre.tsx | Task 3 |

<output>
Create `.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-02-SUMMARY.md` when done
</output>
