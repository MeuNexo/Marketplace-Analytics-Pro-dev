---
phase: 78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/mercadolivre/MLAnuncios.tsx
  - src/components/mercadolivre/anuncios/ListingDetailModal.tsx
  - src/pages/mercadolivre/MLEstoque.tsx
  - src/pages/mercadolivre/MLPedidos.tsx
  - src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx
  - src/components/financial/CashFlowSimulator.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Em /anuncios, o PriceDetailSheet e o ListingDetailModal cabem e são utilizáveis em 360px (sem estourar o viewport)"
    - "Os sticky headers de /anuncios e /pedidos empilham em mobile em vez de esmagar/truncar conteúdo"
    - "A tabela de inventário de /estoque tem variante mobile (card) e as tabelas de relatórios de /anuncios têm scroll horizontal intencional (min-w) em vez de comprimir colunas"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/ListingDetailModal.tsx"
      provides: "DialogContent com w-full no mobile e max-w-4xl só em sm:"
      contains: "sm:max-w-4xl"
    - path: "src/pages/mercadolivre/MLAnuncios.tsx"
      provides: "PriceDetailSheet w-full mobile + sticky header empilhado + Calendar 1 mês mobile + tabelas de relatório com min-w"
      contains: "w-full sm:max-w-"
  key_links:
    - from: "src/pages/mercadolivre/MLAnuncios.tsx"
      to: "src/hooks/use-mobile.tsx"
      via: "useIsMobile já importado (linha ~2/545) — reusar para Calendar numberOfMonths e PieChart labels"
      pattern: "isMobile"
---

<objective>
Corrigir a responsividade das páginas de Operações: /anuncios (incl. modal de detalhe e PriceDetailSheet), /estoque, /pedidos, /precificacao (Simulador) e /fluxo-de-caixa (CashFlowSimulator).

Escopo (findings do audit Grupo B): B-01 (PriceDetailSheet w-560px — BLOCKER), B-02 (ListingDetailModal max-w-4xl sem override — BLOCKER), B-03 (sticky header /anuncios), B-04 (sticky header /pedidos), B-05 (controles Relatórios overflow), B-06 (tabela catálogo overflow-x explícito), B-07 (tabelas Ranking/Marca/ABC sem min-w), B-08 (tabela inventário /estoque sem card mobile), B-09 (Calendar 2 meses em /anuncios), B-13 (tabela Reposição Urgente sem min-w), B-14 (PieChart labels sobrepostos), B-15 (filtros catálogo amontoados), B-10 (Simulador resultado enterrado), B-11 (ExtraField estoura linha), B-12 (CashFlowSimulator ordem).

Purpose: /anuncios é a página mais crítica do grupo — dois BLOCKERs (Sheet de preço e modal de detalhe) tornam funções inacessíveis no mobile. /estoque tem a tabela mais usada sem variante card (UX confusa para leigos, o público-alvo). Reusar padrões já provados do projeto: dual-layout de MLPedidos.tsx:1311-1344, w-full sm:max-w do shadcn SheetContent, min-w em tabelas de scroll intencional.
Output: 6 arquivos de Operações responsivos, sem regressão de testes/build.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-CONTEXT.md
@.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-B.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /anuncios — BLOCKERs de Sheet/Modal + sticky header + tabelas de relatório + Calendar + PieChart + filtros</name>
  <files>src/pages/mercadolivre/MLAnuncios.tsx, src/components/mercadolivre/anuncios/ListingDetailModal.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLAnuncios.tsx (arquivo alvo — ler inteiro é caro; ler os trechos: ~297 PriceDetailSheet, ~1075 sticky header, ~1134-1217 filtros catálogo, ~1297 wrapper tabela catálogo, ~1783 controles Relatórios, ~1842 Calendar, ~1955/2115/2221 tabelas Ranking/Marca/ABC, ~2082 PieChart)
    - src/components/mercadolivre/anuncios/ListingDetailModal.tsx (~linha 105 DialogContent)
    - src/pages/mercadolivre/MLPedidos.tsx (~1311-1344 — padrão dual-layout card mobile de referência; ~1040 sticky header como contra-exemplo a corrigir na Task 3)
    - src/pages/mercadolivre/MLEstoque.tsx (~1002 — padrão sticky header flex-col gap-3 lg:flex-row de referência)
    - src/hooks/use-mobile.tsx (useIsMobile — já importado em MLAnuncios linha ~2/545)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-B.md (findings B-01, B-02, B-03, B-05, B-06, B-07, B-09, B-14, B-15)
  </read_first>
  <action>
    [B-02] BLOCKER — ListingDetailModal.tsx DialogContent usa max-w-4xl sem breakpoint mobile → em 360px o dialog é cortado. Trocar className para começar com w-full e limitar em sm: — resultado: w-full sm:max-w-4xl max-h-[90vh] overflow-y-auto (padrão shadcn: 100vw no mobile, 4xl só em ≥640px).

    [B-01] BLOCKER — PriceDetailSheet em MLAnuncios.tsx (~297) usa SheetContent w-[560px] sm:max-w-[560px] → estoura em 360px. Trocar para w-full sm:max-w-[560px] (padrão shadcn SheetContent). Manter overflow-y-auto p-0 e side.

    [B-03] MAJOR — Sticky header (~1075) usa flex items-center justify-between sem empilhar → título + TabsList (3 abas) + botão não cabem em 360px. Trocar para flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between, igual ao padrão de MLEstoque.tsx:1002.

    [B-09] MAJOR — Calendar (~1842) da aba Relatórios usa numberOfMonths={2} → não cabe no Popover mobile. Trocar para numberOfMonths={isMobile ? 1 : 2} (isMobile já importado na página).

    [B-05] MAJOR — Controles da sub-aba Ranking (~1783) em flex items-center justify-between gap-3 estouram (TabsList + Popover + Input + Select + Button). Converter o container para flex flex-col gap-3 e colocar os controles abaixo da TabsList num div flex items-center gap-2 flex-wrap.

    [B-07] MAJOR — Tabelas Ranking/Marca/ABC (~1955/2115/2221) dentro de wrapper overflow-auto sem min-w comprimem colunas em 360px. Adicionar min-w à Table (ex.: className min-w-[600px], ajustando o valor à soma das larguras das colunas de cada tabela) para forçar scroll horizontal intencional em vez de compressão. Padrão análogo: ReplenishmentSkuTable de /compras.

    [B-06] MAJOR (risco) — Wrapper da tabela do catálogo (~1297) usa overflow-auto ambíguo. Explicitar overflow-x-auto overflow-y-auto para clareza e prevenir regressão futura do sticky header. (Mudança não-funcional, só clareza.)

    [B-14] MINOR — PieChart de Análise por Marca (~2082) com label inline pode sobrepor em 360px. Remover o prop label do Pie no mobile (condicionar a !isMobile) e usar Legend abaixo do gráfico no mobile (padrão de /estoque:232-237). Se preferir, manter label só no desktop.

    [B-15] MINOR — Filtros do catálogo (~1134-1217) com larguras fixas w-44/w-36/w-32 se amontoam. No mobile, tornar a busca fluida (flex-1 min-w-[120px]) e os Selects w-full sm:w-36 (ou equivalente), reduzindo linhas de filtro. Mudança de layout apenas.

    Regra dual-layout (lição Phase 71): o catálogo de /anuncios já tem card mobile com interação por item — NÃO regredir os gatilhos de clique/menu existentes nesse ramo ao mexer no header/filtros.
  </action>
  <verify>
    <automated>grep -q "w-full sm:max-w-\[560px\]" src/pages/mercadolivre/MLAnuncios.tsx && grep -q "numberOfMonths={isMobile ? 1 : 2}" src/pages/mercadolivre/MLAnuncios.tsx && grep -Eq "flex-col gap-3 sm:flex-row|flex flex-col gap-3" src/pages/mercadolivre/MLAnuncios.tsx && grep -q "min-w-\[" src/pages/mercadolivre/MLAnuncios.tsx && grep -q "sm:max-w-4xl" src/components/mercadolivre/anuncios/ListingDetailModal.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - ListingDetailModal.tsx DialogContent contém sm:max-w-4xl (e w-full no mobile) — grep.
    - MLAnuncios.tsx PriceDetailSheet contém w-full sm:max-w-[560px] — grep.
    - Calendar de Relatórios usa numberOfMonths={isMobile ? 1 : 2} — grep.
    - Sticky header empilha (flex-col ... sm:flex-row) — grep.
    - Ao menos uma Table de relatório com min-w-[...] — grep.
    - Dados/lógica de negócio inalterados; card mobile do catálogo mantém interações.
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Em 360px: Sheet de preço e modal de detalhe abrem cabendo na tela; header empilhado; calendário de 1 mês; tabelas de relatório com scroll lateral intencional; filtros do catálogo enxutos.</done>
</task>

<task type="auto">
  <name>Task 2: /estoque — variante card mobile da tabela de inventário + min-w na Reposição Urgente</name>
  <files>src/pages/mercadolivre/MLEstoque.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLEstoque.tsx (arquivo alvo; foco ~1206 tabela de inventário 12 colunas, ~308 sub-tab Reposição Urgente, ~232-237 padrão Legend em chart)
    - src/pages/mercadolivre/MLPedidos.tsx (~1311-1344 — padrão card mobile de referência a copiar)
    - src/hooks/use-mobile.tsx (useIsMobile)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-B.md (findings B-08, B-13)
  </read_first>
  <action>
    [B-08] MAJOR — Tabela de inventário (~1206, 12 colunas) sem variante mobile e sem min-w → em 360px vira scroll horizontal dentro de scroll vertical (UX confusa para leigos). Implementar dual-layout igual ao MLPedidos.tsx:1311-1344: importar/usar useIsMobile; no mobile renderizar cards com os campos essenciais (Produto, Estoque, Cobertura, Saúde, + a ação existente da linha), e manter a Table completa apenas em lg:/desktop. O card mobile lê os MESMOS dados/linhas. IMPORTANTE (lição Phase 71): se a linha da tabela tem ação por item (ex.: botão/menu na última coluna), replicar essa ação no card mobile — não deixar gatilho só no ramo desktop.

    [B-13] MAJOR — Tabela da sub-aba Reposição Urgente (~308, dentro de max-h-80 overflow-auto) sem min-w comprime colunas. Adicionar min-w à table (ex.: min-w-[500px]) para scroll horizontal intencional, igual ao fix de B-07.
  </action>
  <verify>
    <automated>grep -Eq "useIsMobile|isMobile" src/pages/mercadolivre/MLEstoque.tsx && grep -q "min-w-\[" src/pages/mercadolivre/MLEstoque.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - MLEstoque.tsx usa isMobile e tem ramo de cards para a tabela de inventário (dual-layout) — verificável por leitura.
    - Ações por item preservadas no ramo mobile (paridade dual-layout).
    - Tabela de Reposição Urgente com min-w-[...] — grep.
    - Table completa preservada no desktop.
    - Zero mudança de dados.
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Em 360px: inventário em cards legíveis com ação por item; Reposição Urgente com scroll lateral intencional.</done>
</task>

<task type="auto">
  <name>Task 3: /pedidos sticky header + Simulador de Precificação + CashFlowSimulator</name>
  <files>src/pages/mercadolivre/MLPedidos.tsx, src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx, src/components/financial/CashFlowSimulator.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLPedidos.tsx (~1040 sticky header — NÃO mexer no dual-layout de card ~1311, só no header)
    - src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx (~308 grid lg:grid-cols-[1fr_340px], ~756-782 ExtraField)
    - src/components/financial/CashFlowSimulator.tsx (~240 grid lg:grid-cols-[340px_1fr])
    - src/pages/mercadolivre/MLEstoque.tsx (~1002 — padrão sticky header de referência)
    - .planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-AUDIT-B.md (findings B-04, B-10, B-11, B-12)
  </read_first>
  <action>
    [B-04] MAJOR — Sticky header de /pedidos (~1040) usa flex items-center justify-between sem empilhar (MLPeriodPicker + TabsList + 2 botões + título não cabem em 360px). Trocar para flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between, idêntico ao B-03. NÃO tocar no dual-layout de cards de pedidos (~1311) — está correto.

    [B-10] MINOR — SimuladorPrecificacao.tsx: grid grid-cols-1 lg:grid-cols-[1fr_340px] (~308) enterra o painel de resultado no mobile (usuário rola ~500px). Expor o resultado mais cedo: aplicar order no CSS grid para que o card Preço de venda apareça antes dos inputs no mobile (ex.: order-first no painel de resultado + order-none em sm:/lg:), OU separar em tablet com sm:grid-cols-[1fr_300px]. Escolher a abordagem que não regrida o desktop (sticky lg:top-14 do resultado preservado).

    [B-11] MINOR — SimuladorPrecificacao.tsx ExtraField (~756-782): linha Switch + label + Select + Input estoura 360px. Envolver em flex flex-col gap-1.5 sm:flex-row sm:items-center sm:gap-2 — label na linha 1, controles (Switch+Select+Input) na linha 2 no mobile; layout desktop inalterado.

    [B-12] MINOR — CashFlowSimulator.tsx: grid lg:grid-cols-[340px_1fr] (~240) mostra controles antes do gráfico no mobile. Adicionar sm:grid-cols-[300px_1fr] e/ou reordenar (gráfico order-2 lg:order-none) para o resultado aparecer mais cedo no mobile. Não regredir desktop.
  </action>
  <verify>
    <automated>grep -Eq "flex-col gap-3 sm:flex-row|flex flex-col gap-3" src/pages/mercadolivre/MLPedidos.tsx && grep -Eq "order-|sm:grid-cols-\[" src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx && grep -Eq "sm:flex-row|flex-col" src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx && grep -Eq "order-|sm:grid-cols-\[" src/components/financial/CashFlowSimulator.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - MLPedidos.tsx sticky header empilha (flex-col ... sm:flex-row) — grep; dual-layout de cards intocado.
    - SimuladorPrecificacao.tsx: resultado exposto mais cedo no mobile (order ou sm:grid-cols) e ExtraField empilha no mobile (flex-col ... sm:flex-row) — grep.
    - CashFlowSimulator.tsx: gráfico/controles reordenados ou tablet grid — grep.
    - Layout desktop preservado em todos os três.
    - Zero mudança de dados/cálculo (Simulador continua com os mesmos números).
    - `npx vitest run` exit 0 e `npx vite build` exit 0.
  </acceptance_criteria>
  <done>Header de /pedidos empilhado; resultado do Simulador e do CashFlowSimulator visíveis sem rolar formulário longo; ExtraField cabe em 360px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → nenhuma nova superfície | Mudanças de layout/CSS/ordem DOM. Sheet/Modal/Simulador leem os MESMOS dados; nenhum fetch/RPC/EF alterado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03 | Information Disclosure | Cards mobile de inventário/pedidos e ações por item | accept | Cards renderizam subconjunto dos MESMOS dados e disparam as MESMAS ações já existentes no ramo desktop; nenhuma nova operação é introduzida. |
| T-78-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova instalada (apenas className/JSX/order). Sem pacotes a auditar. |
</threat_model>

<verification>
- `npx vitest run` → 318+ testes verdes (exit 0).
- `npx vite build` → build limpo (exit 0).
- Grep gates das 3 tasks passam.
- Paridade dual-layout: ações por item presentes no ramo mobile (inventário/pedidos).
- Nenhuma mudança de comportamento de dados.
</verification>

<success_criteria>
- /anuncios (Sheet, Modal, header, tabelas, calendário), /estoque (inventário em cards), /pedidos (header), /precificacao e /fluxo-de-caixa utilizáveis em 360px.
- Build + testes verdes.
</success_criteria>

## Artifacts this phase produces (Plano 03)

- `ListingDetailModal.tsx`: DialogContent responsivo (w-full sm:max-w-4xl).
- `MLAnuncios.tsx`: PriceDetailSheet w-full mobile; sticky header empilhado; Calendar condicional; tabelas de relatório com min-w; PieChart/filtros mobile.
- `MLEstoque.tsx`: dual-layout de inventário (novo ramo de cards mobile); min-w na Reposição Urgente.
- `MLPedidos.tsx`: sticky header empilhado (dual-layout de cards intocado).
- `SimuladorPrecificacao.tsx` / `CashFlowSimulator.tsx`: reordenação mobile do resultado; ExtraField empilhado. Sem novas props públicas.

## Cobertura finding → task (Plano 03)

| Finding | Severidade | Arquivo | Task |
|---------|-----------|---------|------|
| B-01 — PriceDetailSheet w-[560px] estoura | BLOCKER | MLAnuncios.tsx | Task 1 |
| B-02 — ListingDetailModal max-w-4xl sem override | BLOCKER | ListingDetailModal.tsx | Task 1 |
| B-03 — sticky header /anuncios não empilha | MAJOR | MLAnuncios.tsx | Task 1 |
| B-05 — controles Relatórios overflow | MAJOR | MLAnuncios.tsx | Task 1 |
| B-06 — tabela catálogo overflow-x ambíguo | MAJOR | MLAnuncios.tsx | Task 1 |
| B-07 — tabelas Ranking/Marca/ABC sem min-w | MAJOR | MLAnuncios.tsx | Task 1 |
| B-09 — Calendar 2 meses não cabe | MAJOR | MLAnuncios.tsx | Task 1 |
| B-14 — PieChart labels sobrepostos | MINOR | MLAnuncios.tsx | Task 1 |
| B-15 — filtros catálogo amontoados | MINOR | MLAnuncios.tsx | Task 1 |
| B-08 — tabela inventário sem card mobile | MAJOR | MLEstoque.tsx | Task 2 |
| B-13 — tabela Reposição Urgente sem min-w | MAJOR | MLEstoque.tsx | Task 2 |
| B-04 — sticky header /pedidos não empilha | MAJOR | MLPedidos.tsx | Task 3 |
| B-10 — Simulador resultado enterrado | MINOR | SimuladorPrecificacao.tsx | Task 3 |
| B-11 — ExtraField estoura linha | MINOR | SimuladorPrecificacao.tsx | Task 3 |
| B-12 — CashFlowSimulator controles antes do gráfico | MINOR | CashFlowSimulator.tsx | Task 3 |

<output>
Create `.planning/phases/78-revisao-mobile-first-responsividade-e-ux-100-por-cento-no-mo/78-03-SUMMARY.md` when done
</output>
