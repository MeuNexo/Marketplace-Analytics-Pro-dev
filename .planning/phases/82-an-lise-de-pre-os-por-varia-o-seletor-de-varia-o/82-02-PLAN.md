---
phase: 82-an-lise-de-pre-os-por-varia-o-seletor-de-varia-o
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified: []
autonomous: false
requirements: ["APV-RPC-DEPLOY"]
user_setup: []

must_haves:
  truths:
    - "A migration 20260682000000_orders_price_timeseries_sku está aplicada em produção (ckcdevcxgvueywivefgx) via MCP apply_migration"
    - "A RPC de 6 argumentos existe: orders_price_timeseries(item, ids, from, to, gran, _sku) retorna as 13 colunas sem erro de sobrecarga/ambiguidade"
    - "Chamada de 5 argumentos (sem _sku) continua funcionando idêntica ao pai (não quebrou nenhuma chamada existente)"
    - "Reconciliação por SKU: para 1 variação real (SKU …420603), a série filtrada bate contra a soma manual em orders para o mesmo item+sku (total/qtd/cmv/impostos)"
    - "Cobertura da variação campeã na faixa de preço baixo dá ~0 dias (estoque 19 ÷ giro alto), NÃO os 6 dias do pai — prova de que o join por SKU corrige o número enganoso"
    - "Chamada como role authenticated de org alheia para um item da Pé Vermeio retorna 0 linhas (anti-IDOR), com e sem _sku"
  artifacts: []
  key_links:
    - from: "MCP apply_migration"
      to: "orders_price_timeseries (prod, 6 args)"
      via: "aplicação da migration 20260682000000 no projeto ckcdevcxgvueywivefgx"
      pattern: "orders_price_timeseries"
---

<objective>
Aplicar a migration escrita em 82-01 ao banco de produção e provar (smoke) que a RPC com
`_sku` filtra corretamente por variação, continua isolada por org e não quebrou a forma de 5
argumentos usada pela análise do pai. Este é um plano de CHECKPOINT executado pelo
ORQUESTRADOR — o gsd-executor NÃO tem acesso ao MCP claude.ai do Supabase; a aplicação e o
smoke são feitos pelo orquestrador via MCP `apply_migration` / `execute_sql` (padrão das
Phases 62/63/69/79-02).

Purpose: [BLOCKING] a phase não pode passar verificação sem a migration aplicada — build e
testes passam sem o deploy (os tipos vêm de config, não do banco vivo), criando falso-positivo.
Além disso, este é o ponto onde a LIÇÃO CRÍTICA se prova em dados reais: o join por SKU tem
que reconciliar (`o.sku = _sku` casa 43/43; `variation_id` casaria 0/43). Fecha essa lacuna
antes da UI (82-03) consumir dados reais.

Output: RPC de 6 argumentos viva em prod + evidência de reconciliação por SKU (incluindo a
prova cobertura ~0d da variação campeã) e anti-IDOR, registrados no SUMMARY.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/82-an-lise-de-pre-os-por-varia-o-seletor-de-varia-o/82-CONTEXT.md
@supabase/migrations/20260682000000_orders_price_timeseries_sku.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING]: Aplicar migration via MCP + smoke por SKU (reconciliação + prova cobertura ~0d) + anti-IDOR</name>
  <read_first>
    - supabase/migrations/20260682000000_orders_price_timeseries_sku.sql (escrita em 82-01) — o conteúdo a aplicar.
    - .planning/phases/79-analise-de-precos-com-mco/79-02-PLAN.md — o padrão de smoke/reconciliação/anti-IDOR desta família de RPC.
    - .planning/phases/82-.../82-CONTEXT.md — seção "Achados da validação manual" (MLB4113792113, SKU …420603, estoque 19, faixa R$40–60 → cobertura 0d).
  </read_first>
  <what-built>
    A migration `supabase/migrations/20260682000000_orders_price_timeseries_sku.sql`
    (escrita em 82-01) adiciona o parâmetro opcional `_sku text DEFAULT NULL` a
    `orders_price_timeseries` e o predicado `AND (_sku IS NULL OR o.sku = _sku)`. Ainda NÃO
    aplicada em prod.
  </what-built>
  <how-to-verify>
    O ORQUESTRADOR executa (não o gsd-executor):
    1. Aplicar a migration no projeto `ckcdevcxgvueywivefgx` via MCP
       `mcp__claude_ai_Supabase__apply_migration` (name: orders_price_timeseries_sku;
       conteúdo = o arquivo .sql do 82-01). NUNCA via SQL Editor, NUNCA `supabase db push`.
    2. Smoke de compatibilidade (forma de 5 args, pai): `SELECT * FROM orders_price_timeseries('<MLB de anúncio COM variações da Pé Vermeio>', NULL, NULL, NULL, 'day') LIMIT 3;`
       via `execute_sql` — deve retornar as 13 colunas sem erro de ambiguidade ("function is not unique") nem "function does not exist".
    3. Smoke de SKU (forma de 6 args): repetir com o 6º argumento = SKU de uma variação real
       (ex.: a variação campeã do MLB4113792113, SKU …420603): `... , 'day', '<SKU>')`. Deve
       retornar só as linhas daquele SKU (soma de qtd bem menor que a do pai).
    4. Reconciliação por SKU (role authenticated, NÃO postgres — impersonar a org Pé Vermeio via
       SET LOCAL ROLE authenticated + set_config request.jwt.claims em transação ROLLBACK, método
       Phases 43/63/79-02). Para o mesmo item+sku, comparar 1-2 buckets do resultado contra a soma
       manual direta em `orders WHERE item_id=... AND sku=... AND status IN (...)`:
       SUM(receita_bruta) ≡ total; SUM(quantidade) ≡ qtd; SUM(custo_unit*quantidade) ≡ cmv;
       SUM(tax_amount) ≡ impostos.
    5. Prova da LIÇÃO CRÍTICA (o número que o pai esconde): confirmar que a variação campeã
       (SKU …420603, estoque 19) tem giro alto na faixa de preço baixo → cobertura ~0 dias, contra
       os ~6 dias do pai na mesma faixa. Basta somar as unidades do SKU na faixa e dividir por
       dias-com-venda (giro), e comparar 19 ÷ giro ≈ 0. Registrar os dois números (pai vs variação)
       no SUMMARY. (Confirma que `o.sku = _sku` casa 43/43 e `variation_id` casaria 0/43.)
    6. Anti-IDOR: repetir a chamada (com e sem `_sku`) impersonando uma org ALHEIA (ex.: Thales)
       para um item da Pé Vermeio → deve retornar 0 linhas nas duas formas.
  </how-to-verify>
  <resume-signal>
    Confirmar: "migration aplicada em prod; forma de 5 args intacta (sem ambiguidade); forma de 6
    args filtra por SKU; 1-2 buckets reconciliaram (total/qtd/cmv/impostos batem); cobertura da
    variação campeã ≈ 0d vs ~6d do pai; anti-IDOR = 0 linhas para org alheia com e sem _sku." Se
    algum bucket não bater ou o SKU não filtrar, investigar (predicado, cast ::date, status) antes
    de liberar o wave 3.
  </resume-signal>
  <action>
    [ORQUESTRADOR — o gsd-executor não tem MCP claude.ai] Aplicar a migration 82-01 no projeto
    ckcdevcxgvueywivefgx via MCP `apply_migration` (nunca SQL Editor, nunca `supabase db push`),
    depois rodar os passos 2-6 de "how-to-verify" via `execute_sql`: smoke de compatibilidade (5
    args, sem ambiguidade), smoke de SKU (6 args), reconciliação de 1-2 buckets por SKU como role
    authenticated (Pé Vermeio) contra soma manual em `orders`, a prova cobertura ~0d da variação
    campeã vs ~6d do pai, e o teste anti-IDOR (org alheia → 0 linhas, com e sem _sku). Registrar
    todos os números no SUMMARY. Bloqueante: só liberar o wave 3 (UI, 82-03) após reconciliação,
    prova de cobertura e anti-IDOR OK.
  </action>
  <files>supabase/migrations/20260682000000_orders_price_timeseries_sku.sql (aplicada em prod, não editada)</files>
  <verify>
    <automated>MISSING — verificação é via MCP execute_sql pelo orquestrador (checkpoint BLOCKING); não há comando local. Evidência registrada no 82-02-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - Migration aplicada em prod via MCP apply_migration (não SQL Editor, não db push).
    - Forma de 5 args retorna 13 colunas sem "function is not unique" (compatibilidade do pai provada).
    - Forma de 6 args filtra por SKU (qtd do SKU << qtd do pai).
    - 1-2 buckets do SKU ≡ soma manual em orders (total/qtd/cmv/impostos).
    - Cobertura da variação campeã (SKU …420603, estoque 19) ≈ 0d na faixa baixa vs ~6d do pai — registrado no SUMMARY.
    - Anti-IDOR: org alheia → 0 linhas com e sem _sku.
  </acceptance_criteria>
  <done>RPC de 6 args viva em prod; reconciliação por SKU OK; prova cobertura ~0d vs 6d registrada; anti-IDOR = 0 linhas cross-org; números no SUMMARY.</done>
</task>

</tasks>

<artifacts_produced>
- RPC `orders_price_timeseries` em produção (ckcdevcxgvueywivefgx) com o 6º parâmetro `_sku` ativo.
- Evidência no SUMMARY: reconciliação por SKU, prova cobertura ~0d (variação) vs ~6d (pai), e anti-IDOR (0 linhas cross-org, com e sem _sku).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador (MCP) → banco prod | aplicação de DDL e execução de SQL de smoke |
| role authenticated (org X) → RPC | leitura agregada de `orders` sob RLS, com filtro opcional por SKU |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-03 | Elevation of Privilege (IDOR) | orders_price_timeseries em prod (com _sku) | mitigate | smoke anti-IDOR obrigatório: org alheia → 0 linhas com E sem _sku, provado como role authenticated (não postgres) |
| T-82-04 | Repudiation/erro silencioso | DDL aplicada | mitigate | reconciliação por SKU (1-2 buckets ≡ soma manual) + prova cobertura ~0d antes de liberar a UI (82-03) |
| T-82-05 | Denial of Service (regressão de sobrecarga) | assinatura de 5 args do pai | mitigate | smoke de compatibilidade: forma de 5 args sem ambiguidade ("function is not unique") — valida o DROP explícito do 82-01 |
| T-82-SC | Tampering | supply chain | accept | nenhum pacote/instalação nesta phase |
</threat_model>

<verification>
- Migration aplicada via MCP apply_migration (não SQL Editor).
- Forma de 5 e de 6 args funcionam; reconciliação por SKU OK; cobertura variação ≈ 0d vs 6d pai; anti-IDOR = 0 linhas.
</verification>

<success_criteria>
RPC com `_sku` viva em prod, reconciliada por SKU e provadamente isolada por org, com a prova
numérica da lição (cobertura ~0d da variação vs ~6d do pai) — pronta para a UI (82-03) consumir.
</success_criteria>

<output>
Create `.planning/phases/82-an-lise-de-pre-os-por-varia-o-seletor-de-varia-o/82-02-SUMMARY.md` when done (registrar os números do smoke, a reconciliação por SKU, a prova de cobertura e o resultado anti-IDOR).
</output>
