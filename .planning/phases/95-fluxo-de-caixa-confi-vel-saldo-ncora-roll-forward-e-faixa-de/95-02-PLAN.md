---
phase: 95-fluxo-de-caixa-confiavel
plan: 02
type: execute
wave: 2
depends_on: ["95-01"]
files_modified:
  - src/hooks/useCashflowDataHealth.ts
  - src/hooks/useCashflowDataHealth.test.ts
  - src/components/financial/CashflowHealthBanner.tsx
  - src/pages/mercadolivre/MLFluxoCaixa.tsx
autonomous: true
requirements: [CASH-95-05, CASH-95-06]
must_haves:
  truths:
    - "useCashflowDataHealth chama supabase.rpc('get_cashflow_data_health', { p_org_id }) e mapeia as flags"
    - "CashflowHealthBanner aparece só quando alguma flag stale é true e some quando todas false"
    - "Banner mostra texto acionável por gatilho: Tiny com link para /integracoes, MP e âncora sem link"
    - "AdjustBalanceDialog grava via supabase.rpc('set_financial_balance') em vez do upsert direto"
    - "Banner fica acima das Tabs, visível em Caixa Real e Simulador"
  artifacts:
    - path: "src/hooks/useCashflowDataHealth.ts"
      provides: "hook TanStack Query da RPC de saúde"
      exports: ["useCashflowDataHealth"]
    - path: "src/hooks/useCashflowDataHealth.test.ts"
      provides: "testes do hook (rpc mockado)"
      min_lines: 30
    - path: "src/components/financial/CashflowHealthBanner.tsx"
      provides: "faixa de alerta condicional"
      exports: ["CashflowHealthBanner"]
    - path: "src/pages/mercadolivre/MLFluxoCaixa.tsx"
      provides: "insere banner + troca upsert por set_financial_balance"
      contains: "CashflowHealthBanner"
  key_links:
    - from: "src/components/financial/CashflowHealthBanner.tsx"
      to: "src/hooks/useCashflowDataHealth.ts"
      via: "consome flags tinyStale/mpStale/anchorStale"
      pattern: "useCashflowDataHealth"
    - from: "src/pages/mercadolivre/MLFluxoCaixa.tsx"
      to: "set_financial_balance RPC"
      via: "supabase.rpc('set_financial_balance', { p_org_id, p_amount })"
      pattern: "set_financial_balance"
    - from: "CashflowHealthBanner (gatilho Tiny)"
      to: "/integracoes"
      via: "<Link to=\"/integracoes\">"
      pattern: "/integracoes"
---

<objective>
Parte B (frontend) da Phase 95: expor a saúde dos dados numa faixa no topo de /fluxo-de-caixa e
fechar o furo de escrita da âncora — o dialog de ajuste de saldo passa a gravar via
set_financial_balance (que grava balance_anchor_date), não mais pelo upsert parcial.

Purpose: dar visibilidade quando Tiny/MP congelam ou a âncora envelhece (os dois furos que faziam a
página "nunca bater"), e garantir que toda reancoragem do saldo atualize a data da âncora.
Output: hook + componente de banner + edição cirúrgica de MLFluxoCaixa.tsx. TDD no hook.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-CONTEXT.md
@.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-RESEARCH.md
@src/hooks/useTreasuryPanel.ts
@src/hooks/useDreOperational.test.ts
</context>

<artifacts_this_phase_produces>
Hooks: useCashflowDataHealth (molde: useTreasuryPanel.ts)
Componentes: CashflowHealthBanner (molde de banner: MLPedidos.tsx:1116-1128, componente Alert)
Edições: MLFluxoCaixa.tsx — insere <CashflowHealthBanner /> entre header sticky e <Tabs> (linha ~222);
  AdjustBalanceDialog.handleSave troca .from('financial_settings').upsert(...) por
  supabase.rpc('set_financial_balance', { p_org_id, p_amount })
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook useCashflowDataHealth + testes (TDD)</name>
  <files>src/hooks/useCashflowDataHealth.ts, src/hooks/useCashflowDataHealth.test.ts</files>
  <read_first>
    - src/hooks/useTreasuryPanel.ts (molde EXATO: RPC escalar única, queryKey com orgId, enabled !!orgId, staleTime 3min, mapeamento Number/Boolean)
    - src/hooks/useDreOperational.test.ts (molde de teste: mock de supabase.rpc = vi.fn(), NÃO a chain from/select/eq; mock de useOrganization; createWrapper com QueryClient)
    - .planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-RESEARCH.md (Pattern 2 linhas 257-291; Pattern 3 linhas 293-302; Estratégia de Testes linhas 459-471)
  </read_first>
  <behavior>
    - Test 1: com orgId presente, o hook chama supabase.rpc('get_cashflow_data_health', { p_org_id: orgId }) e mapeia data[0] para { tinyHoursAgo, tinyStale, mpHoursAgo, mpStale, anchorDaysAgo, anchorStale } com Number()/Boolean().
    - Test 2: coerção — Postgres pode devolver 'true'/'false' string para *_stale; Boolean/coerção explícita deve resultar em boolean (mesmo padrão de double_count_risk no teste de useDreOperational).
    - Test 3: rpc retorna error → hook em isError: true.
    - Test 4: sem orgId (currentOrg null) → query disabled, rpc NÃO é chamada.
    - Test 5 (opcional): data vazio/null → hook retorna null sem quebrar.
  </behavior>
  <action>
    Escrever PRIMEIRO os testes (RED) em useCashflowDataHealth.test.ts clonando a estrutura de
    useDreOperational.test.ts (mesmo mock de supabase.rpc e de useOrganization). Depois implementar
    useCashflowDataHealth.ts clonando useTreasuryPanel.ts: interface CashflowDataHealth com
    tinyHoursAgo:number, tinyStale:boolean, mpHoursAgo:number, mpStale:boolean, anchorDaysAgo:number,
    anchorStale:boolean; useQuery com queryKey ["cashflow","data_health", orgId] as const,
    enabled: !!orgId, staleTime: 3*60*1000; queryFn chama supabase.rpc("get_cashflow_data_health",
    { p_org_id: orgId }), pega data[0], mapeia com Number(...)/Boolean(...). Coerção de *_stale via
    Boolean/comparação explícita porque o Postgres pode serializar boolean como string. Sem lógica de
    diff de datas no cliente (o cálculo de horas/dias vem pronto da RPC — Don't Hand-Roll).
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useCashflowDataHealth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Testes escritos antes da implementação e passam (RED→GREEN).
    - Hook chama a RPC com args corretos e mapeia as 6 flags.
    - queryKey inclui orgId; enabled !!orgId (não chama sem org).
    - error da RPC propaga isError; sem orgId não dispara a RPC.
    - vitest verde para o arquivo de teste.
  </acceptance_criteria>
  <done>useCashflowDataHealth (molde useTreasuryPanel) mapeia as 6 flags; testes escritos antes e verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Componente CashflowHealthBanner (faixa condicional acionável)</name>
  <files>src/components/financial/CashflowHealthBanner.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLPedidos.tsx (linhas 1116-1128 — molde de Alert amber + AlertDescription + Link)
    - src/components/ui/alert.tsx (Alert/AlertTitle/AlertDescription do design system)
    - src/components/financial/TodayBalanceCard.tsx (convenção de estilo/tokens da pasta financial)
    - .planning/specs/2026-07-13-fluxo-caixa-confiavel.md (textos por gatilho, linhas 55-59)
  </read_first>
  <action>
    Criar componente exportado CashflowHealthBanner que consome useCashflowDataHealth. Se o hook ainda
    carrega, ou data é null, ou NENHUMA flag stale é true → renderizar null (nada). Quando qualquer
    flag stale for true → renderizar um Alert no estilo de alerta (amber/vermelho, seguindo o molde
    MLPedidos.tsx:1116-1128 e os tokens do design system existente), com uma linha acionável POR
    gatilho ativo:
      - tinyStale: "Contas a pagar sem atualizar há {arredondar tinyHoursAgo}h — reconecte o Tiny em
        Integrações" com <Link to="/integracoes"> na palavra Integrações (rota real, App.tsx).
      - mpStale: "Entradas do Mercado Pago sem atualizar há {mpHoursAgo}h." (sem link).
      - anchorStale: "Você não confirma o saldo real há {anchorDaysAgo} dia(s) — atualize para a curva
        não desviar." (sem link).
    Mostrar só os gatilhos ativos (condicional por flag), várias linhas se mais de um. Ícone de alerta
    (lucide AlertTriangle, como no molde). Arredondar horas/dias para inteiro na exibição. Sem novos
    pacotes; usar Alert/Link/date-fns já presentes.
  </action>
  <verify>
    <automated>grep -q "useCashflowDataHealth" src/components/financial/CashflowHealthBanner.tsx && grep -q "/integracoes" src/components/financial/CashflowHealthBanner.tsx && grep -q "Mercado Pago" src/components/financial/CashflowHealthBanner.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Retorna null quando loading/null/sem flags stale.
    - Renderiza uma linha por gatilho ativo (Tiny/MP/âncora) com os textos do spec.
    - Gatilho Tiny tem Link para /integracoes; MP e âncora sem link.
    - Horas/dias exibidos arredondados; estilo consistente com o design system (Alert amber/vermelho).
    - tsc sem erros.
  </acceptance_criteria>
  <done>CashflowHealthBanner condicional com uma linha por gatilho (Tiny com link /integracoes); tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 3: Wire em MLFluxoCaixa.tsx — inserir banner + trocar upsert por set_financial_balance</name>
  <files>src/pages/mercadolivre/MLFluxoCaixa.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFluxoCaixa.tsx (return principal linhas 215-228 — inserir entre header sticky (linha 221) e comentário de Tabs (linha 223); AdjustBalanceDialog.handleSave linhas 86-118 — bloco do upsert linhas 94-102)
    - .planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-RESEARCH.md (Pitfall 1 linhas 354-371; RPC de escrita client-side linhas 562-568)
  </read_first>
  <action>
    Duas edições cirúrgicas:
    (1) Importar CashflowHealthBanner e inseri-lo no return principal ENTRE a div do header sticky
    (fecha na linha 221) e o comentário/bloco de <Tabs> (linha 223), para ficar visível nas duas abas
    (Caixa Real e Simulador). Não mexer no conteúdo das abas.
    (2) Em AdjustBalanceDialog.handleSave, substituir o bloco
    supabase.from("financial_settings").upsert({ organization_id: orgId, initial_balance: parsed },
    { onConflict: "organization_id" }) por
    supabase.rpc("set_financial_balance", { p_org_id: orgId, p_amount: parsed }) — resolve o Pitfall 1
    (o upsert parcial nunca gravava balance_anchor_date; a RPC grava initial_balance + a data da âncora
    em BRT no servidor, atomicamente). Manter o resto do handleSave intacto: validação de NaN, os
    invalidateQueries (["financial_settings", orgId], ["cashflow"], ["today_balance"],
    ["projected_balance"] — e adicionar invalidate de ["cashflow","data_health", orgId] para o banner
    reavaliar a âncora após reancorar), toasts de sucesso/erro, setSaving. Atualizar o comentário de
    cabeçalho do arquivo (linha 6) que menciona "upsert financial_settings" para refletir a RPC.
  </action>
  <verify>
    <automated>grep -q "set_financial_balance" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "CashflowHealthBanner" src/pages/mercadolivre/MLFluxoCaixa.tsx && ! grep -q "\.upsert(" src/pages/mercadolivre/MLFluxoCaixa.tsx && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - <CashflowHealthBanner /> renderizado acima das Tabs (visível nas duas abas).
    - handleSave usa supabase.rpc('set_financial_balance', { p_org_id, p_amount }); upsert direto removido.
    - invalidateQueries inclui a chave do data_health para o banner reavaliar após reancorar.
    - Validação NaN, toasts e setSaving preservados.
    - tsc limpo; suíte vitest completa verde.
  </acceptance_criteria>
  <done>Banner acima das Tabs; handleSave usa set_financial_balance (upsert direto removido); tsc + vitest verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → RPC set_financial_balance | escrita de saldo; só owner pode |
| client → RPC get_cashflow_data_health | leitura de metadados de sync da própria org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-05 | Elevation of Privilege | escrita de saldo por não-owner via set_financial_balance | mitigate | RLS financial_settings_write (owner) herdada pela RPC INVOKER (backend, plano 95-01); frontend só invoca |
| T-95-06 | Information Disclosure | banner exibindo dados de sync de outra org | mitigate | hook usa currentOrg.id; RPC INVOKER + RLS filtra a org do caller |
| T-95-SC | Tampering (supply chain) | npm installs | accept | nenhum pacote novo (usa Alert/Link/date-fns/react-query já presentes) |
</threat_model>

<verification>
- vitest run verde (hook + suíte existente sem regressão).
- tsc --noEmit sem erros.
- grep confirma banner inserido e upsert direto removido de MLFluxoCaixa.tsx.
- Validação visual (banner aparece/some por flag; reancoragem atualiza âncora) é checkpoint do plano 95-03.
</verification>

<success_criteria>
- Hook, componente e edições entregues; TDD no hook (testes antes, verdes depois).
- Reancoragem do saldo passa por set_financial_balance (balance_anchor_date sempre atualizado).
- Banner condicional acionável no topo da página, textos e link corretos.
</success_criteria>

<output>
Create `.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-02-SUMMARY.md` when done
</output>
