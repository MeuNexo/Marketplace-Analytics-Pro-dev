---
phase: 95-fluxo-de-caixa-confiavel
plan: 03
type: execute
wave: 3
depends_on: ["95-01", "95-02"]
files_modified: []
autonomous: false
requirements: [CASH-95-07, CASH-95-06]
must_haves:
  truths:
    - "As 4 migrations da Phase 95 estão aplicadas no projeto vivo ckcdevcxgvueywivefgx via MCP"
    - "Com anchor_date=hoje, get_cashflow produz curva idêntica ao comportamento anterior (não-regressão)"
    - "get_rolled_opening_balance com âncora no passado soma entradas − saídas pagas do intervalo"
    - "outflow status='pending' no intervalo é ignorado pelo roll (só 'paid' conta)"
    - "get_cashflow_data_health devolve flags corretas e cross-org retorna 0 linhas (anti-IDOR)"
    - "Banner e reancoragem validados visualmente por Wesley"
  artifacts: []
  key_links:
    - from: "MCP apply_migration"
      to: "projeto ckcdevcxgvueywivefgx"
      via: "aplicação das 4 migrations na ordem de timestamp"
      pattern: "get_rolled_opening_balance"
---

<objective>
Aplicar as 4 migrations da Phase 95 no projeto Supabase vivo (via MCP — o executor não aplica
migrations nem faz deploy neste projeto; o orquestrador faz via MCP apply_migration) e PROVAR
não-regressão + anti-IDOR com SQL ao vivo, mais o ok visual do Wesley. Este plano é o gate de
verdade da fase: a lógica só é considerada correta depois de reconciliar ao comportamento atual.

Purpose: garantir que o roll-forward não muda a curva quando âncora=hoje (não-regressão travada por
Wesley) e que as RPCs INVOKER filtram por org de fato.
Output: prova SQL registrada no SUMMARY + aprovação visual.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-CONTEXT.md
@.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Aplicar migrations via MCP + provar não-regressão, roll, health e anti-IDOR</name>
  <action>
    Checkpoint executado pelo ORQUESTRADOR (não pelo gsd-executor — este projeto aplica migrations e
    faz deploy só via MCP/CLI). Seguir os passos de <how-to-verify>: checar drift de timestamp,
    aplicar as 4 migrations em ordem via apply_migration, e provar por SQL ao vivo (como role
    authenticated) não-regressão (âncora=hoje → delta 0), roll com âncora passada, pending ignorado,
    flags de health e anti-IDOR cross-org. Só então colher o ok visual do Wesley. Registrar provas no SUMMARY.
  </action>
  <verify>
    <human-check>Wesley confirma banner (aparece/some, textos, link /integracoes) e que reancorar o saldo atualiza balance_anchor_date; provas SQL de não-regressão e anti-IDOR registradas no SUMMARY</human-check>
  </verify>
  <what-built>
    4 migrations SQL (plano 95-01) + frontend (plano 95-02) prontos. Falta aplicar ao vivo e provar.
    Contas de referência (memória do projeto): org 7f615df7 = "Pé Vermeio" (seller 1639558873, TEM
    custos/âncora real); org e4150d57 = "Thales" (revenda) — usar as duas para o teste cross-org.
  </what-built>
  <how-to-verify>
    O ORQUESTRADOR executa via MCP Supabase (projeto ckcdevcxgvueywivefgx — NÃO o do CLAUDE.md):
    1. DRIFT (Pitfall 4): list_migrations / SELECT max(version) para achar o MAIOR timestamp já
       aplicado no banco vivo. Se algum for >= 20260695000000, RENOMEAR os 4 arquivos para timestamps
       maiores (mantendo a ordem relativa) antes de aplicar. Confirmar que 20260694000000_dre_month_close
       está no banco (drift conhecido).
    2. APLICAR as 4 migrations na ordem: 20260695000000 (coluna+helper) → ...000100 (3 RPCs) →
       ...000200 (set_financial_balance) → ...000300 (health), via apply_migration.
    3. BACKFILL sanity (Pitfall 2): SELECT organization_id, initial_balance, balance_anchor_date,
       updated_at FROM financial_settings — conferir se balance_anchor_date pós-backfill faz sentido
       (Wesley relatou saldo parado ~18 dias; se a âncora vier MUITO mais antiga, reportar).
    4. NÃO-REGRESSÃO (decisão travada): para a org Pé Vermeio, com balance_anchor_date = hoje_BRT,
       provar que get_rolled_opening_balance(org) == initial_balance cru, e que a série de get_cashflow
       é idêntica à de antes da migration (diff = 0). Um jeito: SET balance_anchor_date = hoje via
       set_financial_balance com o valor atual e comparar a curva.
    5. ROLL com âncora passada: com uma âncora −Nd, get_rolled_opening_balance = âncora
       + SUM(cash_inflows.net_amount no intervalo) − SUM(cash_outflows.amount status='paid' no intervalo).
       Conferir contra um SELECT manual das somas.
    6. PENDING ignorado (Pitfall 5): confirmar que um cash_outflows status='pending' dentro do
       intervalo NÃO entra no roll (só 'paid').
    7. HEALTH: get_cashflow_data_health(Pé Vermeio) devolve tiny_stale/mp_stale/anchor_stale coerentes
       com MAX(synced_at FILTER source='tiny') / MAX(cash_inflows.synced_at) / balance_anchor_date.
       Testar Pitfall 3: inserir/considerar uma linha cash_outflows sem source='tiny' e confirmar que
       tiny_stale reflete o sync REAL do Tiny, não a linha manual.
    8. ANTI-IDOR (rodar como role authenticated real, não postgres — via impersonação/JWT): chamar as
       RPCs com JWT de um usuário da Pé Vermeio passando p_org_id da Thales → deve retornar 0/erro,
       nunca dados da outra org. own vs cross documentados.
    9. VISUAL (Wesley, no preview/prod): (a) a faixa de saúde aparece quando há gatilho e some quando
       não há; textos e link para /integracoes corretos; (b) ajustar o saldo pelo dialog atualiza
       balance_anchor_date no banco (reancoragem) e o alerta de âncora some.
    Registrar todas as provas (valores, deltas, own/cross) no SUMMARY.
  </how-to-verify>
  <resume-signal>Digite "approved" após aplicar as migrations e confirmar não-regressão + anti-IDOR + ok visual, ou descreva os problemas encontrados</resume-signal>
  <done>4 migrations aplicadas via MCP; não-regressão (delta 0 com âncora=hoje), roll, pending-ignorado, health e anti-IDOR cross-org provados por SQL; ok visual do Wesley registrado no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC (authenticated) → tabelas | validação anti-IDOR ao vivo como role real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Elevation of Privilege (IDOR) | RPCs Phase 95 chamadas com p_org_id alheio | mitigate | prova ao vivo como role authenticated: cross-org retorna 0 (passo 8) |
| T-95-04 | Tampering | falso "Tiny OK" mascarando token morto | mitigate | prova do FILTER source='tiny' (passo 7) |
| T-95-07 | Tampering (drift de migration) | timestamp colidindo com estado vivo | mitigate | checar max version antes de aplicar e renumerar (passo 1, Pitfall 4) |
| T-95-SC | Tampering (supply chain) | npm/deno installs | accept | nenhum pacote novo nesta fase |
</threat_model>

<verification>
- 4 migrations aplicadas no banco vivo na ordem correta, sem colisão de timestamp.
- Não-regressão provada por SQL (delta 0 com âncora=hoje).
- Roll com âncora passada e pending-ignorado provados por SQL.
- Anti-IDOR provado como role authenticated (cross-org = 0).
- Ok visual do Wesley (banner + reancoragem).
</verification>

<success_criteria>
- Fase reconciliada ao comportamento atual (não-regressão) + os dois furos (saldo velho, sync
  congelado) agora visíveis/corrigidos.
- Provas registradas no SUMMARY; aprovação do Wesley registrada.
</success_criteria>

<output>
Create `.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-03-SUMMARY.md` when done
</output>
