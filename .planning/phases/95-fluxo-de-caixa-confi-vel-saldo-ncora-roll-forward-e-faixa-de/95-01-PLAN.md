---
phase: 95-fluxo-de-caixa-confiavel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260695000000_cashflow_balance_anchor.sql
  - supabase/migrations/20260695000100_cashflow_rpcs_use_rolled_balance.sql
  - supabase/migrations/20260695000200_set_financial_balance_rpc.sql
  - supabase/migrations/20260695000300_cashflow_data_health_rpc.sql
autonomous: true
requirements: [CASH-95-01, CASH-95-02, CASH-95-03, CASH-95-04, CASH-95-06]
must_haves:
  truths:
    - "financial_settings tem coluna balance_anchor_date com backfill = updated_at::date"
    - "get_rolled_opening_balance(org) retorna âncora + entradas MP − saídas pagas no intervalo [anchor_date, hoje_BRT)"
    - "Com anchor_date = hoje, get_rolled_opening_balance retorna a âncora crua (não-regressão)"
    - "get_cashflow, get_projected_balance_summary e get_treasury_panel derivam v_initial de get_rolled_opening_balance"
    - "set_financial_balance grava initial_balance + balance_anchor_date (BRT) atomicamente"
    - "get_cashflow_data_health retorna flags tiny_stale (>6h), mp_stale (>6h), anchor_stale (>7d)"
    - "Todas as RPCs novas são SECURITY INVOKER (anti-IDOR via RLS is_org_member)"
  artifacts:
    - path: "supabase/migrations/20260695000000_cashflow_balance_anchor.sql"
      provides: "coluna balance_anchor_date + backfill + get_rolled_opening_balance"
      contains: "get_rolled_opening_balance"
    - path: "supabase/migrations/20260695000100_cashflow_rpcs_use_rolled_balance.sql"
      provides: "3 RPCs consumidoras trocam v_initial"
      contains: "get_rolled_opening_balance"
    - path: "supabase/migrations/20260695000200_set_financial_balance_rpc.sql"
      provides: "RPC de escrita atômica da âncora"
      contains: "set_financial_balance"
    - path: "supabase/migrations/20260695000300_cashflow_data_health_rpc.sql"
      provides: "RPC de saúde dos dados"
      contains: "get_cashflow_data_health"
  key_links:
    - from: "get_cashflow / get_projected_balance_summary / get_treasury_panel"
      to: "get_rolled_opening_balance"
      via: "v_initial := public.get_rolled_opening_balance(p_org_id)"
      pattern: "get_rolled_opening_balance"
    - from: "get_rolled_opening_balance"
      to: "cash_inflows / cash_outflows / financial_settings"
      via: "SUM sobre release_date/outflow_date filtrados por status='paid'"
      pattern: "status = 'paid'"
---

<objective>
Parte A (backend/RPCs) da Phase 95: trocar o saldo de abertura manual e velho por um saldo
**âncora + roll-forward** calculado no banco, e criar a RPC de saúde dos dados. Escreve 4 migrations
SQL (aplicadas ao vivo via MCP no plano 95-03 — este plano só as AUTORA).

Purpose: eliminar o furo do saldo que envelhece (base do acúmulo da curva) e centralizar a lógica
do roll-forward num único lugar (get_rolled_opening_balance), consumida pelas 3 RPCs existentes.
Output: 4 arquivos de migration em supabase/migrations/ prontos para aplicação via MCP.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-CONTEXT.md
@.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-RESEARCH.md
@.planning/specs/2026-07-13-fluxo-caixa-confiavel.md
</context>

<artifacts_this_phase_produces>
Colunas: financial_settings.balance_anchor_date (date, nullable, backfill = updated_at::date)
Funções/RPCs novas:
  - get_rolled_opening_balance(p_org_id uuid) RETURNS numeric
  - set_financial_balance(p_org_id uuid, p_amount numeric) RETURNS void
  - get_cashflow_data_health(p_org_id uuid) RETURNS TABLE(...)
Funções alteradas (CREATE OR REPLACE, só a origem de v_initial muda):
  - get_cashflow, get_projected_balance_summary, get_treasury_panel
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migration — coluna balance_anchor_date + backfill + get_rolled_opening_balance</name>
  <files>supabase/migrations/20260695000000_cashflow_balance_anchor.sql</files>
  <read_first>
    - .planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-RESEARCH.md (seção "Code Examples" → esqueleto get_rolled_opening_balance, linhas 484-527; Pitfall 5 sobre status='paid' vs 'pending')
    - supabase/migrations/20260686000000_cash_outflows_competence_date.sql (molde de coluna aditiva + backfill idempotente)
    - supabase/migrations/20260618100000_cash_flow_tables.sql (schema financial_settings/cash_inflows/cash_outflows + RLS is_org_member; colunas release_date, net_amount, outflow_date, amount, status)
    - supabase/migrations/20260619020000_cashflow_brt_timezone.sql (padrão "hoje" = (now() AT TIME ZONE 'America/Sao_Paulo')::date)
  </read_first>
  <action>
    Criar migration idempotente que: (1) adiciona coluna financial_settings.balance_anchor_date DATE
    (ALTER TABLE ... ADD COLUMN IF NOT EXISTS), nullable; (2) backfill das linhas existentes com
    UPDATE ... SET balance_anchor_date = updated_at::date WHERE balance_anchor_date IS NULL (decisão
    travada por Wesley — backfill = updated_at::date); (3) cria a função
    get_rolled_opening_balance(p_org_id uuid) RETURNS numeric como LANGUAGE plpgsql SECURITY INVOKER
    SET search_path = 'public', seguindo o esqueleto do RESEARCH linhas 487-521.
    A função: lê balance_anchor_date + initial_balance de financial_settings da org; define v_today =
    (now() AT TIME ZONE 'America/Sao_Paulo')::date; se anchor_date IS NULL retorna initial_balance cru
    (COALESCE 0) — org sem âncora = comportamento atual; senão retorna
    âncora + SUM(cash_inflows.net_amount WHERE release_date >= anchor_date AND release_date < v_today)
    − SUM(cash_outflows.amount WHERE status='paid' AND outflow_date >= anchor_date AND outflow_date < v_today).
    CRÍTICO (Pitfall 5): o filtro do roll-forward é status='paid' (dinheiro que JÁ saiu entre âncora e
    hoje) — NÃO reusar o filtro 'pending' da série futura de get_cashflow; comentar por que o status
    difere. Intervalo semi-aberto [anchor_date, v_today): âncora=hoje → intervalo vazio → retorna
    âncora crua (não-regressão). NÃO adicionar checagem manual de org (IF is_org_member...) — INVOKER +
    RLS de is_org_member das 3 tabelas já é o guard (padrão do domínio, RESEARCH Pattern 1).
    Fechar com REVOKE EXECUTE ... FROM PUBLIC, anon e GRANT EXECUTE ... TO authenticated, service_role.
    NOTA de timestamp (Pitfall 4): o nome do arquivo usa 20260695000000 (> 20260694000000 que já está
    em prod/main mas não neste checkout). O plano 95-03 confere o max real via MCP antes de aplicar e
    renumera se necessário — não colidir.
  </action>
  <verify>
    <automated>grep -q "get_rolled_opening_balance" supabase/migrations/20260695000000_cashflow_balance_anchor.sql && grep -q "SECURITY INVOKER" supabase/migrations/20260695000000_cashflow_balance_anchor.sql && grep -q "balance_anchor_date" supabase/migrations/20260695000000_cashflow_balance_anchor.sql && grep -q "America/Sao_Paulo" supabase/migrations/20260695000000_cashflow_balance_anchor.sql && grep -c "status = 'paid'" supabase/migrations/20260695000000_cashflow_balance_anchor.sql | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Coluna balance_anchor_date adicionada de forma idempotente (IF NOT EXISTS) + backfill = updated_at::date.
    - get_rolled_opening_balance criada como SECURITY INVOKER, sem checagem manual de org.
    - Roll usa release_date para entradas (cash_inflows.net_amount) e outflow_date + status='paid' para saídas.
    - Intervalo semi-aberto [anchor_date, hoje_BRT); anchor_date=hoje → retorna âncora crua.
    - anchor_date NULL → retorna initial_balance cru (COALESCE 0).
    - GRANT/REVOKE de execução aplicados.
  </acceptance_criteria>
  <done>Migration cria coluna balance_anchor_date + backfill e get_rolled_opening_balance (INVOKER, status='paid', BRT); grep gate verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migration — get_cashflow / get_projected_balance_summary / get_treasury_panel usam get_rolled_opening_balance</name>
  <files>supabase/migrations/20260695000100_cashflow_rpcs_use_rolled_balance.sql</files>
  <read_first>
    - supabase/migrations/20260660000000_cashflow_dfc_alignment.sql (corpo ATUAL de get_cashflow — v_initial na linha ~58; CTE exp com status='pending' na linha ~85)
    - supabase/migrations/20260660000200_cashflow_saldo_indicators_forecasts.sql (corpo ATUAL de get_projected_balance_summary — v_initial ~linha 44; e get_treasury_panel — SELECT fs.initial_balance INTO v_initial junto com fs.alert_threshold ~linha 97)
    - .planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-RESEARCH.md (seção "Substituição nas 3 RPCs", linhas 529-540; Pitfall 5 linhas 427-442)
  </read_first>
  <action>
    Criar migration que faz CREATE OR REPLACE das 3 funções, copiando VERBATIM o corpo atual de cada
    uma (lido dos arquivos de migration mais recentes acima) e mudando APENAS a origem de v_initial:
    trocar o bloco "v_initial := COALESCE((SELECT fs.initial_balance FROM financial_settings ...),0)"
    por "v_initial := public.get_rolled_opening_balance(p_org_id);". Mudança cirúrgica — preservar TODO
    o resto: a projeção SMA, o CASE de accumulated_balance_sma (CASHFIX projeção 7d), o daily_projection,
    a CTE exp com status='pending' (é a série FUTURA — intencionalmente diferente do 'paid' do roll,
    Pitfall 5), assinaturas públicas idênticas, GRANT/REVOKE.
    ATENÇÃO get_treasury_panel: hoje o SELECT lê fs.initial_balance E fs.alert_threshold na MESMA query
    (INTO v_initial, v_threshold). Ao trocar, manter a leitura de fs.alert_threshold intacta — só o
    initial_balance vira get_rolled_opening_balance. NÃO reintroduzir leitura direta de initial_balance
    em nenhum lugar novo (Anti-Pattern do RESEARCH). Manter SECURITY INVOKER em todas.
    Verificação de escopo (Assumption A3): rodar um grep de "initial_balance" em TODAS as migrations
    para confirmar que só estas 3 RPCs (+ a coluna) leem a coluna direto; se aparecer outra RPC fora do
    domínio de fluxo de caixa lendo initial_balance, registrar no SUMMARY como fora-de-escopo/risco.
  </action>
  <verify>
    <automated>grep -c "get_rolled_opening_balance(p_org_id)" supabase/migrations/20260695000100_cashflow_rpcs_use_rolled_balance.sql | grep -qE '^[3-9]|^[1-9][0-9]' && grep -q "CREATE OR REPLACE FUNCTION public.get_cashflow" supabase/migrations/20260695000100_cashflow_rpcs_use_rolled_balance.sql && grep -q "get_projected_balance_summary" supabase/migrations/20260695000100_cashflow_rpcs_use_rolled_balance.sql && grep -q "get_treasury_panel" supabase/migrations/20260695000100_cashflow_rpcs_use_rolled_balance.sql && grep -q "alert_threshold" supabase/migrations/20260695000100_cashflow_rpcs_use_rolled_balance.sql</automated>
  </verify>
  <acceptance_criteria>
    - As 3 funções recriadas via CREATE OR REPLACE, corpo preservado exceto a origem de v_initial.
    - v_initial := public.get_rolled_opening_balance(p_org_id) nas 3.
    - get_treasury_panel ainda lê fs.alert_threshold (não regrediu).
    - CTE exp de get_cashflow mantém status='pending' (série futura); confirmado que difere do 'paid' do roll.
    - Nenhuma leitura nova/direta de initial_balance fora de get_rolled_opening_balance; grep de escopo executado e reportado.
    - Todas SECURITY INVOKER; assinaturas públicas inalteradas.
  </acceptance_criteria>
  <done>As 3 RPCs derivam v_initial de get_rolled_opening_balance, resto do corpo preservado; grep de escopo de initial_balance reportado.</done>
</task>

<task type="auto">
  <name>Task 3: Migrations — set_financial_balance (escrita atômica da âncora) + get_cashflow_data_health</name>
  <files>supabase/migrations/20260695000200_set_financial_balance_rpc.sql, supabase/migrations/20260695000300_cashflow_data_health_rpc.sql</files>
  <read_first>
    - .planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-RESEARCH.md (seção "RPC de escrita da âncora" linhas 542-568; Pattern 2 shape do health linhas 257-291; Pitfall 3 sobre FILTER source='tiny' linhas 390-404)
    - supabase/migrations/20260618100000_cash_flow_tables.sql (RLS financial_settings_write = get_org_role owner, linhas 76-81; colunas synced_at de cash_inflows/cash_outflows; cash_outflows.source)
    - supabase/functions/sync-tiny-payables/index.ts (grava source='tiny' + synced_at) e sync-mp-releases/index.ts (cash_inflows sem coluna source)
  </read_first>
  <action>
    Migration set_financial_balance (arquivo 20260695000200): criar
    set_financial_balance(p_org_id uuid, p_amount numeric) RETURNS void, LANGUAGE plpgsql SECURITY
    INVOKER SET search_path='public'. Faz INSERT ... ON CONFLICT (organization_id) DO UPDATE gravando
    ATOMICAMENTE initial_balance = p_amount, balance_anchor_date = (now() AT TIME ZONE
    'America/Sao_Paulo')::date, updated_at = now(). Resolve o Pitfall 1 (o upsert parcial do dialog
    nunca tocava balance_anchor_date). Data BRT calculada NO SERVIDOR (mesma fonte que
    get_rolled_opening_balance — não confiar na data do browser). NÃO reimplementar checagem de owner:
    a RLS financial_settings_write (get_org_role=owner) já é herdada via INVOKER. REVOKE FROM PUBLIC,
    anon; GRANT TO authenticated.
    Migration get_cashflow_data_health (arquivo 20260695000300): criar
    get_cashflow_data_health(p_org_id uuid) RETURNS TABLE com colunas: tiny_last_sync timestamptz,
    tiny_hours_ago numeric, tiny_stale boolean; mp_last_sync timestamptz, mp_hours_ago numeric,
    mp_stale boolean; anchor_date date, anchor_days_ago numeric, anchor_stale boolean. INVOKER,
    search_path='public'. tiny_last_sync = MAX(cash_outflows.synced_at) FILTER (WHERE source = 'tiny')
    — FILTER obrigatório (Pitfall 3: linha manual não pode mascarar Tiny morto). mp_last_sync =
    MAX(cash_inflows.synced_at) puro (cash_inflows não tem coluna source). hours_ago =
    EXTRACT(EPOCH FROM (now() - last_sync))/3600. tiny_stale/mp_stale = (last_sync IS NULL OR hours_ago > 6);
    NULL (nunca sincronizou) conta como stale=true. anchor_date de financial_settings.balance_anchor_date;
    anchor_days_ago = v_today − anchor_date (BRT); anchor_stale = (anchor_date IS NULL OR anchor_days_ago > 7).
    Sem checagem manual de org (INVOKER + RLS is_org_member). REVOKE/GRANT como as demais.
  </action>
  <verify>
    <automated>grep -q "set_financial_balance" supabase/migrations/20260695000200_set_financial_balance_rpc.sql && grep -q "balance_anchor_date" supabase/migrations/20260695000200_set_financial_balance_rpc.sql && grep -q "America/Sao_Paulo" supabase/migrations/20260695000200_set_financial_balance_rpc.sql && grep -q "get_cashflow_data_health" supabase/migrations/20260695000300_cashflow_data_health_rpc.sql && grep -q "FILTER" supabase/migrations/20260695000300_cashflow_data_health_rpc.sql && grep -q "SECURITY INVOKER" supabase/migrations/20260695000300_cashflow_data_health_rpc.sql</automated>
  </verify>
  <acceptance_criteria>
    - set_financial_balance grava initial_balance + balance_anchor_date (BRT servidor) + updated_at atomicamente (INSERT ON CONFLICT).
    - set_financial_balance é INVOKER; não reimplementa checagem de owner (herda financial_settings_write).
    - get_cashflow_data_health retorna as 9 colunas; tiny via MAX(synced_at) FILTER (source='tiny'); mp via MAX puro.
    - Flags stale: tiny/mp >6h, anchor >7d; NULL last_sync/anchor → stale=true.
    - Ambas SECURITY INVOKER com REVOKE/GRANT corretos.
  </acceptance_criteria>
  <done>set_financial_balance grava âncora atômica (BRT servidor); get_cashflow_data_health devolve 9 colunas com FILTER source='tiny'; grep gate verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(authenticated JWT) → RPC | caller passa p_org_id; poderia tentar ler/escrever org alheia |
| RPC → tabelas (financial_settings, cash_inflows, cash_outflows) | leitura/escrita filtrada por RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Elevation of Privilege (IDOR) | get_rolled_opening_balance / get_cashflow_data_health via p_org_id arbitrário | mitigate | SECURITY INVOKER + RLS is_org_member das 3 tabelas; NUNCA DEFINER; sem checagem manual redundante |
| T-95-02 | Elevation of Privilege | set_financial_balance escrita por não-owner | mitigate | INVOKER herda RLS financial_settings_write (get_org_role = owner); não reimplementar dentro da função |
| T-95-03 | Information Disclosure | leitura de synced_at/saldo de outra org via health RPC | mitigate | RLS SELECT de cash_inflows/cash_outflows/financial_settings herdada via INVOKER |
| T-95-04 | Tampering | falso "Tiny sincronizado" mascarando token morto | mitigate | MAX(synced_at) FILTER (source='tiny') em vez de MAX puro (Pitfall 3) |
| T-95-SC | Tampering (supply chain) | npm/deno installs | accept | nenhum pacote novo instalado nesta fase (RESEARCH: audit N/A) |
</threat_model>

<verification>
- grep confirma as 4 migrations existem com as funções/coluna esperadas.
- Nenhuma função usa SECURITY DEFINER (grep -L não deve encontrar DEFINER nas novas funções).
- get_rolled_opening_balance usa status='paid'; get_cashflow mantém status='pending' na série futura.
- Validação SQL AO VIVO (não-regressão, anti-IDOR, roll com âncora passada, pending ignorado, health
  flags) é feita no plano 95-03 via MCP execute_sql como role authenticated — NÃO neste plano
  (executor não aplica migrations).
</verification>

<success_criteria>
- 4 migrations SQL escritas, idempotentes onde aplicável, SECURITY INVOKER, GRANT/REVOKE corretos.
- Lógica do roll-forward centralizada em get_rolled_opening_balance; 3 RPCs só trocam v_initial.
- set_financial_balance e get_cashflow_data_health criadas conforme spec.
- Grep de escopo de initial_balance executado e reportado no SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/95-fluxo-de-caixa-confi-vel-saldo-ncora-roll-forward-e-faixa-de/95-01-SUMMARY.md` when done
</output>
