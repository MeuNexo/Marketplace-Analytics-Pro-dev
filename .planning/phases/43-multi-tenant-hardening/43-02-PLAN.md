---
phase: 43-multi-tenant-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sync-ml-orders/index.ts
  - supabase/functions/sync-ml-billing/index.ts
  - supabase/functions/ml-reputation/index.ts
  - supabase/functions/ml-inventory/index.ts
  - supabase/migrations/20260614122000_tenant03_check_quota_rpc.sql
  - supabase/migrations/20260614122500_tenant03_fix_sync_cron_pattern_b.sql
  - supabase/functions/process-sync-job/index.ts
autonomous: false
requirements: [TENANT-03]
must_haves:
  truths:
    - "Token lookup em sync-ml-orders e sync-ml-billing usa ORDER BY determinístico (D-13, ME-04)"
    - "ml-reputation e ml-inventory validam is_org_member antes de aceitar ml_user_id de input (D-14, ME-05)"
    - "check_quota RPC bloqueia sync quando a org excede a quota diaria e loga; tier enterprise (-1) nunca bloqueia (D-04, D-06, TENANT-03)"
    - "pg_cron de sync aponta para ckcdevcxgvueywivefgx via Pattern B (vault SERVICE_ROLE_KEY sb_secret_), nao para o projeto errado (D-04, RESEARCH Pitfall 4)"
  artifacts:
    - path: "supabase/migrations/20260614122000_tenant03_check_quota_rpc.sql"
      provides: "RPC check_quota(_org_id) SECURITY DEFINER que incrementa sync_quota_daily e retorna boolean"
      contains: "check_quota"
    - path: "supabase/migrations/20260614122500_tenant03_fix_sync_cron_pattern_b.sql"
      provides: "Recria pg_cron sync-process-job com URL ckcdevcxgvueywivefgx + vault SERVICE_ROLE_KEY"
      contains: "ckcdevcxgvueywivefgx"
    - path: "supabase/functions/process-sync-job/index.ts"
      provides: "Gate check_quota antes de processar o job (D-04)"
      contains: "check_quota"
  key_links:
    - from: "supabase/functions/process-sync-job/index.ts"
      to: "check_quota RPC"
      via: "sb.rpc('check_quota')"
      pattern: "check_quota"
    - from: "supabase/functions/sync-ml-orders/index.ts"
      to: "ml_tokens"
      via: "ORDER BY no token lookup"
      pattern: "order\\("
---

<objective>
Endurecer a seguranca multi-tenant nas Edge Functions e habilitar enforcement de quota de sync: token lookup determinístico (ME-04, D-13), guard is_org_member em ml-reputation/ml-inventory (ME-05, D-14), e RPC check_quota com gate no dispatch/process (TENANT-03, D-04/D-05/D-06). Tambem corrigir o pg_cron de sync que aponta para o projeto errado (RESEARCH Pitfall 4).

Purpose: Lookup sem ORDER BY e token confusion cross-tenant. ml_user_id sem guard permite enumeracao de dados de outra org. Quota sem enforcement permite abuso de tier. pg_cron apontando para projeto errado deixa jobs sem processamento.
Output: 4 EFs ajustadas + 2 migrations (RPC check_quota + recria cron Pattern B), aplicadas via MCP, EFs deployadas com aprovacao de Wesley.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-multi-tenant-hardening/43-CONTEXT.md
@.planning/phases/43-multi-tenant-hardening/43-RESEARCH.md
@supabase/functions/sync-ml-orders/index.ts
@supabase/functions/sync-ml-billing/index.ts
@supabase/functions/ml-reputation/index.ts
@supabase/functions/ml-inventory/index.ts
@supabase/functions/sync-ml-inventory/index.ts
@supabase/functions/process-sync-job/index.ts
@supabase/migrations/20260519120000_organization_plans_quota.sql
@supabase/migrations/20260519140000_sync_jobs.sql
</context>

<artifacts_this_phase_produces>
Simbolos criados/alterados neste plano:
- supabase/functions/sync-ml-orders/index.ts: token lookup com .order("updated_at", desc) + filtro por organization_id quando conhecido
- supabase/functions/sync-ml-billing/index.ts: idem ME-04
- supabase/functions/ml-reputation/index.ts: confirmar/completar guard is_org_member (ja existe parcial)
- supabase/functions/ml-inventory/index.ts: confirmar/completar guard is_org_member (ja existe parcial)
- supabase/functions/process-sync-job/index.ts: chamada sb.rpc("check_quota", { _org_id }) antes de despachar; se false, marca job como skipped/blocked + log
- Migration 20260614122000_tenant03_check_quota_rpc.sql (NOVO): RPC check_quota(_org_id uuid) RETURNS boolean
- Migration 20260614122500_tenant03_fix_sync_cron_pattern_b.sql (NOVO): recria cron sync-process-job/dispatch Pattern B com URL ckcdevcxgvueywivefgx
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: ME-04 token lookup determinístico + ME-05 confirmar/completar guard is_org_member</name>
  <read_first>
    - supabase/functions/sync-ml-orders/index.ts (token lookup sem ORDER BY)
    - supabase/functions/sync-ml-billing/index.ts (token lookup sem ORDER BY)
    - supabase/functions/ml-reputation/index.ts (guard is_org_member ja presente — confirmar token lookup ORDER BY)
    - supabase/functions/ml-inventory/index.ts (guard is_org_member ja presente — confirmar)
    - supabase/functions/ml-ads/index.ts (ja tem .order updated_at + guard — usar como referencia do padrao correto)
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Code Examples ME-04 e ME-05)
  </read_first>
  <action>
    ME-04 (D-13): nos token lookups de sync-ml-orders e sync-ml-billing (que hoje fazem .eq("ml_user_id", X).not(access_token is null).limit(1).maybeSingle() SEM ordenacao), adicionar .order("updated_at", { ascending: false }) antes do .limit(1) — o token mais recente, determinístico. Quando o caller conhece organization_id (sync-ml-orders recebe via job), adicionar .eq("organization_id", organization_id) ao lookup para filtrar por org. Em ml-reputation, confirmar se o token lookup tem ORDER BY; se nao, adicionar o mesmo padrao. Espelhar exatamente o padrao ja correto de ml-ads/index.ts (getAccessToken).
    ME-05 (D-14): confirmar que ml-reputation e ml-inventory validam is_org_member ANTES de aceitar o ml_user_id de input (retornando 403 generico se nao-membro — nao vazar existencia de recurso de outra org). Pela leitura atual, ml-reputation (guard linhas ~58-62) e ml-inventory (guard linhas ~104-111) JA possuem o guard; ml-ads tambem. Auditar os tres e: se algum NAO tiver o guard completo, adicionar seguindo o exemplo do RESEARCH (lookup org do token -> rpc is_org_member -> 403 se !ok). Registrar no SUMMARY o estado de cada EF (ja-tinha vs adicionado). Nao tocar funcoes que so rodam via service role (sem userId de input).
    Cuidado com tamanho da funcao Deno (STATE blocker) — mudancas sao pontuais, nao reescrever os arquivos.
  </action>
  <verify>
    <automated>grep -q 'order("updated_at"' supabase/functions/sync-ml-orders/index.ts && grep -q 'order("updated_at"' supabase/functions/sync-ml-billing/index.ts && grep -q "is_org_member" supabase/functions/ml-reputation/index.ts && grep -q "is_org_member" supabase/functions/ml-inventory/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - sync-ml-orders e sync-ml-billing fazem token lookup com .order("updated_at", desc) + filtro por organization_id quando disponivel.
    - ml-reputation, ml-inventory e ml-ads validam is_org_member antes de aceitar ml_user_id de input, retornando 403 generico.
    - SUMMARY registra, por EF, se o guard ja existia ou foi adicionado.
  </acceptance_criteria>
  <done>ME-04 e ME-05 fechados nas EFs; lookups determinísticos e guards confirmados.</done>
</task>

<task type="auto">
  <name>Task 2: RPC check_quota + gate no process-sync-job + recriar cron Pattern B</name>
  <read_first>
    - supabase/functions/sync-ml-inventory/index.ts (checkAndIncrementQuota — padrao de referencia, D-06)
    - supabase/migrations/20260519120000_organization_plans_quota.sql (organization_plans, sync_quota_daily, enum plan_tier)
    - supabase/migrations/20260519140000_sync_jobs.sql (dispatch_sync_jobs + cron com URL ERRADA gionpsuunfkkzzjdubfy + JWT legado)
    - supabase/functions/process-sync-job/index.ts (drain; ponto de injecao do gate)
    - SUMMARY da Task 1 do plano 43-01 (estado real do cron.job lido via MCP)
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Pattern 2 + Pitfall 4/5)
  </read_first>
  <action>
    A) Migration 20260614122000_tenant03_check_quota_rpc.sql (D-04/D-06): criar RPC public.check_quota(_org_id uuid) RETURNS boolean LANGUAGE plpgsql SECURITY DEFINER SET search_path = public. Logica derivada de checkAndIncrementQuota (sync-ml-inventory): le sync_interval_minutes de organization_plans; se NULL ou = -1 retorna true (enterprise/ilimitado nunca bloqueia — D-04). Senao INSERT em sync_quota_daily (organization_id, date=current_date, sync_count=1) ON CONFLICT (organization_id, date) DO UPDATE sync_count+1 RETURNING sync_count; limite v_limit = greatest(1, floor(1440.0 / v_interval)); retorna sync_count <= v_limit. Escopo Phase 43 = apenas sync (sync_interval_minutes + contagem diaria) — NAO implementar history_days nem nº de lojas (D-05, fica para Phase 44).
    B) process-sync-job/index.ts (D-04): apos claim_next_sync_job e antes de despachar para a sub-funcao, chamar const { data: within } = await sb.rpc("check_quota", { _org_id: job.organization_id }). Se within === false: atualizar o job para status com erro/log explicativo (ex: error_msg = "quota exceeded for org" e status = "failed" ou novo estado de skip — usar "failed" com error_msg claro para nao recriar enum) e RETORNAR sem despachar (bloqueia no dispatch + loga — D-04). Logar console.warn com org_id e contagem. Nao alterar a logica de claim.
    C) Migration 20260614122500_tenant03_fix_sync_cron_pattern_b.sql (Pitfall 4): com base no estado real do cron.job (Task 1 do 43-01), recriar os schedules de sync (sync-process-job-every-5min e, se necessario, dispatch) usando URL https://ckcdevcxgvueywivefgx.supabase.co e Authorization Bearer com a chave do vault Pattern B (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'SERVICE_ROLE_KEY') — NUNCA o JWT anon legado nem a URL gionpsuunfkkzzjdubfy. Espelhar o padrao ja correto das migrations de questions/claims (Pattern B). Se o cron ja estiver correto (confirmado no SUMMARY do 43-01), a migration vira no-op idempotente (unschedule+schedule com valores corretos) — registrar.
    NAO aplicar/deployar ainda (Task 3 BLOCKING).
  </action>
  <verify>
    <automated>grep -q "FUNCTION public.check_quota" supabase/migrations/20260614122000_tenant03_check_quota_rpc.sql && grep -q "= -1" supabase/migrations/20260614122000_tenant03_check_quota_rpc.sql && grep -q "check_quota" supabase/functions/process-sync-job/index.ts && grep -q "ckcdevcxgvueywivefgx" supabase/migrations/20260614122500_tenant03_fix_sync_cron_pattern_b.sql && ! grep -q "gionpsuunfkkzzjdubfy" supabase/migrations/20260614122500_tenant03_fix_sync_cron_pattern_b.sql</automated>
  </verify>
  <acceptance_criteria>
    - check_quota retorna true imediatamente para sync_interval_minutes NULL ou -1 (enterprise nunca bloqueia).
    - check_quota incrementa sync_quota_daily atomicamente (ON CONFLICT) e compara com limite derivado de 1440/interval.
    - process-sync-job chama check_quota apos o claim e NAO despacha quando false, gravando error_msg de quota.
    - Migration de cron usa URL ckcdevcxgvueywivefgx + vault SERVICE_ROLE_KEY; nenhuma referencia ao projeto errado nem JWT legado.
    - Nenhuma logica de history_days ou nº de lojas (fora de escopo — Phase 44, D-05).
  </acceptance_criteria>
  <done>RPC check_quota criado, gate injetado no process-sync-job, cron Pattern B corrigido — tudo escrito, ainda nao aplicado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING] Aplicar migrations via MCP + deploy das EFs (aprovacao Wesley)</name>
  <read_first>
    - As 2 migrations da Task 2 + as EFs alteradas (Task 1 e 2)
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Environment Availability: deploy de EF pode exigir SUPABASE_ACCESS_TOKEN)
    - STATE.md (sessao 2026-06-13b: SERVICE_ROLE_KEY = sb_secret_, vault Pattern B)
  </read_first>
  <what-built>
    RPC check_quota, gate no process-sync-job, fix de ME-04/ME-05 em 4 EFs e cron Pattern B. Ainda NAO aplicados/deployados.
  </what-built>
  <action>
    Migration/deploy em producao exige aprovacao de Wesley (feedback_platform_actions). Pre-flight: confirmar via MCP execute_sql que vault.decrypted_secrets tem name='SERVICE_ROLE_KEY' (Pattern B) ANTES de aplicar a migration de cron — se faltar, pausar e pedir a Wesley para inserir (a migration de cron quebra sem o segredo). Confirmar disponibilidade de SUPABASE_ACCESS_TOKEN para deploy de EF; se ausente, registrar como bloqueio e pedir a Wesley.
    Apos aprovacao: aplicar as 2 migrations via Supabase MCP apply_migration no ckcdevcxgvueywivefgx (NUNCA db push). Deploy das EFs alteradas (sync-ml-orders, sync-ml-billing, ml-reputation, ml-inventory, process-sync-job). Smoke pos-deploy: invocar process-sync-job sem auth -> 401; com Bearer service-role e fila vazia -> {ok:true, no pending jobs}; confirmar via logs que check_quota foi chamado. Confirmar via MCP que cron.job sync-process-job aponta para ckcdevcxgvueywivefgx.
  </action>
  <how-to-verify>
    1. vault tem SERVICE_ROLE_KEY (Pattern B) confirmado via MCP.
    2. Wesley aprova migrations + deploy.
    3. Migrations aplicadas via apply_migration; EFs deployadas (status ACTIVE).
    4. cron.job sync-process-job referencia ckcdevcxgvueywivefgx (nao gionpsuunfkkzzjdubfy).
    5. Smoke: process-sync-job 401 sem auth, ok com service role.
  </how-to-verify>
  <resume-signal>Wesley digita "aprovado"; executor aplica/deploya e cola no SUMMARY a saida do cron.job + status das EFs.</resume-signal>
  <acceptance_criteria>
    - vault Pattern B confirmado antes da migration de cron.
    - 2 migrations aplicadas via MCP apply_migration no ckcdevcxgvueywivefgx.
    - 5 EFs deployadas e ACTIVE; smoke 401/ok confirmado.
    - cron.job de sync aponta para o projeto correto.
  </acceptance_criteria>
  <done>Quota enforcement ativo, ME-04/05 deployados e cron corrigido em producao, com aprovacao de Wesley.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuario authenticated -> EF (ml-reputation/ml-inventory/ml-ads) | usuario passa ml_user_id; EF deve validar membership antes de retornar dados |
| pg_cron -> process-sync-job (service role) | cron dispara drain; quota gate decide se processa |
| EF service role -> ml_tokens | token lookup deve ser determinístico para nao usar token de outra org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05 | Spoofing | token lookup cross-tenant em sync-ml-orders/billing | mitigate | ORDER BY updated_at + filtro organization_id (ME-04, D-13) |
| T-43-06 | Information Disclosure | enumeracao de ml_user_id em ml-reputation/ml-inventory/ml-ads | mitigate | guard is_org_member antes de aceitar ml_user_id; 403 generico (ME-05, D-14) |
| T-43-07 | Abuse / DoS | org excede quota de sync | mitigate | check_quota RPC bloqueia no dispatch + log (TENANT-03, D-04) |
| T-43-08 | Tampering | pg_cron aponta para projeto errado (jobs nunca processados) | mitigate | recriar cron Pattern B com URL ckcdevcxgvueywivefgx + vault (Pitfall 4) |
| T-43-SC | Tampering | npm/deno installs | accept | Nenhum pacote novo instalado nesta fase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- sync-ml-orders/sync-ml-billing token lookup com ORDER BY; ml-reputation/ml-inventory/ml-ads com guard is_org_member.
- check_quota RPC: enterprise(-1) nao bloqueia; tiers menores bloqueiam ao exceder; incremento atomico.
- process-sync-job chama check_quota e bloqueia+loga quando false.
- cron.job de sync aponta para ckcdevcxgvueywivefgx (Pattern B vault).
- Migrations via apply_migration; EFs deployadas ACTIVE.
</verification>

<success_criteria>
- ME-04/ME-05 fechados (deferidos da Phase 41).
- TENANT-03: sync consulta quota por plan_tier e bloqueia excedente, confirmado em logs; enterprise nunca bloqueia.
- Cron de sync funcional no projeto correto.
</success_criteria>

<output>
Create `.planning/phases/43-multi-tenant-hardening/43-02-SUMMARY.md` when done
</output>
