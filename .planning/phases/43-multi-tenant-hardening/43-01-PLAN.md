---
phase: 43-multi-tenant-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql
  - supabase/migrations/20260614120500_tenant02_backfill_orphans_and_notnull.sql
  - supabase/migrations/20260614121000_me06_billing_for_select.sql
  - src/hooks/useMLProductCosts.ts
autonomous: false
requirements: [TENANT-01, TENANT-02]
must_haves:
  truths:
    - "Upsert em ml_product_costs via service role funciona para qualquer org sem depender de user_id = auth.uid() (D-10)"
    - "Custo cadastrado por um membro da org aparece para outro membro da mesma org (D-11)"
    - "Membro de uma org nao le linhas de ml_product_costs de outra org (TENANT-01)"
    - "Tabelas de cache nao tem linhas com organization_id NULL apos backfill/delete (D-01, TENANT-02)"
    - "Viewer nao consegue INSERT/UPDATE/DELETE em ml_billing_monthly (D-15, ME-06)"
  artifacts:
    - path: "supabase/migrations/20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql"
      provides: "RLS org-first consolidada de ml_product_costs"
      contains: "is_org_member"
    - path: "supabase/migrations/20260614120500_tenant02_backfill_orphans_and_notnull.sql"
      provides: "Backfill via ml_tokens + DELETE de orfaos regeneraveis + NOT NULL organization_id"
      contains: "ml_tokens"
    - path: "supabase/migrations/20260614121000_me06_billing_for_select.sql"
      provides: "ml_billing_monthly FOR SELECT (membros leem; escrita so service role)"
      contains: "FOR SELECT"
    - path: "src/hooks/useMLProductCosts.ts"
      provides: "fetchAll le por organization_id (D-11)"
      contains: "organization_id"
  key_links:
    - from: "src/hooks/useMLProductCosts.ts"
      to: "ml_product_costs"
      via: ".eq organization_id"
      pattern: "organization_id"
    - from: "supabase/migrations/20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql"
      to: "is_org_member / get_org_role"
      via: "RLS policy"
      pattern: "is_org_member|get_org_role"
---

<objective>
Consolidar a RLS de ml_product_costs numa policy org-first unica (TENANT-01, D-10/D-11/D-12), resolver dados orfaos (organization_id NULL) em todas as tabelas de cache via estrategia hibrida delete-vs-backfill (TENANT-02, D-01/D-02/D-03), e endurecer ml_billing_monthly para leitura-apenas por membros (ME-06, D-15).

Purpose: Sem RLS org-first, custos cadastrados por um membro nao aparecem para outro membro da org e o isolamento entre orgs fica furado. Orfaos sao dados sem dono acessiveis indevidamente. Viewer escrevendo billing e elevacao de privilegio.
Output: 3 migrations SQL aplicadas via MCP no ckcdevcxgvueywivefgx + ajuste de useMLProductCosts.fetchAll para ler por org.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-multi-tenant-hardening/43-CONTEXT.md
@.planning/phases/43-multi-tenant-hardening/43-RESEARCH.md
@supabase/migrations/20260514120000_ml_product_costs.sql
@supabase/migrations/20260612140000_ml_billing_monthly.sql
@src/hooks/useMLProductCosts.ts
</context>

<artifacts_this_phase_produces>
Simbolos criados/alterados neste plano:
- Migration 20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql (NOVO)
- Migration 20260614120500_tenant02_backfill_orphans_and_notnull.sql (NOVO)
- Migration 20260614121000_me06_billing_for_select.sql (NOVO)
- Policies em ml_product_costs: mpc_select, mpc_insert, mpc_update, mpc_delete (org-first; substituem antigas)
- Policy em ml_billing_monthly: org_member_billing_select (substitui org_member_billing FOR ALL)
- ml_product_costs.organization_id torna-se NOT NULL (apos backfill)
- organization_id NOT NULL nas tabelas de cache backfilladas (matriz da Task 1)
- src/hooks/useMLProductCosts.ts: fetchAll passa a filtrar por organization_id
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar estado real do banco via MCP + montar matriz delete-vs-backfill</name>
  <read_first>
    - .planning/phases/43-multi-tenant-hardening/43-CONTEXT.md (Pre-requisito MCP + D-01/D-02)
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Runtime State Inventory + Open Questions Q1/Q2 + Pitfall 1/2)
    - supabase/migrations/20260514120000_ml_product_costs.sql (RLS antiga a substituir)
    - supabase/migrations/20260612140000_ml_billing_monthly.sql (policy FOR ALL a trocar)
  </read_first>
  <action>
    Usar o Supabase MCP (project ref ckcdevcxgvueywivefgx, NUNCA gionpsuunfkkzzjdubfy) com execute_sql para confirmar o estado real ANTES de escrever as migrations.
    Consultar pg_policies de ml_product_costs e ml_billing_monthly (policyname, cmd, qual, with_check) — listar TODAS as policies ativas a serem dropadas.
    Para CADA tabela com coluna organization_id (ml_daily_cache, ml_hourly_cache, ml_product_daily_cache, ml_state_daily_cache, ml_ads_daily_cache, ml_ads_campaigns_cache, ml_ads_products_cache, ml_user_cache, ml_sync_log, sellers, seller_stores, ml_targets, ml_tax_config, ml_product_costs, ml_tokens, audit_log, shopee_orders, shopee_sales): rodar count(*) FILTER (WHERE organization_id IS NULL) e count(*) total. Paginar/consolidar — nao depender de retorno maior que 1000 linhas (feedback_postgrest_pagination).
    Consultar cron.job (jobname, schedule, command) e registrar quais jobs referenciam URL/JWT do projeto errado (apenas registrar; correcao do cron e do plano 43-02).
    Montar a MATRIZ delete-vs-backfill por D-01: caches regeneraveis (ml_daily_cache, ml_hourly_cache, ml_product_daily_cache, ml_state_daily_cache, ml_ads_daily_cache, ml_ads_campaigns_cache, ml_ads_products_cache, ml_user_cache, ml_sync_log) com organization_id NULL vao para DELETE; config/historico nao-regeneravel (ml_product_costs, ml_tax_config, sellers, seller_stores, ml_targets, ml_tokens) vao para BACKFILL via ml_tokens (D-02). Tabelas SEM ml_user_id (audit_log, shopee_*) nao sao tocadas nesta fase (registrar no SUMMARY como fora de escopo — A3 do RESEARCH). Registrar matriz e contagens reais no SUMMARY.
    NAO aplicar nenhuma migration nesta task (apenas leitura/confirmacao).
  </action>
  <verify>
    <automated>MISSING — confirmacao via MCP execute_sql; registrar no SUMMARY pg_policies (ml_product_costs e ml_billing_monthly) + contagens de orfaos por tabela + lista de cron.job. Acceptance abaixo valida presenca.</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contem a lista exata de policies ativas em ml_product_costs e ml_billing_monthly (nomes + cmd) lidas via MCP.
    - SUMMARY contem tabela "tabela | orfaos | total | acao(delete|backfill|skip)" para cada tabela com organization_id.
    - SUMMARY registra se algum cron.job referencia gionpsuunfkkzzjdubfy ou JWT legado (handoff para 43-02).
    - Nenhuma migration aplicada nesta task.
  </acceptance_criteria>
  <done>Estado real do banco confirmado via MCP e matriz delete-vs-backfill documentada com contagens reais.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever migrations RLS org-first + backfill/orfaos + billing FOR SELECT</name>
  <read_first>
    - Saida da Task 1 (policies reais + matriz delete/backfill)
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Pattern 1 SQL idempotente; Code Examples ME-06; Pitfall 2/3)
    - supabase/migrations/20260519120000_organization_plans_quota.sql (referencia de is_org_member/get_org_role e org_role enum)
    - src/hooks/useMLProductCosts.ts (fetchAll le por user_id — ajustado na Task 3)
  </read_first>
  <action>
    Criar TRES migrations idempotentes (DDL so via migration commitada — feedback_no_drift_via_sql_editor). Descricao concreta do conteudo:

    Migration A 20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql (D-10/D-11): bloco DO com DROP POLICY IF EXISTS para CADA policy listada na Task 1 (incluindo "Users can manage own product costs" e quaisquer org-aware da 20260515133732 se existirem). Criar mpc_select FOR SELECT TO authenticated USING organization_id IS NOT NULL AND is_org_member(auth.uid(), organization_id). Criar mpc_insert FOR INSERT TO authenticated WITH CHECK organization_id IS NOT NULL AND get_org_role(auth.uid(), organization_id) = ANY(ARRAY owner/admin/member cast org_role). Criar mpc_update FOR UPDATE TO authenticated com USING e WITH CHECK identicos a condicao de insert. Criar mpc_delete FOR DELETE TO authenticated USING a mesma condicao. Comentario no topo: service role ignora RLS (recalc-order-costs, sync-tiny-costs, useMLProductCosts upsert escrevem livre); user_id permanece coluna de auditoria (D-10).

    Migration B 20260614120500_tenant02_backfill_orphans_and_notnull.sql (D-01/D-02/D-03): para tabelas BACKFILL, UPDATE t SET organization_id = tok.organization_id FROM ml_tokens tok WHERE t.organization_id IS NULL AND t.ml_user_id = tok.ml_user_id AND tok.organization_id IS NOT NULL (via ml_tokens, NAO organization_members — Pitfall 2/D-02). Para ml_product_costs (sem ml_user_id direto): resolver via item_id->seller_sku->ml_tokens quando possivel; senao backfill para a unica org com ml_tokens (Pe Vermeio), criterio explicito no comentario. NAO usar JOIN que multiplique linhas. Para tabelas DELETE: DELETE FROM <tabela> WHERE organization_id IS NULL (D-01). Apos isso, ALTER COLUMN organization_id SET NOT NULL SOMENTE em tabelas com 0 orfaos restantes (guard via DO block com RAISE se ainda houver NULL — Pitfall 3). Comentario: writers de EF que gravam NULL serao ajustados em 43-02.

    Migration C 20260614121000_me06_billing_for_select.sql (D-15/ME-06): DROP POLICY IF EXISTS org_member_billing ON ml_billing_monthly. Criar org_member_billing_select FOR SELECT TO authenticated USING is_org_member(auth.uid(), organization_id). Sem policy de INSERT/UPDATE/DELETE para authenticated (escrita exclusiva de service role via sync-ml-billing).

    NAO aplicar ainda (apply e a Task 4, BLOCKING).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql | grep -c "DROP POLICY IF EXISTS" | awk '{ exit ($1>=1)?0:1 }' && grep -q "FOR SELECT" supabase/migrations/20260614121000_me06_billing_for_select.sql && grep -q "ml_tokens" supabase/migrations/20260614120500_tenant02_backfill_orphans_and_notnull.sql && grep -q "SET NOT NULL" supabase/migrations/20260614120500_tenant02_backfill_orphans_and_notnull.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration A dropa TODAS as policies listadas na Task 1 antes de criar as novas (idempotente).
    - Policies novas usam is_org_member (SELECT) e get_org_role com owner/admin/member; nenhuma referencia a auth.uid() = user_id como scope.
    - Migration B faz backfill via ml_tokens (nunca organization_members) e DELETE so em caches regeneraveis; SET NOT NULL apenas onde 0 orfaos restam.
    - Migration C deixa ml_billing_monthly somente FOR SELECT para authenticated.
  </acceptance_criteria>
  <done>Tres migrations escritas e commitadas em supabase/migrations/, idempotentes, ainda nao aplicadas.</done>
</task>

<task type="auto">
  <name>Task 3: Ajustar useMLProductCosts.fetchAll para ler por organization_id</name>
  <read_first>
    - src/hooks/useMLProductCosts.ts (fetchAll le por user_id; upsert seta organization_id)
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Pattern 1 — ACAO sobre fetchAll)
    - src/contexts/OrganizationContext.tsx (currentOrg.id)
  </read_first>
  <action>
    Em src/hooks/useMLProductCosts.ts, alterar fetchAll para filtrar por organization_id em vez de user_id (D-11): trocar a clausula que filtra por user_id por filtro por currentOrg.id, e o guard inicial de !user para !currentOrg. Adicionar currentOrg ao array de dependencias do useCallback. Manter limit(10000) ou paginar via range() se a org tiver mais de 10000 custos (feedback_postgrest_pagination — registrar se aplicavel). Manter user_id no payload do upsert/upsertBatch como auditoria (nao remover); apenas a LEITURA muda. Sem novas dependencias.
  </action>
  <verify>
    <automated>grep -q 'eq("organization_id"' src/hooks/useMLProductCosts.ts && ! grep -q 'eq("user_id", user.id)' src/hooks/useMLProductCosts.ts && npx tsc --noEmit 2>&1 | grep "error TS" | grep -c "useMLProductCosts" | awk '{ exit ($1==0)?0:1 }'</automated>
  </verify>
  <acceptance_criteria>
    - fetchAll filtra por currentOrg.id (organization_id), nao por user_id.
    - upsert/upsertBatch continuam gravando user_id (auditoria) + organization_id.
    - tsc --noEmit nao introduz erro novo neste arquivo.
  </acceptance_criteria>
  <done>Frontend le custos por org; custo de um membro aparece para os demais membros da org.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: [BLOCKING] Aplicar as 3 migrations via MCP apply_migration (aprovacao Wesley)</name>
  <read_first>
    - As 3 migrations escritas na Task 2
    - SUMMARY da Task 1 (matriz + contagens — para Wesley conferir o impacto do DELETE/backfill)
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Anti-Patterns: nunca usar supabase db push)
  </read_first>
  <what-built>
    Tres migrations: RLS org-first de ml_product_costs, backfill/delete de orfaos + NOT NULL, e ml_billing_monthly FOR SELECT. Ainda NAO aplicadas em producao.
  </what-built>
  <action>
    Migration/deploy em producao exige aprovacao de Wesley (feedback_platform_actions). Apresentar a Wesley a matriz delete-vs-backfill com contagens reais (Task 1) e o que cada DELETE remove. Apos aprovacao, aplicar via Supabase MCP apply_migration no project ref ckcdevcxgvueywivefgx, NA ORDEM: (1) tenant01 RLS, (2) tenant02 backfill/notnull, (3) me06 billing. NUNCA usar supabase db push (CLI local linkado no projeto errado gionpsuunfkkzzjdubfy). Apos aplicar, validar via execute_sql: pg_policies de ml_product_costs mostra mpc_* org-first; pg_policies de ml_billing_monthly mostra apenas org_member_billing_select; count de organization_id NULL = 0 nas tabelas afetadas.
  </action>
  <how-to-verify>
    1. Wesley aprova a matriz de delete/backfill.
    2. Apos apply: execute_sql confirma policies mpc_* em ml_product_costs e org_member_billing_select unica em ml_billing_monthly.
    3. execute_sql confirma 0 linhas organization_id NULL nas tabelas marcadas NOT NULL.
  </how-to-verify>
  <resume-signal>Wesley digita "aprovado" apos confirmar a matriz; executor aplica e cola a saida do pg_policies pos-apply no SUMMARY.</resume-signal>
  <acceptance_criteria>
    - As 3 migrations aplicadas via MCP apply_migration no ckcdevcxgvueywivefgx (nunca db push).
    - pg_policies pos-apply confirma policies org-first em ml_product_costs e FOR SELECT em ml_billing_monthly.
    - 0 linhas organization_id NULL nas tabelas marcadas NOT NULL.
  </acceptance_criteria>
  <done>Schema aplicado em producao com aprovacao de Wesley e estado validado via MCP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente authenticated -> Postgres (RLS) | Membro de org consulta ml_product_costs / ml_billing_monthly; RLS e a unica fronteira de isolamento |
| service role (EF) -> Postgres | recalc-order-costs / sync-tiny-costs / sync-ml-billing escrevem ignorando RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | ml_product_costs RLS (cross-tenant read) | mitigate | RLS org-first via is_org_member; SELECT exige organization_id IS NOT NULL (TENANT-01, D-10) |
| T-43-02 | Information Disclosure | linhas orfas organization_id NULL acessiveis sem dono | mitigate | Backfill via ml_tokens + DELETE de caches regeneraveis + NOT NULL (TENANT-02, D-01/D-03) |
| T-43-03 | Tampering / Elevation | viewer escrevendo ml_billing_monthly | mitigate | Trocar FOR ALL por FOR SELECT; escrita so service role (ME-06, D-15) |
| T-43-04 | Tampering | NOT NULL aplicado com orfaos remanescentes quebra inserts | accept-with-guard | DO block aborta com RAISE se restar NULL antes do SET NOT NULL (Pitfall 3) |
</threat_model>

<verification>
- Migrations aplicadas via MCP apply_migration no ckcdevcxgvueywivefgx (nao db push).
- pg_policies de ml_product_costs = mpc_select/insert/update/delete org-first; nenhuma policy auth.uid()=user_id.
- pg_policies de ml_billing_monthly = org_member_billing_select apenas.
- 0 linhas organization_id NULL nas tabelas marcadas NOT NULL.
- useMLProductCosts.fetchAll filtra por organization_id; tsc --noEmit limpo.
</verification>

<success_criteria>
- TENANT-01: upsert via service role funciona para qualquer org; custo de um membro visivel para outro membro da mesma org; sem leitura cross-org.
- TENANT-02: zero organization_id NULL nas tabelas de cache resolvidas; NOT NULL aplicado onde seguro.
- ME-06: viewer nao escreve billing.
</success_criteria>

<output>
Create `.planning/phases/43-multi-tenant-hardening/43-01-SUMMARY.md` when done
</output>
