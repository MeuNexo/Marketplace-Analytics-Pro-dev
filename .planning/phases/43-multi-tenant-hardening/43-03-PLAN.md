---
phase: 43-multi-tenant-hardening
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - supabase/migrations/20260614123000_tenant04_onboarding_progress.sql
  - src/hooks/useOnboardingProgress.ts
  - src/components/onboarding/OnboardingBanner.tsx
  - src/components/onboarding/OnboardingWizard.tsx
  - src/pages/MercadoLivre.tsx
  - src/pages/AcceptInvite.tsx
autonomous: false
requirements: [TENANT-04]
must_haves:
  truths:
    - "Owner novo ve banner/CTA de onboarding nao-bloqueante no topo do dashboard quando ML nao esta conectado (D-07)"
    - "Wizard guia os passos Conectar ML -> Tiny (opcional) -> Custos -> Fiscal -> Pronto (D-08)"
    - "Progresso de onboarding persiste entre sessoes via tabela onboarding_progress escopada por organization_id (D-08)"
    - "Wizard auto-detecta passos ja feitos a partir do estado real (ml_tokens, ml_product_costs, ml_tax_config) e nao bloqueia a navegacao (D-07)"
    - "Wizard usa react-hook-form + shadcn sem novas dependencias (D-09)"
  artifacts:
    - path: "supabase/migrations/20260614123000_tenant04_onboarding_progress.sql"
      provides: "Tabela onboarding_progress (org-scoped) + RLS (member le, owner escreve)"
      contains: "onboarding_progress"
    - path: "src/hooks/useOnboardingProgress.ts"
      provides: "Read/write de onboarding_progress + auto-deteccao de passos pelo estado real"
      contains: "onboarding_progress"
    - path: "src/components/onboarding/OnboardingWizard.tsx"
      provides: "Wizard multi-step rhf + shadcn"
      contains: "react-hook-form"
    - path: "src/components/onboarding/OnboardingBanner.tsx"
      provides: "Banner/CTA nao-bloqueante no topo do dashboard"
      min_lines: 20
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "OnboardingBanner"
      via: "render quando !hasMLConnection ou onboarding incompleto"
      pattern: "OnboardingBanner"
    - from: "src/hooks/useOnboardingProgress.ts"
      to: "onboarding_progress"
      via: "supabase select/upsert por organization_id"
      pattern: "onboarding_progress"
---

<objective>
Construir o wizard de onboarding guiado nao-bloqueante (TENANT-04, D-07/D-08/D-09): tabela onboarding_progress org-scoped, hook de leitura/escrita com auto-deteccao de passos pelo estado real, banner/CTA no topo do dashboard e nos empty states, e o componente wizard (Conectar ML -> Tiny opcional -> Custos -> Fiscal -> Pronto) com react-hook-form + shadcn sem novas dependencias.

Purpose: Owner novo precisa chegar ao dashboard com dados reais sem passo manual de super-admin alem de criar org+convite. Sem onboarding guiado, o owner cai em "/" vazio sem saber o que fazer.
Output: 1 migration (onboarding_progress) + hook + 2 componentes + wiring no dashboard e no AcceptInvite.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-multi-tenant-hardening/43-CONTEXT.md
@.planning/phases/43-multi-tenant-hardening/43-RESEARCH.md
@src/pages/MercadoLivre.tsx
@src/pages/AcceptInvite.tsx
@src/contexts/OrganizationContext.tsx
</context>

<artifacts_this_phase_produces>
Simbolos criados/alterados neste plano:
- Migration 20260614123000_tenant04_onboarding_progress.sql (NOVO): tabela onboarding_progress (organization_id PK, current_step, completed_steps text[], completed_at, updated_at) + RLS ob_select (member) / ob_write (owner)
- src/hooks/useOnboardingProgress.ts (NOVO): hook com state, completeStep, currentStep, auto-deteccao
- src/components/onboarding/OnboardingBanner.tsx (NOVO): banner/CTA nao-bloqueante
- src/components/onboarding/OnboardingWizard.tsx (NOVO): wizard multi-step rhf + shadcn Progress/Dialog
- src/pages/MercadoLivre.tsx: render do OnboardingBanner quando onboarding incompleto
- src/pages/AcceptInvite.tsx: owner novo navega para wizard/dashboard com onboarding (em vez de "/" puro)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migration onboarding_progress + hook useOnboardingProgress com auto-deteccao</name>
  <read_first>
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Pattern 3 — SQL onboarding_progress + auto-deteccao por estado real)
    - .planning/phases/43-multi-tenant-hardening/43-CONTEXT.md (D-07/D-08/D-09)
    - src/contexts/OrganizationContext.tsx (currentOrg, get_org_role)
    - supabase/migrations/20260515133645_74fe9eff-0844-43a7-b4fc-7fef029b4916.sql (ml_tax_config — passo Fiscal)
    - src/hooks/useMLQueries.ts e o hook/contexto que expoe hasMLConnection (useMLStore) — passo Conectar ML
  </read_first>
  <action>
    A) Migration 20260614123000_tenant04_onboarding_progress.sql (D-08): criar tabela public.onboarding_progress com organization_id uuid PRIMARY KEY REFERENCES organizations(id) ON DELETE CASCADE, current_step text NOT NULL DEFAULT 'connect_ml' (valores: connect_ml|tiny|costs|fiscal|done), completed_steps text[] NOT NULL DEFAULT '{}', completed_at timestamptz, updated_at timestamptz NOT NULL DEFAULT now(). Habilitar RLS. Policy ob_select FOR SELECT TO authenticated USING is_org_member(auth.uid(), organization_id). Policy ob_write FOR ALL TO authenticated USING get_org_role(auth.uid(), organization_id) = 'owner' WITH CHECK get_org_role(...) = 'owner' (config de onboarding restrita a owner — CLAUDE.md). Idempotente (IF NOT EXISTS / DROP POLICY IF EXISTS). Atualizar src/integrations/supabase/types.ts manualmente com a nova tabela (projeto edita types a mao — RESEARCH Build artifacts).
    B) src/hooks/useOnboardingProgress.ts (D-08): hook que (1) le onboarding_progress por currentOrg.id; (2) AUTO-DETECTA o estado real dos passos — connect_ml feito se ML conectado (hasMLConnection/ml_tokens), tiny feito/skippable se ml_tokens.tiny_* presente (opcional), costs feito se existe pelo menos 1 ml_product_costs com cost>0 na org, fiscal feito se existe linha em ml_tax_config para a org/loja; (3) expoe currentStep, completedSteps (merge do persistido + auto-detectado), isComplete, e completeStep(step) que faz upsert em onboarding_progress (organization_id, current_step proximo, completed_steps append, updated_at). Usar @tanstack/react-query para refetch. Sem novas dependencias.
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE" supabase/migrations/20260614123000_tenant04_onboarding_progress.sql && grep -q "onboarding_progress" supabase/migrations/20260614123000_tenant04_onboarding_progress.sql && grep -q "is_org_member" supabase/migrations/20260614123000_tenant04_onboarding_progress.sql && grep -q "onboarding_progress" src/hooks/useOnboardingProgress.ts && grep -q "completeStep\|currentStep" src/hooks/useOnboardingProgress.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tabela onboarding_progress criada com PK organization_id e RLS ob_select(member)/ob_write(owner).
    - types.ts atualizado com onboarding_progress.
    - Hook auto-detecta passos pelo estado real (ml_tokens, ml_product_costs, ml_tax_config) alem de ler o persistido.
    - completeStep persiste current_step + completed_steps.
  </acceptance_criteria>
  <done>Persistencia e logica de progresso de onboarding prontas, org-scoped, com auto-deteccao.</done>
</task>

<task type="auto">
  <name>Task 2: OnboardingWizard + OnboardingBanner (rhf + shadcn, nao-bloqueante)</name>
  <read_first>
    - src/hooks/useOnboardingProgress.ts (da Task 1)
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Pattern 3; Don't Hand-Roll — usar rhf + shadcn Progress)
    - CLAUDE.md (tokens de design existentes; sem novas deps; D-09)
    - src/components/auth/RoleRoute.tsx (padrao de gating por role — wizard so para owner)
  </read_first>
  <action>
    A) src/components/onboarding/OnboardingWizard.tsx (D-08/D-09): componente multi-step usando react-hook-form para estado e shadcn (Progress, Card, Button, Dialog). Passos na ordem Conectar ML -> Tiny (opcional, com botao "Pular") -> Custos -> Fiscal -> Pronto. Cada passo mostra o que fazer e um CTA que leva a rota existente correspondente (Conectar ML -> /integracoes; Tiny -> /integracoes; Custos -> /precos-custos; Fiscal -> a tela/aba de regime fiscal existente). Marca o passo como completo via completeStep do hook quando o estado real detectar conclusao. NAO bloquear navegacao — o wizard e um Dialog/Card dispensavel; o owner pode fechar e navegar livre (D-07). Usar tokens de design existentes (kpi/sidebar/shadow-glow). Restrito a owner (get_org_role / RoleRoute pattern).
    B) src/components/onboarding/OnboardingBanner.tsx (D-07): banner compacto no topo do dashboard que aparece quando onboarding incompleto, mostrando shadcn Progress (X de 5 passos) + CTA "Continuar configuracao" que abre o OnboardingWizard. Dispensavel (botao fechar) e nao-bloqueante. Reusa shadcn + tokens, sem contrato de design formal (fase --skip-ui).
    Named exports (convencao do projeto). Sem novas dependencias.
  </action>
  <verify>
    <automated>grep -q "react-hook-form" src/components/onboarding/OnboardingWizard.tsx && grep -q "useOnboardingProgress" src/components/onboarding/OnboardingWizard.tsx && grep -q "Progress" src/components/onboarding/OnboardingBanner.tsx && npx tsc --noEmit 2>&1 | grep "error TS" | grep -cE "OnboardingWizard|OnboardingBanner" | awk '{ exit ($1==0)?0:1 }'</automated>
  </verify>
  <acceptance_criteria>
    - OnboardingWizard usa react-hook-form + shadcn; passos na ordem ML->Tiny(opcional)->Custos->Fiscal->Pronto; Tiny tem "Pular".
    - Cada passo leva a rota existente correta (/integracoes, /precos-custos, fiscal).
    - Wizard e banner sao dispensaveis e nao bloqueiam navegacao (D-07).
    - Nenhuma dependencia nova no package.json; tsc sem erro novo nesses arquivos.
  </acceptance_criteria>
  <done>Wizard e banner construidos, nao-bloqueantes, com rhf + shadcn.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring no dashboard e AcceptInvite + CTA nos empty states</name>
  <read_first>
    - src/pages/MercadoLivre.tsx (hasMLConnection via useMLStore linha 86; connected linha 120 — ponto de render do banner)
    - src/pages/AcceptInvite.tsx (navigate("/") na linha 91 — owner novo)
    - src/hooks/useOnboardingProgress.ts e os componentes da Task 2
  </read_first>
  <action>
    A) src/pages/MercadoLivre.tsx: renderizar OnboardingBanner no topo do dashboard quando o onboarding estiver incompleto (useOnboardingProgress().isComplete === false) OU quando !hasMLConnection. O banner nao substitui o conteudo — fica acima dele (nao-bloqueante, D-07). Onde ja existir empty state de "sem ML conectado", adicionar CTA "Comecar configuracao" que abre o wizard.
    B) src/pages/AcceptInvite.tsx: apos aceite, se o usuario for owner da org e o onboarding estiver incompleto, navegar para "/" (onde o OnboardingBanner ja aparece) — manter o redirect para "/" mas garantir que o banner/wizard apareca. Nao criar route-guard que trave (D-07). Se preferivel, abrir o wizard automaticamente na primeira visita ao dashboard quod onboarding incompleto e role owner.
    Verificar build limpo.
  </action>
  <verify>
    <automated>grep -q "OnboardingBanner" src/pages/MercadoLivre.tsx && npx tsc --noEmit 2>&1 | grep -c "error TS" | awk '{ exit ($1==0)?0:1 }' && (cd /root/garment-glow-test && npm run build >/dev/null 2>&1)</automated>
  </verify>
  <acceptance_criteria>
    - OnboardingBanner aparece no topo do dashboard quando onboarding incompleto/!hasMLConnection, sem bloquear o conteudo.
    - Empty state de "sem ML" tem CTA que abre o wizard.
    - AcceptInvite leva owner novo ao dashboard com onboarding visivel; sem route-guard bloqueante.
    - tsc --noEmit e npm run build limpos.
  </acceptance_criteria>
  <done>Wizard integrado ao fluxo: owner novo e guiado do convite ate dados reais sem passo manual de super-admin.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: [BLOCKING] Aplicar migration onboarding_progress via MCP + checkpoint visual (Wesley)</name>
  <read_first>
    - supabase/migrations/20260614123000_tenant04_onboarding_progress.sql
    - .planning/phases/43-multi-tenant-hardening/43-RESEARCH.md (Anti-Patterns: nunca db push)
  </read_first>
  <what-built>
    Tabela onboarding_progress + wizard/banner nao-bloqueante integrados ao dashboard e AcceptInvite. Migration ainda nao aplicada.
  </what-built>
  <action>
    Migration em producao exige aprovacao de Wesley (feedback_platform_actions). Apos aprovacao, aplicar 20260614123000_tenant04_onboarding_progress.sql via Supabase MCP apply_migration no ckcdevcxgvueywivefgx (NUNCA db push). Validar via execute_sql que a tabela existe e tem RLS ob_select/ob_write. Frontend ja deploya via push->Vercel (sem acao manual de deploy de EF aqui). Apresentar a Wesley para checkpoint visual: banner de onboarding no topo do dashboard mostrando progresso (X de 5), wizard abre com os 5 passos na ordem correta e CTAs levam as rotas certas, e a navegacao continua livre (nao-bloqueante).
  </action>
  <how-to-verify>
    1. Wesley aprova a migration.
    2. apply_migration aplicado; execute_sql confirma onboarding_progress + policies.
    3. Visual: banner aparece no topo de "/", wizard abre com 5 passos, CTAs corretos, navegacao nao bloqueada.
  </how-to-verify>
  <resume-signal>Wesley digita "aprovado" e confirma o visual do banner/wizard.</resume-signal>
  <acceptance_criteria>
    - Migration aplicada via MCP apply_migration no ckcdevcxgvueywivefgx.
    - onboarding_progress + RLS confirmados via execute_sql.
    - Wesley confirma visual nao-bloqueante do banner/wizard.
  </acceptance_criteria>
  <done>Onboarding em producao com aprovacao de Wesley.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuario authenticated -> onboarding_progress | leitura por membro, escrita so por owner |
| owner -> wizard (config de integracoes/custos/fiscal) | wizard so leva a rotas ja gated por role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-09 | Tampering / Elevation | nao-owner escrevendo onboarding_progress | mitigate | RLS ob_write restrita a get_org_role = 'owner' (D-08, CLAUDE.md) |
| T-43-10 | Information Disclosure | progresso de onboarding de outra org | mitigate | RLS ob_select via is_org_member; PK organization_id |
| T-43-11 | (UX/abuse) | route-guard bloqueante prendendo o owner | accept | Decisao D-07: onboarding e NAO-bloqueante por design; sem guard |
</threat_model>

<verification>
- onboarding_progress criada via MCP, org-scoped, RLS member-read/owner-write.
- Hook auto-detecta passos pelo estado real; completeStep persiste.
- Banner/wizard nao-bloqueantes no dashboard; CTAs para rotas existentes; Tiny opcional.
- Sem novas dependencias; tsc + build limpos.
</verification>

<success_criteria>
- TENANT-04: owner novo passa por wizard guiado (ML->Tiny opcional->Custos->Fiscal->Pronto) com progresso persistido entre sessoes e CTA no dashboard vazio.
</success_criteria>

<output>
Create `.planning/phases/43-multi-tenant-hardening/43-03-SUMMARY.md` when done
</output>
