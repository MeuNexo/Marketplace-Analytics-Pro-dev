---
phase: 72-quality-score-issues
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - src/components/mercadolivre/anuncios/useMLListingHealth.ts
  - src/components/mercadolivre/anuncios/ListingIssues.tsx
  - src/components/mercadolivre/anuncios/ListingIssues.test.tsx
  - src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx
autonomous: true
requirements: [ADM-72]
user_setup: []

must_haves:
  truths:
    - "Ao abrir o modal, a aba Indicadores invoca a EF ml-listing-health LAZY (so para o anuncio aberto)"
    - "Os issues sao exibidos em PT-BR como lista acionavel dentro do ListingIndicatorsTab"
    - "A aba mostra estados loading / vazio (anuncio saudavel) / indisponivel / erro de forma explicita"
    - "Se a EF falhar ou _ml_user_id for undefined, o ListingQualityScore existente (item.health) continua aparecendo sem quebrar"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/useMLListingHealth.ts"
      provides: "Hook on-demand (useState+useEffect) que invoca a EF e expoe status + HealthResult"
      min_lines: 30
    - path: "src/components/mercadolivre/anuncios/ListingIssues.tsx"
      provides: "Subcomponente que renderiza a lista de issues + estados loading/vazio/erro"
      min_lines: 30
    - path: "src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx"
      provides: "Aba Indicadores estendida com a secao de issues abaixo do quality score"
      contains: "useMLListingHealth"
  key_links:
    - from: "src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx"
      to: "src/components/mercadolivre/anuncios/useMLListingHealth.ts"
      via: "chama o hook com o item da prop"
      pattern: "useMLListingHealth\\("
    - from: "src/components/mercadolivre/anuncios/useMLListingHealth.ts"
      to: "supabase functions.invoke('ml-listing-health')"
      via: "fetch on-demand no useEffect com cleanup"
      pattern: "functions\\.invoke\\(\"ml-listing-health\"|functions\\.invoke\\('ml-listing-health'"
    - from: "src/components/mercadolivre/anuncios/ListingIssues.tsx"
      to: "HealthResult (do hook)"
      via: "recebe status + issues[] como props e renderiza"
      pattern: "issues"
---

<objective>
Estender a aba Indicadores (Phase 71) com a saude detalhada AO VIVO: um hook `useMLListingHealth` que invoca a EF `ml-listing-health` lazy quando o modal abre, e um subcomponente `ListingIssues` que renderiza a lista de problemas acionaveis em PT-BR com estados loading/vazio/indisponivel/erro. O `ListingQualityScore` existente (de `item.health`) permanece intocado e nunca quebra por causa do fetch.

Purpose: frontend da Fase B. Entrega ao usuario a lista de "o que melhorar neste anuncio" dentro do modal, sem regredir a aba da Phase 71.
Output: hook + subcomponente isolado + wiring no `ListingIndicatorsTab`, com testes do subcomponente.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-quality-score-issues/72-CONTEXT.md
@.planning/phases/72-quality-score-issues/72-RESEARCH.md

# Ponto de integracao e tipos
@src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx
@src/components/mercadolivre/anuncios/ListingQualityScore.tsx

# Shape do retorno da EF (deve casar com 72-01)
@.planning/phases/72-quality-score-issues/72-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook useMLListingHealth + tipos</name>
  <files>src/components/mercadolivre/anuncios/useMLListingHealth.ts</files>
  <read_first>
    - src/hooks/useMLReputation.ts — padrao de hook on-demand do projeto (useState+useEffect, `supabase.functions.invoke`, import `{ supabase } from "@/integrations/supabase/client"`). Espelhar.
    - src/contexts/MLInventoryContext.tsx — interface `ProductItem` (campos `id`, `health`, `_ml_user_id?: string` OPCIONAL).
    - 72-RESEARCH.md secao "Wiring no Front" (shape do hook, tabela de estados, landmines 1 e 3) + "Padrao de EF" (interface HealthResult/Issue — deve casar com 72-01).
  </read_first>
  <action>
    Criar `useMLListingHealth.ts` co-localizado em `anuncios/`. Definir e exportar os tipos `HealthResult` e `Issue` exatamente como o retorno da EF do 72-01: `HealthResult = { item_id, score: number|null, level: string|null, level_wording: string|null, issues: Issue[], source: "performance_api"|"health_api"|"unavailable" }`; `Issue = { id, category, title, action_label, progress, progress_max }`. Estes tipos sao consumidos pela Task 2.

    Hook `useMLListingHealth(item: ProductItem | null)`:
    - Estado: `status: 'idle'|'loading'|'success'|'error'|'unavailable'` (useState) e `data: HealthResult|null` (useState).
    - useEffect com deps `[item?.id, item?._ml_user_id]`:
      - GUARD (landmine 1): se `!item?.id || !item?._ml_user_id` -> `setStatus('idle'); setData(null); return;` (NAO invoca, NAO mostra erro — apenas nao renderiza a secao).
      - Senao: `setStatus('loading')`, invocar `supabase.functions.invoke("ml-listing-health", { body: { item_id: item.id, ml_user_id: item._ml_user_id } })`.
      - No `.then(({ data, error })`: se `cancelled` retornar; se `error` -> `setStatus('error')`; se `data.source === 'unavailable'` -> `setStatus('unavailable')`; senao `setData(data); setStatus('success')`.
      - CLEANUP (landmine 3): `let cancelled = false` no inicio do effect e `return () => { cancelled = true }` para evitar setState apos desmontar o modal.
    - Retornar `{ status, data }`.
    - NAO usar TanStack Query (fetch efemero de vida util igual a abertura do modal — consistente com useMLReputation).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` 0 erros.
    - O hook guarda contra `_ml_user_id` undefined retornando status 'idle' (nao 'error').
    - useEffect tem cleanup com flag `cancelled`.
    - Tipos `HealthResult`/`Issue` exportados e casam com o retorno do 72-01.
  </acceptance_criteria>
  <done>Hook on-demand pronto, type-checado, com guard de undefined e cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Subcomponente ListingIssues + wiring no ListingIndicatorsTab</name>
  <files>src/components/mercadolivre/anuncios/ListingIssues.tsx, src/components/mercadolivre/anuncios/ListingIssues.test.tsx, src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx — `rightCol` (md:col-span-3) com `ListingQualityScore` + KPIs + Informacoes. A secao de issues entra como um novo `Card` LOGO APOS o `ListingQualityScore`, na mesma coluna direita. NAO alterar a logica existente do `ListingQualityScore` nem do `marginDisplay`.
    - src/components/mercadolivre/anuncios/listingIndicators.test.ts — padrao de teste vitest do diretorio (estrutura describe/it).
    - src/components/ui/skeleton.tsx e src/components/ui/card.tsx — usar `Skeleton` para loading e `Card`/`CardContent` para a moldura (zero deps novas).
  </read_first>
  <behavior>
    - status 'idle': componente NAO renderiza nada (retorna null).
    - status 'loading': renderiza Skeleton(s) dentro do Card de issues.
    - status 'success' com issues.length > 0: renderiza a lista de issues (title + action_label PT-BR, agrupado/rotulado por category).
    - status 'success' com issues.length === 0: renderiza card "Nenhum problema encontrado" (anuncio saudavel).
    - status 'unavailable': inline "Dados de saude indisponiveis no momento".
    - status 'error': inline "Nao foi possivel carregar os problemas".
  </behavior>
  <action>
    TDD: escrever PRIMEIRO `ListingIssues.test.tsx` cobrindo os 6 casos do bloco <behavior> (render por status + presenca dos textos PT-BR), confirmar que falha (RED), depois implementar o componente (GREEN).

    Criar `ListingIssues.tsx` (named export `ListingIssues`): props `{ status, issues }` onde `status` = o `Status` do hook e `issues: Issue[]` (importar o tipo `Issue` de `./useMLListingHealth`). Renderizar conforme o bloco <behavior>, usando `Card`/`CardContent` (moldura igual aos outros cards da coluna), `Skeleton` para loading, e icone lucide-react (ex: `AlertCircle`/`CheckCircle2`) para os estados. Lista acionavel: cada issue mostra `title` (acao) e `action_label` (CTA curto) em PT-BR; agrupar/rotular por `category`. Sem novas dependencias.

    Wiring em `ListingIndicatorsTab.tsx`: importar `useMLListingHealth` e `ListingIssues`. Chamar `const { status, data } = useMLListingHealth(item);` no corpo do componente. Inserir `<ListingIssues status={status} issues={data?.issues ?? []} />` na `rightCol`, imediatamente apos `<ListingQualityScore health={item.health} />`. NAO substituir `item.health` pelo score ao vivo (decisao do RESEARCH Questao Aberta 1 — manter escopo simples). NAO remover nem alterar o `ListingQualityScore` existente.
  </action>
  <verify>
    <automated>npx vitest run src/components/mercadolivre/anuncios/ListingIssues.test.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Testes do `ListingIssues` cobrem os 6 estados e passam.
    - `tsc --noEmit` 0 erros e `npm run build` ok.
    - `ListingIndicatorsTab` chama `useMLListingHealth(item)` e renderiza `<ListingIssues>` apos `<ListingQualityScore>`; o `ListingQualityScore` continua recebendo `item.health` inalterado (sem regressao da Phase 71).
    - Nenhuma nova dependencia npm.
  </acceptance_criteria>
  <done>Lista de issues PT-BR renderizada no modal com todos os estados; aba da Phase 71 sem regressao; tsc/build/testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (modal) -> EF | O hook invoca `ml-listing-health` com o JWT da sessao; o item vem do cache org-scoped (`ml_inventory_cache`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05 | Tampering | useMLListingHealth (input) | mitigate | Guard `item?.id && item?._ml_user_id` antes de invocar; `_ml_user_id` undefined -> status 'idle' (nao envia body invalido). A autorizacao real (IDOR) e enforced server-side pela EF (72-01, T-72-01). |
| T-72-06 | Information Disclosure | UI de issues | accept | A UI so renderiza o que a EF retorna; a EF ja garante escopo org/seller. Nenhum dado cross-org alcanca o front. |
| T-72-SC | Tampering | npm installs | mitigate | Nenhum pacote novo (Skeleton/Card/lucide-react ja no projeto). |
</threat_model>

<verification>
- `npx vitest run src/components/mercadolivre/anuncios/ListingIssues.test.tsx` verde (6 estados).
- `npx tsc --noEmit` 0 erros; `npm run build` ok.
- Inspecao visual (apos 72-01 deployada): abrir o modal de um anuncio real -> aba Indicadores mostra issues PT-BR; abrir anuncio sem token/saude -> estado indisponivel sem quebrar o quality score.
</verification>

<success_criteria>
- Aba Indicadores invoca a EF lazy ao abrir o modal, com estados loading/erro/vazio (Success Criteria 2).
- Issues em PT-BR como lista acionavel dentro do ListingIndicatorsTab (Success Criteria 3).
- Quality score existente continua funcionando mesmo se a EF falhar; sem regressao da Phase 71 (Success Criteria 2 e 5).
- `tsc` 0 erros + build ok (Success Criteria 6).
</success_criteria>

<output>
Create `.planning/phases/72-quality-score-issues/72-02-SUMMARY.md` when done
</output>
