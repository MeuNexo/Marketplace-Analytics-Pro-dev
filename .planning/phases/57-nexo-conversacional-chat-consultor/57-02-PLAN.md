---
phase: 57-nexo-conversacional-chat-consultor
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - supabase/functions/nexo-chat/tools.ts
  - supabase/functions/nexo-chat/loop.ts
  - supabase/functions/nexo-chat/index.ts
  - supabase/functions/nexo-chat/tools.test.ts
  - supabase/functions/nexo-chat/loop.test.ts
autonomous: true
requirements: [NEXO-03, NEXO-07]
must_haves:
  truths:
    - "O Nexo puxa dados ao vivo via function-calling: o dispatcher mapeia cada tool a uma RPC/tabela real existente"
    - "TODA tool é escopada por organization_id do JWT (+ mlUserIds derivados server-side); args de org/seller vindos do modelo são IGNORADOS"
    - "O loop de tool-calling termina por resposta-texto, por cap de iterações (5) ou por timeout (~25s) — nunca roda indefinidamente"
    - "Cada functionResponse é limitado em linhas (≤50) para conter tokens/custo"
  artifacts:
    - path: "supabase/functions/nexo-chat/tools.ts"
      provides: "TOOL_DECLARATIONS (function declarations Gemini, sem param org/seller) + dispatchTool(sb, orgId, mlUserIds, name, args) escopado"
      exports: ["TOOL_DECLARATIONS", "dispatchTool"]
    - path: "supabase/functions/nexo-chat/loop.ts"
      provides: "runChat() — loop server-side: Gemini → functionCall → dispatch → functionResponse, com cap + timeout + append correto de contents"
      exports: ["runChat"]
    - path: "supabase/functions/nexo-chat/tools.test.ts"
      provides: "Unit: dispatcher injeta orgId/mlUserIds e ignora args.org_id/args.seller_id"
    - path: "supabase/functions/nexo-chat/loop.test.ts"
      provides: "Unit (fetch mockado): sequência functionCall→functionResponse→text; cap e timeout terminam o loop"
  key_links:
    - from: "supabase/functions/nexo-chat/index.ts"
      to: "supabase/functions/nexo-chat/loop.ts"
      via: "serve() chama runChat(sb, gkey, orgId, mlUserIds, systemPrompt, messages)"
      pattern: "runChat\\("
    - from: "supabase/functions/nexo-chat/loop.ts"
      to: "supabase/functions/nexo-chat/tools.ts"
      via: "runChat envia TOOL_DECLARATIONS e chama dispatchTool por functionCall"
      pattern: "dispatchTool\\("
    - from: "supabase/functions/nexo-chat/tools.ts"
      to: "RPCs reais (ckcdevcxgvueywivefgx)"
      via: "rpc('get_margin_with_ads_by_product'|'get_consultor_coverage'|...) com p_org_id/p_user_ids do servidor"
      pattern: "get_margin_with_ads_by_product|get_consultor_coverage"
  prohibitions:
    - "NUNCA respeitar args.org_id / args.seller_id / args.ml_user_id vindos do modelo — orgId vem só do JWT; mlUserIds só do servidor (ml_tokens WHERE organization_id)"
    - "NUNCA declarar org_id/seller_id como parâmetro de function declaration (o modelo não escolhe org)"
    - "NUNCA devolver linhas cruas sem cap (≤50 linhas/tool) nem selects sem .eq('organization_id', orgId) em insights/ml_ads_products_cache/ml_billing_monthly (service_role bypassa RLS)"
    - "NUNCA permitir loop sem cap=5 + timeout; ao estourar, última chamada sem tools (toolConfig NONE) ou fallback"
    - "NUNCA mutar o ML — tools são 100% read-only"
---

<objective>
Adicionar a camada de function-calling à EF `nexo-chat`: as function declarations (sem param de org), o dispatcher escopado por org (anti-IDOR), e o loop server-side de tool-calling com cap + timeout. Ao final, o Nexo consulta dados ao vivo da conta (margem/MCO/ads, estoque/cobertura, KPIs do dia, insights, DRE, cashflow, health) sob demanda, sempre escopado ao org do JWT.

Purpose: entregar NEXO-03 (function-calling read-only escopado por org, anti-IDOR) e NEXO-07 (guardrails de custo/latência: cap de tool-calls + timeout). A "inteligência de dados" já existe como RPCs determinísticas — esta fase é orquestração + escopo, não recomputo.

Output: `supabase/functions/nexo-chat/{tools.ts,loop.ts,tools.test.ts,loop.test.ts}` + index.ts passa a usar runChat().
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-nexo-conversacional-chat-consultor/57-CONTEXT.md
@.planning/phases/57-nexo-conversacional-chat-consultor/57-RESEARCH.md

# Skeleton do Plan 57-01 (esta EF) — runChat será chamado a partir daqui
@supabase/functions/nexo-chat/index.ts
# Anti-IDOR + resolução de mlUserIds via ml_tokens (padrão a copiar)
@supabase/functions/consultor-insights/index.ts
# Paginação .range() para ml_ads_products_cache (evita truncamento PostgREST 1000)
@supabase/functions/consultor-actions/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tools.ts — declarations (sem org param) + dispatchTool escopado (anti-IDOR)</name>
  <read_first>
    - 57-RESEARCH.md "Tool → RPC/Table Mapping (REAL names found in repo)" — nomes/assinaturas EXATOS: get_margin_with_ads_by_product(p_org_id,p_user_ids,p_from,p_to); get_margin_summary(idem); get_cost_waterfall(idem); get_consultor_coverage(p_org_id,p_from); get_consultor_paused_with_sales(p_org_id,p_from); get_consultor_no_cost_count(p_org_id); get_cashflow(p_org_id,p_start_date,p_end_date); get_treasury_panel(p_org_id,p_horizon); tabelas insights / ml_ads_products_cache / ml_billing_monthly / consultor_health_snapshots
    - 57-RESEARCH.md Pattern 2 (dispatchTool sketch) + Anti-IDOR Pattern (cadeia exata) + Anti-Patterns (cap de linhas, .eq org obrigatório)
    - consultor-insights/index.ts linhas ~774-784 (resolução de mlUserIds via ml_tokens)
  </read_first>
  <files>supabase/functions/nexo-chat/tools.ts, supabase/functions/nexo-chat/tools.test.ts</files>
  <behavior>
    - dispatchTool(sb, orgId, mlUserIds, name, args) ignora qualquer args.org_id/args.seller_id/args.ml_user_id e usa SEMPRE orgId/mlUserIds dos parâmetros (do servidor).
    - Para uma tool de margem, dispatchTool chama rpc('get_margin_with_ads_by_product') passando p_org_id=orgId e p_user_ids=mlUserIds (não os args do modelo).
    - args só influenciam datas (from/to), com clamp e defaults (to=hoje, from=30d atrás); data malformada → default.
    - Toda tool retorna no máximo 50 linhas.
    - Tool desconhecida retorna { error: "unknown_tool" } (não lança).
    - Selects diretos (insights/ml_ads_products_cache/ml_billing_monthly) incluem .eq('organization_id', orgId).
  </behavior>
  <action>
    Definir TOOL_DECLARATIONS: array de function declarations no formato Gemini (name, description em PT-BR orientando QUANDO usar, parameters object). Tools a declarar (mapeamento confirmado na pesquisa): get_margin_by_product, get_margin_summary, get_day_kpis, get_coverage, get_paused_with_sales, get_no_cost_count, get_active_insights, get_ads_by_product, get_dre_monthly, get_cashflow, get_treasury_panel, get_health_score. NENHUMA declara org_id/seller_id como parâmetro — só aceitam datas opcionais (from/to YYYY-MM-DD) onde a RPC suporta. Definir helpers clampDate(s)/today()/daysAgo(n). Definir dispatchTool(sb, orgId, mlUserIds, name, args): switch por name mapeando à RPC/tabela real conforme a tabela da pesquisa, SEMPRE injetando p_org_id=orgId e p_user_ids=mlUserIds (para as INVOKER) ou p_org_id=orgId (para as DEFINER), e .eq('organization_id', orgId) (+ .in('ml_user_id', mlUserIds) onde aplicável) para os selects diretos. ads (ml_ads_products_cache) deve paginar/limitar com .range()/.limit() (cache ~6000 linhas/30d). Cada branch faz .slice(0,50) (cap de linhas). default → { error:"unknown_tool" }. Escrever tools.test.ts (vitest) com um sb mockado (objeto com .rpc/.from encadeáveis que registram os argumentos): afirmar que ao chamar dispatchTool com args.org_id="ORG-ALHEIA" e args.seller_id="999", a RPC recebe p_org_id = orgId-do-servidor (não "ORG-ALHEIA"); afirmar que tool desconhecida retorna {error:"unknown_tool"}; afirmar que nenhuma TOOL_DECLARATIONS contém "org_id"/"seller_id" como property.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npm run test -- --run tools.test 2>&1 | tail -20</automated>
  </verify>
  <done>tools.test passa: dispatcher injeta orgId do servidor mesmo com args.org_id alheio; tool desconhecida não lança; declarations sem param de org/seller; tools mapeiam às RPCs/tabelas reais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: loop.ts — runChat() server-side com cap + timeout (NEXO-07)</name>
  <read_first>
    - 57-RESEARCH.md Pattern 1 (runChat sketch COMPLETO: contents append de model.functionCall e user.functionResponse; MAX_TOOL_ITERS=5; TURN_DEADLINE_MS=25000; thinkingBudget=-1)
    - 57-RESEARCH.md Pitfall 5 (runaway loop: ao estourar cap, última chamada com toolConfig mode NONE ou fallback)
    - supabase/functions/nexo-chat/tools.ts (TOOL_DECLARATIONS + dispatchTool — do Task 1)
  </read_first>
  <files>supabase/functions/nexo-chat/loop.ts, supabase/functions/nexo-chat/loop.test.ts</files>
  <behavior>
    - runChat com uma resposta Gemini contendo só texto retorna { reply: texto, usedTools:[], fallback:false } sem chamar dispatchTool.
    - runChat com functionCall na 1ª resposta e texto na 2ª: chama dispatchTool uma vez, faz o append correto (model.functionCall depois user.functionResponse) e retorna o texto final com usedTools contendo o nome da tool.
    - Ao atingir MAX_TOOL_ITERS (5) sem texto final, runChat termina com fallback:true (não roda além do cap).
    - Ao ultrapassar TURN_DEADLINE_MS, runChat retorna fallback:true sem nova chamada.
    - generationConfig usa thinkingBudget:-1 (nunca 0).
  </behavior>
  <action>
    Implementar runChat(sb, gkey, orgId, mlUserIds, systemPrompt, clientMessages) conforme o sketch da pesquisa, com fetch real ao Gemini (injetável/mockável: aceitar um fetchImpl opcional default globalThis.fetch para o teste). contents inicia como cópia de clientMessages. Loop até MAX_TOOL_ITERS=5: checar deadline (TURN_DEADLINE_MS=25000) no topo → se estourou, retornar fallback. POST generateContent com system_instruction, contents, tools:[{functionDeclarations:TOOL_DECLARATIONS}], toolConfig.functionCallingConfig.mode="AUTO", generationConfig{temperature:0.3,maxOutputTokens:1200,thinkingConfig:{thinkingBudget:-1}}. Se !res.ok → fallback. Parsear parts; se não há functionCall → retornar o texto join (resposta final). Se há functionCall: push contents {role:"model",parts:[{functionCall}...]}; para cada fc, usedTools.push(fc.name) e result=await dispatchTool(sb,orgId,mlUserIds,fc.name,fc.args??{}); montar responseParts {functionResponse:{name,response:{content:result}}}; push contents {role:"user",parts:responseParts}; próxima iteração. Ao sair do for (cap estourado) → retornar fallback com mensagem pedindo pergunta mais específica. Escrever loop.test.ts (vitest) com fetchImpl mockado que retorna sequências controladas e um dispatchTool mockado (ou tools.ts com sb mockado): (a) só-texto → 1 chamada, sem dispatch; (b) functionCall→text → dispatch chamado 1x, usedTools=[nome], append correto; (c) sempre-functionCall → para no cap=5 com fallback; (d) deadline forçado (TURN_DEADLINE_MS pequeno via injeção ou Date.now mock) → fallback sem nova chamada.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npm run test -- --run loop.test 2>&1 | tail -20</automated>
  </verify>
  <done>loop.test passa: termina por texto, por cap=5 e por timeout; append de contents correto; thinkingBudget -1.</done>
</task>

<task type="auto">
  <name>Task 3: index.ts — resolver mlUserIds server-side e chamar runChat()</name>
  <read_first>
    - supabase/functions/nexo-chat/index.ts (skeleton do Plan 57-01 — a chamada única do Gemini será SUBSTITUÍDA por runChat)
    - consultor-insights/index.ts ~774-784 (query ml_tokens: select ml_user_id WHERE organization_id, refresh_token not null)
    - supabase/functions/nexo-chat/loop.ts (assinatura de runChat — do Task 2)
  </read_first>
  <files>supabase/functions/nexo-chat/index.ts</files>
  <action>
    Após o kill-switch e a leitura da GEMINI_API_KEY, resolver mlUserIds server-side: select ml_user_id from ml_tokens .eq('organization_id', orgId) .not('refresh_token','is',null) → mlUserIds = rows.map(r=>r.ml_user_id) (NEXO-03: derivado do servidor, nunca do modelo). Importar buildSystemPrompt de ./prompt.ts e runChat de ./loop.ts. Substituir a chamada Gemini única (do 57-01) por: const { reply, usedTools, fallback } = await runChat(sb, gkey, orgId, mlUserIds, buildSystemPrompt(), messages). Retornar j({ reply, used_tools: usedTools, fallback }). Manter a cadeia de segurança intacta. Logar (opcional) usedTools.length e fallback para observabilidade — nunca o conteúdo das mensagens.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && grep -q "runChat(" supabase/functions/nexo-chat/index.ts && grep -q "ml_tokens" supabase/functions/nexo-chat/index.ts && grep -q "refresh_token" supabase/functions/nexo-chat/index.ts && echo PASS</automated>
  </verify>
  <done>index.ts resolve mlUserIds de ml_tokens (escopado por org) e delega ao runChat; resposta inclui used_tools + fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modelo → tool args | o Gemini preenche args livremente (pode alucinar/ser injetado) |
| EF → RPCs/tabelas (service_role) | bypassa RLS; escopo depende do filtro org explícito |
| tool-result → próximo prompt | dados da conta voltam ao modelo como functionResponse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-07 | Elevation/Info (IDOR) | args.org_id/seller_id do modelo | mitigate | dispatchTool ignora args de org/seller; usa orgId do JWT + mlUserIds do servidor; declarations sem param de org (tools.test prova) |
| T-57-08 | Info disclosure (cross-org leak) | selects via service_role | mitigate | .eq('organization_id', orgId) (+ .in('ml_user_id', mlUserIds)) obrigatório em insights/ml_ads_products_cache/ml_billing_monthly; RPCs com WHERE org |
| T-57-09 | DoS (custo/latência) | loop de tool-calling | mitigate | cap MAX_TOOL_ITERS=5 + TURN_DEADLINE_MS=25000 + ≤50 linhas/tool (NEXO-07); loop.test prova término |
| T-57-10 | Tampering (prompt injection via tool-result) | título de SKU/insight no functionResponse | mitigate | system prompt trata dados como informação (Plan 57-01); truncar texto livre (títulos) no dispatcher; read-only |
| T-57-11 | Tampering (número alucinado) | resposta final | mitigate | grounding: o modelo é forçado a chamar a tool antes de afirmar número (NEXO-05, regra do prompt 57-01) |
| T-57-12 | Tampering (mutação ML) | tools | mitigate | tools 100% read-only (só rpc/select); nenhuma mutação; ação concreta → pipeline Phase 54 |
| T-57-SC | Tampering | deno installs | accept | zero deps novas (RESEARCH audit) |
</threat_model>

<verification>
- `npm run test -- --run tools.test` e `--run loop.test` verdes.
- `npm run test` (suite completa) + `npm run build` verdes no merge da wave.
- grep asserts da Task 3 (runChat + ml_tokens + refresh_token) passam.
- EF não deployada aqui (deploy = checkpoint Plan 57-04).
</verification>

<success_criteria>
- Function-calling read-only operante: dispatcher mapeia 12 tools às RPCs/tabelas reais, escopado por org (NEXO-03).
- Anti-IDOR provado por tools.test: args.org_id alheio é ignorado; sem param de org nas declarations.
- Guardrails provados por loop.test: cap=5 + timeout + cap de linhas (NEXO-07).
- mlUserIds derivado server-side de ml_tokens; chat permanece read-only.
</success_criteria>

<output>
Create `.planning/phases/57-nexo-conversacional-chat-consultor/57-02-SUMMARY.md` when done
</output>