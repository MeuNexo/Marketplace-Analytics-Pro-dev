---
phase: 57-nexo-conversacional-chat-consultor
plan: 03
type: execute
wave: 3
depends_on: ["57-01"]
files_modified:
  - src/hooks/useNexoChat.ts
  - src/components/nexo/NexoChatPanel.tsx
  - src/components/nexo/NexoChatFab.tsx
  - src/components/layout/LayoutShell.tsx
  - src/hooks/useNexoChat.test.tsx
  - src/components/nexo/NexoChatFab.test.tsx
autonomous: true
requirements: [NEXO-01, NEXO-04]
must_haves:
  truths:
    - "Um botão flutuante 'Nexo' aparece em todas as telas autenticadas (montado no LayoutShell), abrindo um painel de chat"
    - "O FAB só renderiza quando há ML conectado (hasMLConnection) e o kill-switch está ligado"
    - "A conversa é efêmera: o histórico vive no estado React e é reenviado a cada turno; nenhuma persistência"
    - "O texto da resposta é renderizado como parágrafos (split por \\n), sem markdown renderer e sem dangerouslySetInnerHTML"
  artifacts:
    - path: "src/hooks/useNexoChat.ts"
      provides: "Estado efêmero de mensagens + mutation que invoca a EF nexo-chat com {org_id, messages}"
      exports: ["useNexoChat"]
    - path: "src/components/nexo/NexoChatPanel.tsx"
      provides: "Sheet com lista de mensagens (ScrollArea) + input + estados enviando/erro"
      exports: ["NexoChatPanel"]
    - path: "src/components/nexo/NexoChatFab.tsx"
      provides: "FAB fixed bottom-right; gate por hasMLConnection + kill-switch; abre o painel"
      exports: ["NexoChatFab"]
    - path: "src/components/layout/LayoutShell.tsx"
      provides: "Monta <NexoChatFab/> fora do <main>, presente em todas as telas"
      contains: "NexoChatFab"
  key_links:
    - from: "src/components/layout/LayoutShell.tsx"
      to: "src/components/nexo/NexoChatFab.tsx"
      via: "render <NexoChatFab/> dentro do shell, fora do <main>"
      pattern: "NexoChatFab"
    - from: "src/components/nexo/NexoChatPanel.tsx"
      to: "src/hooks/useNexoChat.ts"
      via: "usa useNexoChat() para messages/send/loading/error"
      pattern: "useNexoChat\\("
    - from: "src/hooks/useNexoChat.ts"
      to: "EF nexo-chat"
      via: "supabase.functions.invoke('nexo-chat', { body: { org_id, messages } })"
      pattern: "functions.invoke\\(\"nexo-chat\""
  prohibitions:
    - "NUNCA persistir o histórico (sem tabela, sem localStorage de mensagens) — efêmero, client-held (NEXO-04)"
    - "NUNCA renderizar a resposta via markdown renderer ou dangerouslySetInnerHTML — split por \\n em <p> (anti-XSS, postura da 53)"
    - "NUNCA renderizar o FAB sem ML conectado nem com kill-switch desligado (NEXO-01/NEXO-06)"
    - "NUNCA disparar mutação ML pelo chat — o painel é só conversa read-only; ação concreta encaminha para o fluxo de aprovação (Phase 54)"
---

<objective>
Construir o painel de chat flutuante "Nexo" e o hook de estado efêmero. O FAB vive no LayoutShell (todas as telas), só aparece com ML conectado + kill-switch ligado, e abre um Sheet com a conversa. O hook mantém o histórico em memória e o reenvia inteiro a cada turno para a EF nexo-chat.

Purpose: entregar NEXO-01 (painel flutuante multi-turno acessível de qualquer tela, só com ML conectado) e NEXO-04 (conversa efêmera, histórico no cliente reenviado a cada turno, sem tabela). Reusa o padrão de invoke do useConsultorInsights e o render de texto do ConsultorLLMSummary (anti-XSS).

Output: `src/hooks/useNexoChat.ts`, `src/components/nexo/{NexoChatFab,NexoChatPanel}.tsx`, mount no LayoutShell, + testes.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-nexo-conversacional-chat-consultor/57-CONTEXT.md
@.planning/phases/57-nexo-conversacional-chat-consultor/57-RESEARCH.md

# Padrão de invoke de EF com org_id (copiar a forma)
@src/hooks/useConsultorInsights.ts
# Render de texto LLM como parágrafos, sem markdown (anti-XSS)
@src/components/mercadolivre/ConsultorLLMSummary.tsx
# Ponto de montagem do FAB (fora do <main>) + sinal de ML conectado
@src/components/layout/LayoutShell.tsx
# Sinal hasMLConnection (gate do FAB)
@src/contexts/MLStoreContext.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useNexoChat.ts — estado efêmero + invoke da EF (NEXO-04)</name>
  <read_first>
    - src/hooks/useConsultorInsights.ts (useMutation + supabase.functions.invoke com org_id; useOrganization().currentOrg.id)
    - 57-RESEARCH.md "Frontend: Floating Chat Panel" (formato messages Gemini contents; estado efêmero; sem persistência)
  </read_first>
  <files>src/hooks/useNexoChat.ts, src/hooks/useNexoChat.test.tsx</files>
  <behavior>
    - useNexoChat expõe messages (ChatMsg[]), send(text), loading e o último erro.
    - send(text) faz push da mensagem do usuário no formato { role:'user', parts:[{text}] }, invoca nexo-chat com o array INTEIRO de messages, e ao retornar faz push da reply como { role:'model', parts:[{text: reply}] }.
    - O array enviado à EF é exatamente o histórico acumulado (reenviado a cada turno) — prova de NEXO-04.
    - Não há leitura/escrita de localStorage de mensagens nem de tabela.
    - Em erro, loading volta a false e o erro fica disponível para o componente disparar toast.
  </behavior>
  <action>
    Criar ChatMsg type ({ role:'user'|'model'; parts:{text:string}[] }). useNexoChat: useState<ChatMsg[]> para o histórico; useOrganization() para currentOrg.id; useMutation cujo mutationFn recebe o texto, monta nextMessages=[...messages,{role:'user',parts:[{text}]}], setMessages(nextMessages), invoca supabase.functions.invoke("nexo-chat",{ body:{ org_id: currentOrg.id, messages: nextMessages } }); ao sucesso, se data.disabled retornar sem append (kill-switch); senão setMessages(prev=>[...prev,{role:'model',parts:[{text: data.reply ?? ""}]}]). Expor send=(text)=>mutateAsync(text), messages, loading=isPending, error. Sem persistência. Escrever useNexoChat.test.tsx (vitest + @testing-library/react renderHook, mockando supabase.functions.invoke): afirmar que após send("oi") o body do invoke contém messages com a msg do user; após resolver com {reply:"olá"}, messages tem 2 itens (user+model); um segundo send reenvia o histórico acumulado (array de 3 → invoke recebe 3); nenhum acesso a localStorage de mensagens.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npm run test -- --run useNexoChat.test 2>&1 | tail -20</automated>
  </verify>
  <done>useNexoChat.test passa: histórico efêmero acumulado e reenviado a cada turno; sem persistência; kill-switch (disabled) não faz append.</done>
</task>

<task type="auto">
  <name>Task 2: NexoChatPanel.tsx — Sheet + mensagens + input (render anti-XSS)</name>
  <read_first>
    - src/components/mercadolivre/ConsultorLLMSummary.tsx (split por \n em <p>, sem markdown; Skeleton; toast sonner em erro)
    - src/components/ui/sheet.tsx e src/components/ui/scroll-area.tsx (primitivos disponíveis)
    - src/hooks/useNexoChat.ts (do Task 1)
  </read_first>
  <files>src/components/nexo/NexoChatPanel.tsx</files>
  <action>
    Criar NexoChatPanel({ open, onOpenChange }): Sheet side="right" com largura responsiva. Header com ícone (lucide Sparkles ou MessageCircle) e título "Nexo". Corpo: ScrollArea listando messages — bolhas distintas para role user vs model; o texto de cada mensagem renderizado como parágrafos (split por \n → <p>, igual ConsultorLLMSummary; SEM markdown renderer, SEM dangerouslySetInnerHTML). Indicador "digitando"/spinner quando loading. Footer: input (textarea/Input) + botão Enviar; Enter envia; ao enviar, chamar send(text) do useNexoChat e limpar o input; em erro (catch do mutateAsync) disparar toast.error (sonner). Estado vazio: mensagem de boas-vindas curta apresentando o Nexo. Não renderizar nenhum botão que dispare ação no ML (read-only). Usar tokens de design existentes (sem cores hardcoded fora do padrão).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && grep -q "useNexoChat" src/components/nexo/NexoChatPanel.tsx && grep -q "split" src/components/nexo/NexoChatPanel.tsx && ! grep -q "dangerouslySetInnerHTML" src/components/nexo/NexoChatPanel.tsx && echo PASS</automated>
  </verify>
  <done>NexoChatPanel usa useNexoChat, renderiza parágrafos via split (sem dangerouslySetInnerHTML), tem input/enviar e estados loading/erro; nenhum botão de mutação ML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NexoChatFab.tsx (gate ML+kill-switch) + mount no LayoutShell (NEXO-01)</name>
  <read_first>
    - src/contexts/MLStoreContext.tsx (useMLStore().hasMLConnection — sinal de ML conectado)
    - src/hooks/useConsultorInsights.ts (como ler consultor_config/kill-switch no cliente — summaryDisabled vem da EF; aqui ler llm_enabled proativamente para esconder o FAB)
    - src/components/layout/LayoutShell.tsx (estrutura: render fora do <main>; já está dentro de MLStoreProvider via ApiLayout)
  </read_first>
  <files>src/components/nexo/NexoChatFab.tsx, src/components/layout/LayoutShell.tsx, src/components/nexo/NexoChatFab.test.tsx</files>
  <behavior>
    - NexoChatFab NÃO renderiza nada quando hasMLConnection é false.
    - NexoChatFab NÃO renderiza nada quando o kill-switch (consultor_config.llm_enabled) está false.
    - Quando ML conectado e kill-switch ligado, renderiza um botão fixed bottom-right que abre o NexoChatPanel.
  </behavior>
  <action>
    Criar NexoChatFab: usar useMLStore() para hasMLConnection; ler o kill-switch (query a consultor_config.llm_enabled por currentOrg.id — reusar o padrão de query do projeto; tratar ausência de linha como habilitado). Se !hasMLConnection || llmEnabled===false → return null (NEXO-01 "só com ML conectado" + NEXO-06). Caso contrário, renderizar um Button fixed bottom-right (z alto, ícone Sparkles/MessageCircle, aria-label "Abrir Nexo") que faz setOpen(true), e o <NexoChatPanel open={open} onOpenChange={setOpen}/>. Montar <NexoChatFab/> em LayoutShell.tsx dentro do container do shell, FORA do <main> (irmão do bloco de conteúdo), para aparecer em todas as telas. Escrever NexoChatFab.test.tsx (vitest + testing-library, mockando useMLStore e a query de kill-switch): (a) hasMLConnection=false → componente não renderiza o botão; (b) hasMLConnection=true + llm_enabled=false → não renderiza; (c) hasMLConnection=true + habilitado → renderiza o botão.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && grep -q "NexoChatFab" src/components/layout/LayoutShell.tsx && grep -q "hasMLConnection" src/components/nexo/NexoChatFab.tsx && npm run test -- --run NexoChatFab.test 2>&1 | tail -15</automated>
  </verify>
  <done>NexoChatFab.test passa os 3 casos de gate; FAB montado no LayoutShell (todas as telas); build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF reply → DOM | texto gerado por LLM é renderizado no painel |
| client state → EF | histórico efêmero do cliente é reenviado a cada turno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-13 | Tampering (XSS) | render da reply do LLM | mitigate | split por \n em <p> (React escapa); sem markdown renderer; sem dangerouslySetInnerHTML (postura da 53) |
| T-57-14 | Info disclosure | FAB visível sem ML/contexto | mitigate | gate hasMLConnection + kill-switch → null; NexoChatFab.test prova os 3 casos |
| T-57-15 | Tampering (mutação ML pelo chat) | painel | mitigate | painel é só conversa; nenhum botão de ação ML; ação concreta encaminha à fila da Phase 54 |
| T-57-16 | Info disclosure | persistência indevida do histórico | mitigate | efêmero, em memória; sem localStorage de mensagens, sem tabela (NEXO-04) |
| T-57-SC | Tampering | npm installs | accept | zero deps novas (RESEARCH audit) |
</threat_model>

<verification>
- `npm run test -- --run useNexoChat.test` e `--run NexoChatFab.test` verdes.
- `npm run build` (tsc) + `npm run lint` verdes no merge da wave.
- grep asserts (mount no LayoutShell; sem dangerouslySetInnerHTML; gate hasMLConnection) passam.
</verification>

<success_criteria>
- FAB "Nexo" presente em todas as telas (montado no LayoutShell), só com ML conectado + kill-switch ligado (NEXO-01/NEXO-06).
- Painel multi-turno funcional; render anti-XSS (parágrafos).
- Conversa efêmera reenviada a cada turno; sem persistência (NEXO-04, provado por useNexoChat.test).
- Read-only: nenhum botão de mutação ML no painel.
</success_criteria>

<output>
Create `.planning/phases/57-nexo-conversacional-chat-consultor/57-03-SUMMARY.md` when done
</output>