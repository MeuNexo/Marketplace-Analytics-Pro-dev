---
phase: 57-nexo-conversacional-chat-consultor
plan: 04
type: execute
wave: 3
depends_on: ["57-01", "57-02", "57-03"]
files_modified: []
autonomous: false
requirements: [NEXO-01, NEXO-02, NEXO-03, NEXO-04, NEXO-05, NEXO-06, NEXO-07]
user_setup:
  - service: supabase-edge-function-deploy
    why: "gsd-executor não tem SUPABASE_ACCESS_TOKEN — o deploy da EF nexo-chat é feito pelo orquestrador/humano (padrão das Phases 43/48)"
    dashboard_config:
      - task: "Deploy da EF nexo-chat em ckcdevcxgvueywivefgx (supabase functions deploy nexo-chat) e confirmar verify_jwt=true"
        location: "Supabase CLI / orquestrador"
      - task: "BLOQUEANTE: select get_app_secret('GEMINI_API_KEY') deve retornar não-nulo. get_app_secret NÃO está em migration no repo (existe em prod via consultor-llm) — se retornar nulo/erro, criar a função/segredo ANTES de deployar; não assumir que existe. Confirmar também consultor_config.llm_model = gemini-2.5-pro ou nulo (fallback)"
        location: "Supabase vault / consultor_config (ckcdevcxgvueywivefgx)"
must_haves:
  truths:
    - "A EF nexo-chat está deployada em prod (ckcdevcxgvueywivefgx) com verify_jwt=true"
    - "Wesley valida o painel Nexo em preview/prod: aparece em todas as telas, responde grounded citando playbook, respeita anti-IDOR, kill-switch e read-only"
  artifacts: []
  key_links: []
  prohibitions:
    - "NUNCA aprovar este checkpoint automaticamente — gate=blocking, validação humana de Wesley é obrigatória"
    - "NUNCA marcar a fase concluída sem o deploy da EF confirmado pelo orquestrador"
    - "NUNCA deployar a EF se get_app_secret('GEMINI_API_KEY') retornar nulo/erro — get_app_secret não está em migration no repo; criar a função/segredo primeiro (não assumir que existe)"
---

<objective>
Fechar a Phase 57: o orquestrador faz o deploy da EF `nexo-chat` (o gsd-executor não tem credencial de deploy) e Wesley valida visual e comportamentalmente o Nexo Conversacional no preview/prod. Verifica todos os NEXO-01..07 ponta-a-ponta.

Purpose: o deploy de EF é bloqueante para o executor (lição recorrente das Phases 43/48). A validação humana confirma a barra "especialista de verdade", o grounding (cita playbook, não inventa número), e as invariantes de segurança (anti-IDOR, read-only, kill-switch) que testes unitários não cobrem ponta-a-ponta.

Output: confirmação de deploy + aprovação de Wesley (ou lista de ajustes).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-nexo-conversacional-chat-consultor/57-CONTEXT.md
@.planning/phases/57-nexo-conversacional-chat-consultor/57-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy da EF nexo-chat (orquestrador/humano) + smoke</name>
  <action>Deploy da EF nexo-chat em ckcdevcxgvueywivefgx pelo orquestrador/humano (executor sem SUPABASE_ACCESS_TOKEN) + smoke 200 com reply. Detalhes na verificação abaixo.</action>
  <what-built>EF nexo-chat (skeleton + tools + loop) e o painel de chat flutuante no frontend, prontos no repo. Falta deployar a EF — ação que exige SUPABASE_ACCESS_TOKEN, indisponível ao executor.</what-built>
  <how-to-verify>
    Orquestrador/humano executa o deploy da EF e confirma os pré-requisitos:
    1. PRÉ-REQUISITO BLOQUEANTE (vault): `select get_app_secret('GEMINI_API_KEY')` em ckcdevcxgvueywivefgx (via service_role) DEVE retornar não-nulo. Atenção (WARNING do plan-checker): a função get_app_secret existe em prod (a EF consultor-llm já a usa) mas NÃO está versionada em migration no repo — NÃO assumir que existe só porque consultor-llm a usa. Se o select retornar NULO ou ERRO (função inexistente / segredo ausente), o deploy está BLOQUEADO até criar a função/segredo (criar migration de get_app_secret e/ou inserir GEMINI_API_KEY no vault). Não prosseguir com deploy/smoke enquanto este passo falhar.
    2. `supabase functions deploy nexo-chat --project-ref ckcdevcxgvueywivefgx` (verify_jwt=true conforme config.toml).
    3. Confirmar consultor_config.llm_model: nulo (fallback gemini-2.5-pro no código) ou explicitamente 'gemini-2.5-pro' para a org de teste.
    4. Smoke da EF: 1 pergunta real por org de teste retorna 200 com `reply` não-vazio e `used_tools` populado quando a pergunta exige dado (ex: "qual produto está comendo mais margem com ads?").
  </how-to-verify>
  <resume-signal>Confirme "EF deployada + smoke OK" ou descreva o erro (status Gemini, thinking 400, key ausente)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validação visual e comportamental de Wesley (NEXO-01..07)</name>
  <action>Wesley valida ponta-a-ponta no preview/prod o painel Nexo contra os 7 itens do checklist abaixo. Aprovação humana obrigatória (não auto-aprovável).</action>
  <what-built>Nexo Conversacional completo: FAB flutuante em todas as telas + chat multi-turno grounded em dados reais da conta (Gemini 2.5 Pro + function-calling read-only escopado por org) + persona COO com todos os playbooks.</what-built>
  <how-to-verify>
    Wesley valida no preview Vercel (ou prod) — checklist ponta-a-ponta dos NEXO-01..07:
    1. NEXO-01: o botão "Nexo" aparece no canto em VÁRIAS telas (/vendas, /anuncios, /estoque, /financeiro); abre/fecha o painel; conversa multi-turno (faça 2-3 perguntas encadeadas, o contexto se mantém).
    2. NEXO-01/06: numa org/usuário SEM ML conectado, o FAB NÃO aparece; com o kill-switch desligado (consultor_config.llm_enabled=false), o FAB some.
    3. NEXO-02: faça uma pergunta estratégica (ex: "como está minha saúde de ads e o que priorizar?") — a resposta é de nível especialista, cruza domínios (ads × margem × estoque) e cita `[playbook: ...]` quando aplicável.
    4. NEXO-03/05: pergunte por números concretos (ex: "qual minha margem média e o produto que mais perde dinheiro?") — os valores batem com os dados reais da conta (cruzar com /anuncios ou /financeiro); o Nexo não inventa número (se não tem o dado, chama a tool ou diz que não sabe).
    5. NEXO-03 (anti-IDOR): com um usuário de OUTRA org, o Nexo só fala dos dados DAQUELA org — nunca vaza SKUs/números de outra loja. (Se houver 2 orgs de teste, trocar de org e confirmar isolamento.)
    6. NEXO-07 (read-only): peça "pause esse anúncio" / "baixe o preço" — o Nexo SUGERE e encaminha para o fluxo de aprovação (Phase 54), NUNCA executa a mudança no ML.
    7. Latência/custo: as respostas chegam em tempo razoável (cap de tool-calls + timeout funcionando; sem loop infinito).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou liste os ajustes (ex: persona rasa, número errado, FAB some indevido, playbook não citado, latência alta)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → prod | EF com segredo do Gemini e acesso service_role entra em produção |
| humano → validação | aprovação manual é o gate final das invariantes de segurança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-17 | Elevation/Info | deploy com verify_jwt errado | mitigate | checkpoint confirma verify_jwt=true antes de liberar |
| T-57-18 | Info disclosure (cross-org) | validação E2E | mitigate | Wesley testa isolamento entre orgs (item 5); anti-IDOR provado também por tools.test (57-02) |
| T-57-19 | Tampering (número/persona) | qualidade do agente | mitigate | Wesley cruza números com o dashboard (item 4) e avalia profundidade (item 3) |
| T-57-20 | Tampering (mutação ML) | read-only | mitigate | Wesley testa pedido de ação (item 6) — deve só sugerir/encaminhar |
| T-57-SC | Tampering | deploy/installs | accept | zero deps novas; deploy de EF versionada no repo |
</threat_model>

<verification>
- Deploy da EF nexo-chat confirmado em ckcdevcxgvueywivefgx (verify_jwt=true) + smoke 200 com reply.
- Aprovação de Wesley nos 7 itens do checklist NEXO-01..07.
- Este plano é non-autonomous (autonomous:false) — ambos os checkpoints são blocking e exigem ação/aprovação humana.
</verification>

<success_criteria>
- EF nexo-chat em prod, respondendo grounded e citando playbook.
- NEXO-01..07 validados ponta-a-ponta por Wesley (FAB em todas as telas, persona especialista, grounding numérico, anti-IDOR, kill-switch, read-only, guardrails de custo).
</success_criteria>

<output>
Create `.planning/phases/57-nexo-conversacional-chat-consultor/57-04-SUMMARY.md` when done
</output>
