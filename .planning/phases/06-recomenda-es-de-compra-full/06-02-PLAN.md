---
phase: 06-recomenda-es-de-compra-full
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx
  - src/components/mercadolivre/analise/AnaliseDashboard.tsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
must_haves:
  truths:
    - "Usuário vê, abaixo da AnalisePrecosTable, um Card 'Recomendações de Compra' com uma linha por snapshot"
    - "Usuário informa por produto: dias de cobertura, estoque total, estoque FULL e estoque casa/CD (COMP-01)"
    - "Usuário seleciona o multiplicador (×1,0 / ×1,2 / ×1,5 / ×2,0) no cabeçalho do Card; cálculo atualiza em tempo real (COMP-02)"
    - "Para cada produto, o painel mostra Compra recomendada e Sugestão FULL calculadas via calcularCompra (COMP-03/COMP-04)"
    - "Quando snapshot.strategy === null, a linha exibe nota '(usando GMV)'"
  artifacts:
    - path: "src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx"
      provides: "Painel inline com inputs por produto e saídas calculadas"
    - path: "src/components/mercadolivre/analise/AnaliseDashboard.tsx"
      provides: "Renderização do CompraRecomendadaPanel abaixo da AnalisePrecosTable"
      contains: "CompraRecomendadaPanel"
  key_links:
    - from: "src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx"
      to: "src/lib/analysis/compraUtils.ts"
      via: "calcularCompra dentro de useMemo"
      pattern: "calcularCompra\\("
    - from: "src/components/mercadolivre/analise/AnaliseDashboard.tsx"
      to: "src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx"
      via: "import + render dentro do branch snapshots.length > 0"
      pattern: "CompraRecomendadaPanel"
---

<objective>
Construir a UI do painel "Recomendações de Compra" como um Card inline dentro do AnaliseDashboard, consumindo as funções puras criadas no plano 06-01.

Purpose: Entregar o fluxo end-to-end pedido pela fase — usuário informa estoques + escolhe multiplicador e vê em tempo real a compra recomendada e a sugestão FULL por produto, sem nenhuma mudança de roteamento, hook de dados ou tabela Supabase (conforme RESEARCH.md).

Output: Componente `CompraRecomendadaPanel.tsx` novo + edição cirúrgica em `AnaliseDashboard.tsx` para renderizá-lo abaixo de `<AnalisePrecosTable>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-recomenda-es-de-compra-full/06-RESEARCH.md
@.planning/phases/06-recomenda-es-de-compra-full/06-01-SUMMARY.md

@src/components/mercadolivre/analise/AnaliseDashboard.tsx
@src/components/mercadolivre/analise/AnalisePrecosTable.tsx
@src/hooks/useAnalysisSnapshots.ts
@src/components/ui/input.tsx
@src/components/ui/select.tsx
@src/components/ui/card.tsx
@src/lib/analysis/compraUtils.ts
</context>

<interfaces>
<!-- Contratos consumidos pelo painel -->

From `src/lib/analysis/compraUtils.ts` (plano 06-01):
```ts
export interface StockInputs { diasCobertura: number; estoqueTotal: number; estoqueFull: number; estoqueCasa: number; }
export type Multiplicador = 1.0 | 1.2 | 1.5 | 2.0;
export interface CompraResult {
  vendaDiariaEstrategia: number;
  coberturaAlvo: number;
  compraRecomendada: number;
  sugestaoFull: number;
  pctFull: number;
  estrategiaUsada: 'gmv' | 'neutral' | 'margin';
  fallbackUsed: boolean;
}
export function calcularCompra(snapshot: AnalysisSnapshot, inputs: StockInputs, mult: Multiplicador): CompraResult;
```

From `src/hooks/useAnalysisSnapshots.ts`:
- `AnalysisSnapshot` (campos id, title, brand opcional, strategy, priceGmv/priceNeutral/priceMargin, priceCurve...).

From `src/components/ui/*`:
- `Card`, `CardHeader`, `CardTitle`, `CardContent` do `card.tsx`.
- `Input` do `input.tsx` (type="number" min="0").
- `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem` do `select.tsx`.
- (Confirmar nomes exatos lendo os arquivos antes de implementar.)

Props do novo componente:
```ts
interface CompraRecomendadaPanelProps {
  snapshots: AnalysisSnapshot[];
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar CompraRecomendadaPanel.tsx</name>
  <files>src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx</files>
  <read_first>
    - src/components/mercadolivre/analise/AnalisePrecosTable.tsx (padrão de Card/tabela inline, classes Tailwind usadas)
    - src/components/ui/card.tsx (API exata de Card/CardHeader/CardTitle/CardContent)
    - src/components/ui/input.tsx (props do Input)
    - src/components/ui/select.tsx (API exata: Select, SelectTrigger, SelectValue, SelectContent, SelectItem)
    - src/lib/analysis/compraUtils.ts (assinatura final de calcularCompra e tipos)
    - src/hooks/useAnalysisSnapshots.ts (campos disponíveis em AnalysisSnapshot, especialmente title/brand/strategy)
  </read_first>
  <action>
    Criar componente funcional `CompraRecomendadaPanel({ snapshots })` em `src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx`. Estado interno:
    - `multiplicador` (Multiplicador) via `useState<Multiplicador>(1.0)`.
    - `stockInputs` (`Record<string, StockInputs>`) via `useState({})`; usar um helper `getInputs(id)` que retorne o registro existente OU o default `{ diasCobertura: 30, estoqueTotal: 0, estoqueFull: 0, estoqueCasa: 0 }` (não materializar defaults no estado até o usuário digitar — o componente deve renderizar os defaults via getInputs).
    - `results` via `useMemo` que mapeia cada snapshot.id para `calcularCompra(snapshot, getInputs(snapshot.id), multiplicador)`; dependências `[snapshots, stockInputs, multiplicador]`.

    Layout (usando os primitives do projeto):
    - `<Card>` com `<CardHeader>` contendo um flex row: à esquerda `<CardTitle>Recomendações de Compra</CardTitle>`; à direita um `<Select value={String(multiplicador)} onValueChange={(v) => setMultiplicador(Number(v) as Multiplicador)}>` com itens `1.0`/`1.2`/`1.5`/`2.0` rotulados respectivamente "Normal ×1,0", "Campanha leve ×1,2", "Data forte ×1,5", "Live–oferta ×2,0" (COMP-02). Valor inicial "1.0".
    - `<CardContent>`: se `snapshots.length === 0`, renderizar três Skeleton lines (usar `Skeleton` se já existir em `src/components/ui/skeleton.tsx`; caso não exista, renderizar `<div className="text-sm text-muted-foreground">Sem produtos analisados.</div>`).
    - Senão, renderizar uma lista (não usar `<table>`; usar grid com `grid grid-cols-12 gap-2 items-center` por linha para responsividade) com uma linha por snapshot:
      - Col 1 (`col-span-3`): título truncado (`truncate`) + badge da marca se `snapshot.brand` existir.
      - Cols 2–5 (`col-span-1` cada, totalizando 4): quatro `<Input type="number" min="0">` controlados, rotulados visualmente acima (labels pequenos `text-xs text-muted-foreground`): "Cobertura (dias)", "Est. Total", "Est. FULL", "Est. Casa/CD" (COMP-01). `onChange` atualiza `stockInputs[snapshot.id]` mantendo os outros campos via spread; converter `e.target.value` para Number com fallback 0 quando NaN/string vazia.
      - Col 6 (`col-span-4`): bloco de saídas read-only, dois textos:
        - `Compra: {result.compraRecomendada} unid.` — aplicar `text-emerald-600 font-semibold` quando `result.compraRecomendada > 0`, senão `text-muted-foreground`.
        - `FULL: {result.sugestaoFull} unid.` — `text-blue-600`.
        - Se `result.fallbackUsed`, exibir `<span className="text-xs text-muted-foreground">(usando GMV)</span>` ao lado.

    Tudo é frontend puro. Importar `calcularCompra`, `StockInputs`, `Multiplicador`, `CompraResult` de `@/lib/analysis/compraUtils`. Tipar props com `CompraRecomendadaPanelProps`. Não introduzir hook customizado — toda a lógica vive no componente conforme decisão de RESEARCH.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe e exporta default (ou nomeado) `CompraRecomendadaPanel`.
    - `npx tsc --noEmit` passa.
    - Componente usa `useMemo` para `results` e `useState` para `multiplicador` e `stockInputs`.
    - Quatro inputs por produto presentes; selector de multiplicador presente no header com 4 opções.
    - Outputs renderizam `result.compraRecomendada` e `result.sugestaoFull`.
  </acceptance_criteria>
  <done>
    Painel renderiza-se isoladamente sem erros de tipo; pronto para ser plugado no AnaliseDashboard pela Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Plugar CompraRecomendadaPanel no AnaliseDashboard</name>
  <files>src/components/mercadolivre/analise/AnaliseDashboard.tsx</files>
  <read_first>
    - src/components/mercadolivre/analise/AnaliseDashboard.tsx (localizar o branch `snapshots.length > 0` e a posição do `<AnalisePrecosTable>`)
  </read_first>
  <action>
    Editar `AnaliseDashboard.tsx`:
    1. Adicionar `import CompraRecomendadaPanel from './CompraRecomendadaPanel';` (ou named import se Task 1 exportou nomeado) próximo aos demais imports da pasta `analise`.
    2. Dentro do branch que renderiza `<AnalisePrecosTable ... />` (quando `snapshots.length > 0`), inserir imediatamente após o `<AnalisePrecosTable>` o elemento `<CompraRecomendadaPanel snapshots={snapshots} />`.
    3. Não alterar nenhum outro comportamento, hook, estado ou ordem dos demais componentes. Não tocar em rotas, providers ou Supabase.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `AnaliseDashboard.tsx` importa CompraRecomendadaPanel e o renderiza exatamente uma vez, logo após `<AnalisePrecosTable>` no branch `snapshots.length > 0`.
    - `npx tsc --noEmit` passa.
    - Nenhuma outra mudança no arquivo (diff cirúrgico: 1 import + 1 elemento).
  </acceptance_criteria>
  <done>
    Fluxo end-to-end pronto: ao abrir a tela de análise com snapshots, o painel aparece abaixo da tabela de preços e responde em tempo real às mudanças de inputs e multiplicador.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → estado React | Inputs numéricos digitados pelo usuário entram no estado e alimentam `calcularCompra`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Inputs numéricos do painel | mitigate | `type="number" min="0"` no Input + coerção `Number(e.target.value)` com fallback 0 quando NaN; clamp final em compraUtils. |
| T-06-05 | Denial of Service | Re-render do painel em cada keystroke | mitigate | `useMemo` em `results` com deps `[snapshots, stockInputs, multiplicador]`; cálculo é O(n) sobre snapshots (≤ ~20). |
| T-06-06 | Information Disclosure | Painel inline | accept | Sem PII, sem chamadas de rede; renderização local apenas. |
</threat_model>

<verification>
- `npx tsc --noEmit` sem novos erros.
- Inspeção manual do diff de `AnaliseDashboard.tsx`: apenas 1 import + 1 elemento adicionados.
- Inspeção do painel: 4 inputs por linha, selector com 4 opções no header, outputs read-only.
</verification>

<success_criteria>
- COMP-01: 4 inputs por produto presentes e controlados.
- COMP-02: selector de multiplicador no header com 4 opções; `results` recalcula em tempo real via useMemo.
- COMP-03/COMP-04: outputs exibidos vêm exclusivamente de `calcularCompra` (sem fórmulas duplicadas no componente).
- Painel renderiza abaixo da `AnalisePrecosTable` quando `snapshots.length > 0`.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-recomenda-es-de-compra-full/06-02-SUMMARY.md` resumindo: arquivos criados/modificados, estrutura visual do painel e como a integração no AnaliseDashboard foi feita.
</output>
