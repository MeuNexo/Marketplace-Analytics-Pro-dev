---
phase: 81-giro-e-cobertura-por-faixa-de-pre-o
plan: 02
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Cada barra com vendas exibe rótulo curto de cobertura (~Xd) na faixa"
    - "Cobertura < 7 dias aparece em vermelho no texto do rótulo/tooltip"
    - "Faixa de baixa confiança mostra sufixo ? e tom esmaecido, sem esconder o dado"
    - "Tooltip da faixa mostra giro/dia, cobertura em dias e estoque atual"
    - "Cartão-veredito ganha a frase de cobertura do preço vigente"
    - "Cor da barra continua representando saúde de margem (inalterada)"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      provides: "Leitura de estoque via useMLInventory, injeção de estoqueAtual em computePrecoFaixas, rótulo de cobertura na barra, tooltip estendido, frase no veredito e rodapé de transparência"
      contains: "useMLInventory"
  key_links:
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/contexts/MLInventoryContext.tsx"
      via: "useMLInventory().items → available_quantity do item_id selecionado"
      pattern: "useMLInventory"
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/lib/precoFaixas.ts"
      via: "computePrecoFaixas recebe estoqueAtual no opts"
      pattern: "estoqueAtual"
---

<objective>
Renderizar giro e cobertura na visão de faixas de `PrecoPraticadoReport.tsx`: ler o
estoque atual do anúncio selecionado via `MLInventoryContext`, injetá-lo em
`computePrecoFaixas`, e exibir (1) rótulo `~Xd` em cada barra colorido por risco, (2)
tooltip da faixa com giro/cobertura/estoque, (3) frase de cobertura no cartão-veredito
do preço vigente e (4) rodapé de transparência. A cor da barra (saúde de margem) e o
rótulo de margem % permanecem intocados.

Purpose: Fecha a Phase 81 entregando a leitura "nesse preço, em quanto tempo esvazio o
estoque?" direto no histograma, sem novo fetch/RPC — só consumindo o util do 81-01 e o
estoque DB-first já disponível no contexto.

Output: `PrecoPraticadoReport.tsx` estendido; build `tsc` limpo; página funcionando em
light e dark.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-giro-e-cobertura-por-faixa-de-pre-o/81-CONTEXT.md
@docs/superpowers/specs/2026-07-02-giro-cobertura-por-faixa-design.md
@.planning/phases/81-giro-e-cobertura-por-faixa-de-pre-o/81-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ler estoque via MLInventoryContext e injetar em computePrecoFaixas</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx — INTEIRO. Pontos-chave:
      `selectedId` (item_id, linha ~200), o memo `faixasResult = computePrecoFaixas(dailyPoints,
      { mode: faixaMode })` (linha ~390) e `faixasChartData` (linha ~398). É onde estoque entra.
    - src/contexts/MLInventoryContext.tsx (linhas 17-39, 204-208) — `useMLInventory()` devolve
      `items: ProductItem[]`, cada um com `id` (= item_id) e `available_quantity`. Provider já é
      ancestral de /analise-precos (App.tsx: MLInventoryProvider envolve ApiLayout) — hook seguro.
    - src/lib/precoFaixas.ts (após 81-01) — `ComputeFaixasOpts.estoqueAtual`, `FaixasResult.estoqueAtual`,
      `FaixaPreco.{diasNaFaixa,giroDia,coberturaDias,baixaConfianca}`, `COBERTURA_RISCO_DIAS`.
  </read_first>
  <action>
    Ligar o estoque atual do anúncio ao cálculo de faixas, sem tocar na aba temporal secundária:

    1. Importar useMLInventory de @/contexts/MLInventoryContext e chamá-lo no topo do componente para
       obter `items`.
    2. Derivar `estoqueAtual` num useMemo: procurar em items o item com id === selectedId e usar seu
       available_quantity; quando não encontrado (anúncio fora do escopo de estoque atual / cache não
       sincronizado), usar null. Depender de [items, selectedId]. NÃO fazer fetch novo — o contexto já
       carrega o cache DB-first.
    3. Passar estoqueAtual no opts de computePrecoFaixas: o memo faixasResult passa a ser
       computePrecoFaixas(dailyPoints, { mode: faixaMode, estoqueAtual }), com estoqueAtual nas deps.
    4. Propagar o estoque para as linhas do gráfico: em faixasChartData, além de `saude`, incluir
       `estoqueAtual: faixasResult.estoqueAtual` em cada datum (o tooltip da faixa precisa do valor Z).

    Sem alteração de fetch, RPC, ou da série temporal (rows/serie/chartData). Sem mexer na cor da
    barra (SAUDE_COLOR) nem no rótulo de margem %.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useMLInventory' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 2
      (import + chamada).
    - `grep 'estoqueAtual' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` aparece no
      memo derivado, no opts de computePrecoFaixas e no map de faixasChartData.
    - `grep 'available_quantity' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` presente
      (leitura do item selecionado).
    - `npx tsc --noEmit` sai com código 0.
  </acceptance_criteria>
  <done>
    O componente lê available_quantity do item_id selecionado via useMLInventory, injeta estoqueAtual
    em computePrecoFaixas e o propaga para faixasChartData; tsc limpo; nada da aba temporal alterado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rótulo de cobertura na barra, tooltip, frase no veredito e rodapé</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx — foco em: `FaixaTooltip`
      (linhas ~172-195), o cartão-veredito (linhas ~578-587), o `<LabelList>` de margem % dentro do
      `<Bar>` (linhas ~624-644) e o rodapé de transparência (linhas ~668-673). Reusar `cn`,
      `text-destructive` (vermelho de risco, já usado no arquivo), `intFmt`, `brl`.
    - src/lib/precoFaixas.ts (após 81-01) — campos giroDia/coberturaDias/baixaConfianca em FaixaPreco,
      COBERTURA_RISCO_DIAS, e Veredicto.coberturaTexto.
  </read_first>
  <action>
    Renderizar a informação de giro/cobertura reusando os componentes já existentes, per decisão de
    CONTEXT (apresentação "rótulo na barra + tooltip"). A cor da barra e o rótulo de margem % NÃO
    mudam — cobertura colore apenas texto de rótulo/tooltip, sem conflito de sinal.

    1. Rótulo de cobertura na barra. Adicionar um segundo `<LabelList>` (ou estender o content do
       existente) que, para faixas com unidades > 0 e coberturaDias != null, desenha um texto curto
       do tipo `~Xd` posicionado na barra (posição exata é discricionária — ex. abaixo do rótulo de
       margem ou dentro/topo da barra, via LabelList content com x/y/width). Regras de estilo:
       - "<1d" quando coberturaDias === 0 e estoqueAtual > 0; senão `~{coberturaDias}d`.
       - Cor vermelha (fill = hsl(var(--destructive))) quando coberturaDias != null e
         coberturaDias < COBERTURA_RISCO_DIAS; neutra (muted-foreground/foreground) acima.
       - baixaConfianca: acrescentar sufixo `?` ao texto e reduzir a opacidade (tom esmaecido) —
         sinaliza sem esconder.
       - Faixa vazia (unidades 0) ou coberturaDias null: não desenhar rótulo de cobertura.
       Manter o rótulo de margem % existente e a marca "seu preço" intactos.

    2. Tooltip da faixa. Em FaixaTooltip, acrescentar linhas de giro (X/dia), cobertura (Y dias, ou
       "menos de 1 dia" / "sem estoque" / "sem giro" conforme o caso) e estoque atual (Z und, lido de
       f.estoqueAtual). Pintar o valor de cobertura de vermelho (text-destructive) quando
       coberturaDias < COBERTURA_RISCO_DIAS. Quando baixaConfianca, mostrar um aviso curto do tipo
       "só N dias de dados — estimativa fraca" usando f.diasNaFaixa.

    3. Frase de cobertura no cartão-veredito. Abaixo das duas frases atuais (saudeTexto/otimoTexto),
       renderizar veredicto.coberturaTexto quando não for null, com a mesma tipografia da segunda
       linha; aplicar cor de risco (text-destructive) à linha quando a faixa do preço vigente
       (faixasResult.faixas com isPrecoAtual) tiver coberturaDias != null e < COBERTURA_RISCO_DIAS.

    4. Rodapé de transparência. Estender o texto do rodapé do histograma para explicar giro e estoque:
       giro medido nos dias-com-venda do período selecionado; estoque = saldo atual do anúncio
       (sincronizado diariamente); cobertura é cenário hipotético ("a esse preço, o estoque de hoje
       duraria X dias"). Manter as descrições já existentes de eixo/cor/faixa vazia/outlier.

    Não introduzir dependência nova. Não alterar SAUDE_COLOR, a série temporal, nem o fetch.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit && npx vitest run src/lib/precoFaixas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'coberturaDias' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 3
      (rótulo + tooltip + frase de risco).
    - `grep 'COBERTURA_RISCO_DIAS' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx`
      presente (limiar de vermelho aplicado).
    - `grep -E 'baixaConfianca' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` presente
      (sufixo ? / esmaecido).
    - `grep 'coberturaTexto' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` presente
      (frase no cartão-veredito).
    - `grep -Ec 'giro' src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` >= 2 (tooltip +
      rodapé).
    - `npx tsc --noEmit` código 0 e `npx vitest run src/lib/precoFaixas.test.ts` verde (não regrediu).
  </acceptance_criteria>
  <done>
    Cada barra com vendas mostra `~Xd` (vermelho < 7d, `?`+esmaecido em baixa confiança, "<1d" no
    piso); o tooltip lista giro/cobertura/estoque com aviso de amostra fraca; o cartão-veredito ganha
    a frase de cobertura do preço vigente; o rodapé explica giro nos dias-com-venda e estoque atual.
    Cor da barra e margem % inalterados; tsc limpo.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual de giro e cobertura em /analise-precos</name>
  <action>Checkpoint humano (sem automação): apresentar a página para conferência visual e de cálculo per os passos de how-to-verify abaixo. Não editar código nesta task.</action>
  <what-built>
    Giro e cobertura por faixa no histograma de /analise-precos: rótulo ~Xd na barra (vermelho de
    risco, ? de baixa confiança), tooltip com giro/cobertura/estoque, frase de cobertura no
    cartão-veredito e rodapé de transparência. Estoque vem do cache DB-first (available_quantity).
  </what-built>
  <how-to-verify>
    1. Rodar o app (npm run dev) e abrir /analise-precos.
    2. Selecionar um anúncio com vendas em vários preços no período.
    3. Conferir manualmente para uma faixa: unidades ÷ dias-com-venda = giro; estoque atual ÷ giro =
       cobertura (arredondada p/ baixo). Bater com o rótulo `~Xd` e com o tooltip.
    4. Confirmar: cobertura < 7 dias em vermelho; faixa com < 3 dias com `?` e tom esmaecido;
       cartão-veredito com a frase "No preço atual R$X, seu estoque de N und dura ~Y dias".
    5. Confirmar que a COR DA BARRA continua sendo saúde de margem (verde/âmbar/vermelho da margem),
       não a cobertura.
    6. Validar em light E dark mode.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os ajustes necessários</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
Nenhum símbolo exportado novo (plano de UI). Consome de `src/lib/precoFaixas.ts` (81-01):
`COBERTURA_RISCO_DIAS`, campos `giroDia/coberturaDias/baixaConfianca/diasNaFaixa` de `FaixaPreco`,
`FaixasResult.estoqueAtual` e `Veredicto.coberturaTexto`. Consome de `MLInventoryContext`:
`useMLInventory().items[].available_quantity`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MLInventoryContext → componente | O estoque (available_quantity) já vem de `ml_inventory_cache` sob RLS org-first (fonte existente). Nenhuma nova query/entrada de usuário é introduzida por este plano. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03 | Information Disclosure | estoque por item_id | accept | Leitura reusa o contexto já isolado por RLS/escopo de loja; sem novo caminho de acesso. |
| T-81-04 | Tampering | render de cobertura null/0 | mitigate | Guardas na UI (unidades>0 e coberturaDias!=null) evitam NaN/"~undefinedd"; item ausente no cache → estoque null → sem rótulo. |
| T-81-05 | Denial of Service | (npm installs) | accept | Nenhum pacote novo. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo.
- `npx vitest run src/lib/precoFaixas.test.ts` continua verde (sem regressão do util).
- Checkpoint humano aprovado (cálculo manual bate; risco vermelho; baixa confiança; cor da barra
  inalterada; light+dark).
</verification>

<success_criteria>
- Cada faixa com vendas mostra giro e cobertura corretos (verificação manual bate).
- Faixas com < 3 dias sinalizadas como baixa confiança (? + esmaecido).
- Cobertura < 7 dias em vermelho no rótulo/tooltip.
- Cor da barra (saúde de margem) permanece inalterada.
- Nenhuma RPC/migration/dependência nova; suíte verde; build limpo.
</success_criteria>

<output>
Create `.planning/phases/81-giro-e-cobertura-por-faixa-de-pre-o/81-02-SUMMARY.md` when done
</output>
