---
phase: 62-reposicao-server-side
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analysis/replenishmentUtils.ts
  - src/lib/analysis/replenishmentUtils.test.ts
autonomous: true
requirements:
  - REPL-04
  - REPL-05
  - REPL-06
  - REPL-07
  - REPL-08
  - REPL-11

must_haves:
  truths:
    - "calcReplenishment(estoque, vendaDia, params) reproduz a aritmética da RPC get_replenishment"
    - "Estoque acima do ponto → compraSugerida = 0 (gatilho)"
    - "vendaDia = 0 com estoque > 0 → semGiro = true, compraSugerida = 0"
    - "MOQ e pack_multiple arredondam a sugestão pra cima respeitando o mínimo"
    - "Resolução de params marca > global > default é testada"
    - "Custo nulo → custoAusente = true, valorEstimado = null"
  artifacts:
    - path: "src/lib/analysis/replenishmentUtils.ts"
      provides: "Fórmula TS pura de reposição + resolução de params + interfaces"
      min_lines: 40
      exports: ["calcReplenishment", "resolveParams", "ReplenishmentParams"]
    - path: "src/lib/analysis/replenishmentUtils.test.ts"
      provides: "Suite vitest cobrindo os 8 casos do RESEARCH Test Map"
      contains: "describe"
  key_links:
    - from: "src/lib/analysis/replenishmentUtils.test.ts"
      to: "src/lib/analysis/replenishmentUtils.ts"
      via: "import das funções puras"
      pattern: "from \"./replenishmentUtils\""
  prohibitions:
    - "NÃO alterar src/lib/analysis/compraUtils.ts nem compraUtils.test.ts (sistema antigo, zero regressão)"
    - "NÃO importar supabase nem React aqui — módulo puro, testável em isolamento"
---

<objective>
Extrair a aritmética de reposição num módulo TS puro (`replenishmentUtils.ts`) espelhando a fórmula da RPC `get_replenishment`, e cobrir com testes vitest os casos travados (normal, estoque>alvo→0, sem giro, custo nulo, arredondamento MOQ/pack, override por marca, fallback sem vendas). É a fonte de verdade testável em isolamento da lógica; a RPC em si é validada por SQL no checkpoint do 62-01.

Purpose: REPL-11 exige testes da fórmula no estilo de compraUtils.test.ts. O módulo puro também documenta a aritmética sem depender do banco.
Output: módulo puro + suite vitest verde.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-reposicao-server-side/62-CONTEXT.md
@.planning/phases/62-reposicao-server-side/62-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo puro replenishmentUtils.ts</name>
  <files>src/lib/analysis/replenishmentUtils.ts</files>
  <read_first>
    - .planning/phases/62-reposicao-server-side/62-RESEARCH.md (seção "Pattern 3: fórmula TS pura" — assinatura calcReplenishment e lógica; seção 6 — resolução de precedência marca > global > default)
    - .planning/phases/62-reposicao-server-side/62-CONTEXT.md (bloco Fórmula travada)
    - src/lib/analysis/compraUtils.ts (estilo de módulo de fórmula a espelhar — NÃO editar)
  </read_first>
  <behavior>
    - Test 1 (normal): estoque=100, vendaDia=10, params default {lead 30, meta 60, safety 7, moq 1, pack 1} → ponto=10*(30+7)=370; estoque<=ponto → gatilho; alvo=10*(60+7)=670; compra=max(ceil((670-100)/1)*1, 1)=570
    - Test 2 (estoque>ponto): estoque=1000, vendaDia=10, default → ponto=370; 1000>370 → gatilhoAtivo=false, compra=0
    - Test 3 (sem giro): vendaDia=0, estoque=50 → semGiro=true, compra=0, gatilhoAtivo=false
    - Test 4 (MOQ): necessidade=7, pack=1, moq=10 → compra=10
    - Test 5 (pack): necessidade=7, pack=5, moq=1 → ceil(7/5)*5=10
    - Test 6 (custo nulo): cost=null → custoAusente=true, valorEstimado=null, compra calculada normalmente
    - Test 7 (override marca): resolveParams escolhe a linha scope='marca' do brand do item sobre a global
    - Test 8 (fallback sem global): resolveParams sem linha global cai no default 30/60/7/1/1; param_origem='global'
  </behavior>
  <action>
    Criar `calcReplenishment(estoque: number, vendaDia: number, params: ReplenishmentParams, cost?: number | null)` retornando { compraSugerida, gatilhoAtivo, semGiro, pontoReposicao, alvo, coberturaAtual, custoAusente, valorEstimado, paramOrigem? }. Implementar EXATAMENTE a fórmula da RPC: semGiro = vendaDia===0 && estoque>0; se vendaDia===0 retorna compra 0, gatilho false; pontoReposicao = vendaDia*(leadTimeDias+safetyDays); gatilhoAtivo = estoque <= pontoReposicao; se !gatilho retorna compra 0; alvo = vendaDia*(metaCoberturaDias+safetyDays); necessidade = max(0, alvo-estoque); compra = max(ceil(necessidade/packMultiple)*packMultiple, moq); coberturaAtual = vendaDia>0 ? estoque/vendaDia : null; custoAusente = cost==null; valorEstimado = custoAusente ? null : compra*cost. Guardrail packMultiple<1 → tratar como 1. Exportar interface ReplenishmentParams {leadTimeDias, metaCoberturaDias, safetyDays, moq, packMultiple}. Exportar também `resolveParams(brand, globalRow, marcaRow, defaults)` que aplica precedência marca > global > defaults e retorna {params, origem: 'marca'|'global'} espelhando a CTE params da RPC. Não importar supabase/React.
  </action>
  <acceptance_criteria>
    - `grep -c "export function calcReplenishment" src/lib/analysis/replenishmentUtils.ts` retorna 1
    - `grep -c "export function resolveParams" src/lib/analysis/replenishmentUtils.ts` retorna 1
    - `grep -cE "import .*(supabase|react)" src/lib/analysis/replenishmentUtils.ts` retorna 0
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Suite vitest replenishmentUtils.test.ts</name>
  <files>src/lib/analysis/replenishmentUtils.test.ts</files>
  <read_first>
    - src/lib/analysis/compraUtils.test.ts (estilo a espelhar: describe/it, comentários com a aritmética esperada, fixtures locais)
    - .planning/phases/62-reposicao-server-side/62-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map, 8 casos)
  </read_first>
  <action>
    Escrever a suite vitest cobrindo os 8 casos do bloco <behavior> da Task 1, no estilo de compraUtils.test.ts (describe por função, it por caso, comentário com a conta esperada). Cada `it` deve fazer asserção numérica exata (toBe) sobre compraSugerida/gatilhoAtivo/semGiro/custoAusente/valorEstimado/coberturaAtual e, para resolveParams, sobre origem e valores resolvidos. Cobrir REPL-04 (gatilho ambos os lados), REPL-06 (MOQ e pack separados), REPL-07 (custo nulo), REPL-08 (sem giro), REPL-05 (override marca e fallback). Incluir um caso de coberturaAtual=null quando vendaDia=0.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/analysis/replenishmentUtils.test.ts` passa com todos os testes verdes
    - `grep -c "it(" src/lib/analysis/replenishmentUtils.test.ts` maior-ou-igual a 8
    - A suite completa não regrediu: `npx vitest run` passa
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Módulo puro de cálculo, sem I/O, sem rede, sem auth. Sem superfície de ataque própria. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-06 | Tampering | packMultiple = 0 vindo de params → divisão por zero | mitigate | guardrail packMultiple<1 tratado como 1 (espelha NULLIF da RPC) |
| T-62-SC | Tampering | npm installs | accept | Nenhum pacote novo (vitest já instalado) |
</threat_model>

<verification>
- `npx vitest run src/lib/analysis/replenishmentUtils.test.ts` verde
- `npx tsc --noEmit` sem novos erros
- compraUtils.ts / compraUtils.test.ts intocados (git diff vazio nesses arquivos)
</verification>

<success_criteria>
Módulo puro reproduz a fórmula da RPC e os 8 casos travados passam. REPL-04/05/06/07/08/11 cobertos por teste automatizado.
</success_criteria>

<output>
Create `.planning/phases/62-reposicao-server-side/62-02-SUMMARY.md` when done
</output>
