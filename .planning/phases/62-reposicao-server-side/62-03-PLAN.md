---
phase: 62-reposicao-server-side
plan: 03
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - src/hooks/useReplenishment.ts
  - src/components/mercadolivre/ReplenishmentPanel.tsx
  - src/pages/mercadolivre/MLEstoque.tsx
autonomous: true
requirements:
  - REPL-01
  - REPL-09
  - REPL-10

must_haves:
  truths:
    - "Existe uma aba nova 'Compra Recomendada' em /estoque (MLEstoque.tsx) que consome a RPC get_replenishment"
    - "A aba mostra colunas read-only da fonte: produto/marca, venda/dia, estoque atual, cobertura atual, ponto de reposição, sugestão, valor estimado (ou 'custo ausente'), flags e parâmetros usados com origem"
    - "Há aviso fixo de que v1 NÃO desconta compras a chegar (OC/trânsito)"
    - "O CompraRecomendadaPanel antigo em /precos-custos continua intacto"
  artifacts:
    - path: "src/hooks/useReplenishment.ts"
      provides: "Hook React Query que chama supabase.rpc('get_replenishment')"
      exports: ["useReplenishment"]
    - path: "src/components/mercadolivre/ReplenishmentPanel.tsx"
      provides: "Painel read-only da reposição server-side com aviso 'a chegar'"
      min_lines: 60
    - path: "src/pages/mercadolivre/MLEstoque.tsx"
      provides: "Nova TabsTrigger + TabsContent 'compra' montando ReplenishmentPanel"
      contains: "ReplenishmentPanel"
  key_links:
    - from: "src/hooks/useReplenishment.ts"
      to: "get_replenishment (RPC)"
      via: "supabase.rpc('get_replenishment', { p_org_id, p_sales_window_days, p_demand_multiplier })"
      pattern: "get_replenishment"
    - from: "src/pages/mercadolivre/MLEstoque.tsx"
      to: "src/components/mercadolivre/ReplenishmentPanel.tsx"
      via: "import + TabsContent value='compra'"
      pattern: "ReplenishmentPanel"
  prohibitions:
    - "NÃO remover nem editar src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx (zero regressão em /precos-custos)"
    - "NÃO passar org fornecida pelo usuário/modelo — p_org_id vem de currentOrg.id (anti-IDOR)"
    - "NÃO recriar inputs digitados de estoque — colunas são read-only da fonte"
---

<objective>
Adicionar a tela da Reposição correta: hook React Query `useReplenishment` consumindo a RPC `get_replenishment`, componente novo `ReplenishmentPanel.tsx` (colunas read-only da fonte + flags + params com origem + aviso "não considera a chegar"), e uma aba nova "Compra Recomendada" em `/estoque` (MLEstoque.tsx). O painel antigo em `/precos-custos` permanece intocado.

Purpose: REPL-09/REPL-10 — substituir os inputs digitados por dados reais da fonte, com a limitação de "a chegar" explícita.
Output: hook + componente + aba nova, build verde.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-reposicao-server-side/62-CONTEXT.md
@.planning/phases/62-reposicao-server-side/62-RESEARCH.md
@.planning/phases/62-reposicao-server-side/62-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook useReplenishment (React Query)</name>
  <files>src/hooks/useReplenishment.ts</files>
  <read_first>
    - src/hooks/useMLMarginWithAds.ts (padrão exato a espelhar: useQuery, currentOrg, supabase.rpc, mapeamento de Record para interface tipada, queryKey, staleTime)
    - .planning/phases/62-reposicao-server-side/62-RESEARCH.md (Pattern 2: React Query hook + colunas de retorno da RPC)
    - .planning/phases/62-reposicao-server-side/62-01-SUMMARY.md (assinatura final e colunas reais da RPC após apply)
  </read_first>
  <action>
    Criar `useReplenishment(salesWindowDays = 30, demandMultiplier = 1.0)` espelhando useMLMarginWithAds: usar useOrganization() para currentOrg; queryKey ["get_replenishment", currentOrg?.id, salesWindowDays, demandMultiplier]; queryFn chama supabase.rpc("get_replenishment", { p_org_id: currentOrg.id, p_sales_window_days: salesWindowDays, p_demand_multiplier: demandMultiplier }) — p_org_id SEMPRE de currentOrg.id (nunca de input do usuário, anti-IDOR); mapear (data ?? []) para a interface ReplenishmentRow tipada (item_id, title, brand, logistic_type, estoque_atual, venda_dia, cobertura_atual, ponto_reposicao, alvo, compra_sugerida, valor_estimado, custo_ausente, sem_giro, gatilho_ativo, param_lead_time, param_cobertura, param_safety, param_moq, param_pack, param_origem) com Number()/String()/Boolean() e null-handling; enabled: !!currentOrg?.id; staleTime 5*60*1000. Exportar a interface ReplenishmentRow.
  </action>
  <acceptance_criteria>
    - `grep -c "export function useReplenishment" src/hooks/useReplenishment.ts` retorna 1
    - `grep -c "get_replenishment" src/hooks/useReplenishment.ts` maior-ou-igual a 1
    - `grep -c "currentOrg" src/hooks/useReplenishment.ts` maior-ou-igual a 1
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Componente ReplenishmentPanel.tsx (read-only + aviso a chegar)</name>
  <files>src/components/mercadolivre/ReplenishmentPanel.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLEstoque.tsx (linhas 372-452 SubTabValorRisco e 784-801 EstoqueRelatorios — padrão de Card/Table/Badge/KPICard, currencyFmt, e como tabelas read-only são montadas neste módulo)
    - src/hooks/useReplenishment.ts (interface ReplenishmentRow — criada na Task 1)
  </read_first>
  <action>
    Criar o componente que usa useReplenishment() e renderiza uma tabela read-only (shadcn Table) das linhas, ordenada por compra_sugerida desc. Colunas (REPL-10): produto (title) + marca, venda/dia, estoque atual, cobertura atual (dias; "—" quando null/sem giro), ponto de reposição, sugestão de compra (compra_sugerida; com MOQ/pack já aplicados pela RPC), valor estimado (currencyFmt(valor_estimado) ou Badge "custo ausente" quando custo_ausente), flags (Badges: sem_giro, custo_ausente), e os parâmetros usados com origem (param_lead_time/param_cobertura/param_safety/param_moq/param_pack + param_origem 'global'/'marca'). Exibir loading (Skeleton) e empty state. NO TOPO do painel, um aviso fixo e visível (Alert/banner, REPL-09): texto explicando que a v1 NÃO desconta compras a chegar (ordens de compra/itens em trânsito) — a sugestão considera apenas o estoque atual no ML. Opcional: filtrar para mostrar por padrão só itens com gatilho_ativo (sugestão > 0), com toggle para ver todos. NÃO criar inputs editáveis de estoque. Named export ou default consistente com o uso em MLEstoque.
  </action>
  <acceptance_criteria>
    - `grep -c "useReplenishment" src/components/mercadolivre/ReplenishmentPanel.tsx` maior-ou-igual a 1
    - O aviso "a chegar" está presente: `grep -ic "a chegar\|trânsito\|transito" src/components/mercadolivre/ReplenishmentPanel.tsx` maior-ou-igual a 1
    - `grep -c "param_origem" src/components/mercadolivre/ReplenishmentPanel.tsx` maior-ou-igual a 1
    - `grep -c "custo_ausente" src/components/mercadolivre/ReplenishmentPanel.tsx` maior-ou-igual a 1
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Aba "Compra Recomendada" em MLEstoque.tsx</name>
  <files>src/pages/mercadolivre/MLEstoque.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLEstoque.tsx (linha 999 Tabs defaultValue="estoque"; linhas 1026-1029 TabsList com triggers "estoque"/"relatorios"; linhas 1059, 1398-1403 TabsContent — onde montar a aba nova)
    - src/components/mercadolivre/ReplenishmentPanel.tsx (componente a montar — criado na Task 2)
  </read_first>
  <action>
    Adicionar uma TabsTrigger value="compra" (rótulo "Compra Recomendada", com um ícone lucide já importado, ex. ShoppingCart se não importado adicionar ao import existente de lucide-react) na TabsList das linhas ~1026-1029, e um TabsContent value="compra" montando <ReplenishmentPanel /> ao lado dos TabsContent "estoque"/"relatorios" (após o bloco "relatorios", ~linha 1403). Importar ReplenishmentPanel no topo. NÃO alterar o defaultValue (continua "estoque"). NÃO tocar em nenhuma lógica de inventory/coverage existente. Manter o padrão de className dos triggers vizinhos.
  </action>
  <acceptance_criteria>
    - `grep -c "import.*ReplenishmentPanel" src/pages/mercadolivre/MLEstoque.tsx` retorna 1
    - `grep -c "value=\"compra\"" src/pages/mercadolivre/MLEstoque.tsx` retorna 2 (trigger + content)
    - `grep -c "ReplenishmentPanel" src/pages/mercadolivre/MLEstoque.tsx` maior-ou-igual a 2
    - `npx tsc --noEmit` passa e `npm run build` conclui sem erro
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → supabase.rpc('get_replenishment') | JWT do usuário; p_org_id deve vir de currentOrg.id, nunca de input livre |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07 | Information Disclosure | hook passando org arbitrária | mitigate | p_org_id = currentOrg.id (contexto autenticado); RLS no banco (62-01) é o gate final |
| T-62-08 | Information Disclosure | decisão de compra baseada em estoque incompleto | mitigate | aviso fixo "não desconta a chegar" (REPL-09); estoque soma tudo no ML (não só Full) via RPC |
| T-62-SC | Tampering | npm installs | accept | Nenhum pacote novo (shadcn/recharts/react-query já instalados) |
</threat_model>

<verification>
- `npx tsc --noEmit` sem novos erros e `npm run build` verde
- Aba "Compra Recomendada" presente em /estoque consumindo a RPC; aviso "a chegar" visível
- git diff vazio em src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx
- [Checkpoint visual recomendado no fechamento da fase] Wesley confere /estoque → aba Compra Recomendada com dados reais da Pé Vermeio
</verification>

<success_criteria>
Tela nova de reposição em /estoque consumindo get_replenishment, read-only, com flags, params com origem e aviso de "a chegar". Painel antigo de /precos-custos intacto. REPL-01 (consumo), REPL-09, REPL-10 satisfeitos.
</success_criteria>

<output>
Create `.planning/phases/62-reposicao-server-side/62-03-SUMMARY.md` when done
</output>
