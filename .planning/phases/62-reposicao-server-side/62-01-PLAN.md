---
phase: 62-reposicao-server-side
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260662000000_replenishment_params.sql
  - supabase/migrations/20260662000100_get_replenishment_rpc.sql
  - src/integrations/supabase/types.ts
autonomous: false
requirements:
  - REPL-01
  - REPL-02
  - REPL-03
  - REPL-04
  - REPL-05
  - REPL-06
  - REPL-07
  - REPL-08
user_setup:
  - service: supabase (ckcdevcxgvueywivefgx)
    why: "Aplicar as 2 migrations via MCP apply_migration (CLI local linkado no projeto errado gionpsuunfkkzzjdubfy — db push proibido). gsd-executor não tem MCP Supabase nem SUPABASE_ACCESS_TOKEN; é checkpoint do orquestrador."
    dashboard_config:
      - task: "apply_migration de replenishment_params + get_replenishment no projeto ckcdevcxgvueywivefgx, depois validar via execute_sql"
        location: "MCP Supabase apply_migration / execute_sql"

must_haves:
  truths:
    - "A RPC get_replenishment existe em ckcdevcxgvueywivefgx e retorna linhas reais para a org Pé Vermeio (7f615df7-7bac-45e5-8a93-827fb9ddeec7)"
    - "A tabela replenishment_params existe com RLS habilitada e CHECK em scope ('global'/'marca') e pack_multiple >= 1"
    - "Para um item com estoque acima do ponto de reposição, compra_sugerida = 0 (gatilho)"
    - "Para um item com venda_dia = 0 e estoque > 0, sem_giro = true e compra_sugerida = 0"
    - "Para um item sem custo casável, custo_ausente = true e valor_estimado IS NULL"
    - "Estoque vem de ml_inventory_cache.available_quantity sem filtro de logistic_type"
    - "Venda/dia vem de SUM(qty_sold)/janela em ml_product_daily_cache"
  artifacts:
    - path: "supabase/migrations/20260662000000_replenishment_params.sql"
      provides: "Tabela replenishment_params + RLS org-first + índice + CHECKs"
      contains: "CREATE TABLE"
    - path: "supabase/migrations/20260662000100_get_replenishment_rpc.sql"
      provides: "Função get_replenishment SECURITY INVOKER + REVOKE/GRANT"
      contains: "CREATE OR REPLACE FUNCTION public.get_replenishment"
    - path: "src/integrations/supabase/types.ts"
      provides: "Tipos manuais de replenishment_params (Tables) e get_replenishment (Functions)"
      contains: "get_replenishment"
  key_links:
    - from: "supabase/migrations/20260662000100_get_replenishment_rpc.sql"
      to: "ml_inventory_cache / ml_product_daily_cache / ml_product_costs / replenishment_params"
      via: "JOINs com filtro organization_id = p_org_id e RLS is_org_member nas tabelas base"
      pattern: "ml_inventory_cache"
  prohibitions:
    - "get_replenishment NUNCA usa o modo de segurança DEFINER (seria IDOR como na Phase 48) — sempre INVOKER"
    - "Nenhum GRANT EXECUTE a PUBLIC ou anon na função"
    - "Nenhum filtro logistic_type='fulfillment' no estoque (bug VERAC-01)"
    - "Nenhuma confiança em p_org_id como único gate — RLS org-first é o enforcement real"
---

<objective>
Criar a camada server-side da Reposição: tabela `replenishment_params` (parâmetros global + override por marca, RLS org-first) e a RPC `get_replenishment` (SECURITY INVOKER, escopada por org, paginável) que calcula ponto de reposição, gatilho, MOQ/pack, custo nulo e sem-giro a partir de dados reais. Atualizar `types.ts` manualmente. Aplicar as migrations via MCP `apply_migration` no projeto `ckcdevcxgvueywivefgx` e provar por SQL que a RPC retorna dados reais.

Purpose: Substituir o cálculo frágil do front (estoque digitado + venda simulada) por uma fonte-da-verdade no banco, mesmo padrão validado nas Phases 59/60 (`get_cashflow`).
Output: 2 migrations, types.ts atualizado, RPC + tabela vivas e validadas em produção.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-reposicao-server-side/62-CONTEXT.md
@.planning/phases/62-reposicao-server-side/62-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration da tabela replenishment_params (RLS org-first)</name>
  <files>supabase/migrations/20260662000000_replenishment_params.sql</files>
  <read_first>
    - .planning/phases/62-reposicao-server-side/62-RESEARCH.md (seção "6. Tabela replenishment_params — DDL proposto" — DDL completo proposto, RLS, índice)
    - supabase/migrations/20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql (padrão RLS org-first is_org_member/get_org_role a espelhar)
  </read_first>
  <action>
    Criar a migration que cria `public.replenishment_params` exatamente como o DDL proposto no RESEARCH (seção 6): colunas id (uuid pk), organization_id (FK organizations ON DELETE CASCADE), scope TEXT DEFAULT 'global' com CHECK scope IN ('global','marca') — fornecedor é v2, NÃO incluir (per A2), scope_value TEXT NOT NULL DEFAULT '' (per A1: '' representa global), lead_time_dias INT DEFAULT 30, meta_cobertura_dias INT DEFAULT 60, safety_days INT DEFAULT 7, moq INT DEFAULT 1, pack_multiple INT DEFAULT 1, created_at/updated_at timestamptz DEFAULT NOW(). Adicionar CHECK que garante pack_multiple maior-ou-igual a 1 e moq maior-ou-igual a 1 (guardrail anti-divisão-por-zero, Pitfall 5). UNIQUE (organization_id, scope, scope_value). Habilitar RLS. Policy SELECT para authenticated USING is_org_member(auth.uid(), organization_id). Policy de escrita (FOR ALL) para authenticated restrita a get_org_role IN owner/admin (NÃO incluir member na escrita — params de reposição são config sensível; SELECT já cobre leitura para todos os membros). Criar índice (organization_id, scope, scope_value). NÃO inserir linha de seed — a RPC tem fallback hardcoded (per Open Question 3). Cabeçalho da migration deve citar Projeto ckcdevcxgvueywivefgx e os requisitos REPL-05.
  </action>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE public.replenishment_params" supabase/migrations/20260662000000_replenishment_params.sql` retorna 1
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260662000000_replenishment_params.sql` retorna 1
    - `grep -c "is_org_member" supabase/migrations/20260662000000_replenishment_params.sql` maior-ou-igual a 1
    - `grep -c "get_org_role" supabase/migrations/20260662000000_replenishment_params.sql` maior-ou-igual a 1
    - `grep -vE '^[[:space:]]*--' supabase/migrations/20260662000000_replenishment_params.sql | grep -c "fornecedor"` retorna 0 (fornecedor é v2)
    - CHECK de scope e CHECK de pack_multiple presentes (grep "CHECK" maior-ou-igual a 2)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migration da RPC get_replenishment + types.ts manual</name>
  <files>supabase/migrations/20260662000100_get_replenishment_rpc.sql, src/integrations/supabase/types.ts</files>
  <read_first>
    - .planning/phases/62-reposicao-server-side/62-RESEARCH.md (seção "Pattern 1: RPC SECURITY INVOKER" — assinatura, RETURNS TABLE, CTEs sales/inventory/costs/params, fórmula SQL completa, REVOKE/GRANT; seção "5. Padrão RPC"; Pitfalls 1,2,4,5)
    - supabase/migrations/20260659000000_cashflow_projection_7d_rule.sql (padrão canônico get_cashflow: LANGUAGE plpgsql SECURITY INVOKER SET search_path = 'public' + REVOKE PUBLIC,anon + GRANT authenticated nas linhas 108-109)
    - src/integrations/supabase/types.ts (linhas ~1963-1990: entry get_margin_with_ads_by_product em Functions — espelhar Args/Returns; achar bloco Tables: linha 16 e Functions: linha 1923 para inserir as entradas novas em ordem alfabética)
  </read_first>
  <action>
    Criar a migration da função `public.get_replenishment(p_org_id UUID, p_sales_window_days INTEGER DEFAULT 30, p_demand_multiplier NUMERIC DEFAULT 1.0)` retornando a TABLE com as colunas do RESEARCH (item_id, title, brand, logistic_type, estoque_atual, venda_dia, cobertura_atual, ponto_reposicao, alvo, compra_sugerida, valor_estimado, custo_ausente, sem_giro, gatilho_ativo, param_lead_time, param_cobertura, param_safety, param_moq, param_pack, param_origem). Declarar a função LANGUAGE plpgsql, com SECURITY INVOKER e SET search_path = 'public' — usar o modo INVOKER (nunca o modo de segurança DEFINER, que confiaria em p_org_id e seria IDOR como na Phase 48: a RLS org-first das tabelas base é quem enforça o isolamento mesmo recebendo p_org_id). Implementar as CTEs conforme o RESEARCH:
    (a) sales: SUM(qty_sold)/p_sales_window_days por item_id em ml_product_daily_cache, date maior-ou-igual a CURRENT_DATE - p_sales_window_days, filtrado por organization_id = p_org_id (per REPL-03; multi-store soma across ml_user_ids da org).
    (b) inventory: SUM(available_quantity) GROUP BY item_id em ml_inventory_cache com status='active' e organization_id = p_org_id, SEM filtro de logistic_type (per REPL-02; somar cross-store, Pitfall 2). Preservar brand, logistic_type representativo e seller_custom_field por item.
    (c) costs: DISTINCT ON (item_id) de ml_product_costs ordenado por updated_at DESC, join por c.item_id = i.item_id OR c.seller_sku = i.seller_custom_field (per REPL-07, Pitfall 1).
    (d) params: resolução de precedência marca > global > hardcoded(30/60/7/1/1) via COALESCE com subqueries por campo (lead_time/meta_cobertura/safety/moq/pack) e param_origem = 'marca' quando existe linha scope='marca' para o brand do item, senão 'global' (per REPL-05).
    Aplicar a fórmula (per REPL-04/06/08): venda_dia = avg_daily * p_demand_multiplier; ponto_reposicao = venda_dia*(lead_time+safety); alvo = venda_dia*(meta_cobertura+safety); gatilho_ativo = estoque_atual menor-ou-igual a ponto_reposicao; cobertura_atual = estoque/venda_dia quando venda_dia>0 senão NULL (Pitfall 4, sem divisão por zero); compra_sugerida = 0 quando venda_dia=0 OU estoque acima do ponto, senão GREATEST(CEIL(GREATEST(0, alvo-estoque)/NULLIF(pack,0))*pack, moq)::INTEGER; sem_giro = (venda_dia=0 AND estoque>0); custo_ausente = (cost IS NULL); valor_estimado = NULL quando custo ausente senão compra_sugerida*cost. ORDER BY compra_sugerida DESC NULLS LAST. NÃO colocar LIMIT (paginação é via .range() no front, REPL-01). Encerrar com REVOKE EXECUTE ... FROM PUBLIC, anon e GRANT EXECUTE ... TO authenticated, espelhando get_cashflow.
    Depois, atualizar src/integrations/supabase/types.ts MANUALMENTE (convenção do projeto, Pitfall 6): adicionar a entry replenishment_params em Tables (Row/Insert/Update com as colunas) e a entry get_replenishment em Functions (Args com os 3 params; Returns[] com as 20 colunas tipadas como string/number/boolean/null espelhando get_margin_with_ads_by_product).
  </action>
  <acceptance_criteria>
    - `grep -c "CREATE OR REPLACE FUNCTION public.get_replenishment" supabase/migrations/20260662000100_get_replenishment_rpc.sql` retorna 1
    - `grep -c "SECURITY INVOKER" supabase/migrations/20260662000100_get_replenishment_rpc.sql` retorna 1
    - `grep -vE '^[[:space:]]*--' supabase/migrations/20260662000100_get_replenishment_rpc.sql | grep -c "SECURITY DEFINER"` retorna 0
    - `grep -c "REVOKE EXECUTE ON FUNCTION public.get_replenishment" supabase/migrations/20260662000100_get_replenishment_rpc.sql` retorna 1
    - `grep -c "GRANT EXECUTE ON FUNCTION public.get_replenishment" supabase/migrations/20260662000100_get_replenishment_rpc.sql` retorna 1
    - `grep -vE '^[[:space:]]*--' supabase/migrations/20260662000100_get_replenishment_rpc.sql | grep -c "logistic_type = 'fulfillment'"` retorna 0
    - `grep -c "get_replenishment" src/integrations/supabase/types.ts` maior-ou-igual a 1 e `grep -c "replenishment_params" src/integrations/supabase/types.ts` maior-ou-igual a 1
    - `npx tsc --noEmit` passa sem novos erros relacionados a get_replenishment/replenishment_params
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Aplicar migrations via MCP apply_migration + validar por SQL (checkpoint do orquestrador)</name>
  <action>
    Checkpoint do ORQUESTRADOR (gsd-executor não tem MCP Supabase nem SUPABASE_ACCESS_TOKEN). O executor já escreveu as 2 migrations + types.ts nas Tasks 1-2; aqui o orquestrador aplica via MCP apply_migration no projeto ckcdevcxgvueywivefgx e valida por execute_sql conforme o roteiro em <how-to-verify>. Sem este passo a verificação dá falso-positivo (build/types passam porque vêm do config, não do banco live).
  </action>
  <what-built>
    Duas migrations escritas no repo (replenishment_params + get_replenishment) e types.ts atualizado. Ainda NÃO aplicadas no banco — o gsd-executor não tem MCP Supabase nem SUPABASE_ACCESS_TOKEN.
  </what-built>
  <how-to-verify>
    [ORQUESTRADOR via MCP Supabase no projeto ckcdevcxgvueywivefgx — exatamente como Phases 59/60]
    1. apply_migration com o conteúdo de supabase/migrations/20260662000000_replenishment_params.sql
    2. apply_migration com o conteúdo de supabase/migrations/20260662000100_get_replenishment_rpc.sql
    3. execute_sql de validação (org Pé Vermeio = 7f615df7-7bac-45e5-8a93-827fb9ddeec7):
       SELECT count(*) AS linhas,
              count(*) FILTER (WHERE compra_sugerida > 0) AS sugeridos,
              count(*) FILTER (WHERE custo_ausente)       AS sem_custo,
              count(*) FILTER (WHERE sem_giro)            AS sem_giro,
              count(*) FILTER (WHERE NOT gatilho_ativo AND compra_sugerida = 0) AS acima_do_ponto
       FROM get_replenishment('7f615df7-7bac-45e5-8a93-827fb9ddeec7', 30, 1.0);
       Esperado: linhas > 0 (estoque ativo real existe); nenhum erro de divisão por zero; itens acima do ponto têm compra_sugerida=0.
    4. Anti-IDOR (prova de construção): execute_sql chamando get_replenishment com a org Thales (e4150d57-...) sob impersonação de um usuário Pé Vermeio (SET LOCAL ROLE authenticated + request.jwt.claims do usuário Pé Vermeio, em transação ROLLBACK) — deve retornar 0 linhas. Registrar o resultado no SUMMARY.
    5. get_advisors (security) após apply — sem novos ERROR de rls_disabled/security_definer relacionados às novas entidades.
  </how-to-verify>
  <acceptance_criteria>
    - apply_migration das 2 migrations sem erro
    - SELECT de validação retorna linhas > 0 para a org Pé Vermeio
    - Sem erro de divisão por zero; itens acima do ponto com compra_sugerida = 0
    - Chamada cross-org (org alheia sob JWT Pé Vermeio) retorna 0 linhas
    - get_advisors sem novo ERROR de security para get_replenishment/replenishment_params
  </acceptance_criteria>
  <resume-signal>Digite "aplicado" com o output do SELECT de validação, ou descreva o erro do apply_migration</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (authenticated user) → supabase.rpc('get_replenishment') | JWT do usuário cruza para o banco; p_org_id vem do cliente |
| RPC → tabelas base (ml_inventory_cache, ml_product_daily_cache, ml_product_costs, replenishment_params) | RLS org-first é o gate real de isolamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Information Disclosure / Tampering | get_replenishment recebendo p_org_id de outra org | mitigate | SECURITY INVOKER + RLS is_org_member nas 4 tabelas base; mesmo passando p_org_id alheio, RLS filtra → retorna vazio. Provado no checkpoint (chamada cross-org = 0 linhas) |
| T-62-02 | Elevation of Privilege | EXECUTE concedido a PUBLIC/anon por default do Postgres | mitigate | REVOKE EXECUTE FROM PUBLIC, anon + GRANT só a authenticated (padrão get_cashflow) |
| T-62-03 | Tampering / EoP | viewer/member escrevendo params de reposição | mitigate | RLS de escrita em replenishment_params restrita a get_org_role owner/admin; SELECT para todos os membros |
| T-62-04 | Denial of Service | pack_multiple = 0 → divisão por zero na RPC | mitigate | CHECK pack_multiple >= 1 na tabela + NULLIF(pack,0) na RPC |
| T-62-05 | Information Disclosure | estoque reportado parcial (só Full) induzindo decisão errada | accept/mitigate | SEM filtro logistic_type (soma tudo no ML), corrige VERAC-01; "a chegar" não descontado é exibido como aviso na UI (62-03) |
| T-62-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx tsc --noEmit` sem novos erros
- Ambas migrations existem em supabase/migrations/ com os greps de acceptance acima passando
- Checkpoint aplicado: SELECT de validação na org Pé Vermeio retorna linhas reais; cross-org retorna 0
</verification>

<success_criteria>
get_replenishment e replenishment_params vivos em ckcdevcxgvueywivefgx, RLS org-first, REVOKE/GRANT corretos, retornando dados reais para a Pé Vermeio e nada para org alheia. types.ts tipado. REPL-01..08 satisfeitos no backend.
</success_criteria>

<output>
Create `.planning/phases/62-reposicao-server-side/62-01-SUMMARY.md` when done
</output>
