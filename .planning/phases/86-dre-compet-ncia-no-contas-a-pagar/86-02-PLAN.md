---
phase: 86-dre-compet-ncia-no-contas-a-pagar
plan: 02
type: execute
wave: 2
depends_on: [86-01]
files_modified: []
autonomous: false
requirements: [SC-3, SC-4]
must_haves:
  truths:
    - ">=90% of 2026 tiny-sourced cash_outflows rows (tiny_payable_id IS NOT NULL) have a non-null competence_date"
    - "the enriched-row count does NOT drop after a subsequent sync-tiny-payables run (single-writer preserved)"
    - "get_cashflow definition is unchanged and never references competence_date; outflow_date values are intact (DFC/Phase 60 zero regression)"
    - "competence_date values are first-of-month dates matching the sampled June/2026 Pé Vermeio dataCompetencia"
  artifacts: []
  key_links:
    - from: "enrich_enqueue_new()"
      to: "cat_backfill_queue"
      via: "manual invocation re-queues 2026 rows missing competence_date; treasury_cat crons drain them"
      pattern: "competence_date IS NOT NULL"
    - from: "sync-tiny-payables upsert"
      to: "cash_outflows.competence_date"
      via: "column absent from upsert payload → untouched on conflict (count does not drop)"
      pattern: "onConflict"
---

<objective>
Prove the phase goal is actually TRUE in production: drive the backfill of 2026 through the existing `enrich_enqueue_new` / `treasury_cat_*` crons until `competence_date` coverage for 2026 tiny-sourced rows reaches >=90% (SC-3), then prove zero regression on the DFC and that the enrichment stays the single writer (SC-4). All steps run via Supabase MCP `execute_sql` / cron on `ckcdevcxgvueywivefgx` — orchestrator-driven, no repo file changes.

Purpose: The migration (Plan 01) only enables the data; this plan makes the backfill happen and closes the two behavioral success criteria that Phases 87/88 depend on.
Output: Verified coverage >=90% and a documented non-regression proof (DFC unchanged, enriched count stable after sync).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-dre-compet-ncia-no-contas-a-pagar/86-CONTEXT.md
@.planning/phases/86-dre-compet-ncia-no-contas-a-pagar/86-RESEARCH.md
@.planning/phases/86-dre-compet-ncia-no-contas-a-pagar/86-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Drive 2026 backfill and confirm >=90% competence coverage</name>
  <action>Orchestrator-only (executor has no Supabase MCP): call enrich_enqueue_new() to re-queue 2026 rows, drain via the existing treasury_cat crons (or the live enrich function in a loop), then measure coverage and spot-check first-of-month materialization. See how-to-verify for exact SQL.</action>
  <what-built>The widened enrichment pipeline from Plan 01. The orchestrator now triggers the backfill and confirms >=90% competence coverage for 2026. Uses only the existing queue + crons (no new script), per CONTEXT LOCKED "Backfill".</what-built>
  <read_first>
    - 86-RESEARCH.md → Pitfall 1 (backfill silently no-ops if enqueue predicate not widened — Plan 01 fixed this; here we prove it drains) and Open Questions 1–2 (denominator = tiny_payable_id IS NOT NULL rows, exclude manual).
    - 86-01-SUMMARY.md → baseline `n2026_tiny` count and which enrich function the live cron calls.
    - 86-CONTEXT.md → "Specifics" (validate against junho/2026, org Pé Vermeio 7f615df7).
  </read_first>
  <how-to-verify>
    1. TRIGGER re-enqueue via MCP execute_sql: `SELECT public.enrich_enqueue_new();` — the returned queue_open should jump to roughly the count of 2026 rows with competence_date IS NULL (Pitfall 1 warning sign is a flat/zero jump).
    2. DRAIN: let the existing `treasury_cat_tick` cron drain the queue (Tiny rate limit ~1–2 req/s; a full 2026 batch took ~20–30 min in Phase 61). If faster convergence is wanted, call the live cron's function directly in a loop via execute_sql (the one named in 86-01-SUMMARY — `SELECT public.enrich_payable_step(12);` or `SELECT public.enrich_harvest();`), watching `remaining` fall toward 0. Re-run `SELECT public.enrich_enqueue_new();` if new rows appear.
    3. MEASURE COVERAGE (SC-3), via execute_sql — denominator excludes manual rows (no detail endpoint to enrich):
       `SELECT round(count(*) FILTER (WHERE competence_date IS NOT NULL)::numeric / NULLIF(count(*),0), 4) AS coverage, count(*) AS denom, count(*) FILTER (WHERE competence_date IS NOT NULL) AS filled FROM public.cash_outflows WHERE tiny_payable_id IS NOT NULL AND outflow_date >= '2026-01-01' AND outflow_date < '2027-01-01';`
       PASS when coverage >= 0.90. If stuck below 0.90 with queue drained, inspect `SELECT status, count(*) FROM cat_backfill_queue GROUP BY 1` for stuck 'error'/'sent' rows and check `net._http_response` status codes (429 → retry; other → sample the payload).
    4. SPOT-CHECK first-of-month materialization on real June/2026 Pé Vermeio data:
       `SELECT competence_date, count(*) FROM public.cash_outflows WHERE organization_id='7f615df7'::uuid AND competence_date >= '2026-06-01' AND competence_date < '2026-07-01' GROUP BY 1;` → every competence_date is a first-of-month date (day = 01).
  </how-to-verify>
  <acceptance_criteria>
    - `enrich_enqueue_new()` re-enqueued the historical 2026 rows (queue_open jumped, not flat).
    - Coverage query returns coverage >= 0.90 over the `tiny_payable_id IS NOT NULL` 2026 denominator.
    - June/2026 Pé Vermeio competence_date values are all first-of-month (day 01), confirming YYYY-MM → date parsing.
    - No queue rows stuck in an unrecoverable 'error' state that would drag coverage below 0.90.
  </acceptance_criteria>
  <resume-signal>Type "backfilled" with the coverage/denom/filled numbers, or describe why <90%.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Prove DFC non-regression and single-writer guarantee</name>
  <action>Orchestrator-only (executor has no Supabase MCP): confirm get_cashflow is unchanged and never references competence_date, outflow_date null-count is unchanged vs baseline, and that a sync-tiny-payables run does not lower the enriched count (filled_after >= filled_before). See how-to-verify for exact SQL.</action>
  <what-built>Non-regression proof for the DFC (Phase 60) and the single-writer guarantee. Confirms competence_date is purely additive: get_cashflow untouched, outflow_date intact, and a sync-tiny-payables run does not erase enriched competence_date.</what-built>
  <read_first>
    - 86-RESEARCH.md → "Architectural Responsibility Map" (get_cashflow reads only outflow_date/amount/status/supplier) and "sync-tiny-payables: NO code change required" (column absent from upsert payload → untouched on conflict).
    - 86-CONTEXT.md → "Não-regressão (crítico)": zero regression na DFC/Phase 60; outflow_date intact.
  </read_first>
  <how-to-verify>
    1. DFC untouched (SC-4), via execute_sql:
       - `SELECT pg_get_functiondef('public.get_cashflow'::regproc) LIKE '%competence_date%' AS references_competence;` → false (get_cashflow never references the new column).
       - Confirm the get_cashflow definition matches the latest deployed cashflow migration (unchanged by this phase) — no diff attributable to Phase 86.
    2. outflow_date intact: `SELECT count(*) FILTER (WHERE outflow_date IS NULL) AS null_outflow, count(*) AS total FROM public.cash_outflows WHERE tiny_payable_id IS NOT NULL;` — null_outflow must be unchanged vs. the Plan 01 baseline (competence backfill only writes competence_date/category/supplier, never outflow_date).
    3. SINGLE-WRITER / no-overwrite proof (SC-2 behavioral): record `filled_before = SELECT count(*) FILTER (WHERE competence_date IS NOT NULL) FROM public.cash_outflows WHERE tiny_payable_id IS NOT NULL AND outflow_date >= '2026-01-01' AND outflow_date < '2027-01-01';` → run one sync (invoke `sync-tiny-payables` via its cron/MCP invoke, or wait for the next `sync-tiny-payables-6h` tick) → record `filled_after` with the same query. Require `filled_after >= filled_before` (a sync must NOT zero enriched rows — the column is absent from the upsert payload, so ON CONFLICT leaves it untouched).
    4. Optional reconciliation sanity: confirm the DFC (`get_cashflow`) output for org Pé Vermeio is byte-identical to a pre-migration run (same accumulated_balance curve) — competence_date must not shift any cashflow number.
  </how-to-verify>
  <acceptance_criteria>
    - `get_cashflow` definition does NOT reference competence_date and is unchanged by this phase.
    - `null_outflow` count for tiny rows is unchanged vs. baseline (outflow_date intact).
    - `filled_after >= filled_before` across a sync-tiny-payables run (enrichment remains the single writer; no overwrite).
    - DFC/Phase 60 numbers for Pé Vermeio unchanged (additive-only confirmed).
  </acceptance_criteria>
  <resume-signal>Type "non-regression proven" with filled_before/filled_after and the get_cashflow check result, or describe the regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tiny API → Postgres | backfill re-fetches `/contas-pagar/{id}` detail (untrusted dataCompetencia) through the existing enrich functions |
| sync-tiny-payables upsert → cash_outflows | write path that must NOT touch the enrichment-owned competence_date column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Denial of Service | Tiny rate limit during backfill drain | mitigate | reuse existing throttled cat_backfill_queue dispatch + 429→requeue handling (no parallel fetcher); drain over minutes, not a burst |
| T-86-05 | Tampering (data loss) | sync-tiny-payables overwriting competence_date | mitigate | verified behaviorally (filled_after >= filled_before); column stays absent from the upsert payload — do not add it |
| T-86-03 | Information Disclosure | cross-tenant queue token lookup | accept | unchanged from Phase 51 CR-01 (per-row ml_user_id join); no regression introduced here |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- Coverage query: >=90% competence_date over 2026 tiny_payable_id-bearing rows.
- get_cashflow does not reference competence_date; outflow_date null-count unchanged.
- filled_after >= filled_before across a sync run.
- June/2026 Pé Vermeio competence_date all first-of-month.
</verification>

<success_criteria>
- SC-3 met: >=90% of 2026 tiny-sourced rows carry competence_date.
- SC-4 met: DFC/Phase 60 unchanged (get_cashflow + outflow_date intact); competence_date coexists without conflict.
- SC-2 behaviorally confirmed: enriched count does not drop after a sync (single writer preserved).
</success_criteria>

<output>
Create `.planning/phases/86-dre-compet-ncia-no-contas-a-pagar/86-02-SUMMARY.md` when done, recording final coverage (coverage/denom/filled), the get_cashflow check result, and filled_before/filled_after.
</output>
