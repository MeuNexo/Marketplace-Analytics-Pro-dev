---
phase: 86-dre-compet-ncia-no-contas-a-pagar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260686000000_cash_outflows_competence_date.sql
autonomous: false
requirements: [SC-1, SC-2, SC-5]
must_haves:
  truths:
    - "cash_outflows has a nullable competence_date (date) column materialized as first-of-month from Tiny dataCompetencia"
    - "enrich_payable_step and enrich_harvest write competence_date in the SAME UPDATE that writes category/supplier (single-writer, ON CONFLICT-safe)"
    - "enrich_enqueue_new WHERE also selects rows where competence_date IS NULL, so already-enriched 2026 rows re-enter the queue"
    - "index (organization_id, competence_date, category) exists as an ADDITIONAL index (existing outflow_date index untouched)"
    - "all 3 SECURITY DEFINER functions keep REVOKE EXECUTE FROM PUBLIC, anon, authenticated"
  artifacts:
    - path: "supabase/migrations/20260686000000_cash_outflows_competence_date.sql"
      provides: "ALTER TABLE + index + CREATE OR REPLACE of enrich_enqueue_new / enrich_payable_step / enrich_harvest + REVOKEs"
      contains: "competence_date"
  key_links:
    - from: "supabase/migrations/20260686000000_cash_outflows_competence_date.sql"
      to: "public.cash_outflows"
      via: "ADD COLUMN IF NOT EXISTS competence_date date + new composite index"
      pattern: "competence_date date"
    - from: "enrich_enqueue_new"
      to: "cat_backfill_queue"
      via: "widened WHERE predicate re-enqueues rows missing competence_date"
      pattern: "OR co.competence_date IS NULL"
---

<objective>
Ship the schema + enrichment-pipeline change that makes `cash_outflows` carry the Tiny competence (`dataCompetencia`) per row. One migration adds a nullable `competence_date date` column, one supporting index `(organization_id, competence_date, category)`, and `CREATE OR REPLACE`s the three existing Phase 61 enrichment functions so they parse `dataCompetencia` and write `competence_date` alongside `category`/`supplier`, plus widens `enrich_enqueue_new`'s WHERE so already-enriched rows re-enter the backfill queue. Applied to `ckcdevcxgvueywivefgx` via Supabase MCP `apply_migration` (no CLI token exists for this project).

Purpose: Fase 1 of 3 of the "DRE de Resultado" milestone — habilita ler custos por mês de competência (Phases 87/88 build on this column). Backend only; no frontend, no RPC aggregation.
Output: `supabase/migrations/20260686000000_cash_outflows_competence_date.sql`, applied and confirmed in prod.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-dre-compet-ncia-no-contas-a-pagar/86-CONTEXT.md
@.planning/phases/86-dre-compet-ncia-no-contas-a-pagar/86-RESEARCH.md
@supabase/migrations/20260661000000_enrich_supplier_category.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the competence_date migration file</name>
  <files>supabase/migrations/20260686000000_cash_outflows_competence_date.sql</files>
  <read_first>
    - 86-RESEARCH.md → section "Migration skeleton (new file...)" — the authoritative SQL to transcribe.
    - 86-RESEARCH.md → "Pattern 1" (parse dataCompetencia), "Pattern 2" (widen enqueue predicate), Pitfalls 1–4.
    - supabase/migrations/20260661000000_enrich_supplier_category.sql — the deployed template: current bodies of enrich_enqueue_new (line 18), enrich_payable_step (line 55), enrich_harvest (line 131) and their REVOKE lines (50, 126, 177). Copy each function body from THIS deployed source, then add competence logic — do not invent bodies.
  </read_first>
  <action>
    Create the migration transcribing the SQL from 86-RESEARCH.md "Migration skeleton" section, using the deployed Phase 61 function bodies (20260661000000) as the base for each CREATE OR REPLACE so no existing behavior is dropped. The file must, in order:
    (1) ALTER TABLE public.cash_outflows ADD COLUMN IF NOT EXISTS competence_date date — nullable (dataCompetencia may be absent on old lançamentos; target is >=90%, not 100%, per CONTEXT LOCKED "Backfill").
    (2) CREATE INDEX IF NOT EXISTS cash_outflows_org_competence_category_idx ON public.cash_outflows (organization_id, competence_date, category) — ADDITIVE per CONTEXT LOCKED "Modelagem"; do NOT alter/merge/drop the existing outflow_date index (Pitfall 4).
    (3) CREATE OR REPLACE enrich_enqueue_new() with its WHERE extended by an additional disjunct on a null competence_date (Pattern 2 — the backfill trigger; without it already-enriched rows never re-queue and SC-3 silently fails). Keep the ON CONFLICT (tiny_payable_id) DO UPDATE ... WHERE status IN ('done','error') branch verbatim.
    (4) CREATE OR REPLACE enrich_payable_step(integer) AND enrich_harvest() — in BOTH, add a v_competence date local, parse it null-safe: guard with NULLIF(TRIM(COALESCE(v_content->>'dataCompetencia',''))) then to_date(trimmed || '-01','YYYY-MM-DD'), and add competence_date = v_competence to the SAME UPDATE of public.cash_outflows that already sets category/supplier. Update BOTH functions identically (Pitfall 3 — prod cron may call either).
    (5) Re-issue REVOKE EXECUTE ON FUNCTION ... FROM PUBLIC, anon, authenticated for all three functions (CREATE OR REPLACE does not reliably preserve prior REVOKEs — threat T-86-02).
    Preserve each function's existing signature, LANGUAGE plpgsql, SECURITY DEFINER and SET search_path. Never cast "YYYY-MM" directly to date (Pitfall 2 — that raw cast throws). Do NOT touch outflow_date, get_cashflow, sync-tiny-payables, or any cron.schedule/unschedule. Header comment must state: apply via MCP apply_migration on ckcdevcxgvueywivefgx, never `supabase db push`.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && f=supabase/migrations/20260686000000_cash_outflows_competence_date.sql && grep -q 'ADD COLUMN IF NOT EXISTS competence_date date' "$f" && grep -q 'cash_outflows_org_competence_category_idx' "$f" && grep -q 'OR co.competence_date IS NULL' "$f" && [ "$(grep -v '^--' "$f" | grep -c 'competence_date = v_competence')" -ge 2 ] && [ "$(grep -c 'CREATE OR REPLACE FUNCTION public.enrich_' "$f")" -eq 3 ] && [ "$(grep -c 'REVOKE EXECUTE ON FUNCTION' "$f")" -eq 3 ] && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260686000000_cash_outflows_competence_date.sql` exists.
    - Contains `ADD COLUMN IF NOT EXISTS competence_date date` and index `cash_outflows_org_competence_category_idx` on `(organization_id, competence_date, category)`.
    - `enrich_enqueue_new` WHERE contains the added `OR co.competence_date IS NULL` disjunct.
    - `competence_date = v_competence` appears in the cash_outflows UPDATE of BOTH `enrich_payable_step` and `enrich_harvest` (>=2 non-comment occurrences).
    - Exactly 3 `CREATE OR REPLACE FUNCTION public.enrich_*` and 3 matching `REVOKE EXECUTE ON FUNCTION` lines.
    - No direct `::date` cast of a "YYYY-MM" value; parsing goes through `to_date(... || '-01','YYYY-MM-DD')`.
  </acceptance_criteria>
  <done>Migration file authored and self-consistent; grep gate prints MIGRATION_OK. No changes to outflow_date, get_cashflow, sync-tiny-payables, or cron definitions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply migration via Supabase MCP and confirm schema objects</name>
  <action>Orchestrator-only (executor has no Supabase MCP): take a baseline snapshot, apply the migration via MCP apply_migration to ckcdevcxgvueywivefgx, then confirm the column/index/functions/privileges per how-to-verify. See how-to-verify for the exact MCP calls and SQL.</action>
  <what-built>The migration `20260686000000_cash_outflows_competence_date.sql` (column + additive index + 3 CREATE OR REPLACE enrichment functions + REVOKEs). The executor cannot reach Supabase MCP; the orchestrator applies and confirms it against project `ckcdevcxgvueywivefgx`.</what-built>
  <read_first>
    - 86-RESEARCH.md → "Runtime State Inventory" and "Assumptions Log" A3 (drift risk — take a baseline snapshot first).
    - 86-CONTEXT.md → "Specifics" (deploy só via MCP apply_migration; Supabase id = ckcdevcxgvueywivefgx, NOT the CLAUDE.md id).
  </read_first>
  <how-to-verify>
    1. BASELINE SNAPSHOT (before apply), via MCP execute_sql on ckcdevcxgvueywivefgx — record these numbers for Plan 02:
       - Confirm no drift: `SELECT jobname, schedule, command FROM cron.job WHERE jobname LIKE 'treasury_cat%'` (expect existing enqueue + tick jobs; note which enrich function each command calls).
       - Baseline denominator: `SELECT count(*) AS n2026_tiny FROM public.cash_outflows WHERE tiny_payable_id IS NOT NULL AND outflow_date >= '2026-01-01' AND outflow_date < '2027-01-01'`.
       - Confirm column absent today: `SELECT column_name FROM information_schema.columns WHERE table_name='cash_outflows' AND column_name='competence_date'` (expect 0 rows).
    2. APPLY the migration via MCP `apply_migration` (name: cash_outflows_competence_date, body = the migration file). Do NOT use `supabase db push` (no CLI token).
    3. CONFIRM schema objects exist post-apply, via execute_sql:
       - Column: `SELECT data_type FROM information_schema.columns WHERE table_name='cash_outflows' AND column_name='competence_date'` → returns `date`.
       - Index: `SELECT indexname FROM pg_indexes WHERE tablename='cash_outflows' AND indexname='cash_outflows_org_competence_category_idx'` → 1 row.
       - Functions carry the new logic: `SELECT proname FROM pg_proc WHERE proname IN ('enrich_enqueue_new','enrich_payable_step','enrich_harvest') AND pg_get_functiondef(oid) LIKE '%competence_date%'` → 3 rows.
       - Privilege intact (T-86-02): `SELECT has_function_privilege('anon','public.enrich_payable_step(integer)','EXECUTE')` → false.
    4. Confirm no advisor regressions: MCP `get_advisors` (security + performance) shows no NEW issue attributable to this migration.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied via MCP apply_migration (not CLI).
    - `cash_outflows.competence_date` exists with type `date`; index `cash_outflows_org_competence_category_idx` exists.
    - All 3 enrich_* function definitions now contain `competence_date`.
    - `anon`/`authenticated` have no EXECUTE on the enrich_* functions.
    - Baseline `n2026_tiny` count and the cron→function mapping are recorded (needed by Plan 02).
    - No new security/performance advisor issue introduced.
  </acceptance_criteria>
  <resume-signal>Type "applied" with the recorded n2026_tiny baseline and cron mapping, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tiny API → Postgres | `dataCompetencia` is untrusted external text parsed inside the enrich functions |
| authenticated/anon role → SECURITY DEFINER enrich functions | privilege boundary; functions run as owner and write cash_outflows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering/Input Validation | enrich_payable_step / enrich_harvest dataCompetencia parse | mitigate | NULLIF(TRIM()) guard then to_date with a fixed format mask; malformed/blank → NULL, no dynamic SQL, no raw ::date cast (Pitfall 2) |
| T-86-02 | Elevation of Privilege | 3 SECURITY DEFINER functions after CREATE OR REPLACE | mitigate | re-issue REVOKE EXECUTE FROM PUBLIC, anon, authenticated for all three in the same migration; verify with has_function_privilege |
| T-86-03 | Information Disclosure | cat_backfill_queue per-row token lookup | accept | already mitigated in Phase 51 CR-01 (token resolved per-row via ml_user_id join); this phase does not touch it — do not reintroduce a hardcoded ml_user_id |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | this phase installs zero packages (pure SQL migration); no legitimacy audit required |
</threat_model>

<verification>
- Migration file passes the Task 1 grep gate (MIGRATION_OK).
- MCP-confirmed: column `date`, index present, 3 functions contain competence logic, enrich_* not executable by anon/authenticated.
- `get_cashflow`, `outflow_date`, `sync-tiny-payables`, and all cron definitions are untouched (verified by absence of changes; DFC non-regression proven in Plan 02).
</verification>

<success_criteria>
- SC-1 (column populated path) enabled: `competence_date` exists and the enrich functions write it.
- SC-2 (single-writer, no overwrite) structurally in place: same UPDATE + ON CONFLICT pattern as Phase 61 (behavioral proof in Plan 02).
- SC-5 (index) satisfied: `cash_outflows_org_competence_category_idx` created, additive.
</success_criteria>

<output>
Create `.planning/phases/86-dre-compet-ncia-no-contas-a-pagar/86-01-SUMMARY.md` when done, recording the baseline `n2026_tiny` count and which enrich function the live cron calls (hand-off to Plan 02).
</output>
