---
phase: 11-oauth-secrets
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Integrations.tsx
autonomous: true

must_haves:
  truths:
    - O código OAuth retornado pelo ML é trocado usando o redirect URI correto do ambiente dev
    - O botão "Conectar Mercado Livre" gera um auth URL com o redirect URI correto do ambiente dev
    - O build deployado na Vercel contém o redirect URI atualizado (sem `analytics.alcavie.com`)
  artifacts:
    - src/pages/Integrations.tsx com redirect_uri apontando para marketplace-analytics-pro-dev.vercel.app
  key_links:
    - redirectUri (linha 287) → parâmetro redirect_uri enviado ao ml-oauth exchange_code
    - redirectUri (linha 341) → parâmetro redirect_uri enviado ao ml-oauth get_auth_url
    - Se exchange_code e get_auth_url usarem URIs diferentes, o ML rejeita o code exchange com "redirect_uri mismatch"
---

<objective>
Corrigir as duas ocorrências de redirect URI hardcoded em `src/pages/Integrations.tsx`.

O arquivo contém `"https://analytics.alcavie.com/integracoes"` em duas posições (linhas 287 e 341).
Ambas precisam ser trocadas para `"https://marketplace-analytics-pro-dev.vercel.app/integracoes"`.
Sem essa correção, o OAuth falha com "redirect_uri mismatch" porque o ML compara o URI do
`get_auth_url` com o do `exchange_code` — e nenhum dos dois corresponde ao app dev.

Purpose: Habilitar o fluxo OAuth completo no ambiente dev sem tocar no ambiente de produção.
Output: `src/pages/Integrations.tsx` com redirect URI correto em ambas as posições + push para Vercel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/Integrations.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir redirect URI hardcoded nas duas ocorrências</name>
  <files>src/pages/Integrations.tsx</files>
  <action>
    Substituir as duas ocorrências de `"https://analytics.alcavie.com/integracoes"` por
    `"https://marketplace-analytics-pro-dev.vercel.app/integracoes"`.

    Posições exatas:
    - Linha ~287: dentro da função `exchangeCode` (handler de callback OAuth)
      `const redirectUri = "https://analytics.alcavie.com/integracoes";`
    - Linha ~341: dentro da função `handleConnect` (inicia o fluxo OAuth)
      `const redirectUri = "https://analytics.alcavie.com/integracoes";`

    Usar a ferramenta Edit com replace_all: false e substituir uma de cada vez para confirmar
    que ambas foram trocadas.

    NÃO extrair para variável de ambiente — o valor é específico do ambiente dev e o build
    da Vercel Dev já existe. Hardcoded é aceitável aqui porque este arquivo é o frontend dev.

    NÃO alterar nenhuma outra linha do arquivo.
  </action>
  <verify>
    Após edição, executar:
    `grep -n "alcavie.com" src/pages/Integrations.tsx`
    Deve retornar vazio (zero matches).

    `grep -n "marketplace-analytics-pro-dev" src/pages/Integrations.tsx`
    Deve retornar exatamente 2 linhas.
  </verify>
  <done>
    Zero ocorrências de `analytics.alcavie.com` no arquivo.
    Exatamente 2 ocorrências de `marketplace-analytics-pro-dev.vercel.app/integracoes`.
    Ambas as posições (exchangeCode e handleConnect) atualizadas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit e push para acionar rebuild na Vercel</name>
  <files>src/pages/Integrations.tsx</files>
  <action>
    Fazer git add e commit com mensagem clara, depois push para o branch principal.

    Comando:
    ```
    git add src/pages/Integrations.tsx
    git commit -m "fix(oauth): corrige redirect_uri para ambiente dev

    Substitui https://analytics.alcavie.com/integracoes por
    https://marketplace-analytics-pro-dev.vercel.app/integracoes
    nas duas ocorrências em Integrations.tsx (exchangeCode e handleConnect)."
    git push
    ```

    Aguardar confirmação de que o push foi aceito. O rebuild na Vercel é automático via
    webhook do GitHub — não é necessário acionar manualmente.
  </action>
  <verify>
    Executar `git log --oneline -1` para confirmar o commit.
    Aguardar ~2 minutos e acessar https://marketplace-analytics-pro-dev.vercel.app/integracoes
    via curl para confirmar que o novo bundle está ativo:
    `curl -s https://marketplace-analytics-pro-dev.vercel.app/integracoes | grep -c "marketplace-analytics-pro-dev"`
    Deve retornar 1+ (o bundle JS vai conter a string).
  </verify>
  <done>
    Commit no git com a correção do redirect URI.
    Build deployado na Vercel com o novo bundle.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "alcavie.com" src/pages/Integrations.tsx` → zero resultados
2. `grep -n "marketplace-analytics-pro-dev" src/pages/Integrations.tsx` → exatamente 2 linhas
3. `git log --oneline -1` → mostra commit de correção
4. Deploy na Vercel completado (observar output do push ou checar dashboard Vercel)
</verification>

<success_criteria>
- Arquivo Integrations.tsx não contém mais nenhuma referência a `analytics.alcavie.com`
- O redirect_uri enviado ao ML em ambas as chamadas (get_auth_url e exchange_code) é `https://marketplace-analytics-pro-dev.vercel.app/integracoes`
- O deploy na Vercel Dev está atualizado com essa mudança
</success_criteria>

<output>
Após completion, criar `.planning/phases/11-oauth-secrets/11-01-SUMMARY.md` com:
- O que foi feito
- Resultado dos greps de verificação
- Status do deploy Vercel
</output>
