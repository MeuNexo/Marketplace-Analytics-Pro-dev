---
phase: 11-oauth-secrets
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: []
autonomous: false

user_setup:
  - service: mercadolivre
    why: "OAuth requer ML_APP_ID e ML_CLIENT_SECRET configurados como secrets no Supabase Dev"
    env_vars:
      - name: ML_APP_ID
        source: "Painel do app Nexo em developers.mercadolibre.com.br → Detalhes do app → App ID"
      - name: ML_CLIENT_SECRET
        source: "Painel do app Nexo em developers.mercadolibre.com.br → Detalhes do app → Secret key"
    dashboard_config:
      - task: "Adicionar redirect URL autorizado no app ML Nexo"
        location: "developers.mercadolibre.com.br → app Nexo → Configurações → Redirect URLs → adicionar https://marketplace-analytics-pro-dev.vercel.app/integracoes"
      - task: "Configurar ML_APP_ID como secret no Supabase Dev"
        location: "supabase.com → projeto ckcdevcxgvueywivefgx → Edge Functions → Secrets → Add new secret"
      - task: "Configurar ML_CLIENT_SECRET como secret no Supabase Dev"
        location: "supabase.com → projeto ckcdevcxgvueywivefgx → Edge Functions → Secrets → Add new secret"

must_haves:
  truths:
    - O usuário wesleysantos@pevermeio.com consegue clicar "Conectar Mercado Livre" em /integracoes, autorizar no ML e retornar com conta conectada
    - A tabela ml_tokens tem um registro com user_id do Wesley e ml_user_id da conta Pé Vermeio após o OAuth
    - A edge function ml-token-refresh invocada manualmente renova o token sem erro
  artifacts:
    - Registro em ml_tokens (Supabase Dev ckcdevcxgvueywivefgx) com user_id = ce8c797c-f984-4abb-b5f1-3e2f2eecbb73
  key_links:
    - ML_APP_ID ausente → ml-oauth retorna 500 antes de gerar auth URL
    - redirect_uri não registrado no ML → ML retorna "redirect_uri mismatch" após autorização
    - ml_tokens sem registro → dashboard não mostra dados mesmo com token válido
---

<objective>
Configurar os secrets de produção do app ML Nexo no Supabase Dev e validar o fluxo OAuth
end-to-end com a conta real da Pé Vermeio.

Este plano requer ações humanas que não podem ser automatizadas: obter as credenciais do
painel do ML Developers, configurá-las no Supabase Dev, e executar o fluxo OAuth no browser.

Purpose: Completar os pré-requisitos de configuração e validar que o OAuth funciona no dev.
Output: Token da Pé Vermeio salvo em ml_tokens no Supabase Dev; OAuth validado end-to-end.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-oauth-secrets/11-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Configurar secrets ML no Supabase Dev e redirect URL no app ML</name>
  <action>
    AÇÃO MANUAL NECESSÁRIA — Claude não consegue acessar painéis externos para obter credenciais.

    Passo 1 — Obter credenciais do app Nexo ML:
    - Acesse https://developers.mercadolibre.com.br/
    - Entre com a conta da Pé Vermeio
    - Abra o app "Nexo" (o app já existente que usamos no projeto Nexo)
    - Anote o APP_ID e o Secret Key

    Passo 2 — Adicionar redirect URL ao app ML Nexo:
    - No mesmo painel do app Nexo, vá em Configurações (ou "URLs de redirecionamento")
    - Adicione: `https://marketplace-analytics-pro-dev.vercel.app/integracoes`
    - Salve

    Passo 3 — Configurar secrets no Supabase Dev (projeto ckcdevcxgvueywivefgx):
    - Acesse https://supabase.com/dashboard/project/ckcdevcxgvueywivefgx/settings/edge-functions
    - Adicione o secret `ML_APP_ID` com o valor obtido no Passo 1
    - Adicione o secret `ML_CLIENT_SECRET` com o Secret Key obtido no Passo 1
    - Salve ambos

    Ao confirmar esta task, informe os valores configurados (pode omitir o secret, só confirmar que foi feito).
  </action>
  <verify>
    Confirmar com o usuário:
    - [ ] redirect URL adicionado no painel ML Developers
    - [ ] ML_APP_ID configurado no Supabase Dev
    - [ ] ML_CLIENT_SECRET configurado no Supabase Dev
  </verify>
  <done>
    Usuário confirmou que os três passos foram concluídos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar que a edge function ml-oauth responde com auth URL</name>
  <action>
    Invocar a edge function ml-oauth via MCP Supabase para confirmar que ela consegue
    gerar uma auth URL usando os secrets recém-configurados.

    Usar a ferramenta mcp__claude_ai_Supabase__execute_sql ou a ferramenta de invoke se
    disponível. Como alternativa, verificar via logs da edge function que não há erro de
    "ML_APP_ID not found".

    Se não houver ferramenta de invoke direta, verificar os logs mais recentes da edge function
    ml-oauth no Supabase Dev para confirmar que ela inicializou sem erro de configuração:
    - Projeto: ckcdevcxgvueywivefgx
    - Função: ml-oauth
    - Verificar: logs não contêm "ML_APP_ID" ou "ML_CLIENT_SECRET" como undefined/missing
  </action>
  <verify>
    Logs da edge function ml-oauth (últimas invocações ou cold start) não contêm erros de
    secret não encontrado.
  </verify>
  <done>
    Edge function ml-oauth está configurada e pronta para receber requests OAuth.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Executar fluxo OAuth end-to-end e confirmar token salvo</name>
  <action>
    VERIFICAÇÃO MANUAL NECESSÁRIA — o fluxo OAuth requer interação em browser.

    Passo 1 — Acesse https://marketplace-analytics-pro-dev.vercel.app/integracoes
    - Faça login como wesleysantos@pevermeio.com (senha: Wesley6873*)
    - Na tela de Integrações, localize o card do Mercado Livre

    Passo 2 — Inicie o OAuth:
    - Clique em "Conectar Mercado Livre"
    - Você será redirecionado para o ML para autorizar o app Nexo
    - Autorize o app com a conta da Pé Vermeio

    Passo 3 — Verifique o retorno:
    - Você deve retornar para /integracoes com a mensagem "Mercado Livre conectado!"
    - O card do ML deve mostrar o status "conectado" ou similar

    Passo 4 — Confirme o token no banco:
    - Após o OAuth, Claude verificará automaticamente via SQL se o token foi salvo

    Confirme quando o fluxo OAuth foi concluído com sucesso (ou descreva o erro se falhou).
  </action>
  <verify>
    Após confirmação do usuário, executar SQL no Supabase Dev para verificar o token:

    ```sql
    SELECT id, user_id, ml_user_id, created_at,
           LEFT(access_token, 20) as token_prefix,
           expires_at
    FROM ml_tokens
    WHERE user_id = 'ce8c797c-f984-4abb-b5f1-3e2f2eecbb73'
    ORDER BY created_at DESC
    LIMIT 1;
    ```

    Deve retornar 1 linha com ml_user_id preenchido e token não nulo.
  </verify>
  <done>
    - Usuário confirmou que o OAuth completou sem erro no browser
    - SQL confirma registro em ml_tokens com user_id do Wesley e ml_user_id da Pé Vermeio
    - Token tem expires_at no futuro (não expirado)
  </done>
</task>

</tasks>

<verification>
1. SQL query em ml_tokens retorna registro válido para user_id = ce8c797c-f984-4abb-b5f1-3e2f2eecbb73
2. ml_user_id corresponde à conta da Pé Vermeio no ML
3. access_token não nulo, expires_at no futuro
4. (Opcional) Invocar ml-token-refresh manualmente via MCP para confirmar OAUTH-04
</verification>

<success_criteria>
- OAUTH-01: ML_APP_ID e ML_CLIENT_SECRET configurados no Supabase Dev ckcdevcxgvueywivefgx ✓
- OAUTH-02: URL https://marketplace-analytics-pro-dev.vercel.app/integracoes é redirect URL válido no app ML Nexo ✓
- OAUTH-03: Fluxo OAuth completo funciona — usuário autorizou, token salvo em ml_tokens ✓
- OAUTH-04: ml-token-refresh consegue renovar o token (verificação secundária) ✓
</success_criteria>

<output>
Após completion, criar `.planning/phases/11-oauth-secrets/11-02-SUMMARY.md` com:
- Confirmação de cada requirement OAUTH-01 a OAUTH-04
- ml_user_id da conta Pé Vermeio (para uso nas fases 2 e 3)
- Resultado do SQL de verificação do ml_tokens
- Status geral da Phase 1
</output>
