---
phase: 44-monetizacao-stripe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260615120000_subscriptions_billing_events.sql
  - supabase/migrations/20260615120500_tier_price_mapping.sql
  - supabase/migrations/20260615121000_apply_subscription_tier_rpc.sql
  - supabase/migrations/20260615121500_history_days_enforcement.sql
  - src/integrations/supabase/types.ts
autonomous: false
requirements: [PAY-02, PAY-04]
must_haves:
  truths:
    - "Org member can read subscriptions/billing_events rows for their own org only (RLS)"
    - "Only the webhook EF (service_role) can write subscriptions/billing_events — no authenticated INSERT/UPDATE policy"
    - "apply_subscription_tier atomically UPSERTs subscriptions and UPDATEs organization_plans tier+sync_interval_minutes+history_days from tier_prices mapping"
    - "billing_events.event_id is UNIQUE so duplicate Stripe events are rejected (dedupe)"
    - "history_days enforcement floor is computable server-side via org_history_floor(org_id)"
  artifacts:
    - path: "supabase/migrations/20260615120000_subscriptions_billing_events.sql"
      provides: "subscriptions + billing_events tables, RLS, indexes"
      contains: "CREATE TABLE"
    - path: "supabase/migrations/20260615120500_tier_price_mapping.sql"
      provides: "tier_prices(price_id PK -> plan_tier, sync_interval_minutes, history_days) + seed"
      contains: "tier_prices"
    - path: "supabase/migrations/20260615121000_apply_subscription_tier_rpc.sql"
      provides: "apply_subscription_tier SECURITY DEFINER RPC"
      contains: "apply_subscription_tier"
    - path: "supabase/migrations/20260615121500_history_days_enforcement.sql"
      provides: "org_history_floor + history_days enforcement (per A4 decision)"
      contains: "org_history_floor"
    - path: "src/integrations/supabase/types.ts"
      provides: "hand-edited Row/Insert/Update blocks for subscriptions, billing_events, tier_prices"
      contains: "subscriptions"
  key_links:
    - from: "apply_subscription_tier"
      to: "organization_plans"
      via: "UPDATE plan_tier/sync_interval_minutes/history_days from tier_prices"
      pattern: "UPDATE.*organization_plans"
    - from: "apply_subscription_tier"
      to: "tier_prices"
      via: "lookup limits by price_id"
      pattern: "tier_prices"
    - from: "check_quota (Phase 43)"
      to: "organization_plans.sync_interval_minutes"
      via: "unchanged — keeps working after tier change"
      pattern: "sync_interval_minutes"
---

<objective>
Build the database foundation for Stripe monetization: the `subscriptions` and `billing_events` tables (local mirror + idempotency), the `tier_prices` mapping table, the atomic `apply_subscription_tier` RPC (the single source of truth that the webhook calls), and the server-side `history_days` enforcement primitive. Hand-edit `types.ts` to match. This plan also resolves the two product decisions (PAY-04 tier limits A2 + enforcement strategy A4) via a blocking checkpoint, because the migrations encode those values.

Purpose: Phase 43 already delivered `organization_plans` + `check_quota` (sync_interval_minutes enforcement). PAY-02 needs tables to mirror Stripe state and an atomic tier-change RPC; PAY-04 needs server-side history_days enforcement. Everything downstream (the EFs in 44-02, the /planos page in 44-03) depends on these tables and the RPC existing.
Output: 4 migrations applied to ckcdevcxgvueywivefgx + updated types.ts.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-monetizacao-stripe/44-RESEARCH.md
@.planning/phases/44-monetizacao-stripe/44-PATTERNS.md
@supabase/migrations/20260519120000_organization_plans_quota.sql
@supabase/migrations/20260614122000_tenant03_check_quota_rpc.sql
@supabase/migrations/20260614123000_tenant04_onboarding_progress.sql
</context>

<artifacts_this_phase_produces>
This plan produces:
- TABLE `public.subscriptions` (org-scoped mirror of Stripe subscription)
- TABLE `public.billing_events` (event_id UNIQUE dedupe + payload audit)
- TABLE `public.tier_prices` (price_id -> plan_tier, sync_interval_minutes, history_days)
- RPC `public.apply_subscription_tier(...)` (SECURITY DEFINER, service_role only)
- FUNCTION `public.org_history_floor(uuid)` (STABLE SECURITY DEFINER)
- Migration `20260615121500_history_days_enforcement.sql` (RLS-by-date and/or RPC clamp per A4)
- types.ts blocks: subscriptions, billing_events, tier_prices (hand-edited)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide tier limits (A2) and history_days enforcement strategy (A4)</name>
  <decision>
  Two product decisions must be fixed before the migrations are written, because the migrations hard-encode them.

  (A2) The per-tier limits for `sync_interval_minutes` and `history_days`. Known: free = 1440 / 30, enterprise = -1 / -1 (unlimited). UNKNOWN and required: starter and pro values.

  (A4) The enforcement mechanism for `history_days` (PAY-04 says "applied for real" — frontend-only fails verification).
  </decision>
  <context>
  These come straight from RESEARCH Assumptions A2/A4 and are flagged as needing Wesley's input. Do NOT invent values — the seed in tier_prices and the enforcement migration depend on the answers. The frontend date-picker clamp (44-03) is UX only; the real ceiling must be server-side.
  </context>
  <options>
    <option id="a4-rls">
      <name>A4 = RLS-by-date on cache tables</name>
      <pros>Database refuses out-of-range rows even if a client calls Supabase directly. Strongest guarantee (passes Pitfall 7 trivially).</pros>
      <cons>Adds a STABLE function call (org_history_floor) to the USING clause of every SELECT on ml_daily_cache / ml_hourly_cache / ml_product_daily_cache / ml_state_daily_cache — cost on every cache query.</cons>
    </option>
    <option id="a4-rpc">
      <name>A4 = RPC clamp only (effective_date_from) — NOT SELECTABLE: FAILS PAY-04</name>
      <pros>Cheap; no per-query RLS overhead.</pros>
      <cons>FORBIDDEN for this phase. mlCacheService.fetchScoped ALWAYS issues a user_id-scoped query first (mlCacheService.ts:41), and the 44-03 clamp skips the user_id path — so an RPC-only clamp would let a free org read pro-tier history via that first query. This fails success criterion 4 / PAY-04 ("aplicados de verdade"). Because Phase 43 made organization_id NOT NULL on all cache tables, a RESTRICTIVE RLS-by-date policy keyed on organization_id covers EVERY row regardless of the WHERE scope column — only rls/hybrid satisfy PAY-04. Do NOT select this option.</cons>
    </option>
    <option id="a4-hybrid">
      <name>A4 = Hybrid (RLS-by-date for real defense + frontend clamp for UX) — RESEARCH recommended</name>
      <pros>Real server-side defense plus good UX (date-picker limited to tier). Recommended in RESEARCH Pattern 5.</pros>
      <cons>Most work; RLS cost noted above.</cons>
    </option>
  </options>
  <resume-signal>Provide: starter sync_interval_minutes + history_days, pro sync_interval_minutes + history_days, and select A4 = a4-rls | a4-hybrid (a4-rpc is NOT selectable — it fails PAY-04 per the cons above; PAY-04 passes only with RESTRICTIVE RLS-by-date on organization_id). (Optionally confirm trial_period_days default and whether tier prices in R$ matter for test mode — A3/A5.)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Migrations — subscriptions + billing_events + tier_prices (model + RLS + seed)</name>
  <files>supabase/migrations/20260615120000_subscriptions_billing_events.sql, supabase/migrations/20260615120500_tier_price_mapping.sql</files>
  <read_first>
  - supabase/migrations/20260519120000_organization_plans_quota.sql (table+enum+RLS; service-role-only writes pattern at :52-61; reuse plan_tier enum :2-7 — do NOT recreate it)
  - supabase/migrations/20260614123000_tenant04_onboarding_progress.sql (org-first RLS member-select/owner-write :53-66; idempotency guards CREATE TABLE IF NOT EXISTS + DROP POLICY IF EXISTS + DO $$ EXCEPTION duplicate_object :28-50)
  </read_first>
  <action>
  Create migration `20260615120000_subscriptions_billing_events.sql` (PAY-02):
  - TABLE `public.subscriptions`: `organization_id uuid PRIMARY KEY REFERENCES public.organizations(id) ON DELETE CASCADE` (one subscription row per org), `stripe_customer_id text`, `stripe_subscription_id text`, `price_id text`, `status text NOT NULL DEFAULT 'inactive'`, `current_period_end timestamptz`, `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()`. Use the FK + PK shape copied from organization_plans:11.
  - TABLE `public.billing_events`: `id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY`, `event_id text NOT NULL UNIQUE` (Stripe event.id — dedupe key, insert-first idempotency per RESEARCH Pattern 3), `type text NOT NULL`, `organization_id uuid REFERENCES public.organizations(id) ON DELETE SET NULL` (nullable — some events arrive before org binding is resolvable), `payload jsonb`, `received_at timestamptz NOT NULL DEFAULT now()`.
  - Index `subscriptions_customer_idx` on `(stripe_customer_id)`; index `billing_events_org_received_idx` on `(organization_id, received_at DESC)`.
  - RLS on BOTH tables: ENABLE; add a member-SELECT policy via `public.is_org_member(auth.uid(), organization_id)` (copy organization_plans select :38-40); OMIT any INSERT/UPDATE policy for `authenticated` so only service_role (the webhook EF) writes — add the exact NOTE comment from organization_plans:60-61. billing_events SELECT can be member-scoped on organization_id (rows with NULL org_id are then invisible to authenticated, which is fine — audit-only).
  - Idempotency: CREATE TABLE IF NOT EXISTS; wrap the UNIQUE constraint and policies in DROP POLICY IF EXISTS / DO $$ EXCEPTION WHEN duplicate_object THEN NULL END $$ guards (pattern from onboarding_progress:37-50).

  Create migration `20260615120500_tier_price_mapping.sql` (PAY-04 mapping, Pitfall 6):
  - TABLE `public.tier_prices`: `price_id text PRIMARY KEY`, `plan_tier public.plan_tier NOT NULL` (REUSE existing enum, do not recreate), `sync_interval_minutes integer NOT NULL`, `history_days integer NOT NULL`, `created_at timestamptz NOT NULL DEFAULT now()`. CREATE TABLE IF NOT EXISTS.
  - RLS: ENABLE; SELECT policy `TO authenticated USING (true)` (read-only catalogue, no org scope — it is a price catalogue, not org data); OMIT write policies (service-role/migration only). Add NOTE comment.
  - Seed: insert one row per non-free tier using the limits decided in Task 1 (starter + pro from Wesley; enterprise = -1/-1). Use placeholder price_id values `'price_starter_PLACEHOLDER'`, `'price_pro_PLACEHOLDER'`, `'price_enterprise_PLACEHOLDER'` with `ON CONFLICT (price_id) DO UPDATE SET plan_tier=EXCLUDED.plan_tier, sync_interval_minutes=EXCLUDED.sync_interval_minutes, history_days=EXCLUDED.history_days` — these placeholders are replaced with the real Stripe price_ids during the 44-02 Dashboard-setup checkpoint (an UPDATE there swaps the price_id). free has no Stripe price (default plan), so no tier_prices row for free.
  Header comment block in each migration: rules + idempotency note, style from check_quota:1-20.
  </action>
  <verify>
    <automated>grep -q "event_id text NOT NULL UNIQUE" supabase/migrations/20260615120000_subscriptions_billing_events.sql && grep -q "is_org_member" supabase/migrations/20260615120000_subscriptions_billing_events.sql && grep -vq "FOR INSERT\|FOR UPDATE" <(grep -i "policy" supabase/migrations/20260615120000_subscriptions_billing_events.sql) && grep -q "tier_prices" supabase/migrations/20260615120500_tier_price_mapping.sql && grep -q "public.plan_tier" supabase/migrations/20260615120500_tier_price_mapping.sql && ! grep -q "CREATE TYPE public.plan_tier" supabase/migrations/20260615120500_tier_price_mapping.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
  Two migration files exist. subscriptions/billing_events have member-SELECT RLS and no authenticated write policy; billing_events.event_id is UNIQUE. tier_prices reuses the existing plan_tier enum (does not recreate it), is read-only to authenticated, and is seeded with starter/pro/enterprise rows using the Task-1 values. All idempotent.
  </acceptance_criteria>
  <done>Both migrations written, syntactically Postgres-valid, encoding the Task-1 decisions; no enum recreation; no authenticated write policy on webhook-owned tables.</done>
</task>

<task type="auto">
  <name>Task 3: Migrations — apply_subscription_tier RPC + history_days enforcement</name>
  <files>supabase/migrations/20260615121000_apply_subscription_tier_rpc.sql, supabase/migrations/20260615121500_history_days_enforcement.sql</files>
  <read_first>
  - supabase/migrations/20260614122000_tenant03_check_quota_rpc.sql (canonical SECURITY DEFINER atomic RPC: DEFINER + SET search_path :21-26; ON CONFLICT DO UPDATE :44-48; REVOKE ALL FROM PUBLIC + GRANT EXECUTE TO service_role :60-61; STABLE read of organization_plans :33-36)
  - supabase/migrations/20260519120000_organization_plans_quota.sql (organization_plans columns :10-17; SELECT RLS policy form :38-40)
  </read_first>
  <action>
  Create `20260615121000_apply_subscription_tier_rpc.sql` (PAY-02 source of truth):
  - `CREATE OR REPLACE FUNCTION public.apply_subscription_tier(_org_id uuid, _stripe_customer_id text, _stripe_subscription_id text, _price_id text, _status text, _current_period_end timestamptz DEFAULT NULL) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`.
  - Body:
    1. UPSERT into `subscriptions` ON CONFLICT (organization_id) DO UPDATE SET stripe_customer_id, stripe_subscription_id, price_id, status, current_period_end, updated_at = now() (atomic, idempotent, tolerant of out-of-order events — always writes the latest object state, no deltas — per Pitfall 3). When `_stripe_customer_id` is NULL, COALESCE to keep the existing value (subscription.updated events may omit it).
    2. Resolve limits: when `_price_id IS NULL` (cancellation/deleted), target tier = 'free' with sync_interval_minutes = 1440, history_days = 30 (the organization_plans defaults). Otherwise SELECT plan_tier, sync_interval_minutes, history_days INTO locals FROM tier_prices WHERE price_id = _price_id. If no mapping row found for a non-null price_id, RAISE EXCEPTION (fail loud — unknown price must not silently mis-tier).
    3. UPDATE organization_plans SET plan_tier = resolved_tier, sync_interval_minutes = resolved_sync, history_days = resolved_history, updated_at = now() WHERE organization_id = _org_id. (Do NOT touch check_quota — it reads organization_plans.sync_interval_minutes and keeps working unchanged.)
  - REVOKE ALL ON FUNCTION ... FROM PUBLIC; GRANT EXECUTE ... TO service_role (copy :60-61). Header comment block (rules + idempotency) style from check_quota:1-20.

  Create `20260615121500_history_days_enforcement.sql` (PAY-04, implements the A4 choice from Task 1):
  - Always create the helper `public.org_history_floor(_org_id uuid) RETURNS date LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public` returning `CASE WHEN history_days = -1 THEN '1900-01-01'::date ELSE current_date - history_days END FROM organization_plans WHERE organization_id = _org_id` (RESEARCH Pattern 5). REVOKE ALL FROM PUBLIC; GRANT EXECUTE TO authenticated, service_role (authenticated needs it for the RLS USING clause / clamp).
  - For A4 = a4-rls or a4-hybrid (the only valid choices — a4-rpc is forbidden, fails PAY-04): add a FOR SELECT policy named `<table>_history_floor` on each cache table that carries `organization_id` (ml_daily_cache, ml_hourly_cache, ml_product_daily_cache, ml_state_daily_cache) with `USING (date >= public.org_history_floor(organization_id))`. IMPORTANT: this is an ADDITIONAL restrictive consideration — Postgres combines multiple permissive SELECT policies with OR. To make it a true ceiling, create it as a RESTRICTIVE policy: `CREATE POLICY ... AS RESTRICTIVE FOR SELECT TO authenticated USING (date >= public.org_history_floor(organization_id))`. All four cache tables have organization_id NOT NULL since Phase 43, so key the policy on organization_id on every one (do NOT skip any — skipping reopens the PAY-04 hole). Confirm the column via grep of the existing migrations / types.ts before writing. Wrap each in DROP POLICY IF EXISTS guards.
  - RESTRICTIVE RLS-by-date is MANDATORY (do not ship an RPC-clamp-only build): mlCacheService.fetchScoped runs a user_id-scoped query first (mlCacheService.ts:41) that an RPC clamp would not cover, so the server-side ceiling must be RLS keyed on organization_id (NOT NULL on all cache tables since Phase 43). Additionally, for the 44-03 frontend UX clamp, create `public.effective_date_from(_org_id uuid, _requested_from date) RETURNS date LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public` returning `greatest(_requested_from, public.org_history_floor(_org_id))` and GRANT EXECUTE TO authenticated — this is a UX helper that complements (never replaces) the RESTRICTIVE RLS policies above.
  - Header comment must state which A4 option was chosen and why.
  </action>
  <verify>
    <automated>grep -q "FUNCTION public.apply_subscription_tier" supabase/migrations/20260615121000_apply_subscription_tier_rpc.sql && grep -q "SECURITY DEFINER" supabase/migrations/20260615121000_apply_subscription_tier_rpc.sql && grep -q "ON CONFLICT (organization_id)" supabase/migrations/20260615121000_apply_subscription_tier_rpc.sql && grep -q "UPDATE.*organization_plans\|UPDATE public.organization_plans" supabase/migrations/20260615121000_apply_subscription_tier_rpc.sql && grep -q "GRANT EXECUTE ON FUNCTION public.apply_subscription_tier" supabase/migrations/20260615121000_apply_subscription_tier_rpc.sql && grep -q "org_history_floor" supabase/migrations/20260615121500_history_days_enforcement.sql && grep -qi "AS RESTRICTIVE" supabase/migrations/20260615121500_history_days_enforcement.sql && [ "$(grep -ci "AS RESTRICTIVE" supabase/migrations/20260615121500_history_days_enforcement.sql)" -ge 4 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
  apply_subscription_tier is SECURITY DEFINER, UPSERTs subscriptions atomically, resolves limits from tier_prices (free fallback on NULL price), UPDATEs organization_plans, and is service_role-only. history_days_enforcement creates org_history_floor AND RESTRICTIVE RLS-by-date policies on every cache table keyed on organization_id (mandatory — a4-rpc-only is forbidden, fails PAY-04); effective_date_from is also created as the UX helper for the 44-03 clamp. check_quota is untouched.
  </acceptance_criteria>
  <done>Both migrations written; RPC atomic + idempotent + tolerant of event order; enforcement matches A4 decision and is server-side.</done>
</task>

<task type="auto">
  <name>Task 4: Hand-edit types.ts for subscriptions, billing_events, tier_prices</name>
  <files>src/integrations/supabase/types.ts</files>
  <read_first>
  - src/integrations/supabase/types.ts (the existing organization_plans block at :1267-1301 — Row/Insert/Update/Relationships shape to copy; Enums.plan_tier at :1628 and runtime Constants at :1761)
  </read_first>
  <action>
  Hand-edit (do NOT regenerate — project rule): add three table blocks inside `Database["public"]["Tables"]`, each with `Row`, `Insert`, `Update`, and `Relationships`, copying the exact structural shape of the organization_plans block (:1267-1301):
  - `subscriptions`: organization_id (string), stripe_customer_id (string | null), stripe_subscription_id (string | null), price_id (string | null), status (string), current_period_end (string | null), created_at (string), updated_at (string). Relationship FK organization_id -> organizations.id (copy the Relationships array shape from organization_plans).
  - `billing_events`: id (number), event_id (string), type (string), organization_id (string | null), payload (Json | null), received_at (string). Relationship FK organization_id -> organizations.id.
  - `tier_prices`: price_id (string), plan_tier (Database["public"]["Enums"]["plan_tier"]), sync_interval_minutes (number), history_days (number), created_at (string). No FK relationships ([]).
  Reuse the existing plan_tier enum reference for tier_prices.plan_tier exactly as organization_plans.Row does at :1272. Insert types make defaulted/nullable columns optional (string | null or `?`); Update types make all fields optional — match the organization_plans block conventions precisely. Do NOT add a new status enum (status is plain text in the table).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -iq "error" && { echo "TSC ERRORS"; exit 1; } || (grep -q "subscriptions:" src/integrations/supabase/types.ts && grep -q "billing_events:" src/integrations/supabase/types.ts && grep -q "tier_prices:" src/integrations/supabase/types.ts && echo OK)</automated>
  </verify>
  <acceptance_criteria>
  types.ts contains subscriptions, billing_events, and tier_prices table blocks with Row/Insert/Update/Relationships matching the organization_plans shape; tier_prices.plan_tier reuses the plan_tier enum; `tsc --noEmit` passes with no new errors.
  </acceptance_criteria>
  <done>Three blocks added by hand; type-check clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 5: [BLOCKING — ORCHESTRATOR] Apply the 4 migrations to ckcdevcxgvueywivefgx</name>
  <what-built>4 migrations: subscriptions/billing_events, tier_prices (seeded), apply_subscription_tier RPC, history_days enforcement + org_history_floor.</what-built>
  <how-to-verify>
  The gsd-executor has no Supabase MCP/deploy access (STATE.md 2026-06-14). The ORCHESTRATOR applies each migration via Supabase MCP `apply_migration` against project ref `ckcdevcxgvueywivefgx` (NOT gionpsuunfkkzzjdubfy — Pitfall 4), in filename order:
  1. 20260615120000_subscriptions_billing_events.sql
  2. 20260615120500_tier_price_mapping.sql
  3. 20260615121000_apply_subscription_tier_rpc.sql
  4. 20260615121500_history_days_enforcement.sql
  Then verify via execute_sql:
  - `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('subscriptions','billing_events','tier_prices');` → 3 rows.
  - `SELECT proname FROM pg_proc WHERE proname IN ('apply_subscription_tier','org_history_floor');` → 2 rows.
  - `SELECT price_id, plan_tier, sync_interval_minutes, history_days FROM tier_prices ORDER BY plan_tier;` → starter/pro/enterprise rows with the Task-1 values.
  - Run Supabase advisors (security) — no new critical RLS-disabled finding on the 3 new tables.
  </how-to-verify>
  <resume-signal>Type "applied" once all 4 migrations succeed and the verification queries return the expected rows on ckcdevcxgvueywivefgx.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → Postgres (RLS) | Org members read subscriptions/billing_events; must NOT write tier |
| service_role (EF) → Postgres | Only path that mutates tier via apply_subscription_tier |
| cache query client → cache tables | history_days ceiling must hold even on direct Supabase reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44A-01 | Tampering | subscriptions/billing_events write | mitigate | No authenticated INSERT/UPDATE policy; only service_role writes (RESEARCH Pattern 3 / organization_plans precedent) |
| T-44A-02 | Elevation of Privilege | client sets own plan_tier | mitigate | Tier mutated only by apply_subscription_tier (service_role only via REVOKE/GRANT); authenticated cannot UPDATE organization_plans except via Phase-43 owner policy which does not change tier limits maliciously without a paid price_id |
| T-44A-03 | Information Disclosure | reading another org's billing | mitigate | RLS member-SELECT via is_org_member on organization_id |
| T-44A-04 | Information Disclosure | history beyond tier (PAY-04) | mitigate | org_history_floor + RESTRICTIVE RLS-by-date (A4 = rls/hybrid) — DB refuses old rows even on direct query (Pitfall 7) |
| T-44A-05 | Tampering | unknown/spoofed price_id mis-tiers org | mitigate | apply_subscription_tier RAISEs on unmapped non-null price_id; tier_prices is the only mapping authority |
| T-44A-06 | Repudiation | event audit gap | accept | billing_events stores event_id/type/payload/received_at for audit |
</threat_model>

<verification>
- 3 new tables exist on ckcdevcxgvueywivefgx with RLS enabled; only service_role writes subscriptions/billing_events.
- apply_subscription_tier + org_history_floor exist; apply_subscription_tier is service_role-only.
- tier_prices seeded with the Task-1 decided values.
- types.ts type-checks clean.
- Supabase advisors: no new critical finding.
</verification>

<success_criteria>
- PAY-02 foundation: tables + atomic tier RPC + dedupe key ready for the webhook EF (44-02).
- PAY-04 server-side enforcement primitive in place (org_history_floor + chosen mechanism).
- check_quota (Phase 43) still functions unchanged.
</success_criteria>

<output>
Create `.planning/phases/44-monetizacao-stripe/44-01-SUMMARY.md` when done — record the Task-1 decided values (starter/pro limits, A4 choice, trial default) and the applied migration filenames.
</output>
