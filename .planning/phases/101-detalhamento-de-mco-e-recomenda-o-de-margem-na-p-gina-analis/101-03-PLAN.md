---
phase: 101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis
plan: 03
type: execute
wave: 2
depends_on: ["101-01", "101-02"]
files_modified:
  - src/hooks/useMcoTargets.ts
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
autonomous: false
requirements: [D-01, D-03, D-04, D-05, D-08, D-10]
user_setup: []
must_haves:
  truths:
    - "A fixed card appears below the existing chart showing the per-unit MCO waterfall for the selected item/variation/period"
    - "The card reacts to selectedId/selectedSku/currentFrom/currentTo and the incluir-ads toggle"
    - "A semáforo badge uses the same mcoHealth band (5/9) as Phase 83 — no divergence"
    - "The user can edit 'Meta MCO%' and it persists across reloads (per item_id + sku sentinel)"
    - "Both recommendation levers (preço mínimo, ACOS-alvo) are always visible, even when MCO is healthy"
    - "Missing cost/tax renders the existing warning copy, never an invented number"
    - "The existing chart tooltip (Phase 79) is unchanged"
  artifacts:
    - path: "src/hooks/useMcoTargets.ts"
      provides: "useMcoTargets() — org-scoped fetch + optimistic upsert of ml_mco_targets"
      exports: ["useMcoTargets"]
    - path: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      provides: "new fixed MCO detail + recommendation card below the chart"
      contains: "computeWaterfallCard"
  key_links:
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/hooks/useMcoTargets.ts"
      via: "reads target for selected item_id+sku, upserts on edit"
      pattern: "useMcoTargets"
    - from: "src/hooks/useMcoTargets.ts"
      to: "public.ml_mco_targets"
      via: "supabase.from('ml_mco_targets') select/upsert scoped by currentOrg.id"
      pattern: "from\\(\"ml_mco_targets\"\\)"
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/lib/pricing/mcoRecommendation.ts"
      via: "computeMcoRecommendation(card, targetPct) for the 2 levers"
      pattern: "computeMcoRecommendation"
---

<objective>
Wire the persistence hook and render the new fixed card in `PrecoPraticadoReport.tsx`: the per-unit MCO waterfall (D-01/D-02), the semáforo badge from `mcoHealth` (D-04), an editable "Meta MCO%" per item persisted via `useMcoTargets` (D-05), and both always-visible recommendation levers (D-07/D-08) — reusing the pure utils from Plan 02 and the table from Plan 01. The existing chart tooltip stays untouched (D-03). No minimum-sample gate (D-10).

Purpose: deliver the two user-facing features of the phase, composing only already-built, tested pieces.
Output: `useMcoTargets` hook + extended component, following the locked UI-SPEC contract.
</objective>

<artifacts_produced>
Symbols this plan creates:
- `useMcoTargets()` hook (in `src/hooks/useMcoTargets.ts`) — returns `{ targets: Map<string, number>, keyOf, upsert, refetch }`
- New module-scope `Row` component (moved out of `ChartTooltip`) reused by tooltip + card in `PrecoPraticadoReport.tsx`
- New MCO detail + recommendation `Card` block rendered below the chart in `PrecoPraticadoReport.tsx`
- Inline-edit "Meta MCO%" input (InlineEditCell-style, onBlur/Enter, client validation >0 and ≤100 with sonner toast)
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-CONTEXT.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-RESEARCH.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-PATTERNS.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-UI-SPEC.md
@src/hooks/useMLProductCosts.ts
@src/lib/mcoHealth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useMcoTargets hook (org-scoped fetch + optimistic upsert)</name>
  <read_first>
    - src/hooks/useMLProductCosts.ts (analog to mirror 1:1 — imports, fetchAll by org, optimistic upsert, onConflict pattern)
    - .planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-PATTERNS.md (full useMcoTargets implementation to copy, lines 142-176)
    - .planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-RESEARCH.md (Code Examples — useMcoTargets, lines 362-399)
  </read_first>
  <files>src/hooks/useMcoTargets.ts</files>
  <action>
    Create `useMcoTargets` mirroring `useMLProductCosts` structure. State: `targets: Map<string, number>` keyed by `keyOf(itemId, sku) = ` item_id + "::" + (sku ?? ""). `fetchAll`: `supabase.from("ml_mco_targets").select("item_id, sku, target_mco_pct").eq("organization_id", currentOrg.id).limit(10000)`, build the Map (Number(target_mco_pct)); run in a useEffect on `[fetchAll]`. `upsert(itemId, sku, pct)`: optimistic Map set, then `supabase.from("ml_mco_targets").upsert({ organization_id: currentOrg.id, item_id: itemId, sku: sku ?? "", target_mco_pct: pct, updated_by: user?.id ?? null }, { onConflict: "organization_id,item_id,sku" })`. Always normalize `sku ?? ""` before read/write (Pitfall 4 — never pass null as the sentinel). Guard on `currentOrg` / `currentOrg?.id` (no-op + console.warn if absent), same as the analog. Return `{ targets, keyOf, upsert, refetch: fetchAll }`.
    Do NOT add a delete path (out of scope — clearing a custom target is not required this phase). Do NOT create an RPC. Note: `supabase.from("ml_mco_targets")` type-checks without adding the table to types.ts (established project decision, Phase 90-04) — do not regenerate types.ts.
  </action>
  <verify>
    <automated>grep -q "export function useMcoTargets" src/hooks/useMcoTargets.ts && grep -q "from(\"ml_mco_targets\")" src/hooks/useMcoTargets.ts && grep -q "organization_id,item_id,sku" src/hooks/useMcoTargets.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `useMcoTargets` exported; returns `{ targets, keyOf, upsert, refetch }`
    - Reads/writes `ml_mco_targets` scoped by `currentOrg.id`; onConflict `organization_id,item_id,sku`
    - `sku` normalized to `""` on both read and write (no null passed)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Hook fetches targets by org and upserts optimistically with the sentinel-safe key, type-checks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the MCO detail + recommendation card in PrecoPraticadoReport.tsx</name>
  <read_first>
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx (read fully: `ChartTooltip`/`Row` lines 113-162, warning footer lines 822-838, existing Card/CardContent blocks, state `selectedId`/`selectedSku`/`currentFrom`/`currentTo`/`incluirAds`, existing formatters intFmt/pctFmt/currency)
    - src/pages/mercadolivre/MLAnuncios.tsx (InlineEditCell pattern, lines 156-196 — onBlur/Enter, no debounce)
    - src/lib/mcoHealth.ts (classifyMcoHealth, mcoHealthRole, MCO_SAUDAVEL_PCT)
    - .planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-UI-SPEC.md (Card Layout section 1-5 + Copywriting Contract — the locked visual/interaction/copy contract)
    - .planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-PATTERNS.md (component section — Row move to module scope, warning footer copy, card wrapper)
  </read_first>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <behavior>
    - Card renders below the chart, driven by computeWaterfallCard(rows, opts) for the currently selected item/sku/period with the incluirAds toggle honored
    - Waterfall rows appear in fixed order: Receita/un → (−) CMV → (−) Comissão → (−) Frete → (−) Impostos → = Margem de Contribuição/un → (−) Ads (only when incluirAds ON) → = MCO/un
    - Semáforo badge shows pctFmt(mcoPct) colored via mcoHealthRole(classifyMcoHealth(mcoPct)) — value always shown alongside color (never color alone)
    - Meta MCO% input pre-fills the custom target if set (from useMcoTargets for this item_id+sku), else shows helper "Usando padrão do semáforo (≥ 9% saudável)"; editing persists via upsert; entering 0, negative, or >100 shows sonner toast "Meta precisa ser maior que 0% e até 100%" and does not save
    - Both recommendation levers always visible (D-08): "Preço mínimo para a meta: R$ Y,YY" (or "Meta impraticável com os custos atuais deste item" when precoMinimo null), and "ACOS-alvo da campanha (mantendo o preço atual): Z,Z%" (or "Meta inatingível mesmo sem gastar em ads" when acosInatingivel)
    - custoAusente/impostoAusente → existing warning footer copy (verbatim from lines 828-838); no invented number
    - No sales in the selected period → empty-state copy from UI-SPEC ("Sem vendas no período selecionado" + body), no waterfall
    - The existing ChartTooltip and its behavior are unchanged (D-03)
  </behavior>
  <action>
    Move the local `Row` component out of `ChartTooltip` to module scope (per PATTERNS.md Code Examples) so both the tooltip and the new card reuse it — do not duplicate the row JSX. Import `computeWaterfallCard` from `@/lib/precoMcoSeries`, `computeMcoRecommendation` from `@/lib/pricing/mcoRecommendation`, `classifyMcoHealth`/`mcoHealthRole`/`MCO_SAUDAVEL_PCT` from `@/lib/mcoHealth`, and `useMcoTargets` from `@/hooks/useMcoTargets`. Call the hook at component top; resolve `targetPct = targets.get(keyOf(selectedId, selectedSku)) ?? MCO_SAUDAVEL_PCT.green`. Compute `card = computeWaterfallCard(rows, opts)` and `rec = computeMcoRecommendation(card, targetPct)` from the same `rows`/`opts` the chart already builds (reuse existing memoized data — do not refetch). Render a `<Card className="mt-6"><CardContent>` block following UI-SPEC Card Layout order 1-5: header (Target icon accent + "Detalhamento de MCO" + semáforo Badge + period label), waterfall rows (reused `Row`, R$ and % side-by-side, `= Margem de Contribuição/un` and `= MCO/un` set apart via border-top + font-semibold, NOT a heavier weight), the "Meta MCO%:" inline-edit row (InlineEditCell-style onBlur/Enter, client validation rejecting 0/negative/>100 with a sonner toast, upsert on valid commit), the always-visible recommendation block (two lines, `text-xl font-semibold tabular-nums`, impossible states in `text-destructive text-xs`), and the conditional warning footer (verbatim existing copy). Only render the "(−) Ads" row when the `incluirAds` toggle is ON. Use only existing tokens (`--success`/`--warning`/`--destructive`/`--chart-margin-*`/`--accent`) — no new color tokens (UI-SPEC Color).
    Do NOT modify `ChartTooltip`'s output (D-03). Do NOT add side-by-side item comparison (deferred). Do NOT introduce a new debounce/save pattern or any new npm dependency.
    Add/extend a component test asserting the card renders the waterfall rows and both recommendation labels for a fixture with sales, and shows the empty-state copy for a period with zero sales.
  </action>
  <verify>
    <automated>grep -q "computeWaterfallCard" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx && grep -q "computeMcoRecommendation" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx && grep -q "useMcoTargets" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx && npx tsc --noEmit && npx vitest run src/components/mercadolivre/anuncios && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Card renders below the chart with all 8 waterfall rows (7 when incluirAds OFF) in the fixed order, each showing R$ + %
    - Semáforo badge uses mcoHealthRole/classifyMcoHealth with MCO_SAUDAVEL_PCT (grep confirms these imports/usages)
    - Meta MCO% input persists via useMcoTargets.upsert; invalid values (0/neg/>100) toast and do not save
    - Both recommendation lines always render (healthy and unhealthy) with impossible-case copy from UI-SPEC
    - custoAusente/impostoAusente reuse the existing warning footer copy; empty period shows the empty-state copy
    - `ChartTooltip` output unchanged (Row extracted but tooltip rendering identical)
    - `npx tsc --noEmit`, component vitest, and `npm run build` all pass
  </acceptance_criteria>
  <done>The fixed MCO detail + editable target + dual-lever recommendation card renders per the UI-SPEC contract, tooltip untouched, build/tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual approval on /analise-precos (Wesley ok visual)</name>
  <what-built>
    The new fixed "Detalhamento de MCO" card below the price×break-even chart on /analise-precos: per-unit waterfall, semáforo badge, editable "Meta MCO%", and the two always-visible recommendation levers (preço mínimo + ACOS-alvo).
  </what-built>
  <action>
    Orchestrator presents the built card to Wesley for visual approval on /analise-precos (preview or prod) following the how-to-verify steps; collect the ok visual or the list of fixes before closing the phase.
  </action>
  <how-to-verify>
    1. Open /analise-precos (preview or prod), pick an item with sales (e.g. a Pé Vermeio anúncio with real cost) in the selector; confirm the card shows the waterfall (Receita→CMV→Comissão→Frete→Impostos→MC→Ads→MCO) with R$ and % per line and the correct semáforo color/value.
    2. Toggle "incluir ads" off/on — the "(−) Ads" row and MCO/un update accordingly.
    3. Edit "Meta MCO%" (e.g. 12), press Enter, reload the page — the custom target persists; try 0 and 150 — both rejected with the toast.
    4. Confirm both recommendation lines are always visible, including for a healthy (🟢) item; the preço mínimo differs from the chart's break-even line when the meta ≠ 0.
    5. Check an item with missing cost — the warning footer appears and no fabricated number is shown. Check light + dark mode (CVD-safe palette).
    6. Confirm the existing chart tooltip (hover) is unchanged.
  </how-to-verify>
  <acceptance_criteria>
    - Wesley confirms the card reads correctly, target persists, both levers always show, warnings behave, and the tooltip is unchanged, in light + dark mode
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → Supabase (RLS) | Authenticated user reads/writes MCO% targets via useMcoTargets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-05 | Tampering (bad input) | Meta MCO% out of range | mitigate | Client validation rejects 0/negative/>100 with toast before upsert; DB CHECK (Plan 01) is the backstop |
| T-101-01 | Information Disclosure (IDOR) | reading another org's targets | mitigate | Hook always filters by `currentOrg.id` (session context, never a client-typed org); org-first RLS on the table enforces isolation server-side |
| T-101-04 | Tampering (correctness) | invented numbers on missing data | mitigate | custoAusente/impostoAusente warnings + null-handling from Plan 02 utils; never render NaN/Infinity |
| T-101-SC | Tampering | package installs | accept | No new dependencies added (RESEARCH) |
</threat_model>

<verification>
- `npx tsc --noEmit`, component vitest suite, and `npm run build` pass
- Manual: target persists across reload; both levers always visible; tooltip unchanged; light+dark OK
</verification>

<success_criteria>
`/analise-precos` shows the fixed MCO waterfall card with an editable, persisted per-item target and both recommendation levers always visible, reusing the mcoHealth band, with the existing tooltip and chart untouched.
</success_criteria>

<output>
Create `.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-03-SUMMARY.md` when done
</output>
