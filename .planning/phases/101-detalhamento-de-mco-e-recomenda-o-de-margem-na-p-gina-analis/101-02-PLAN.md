---
phase: 101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/precoMcoSeries.ts
  - src/lib/precoMcoSeries.test.ts
  - src/lib/pricing/mcoRecommendation.ts
  - src/lib/pricing/mcoRecommendation.test.ts
autonomous: true
requirements: [D-02, D-04, D-07]
must_haves:
  truths:
    - "The per-unit period-average waterfall (receita→CMV→comissão→frete→impostos→MC→ads→MCO) is derived once, purely, from the same rows/adsDaily the chart already uses"
    - "Minimum recommended price for a target MCO% is computed via the existing reversePrice algebra, not by reusing breakevenUnit"
    - "Target ACOS = margin-before-ads% minus target% ; null/≤0 handled as impossible"
    - "Missing cost/tax is surfaced as flags, never as an invented number"
  artifacts:
    - path: "src/lib/precoMcoSeries.ts"
      provides: "computeWaterfallCard() + WaterfallCard interface"
      contains: "export function computeWaterfallCard"
    - path: "src/lib/pricing/mcoRecommendation.ts"
      provides: "computeMcoRecommendation() — preço mínimo (via reversePrice) + acosMeta"
      contains: "export function computeMcoRecommendation"
  key_links:
    - from: "src/lib/precoMcoSeries.ts"
      to: "src/lib/mco.ts"
      via: "computeWaterfallCard reuses computeMco (single source of the MCO formula)"
      pattern: "computeMco"
    - from: "src/lib/pricing/mcoRecommendation.ts"
      to: "src/lib/pricing/calculator.ts"
      via: "reuses reversePrice() (mode margin) for the minimum-price lever"
      pattern: "reversePrice"
---

<objective>
Add the two pure, tested calculation building blocks the new card needs, with zero I/O and zero new dependencies: (1) `computeWaterfallCard` — the per-unit, period-average MCO waterfall (D-02); (2) `computeMcoRecommendation` — the two recommendation levers (D-07) that reuse `reversePrice` and one-line ACOS algebra.

Purpose: keep the MCO formula single-sourced (`computeMco`) and the price algebra single-sourced (`reversePrice`) — no duplication in the component, no reinventing break-even (Pitfall 1). All logic is deterministic and unit-tested (TDD) so the component in Plan 03 is pure presentation.
Output: two util files + their test suites, green.
</objective>

<artifacts_produced>
Symbols this plan creates:
- `WaterfallCard` interface (in `src/lib/precoMcoSeries.ts`) — fields: `precoUnit`, `cmvUnit`, `comissaoUnit`, `freteUnit`, `adsUnit`, `impostoUnit`, `mcUnit`, `mcoUnit`, `mcoPct`, `mcBeforeAdsPct`, `custoAusente`, `impostoAusente`
- `computeWaterfallCard(rows, opts): WaterfallCard` (in `src/lib/precoMcoSeries.ts`)
- `McoRecommendation` interface (in `src/lib/pricing/mcoRecommendation.ts`) — fields: `precoMinimo: number | null`, `acosMeta: number | null`, `metaImpraticavel: boolean`, `acosInatingivel: boolean`
- `computeMcoRecommendation(card: WaterfallCard, targetMcoPct: number): McoRecommendation` (in `src/lib/pricing/mcoRecommendation.ts`)
- Test files `src/lib/precoMcoSeries.test.ts` (extended) and `src/lib/pricing/mcoRecommendation.test.ts` (new)
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-RESEARCH.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-PATTERNS.md
@src/lib/precoMcoSeries.ts
@src/lib/mco.ts
@src/lib/mcoHealth.ts
@src/lib/pricing/calculator.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: computeWaterfallCard() — per-unit period-average waterfall</name>
  <read_first>
    - src/lib/precoMcoSeries.ts (read fully: `computePrecoMcoSeries`, `computePriceKpis` lines ~201-228, types `PrecoSeriesRow`/`McoSeriesPoint`/`ComputePrecoMcoSeriesOpts`, and how `qtd_sem_custo`/`qtd_sem_imposto` are named on the row type)
    - src/lib/mco.ts (`computeMco` signature: grossRevenue, cmv, platformCost, ads, tax → { mco, pct })
    - .planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-PATTERNS.md (Pattern 2 exact reference implementation, lines 210-254)
  </read_first>
  <files>src/lib/precoMcoSeries.ts, src/lib/precoMcoSeries.test.ts</files>
  <behavior>
    - Given rows with qtd>0, returns each component divided by total qtd: precoUnit = Σtotal/Σqtd, cmvUnit = Σcmv/Σqtd, comissaoUnit, freteUnit, impostoUnit, adsUnit = Σads(from serie)/Σqtd
    - mcUnit (margin before ads, per unit) = (Σreceita − Σcmv − Σcomissao − Σfrete − Σimpostos)/Σqtd
    - mcoUnit and mcoPct come from computeMco over the period totals (ads included) — mcoPct is the period MCO%, not an average of bucket %s
    - mcBeforeAdsPct = precoUnit>0 ? (mcUnit/precoUnit)*100 : null
    - qtd == 0 (no sales in period): every unit field is 0 and mcoPct/mcBeforeAdsPct handling matches computePriceKpis convention (no NaN/Infinity)
    - custoAusente = any row with qtd_sem_custo>0 ; impostoAusente = any row with qtd_sem_imposto>0 (use the actual field names found in PrecoSeriesRow)
    - `opts` respects the `incluirAds` toggle exactly as computePrecoMcoSeries already does (ads excluded from the serie when the toggle is off)
  </behavior>
  <action>
    RED: extend `src/lib/precoMcoSeries.test.ts` with a failing suite for `computeWaterfallCard` covering: (a) normal multi-row period (assert each per-unit value + mcoPct against hand-computed numbers); (b) qtd=0 empty period (all zeros, no NaN); (c) custoAusente true when a row has missing cost; (d) impostoAusente true when a row has missing tax; (e) incluirAds=false → adsUnit=0 and mcoUnit == mcUnit. Commit test-only (RED).
    GREEN: add `export interface WaterfallCard` and `export function computeWaterfallCard(rows, opts)` to `src/lib/precoMcoSeries.ts` following 101-PATTERNS.md Pattern 2 — reuse `computePrecoMcoSeries` for the ads-aware serie and `computeMco` for the MCO totals; do the same Σ reductions as `computePriceKpis`; guard every division with `qtd > 0`. Confirm the exact `PrecoSeriesRow` missing-cost/missing-tax field names from the file (do not assume — read them). Commit implementation (GREEN).
    Do NOT recompute the MCO formula inline; do NOT touch `src/lib/mco.ts` or `computePriceKpis`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/precoMcoSeries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `computeWaterfallCard` and `WaterfallCard` exported from `src/lib/precoMcoSeries.ts`
    - All new test cases (normal, empty, custoAusente, impostoAusente, incluirAds off) pass
    - Function uses `computeMco` (grep `computeMco` in the new function region) — no re-derived MCO formula
    - No NaN/Infinity for the qtd=0 case (asserted in tests)
  </acceptance_criteria>
  <done>computeWaterfallCard returns the 12-field per-unit waterfall, derived purely from rows+opts via computeMco, with tests green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: computeMcoRecommendation() — preço mínimo (reversePrice) + ACOS-alvo</name>
  <read_first>
    - src/lib/pricing/calculator.ts (read fully: `reversePrice` signature/params — cost, commissionPct, fixedFee, shippingCost, taxPct, difalEnabled, difalPct, rebate/cupom/afiliado/promo extras; the `if (target <= 0) return null` guard; and mode "margin")
    - .planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-PATTERNS.md ("Recommendation levers" block, lines 258-292)
    - .planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-RESEARCH.md (Pattern 3 mapping + Pitfall 1/Pitfall 2)
  </read_first>
  <files>src/lib/pricing/mcoRecommendation.ts, src/lib/pricing/mcoRecommendation.test.ts</files>
  <behavior>
    - commissionPct derived = card.precoUnit>0 ? (card.comissaoUnit/card.precoUnit)*100 : 0 ; taxPct derived similarly from impostoUnit
    - precoMinimo = reversePrice({ cost: card.cmvUnit, commissionPct, fixedFee: 0, shippingCost: card.freteUnit + card.adsUnit, taxPct, difalEnabled:false, difalPct:0, rebate/cupom/afiliado/promo all disabled }, targetMcoPct, "margin")
    - precoMinimo === null (reversePrice denom ≤ 0 or target ≤ 0) → metaImpraticavel = true
    - acosMeta = card.mcBeforeAdsPct != null ? card.mcBeforeAdsPct − targetMcoPct : null ; acosMeta <= 0 → acosInatingivel = true (do not surface a negative ACOS)
    - Never returns NaN/Infinity; when card.precoUnit is 0 the levers degrade gracefully (precoMinimo null / acos null)
  </behavior>
  <action>
    RED: create `src/lib/pricing/mcoRecommendation.test.ts` with failing cases: (a) healthy item, target 9% → assert precoMinimo is a finite number greater than precoUnit and that it is NOT equal to a naive break-even sum (Pitfall 1 guard — proves reversePrice used, not breakevenUnit); (b) target so high it is unreachable → precoMinimo null + metaImpraticavel true; (c) acosMeta positive when mcBeforeAdsPct > target; (d) acosMeta ≤ 0 → acosInatingivel true; (e) precoUnit=0 → both levers null, no throw. Commit RED.
    GREEN: create `src/lib/pricing/mcoRecommendation.ts` exporting `McoRecommendation` interface + `computeMcoRecommendation(card, targetMcoPct)` importing `reversePrice` from `@/lib/pricing/calculator` and `WaterfallCard` type from `@/lib/precoMcoSeries`. Follow the exact mapping in 101-PATTERNS.md. Commit GREEN.
    Do NOT reimplement the reversePrice algebra; do NOT modify `src/lib/pricing/calculator.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pricing/mcoRecommendation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `computeMcoRecommendation` + `McoRecommendation` exported from `src/lib/pricing/mcoRecommendation.ts`
    - Imports and calls `reversePrice` (grep `reversePrice`) — no re-derived price algebra
    - Test proves precoMinimo differs from a naive break-even sum for target ≠ 0 (Pitfall 1)
    - Unreachable target → null + metaImpraticavel; acosMeta ≤ 0 → acosInatingivel; precoUnit=0 → no throw
  </acceptance_criteria>
  <done>computeMcoRecommendation returns both levers with impossible-case flags, reusing reversePrice, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side calculation over already-fetched data; no I/O, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-04 | Tampering (correctness) | invented numbers when cost/tax missing | mitigate | custoAusente/impostoAusente flags surfaced instead of fabricated values; reversePrice returns null (not NaN) on impossible target — never render Infinity/NaN |
| T-101-SC | Tampering | package installs | accept | Zero new dependencies (RESEARCH) — no supply-chain surface |
</threat_model>

<verification>
- `npx vitest run src/lib/precoMcoSeries.test.ts src/lib/pricing/mcoRecommendation.test.ts` green
- `npx tsc --noEmit` passes (new exports type-check)
</verification>

<success_criteria>
Both pure utils exist, single-source the MCO formula and price algebra, and are fully unit-tested — Plan 03 can compose them without any calculation logic in the component.
</success_criteria>

<output>
Create `.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-02-SUMMARY.md` when done
</output>
