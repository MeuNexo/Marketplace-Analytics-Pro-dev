---
phase: 101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260719000000_ml_mco_targets.sql
autonomous: false
requirements: [D-06, D-09]
must_haves:
  truths:
    - "Table ml_mco_targets exists in prod (ckcdevcxgvueywivefgx) with org-first RLS"
    - "A custom MCO% target persisted for org A is invisible to org B (anti-IDOR)"
    - "target_mco_pct is constrained to > 0 and <= 100 at the DB level"
  artifacts:
    - path: "supabase/migrations/20260719000000_ml_mco_targets.sql"
      provides: "ml_mco_targets table + org-first RLS (clone of ml_product_costs) + unique(org,item_id,sku) + CHECK"
      contains: "CREATE TABLE public.ml_mco_targets"
  key_links:
    - from: "supabase/migrations/20260719000000_ml_mco_targets.sql"
      to: "public.is_org_member / public.get_org_role"
      via: "RLS policies reference org membership helpers (org-first, never a client org param)"
      pattern: "is_org_member|get_org_role"
---

<objective>
Create the `ml_mco_targets` config table (per-`item_id`, org-scoped, with optional `sku` sentinel) that persists the custom MCO% target for the recommendation card on `/analise-precos`, and apply it to production via MCP.

Purpose: D-05/D-06 require persisting a per-item MCO% goal; there is no existing table for it. D-09 locks the grain to `item_id` (with `sku=''` sentinel for the whole-listing case). This is the only new backend surface in the phase.
Output: migration file + table live in prod with org-first RLS proven anti-IDOR.
</objective>

<artifacts_produced>
Symbols this plan creates:
- Table `public.ml_mco_targets` — columns: `id uuid PK`, `organization_id uuid FK→organizations`, `item_id text`, `sku text NOT NULL DEFAULT ''`, `target_mco_pct numeric(5,2) CHECK (>0 AND <=100)`, `updated_by uuid NULL`, `updated_at timestamptz`
- Unique constraint `ml_mco_targets_org_item_sku_unique (organization_id, item_id, sku)`
- Index `idx_ml_mco_targets_org_item (organization_id, item_id, sku)`
- RLS policies `mmt_select` (any org member), `mmt_insert` / `mmt_update` / `mmt_delete` (owner/admin/member only)
- Migration file `supabase/migrations/20260719000000_ml_mco_targets.sql`
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-CONTEXT.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-RESEARCH.md
@.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-PATTERNS.md
@supabase/migrations/20260514120000_ml_product_costs.sql
@supabase/migrations/20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql
@supabase/migrations/20260662000000_replenishment_params.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ml_mco_targets migration (table + org-first RLS + sentinel sku)</name>
  <read_first>
    - supabase/migrations/20260614120000_tenant01_ml_product_costs_rls_orgfirst.sql (RLS org-first policies to clone 1:1 — mpc_select/insert/update/delete, lines 46-93)
    - supabase/migrations/20260514120000_ml_product_costs.sql (base table shape)
    - supabase/migrations/20260662000000_replenishment_params.sql (sentinel `''` pattern for optional scope column)
    - .planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-PATTERNS.md (exact CREATE TABLE + RLS blocks to copy verbatim, lines 26-83)
  </read_first>
  <files>supabase/migrations/20260719000000_ml_mco_targets.sql</files>
  <action>
    Create the migration exactly as specified in 101-PATTERNS.md "Pattern Assignments → migration" block. Table `public.ml_mco_targets` with columns `id` (uuid PK default gen_random_uuid()), `organization_id` (uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE), `item_id` (text NOT NULL), `sku` (text NOT NULL DEFAULT '' — sentinel for whole-listing; NEVER nullable, per Pitfall 4), `target_mco_pct` (numeric(5,2) NOT NULL CHECK (target_mco_pct > 0 AND target_mco_pct <= 100)), `updated_by` (uuid NULL, audit only — not referenced in RLS), `updated_at` (timestamptz NOT NULL DEFAULT now()). Add UNIQUE constraint `ml_mco_targets_org_item_sku_unique (organization_id, item_id, sku)`.
    Enable RLS and create four policies cloned 1:1 from `ml_product_costs` org-first RLS, renamed to `mmt_select`/`mmt_insert`/`mmt_update`/`mmt_delete`: SELECT `TO authenticated USING (organization_id IS NOT NULL AND public.is_org_member(auth.uid(), organization_id))`; INSERT/UPDATE/DELETE gated on `public.get_org_role(auth.uid(), organization_id) = ANY (ARRAY['owner','admin','member']::public.org_role[])` (never viewer). UPDATE has both USING and WITH CHECK. Add `CREATE INDEX IF NOT EXISTS idx_ml_mco_targets_org_item ON public.ml_mco_targets (organization_id, item_id, sku)`.
    Do NOT create any RPC — writes go through direct `supabase.from(...).upsert(...)` (RESEARCH anti-pattern: neither ml_product_costs nor replenishment_params uses an RPC for simple config writes). SECURITY DEFINER is forbidden here. The `organization_id` in RLS always comes from the session via is_org_member/get_org_role — never a client-supplied org parameter (anti-IDOR).
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE public.ml_mco_targets" supabase/migrations/20260719000000_ml_mco_targets.sql && grep -q "target_mco_pct > 0 AND target_mco_pct <= 100" supabase/migrations/20260719000000_ml_mco_targets.sql && grep -Eq "sku[[:space:]]+text[[:space:]]+NOT NULL DEFAULT ''" supabase/migrations/20260719000000_ml_mco_targets.sql && grep -c "get_org_role" supabase/migrations/20260719000000_ml_mco_targets.sql | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260719000000_ml_mco_targets.sql` exists
    - Contains `CREATE TABLE public.ml_mco_targets` with the 7 columns above
    - `sku` is `text NOT NULL DEFAULT ''` (no nullable sku)
    - CHECK constraint `target_mco_pct > 0 AND target_mco_pct <= 100` present
    - UNIQUE `(organization_id, item_id, sku)` present
    - Four RLS policies present; INSERT/UPDATE/DELETE reference `get_org_role(...) = ANY(ARRAY['owner','admin','member'])`; SELECT references `is_org_member`
    - No `CREATE FUNCTION` / no `SECURITY DEFINER` in the file
  </acceptance_criteria>
  <done>Migration file written matching the ml_product_costs org-first RLS shape, sentinel sku, CHECK, and unique key; no RPC/DEFINER.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration via MCP + anti-IDOR smoke test as role authenticated</name>
  <what-built>
    The `20260719000000_ml_mco_targets.sql` migration (table + org-first RLS). This checkpoint applies it to prod because the executor has no Supabase MCP/CLI token — the orchestrator applies it via MCP.
  </what-built>
  <action>
    Orchestrator applies the migration to project `ckcdevcxgvueywivefgx` via MCP `mcp__claude_ai_Supabase__apply_migration` (never CLI db push, never SQL Editor), then runs the anti-IDOR + CHECK smoke test under role `authenticated` as described in how-to-verify. Do not proceed to Plan 03 until the table exists and the smoke test passes.
  </action>
  <how-to-verify>
    1. Confirm the max applied migration version in prod BEFORE applying (MCP live uses real timestamp; check `SELECT max(version) FROM supabase_migrations.schema_migrations`) — ensure `20260719000000` sorts after it; if a later version already exists, bump the filename timestamp accordingly and re-run Task 1's grep gate.
    2. Apply the migration with MCP `mcp__claude_ai_Supabase__apply_migration` on project `ckcdevcxgvueywivefgx` (NEVER `supabase db push`, NEVER the SQL Editor — drift is forbidden per CONTEXT.md canonical_refs).
    3. Verify the table exists: `SELECT to_regclass('public.ml_mco_targets')` returns non-null; `list_tables` shows RLS enabled.
    4. Anti-IDOR smoke test as role `authenticated` (not postgres — service_role bypasses RLS): in a single ROLLBACK transaction, `SET LOCAL ROLE authenticated` + `set_config('request.jwt.claims', ...)` impersonating a Pé Vermeio user (org `7f615df7-7bac-45e5-8a93-827fb9ddeec7`), INSERT one target row (item_id='TEST-MMT', sku='', target_mco_pct=12), then re-impersonate a Thales user (org `e4150d57-1349-48c9-9a89-82b1774857b0`) and SELECT — must return 0 rows for the Pé Vermeio target. Then confirm the CHECK: INSERT with target_mco_pct=0 must fail (23514), and 101 must fail. ROLLBACK the whole probe (no test data persisted).
    5. Do NOT complete UUIDs from a prefix — always `SELECT id, name FROM organizations` to get the real org IDs before impersonating (a non-existent org also returns 0 and produces a false PASS).
  </how-to-verify>
  <acceptance_criteria>
    - `to_regclass('public.ml_mco_targets')` is non-null in prod
    - RLS enabled on the table (advisors show no rls_disabled_in_public for it)
    - Cross-org SELECT under role authenticated returns 0 rows for another org's target; same-org SELECT returns the row
    - INSERT with target_mco_pct=0 and =101 both rejected by CHECK (error 23514)
    - No test data left behind (probe ran in a ROLLBACK transaction)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the migration is applied and the anti-IDOR + CHECK smoke test passed, or report the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → Supabase (PostgREST/RLS) | Authenticated user reads/writes MCO% targets; org isolation enforced only by RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Information Disclosure / Tampering (IDOR) | ml_mco_targets read/write across orgs | mitigate | Org-first RLS cloned 1:1 from ml_product_costs (audited anti-IDOR in Phase 43 TENANT-01); `organization_id` sourced from session helpers, never a client param; proven by cross-org smoke test in Task 2 |
| T-101-02 | Tampering (data integrity) | duplicate targets via nullable sku | mitigate | `sku text NOT NULL DEFAULT ''` sentinel + UNIQUE(org,item_id,sku) prevents silent duplicates (Pitfall 4) |
| T-101-03 | Tampering (bad input) | out-of-range target_mco_pct breaking reversePrice algebra | mitigate | DB `CHECK (target_mco_pct > 0 AND target_mco_pct <= 100)`; client validation added in Plan 03 (defense in depth) |
| T-101-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase (RESEARCH: zero new dependencies) — no supply-chain surface |
</threat_model>

<verification>
- Migration applied in prod; table present with RLS enabled
- Anti-IDOR proven under role authenticated (cross-org = 0 rows)
- CHECK rejects 0 and >100
</verification>

<success_criteria>
`ml_mco_targets` live in ckcdevcxgvueywivefgx with org-first RLS proven anti-IDOR and CHECK enforced — ready for the hook in Plan 03 to read/write.
</success_criteria>

<output>
Create `.planning/phases/101-detalhamento-de-mco-e-recomenda-o-de-margem-na-p-gina-analis/101-01-SUMMARY.md` when done
</output>
