---
phase: 91-sino-da-navbar-marcar-notifica-es-como-lidas-badge-s-conta-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/bellSeen.ts
  - src/lib/bellSeen.test.ts
  - src/hooks/useBellSeen.ts
  - src/hooks/useAtendimentoPendencias.ts
  - src/components/layout/AtendimentoBell.tsx
autonomous: true
requirements: [BELL-01, BELL-02, BELL-03]

must_haves:
  truths:
    - "Na primeira visita (sem localStorage) o badge do sino começa em 0, mesmo com pendências antigas vivas"
    - "A semeadura só acontece DEPOIS que a query de pendências fica pronta (enabled + já buscou ao menos uma vez) — nunca durante a janela fria em que a query está desabilitada e items=[]"
    - "Quando chega uma pendência nova via refetch, o badge sobe para o nº de itens ainda não vistos"
    - "Abrir o popover do sino zera o badge (marca tudo como visto) e persiste em localStorage"
    - "O header do popover ('X item(s)') e a lista continuam mostrando o TOTAL de pendências vivas, não os não-vistos"
    - "Uma pendência resolvida (que sai da lista) tem sua key removida do conjunto visto ao abrir o sino (prune)"
    - "O estado visto é chaveado por org (bell-seen:{orgId}) — trocar de org não vaza contagem entre organizações"
  artifacts:
    - path: "src/lib/bellSeen.ts"
      provides: "Funções puras React-free: computeUnread + mergeAndPruneSeen + shouldSeed"
      exports: ["computeUnread", "mergeAndPruneSeen", "shouldSeed"]
      min_lines: 24
    - path: "src/hooks/useAtendimentoPendencias.ts"
      provides: "Expõe readiness (isReady = enabled && query.isFetched) para gatear a semeadura do sino"
      contains: "isReady"
    - path: "src/lib/bellSeen.test.ts"
      provides: "Testes vitest das funções puras (6 cenários, incl. guard de prontidão)"
      contains: "describe"
    - path: "src/hooks/useBellSeen.ts"
      provides: "Hook: unreadCount + markAllSeen, localStorage keyed bell-seen:{orgId}"
      exports: ["useBellSeen"]
      min_lines: 30
    - path: "src/components/layout/AtendimentoBell.tsx"
      provides: "Badge usa unreadCount; onOpenChange(true) chama markAllSeen"
      contains: "useBellSeen"
  key_links:
    - from: "src/components/layout/AtendimentoBell.tsx"
      to: "src/hooks/useBellSeen.ts"
      via: "useBellSeen(items, isReady) → { unreadCount, markAllSeen }"
      pattern: "useBellSeen\\("
    - from: "src/hooks/useBellSeen.ts"
      to: "src/lib/bellSeen.ts"
      via: "computeUnread / mergeAndPruneSeen"
      pattern: "computeUnread|mergeAndPruneSeen"
    - from: "src/components/layout/AtendimentoBell.tsx"
      to: "src/hooks/useBellSeen.ts"
      via: "Popover onOpenChange dispara markAllSeen no open"
      pattern: "onOpenChange"
---

<objective>
Transformar o badge vermelho do sino de atendimento (`AtendimentoBell`) de "total de pendências vivas" para "novidades ainda não vistas". Abrir o sino marca tudo como visto e zera o badge; ele volta a subir só quando chega algo genuinamente novo via o refetch de 45s de `useAtendimentoPendencias`. A lista e o header do popover continuam mostrando o TOTAL de pendências — o "lido" afeta só o alerta, nunca a fila de trabalho.

Purpose: Reduzir ruído do sino. Hoje o badge nunca zera enquanto houver pendência viva; o operador perde a noção do que é NOVO. Implementa BELL-01 (badge = não-vistos), BELL-02 (abrir zera + persiste) e BELL-03 (prune de keys resolvidas).

Output: lib pura testável (`src/lib/bellSeen.ts` + testes), hook `useBellSeen` (localStorage por org), e fiação do `AtendimentoBell`. Client-only — sem backend/EF/RPC/migration/nova dependência.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/91-sino-da-navbar-marcar-notifica-es-como-lidas-badge-s-conta-n/91-CONTEXT.md

# Fonte das pendências (produz items com key estável q-/c-, count, isLoading)
@src/hooks/useAtendimentoPendencias.ts
# Componente do sino (Popover + badge) — ponto de mudança
@src/components/layout/AtendimentoBell.tsx
# Padrão de localStorage guardado (load/save try-catch) a replicar
@src/hooks/useDashboardLayout.ts
# Fonte do orgId para chavear o localStorage
@src/contexts/OrganizationContext.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces

**New pure lib — `src/lib/bellSeen.ts`** (React-free, testável em vitest):
- `computeUnread(items, seenKeys)` — nº de `items` cuja `key` NÃO está em `seenKeys`. `items: { key: string }[]`; `seenKeys: readonly string[]`. Retorna `number`.
- `mergeAndPruneSeen(seenKeys, items)` — recebe o conjunto visto atual + as pendências vivas; retorna o novo conjunto visto = **merge das keys atuais** e **prune das keys que não estão mais entre `items`**. Resultado líquido = exatamente as keys presentes em `items` (dedupadas). Assinatura: `(seenKeys: readonly string[], items: { key: string }[]) => string[]`.
- `shouldSeed(seen, orgId, isReady)` — decisão pura de "semear agora?": retorna `seen === null && !!orgId && isReady`. Assinatura: `(seen: readonly string[] | null, orgId: string | null, isReady: boolean) => boolean`. Existe para gatear a semeadura APENAS depois que a query de pendências ficou pronta (evita semear contra `items=[]` na janela fria em que a query ainda está desabilitada).

**New test — `src/lib/bellSeen.test.ts`** cobrindo os 6 cenários (ver Task 1 `<behavior>`), incluindo "não-pronto → não semeia".

**Modified — `src/hooks/useAtendimentoPendencias.ts`** (aditivo): passa a retornar `isReady = enabled && query.isFetched` além de `{ items, count, isLoading }`. `enabled` já é calculado no hook (`!!orgId && resolvedMLUserIds.length > 0`); `query.isFetched` vem do `useQuery`. Esse é o sinal de prontidão robusto — cobre o caso TanStack v5 em que uma query DESABILITADA tem `isLoading === false` (janela fria: `orgId` truthy, `resolvedMLUserIds` ainda `[]`, `items=[]`). Nada mais no hook muda.

**New hook — `src/hooks/useBellSeen.ts`**:
- Assinatura: `useBellSeen(items: { key: string }[], isReady: boolean) => { unreadCount: number; markAllSeen: () => void }`
- Lê `orgId` internamente via `useOrganization()` (`currentOrg?.id ?? null`).
- Chave de localStorage: **`bell-seen:{orgId}`** (valor = array JSON de keys já vistas).
- Semeadura na 1ª montagem sem registro: badge começa em 0. Efeito de semeadura gateado por `shouldSeed(seen, orgId, isReady)` — semeia com as keys atuais só quando a query ficou pronta.
- `markAllSeen()`: aplica `mergeAndPruneSeen` no set atual + `items` e persiste.

**Modified — `src/components/layout/AtendimentoBell.tsx`**: consome `isReady` de `useAtendimentoPendencias` e passa a `useBellSeen(items, isReady)`; badge passa a usar `unreadCount`; `Popover onOpenChange(true)` chama `markAllSeen()`; header/lista inalterados (continuam usando `count`/`items`).

**Formato da chave de localStorage:** `bell-seen:{orgId}` — ex.: `bell-seen:7f615df7-...`. Sem orgId (org nula) → hook não persiste e reporta `unreadCount = 0`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lib pura bellSeen (computeUnread + mergeAndPruneSeen + shouldSeed) com testes vitest</name>
  <files>src/lib/bellSeen.ts, src/lib/bellSeen.test.ts</files>
  <read_first>
    - src/lib/claimStatus.test.ts — padrão de teste do projeto (import { describe, it, expect } from "vitest"; funções puras colocadas)
    - src/hooks/useAtendimentoPendencias.ts — formato do `PendenciaItem` (campo `key: string`, ex. `q-123` / `c-456`)
  </read_first>
  <behavior>
    Escreva os testes ANTES da implementação (RED → GREEN). Tipe o parâmetro de item como um shape mínimo `{ key: string }` (não importe `PendenciaItem` para manter a lib React-free e desacoplada). Cenários (BELL-01/02/03):
    - Test A — "sem seen → semear zera": `const seeded = mergeAndPruneSeen([], items)` retorna as keys atuais; `computeUnread(items, seeded)` === 0. (primeira carga não explode o badge)
    - Test B — "item novo → unread": `computeUnread([{key:'q-1'}], [])` === 1; com mix `computeUnread([{key:'q-1'},{key:'c-2'}], ['q-1'])` === 1 (só `c-2` conta).
    - Test C — "já visto e ainda vivo não conta": `computeUnread([{key:'q-1'}], ['q-1'])` === 0.
    - Test D — "abrir zera (merge)": `computeUnread(items, mergeAndPruneSeen(seen, items))` === 0 para qualquer `seen`/`items`.
    - Test E — "prune de resolvidos": partindo de `seen=['q-1','c-9']` e `items=[{key:'q-1'}]`, `mergeAndPruneSeen(seen, items)` NÃO contém `c-9` (key resolvida sai do set) e contém `q-1`. Consequência coberta: se `c-9` reabrir depois (mesma key), como saiu do set, `computeUnread` volta a contá-la como novidade — evita ressuscitar como "já visto" (decisão LOCKED 91-CONTEXT linha 33).
    - Test F — "guard de prontidão (cold-start) — não-pronto → não semeia": `shouldSeed(null, 'org-1', false)` === false (query ainda não pronta, mesmo com orgId truthy → NÃO semeia; é exatamente a janela fria onde `items=[]`); `shouldSeed(null, 'org-1', true)` === true (pronto e sem registro → semeia); `shouldSeed(['q-1'], 'org-1', true)` === false (já semeado → não re-semeia); `shouldSeed(null, null, true)` === false (sem org → não semeia).
  </behavior>
  <action>
    Crie `src/lib/bellSeen.ts` com TRÊS funções puras exportadas nomeadas, SEM imports de React/Supabase/rede. `computeUnread(items, seenKeys)`: constrói um `Set` de `seenKeys` e retorna `items.filter(i => !set.has(i.key)).length`. `mergeAndPruneSeen(seenKeys, items)`: calcula o conjunto de keys vivas (`new Set(items.map(i => i.key))`) e retorna esse conjunto como array — o merge das keys atuais somado ao prune de tudo que não está em `items` equivale às keys vivas dedupadas; documente esse invariante em comentário. `shouldSeed(seen, orgId, isReady)`: retorna `seen === null && !!orgId && isReady` — decisão pura de "posso semear agora?"; o hook usa isso para semear SÓ depois que a query de pendências ficou pronta (nunca na janela fria com query desabilitada e `items=[]`, senão o badge explodiria quando os dados reais chegassem). Compare SEMPRE por `key`, nunca por timestamp (a granularidade de dia de `data_abertura` daria falso-positivo). Trate `seenKeys` como `readonly string[]` e tolere entradas duplicadas. Crie `src/lib/bellSeen.test.ts` com os 6 testes (A–F) do `<behavior>`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/bellSeen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/bellSeen.ts` exporta `computeUnread`, `mergeAndPruneSeen` e `shouldSeed` como named exports, sem qualquer import de React/Supabase.
    - `src/lib/bellSeen.test.ts` cobre os 6 cenários A–F (incl. F, o guard de prontidão que impede o badge de explodir no cold-start) e passa verde.
    - `npx vitest run src/lib/bellSeen.test.ts` sai 0 (todos verdes).
  </acceptance_criteria>
  <done>Funções puras testadas cobrindo semear/novo/já-visto/zerar/prune/guard-de-prontidão; comparação por key.</done>
</task>

<task type="auto">
  <name>Task 2: Expor readiness em useAtendimentoPendencias + hook useBellSeen (localStorage por org, unreadCount + markAllSeen, semeadura gateada por prontidão)</name>
  <files>src/hooks/useAtendimentoPendencias.ts, src/hooks/useBellSeen.ts</files>
  <read_first>
    - src/hooks/useAtendimentoPendencias.ts — hook de dados: `enabled = !!orgId && resolvedMLUserIds.length > 0`; hoje retorna `{ items, count, isLoading }`
    - src/hooks/useDashboardLayout.ts — padrão load/save de localStorage com try-catch (funções `loadLayout`/`saveLayout`) a replicar
    - src/contexts/OrganizationContext.tsx — `useOrganization()` → `currentOrg?.id ?? null` (fonte do orgId)
    - src/lib/bellSeen.ts — funções puras criadas na Task 1 (computeUnread / mergeAndPruneSeen / shouldSeed)
  </read_first>
  <action>
    PARTE A — `src/hooks/useAtendimentoPendencias.ts` (mudança aditiva mínima): exponha um sinal de prontidão no retorno. Já existe `const enabled = !!orgId && resolvedMLUserIds.length > 0;`. Calcule `const isReady = enabled && query.isFetched;` e adicione ao objeto retornado: `return { items, count: items.length, isLoading: query.isLoading, isReady };`. RACIONAL (crítico): no TanStack Query v5 uma query DESABILITADA tem `isLoading === false` — logo `!isLoading` NÃO distingue "carregou vazio" de "ainda desabilitada". `resolvedMLUserIds` vem do `MLStoreContext` e resolve de forma assíncrona e independente de `currentOrg`, criando uma janela fria (orgId truthy, `resolvedMLUserIds=[]`, query off, `items=[]`). `enabled && query.isFetched` só fica true depois que a query realmente buscou ao menos uma vez com dados. Não mude mais nada no hook (queryFn, refetch, etc. intocados).

    PARTE B — crie `src/hooks/useBellSeen.ts` exportando `useBellSeen(items: { key: string }[], isReady: boolean)`. Leia `orgId` via `useOrganization()` (`currentOrg?.id ?? null`). Defina a chave `bell-seen:{orgId}`. Estado interno `seen: string[] | null` (null = ainda não semeado para esta org). Helpers locais `loadSeen(orgId)` e `saveSeen(orgId, keys)` com guarda SSR (`typeof window === "undefined"` → no-op) e `try/catch` (ignora quota/security) espelhando `useDashboardLayout`. Comportamento: (1) num `useEffect` keyed em `orgId`, recarregue: orgId nulo → `setSeen(null)`; há registro → `setSeen(parsed)`; sem registro → `setSeen(null)` (pendente de semeadura). (2) `useEffect` de semeadura gateado por `shouldSeed(seen, orgId, isReady)`: quando true, semeie com as keys atuais — `const seeded = items.map(i => i.key); setSeen(seeded); saveSeen(orgId, seeded)` (badge começa em 0 na 1ª visita, per D-LOCKED "não explodir o badge"; NUNCA semeia com query off). (3) `unreadCount = seen === null ? 0 : computeUnread(items, seen)`. (4) `markAllSeen = useCallback(() => { if (!orgId) return; const next = mergeAndPruneSeen(seen ?? [], items); setSeen(next); saveSeen(orgId, next); }, [orgId, seen, itemsKey])`. Retorne `{ unreadCount, markAllSeen }`. ESTABILIZAÇÃO DE DEPENDÊNCIA (WARNING do checker): `items = query.data ?? []` muda de identidade a cada render; para evitar churn no `useEffect` de semeadura e no `useCallback`, derive uma string estável das keys — `const itemsKey = items.map(i => i.key).join('|');` (via `useMemo`) e use `itemsKey` nas deps (funcionalmente já é seguro pelo guard `seen === null`, mas evita re-execuções desnecessárias). NÃO altere a lógica de dados de `useAtendimentoPendencias` (só o retorno na Parte A). Sem novas dependências.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `useAtendimentoPendencias` retorna `isReady` (= `enabled && query.isFetched`) além de `{ items, count, isLoading }`; queryFn/refetch inalterados.
    - `useBellSeen` é named export, recebe `(items, isReady)` e retorna `{ unreadCount, markAllSeen }`.
    - A semeadura é gateada por `shouldSeed(seen, orgId, isReady)` — NÃO semeia enquanto `isReady === false` (cold-start com query desabilitada e `items=[]`).
    - localStorage é lido/escrito na chave `bell-seen:{orgId}`, com guarda SSR + try/catch; org nula não persiste e reporta `unreadCount = 0`.
    - Dependências do efeito/callback estabilizadas por uma chave derivada das keys (não pela identidade do array `items`).
    - `npx tsc --noEmit` sai 0 erros.
  </acceptance_criteria>
  <done>Readiness exposto no hook de dados; useBellSeen expõe unreadCount (semeado a 0 só quando pronto) + markAllSeen persistente, chaveado por org.</done>
</task>

<task type="auto">
  <name>Task 3: Fiar AtendimentoBell — badge usa unreadCount, abrir chama markAllSeen; header/lista mantêm total</name>
  <files>src/components/layout/AtendimentoBell.tsx</files>
  <read_first>
    - src/components/layout/AtendimentoBell.tsx — estado atual: usa `count` no badge, header e aria-label; Popover é não-controlado
    - src/hooks/useBellSeen.ts — hook criado na Task 2 (assinatura `(items, isReady)`)
    - src/hooks/useAtendimentoPendencias.ts — agora retorna também `isReady` (Task 2, Parte A)
  </read_first>
  <action>
    No `AtendimentoBell`, troque a desestruturação para `const { items, count, isLoading, isReady } = useAtendimentoPendencias();` e acrescente `const { unreadCount, markAllSeen } = useBellSeen(items, isReady);` (passe `isReady`, NÃO `isLoading` — é o sinal que evita a semeadura na janela fria). No `<Popover>`, adicione `onOpenChange={(open) => { if (open) markAllSeen(); }}` (o Radix Popover honra `onOpenChange` mesmo não-controlado — não introduza estado `open` controlado). Troque o badge para usar `unreadCount`: renderize o `<span>` do badge apenas quando `unreadCount > 0` e exiba `unreadCount > 9 ? "9+" : unreadCount`. MANTENHA o header do popover ("{count} item(s)") e a lista (`items`) usando o TOTAL — não troque para unreadCount ali. Atualize o `aria-label` do botão para refletir novidades vs total: quando `unreadCount > 0` → algo como `${unreadCount} novas notificações de atendimento`; senão quando `count > 0` → `${count} pendências de atendimento`; senão `"Atendimento"`. Não mexa em `PendenciaRow`, `tempoRelativo`, nem nos imports de dados.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `useBellSeen` é chamado com `isReady` (não `isLoading`).
    - O badge vermelho renderiza `unreadCount` (com "9+" acima de 9) e some quando `unreadCount === 0`.
    - `Popover` tem `onOpenChange` que chama `markAllSeen()` no open (sem converter para Popover controlado).
    - Header do popover e a lista continuam mostrando `count`/`items` (total de pendências vivas).
    - `aria-label` reflete unread vs total.
    - `npx tsc --noEmit` 0 erros e `npm run build` conclui sem erro.
  </acceptance_criteria>
  <done>Badge = não-vistos; abrir zera; header/lista continuam no total; a11y atualizada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador ↔ localStorage | Estado "visto" persistido client-side; nenhum dado cruza para servidor. Sem trust boundary de rede nesta phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering | localStorage `bell-seen:{orgId}` | accept | Adulterar o valor só afeta o próprio badge de novidades do usuário (contagem visual). Nenhuma decisão de negócio, autorização ou dado sensível depende dele; a lista/fila de trabalho vem do backend com RLS e é imune. |
| T-91-02 | Information Disclosure | chave por org | mitigate | Chave inclui `{orgId}` → trocar de org não vaza contagem de outra organização; keys são apenas `q-{id}`/`c-{id}` (identificadores já visíveis ao próprio usuário na sua org). |
| T-91-03 | Denial of Service | JSON corrompido / quota cheia | mitigate | `loadSeen` faz `try/catch` e cai para "semear" (badge 0); `saveSeen` engole erro de quota/security — sino nunca quebra a navbar. |

Anti-IDOR: N/A — client-only, sem EF/RPC/migration/nova dependência.
</threat_model>

<verification>
- `npx vitest run src/lib/bellSeen.test.ts` — novos testes verdes (6 cenários, incl. guard de prontidão F).
- `npm test` — suíte completa continua verde (nenhuma regressão).
- `npx tsc --noEmit` — 0 erros.
- `npm run build` — build conclui.
- Grep de fiação: `grep -n "useBellSeen" src/components/layout/AtendimentoBell.tsx` e `grep -n "onOpenChange" src/components/layout/AtendimentoBell.tsx` retornam match.
</verification>

<success_criteria>
- Badge do sino conta só não-vistos (`unreadCount`), não o total — BELL-01.
- Abrir o popover zera o badge e persiste em `localStorage` na chave `bell-seen:{orgId}` — BELL-02.
- Pendências resolvidas têm suas keys removidas do conjunto visto ao abrir (prune) — BELL-03.
- Header ("X item(s)") e lista do popover seguem mostrando o TOTAL de pendências vivas.
- Primeira visita sem registro: badge começa em 0 (não explode com pendências antigas).
- tsc 0, vitest verde (incl. novos testes), build ok.
</success_criteria>

<output>
Create `.planning/phases/91-sino-da-navbar-marcar-notifica-es-como-lidas-badge-s-conta-n/91-01-SUMMARY.md` when done
</output>
