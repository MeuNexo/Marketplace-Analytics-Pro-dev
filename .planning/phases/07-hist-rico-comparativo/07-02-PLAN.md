---
phase: 07-hist-rico-comparativo
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/mercadolivre/analise/HistoricoSnapshotTable.tsx
  - src/components/mercadolivre/analise/HistoricoComparacaoPanel.tsx
  - src/components/mercadolivre/analise/HistoricoComparativo.tsx
  - src/pages/mercadolivre/MLPrecificacao.tsx
autonomous: true
requirements:
  - HIST-02
must_haves:
  truths:
    - "Usuário vê uma aba 'Histórico' em MLPrecificacao ao lado de 'Simulador' e 'Análise'"
    - "Usuário seleciona um produto e vê a lista de snapshots salvos com data, período e três preços estratégicos"
    - "Usuário marca dois checkboxes (no máximo) e vê um painel comparativo lado a lado"
    - "O painel mostra variação de Preço GMV, Neutro, Margem, elasticidade (%) e classificação entre os dois snapshots"
    - "Ao trocar de produto, a seleção é resetada"
    - "Ao atingir 2 selecionados, o terceiro checkbox fica disabled"
  artifacts:
    - path: "src/components/mercadolivre/analise/HistoricoSnapshotTable.tsx"
      provides: "Tabela de snapshots com checkbox de seleção (max 2)"
      exports: ["HistoricoSnapshotTable", "HistoricoSnapshotTableProps"]
    - path: "src/components/mercadolivre/analise/HistoricoComparacaoPanel.tsx"
      provides: "Painel de comparação lado a lado entre dois snapshots"
      exports: ["HistoricoComparacaoPanel"]
    - path: "src/components/mercadolivre/analise/HistoricoComparativo.tsx"
      provides: "Container que orquestra produto → snapshots → seleção → comparação"
      exports: ["HistoricoComparativo"]
    - path: "src/pages/mercadolivre/MLPrecificacao.tsx"
      provides: "Aba 'Histórico' renderizando HistoricoComparativo"
      contains: "id: \"historico\""
  key_links:
    - from: "src/pages/mercadolivre/MLPrecificacao.tsx"
      to: "src/components/mercadolivre/analise/HistoricoComparativo.tsx"
      via: "lazy import + render quando tab === 'historico'"
      pattern: "HistoricoComparativo"
    - from: "src/components/mercadolivre/analise/HistoricoComparativo.tsx"
      to: "src/hooks/useAnalysisSnapshots.ts"
      via: "fetchSnapshots(itemId, orgId)"
      pattern: "fetchSnapshots\\("
    - from: "src/components/mercadolivre/analise/HistoricoSnapshotTable.tsx"
      to: "src/lib/analysis/elasticityConfig.ts"
      via: "import ELASTICITY_BADGE"
      pattern: "from ['\"]@/lib/analysis/elasticityConfig['\"]"
    - from: "src/components/mercadolivre/analise/HistoricoComparacaoPanel.tsx"
      to: "src/lib/analysis/elasticityConfig.ts"
      via: "import ELASTICITY_BADGE"
      pattern: "from ['\"]@/lib/analysis/elasticityConfig['\"]"
---

<objective>
Entregar o fluxo completo de Histórico Comparativo (HIST-02): aba "Histórico" em MLPrecificacao, lista de snapshots por produto com seleção de até 2, e painel comparativo lado a lado mostrando variações de preço, elasticidade e classificação.

Purpose: Permitir que o usuário consulte análises anteriores do mesmo produto e identifique variações ao longo do tempo (elasticidade, preços estratégicos), fechando o requisito HIST-02.
Output: 3 novos componentes em `src/components/mercadolivre/analise/` + atualização da página `MLPrecificacao.tsx` adicionando a aba.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hist-rico-comparativo/07-RESEARCH.md
@.planning/phases/07-hist-rico-comparativo/07-01-SUMMARY.md
@src/pages/mercadolivre/MLPrecificacao.tsx
@src/hooks/useAnalysisSnapshots.ts
@src/lib/analysis/types.ts
@src/lib/analysis/elasticityConfig.ts
@src/components/mercadolivre/analise/AnaliseDashboard.tsx
@src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx
@src/components/ui/table.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/components/ui/checkbox.tsx
</context>

<interfaces>
Tipos consumidos (já existentes):

```typescript
// src/hooks/useAnalysisSnapshots.ts
export interface AnalysisSnapshot {
  id: string;
  createdAt: string;        // ISO timestamp
  mlUserId: string;
  organizationId: string;
  itemId: string;
  productTitle: string;
  brand: string | null;
  periodStart: string;      // YYYY-MM-DD
  periodEnd: string;        // YYYY-MM-DD
  priceCurve: PriceBucket[];
  priceGmv: number;
  priceNeutral: number;
  priceMargin: number;
  elasticityPct: number;
  elasticityClass: ElasticityClass;
  strategy: 'gmv' | 'neutral' | 'margin' | null;
}

export function useAnalysisSnapshots(): {
  fetchSnapshots: (itemId: string, orgId: string) => Promise<AnalysisSnapshot[]>;  // já ordena por created_at DESC
  loading: boolean;
  // ...outros membros não usados aqui
};
```

```typescript
// src/lib/analysis/elasticityConfig.ts (criado em 07-01)
export const ELASTICITY_BADGE: Record<ElasticityClass, { label: string; className: string }>;
```

```typescript
// src/lib/pricing/calculator.ts (já existente)
export function formatBRL(value: number): string;  // ex: 49.9 -> "R$ 49,90"
```

Contratos NOVOS criados por este plan:

```typescript
// HistoricoSnapshotTable.tsx
export interface HistoricoSnapshotTableProps {
  snapshots: AnalysisSnapshot[];
  selected: string[];                       // até 2 IDs
  onToggle: (id: string) => void;
}
export function HistoricoSnapshotTable(props: HistoricoSnapshotTableProps): JSX.Element;

// HistoricoComparacaoPanel.tsx
export function HistoricoComparacaoPanel(props: {
  snapshots: [AnalysisSnapshot, AnalysisSnapshot];   // tupla obrigatória de 2
}): JSX.Element;

// HistoricoComparativo.tsx
export function HistoricoComparativo(): JSX.Element;
```

Padrão de obtenção de `orgId` e `mlUserId`: replicar EXATAMENTE o padrão de `AnaliseDashboard` (mesmo contexto/hook que o dashboard usa hoje). Não inventar novo padrão — copiar a forma de leitura do contexto da org/ML.

Padrão de seletor de produto: replicar o seletor (`<Select>` com lista de itens distintos) usado no `SimuladorPrecificacao` ou no `AnaliseDashboard` — qual deles servir como base é discricionário, mas DEVE ser o mesmo `<Select>` shadcn já em uso na área de análise para manter consistência visual.

Ordenação dos snapshots: `fetchSnapshots` retorna DESC por `created_at`. Portanto, quando 2 snapshots forem selecionados, o "mais novo" é o de menor índice no array original. Para o painel comparativo, o cliente DEVE garantir que a tupla passada é `[older, newer]` (ver Task 2 abaixo) para que o "diff" seja calculado como `newer - older`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar HistoricoSnapshotTable.tsx</name>
  <files>src/components/mercadolivre/analise/HistoricoSnapshotTable.tsx</files>
  <behavior>
    - Renderiza `<Table>` shadcn com uma linha por snapshot
    - Colunas (nessa ordem): Checkbox | Data | Período | Preço GMV | Preço Neutro | Preço Margem | Elasticidade
    - Coluna "Data": formata `snapshot.createdAt` (ISO) como `dd/MM/yyyy HH:mm` (usar `Intl.DateTimeFormat('pt-BR', ...)` para evitar adicionar dependência se date-fns não estiver disponível; verificar com grep `from "date-fns"` se já é usado no projeto — se sim, usar `format(new Date(createdAt), "dd/MM/yyyy HH:mm")`)
    - Coluna "Período": string `${periodStart} → ${periodEnd}` (formato YYYY-MM-DD bruto, conforme já armazenado)
    - Colunas de preço: `formatBRL` para os três valores
    - Coluna "Elasticidade": `<Badge variant="outline" className={ELASTICITY_BADGE[snapshot.elasticityClass].className}>{ELASTICITY_BADGE[snapshot.elasticityClass].label}</Badge>`, importado de `@/lib/analysis/elasticityConfig`
    - Checkbox: `checked={selected.includes(snapshot.id)}`; `disabled={selected.length >= 2 && !selected.includes(snapshot.id)}`; `onCheckedChange={() => onToggle(snapshot.id)}`
    - Estado vazio (`snapshots.length === 0`): linha única com `colSpan={7}` e texto "Nenhuma análise salva para este produto."
  </behavior>
  <action>
    Criar `src/components/mercadolivre/analise/HistoricoSnapshotTable.tsx` exportando `HistoricoSnapshotTable` e `HistoricoSnapshotTableProps` conforme contratos em `<interfaces>`. Importar `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell` de `@/components/ui/table`, `Badge` de `@/components/ui/badge`, `Checkbox` de `@/components/ui/checkbox`, `formatBRL` de `@/lib/pricing/calculator`, `ELASTICITY_BADGE` de `@/lib/analysis/elasticityConfig`, e o tipo `AnalysisSnapshot` de `@/hooks/useAnalysisSnapshots`. Para formatar a data, primeiro fazer `grep -l "from \"date-fns\"" src/` — se date-fns já é dependência usada, importar `format` dela; caso contrário, usar `new Intl.DateTimeFormat("pt-BR", { day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit" }).format(new Date(snapshot.createdAt))`. Não introduzir nova dependência de NPM.
  </action>
  <verify>
    <automated>cd "C:/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Componente exportado, tipa props conforme `<interfaces>`, renderiza tabela com checkbox controlado, formata data em pt-BR, importa `ELASTICITY_BADGE` do módulo compartilhado, `tsc --noEmit` sem erros.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar HistoricoComparacaoPanel.tsx</name>
  <files>src/components/mercadolivre/analise/HistoricoComparacaoPanel.tsx</files>
  <behavior>
    - Recebe `snapshots: [AnalysisSnapshot, AnalysisSnapshot]` onde índice 0 = mais antigo (A), índice 1 = mais novo (B)
    - Renderiza `<Card>` com `<CardHeader>` "Comparação" e `<CardContent>` contendo uma grade de 4 colunas: rótulo | valor A | indicador diff | valor B
    - Linhas (nessa ordem):
      1. "Período analisado": `${A.periodStart} → ${A.periodEnd}` vs `${B.periodStart} → ${B.periodEnd}` (sem indicador de diff)
      2. "Preço GMV": formatBRL(A.priceGmv) | diff | formatBRL(B.priceGmv)
      3. "Preço Neutro": idem com priceNeutral
      4. "Preço Margem": idem com priceMargin
      5. "Elasticidade (%)": `${A.elasticityPct.toFixed(2).replace(".", ",")}%` | diff (de elasticityPct) | mesmo para B
      6. "Classificação": Badge ELASTICITY_BADGE de A | (sem diff numérico, mostrar "=" se igual ou "→" se diferente) | Badge ELASTICITY_BADGE de B
    - Diff numérico (preços e elasticidade):
      - `diff = B.value - A.value`
      - `diff > 0` → texto verde `"↑ +R$X,XX"` (preços) ou `"↑ +X,XX pp"` (elasticidade em pontos percentuais) — para elasticidade usar `"↑ +X,XX%"`
      - `diff < 0` → texto vermelho `"↓ −R$X,XX"` ou `"↓ −X,XX%"` (sinal de menos Unicode `−` ou hífen normal — escolha do executor)
      - `diff === 0` → texto muted `"="`
      - Cores via Tailwind: verde `text-emerald-600`, vermelho `text-red-600`, muted `text-muted-foreground`
  </behavior>
  <action>
    Criar `src/components/mercadolivre/analise/HistoricoComparacaoPanel.tsx` exportando `HistoricoComparacaoPanel`. Importar `Card`, `CardHeader`, `CardTitle`, `CardContent` de `@/components/ui/card`, `Badge` de `@/components/ui/badge`, `cn` de `@/lib/utils`, `formatBRL` de `@/lib/pricing/calculator`, `ELASTICITY_BADGE` de `@/lib/analysis/elasticityConfig`, e o tipo `AnalysisSnapshot` de `@/hooks/useAnalysisSnapshots`. Implementar uma função helper local `priceDiff(a: number, b: number, kind: 'currency' | 'percent'): { label: string; className: string }` que retorna `{ label, className }` conforme as três regras de cor/sinal acima. Não criar diff para "Período analisado" (apenas mostrar as duas strings). Layout principal: `<div className="grid grid-cols-[1fr_1fr_auto_1fr] gap-x-4 gap-y-2 items-center">` para a tabela de comparação. Texto da linha pode usar `<p className="text-sm font-medium">` para rótulos e `tabular-nums` para valores numéricos. Garantir que o painel funciona quando A e B forem o mesmo snapshot (caso extremo: usuário marca e desmarca rapidamente — o container nunca passará dois iguais, mas o componente deve tolerar `diff === 0` em todas as linhas).
  </action>
  <verify>
    <automated>cd "C:/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Componente exportado, tipa props como tupla `[AnalysisSnapshot, AnalysisSnapshot]`, renderiza 6 linhas de comparação com diff colorido e badges de classificação, `tsc --noEmit` sem erros.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar HistoricoComparativo.tsx (container)</name>
  <files>src/components/mercadolivre/analise/HistoricoComparativo.tsx</files>
  <behavior>
    - Componente sem props, obtém `orgId` e `mlUserId` do mesmo contexto usado por `AnaliseDashboard` (replicar EXATAMENTE)
    - Estado interno:
      - `itemId: string` — produto selecionado
      - `snapshots: AnalysisSnapshot[]` — lista vinda de `fetchSnapshots`
      - `selected: string[]` — até 2 IDs
    - Usa `loading` retornado por `useAnalysisSnapshots()` para o estado de carregamento (não duplicar com state local)
    - Seletor de produto: replicar padrão do `AnaliseDashboard` (mesmo `<Select>` shadcn com itens distintos disponíveis). Se o padrão exato em `AnaliseDashboard` for derivar a lista de produtos de orders, copiar essa lógica. Não inventar fonte de dados nova.
    - Ao trocar `itemId`:
      1. Chama `fetchSnapshots(itemId, orgId)` e atualiza `snapshots`
      2. Reseta `selected` para `[]`
    - `handleToggle(id)`: se `selected.includes(id)` → remover; senão se `selected.length < 2` → adicionar; caso contrário, ignorar (no-op — o componente filho já desabilita o checkbox)
    - Render:
      - Seletor de produto no topo
      - Se `loading`: `<p className="text-sm text-muted-foreground">Carregando…</p>`
      - `<HistoricoSnapshotTable snapshots={snapshots} selected={selected} onToggle={handleToggle} />`
      - Se `selected.length === 2`: monta tupla `[older, newer]` ordenando os dois snapshots selecionados por `createdAt` ASC (older primeiro), e renderiza `<HistoricoComparacaoPanel snapshots={[older, newer]} />`
  </behavior>
  <action>
    Criar `src/components/mercadolivre/analise/HistoricoComparativo.tsx` exportando `HistoricoComparativo`. Antes de implementar, LER `src/components/mercadolivre/analise/AnaliseDashboard.tsx` para identificar o padrão exato de leitura de `orgId`/`mlUserId` e do seletor de produto — replicar fielmente. Usar `useAnalysisSnapshots()` para `fetchSnapshots` e `loading`. Implementar `useEffect` que dispara quando `itemId` muda: chama `fetchSnapshots`, seta `snapshots`, reseta `selected`. Implementar `handleToggle` conforme `<behavior>`. Para montar a tupla ordenada: `const [a, b] = selected.map(id => snapshots.find(s => s.id === id)!).sort((x, y) => x.createdAt.localeCompare(y.createdAt));` (ASC por `createdAt` como string ISO). Renderizar conforme `<behavior>`. Não introduzir novo hook ou contexto.
  </action>
  <verify>
    <automated>cd "C:/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Componente exportado, orquestra seletor → fetch → seleção → comparação; reseta seleção ao trocar produto; passa tupla `[older, newer]` ao painel; `tsc --noEmit` sem erros.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Adicionar aba "Histórico" em MLPrecificacao.tsx</name>
  <files>src/pages/mercadolivre/MLPrecificacao.tsx</files>
  <behavior>
    - Array `TABS` ganha terceira entrada `{ id: "historico", label: "Histórico" }`
    - Tipo `TabId` continua derivado de `typeof TABS[number]["id"]` (automático)
    - Bloco condicional de renderização ganha `{tab === "historico" && <HistoricoComparativo />}`
    - `HistoricoComparativo` é importado via `React.lazy` para code-splitting, envelopado em `<Suspense fallback={<p className="text-sm text-muted-foreground">Carregando…</p>}>` em volta do `<motion.div>` ou apenas em volta do componente lazy (escolha do executor; manter consistência se Simulador/Análise já usam Suspense)
    - Comportamento existente das abas "Simulador" e "Análise" inalterado
  </behavior>
  <action>
    Editar `src/pages/mercadolivre/MLPrecificacao.tsx`: adicionar `{ id: "historico", label: "Histórico" }` ao array `TABS`. Adicionar import lazy: `const HistoricoComparativo = lazy(() => import("@/components/mercadolivre/analise/HistoricoComparativo").then(m => ({ default: m.HistoricoComparativo })));` (e importar `lazy, Suspense` de `react`). Envelopar o `<motion.div>` (ou o seu conteúdo) com `<Suspense fallback={<p className="text-sm text-muted-foreground">Carregando…</p>}>`. Adicionar a linha `{tab === "historico" && <HistoricoComparativo />}` após a linha existente de "analise". Não tocar em nada mais.
  </action>
  <verify>
    <automated>cd "C:/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc" && npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>Aba "Histórico" aparece em MLPrecificacao ao lado de Simulador e Análise; clicar a aba renderiza `HistoricoComparativo`; demais abas continuam funcionando; `tsc --noEmit` sem erros; suite de testes mantém 63 testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase (via fetchSnapshots) | Já existente — `fetchSnapshots` filtra por `organization_id` server-side; este plan apenas consome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | HistoricoComparativo → fetchSnapshots | mitigate | `fetchSnapshots` já aplica `.eq('organization_id', orgId)` server-side; o container DEVE obter `orgId` do mesmo contexto que `AnaliseDashboard` (não de parâmetro de URL ou input de usuário). Auditoria: grep confirma que `orgId` vem do contexto da org, não de input. |
| T-07-04 | Tampering | HistoricoSnapshotTable seleção de IDs | accept | IDs selecionados são UUIDs vindos do próprio `snapshots` carregado; nenhum input livre do usuário; usados apenas para `find` local no array. |
| T-07-05 | Denial of Service | fetchSnapshots ao trocar produto rapidamente | accept | Volume esperado de snapshots por produto é baixo (poucas análises por mês); sem necessidade de debounce nesta fase. |
| T-07-06 | Information Disclosure | HistoricoComparacaoPanel renderização | accept | Apenas exibe dados já carregados; nenhuma nova superfície. |
</threat_model>

<verification>
- `npx tsc --noEmit` reporta zero erros
- `npm test` mantém os 63 testes existentes passando
- Manual smoke (descrito em `<done>` da Task 4): a aba "Histórico" aparece e o fluxo produto → seleção (max 2) → painel comparativo funciona
- Grep confirma que `ELASTICITY_BADGE` é importado dos novos componentes a partir de `@/lib/analysis/elasticityConfig` (não redefinido localmente): `grep -rn "const ELASTICITY_BADGE" src/components/mercadolivre/analise/Historico*.tsx` deve retornar zero linhas
- Grep confirma a presença da aba: `grep -n "historico" src/pages/mercadolivre/MLPrecificacao.tsx` lista a entrada do TABS e o render condicional
</verification>

<success_criteria>
- Requisito HIST-02 plenamente satisfeito:
  1. Usuário vê lista de análises salvas com data de execução, período analisado e preços estratégicos (Tasks 1+3+4)
  2. Usuário seleciona duas análises do mesmo produto e vê comparação lado a lado com variações em Preço GMV/Neutro/Margem e classificação de elasticidade (Tasks 2+3)
- Restrição de seleção máxima (2) implementada via `disabled` no terceiro checkbox
- Seleção reseta ao trocar de produto
- Zero erros TypeScript; suite de testes passa integral
- Nenhuma duplicação de `ELASTICITY_BADGE` é reintroduzida (refactor de 07-01 preservado)
</success_criteria>

<output>
After completion, create `.planning/phases/07-hist-rico-comparativo/07-02-SUMMARY.md` documenting the three new components, the MLPrecificacao tab integration, and confirming HIST-02 acceptance criteria (1 and 2) are met.
</output>
