---
phase: 07-hist-rico-comparativo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analysis/elasticityConfig.ts
  - src/components/mercadolivre/analise/AnalisePrecosTable.tsx
  - src/components/mercadolivre/analise/AnalysisProductCard.tsx
autonomous: true
requirements:
  - HIST-02
must_haves:
  truths:
    - "ELASTICITY_BADGE existe em um único módulo compartilhado"
    - "AnalisePrecosTable e AnalysisProductCard importam o badge do módulo compartilhado"
    - "Aparência visual dos badges de elasticidade permanece idêntica antes e depois do refactor"
  artifacts:
    - path: "src/lib/analysis/elasticityConfig.ts"
      provides: "ELASTICITY_BADGE constant tipada por ElasticityClass"
      contains: "export const ELASTICITY_BADGE"
    - path: "src/components/mercadolivre/analise/AnalisePrecosTable.tsx"
      provides: "Tabela de análise consumindo elasticityConfig"
    - path: "src/components/mercadolivre/analise/AnalysisProductCard.tsx"
      provides: "Card de análise consumindo elasticityConfig"
  key_links:
    - from: "src/components/mercadolivre/analise/AnalisePrecosTable.tsx"
      to: "src/lib/analysis/elasticityConfig.ts"
      via: "import { ELASTICITY_BADGE } from '@/lib/analysis/elasticityConfig'"
      pattern: "from ['\"]@/lib/analysis/elasticityConfig['\"]"
    - from: "src/components/mercadolivre/analise/AnalysisProductCard.tsx"
      to: "src/lib/analysis/elasticityConfig.ts"
      via: "import { ELASTICITY_BADGE } from '@/lib/analysis/elasticityConfig'"
      pattern: "from ['\"]@/lib/analysis/elasticityConfig['\"]"
---

<objective>
Extrair a constante duplicada `ELASTICITY_BADGE` de `AnalisePrecosTable.tsx` e `AnalysisProductCard.tsx` para um módulo único `src/lib/analysis/elasticityConfig.ts`, preparando a base reutilizável para o componente de histórico (Plan 07-02).

Purpose: Eliminar duplicação antes de introduzir um terceiro consumidor (HistoricoSnapshotTable) e garantir consistência visual da classificação de elasticidade em todos os pontos da UI.
Output: Novo módulo `elasticityConfig.ts`; dois componentes existentes refatorados para consumi-lo, sem mudança de comportamento ou aparência.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hist-rico-comparativo/07-RESEARCH.md
@src/lib/analysis/types.ts
@src/components/mercadolivre/analise/AnalisePrecosTable.tsx
@src/components/mercadolivre/analise/AnalysisProductCard.tsx
</context>

<interfaces>
Tipo `ElasticityClass` (já existe em `src/lib/analysis/types.ts`):

```typescript
export type ElasticityClass = "baixa" | "media" | "alta" | "extrema";
```

Valores EXATOS de `ELASTICITY_BADGE` atualmente duplicados em `AnalisePrecosTable.tsx` (linhas 32-37) e `AnalysisProductCard.tsx` (linhas 10-15). DEVEM ser preservados literalmente na extração:

- `baixa`   → label "Baixa",   className `"bg-emerald-500/15 text-emerald-700 border-emerald-500/30"`
- `media`   → label "Média",   className `"bg-blue-500/15    text-blue-700    border-blue-500/30"`
- `alta`    → label "Alta",    className `"bg-amber-500/15   text-amber-700   border-amber-500/30"`
- `extrema` → label "Extrema", className `"bg-red-500/15     text-red-700     border-red-500/30"`

Forma de uso atual nos dois componentes:

```typescript
const badge = ELASTICITY_BADGE[snapshot.elasticityClass];
<Badge variant="outline" className={cn(badge.className)}>{badge.label}</Badge>
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar módulo elasticityConfig.ts</name>
  <files>src/lib/analysis/elasticityConfig.ts</files>
  <behavior>
    - Exporta uma única const nomeada `ELASTICITY_BADGE` tipada como `Record<ElasticityClass, { label: string; className: string }>`
    - Cobre as quatro chaves: `baixa`, `media`, `alta`, `extrema`
    - Cada entrada possui exatamente o `label` e o `className` listados em `<interfaces>` (palette emerald/blue/amber/red `*-500/15`, NÃO `*-100`)
    - Importa `ElasticityClass` de `./types` (path relativo, mesmo diretório)
  </behavior>
  <action>
    Criar `src/lib/analysis/elasticityConfig.ts` com um único import `import type { ElasticityClass } from './types';` seguido da exportação da constante `ELASTICITY_BADGE` conforme tipo e valores listados em `<interfaces>`. Preservar literalmente os className com palette emerald/blue/amber/red `*-500/15` extraídos dos componentes existentes. Não adicionar nenhum outro símbolo ao arquivo.
  </action>
  <verify>
    <automated>cd "C:/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc" && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "elasticityConfig" | grep -q "^0$"</automated>
  </verify>
  <done>Arquivo `src/lib/analysis/elasticityConfig.ts` existe, exporta `ELASTICITY_BADGE` tipada, e o build TypeScript não reporta erros relacionados a esse arquivo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrar AnalisePrecosTable e AnalysisProductCard para consumir elasticityConfig</name>
  <files>src/components/mercadolivre/analise/AnalisePrecosTable.tsx, src/components/mercadolivre/analise/AnalysisProductCard.tsx</files>
  <behavior>
    - Em ambos os arquivos, a definição local da const `ELASTICITY_BADGE` é removida
    - Ambos passam a importar `ELASTICITY_BADGE` de `@/lib/analysis/elasticityConfig`
    - Nenhum outro comportamento, JSX, classe CSS ou import muda
    - `tsc --noEmit` continua com zero erros
    - Suite `npm test` continua passando (63 testes)
  </behavior>
  <action>
    Em `AnalisePrecosTable.tsx`: remover o bloco de comentário "── Elasticity badge config ──" e a const local `ELASTICITY_BADGE` (linhas 30-37). Adicionar `import { ELASTICITY_BADGE } from "@/lib/analysis/elasticityConfig";` junto aos demais imports. O import existente `import type { ElasticityClass } from "@/lib/analysis/types";` pode permanecer (segue usado pela tipagem de Strategy? — não; remover se ficar não-usado para evitar warning de lint). Em `AnalysisProductCard.tsx`: remover o bloco "── Elasticity badge config ──" e a const local `ELASTICITY_BADGE` (linhas 8-15). Adicionar o mesmo import. Remover o `import type { ElasticityClass }` se ficar sem uso. Não alterar nenhum JSX, className, ou lógica de renderização. Confirmar com leitura dos arquivos antes de editar que os valores em `<interfaces>` correspondem exatamente ao código atual.
  </action>
  <verify>
    <automated>cd "C:/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc" && npx tsc --noEmit 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>Ambos os componentes importam `ELASTICITY_BADGE` de `@/lib/analysis/elasticityConfig`; nenhuma definição local da constante permanece; `npx tsc --noEmit` reporta zero erros; `npm test` reporta os 63 testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Refactor puramente interno: sem novas entradas de dados, sem nova superfície de rede ou IPC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | src/lib/analysis/elasticityConfig.ts | accept | Constante estática sem input do usuário; risco efetivamente nulo. |
| T-07-02 | Information Disclosure | AnalisePrecosTable / AnalysisProductCard | accept | Nenhum dado novo é exposto; apenas reorganização de import. |
</threat_model>

<verification>
- `npx tsc --noEmit` reporta zero erros
- `npm test` mantém os 63 testes existentes passando
- Grep confirma que `ELASTICITY_BADGE` é definido em apenas um arquivo: o resultado de `grep -rn "const ELASTICITY_BADGE" src/` deve listar somente `src/lib/analysis/elasticityConfig.ts`
- Grep confirma que os dois componentes alvo importam o módulo: `grep -rn "from \"@/lib/analysis/elasticityConfig\"" src/components/mercadolivre/analise/` deve listar `AnalisePrecosTable.tsx` e `AnalysisProductCard.tsx`
</verification>

<success_criteria>
- `src/lib/analysis/elasticityConfig.ts` existe e exporta `ELASTICITY_BADGE` tipada por `ElasticityClass`
- `AnalisePrecosTable.tsx` e `AnalysisProductCard.tsx` consomem o módulo compartilhado e não mantêm definição local
- Nenhuma regressão visual ou comportamental: testes existentes continuam verdes
- Plan 07-02 pode importar `ELASTICITY_BADGE` do mesmo caminho sem qualquer trabalho adicional
</success_criteria>

<output>
After completion, create `.planning/phases/07-hist-rico-comparativo/07-01-SUMMARY.md` documenting the extraction and confirming downstream consumers (Plan 07-02) can import from `@/lib/analysis/elasticityConfig`.
</output>
