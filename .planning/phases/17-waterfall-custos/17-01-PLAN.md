---
phase: 17-waterfall-custos
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useMLCostWaterfall.ts
  - src/components/mercadolivre/MLCostCard.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements:
  - WATERFALL-01
  - WATERFALL-02
  - WATERFALL-03

must_haves:
  truths:
    - "MLCostCard exibe waterfall: receita bruta → comissão → frete → ads → CMV → impostos = lucro bruto"
    - "CMV = SUM(orders.custo_unit * quantidade) para pedidos pagos — sem hardcode"
    - "Impostos = SUM(receita_por_loja × effective_rate/100) usando ml_tax_config"
    - "CMV exibe 's/ custo' quando custo_unit é null em todos os pedidos do período"
    - "Impostos exibe 's/ config' quando nenhuma loja do período tem regime fiscal configurado"
    - "Badge 'Em desenvolvimento' removido do MLCostCard"
    - "Lucro Bruto exibe verde quando positivo, vermelho quando negativo"
  artifacts:
    - path: "src/hooks/useMLCostWaterfall.ts"
      provides: "Hook que agrega dados de orders para o waterfall financeiro"
      exports: ["useMLCostWaterfall", "CostWaterfallData"]
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "Card redesenhado com waterfall completo de 7 linhas + lucro bruto"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useMLCostWaterfall.ts"
      via: "import { useMLCostWaterfall }"
      pattern: "useMLCostWaterfall"
---

<objective>
Redesenhar o painel de custos da página de Vendas com o waterfall financeiro completo: receita bruta → (-) comissão → (-) frete → (-) publicidade → (-) CMV → (-) impostos = **lucro bruto**.

CMV vem de `SUM(orders.custo_unit × quantidade)`. Impostos vem de `ml_tax_config.effective_rate × receita_por_loja`. Ambos usam dados já existentes no banco — sem nova API, sem novo sync.

O MLCostCard remove o badge "Em desenvolvimento" e passa a ser funcional.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-waterfall-custos/17-CONTEXT.md

<interfaces>

De src/hooks/useMLOrders.ts (já existe — NÃO recriar):
```typescript
export interface MLOrderSummary {
  total_comissao: number;
  total_frete: number;
  paid_orders_count: number;
  paid_revenue: number;
}
// Query: public.orders filtrado por organization_id + ml_user_id + data_pedido
```

De src/hooks/useMLTaxConfig.ts (já existe — importar e usar):
```typescript
export function useMLTaxConfig(mlUserIds: string[], orgId: string)
// Retorna UseQueryResult<Map<string, MLTaxConfigEntry>>
// MLTaxConfigEntry tem: effective_rate: number
```

De src/contexts/MLStoreContext.tsx:
```typescript
const { resolvedMLUserIds } = useMLStore();
```

De src/contexts/OrganizationContext.tsx:
```typescript
const { currentOrg } = useOrganization();
```

De src/components/mercadolivre/MLCostCard.tsx (estado atual):
```typescript
interface CostSummary {
  comissao: number; frete: number; publicidade: number;
  custo_produto: number; impostos: number;
  total_known: number; gross_revenue: number; pct_receita: number;
}
export function MLCostCard({ costSummary }: { costSummary: CostSummary })
// Exibe badge "Em desenvolvimento" — remover
// Exibe custo_produto e impostos como "a informar" — substituir por dados reais
```

De src/pages/MercadoLivre.tsx (estado após Fase 16):
```typescript
// Já disponível:
const { data: ordersSummary } = useMLOrders(currentFrom, currentTo);
const { data: kpiSummary } = useMLKPISummary(currentFrom, currentTo, adsSummary.total_spend);
const adsSummary = useMemo(() => computeAdsSummary(adsDaily.filter(...)), [...]);

// costSummary atual (a substituir):
const costSummary = useMemo(() => {
  const comissao = ordersSummary?.total_comissao ?? grossRevenue * 0.11;
  const frete = ordersSummary?.total_frete ?? grossRevenue * 0.05;
  const ads = adsSummary.total_spend;
  return { comissao, frete, publicidade: ads, custo_produto: 0, impostos: 0, ... };
}, [...]);
```

Tabela public.orders (campos relevantes):
```sql
receita_bruta NUMERIC, custo_unit NUMERIC (nullable),
quantidade INTEGER, frete NUMERIC, comissao NUMERIC,
status TEXT, -- 'paid'|'shipped'|'delivered'|'cancelled'|'returned'|'pending'
ml_user_id TEXT, organization_id UUID, data_pedido TEXT
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook useMLCostWaterfall</name>
  <files>src/hooks/useMLCostWaterfall.ts</files>
  <behavior>
    - Quando orders tem pedidos pagos com custo_unit preenchido: retorna cmv = SUM(custo_unit * quantidade)
    - Quando custo_unit é null em todos os pedidos: has_cmv = false, cmv = 0
    - Quando orders está vazio: retorna null
    - cancelled_revenue = SUM(receita_bruta) WHERE status IN ('cancelled','returned'); 0 se nenhum
    - revenue_per_store: Map<ml_user_id, number> de receita_bruta de pedidos pagos por loja
    - Pedidos "pagos" = status IN ('paid','shipped','delivered')
  </behavior>
  <action>
Criar `src/hooks/useMLCostWaterfall.ts`:

```typescript
import { useQuery } from "@tanstack/react-query";
import { useMLStore } from "@/contexts/MLStoreContext";
import { useOrganization } from "@/contexts/OrganizationContext";
import { supabase } from "@/integrations/supabase/client";

export interface CostWaterfallData {
  /** Receita bruta de pedidos pagos (paid/shipped/delivered) */
  paid_revenue: number;
  /** Receita de pedidos cancelados/devolvidos no período */
  cancelled_revenue: number;
  /** SUM(comissao) dos pedidos pagos */
  total_comissao: number;
  /** SUM(frete) dos pedidos pagos */
  total_frete: number;
  /** SUM(custo_unit * quantidade) dos pedidos pagos com custo_unit != null */
  cmv: number;
  /** true se ao menos 1 pedido pago tem custo_unit preenchido */
  has_cmv: boolean;
  /** Receita bruta por ml_user_id (para calcular impostos no caller) */
  revenue_per_store: Map<string, number>;
}

const PAID_STATUSES = ["paid", "shipped", "delivered"];
const CANCELLED_STATUSES = ["cancelled", "returned"];

export function useMLCostWaterfall(from: string, to: string) {
  const { resolvedMLUserIds } = useMLStore();
  const { currentOrg } = useOrganization();
  const orgId = currentOrg?.id ?? null;

  return useQuery<CostWaterfallData | null>({
    queryKey: ["ml", "cost-waterfall", orgId, resolvedMLUserIds, from, to],
    queryFn: async (): Promise<CostWaterfallData | null> => {
      if (!orgId || resolvedMLUserIds.length === 0) return null;

      const { data, error } = await supabase
        .from("orders")
        .select("receita_bruta, custo_unit, quantidade, frete, comissao, status, ml_user_id")
        .eq("organization_id", orgId)
        .in("ml_user_id", resolvedMLUserIds)
        .gte("data_pedido", from)
        .lte("data_pedido", to);

      if (error) throw error;

      const rows = data ?? [];
      if (rows.length === 0) return null;

      let paid_revenue = 0;
      let cancelled_revenue = 0;
      let total_comissao = 0;
      let total_frete = 0;
      let cmv = 0;
      let has_cmv = false;
      const revenue_per_store = new Map<string, number>();

      for (const r of rows) {
        const receita = r.receita_bruta ?? 0;
        const status = r.status ?? "";
        const mlUserId = r.ml_user_id ?? "";

        if (PAID_STATUSES.includes(status)) {
          paid_revenue += receita;
          total_comissao += r.comissao ?? 0;
          total_frete += r.frete ?? 0;
          revenue_per_store.set(mlUserId, (revenue_per_store.get(mlUserId) ?? 0) + receita);

          const custo = r.custo_unit;
          const qty = r.quantidade ?? 1;
          if (custo != null) {
            has_cmv = true;
            cmv += custo * qty;
          }
        } else if (CANCELLED_STATUSES.includes(status)) {
          cancelled_revenue += receita;
        }
      }

      return {
        paid_revenue,
        cancelled_revenue,
        total_comissao,
        total_frete,
        cmv: Math.round(cmv * 100) / 100,
        has_cmv,
        revenue_per_store,
      };
    },
    enabled: !!orgId && resolvedMLUserIds.length > 0 && !!from && !!to,
    staleTime: 5 * 60 * 1000,
  });
}
```
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "useMLCostWaterfall" | head -10</automated>
  </verify>
  <done>Arquivo existe, exporta CostWaterfallData e useMLCostWaterfall, zero erros TS</done>
</task>

<task type="auto">
  <name>Task 2: Redesenhar MLCostCard com waterfall completo</name>
  <files>src/components/mercadolivre/MLCostCard.tsx</files>
  <action>
Substituir o conteúdo completo de `src/components/mercadolivre/MLCostCard.tsx` pelo seguinte:

```typescript
import { motion } from "framer-motion";
import { Card, CardContent } from "@/components/ui/card";
import { DollarSign, Truck, Megaphone, Package, TrendingDown, TrendingUp } from "lucide-react";

const fmt = (v: number) =>
  v.toLocaleString("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 0 });

const pct = (v: number, base: number) =>
  base > 0 ? `${((v / base) * 100).toFixed(1)}%` : "—";

interface WaterfallLine {
  icon: React.ReactNode;
  label: string;
  value: number | null;
  /** valor exibido quando value === null */
  nullLabel?: string;
  base: number;
  color: string;
}

interface CostWaterfallCardProps {
  /** Receita bruta (de ml_daily_cache ou orders) */
  gross_revenue: number;
  /** Receita de pedidos cancelados/devolvidos — 0 se nenhum */
  cancelled_revenue: number;
  comissao: number;
  frete: number;
  publicidade: number;
  /** SUM(custo_unit × quantidade) — null = sem custo cadastrado */
  cmv: number | null;
  /** SUM(receita × effective_rate/100) por loja — null = sem config fiscal */
  impostos: number | null;
  loading?: boolean;
}

export function MLCostCard({
  gross_revenue,
  cancelled_revenue,
  comissao,
  frete,
  publicidade,
  cmv,
  impostos,
  loading,
}: CostWaterfallCardProps) {
  const lines: WaterfallLine[] = [
    ...(cancelled_revenue > 0
      ? [{
          icon: <TrendingDown className="w-3.5 h-3.5 text-rose-400" />,
          label: "Cancelamentos",
          value: cancelled_revenue,
          nullLabel: undefined,
          base: gross_revenue,
          color: "text-rose-400",
        }]
      : []),
    {
      icon: <DollarSign className="w-3.5 h-3.5 text-orange-400" />,
      label: "Comissão ML",
      value: comissao,
      base: gross_revenue,
      color: "text-foreground",
    },
    {
      icon: <Truck className="w-3.5 h-3.5 text-blue-400" />,
      label: "Frete",
      value: frete,
      base: gross_revenue,
      color: "text-foreground",
    },
    {
      icon: <Megaphone className="w-3.5 h-3.5 text-purple-400" />,
      label: "Publicidade",
      value: publicidade,
      base: gross_revenue,
      color: "text-foreground",
    },
    {
      icon: <Package className="w-3.5 h-3.5 text-emerald-400" />,
      label: "CMV",
      value: cmv,
      nullLabel: "s/ custo",
      base: gross_revenue,
      color: "text-foreground",
    },
    {
      icon: <DollarSign className="w-3.5 h-3.5 text-amber-400" />,
      label: "Impostos",
      value: impostos,
      nullLabel: "s/ config",
      base: gross_revenue,
      color: "text-foreground",
    },
  ];

  const totalDeductions =
    cancelled_revenue +
    comissao +
    frete +
    publicidade +
    (cmv ?? 0) +
    (impostos ?? 0);

  const lucro = gross_revenue - totalDeductions;
  const lucroPositivo = lucro >= 0;

  return (
    <motion.div
      className="lg:col-span-2"
      initial={{ opacity: 0, y: 16 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.4 }}
    >
      <Card className="h-full relative overflow-hidden">
        <div className="px-4 pt-4 pb-2 flex items-center justify-between">
          <span className="text-sm font-medium text-foreground">Custos</span>
        </div>
        <CardContent className="px-4 pb-4">
          {loading ? (
            <div className="space-y-2">
              {[...Array(6)].map((_, i) => (
                <div key={i} className="h-5 bg-muted/30 rounded animate-pulse" />
              ))}
            </div>
          ) : (
            <div className="space-y-0.5">
              {/* Receita bruta */}
              <div className="flex items-end justify-between pb-2 mb-1.5 border-b border-border">
                <div>
                  <p className="text-[10px] text-muted-foreground uppercase tracking-wider">Receita Bruta</p>
                  <p className="text-xl font-bold tabular-nums text-foreground">{fmt(gross_revenue)}</p>
                </div>
              </div>

              {/* Linhas do waterfall */}
              {lines.map((line) => (
                <div key={line.label} className="flex items-center justify-between text-xs py-1">
                  <span className="flex items-center gap-1.5 text-muted-foreground">
                    {line.icon}
                    {line.label}
                  </span>
                  <div className="flex items-center gap-2">
                    {line.value != null ? (
                      <>
                        <span className="text-[10px] text-muted-foreground tabular-nums w-10 text-right">
                          {pct(line.value, line.base)}
                        </span>
                        <span className="font-semibold tabular-nums w-24 text-right">
                          - {fmt(line.value)}
                        </span>
                      </>
                    ) : (
                      <span className="text-[10px] italic text-muted-foreground">
                        {line.nullLabel ?? "—"}
                      </span>
                    )}
                  </div>
                </div>
              ))}

              {/* Lucro bruto */}
              <div className="flex items-center justify-between text-xs pt-2.5 mt-1.5 border-t-2 border-border">
                <span className="flex items-center gap-1.5 font-semibold text-foreground">
                  {lucroPositivo
                    ? <TrendingUp className="w-3.5 h-3.5 text-emerald-500" />
                    : <TrendingDown className="w-3.5 h-3.5 text-red-500" />}
                  Lucro Bruto
                </span>
                <div className="flex items-center gap-2">
                  <span className="text-[10px] text-muted-foreground tabular-nums w-10 text-right">
                    {pct(Math.abs(lucro), gross_revenue)}
                  </span>
                  <span
                    className={`text-base font-bold tabular-nums w-24 text-right ${
                      lucroPositivo ? "text-emerald-500" : "text-red-500"
                    }`}
                  >
                    {fmt(lucro)}
                  </span>
                </div>
              </div>
            </div>
          )}
        </CardContent>
      </Card>
    </motion.div>
  );
}
```

**IMPORTANTE**: A interface muda de `{ costSummary: CostSummary }` para props explícitas. Isso exige atualizar o caller (MercadoLivre.tsx) na Task 3.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "MLCostCard" | head -10</automated>
  </verify>
  <done>
- Interface CostWaterfallCardProps com props explícitas (sem CostSummary)
- Badge "Em desenvolvimento" removido
- 7 linhas de waterfall + lucro bruto
- CMV e impostos com null handling
  </done>
</task>

<task type="auto">
  <name>Task 3: Wiring em MercadoLivre.tsx</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <action>
Ler `src/pages/MercadoLivre.tsx`. Fazer 4 mudanças cirúrgicas:

**1. Adicionar import do hook:**
```typescript
import { useMLCostWaterfall } from "@/hooks/useMLCostWaterfall";
```

**2. Adicionar import do useMLTaxConfig** (se ainda não estiver):
```typescript
import { useMLTaxConfig } from "@/hooks/useMLTaxConfig";
```

**3. Adicionar chamadas aos hooks** (após useMLOrders):
```typescript
const { data: costWaterfall, isLoading: costWaterfallLoading } = useMLCostWaterfall(currentFrom, currentTo);

// Tax config para calcular impostos por loja
const { data: taxMap } = useMLTaxConfig(resolvedMLUserIds, currentOrg?.id ?? "");

// Impostos: receita por loja × alíquota efetiva
const impostosTotal = useMemo(() => {
  if (!costWaterfall?.revenue_per_store || !taxMap || taxMap.size === 0) return null;
  let total = 0;
  let hasConfig = false;
  for (const [mlUserId, rev] of costWaterfall.revenue_per_store) {
    const taxEntry = taxMap.get(mlUserId);
    if (taxEntry?.effective_rate != null && taxEntry.effective_rate > 0) {
      hasConfig = true;
      total += (rev * taxEntry.effective_rate) / 100;
    }
  }
  return hasConfig ? Math.round(total * 100) / 100 : null;
}, [costWaterfall, taxMap]);
```

**ATENÇÃO**: O `useMemo` acima deve vir APÓS `costWaterfall` e `taxMap` estarem definidos. Colocar logo após os dois hooks.

**4. Substituir o uso de `<MLCostCard>` no JSX:**

Localizar:
```tsx
<MLCostCard costSummary={costSummary} />
```

Substituir por:
```tsx
<MLCostCard
  gross_revenue={effectiveMetrics?.total_revenue ?? 0}
  cancelled_revenue={costWaterfall?.cancelled_revenue ?? 0}
  comissao={costWaterfall?.total_comissao ?? ordersSummary?.total_comissao ?? (effectiveMetrics?.total_revenue ?? 0) * 0.11}
  frete={costWaterfall?.total_frete ?? ordersSummary?.total_frete ?? (effectiveMetrics?.total_revenue ?? 0) * 0.05}
  publicidade={adsSummary.total_spend}
  cmv={costWaterfall?.has_cmv ? costWaterfall.cmv : null}
  impostos={impostosTotal}
  loading={costWaterfallLoading}
/>
```

**NOTA**: O `costSummary` useMemo pode ser removido se não for usado em outro lugar. Verificar se `costSummary` ainda é referenciado no JSX antes de remover. Se sim, manter e adaptar para manter compatibilidade.

Verificar também se `useMLTaxConfig` precisa de `currentOrg` — o hook aceita `orgId: string`, então usar `currentOrg?.id ?? ""`.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
- useMLCostWaterfall importado e chamado
- useMLTaxConfig importado e chamado
- impostosTotal calculado em useMemo
- MLCostCard recebe props individuais corretas
- Zero erros TypeScript
  </done>
</task>

</tasks>

<verification>
```bash
cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -10
```

Verificar visualmente:
- `/` → painel de Custos mostra waterfall completo: Receita Bruta → (-) Comissão → (-) Frete → (-) Publicidade → (-) CMV (ou "s/ custo") → (-) Impostos (ou "s/ config") → Lucro Bruto
- Badge "Em desenvolvimento" não aparece mais
- Lucro Bruto positivo = verde, negativo = vermelho
</verification>

<success_criteria>
1. `src/hooks/useMLCostWaterfall.ts` existe com CostWaterfallData e useMLCostWaterfall
2. MLCostCard recebe props individuais (sem CostSummary) e exibe waterfall de 7 linhas
3. CMV = null quando orders sem custo_unit → card exibe "s/ custo"
4. Impostos = null quando sem tax_config → card exibe "s/ config"
5. Lucro Bruto = receita bruta - todas as deduções (nulls tratados como 0)
6. Zero erros TypeScript
7. Badge "Em desenvolvimento" removido
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-waterfall-custos/17-01-SUMMARY.md` com:
- Fórmula do waterfall implementada
- Como CMV e impostos tratam null
- Fallbacks quando costWaterfall não disponível
- Status TypeScript check
</output>
