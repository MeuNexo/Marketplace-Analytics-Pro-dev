---
phase: 17-waterfall-custos
plan: "03"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/pages/MercadoLivre.tsx
  - src/components/mercadolivre/MLSalesAnalytics.tsx
autonomous: true
requirements:
  - CONSOLIDACAO-01
  - CONSOLIDACAO-02

must_haves:
  truths:
    - "A aba 'Relatórios' não existe mais no componente Tabs de MercadoLivre.tsx"
    - "As 4 seções analíticas (Horário, Ticket, Estado, Funil) aparecem na tab Vendas"
    - "Cada seção é expansível/colapsável via Accordion para não poluir a tela"
    - "KPIs existentes (Receita, Pedidos, etc.) não regridem"
    - "MLRelatorios.tsx não é mais importado em MercadoLivre.tsx"
  artifacts:
    - path: "src/components/mercadolivre/MLSalesAnalytics.tsx"
      provides: "Componente que encapsula as 4 seções analíticas em Accordion"
      exports: ["MLSalesAnalytics"]
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/components/mercadolivre/MLSalesAnalytics.tsx"
      via: "import { MLSalesAnalytics }"
      pattern: "MLSalesAnalytics"
---

<objective>
Consolidar a aba "Relatórios" na tab "Vendas". As 4 seções analíticas (Venda por Hora, Ticket Médio, Por Estado, Funil de Conversão) passam a existir como seções accordion expansíveis no final da tab Vendas — a aba "Relatórios" é removida completamente.

A página de Vendas passa a ser uma tela única bem organizada: KPI cards no topo → gráficos de marca (fase 16) → painel de custos → seções analíticas expansíveis.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-waterfall-custos/17-CONTEXT.md

<interfaces>

De src/pages/mercadolivre/MLRelatorios.tsx (fonte dos sub-componentes):
```typescript
// 4 funções internas que precisam ser exportadas/movidas:
function TabHorario()  // usa salesCache.hourly (memória — sem fetch)
function TabTicket()   // usa salesCache.daily (memória — sem fetch)
function TabEstado()   // usa useMLStateQuery(rangeFrom, rangeTo) (1 fetch)
function TabFunil()    // usa salesCache.daily (memória — sem fetch)
// Todas usam useMLStore() para salesCache
```

shadcn/ui Accordion (já instalado via Radix):
```typescript
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion";
// Uso: type="multiple" para permitir múltiplos abertos simultaneamente
```

De src/pages/MercadoLivre.tsx (estrutura a modificar):
```typescript
// Estado atual:
import MLRelatorios from "./mercadolivre/MLRelatorios";

<Tabs defaultValue="vendas">
  <TabsList>
    <TabsTrigger value="vendas">Vendas</TabsTrigger>
    <TabsTrigger value="relatorios">Relatórios</TabsTrigger>  ← REMOVER
  </TabsList>
  <TabsContent value="vendas">
    {/* ... KPI cards, gráficos, custos ... */}
  </TabsContent>
  <TabsContent value="relatorios">  ← REMOVER
    <MLRelatorios />
  </TabsContent>
</Tabs>
```

Posição dos gráficos de marca na tab Vendas (após fase 16):
```tsx
{/* ── Gráficos de Marca — Phase 16 ── */}
<div className="space-y-3">
  <BrandRevenueChart ... />
  <BrandMarkupChart ... />
  <CustoOperacionalChart ... />
  <BrandSharePieChart ... />
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar MLSalesAnalytics.tsx com as 4 seções em Accordion</name>
  <files>src/components/mercadolivre/MLSalesAnalytics.tsx</files>
  <action>
Ler `src/pages/mercadolivre/MLRelatorios.tsx` completo.

Criar `src/components/mercadolivre/MLSalesAnalytics.tsx` que:
1. Copia/importa o conteúdo das 4 funções (TabHorario, TabTicket, TabEstado, TabFunil) do MLRelatorios.tsx — mover a lógica para este novo arquivo
2. Envolve cada uma em um item de Accordion

```typescript
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
import { Clock3, TrendingUp, MapPin, GitMerge } from "lucide-react";
// ... (importar tudo que TabHorario/TabTicket/TabEstado/TabFunil precisam)

// Copiar as 4 funções internas do MLRelatorios.tsx aqui (com os mesmos imports)

export function MLSalesAnalytics() {
  return (
    <div className="space-y-1">
      <p className="text-xs text-muted-foreground font-medium uppercase tracking-wider px-1 pb-1">
        Análises Detalhadas
      </p>
      <Accordion type="multiple" className="space-y-2">
        <AccordionItem value="horario" className="border rounded-lg px-3">
          <AccordionTrigger className="text-sm font-medium hover:no-underline py-3">
            <span className="flex items-center gap-2">
              <Clock3 className="w-4 h-4 text-muted-foreground" />
              Venda por Hora
            </span>
          </AccordionTrigger>
          <AccordionContent className="pb-4">
            <TabHorario />
          </AccordionContent>
        </AccordionItem>

        <AccordionItem value="ticket" className="border rounded-lg px-3">
          <AccordionTrigger className="text-sm font-medium hover:no-underline py-3">
            <span className="flex items-center gap-2">
              <TrendingUp className="w-4 h-4 text-muted-foreground" />
              Ticket Médio
            </span>
          </AccordionTrigger>
          <AccordionContent className="pb-4">
            <TabTicket />
          </AccordionContent>
        </AccordionItem>

        <AccordionItem value="estado" className="border rounded-lg px-3">
          <AccordionTrigger className="text-sm font-medium hover:no-underline py-3">
            <span className="flex items-center gap-2">
              <MapPin className="w-4 h-4 text-muted-foreground" />
              Vendas por Estado
            </span>
          </AccordionTrigger>
          <AccordionContent className="pb-4">
            <TabEstado />
          </AccordionContent>
        </AccordionItem>

        <AccordionItem value="funil" className="border rounded-lg px-3">
          <AccordionTrigger className="text-sm font-medium hover:no-underline py-3">
            <span className="flex items-center gap-2">
              <GitMerge className="w-4 h-4 text-muted-foreground" />
              Funil de Conversão
            </span>
          </AccordionTrigger>
          <AccordionContent className="pb-4">
            <TabFunil />
          </AccordionContent>
        </AccordionItem>
      </Accordion>
    </div>
  );
}
```

**ATENÇÃO**: Copiar os 4 sub-componentes internos do MLRelatorios.tsx (TabHorario, TabTicket, TabEstado, TabFunil) e todos os seus imports para este arquivo. Incluir também os helpers (currencyFmt, pctFmt, tooltipStyle, BRAND_COLORS, UF_NAME_FALLBACK, EmptyState). NÃO importar de MLRelatorios — copiar o código diretamente para evitar dependência circular.

Após criar MLSalesAnalytics.tsx, verificar se `Accordion` está disponível no projeto:
```bash
grep -r "AccordionItem\|from.*accordion" /root/garment-glow-test/src --include="*.tsx" | head -5
```
Se não estiver, verificar se existe em `src/components/ui/accordion.tsx`. Se existir o arquivo mas não estiver sendo usado em nenhum lugar, está ok usá-lo.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "MLSalesAnalytics" | head -10</automated>
  </verify>
  <done>
- Arquivo existe com MLSalesAnalytics exportado
- 4 sub-componentes internos (TabHorario, TabTicket, TabEstado, TabFunil) incluídos
- Accordion com 4 items
- Zero erros TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar MercadoLivre.tsx — remover aba Relatórios, adicionar MLSalesAnalytics</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <action>
Ler `src/pages/MercadoLivre.tsx`. Fazer 4 mudanças cirúrgicas:

**Mudança 1 — Remover import de MLRelatorios:**
```typescript
// REMOVER:
import MLRelatorios from "./mercadolivre/MLRelatorios";
```

**Mudança 2 — Adicionar import de MLSalesAnalytics:**
```typescript
import { MLSalesAnalytics } from "@/components/mercadolivre/MLSalesAnalytics";
```

**Mudança 3 — Remover TabsTrigger de Relatórios:**
Localizar:
```tsx
<TabsTrigger value="relatorios" className="text-xs px-3 h-7">Relatórios</TabsTrigger>
```
Remover completamente essa linha.

**Mudança 4 — Remover TabsContent de Relatórios e adicionar MLSalesAnalytics na tab Vendas:**

Localizar e remover:
```tsx
<TabsContent value="relatorios" className="mt-0 animate-fade-in">
  <MLRelatorios />
</TabsContent>
```

Na tab Vendas (`<TabsContent value="vendas" ...>`), localizar o final do conteúdo (antes do `</TabsContent>`) e adicionar o componente de analytics:

Localizar o último elemento significativo dentro da TabsContent value="vendas":
```tsx
{/* ── Gráficos de Marca — Phase 16 ── */}
<div className="space-y-3">
  ...
  <BrandSharePieChart ... />
</div>
```

Após esse bloco, adicionar:
```tsx
{/* ── Análises Detalhadas — Phase 17 ── */}
<MLSalesAnalytics />
```

**VERIFICAR** se há outros usos de `MLRelatorios` no arquivo antes de remover o import. Se `defaultValue` do Tabs era `"relatorios"`, mudar para `"vendas"`.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
- Import MLRelatorios removido
- Import MLSalesAnalytics adicionado
- TabsTrigger "relatorios" removido
- TabsContent "relatorios" removido
- MLSalesAnalytics aparece no final da tab Vendas
- Tabs tem apenas "vendas" (ou outros tabs se existirem, mas não "relatorios")
- Zero erros TypeScript
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript limpo
cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -5

# Confirmar que MLRelatorios não é mais importado
grep "MLRelatorios" /root/garment-glow-test/src/pages/MercadoLivre.tsx && echo "ERRO: ainda importado" || echo "OK: não importado"

# Confirmar que não há tab relatorios
grep "relatorios" /root/garment-glow-test/src/pages/MercadoLivre.tsx && echo "AVISO: ainda existe" || echo "OK: removido"

# Confirmar que MLSalesAnalytics existe
ls /root/garment-glow-test/src/components/mercadolivre/MLSalesAnalytics.tsx
```

Verificação visual:
- Acessar `/` — apenas 1 tab "Vendas" (sem aba Relatórios)
- Rolar até o final da página → 4 acordeões fechados: Venda por Hora, Ticket Médio, Vendas por Estado, Funil de Conversão
- Clicar em "Venda por Hora" → expande com gráfico de barras
- Clicar em "Ticket Médio" → expande com gráfico de área
</verification>

<success_criteria>
1. Aba "Relatórios" completamente removida (TabsTrigger + TabsContent)
2. `MLSalesAnalytics` aparece na tab Vendas como 4 acordeões expansíveis
3. `MLRelatorios` não é mais importado em MercadoLivre.tsx
4. Cada acordeão expande e exibe os gráficos/cards corretos
5. Zero erros TypeScript
6. KPIs existentes (Receita, Pedidos, Ticket, etc.) não regridem
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-waterfall-custos/17-03-SUMMARY.md` com:
- O que foi removido (aba Relatórios)
- O que foi criado (MLSalesAnalytics com 4 acordeões)
- Estrutura final da página de Vendas
- Status TypeScript
</output>
