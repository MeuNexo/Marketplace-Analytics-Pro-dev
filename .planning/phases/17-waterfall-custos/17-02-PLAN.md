---
phase: 17-waterfall-custos
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260521210000_ml_tokens_add_tiny.sql
  - supabase/functions/sync-tiny-costs/index.ts
  - src/pages/mercadolivre/Integrations.tsx
autonomous: true
requirements:
  - TINY-01
  - TINY-02
  - TINY-03

must_haves:
  truths:
    - "ml_tokens tem colunas tiny_access_token, tiny_refresh_token, tiny_expires_at (nullable)"
    - "Edge function sync-tiny-costs lê precoCustoMedio do Tiny v3 e faz upsert em ml_product_costs"
    - "Matching por seller_sku: ml_product_costs WHERE seller_sku = tiny_sku"
    - "Falha em um produto não aborta o sync do restante — erro isolado por produto"
    - "Rate limit respeitado: 1.1s entre chamadas ao Tiny"
    - "Botão 'Conectar Tiny ERP' aparece na página /integracoes"
    - "Botão 'Sincronizar Custos' invoca sync-tiny-costs e mostra feedback"
  artifacts:
    - path: "supabase/migrations/20260521210000_ml_tokens_add_tiny.sql"
      provides: "Migration que adiciona colunas Tiny em ml_tokens"
      contains: "ADD COLUMN IF NOT EXISTS tiny_access_token"
    - path: "supabase/functions/sync-tiny-costs/index.ts"
      provides: "Edge function que sincroniza custos do Tiny para ml_product_costs"
      contains: "precoCustoMedio, seller_sku"
  key_links:
    - from: "supabase/functions/sync-tiny-costs/index.ts"
      to: "public.ml_product_costs"
      via: "upsert ON CONFLICT seller_sku"
      pattern: "ml_product_costs"
---

<objective>
Criar o pipeline de CMV automático via Tiny ERP: tokens OAuth armazenados em `ml_tokens`, edge function `sync-tiny-costs` que lê `precoCustoMedio` de cada produto no Tiny e atualiza `ml_product_costs.cost` por SKU. UI na página /integracoes para conectar e sincronizar.

O resultado: custo_unit em `public.orders` passa a ser populado automaticamente (sync-ml-orders já lê costMap de ml_product_costs), eliminando entrada manual por produto.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-waterfall-custos/17-CONTEXT.md

<interfaces>

Tiny ERP API v3:
```
Base URL: https://api.tiny.com.br/public-api/v3
Token URL: https://accounts.tiny.com.br/realms/tiny/protocol/openid-connect/token
Auth URL:  https://accounts.tiny.com.br/realms/tiny/protocol/openid-connect/auth

GET /produtos?situacao=A&limit=100&offset=0
→ { data: [...] } ou { data: { itens: [...], paginacao: { total } } }
Cada item: { id, sku (ou codigo), nome, tipoVariacao }
Ignorar tipoVariacao === "P" (produto pai)

GET /produtos/{id}
→ { data: { produto: { id, nome, codigo, precos: { precoCustoMedio, precoCusto, precoVenda } } } }
ou flat: { data: { id, nome, codigo, precos: { ... } } }

Rate limit: 60 req/min → 1.1s entre requests
```

Tabela ml_product_costs (garment-glow-test):
```sql
-- Colunas existentes:
item_id TEXT NOT NULL,  -- ML item ID (ex: MLB123)
cost NUMERIC,           -- custo unitário (a ser preenchido pelo sync)
tax_rate NUMERIC,
seller_sku TEXT,        -- SKU do produto — chave de matching com Tiny
user_id UUID NOT NULL,  -- proprietário (padrão existente do projeto)
-- UNIQUE: (user_id, item_id)
-- NOTA: NÃO há unique em (user_id, seller_sku) ainda — a migration deve criar
```

Tabela ml_tokens (garment-glow-test):
```sql
-- Colunas existentes relevantes:
ml_user_id TEXT NOT NULL,
user_id UUID NOT NULL,
organization_id UUID,
-- NÃO tem colunas Tiny ainda
```

Edge function auth pattern (garment-glow-test):
```typescript
// Todas as edge functions usam user JWT para auth:
const { data: { user } } = await userClient.auth.getUser();
if (!user) return json({ error: "Unauthorized" }, 401);
// Para sync-tiny-costs: aceitar também service_role (para uso interno/cron)
```

De src/pages/mercadolivre/Integrations.tsx (estrutura esperada):
```
Página /integracoes com seção ML já existente.
Adicionar nova seção "Tiny ERP" abaixo.
```

OAuth Tiny:
```
- client_id e client_secret: guardar em Supabase Vault como
  TINY_CLIENT_ID e TINY_CLIENT_SECRET (env vars da edge function)
- Redirect URI: {SUPABASE_URL}/functions/v1/tiny-oauth?ml_user_id={ml_user_id}
- Após callback: armazenar tokens em ml_tokens
- Para esta fase: implementar apenas o fluxo de token via client_credentials
  (simplificação: usar client_credentials grant em vez de authorization_code,
  pois o client_credentials é suficiente para acesso à própria conta)
  
NOTA: Tiny v3 com grant_type=client_credentials:
POST /token com { grant_type: "client_credentials", client_id, client_secret }
Retorna access_token (sem refresh_token em client_credentials — re-gerar quando expirar)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — adicionar colunas Tiny em ml_tokens</name>
  <files>supabase/migrations/20260521210000_ml_tokens_add_tiny.sql</files>
  <action>
Criar `supabase/migrations/20260521210000_ml_tokens_add_tiny.sql`:

```sql
-- Phase 17: Tiny ERP integration — adiciona tokens e índice de SKU em ml_product_costs

-- Tokens Tiny em ml_tokens (por ml_user_id)
ALTER TABLE public.ml_tokens
  ADD COLUMN IF NOT EXISTS tiny_access_token  TEXT,
  ADD COLUMN IF NOT EXISTS tiny_expires_at    BIGINT;

COMMENT ON COLUMN public.ml_tokens.tiny_access_token IS
  'Access token Tiny ERP (client_credentials). Regenerado quando expirado.';
COMMENT ON COLUMN public.ml_tokens.tiny_expires_at IS
  'Unix timestamp (segundos) de expiração do tiny_access_token.';

-- Índice único em ml_product_costs por (user_id, seller_sku) para upsert do sync-tiny-costs
-- Necessário para ON CONFLICT (user_id, seller_sku) funcionar
CREATE UNIQUE INDEX IF NOT EXISTS ml_product_costs_user_sku
  ON public.ml_product_costs (user_id, seller_sku)
  WHERE seller_sku IS NOT NULL;
```

Aplicar via Supabase MCP (`apply_migration`) ou CLI:
```bash
cd /root/garment-glow-test && supabase db push --project-ref ckcdevcxgvueywivefgx
```
  </action>
  <verify>
    <automated>ls /root/garment-glow-test/supabase/migrations/20260521210000_ml_tokens_add_tiny.sql && echo "file exists"</automated>
  </verify>
  <done>Arquivo existe com ADD COLUMN tiny_access_token + CREATE UNIQUE INDEX ml_product_costs_user_sku</done>
</task>

<task type="auto">
  <name>Task 2: Edge function sync-tiny-costs</name>
  <files>supabase/functions/sync-tiny-costs/index.ts</files>
  <action>
Criar o diretório e arquivo `supabase/functions/sync-tiny-costs/index.ts`:

```typescript
import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;
const SERVICE_KEY  = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const TINY_API     = "https://api.tiny.com.br/public-api/v3";
const TINY_TOKEN_URL = "https://accounts.tiny.com.br/realms/tiny/protocol/openid-connect/token";
const TINY_CLIENT_ID     = Deno.env.get("TINY_CLIENT_ID") ?? "";
const TINY_CLIENT_SECRET = Deno.env.get("TINY_CLIENT_SECRET") ?? "";
const RATE_MS   = 1100; // 60 req/min → 1 req/s com margem
const BATCH_SIZE = 50;

const sb = createClient(SUPABASE_URL, SERVICE_KEY);

function sleep(ms: number) { return new Promise((r) => setTimeout(r, ms)); }

function json(body: unknown, status = 200) {
  return new Response(JSON.stringify(body), {
    status,
    headers: { "Content-Type": "application/json" },
  });
}

// ── Token management ──────────────────────────────────────────────────────────

async function getTinyToken(mlUserId: string): Promise<string> {
  // Tentar usar token em cache primeiro
  const { data: tok } = await sb
    .from("ml_tokens")
    .select("tiny_access_token, tiny_expires_at")
    .eq("ml_user_id", mlUserId)
    .single();

  const now = Math.floor(Date.now() / 1000);
  if (tok?.tiny_access_token && tok.tiny_expires_at && tok.tiny_expires_at - now > 300) {
    return tok.tiny_access_token;
  }

  // Gerar novo token via client_credentials
  if (!TINY_CLIENT_ID || !TINY_CLIENT_SECRET) {
    throw new Error("TINY_CLIENT_ID ou TINY_CLIENT_SECRET não configurados");
  }

  const resp = await fetch(TINY_TOKEN_URL, {
    method: "POST",
    headers: { "Content-Type": "application/x-www-form-urlencoded" },
    body: new URLSearchParams({
      grant_type:    "client_credentials",
      client_id:     TINY_CLIENT_ID,
      client_secret: TINY_CLIENT_SECRET,
    }),
  });

  if (!resp.ok) {
    throw new Error(`Tiny token error ${resp.status}: ${await resp.text()}`);
  }

  const data = await resp.json();
  const accessToken  = data.access_token as string;
  const expiresAt    = now + (data.expires_in ?? 3600);

  await sb
    .from("ml_tokens")
    .update({ tiny_access_token: accessToken, tiny_expires_at: expiresAt })
    .eq("ml_user_id", mlUserId);

  return accessToken;
}

// ── Tiny API helpers ──────────────────────────────────────────────────────────

async function tinyGet(token: string, path: string, params: Record<string, string> = {}): Promise<any> {
  const url = new URL(`${TINY_API}${path}`);
  for (const [k, v] of Object.entries(params)) url.searchParams.set(k, v);
  const resp = await fetch(url.toString(), {
    headers: { Authorization: `Bearer ${token}` },
  });
  if (resp.status === 429) throw new Error("Tiny 429 rate limit");
  if (!resp.ok) throw new Error(`Tiny ${path} ${resp.status}`);
  const res = await resp.json();
  return res.data ?? res;
}

// ── Fetch all active product IDs+SKUs ────────────────────────────────────────

async function fetchAllProducts(token: string): Promise<Array<{ id: string; sku: string; nome: string }>> {
  const products: Array<{ id: string; sku: string; nome: string }> = [];
  let offset = 0;
  const limit = 100;

  while (true) {
    const data = await tinyGet(token, "/produtos", { situacao: "A", limit: String(limit), offset: String(offset) });
    const itens: any[] = Array.isArray(data) ? data : (data?.itens ?? []);
    if (!itens.length) break;

    for (const p of itens) {
      if (p?.tipoVariacao === "P") continue; // pular produto pai
      const id  = String(p?.id || "");
      const sku = String(p?.sku || p?.codigo || "").trim();
      const nome = String(p?.nome || "").trim();
      if (id && sku) products.push({ id, sku, nome });
    }

    const total: number = data?.paginacao?.total ?? 0;
    offset += itens.length;
    if (itens.length < limit || (total > 0 && offset >= total)) break;
    await sleep(RATE_MS);
  }
  return products;
}

// ── Main handler ──────────────────────────────────────────────────────────────

serve(async (req: Request) => {
  try {
    // Auth: aceitar user JWT ou service_role
    const authHeader = req.headers.get("Authorization") ?? "";
    const userClient = createClient(SUPABASE_URL, authHeader.replace("Bearer ", ""));

    let userId: string | null = null;
    let mlUserId: string | null = null;

    // Tentar auth via JWT do usuário
    const { data: { user } } = await userClient.auth.getUser();
    if (user) {
      userId = user.id;
    } else if (authHeader.includes(SERVICE_KEY)) {
      // service_role: ml_user_id obrigatório no body
    } else {
      return json({ error: "Unauthorized" }, 401);
    }

    // Ler body
    let body: Record<string, unknown> = {};
    try { body = await req.json(); } catch { /* ok */ }

    mlUserId = (body.ml_user_id as string) ?? null;
    if (!mlUserId) return json({ error: "ml_user_id obrigatório" }, 400);

    // Buscar user_id a partir de ml_user_id (para usar como scope em ml_product_costs)
    const { data: tokenRow } = await sb
      .from("ml_tokens")
      .select("user_id")
      .eq("ml_user_id", mlUserId)
      .single();

    const scopeUserId = userId ?? (tokenRow?.user_id as string ?? null);
    if (!scopeUserId) return json({ error: `ml_user_id ${mlUserId} não encontrado em ml_tokens` }, 404);

    // Obter token Tiny
    const tinyToken = await getTinyToken(mlUserId);

    // Buscar todos os produtos ativos no Tiny
    const allProducts = await fetchAllProducts(tinyToken);
    if (allProducts.length === 0) {
      return json({ ok: true, synced: 0, msg: "Nenhum produto ativo no Tiny" });
    }

    let synced = 0;
    let errors = 0;
    const syncAt = new Date().toISOString();

    // Processar em batches de BATCH_SIZE
    for (let i = 0; i < allProducts.length; i += BATCH_SIZE) {
      const batch = allProducts.slice(i, i + BATCH_SIZE);
      const rows: any[] = [];

      for (const prod of batch) {
        try {
          const rawDetail = await tinyGet(tinyToken, `/produtos/${prod.id}`);
          const detail = rawDetail?.produto ?? rawDetail;
          const precos = detail?.precos ?? {};
          const precoCustoMedio = Number(precos.precoCustoMedio ?? 0);
          const precoCusto      = Number(precos.precoCusto ?? 0);
          const cost = precoCustoMedio || precoCusto; // preferir custo médio

          const sku  = String(detail?.codigo || detail?.sku || prod.sku || "").trim();
          const nome = String(detail?.nome || prod.nome || "").substring(0, 500);

          if (sku && cost > 0) {
            rows.push({
              user_id:    scopeUserId,
              item_id:    `TINY_${sku}`, // placeholder — será sobrescrito quando ML mapear
              seller_sku: sku,
              cost,
              updated_at: syncAt,
            });
          }
        } catch (_err) {
          errors++;
        }
        await sleep(RATE_MS);
      }

      if (rows.length > 0) {
        // Upsert por (user_id, seller_sku) — atualiza cost de produtos já mapeados
        const { error: upsertErr } = await sb
          .from("ml_product_costs")
          .upsert(rows, { onConflict: "user_id,seller_sku", ignoreDuplicates: false });

        if (!upsertErr) {
          synced += rows.length;
        } else {
          console.error("upsert error:", upsertErr.message);
          errors += rows.length;
        }
      }
    }

    return json({
      ok: true,
      synced,
      errors,
      total_products: allProducts.length,
      msg: `${synced} produtos sincronizados, ${errors} erros`,
    });
  } catch (err: any) {
    console.error("sync-tiny-costs error:", err);
    return json({ error: err.message }, 500);
  }
});
```

**NOTA sobre item_id**: `ml_product_costs` tem unique em `(user_id, item_id)` e o campo é NOT NULL. O Tiny não conhece o `item_id` do ML. Solução: usar `TINY_{sku}` como placeholder quando o item ainda não foi mapeado via ML. Quando `sync-ml-orders` faz o upsert de um pedido, ele usa `costMap` keyed por SKU (não por item_id), então o custo será encontrado corretamente.

**VERIFICAR** se `sync-ml-orders` usa `costMap` com chave `seller_sku` ou `item_id`. Se for `item_id`, o matching não funcionará automaticamente. Ver o arquivo atual antes de fazer o deploy.

Fazer deploy:
```bash
cd /root/garment-glow-test && supabase functions deploy sync-tiny-costs --project-ref ckcdevcxgvueywivefgx
```
  </action>
  <verify>
    <automated>ls /root/garment-glow-test/supabase/functions/sync-tiny-costs/index.ts && echo "exists"</automated>
  </verify>
  <done>
- Arquivo existe com getTinyToken + fetchAllProducts + upsert em ml_product_costs
- Deploy realizado sem erros
  </done>
</task>

<task type="auto">
  <name>Task 3: UI Tiny na página /integracoes</name>
  <files>src/pages/mercadolivre/Integrations.tsx</files>
  <action>
Ler `src/pages/mercadolivre/Integrations.tsx` completo primeiro.

Adicionar uma seção "Tiny ERP" após a seção ML existente. A seção deve:
1. Exibir estado de conexão (conectado vs não conectado)
2. Campo para `client_id` e `client_secret` do Tiny (salvos localmente para chamar o sync)
3. Botão "Sincronizar Custos" que invoca `sync-tiny-costs`
4. Mostrar feedback: loading spinner + resultado (`X produtos sincronizados`)

O padrão visual deve seguir o mesmo estilo das outras seções da página.

**Abordagem simplificada** (sem OAuth full, usando client_credentials diretamente):

A UI permite ao usuário informar `tiny_client_id` e `tiny_client_secret` diretamente. Esses valores são usados pela edge function via variáveis de ambiente (`TINY_CLIENT_ID`, `TINY_CLIENT_SECRET`). 

Para esta implementação, o fluxo é:
1. Admin configura `TINY_CLIENT_ID` e `TINY_CLIENT_SECRET` como env vars no Supabase (manualmente ou via dashboard)
2. A UI apenas chama `supabase.functions.invoke("sync-tiny-costs", { body: { ml_user_id } })`
3. Mostrar último sync e contagem de produtos com custo

Implementar um componente `TinyIntegrationCard` na mesma página ou em arquivo separado conforme padrão do projeto. Se a página de Integrações usar componentes separados, criar `src/components/mercadolivre/TinyIntegrationCard.tsx`. Se for inline, adicionar inline.

O card deve ter:
```tsx
<Card>
  <CardHeader>
    <CardTitle className="flex items-center gap-2">
      <Package className="w-4 h-4" />
      Tiny ERP
    </CardTitle>
    <CardDescription>
      Sincroniza custo de produtos do Tiny ERP para calcular CMV automaticamente
    </CardDescription>
  </CardHeader>
  <CardContent>
    {/* Seletor de loja ML */}
    {/* Botão "Sincronizar Custos" */}
    {/* Status da última sync */}
  </CardContent>
</Card>
```

Para o botão de sync, usar:
```typescript
const syncCosts = async (mlUserId: string) => {
  setSyncing(true);
  setResult(null);
  try {
    const { data, error } = await supabase.functions.invoke("sync-tiny-costs", {
      body: { ml_user_id: mlUserId },
    });
    if (error) throw error;
    setResult(data);
  } catch (err: any) {
    setResult({ error: err.message });
  } finally {
    setSyncing(false);
  }
};
```

Adaptar conforme o padrão de código da página. Se a página usa toast (sonner), usar toast para feedback em vez de estado local.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "Integrations\|TinyIntegration" | head -10</automated>
  </verify>
  <done>
- Seção Tiny ERP visível na página /integracoes
- Botão "Sincronizar Custos" funcional (invoca sync-tiny-costs)
- Feedback de resultado exibido
- Zero erros TypeScript
  </done>
</task>

</tasks>

<verification>
```bash
# Verificar migration existe
ls /root/garment-glow-test/supabase/migrations/20260521210000_ml_tokens_add_tiny.sql

# Verificar edge function existe
ls /root/garment-glow-test/supabase/functions/sync-tiny-costs/index.ts

# TypeScript
cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -5
```

**Verificar matching SKU em sync-ml-orders**:
```bash
grep -n "costMap\|seller_sku\|cost" /root/garment-glow-test/supabase/functions/sync-ml-orders/index.ts | head -20
```

Se `costMap` é keyed por `item_id` (não por `seller_sku`), documentar na SUMMARY e abrir uma task de follow-up para ajustar o matching.
</verification>

<success_criteria>
1. Migration existe com `ADD COLUMN tiny_access_token` em `ml_tokens`
2. `UNIQUE INDEX ml_product_costs_user_sku` criado em ml_product_costs
3. Edge function `sync-tiny-costs` existe e faz deploy sem erros
4. Página /integracoes tem seção Tiny com botão "Sincronizar Custos"
5. Invocar `sync-tiny-costs` atualiza `ml_product_costs.cost` por `seller_sku`
6. Zero erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-waterfall-custos/17-02-SUMMARY.md` com:
- Status da migration e deploy
- Como o matching SKU funciona (ou caveats se item_id vs seller_sku)
- Como configurar TINY_CLIENT_ID/TINY_CLIENT_SECRET no Supabase
- Status do TypeScript
</output>
