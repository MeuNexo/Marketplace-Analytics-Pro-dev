---
phase: 22-sync-automatico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260522_intraday_sync.sql
  - src/hooks/useMLLastSync.ts
  - src/hooks/useMLSync.ts
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04

must_haves:
  truths:
    - "Abrir a página não aciona 'Atualizando...' automático — nenhuma edge function é chamada no mount"
    - "Dados de hoje atualizam automaticamente a cada ≤2h (verificável em ml_sync_log.synced_at, source='auto')"
    - "Indicador 'Última sinc' exibe o synced_at real lido do Supabase, não um timestamp de localStorage"
    - "Quando o cron insere novo dado em ml_daily_cache, a UI invalida automaticamente via Realtime — sem reload manual"
    - "Botão 'Atualizar' continua funcional e grava em ml_sync_log"
  artifacts:
    - path: "supabase/migrations/20260522_intraday_sync.sql"
      provides: "Upsert de sync_interval_minutes=120 em organization_plans para todas as orgs ML ativas"
    - path: "src/hooks/useMLLastSync.ts"
      provides: "Hook React Query que lê ml_sync_log e retorna synced_at mais recente"
    - path: "src/hooks/useMLSync.ts"
      provides: "shouldAutoSync e AUTO_SYNC_STALE_MS removidos; syncFromAPI e lastSyncedAt mantidos"
    - path: "src/pages/MercadoLivre.tsx"
      provides: "Bloco autoSyncDoneRef removido; Realtime em ml_daily_cache adicionado; useMLLastSync integrado ao MLPageHeader"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "supabase.channel('ml_daily_cache_changes')"
      via: "postgres_changes subscription"
      pattern: "supabase\\.channel.*ml_daily_cache"
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useMLLastSync.ts"
      via: "useMLLastSync() → data passado como lastUpdated para MLPageHeader"
      pattern: "useMLLastSync"
    - from: "supabase/migrations/20260522_intraday_sync.sql"
      to: "dispatch_sync_jobs()"
      via: "organization_plans.sync_interval_minutes=120 → dispatch a cada 30min verifica elapsed"
      pattern: "sync_interval_minutes.*120"
---

<objective>
Eliminar o auto-sync disparado pelo frontend ao montar a página e substituí-lo por um modelo cache-first:
o cron intraday (existente, via `dispatch_sync_jobs()`) passa a rodar a cada 2h por org com ML conectado,
e a UI lê do Supabase sem nunca puxar dados da API ML sozinha.

Purpose: Remover a fricção de "Atualizando..." no mount, reduzir chamadas desnecessárias à API ML e
exibir o timestamp real de última sincronização (cron ou manual) em vez de um valor do localStorage.

Output:
- Migration que ativa sync intraday no backend (sem mudança de código de edge function)
- Hook `useMLLastSync` que expõe `synced_at` do `ml_sync_log`
- `MercadoLivre.tsx` sem auto-sync no mount, com Realtime para auto-refresh da UI
- `useMLSync.ts` sem `shouldAutoSync` e `AUTO_SYNC_STALE_MS`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-sync-automatico/22-CONTEXT.md
@.planning/phases/21-lucro-cache/21-01-PLAN.md

<!-- Interfaces relevantes extraídas do codebase -->
<interfaces>
<!-- organization_plans (supabase/migrations/20260519120000_organization_plans_quota.sql) -->
-- PRIMARY KEY: organization_id (uuid)
-- Coluna relevante: sync_interval_minutes integer NOT NULL DEFAULT 1440
-- ON CONFLICT target: organization_id
-- RLS: writes requerem service_role (migration roda como postgres/service_role → OK)

<!-- useMLSync.ts — exports que permanecem -->
export function useMLSync(opts: UseMLSyncOptions): {
  syncing: boolean;
  lastSyncedAt: string | null;
  setLastSyncedAt: (v: string | null) => void;
  syncProgress: { current: number; total: number } | null;
  syncFromAPI: (syncOpts?: { from?: Date; to?: Date; periodDays?: number }) => void;
  shouldAutoSync: () => boolean;   // ← REMOVER
  resetSync: () => void;
}
-- Constantes a remover: AUTO_SYNC_STALE_MS (exportada), LAST_ML_SYNC_TS_KEY
-- autoSyncTriggeredRef e shouldAutoSync callback inteiros removidos

<!-- useInvalidateMLQueries (src/hooks/useMLQueries.ts, linha ~194) -->
export function useInvalidateMLQueries(): {
  invalidateAll: () => Promise<void>;
  invalidateDaily: () => Promise<void>;
  invalidateHourly: () => Promise<void>;
  invalidateProducts: () => Promise<void>;
  invalidateUser: () => Promise<void>;
}

<!-- MLPageHeader (src/components/mercadolivre/MLPageHeader.tsx) -->
interface Props {
  title: string;
  children?: React.ReactNode;
  lastUpdated?: Date | null;  // undefined = sem linha; null = nunca sincronizado; Date = formata
}
-- showSync = lastUpdated !== undefined
-- Renderiza: "Última sinc: DD/MM/YYYY HH:MM:SS" ou "Nunca sincronizado"

<!-- MercadoLivre.tsx — bloco a remover (linhas 196-225) -->
const autoSyncDoneRef = useRef(false);
useEffect(() => {
  if (!user || storeLoading || autoSyncDoneRef.current || !connected) return;
  autoSyncDoneRef.current = true;
  // inventory fetch via supabase.functions.invoke("ml-inventory", ...)
  if (shouldAutoSync()) {
    syncFromAPI();
  }
}, [user, storeLoading, connected, stores, resolvedMLUserIds, shouldAutoSync, syncFromAPI]);

useEffect(() => {
  autoSyncDoneRef.current = false;
  resetSync();
  setProductStockMap({});
}, [scopeKey, resetSync]);
-- O inventory fetch (ml-inventory) DEVE ser mantido em effect separado (sem condicional shouldAutoSync)
-- O "Reset on scope change" (autoSyncDoneRef.current = false) deixa de ser necessário após remoção;
   manter apenas resetSync() e setProductStockMap({}) no effect de scopeKey

<!-- ml_sync_log — colunas relevantes para useMLLastSync -->
-- organization_id uuid
-- ml_user_id text
-- synced_at timestamptz
-- source text  ('auto' | 'manual')
-- Sem RLS bloqueando SELECT para authenticated (confirmado pelo uso em useMLSync.syncFromAPI)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — ativar sync intraday via organization_plans</name>
  <files>supabase/migrations/20260522_intraday_sync.sql</files>
  <action>
Criar o arquivo de migration com o seguinte conteúdo exato:

```sql
-- Phase 22: Ativar sync intraday (a cada 2h) para todas as orgs com ML conectado.
--
-- dispatch_sync_jobs() já roda a cada 30min e respeita sync_interval_minutes.
-- Com sync_interval_minutes = 120, o dispatcher cria um job quando elapsed >= 2h.
-- Jobs com date_from = NULL → mercado-libre-integration usa days = 1 (hoje em BRT).
--
-- Operação idempotente: ON CONFLICT atualiza somente sync_interval_minutes.
-- Orgs sem registro em organization_plans recebem novo registro (plan_tier = 'free' default).
-- Orgs que já têm registro (ex: enterprise com -1) são atualizadas para 120.
--
-- NOTA: Se uma org enterprise precisar manter intervalo customizado,
-- ajustar manualmente após esta migration.

INSERT INTO public.organization_plans (organization_id, sync_interval_minutes)
SELECT DISTINCT organization_id, 120
FROM public.ml_tokens
WHERE access_token IS NOT NULL
ON CONFLICT (organization_id)
  DO UPDATE SET
    sync_interval_minutes = 120,
    updated_at = now();
```

Não adicionar nenhuma outra instrução DDL — esta migration é apenas de dados de configuração.
  </action>
  <verify>
File exists at supabase/migrations/20260522_intraday_sync.sql with the INSERT statement.
  </verify>
  <done>
Migration commitada no repositório. Quando aplicada ao Supabase, todas as orgs com ml_tokens
preenchido passam a ter sync_interval_minutes=120. O dispatcher existente cria jobs a cada ~2h.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook useMLLastSync — synced_at real do Supabase</name>
  <files>src/hooks/useMLLastSync.ts</files>
  <behavior>
    - Retorna `{ data: string | null, isLoading: boolean }` de useQuery
    - Query desabilitada se currentOrg?.id for undefined ou resolvedMLUserIds.length === 0
    - queryKey inclui ['ml_last_sync', orgId, resolvedMLUserIds] para invalidação correta
    - staleTime: 2 * 60 * 1000 (2 minutos)
    - refetchInterval: 5 * 60 * 1000 (5 minutos — polling leve)
    - Quando ml_sync_log não tem registro: retorna null (nunca sincronizado)
    - Query ordena por synced_at DESC, limit 1, usa .maybeSingle()
  </behavior>
  <action>
Criar `src/hooks/useMLLastSync.ts` com o seguinte conteúdo:

```typescript
import { useQuery } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
import { useMLStore } from "@/contexts/MLStoreContext";
import { useOrganization } from "@/contexts/OrganizationContext";

/**
 * Retorna o synced_at mais recente do ml_sync_log para a org/lojas atuais.
 * Fonte de verdade para o indicador "Última atualização" — reflete quando o
 * cron (source='auto') ou o botão Atualizar (source='manual') realmente rodaram.
 *
 * staleTime: 2min | refetchInterval: 5min (polling leve; atualiza quando cron roda)
 */
export function useMLLastSync() {
  const { resolvedMLUserIds } = useMLStore();
  const { currentOrg } = useOrganization();

  return useQuery({
    queryKey: ["ml_last_sync", currentOrg?.id, resolvedMLUserIds] as const,
    queryFn: async (): Promise<string | null> => {
      const { data } = await supabase
        .from("ml_sync_log")
        .select("synced_at")
        .eq("organization_id", currentOrg!.id)
        .in("ml_user_id", resolvedMLUserIds)
        .order("synced_at", { ascending: false })
        .limit(1)
        .maybeSingle();
      return data?.synced_at ?? null;
    },
    enabled: !!currentOrg?.id && resolvedMLUserIds.length > 0,
    staleTime: 2 * 60 * 1000,
    refetchInterval: 5 * 60 * 1000,
  });
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep useMLLastSync || echo "OK — sem erros em useMLLastSync"</automated>
  </verify>
  <done>
Arquivo criado sem erros de TypeScript. O hook exporta `useMLLastSync` que retorna
`UseQueryResult<string | null>`. Pronto para ser importado em MercadoLivre.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 3: Limpar useMLSync — remover shouldAutoSync e constantes obsoletas</name>
  <files>src/hooks/useMLSync.ts</files>
  <action>
Editar `src/hooks/useMLSync.ts` com as seguintes remoções cirúrgicas:

**1. Remover export da constante (linha ~13):**
```typescript
// REMOVER:
export const AUTO_SYNC_STALE_MS = 10 * 60 * 1000;
```

**2. Remover constante interna (linha ~12):**
```typescript
// REMOVER:
const LAST_ML_SYNC_TS_KEY = "ml_last_synced_ts";
```

**3. Remover o ref interno (linha ~60):**
```typescript
// REMOVER:
const autoSyncTriggeredRef = useRef(false);
```

**4. Remover o callback shouldAutoSync inteiro (linhas ~222-227):**
```typescript
// REMOVER:
const shouldAutoSync = useCallback(() => {
  if (autoSyncTriggeredRef.current) return false;
  autoSyncTriggeredRef.current = true;
  const lastTs = Number(localStorage.getItem(LAST_ML_SYNC_TS_KEY) ?? 0);
  return Date.now() - lastTs > AUTO_SYNC_STALE_MS;
}, []);
```

**5. Remover as duas linhas que gravam LAST_ML_SYNC_TS_KEY no localStorage (dentro de syncFromAPI, após o loop de sincronização, linhas ~186-188):**
```typescript
// REMOVER apenas a linha do TS_KEY (manter a linha do LAST_ML_SYNC_KEY):
localStorage.setItem(LAST_ML_SYNC_TS_KEY, String(Date.now()));
```
A linha `localStorage.setItem(LAST_ML_SYNC_KEY, nowIso)` permanece — ainda alimenta
o `lastSyncedAt` do módulo singleton para o botão Atualizar.

**6. Remover `shouldAutoSync` do objeto retornado (linhas ~234-242):**
```typescript
// ANTES:
return {
  syncing: state.syncing,
  lastSyncedAt: state.lastSyncedAt,
  setLastSyncedAt: (v: string | null) => _emit({ lastSyncedAt: v }),
  syncProgress: state.progress,
  syncFromAPI,
  shouldAutoSync,   // ← REMOVER esta linha
  resetSync,
};

// DEPOIS:
return {
  syncing: state.syncing,
  lastSyncedAt: state.lastSyncedAt,
  setLastSyncedAt: (v: string | null) => _emit({ lastSyncedAt: v }),
  syncProgress: state.progress,
  syncFromAPI,
  resetSync,
};
```

**Manter intacto:**
- `LAST_ML_SYNC_KEY` constant e uso em localStorage (alimenta `lastSyncedAt` do singleton)
- `syncFromAPI` completo (usado pelo botão Atualizar)
- `resetSync` (usado pelo scopeKey effect em MercadoLivre)
- `lastSyncedAt` e `setLastSyncedAt` no retorno
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep useMLSync || echo "OK — sem erros em useMLSync"</automated>
  </verify>
  <done>
`shouldAutoSync` e `AUTO_SYNC_STALE_MS` removidos. `syncFromAPI` e `lastSyncedAt` intactos.
TypeScript não reclama de uso de `shouldAutoSync` pois será removido de MercadoLivre.tsx na Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 4: MercadoLivre.tsx — remover auto-sync, adicionar Realtime e useMLLastSync</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <action>
Editar `src/pages/MercadoLivre.tsx` com quatro mudanças independentes:

---

**A. Adicionar import de useMLLastSync (junto aos outros imports de hooks, linha ~16):**
```typescript
import { useMLLastSync } from "@/hooks/useMLLastSync";
```

**B. Remover `shouldAutoSync` do destructuring de `useMLSync` (linha ~80 aprox):**
O destructuring atual inclui `shouldAutoSync`. Remover esse campo:
```typescript
// ANTES (exemplo):
const { syncing, lastSyncedAt, setLastSyncedAt, syncProgress, syncFromAPI, shouldAutoSync, resetSync } = useMLSync(...)

// DEPOIS:
const { syncing, lastSyncedAt, setLastSyncedAt, syncProgress, syncFromAPI, resetSync } = useMLSync(...)
```

**C. Adicionar chamada ao hook após o destructuring de useMLSync:**
```typescript
const { data: lastSyncTimestamp } = useMLLastSync();
```

**D. Substituir o bloco de auto-sync no mount (linhas ~195-225) por duas versões separadas:**

REMOVER o bloco completo:
```typescript
// REMOVER INTEIRO:
const autoSyncDoneRef = useRef(false);
useEffect(() => {
  if (!user || storeLoading || autoSyncDoneRef.current || !connected) return;
  autoSyncDoneRef.current = true;
  const firstStore = stores.find((s) => resolvedMLUserIds.includes(s.ml_user_id));
  if (firstStore) {
    supabase.functions
      .invoke("ml-inventory", { body: { ml_user_id: firstStore.ml_user_id } })
      .then(({ data: invData }) => {
        if (invData?.items) {
          const stockMap: Record<string, number> = {};
          for (const item of invData.items) stockMap[item.id] = item.available_quantity ?? 0;
          setProductStockMap(stockMap);
        }
      })
      .catch(() => {});
  }
  if (shouldAutoSync()) {
    syncFromAPI();
  }
}, [user, storeLoading, connected, stores, resolvedMLUserIds, shouldAutoSync, syncFromAPI]);

// Reset on scope change
useEffect(() => {
  autoSyncDoneRef.current = false;
  resetSync();
  setProductStockMap({});
}, [scopeKey, resetSync]);
```

ADICIONAR no lugar (dois effects separados):
```typescript
// ── Inventory fetch on connect (sem auto-sync) ──
useEffect(() => {
  if (!user || storeLoading || !connected) return;
  const firstStore = stores.find((s) => resolvedMLUserIds.includes(s.ml_user_id));
  if (!firstStore) return;
  supabase.functions
    .invoke("ml-inventory", { body: { ml_user_id: firstStore.ml_user_id } })
    .then(({ data: invData }) => {
      if (invData?.items) {
        const stockMap: Record<string, number> = {};
        for (const item of invData.items) stockMap[item.id] = item.available_quantity ?? 0;
        setProductStockMap(stockMap);
      }
    })
    .catch(() => {});
}, [user, storeLoading, connected, stores, resolvedMLUserIds]);

// Reset on scope change
useEffect(() => {
  resetSync();
  setProductStockMap({});
}, [scopeKey, resetSync]);
```

NOTA: O novo inventory effect não tem `autoSyncDoneRef` — vai re-buscar ao trocar de loja
(scopeKey change), o que é o comportamento correto. Se for necessário evitar re-fetch duplo
no mesmo scope, adicionar um ref de controle apenas para inventory (opcional — não obrigatório para fase 22).

**E. Adicionar Realtime subscription em ml_daily_cache:**

Identificar onde `orgId` e `resolvedMLUserIds` estão disponíveis no componente, e adicionar
o effect após o block de "Reset on scope change":

```typescript
// ── Supabase Realtime: auto-refresh quando cron atualiza ml_daily_cache ──
const orgId = currentOrg?.id;
useEffect(() => {
  if (!orgId || resolvedMLUserIds.length === 0) return;
  const channel = supabase
    .channel("ml_daily_cache_changes")
    .on(
      "postgres_changes",
      {
        event: "*",
        schema: "public",
        table: "ml_daily_cache",
        filter: `organization_id=eq.${orgId}`,
      },
      () => {
        invalidate.invalidateAll();
      },
    )
    .subscribe();
  return () => {
    supabase.removeChannel(channel);
  };
}, [orgId, resolvedMLUserIds, invalidate]);
```

Se `invalidate` já existe no componente (vindo de `useInvalidateMLQueries`), usar diretamente.
Caso não exista, adicionar: `const invalidate = useInvalidateMLQueries();` e o import correspondente.

**F. Atualizar `MLPageHeader` para usar lastSyncTimestamp:**

Localizar a passagem de `lastUpdated` para `<MLPageHeader>`. Atualmente usa `lastSyncedAt`
(string ISO do localStorage). Substituir por:

```typescript
// ANTES:
lastUpdated={lastSyncedAt ? new Date(lastSyncedAt) : null}

// DEPOIS:
lastUpdated={lastSyncTimestamp ? new Date(lastSyncTimestamp) : null}
```

`lastSyncTimestamp` vem de `useMLLastSync()` — reflete synced_at do banco (cron ou manual).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "MercadoLivre|shouldAutoSync|autoSyncDoneRef" || echo "OK — sem erros em MercadoLivre"</automated>
  </verify>
  <done>
- `autoSyncDoneRef` e bloco de auto-sync removidos
- Inventory fetch preservado em effect separado sem condição shouldAutoSync
- Realtime subscription ativa em ml_daily_cache
- MLPageHeader recebe lastUpdated do banco via useMLLastSync
- Nenhuma referência a shouldAutoSync permanece no arquivo
  </done>
</task>

<task type="auto">
  <name>Task 5: Verificação final — TypeScript + testes</name>
  <files></files>
  <action>
Rodar as verificações finais em sequência:

**1. TypeScript:**
```bash
npx tsc --noEmit
```
Esperado: 0 erros. Se houver erros relacionados a `shouldAutoSync` (import ou uso esquecido),
localizar e remover o resíduo.

**2. Testes Vitest:**
```bash
npx vitest run
```
Esperado: 63/63 (ou o total atual) passando.

**3. Verificação manual dos arquivos:**
```bash
grep -n "shouldAutoSync\|AUTO_SYNC_STALE_MS\|LAST_ML_SYNC_TS_KEY\|autoSyncDoneRef" \
  src/hooks/useMLSync.ts src/pages/MercadoLivre.tsx
```
Esperado: nenhuma linha retornada (todos os símbolos removidos).

```bash
grep -n "useMLLastSync\|ml_daily_cache_changes" src/pages/MercadoLivre.tsx
```
Esperado: ao menos 2 linhas — import do hook e uso do channel Realtime.

**4. Verificar migration:**
```bash
cat supabase/migrations/20260522_intraday_sync.sql
```
Esperado: arquivo existe com INSERT INTO organization_plans.

Se algum teste falhar, corrigir antes de marcar como concluído.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>
- TypeScript: 0 erros
- Vitest: 63/63 (ou total atual)
- Nenhum resíduo de shouldAutoSync / AUTO_SYNC_STALE_MS nos arquivos modificados
- Migration presente em supabase/migrations/
  </done>
</task>

</tasks>

<verification>
Após todas as tasks:

1. `npx tsc --noEmit` → 0 erros
2. `npx vitest run` → todos os testes passando
3. `grep -rn "shouldAutoSync" src/` → 0 resultados
4. `grep -n "useMLLastSync" src/pages/MercadoLivre.tsx` → ao menos 1 resultado (import + uso)
5. `grep -n "ml_daily_cache_changes" src/pages/MercadoLivre.tsx` → 1 resultado (channel name)
6. `cat supabase/migrations/20260522_intraday_sync.sql` → arquivo existe com INSERT statement
</verification>

<success_criteria>
1. Abrir a página `/` não dispara edge function automaticamente (sem "Atualizando..." no mount)
2. Indicador "Última sinc" exibe timestamp lido de ml_sync_log.synced_at (fonte Supabase)
3. Migration aplicada → dispatch_sync_jobs() cria jobs a cada ~2h para orgs ativas
4. Realtime subscription ativa: quando ml_daily_cache mudar, React Query invalida automaticamente
5. Botão "Atualizar" continua funcional e grava em ml_sync_log
6. TypeScript 0 erros, 63/63 testes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/22-sync-automatico/22-01-SUMMARY.md` com:
- Arquivos modificados e natureza de cada mudança
- Resultado de `npx tsc --noEmit` e `npx vitest run`
- Status da migration (criada, ainda não aplicada ao Supabase)
- Qualquer desvio do plano e motivo
</output>
