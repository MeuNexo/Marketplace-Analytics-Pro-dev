---
phase: 49-fluxo-de-caixa-caixa-real
plan: 04
type: execute
wave: 4
depends_on: ["49-03"]
files_modified:
  - src/components/financial/TodayBalanceCard.tsx
  - src/components/financial/ProjectedBalanceCard.tsx
  - src/components/financial/CapacityCard.tsx
  - src/pages/mercadolivre/MLFluxoCaixa.tsx
  - src/components/layout/ApiSidebar.tsx
  - src/App.tsx
  - src/config/roleAccess.ts
  - src/config/routeMeta.ts
autonomous: false
requirements: [CASH-04, CASH-05]
must_haves:
  truths:
    - "A página /fluxo-de-caixa existe sob o grupo de menu 'Operações' com guard RoleRoute e isolamento por org"
    - "Os 3 cards (Caixa Hoje, Projeção Futura, Capacidade de Compra) mostram dado real via os hooks da Wave 3"
    - "O gráfico 'Como meu dinheiro vai evoluir?' aparece na página com período de 120 dias"
    - "O item 'Fluxo de Caixa' aparece no sidebar dentro de Operações e a rota está protegida por roleAccess OPERATIONAL"
  artifacts:
    - path: "src/pages/mercadolivre/MLFluxoCaixa.tsx"
      provides: "Página /fluxo-de-caixa: header + 3 cards + gráfico"
      contains: "useCashFlowData"
    - path: "src/components/financial/CapacityCard.tsx"
      provides: "Card 'Posso comprar mais estoque?' com status SAFE/DANGER"
      contains: "useFinancialHealth"
  key_links:
    - from: "src/App.tsx"
      to: "MLFluxoCaixa"
      via: "Route /fluxo-de-caixa dentro de RoleRoute"
      pattern: "/fluxo-de-caixa"
    - from: "src/components/layout/ApiSidebar.tsx"
      to: "/fluxo-de-caixa"
      via: "item dentro do grupo Operações"
      pattern: "/fluxo-de-caixa"
    - from: "src/config/roleAccess.ts"
      to: "/fluxo-de-caixa"
      via: "OPERATIONAL"
      pattern: "/fluxo-de-caixa"
---

<objective>
Montar a página /fluxo-de-caixa da Phase 49: os 3 cards (Caixa Hoje, Projeção Futura, Capacidade de
Compra) consumindo os hooks da Wave 3, a página que combina header + cards + gráfico, e a fiação de
navegação (sidebar Operações, rota+guard, roleAccess, routeMeta). Termina com checkpoint visual do
Wesley no preview Vercel. Cobre CASH-04 (página+rota) e CASH-05 (3 cards).

Purpose: Última camada de wiring — conecta os hooks/gráfico já prontos à navegação e à rota protegida.
Nada vai para main/produção sem aprovação visual do Wesley no preview Vercel.

Output: 3 cards, 1 página, 4 arquivos de config modificados, 1 checkpoint visual.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-fluxo-de-caixa-caixa-real/49-CONTEXT.md
@.planning/phases/49-fluxo-de-caixa-caixa-real/49-RESEARCH.md
@.planning/phases/49-fluxo-de-caixa-caixa-real/49-PATTERNS.md
@.planning/phases/49-fluxo-de-caixa-caixa-real/49-03-PLAN.md

# Analogs canônicos a copiar (ler antes de implementar):
@src/pages/mercadolivre/MLFinanceiro.tsx
@src/components/dashboard/KPICard.tsx
@src/components/layout/ApiSidebar.tsx
@src/App.tsx
@src/config/roleAccess.ts
# Referência de portabilidade dos cards (SaaS antigo):
@/tmp/nexointeligence/src/components/financial/TodayBalanceCard.tsx
@/tmp/nexointeligence/src/components/financial/ProjectedBalanceCard.tsx
@/tmp/nexointeligence/src/components/financial/CapacityCard.tsx
</context>

<artifacts_this_phase_produces>
Símbolos novos / modificações introduzidos por este plano:

- Componente `TodayBalanceCard` (card "Quanto tenho hoje?" via useTodayBalance)
- Componente `ProjectedBalanceCard` (card "Quanto vou ter?" via useProjectedBalance; pessimista/realista + data crítica)
- Componente `CapacityCard` (card "Posso comprar mais estoque?" via useFinancialHealth; status SAFE/DANGER)
- Página `MLFluxoCaixa` (rota `/fluxo-de-caixa`)
- Item de menu "Fluxo de Caixa" no grupo "Operações" do ApiSidebar
- Rota lazy `/fluxo-de-caixa` em App.tsx (RoleRoute + ErrorBoundary)
- Entrada `"/fluxo-de-caixa": OPERATIONAL` em roleAccess.ts
- Entrada `"/fluxo-de-caixa"` em routeMeta.ts (title "Fluxo de Caixa", subtitle "Como meu dinheiro vai evoluir?")
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Os 3 cards (TodayBalanceCard, ProjectedBalanceCard, CapacityCard)</name>
  <files>src/components/financial/TodayBalanceCard.tsx, src/components/financial/ProjectedBalanceCard.tsx, src/components/financial/CapacityCard.tsx</files>
  <read_first>
    - /tmp/nexointeligence/src/components/financial/{TodayBalanceCard,ProjectedBalanceCard,CapacityCard}.tsx (estrutura e textos)
    - src/components/dashboard/KPICard.tsx (componente reusável de card do garment)
    - src/pages/mercadolivre/MLFinanceiro.tsx (currFmt, uso de KPICard, tokens kpi.positive/negative)
    - 49-RESEARCH.md "Componentes Frontend — Mapa de Portabilidade" (mudanças por card)
  </read_first>
  <action>
    Portar os 3 cards do nexointeligence (CASH-05), adaptando à camada de dados do garment e removendo features deferidas:
    - TodayBalanceCard ("Quanto tenho hoje?"): consome useTodayBalance(). Exibe saldo_final_previsto como número grande +
      breakdown (saldo_inicial, +entradas_hoje, −saidas_hoje). REMOVER BalanceAdjustmentModal/âncora manual (deferido).
    - ProjectedBalanceCard ("Quanto vou ter?"): consome useProjectedBalance(). Exibe pessimistic_balance vs realistic_balance
      lado a lado + data crítica (critical_date — "seu saldo fica negativo em DD/MM") quando existir. REMOVER link do ScenarioSimulator (deferido).
    - CapacityCard ("Posso comprar mais estoque?"): consome useFinancialHealth(). Exibe capacidade + status SAFE (verde,
      kpi-positive)/DANGER (vermelho, kpi-negative) com mensagem leiga ("Você pode investir até R$ X em estoque" vs "Cuidado:
      sem folga para comprar estoque agora").
    Todos: reusar KPICard quando fizer sentido OU Card/CardContent shadcn, currFmt pt-BR, tokens de cor via CSS custom properties,
    Skeleton no isLoading. Named exports (padrão CONVENTIONS). Tipagem explícita. Sem useSensitiveData/hide_financial_values (deferido).
  </action>
  <verify>
    <automated>grep -q "useTodayBalance" src/components/financial/TodayBalanceCard.tsx && grep -q "useProjectedBalance" src/components/financial/ProjectedBalanceCard.tsx && grep -q "useFinancialHealth" src/components/financial/CapacityCard.tsx && grep -qiE "SAFE|DANGER|comprar" src/components/financial/CapacityCard.tsx && ! grep -riE "BalanceAdjustmentModal|ScenarioSimulator|useSensitiveData" src/components/financial/TodayBalanceCard.tsx src/components/financial/ProjectedBalanceCard.tsx src/components/financial/CapacityCard.tsx && npx tsc --noEmit 2>&1 | grep -E "TodayBalanceCard|ProjectedBalanceCard|CapacityCard" | grep -qv . && echo OK</automated>
  </verify>
  <done>Os 3 cards existem consumindo os hooks corretos, com textos de leigo, status SAFE/DANGER no CapacityCard, sem features deferidas, e tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Página MLFluxoCaixa + fiação de navegação (sidebar, rota, roleAccess, routeMeta)</name>
  <files>src/pages/mercadolivre/MLFluxoCaixa.tsx, src/components/layout/ApiSidebar.tsx, src/App.tsx, src/config/roleAccess.ts, src/config/routeMeta.ts</files>
  <read_first>
    - src/pages/mercadolivre/MLFinanceiro.tsx (estrutura de página: header sticky, period, Skeleton, grid de cards, bloco de gráfico)
    - 49-PATTERNS.md seções ApiSidebar/App.tsx/roleAccess.ts (snippets exatos de modificação)
    - src/components/layout/ApiSidebar.tsx (grupo "Operações" já existente — adicionar item)
    - src/App.tsx (padrão lazy + RoleRoute + ErrorBoundary + Suspense)
    - src/config/roleAccess.ts (constante OPERATIONAL) e src/config/routeMeta.ts
  </read_first>
  <action>
    MLFluxoCaixa.tsx (CASH-04): página padrão MLFinanceiro. `const { currentOrg } = useOrganization()`. Período padrão do gráfico
    = 120 dias à frente (e janela histórica para o passado real). Layout: MLPageHeader (title "Fluxo de Caixa"), grid de 3 cards
    (TodayBalanceCard, ProjectedBalanceCard, CapacityCard) responsivo (grid-cols-1 md:grid-cols-3), depois CashFlowChart recebendo
    a série de useCashFlowData(startDate, endDate). Skeleton states no carregamento. Empty state acionável quando não há cash_inflows
    ainda ("Conecte sua conta e sincronize para ver seu caixa" — padrão UX-02 do garment). Default export (padrão lazy de App.tsx).
    Wiring (todos os 4 arquivos de config), seguindo os snippets EXATOS do PATTERNS:
    - ApiSidebar.tsx: adicionar `{ icon: Banknote, label: "Fluxo de Caixa", path: "/fluxo-de-caixa" }` ao array children do grupo
      "Operações" (após Precificação) + importar Banknote de lucide-react.
    - App.tsx: `const MLFluxoCaixa = React.lazy(() => import("./pages/mercadolivre/MLFluxoCaixa"));` + Route /fluxo-de-caixa dentro
      de RoleRoute + ErrorBoundary (fallbackTitle "Erro no Fluxo de Caixa").
    - roleAccess.ts: `"/fluxo-de-caixa": OPERATIONAL,` (owner/admin/member).
    - routeMeta.ts: `"/fluxo-de-caixa": { title: "Fluxo de Caixa", subtitle: "Como meu dinheiro vai evoluir?" }`.
  </action>
  <verify>
    <automated>grep -q "useCashFlowData" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "CashFlowChart" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "/fluxo-de-caixa" src/components/layout/ApiSidebar.tsx && grep -q "MLFluxoCaixa" src/App.tsx && grep -q '"/fluxo-de-caixa": OPERATIONAL' src/config/roleAccess.ts && grep -q "/fluxo-de-caixa" src/config/routeMeta.ts && npx tsc --noEmit 2>&1 | grep -qv . && npm run build >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <done>Página existe combinando 3 cards + gráfico; item no sidebar Operações; rota lazy+RoleRoute em App.tsx; roleAccess OPERATIONAL; routeMeta; tsc + build limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT VISUAL] Aprovação do Wesley no preview Vercel</name>
  <action>Checkpoint humano: NÃO há edição de arquivo aqui. O orquestrador prepara o preview Vercel e o Wesley valida visualmente (passos de <how-to-verify>). Nada vai a main/produção sem o sinal 'aprovado'. Pausa para interação humana.</action>
  <what-built>
    Página /fluxo-de-caixa completa: gráfico "Como meu dinheiro vai evoluir?" (real + projetado) + 3 cards
    (Caixa Hoje, Projeção Futura, Capacidade de Compra) com dado real, sob o menu Operações. NADA foi mergeado
    para main/produção — apenas branch de preview Vercel.
  </what-built>
  <how-to-verify>
    O orquestrador prepara o preview Vercel (branch + .env.production + share link fura-login — ver reference_vercel_preview_workflow)
    e entrega ao Wesley. Wesley verifica no preview:
    1. Menu lateral: grupo "Operações" → item "Fluxo de Caixa" aparece e navega para /fluxo-de-caixa.
    2. Gráfico "Como meu dinheiro vai evoluir?": 2 linhas (real/pessimista + projetada/realista), 120 dias, tooltip com
       breakdown, e alerta visual se o saldo cruzar zero.
    3. Card "Caixa Hoje" (Quanto tenho hoje?): saldo do dia coerente com as liberações MP reais.
    4. Card "Projeção Futura" (Quanto vou ter?): pessimista vs realista + data crítica quando aplicável.
    5. Card "Capacidade de Compra" (Posso comprar mais estoque?): status SAFE/DANGER + valor.
    6. Mobile (320–768px): cards e gráfico sem overflow quebrado.
    7. Dark mode sem elementos quebrados; tokens kpi.positive/negative corretos.
    8. /financeiro (DRE) permanece intocado.
    Só após "aprovado" do Wesley o orquestrador faz merge para main (deploy prod).
  </how-to-verify>
  <resume-signal>Wesley digita "aprovado" ou descreve ajustes visuais/de dado a corrigir antes do merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (authenticated) → rota /fluxo-de-caixa | RoleRoute + roleAccess OPERATIONAL gateiam acesso por papel |
| página → hooks → RPC | dados sob RLS+INVOKER (Waves 1/2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04-01 | Elevation of Privilege | rota /fluxo-de-caixa | mitigate | RoleRoute + `"/fluxo-de-caixa": OPERATIONAL` em roleAccess (owner/admin/member); viewer default-deny |
| T-49-04-02 | Information Disclosure | dados financeiros na UI | mitigate | hooks usam orgId de useOrganization(); RPCs SECURITY INVOKER + RLS garantem isolamento por org |
| T-49-04-03 | Tampering | merge para produção sem revisão | mitigate | checkpoint visual blocking: nada vai a main sem aprovação do Wesley no preview |
</threat_model>

<verification>
- tsc + npm run build limpos
- `grep` do item de menu, rota, roleAccess e routeMeta
- Checkpoint visual: aprovação explícita do Wesley no preview antes de qualquer merge
</verification>

<success_criteria>
- Página /fluxo-de-caixa sob "Operações" com guard de rota e isolamento por org (CASH-04)
- Gráfico "Como meu dinheiro vai evoluir?" + 3 cards com dado real (CASH-04, CASH-05)
- /financeiro (DRE) intocado
- Aprovação visual do Wesley no preview Vercel antes do merge para main
</success_criteria>

<output>
Create `.planning/phases/49-fluxo-de-caixa-caixa-real/49-04-SUMMARY.md` when done
</output>
