---
phase: 48-mco-com-ads
plan: 03
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - src/hooks/useMLMarginWithAds.ts
  - src/pages/mercadolivre/MLAnuncios.tsx
  - src/components/mercadolivre/MLCostCard.tsx
  - src/pages/MercadoLivre.tsx
autonomous: false
requirements: [MCO-02, MCO-03]
must_haves:
  truths:
    - "Hook useMLMarginWithAds(from, to) consome a RPC get_margin_with_ads_by_product e retorna 2 números por item (operacional + pós-ads)"
    - "MLAnuncios (visão financeiro) exibe colunas Mg. Op. e Mg. Pós-Ads lado a lado por anúncio (MCO-02, D-03)"
    - "MLCostCard/DRE exibe linha Publicidade e inclui ads no cálculo de lucro/MCO agregado (MCO-03, D-05)"
    - "A linha Publicidade no DRE só aparece quando fonte !== 'estimado' (Pitfall 7 — evita duplicação)"
  artifacts:
    - path: "src/hooks/useMLMarginWithAds.ts"
      provides: "Hook + interface ProductMarginWithAds"
      exports: ["useMLMarginWithAds", "ProductMarginWithAds"]
      min_lines: 40
    - path: "src/pages/mercadolivre/MLAnuncios.tsx"
      provides: "2 colunas novas em columnView financeiro"
      contains: "useMLMarginWithAds"
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "prop adsTotalMes + linha Publicidade no DRE"
      contains: "adsTotalMes"
  key_links:
    - from: "useMLMarginWithAds"
      to: "get_margin_with_ads_by_product"
      via: "supabase.rpc com p_org_id/p_user_ids/p_from/p_to"
      pattern: "get_margin_with_ads_by_product"
    - from: "MLAnuncios columnView financeiro"
      to: "useMLMarginWithAds(rankingFrom, rankingTo)"
      via: "mapa item_id → ProductMarginWithAds para lookup O(1) no TableBody"
      pattern: "useMLMarginWithAds"
    - from: "MercadoLivre.tsx"
      to: "MLCostCard adsTotalMes"
      via: "adsTotalMes={dreFonte !== 'estimado' ? adsSpendMes : null}"
      pattern: "adsTotalMes"
---

<objective>
Exibir os dois números (margem operacional + pós-ads) nas superfícies frontend decididas pelo Wesley: criar o hook `useMLMarginWithAds` que consome a RPC de 48-01; adicionar 2 colunas (Mg. Op. e Mg. Pós-Ads) na visão "financeiro" de MLAnuncios (D-03/MCO-02); e adicionar a linha "Publicidade (ads ML)" + MCO agregado no card Custos/DRE de /vendas (D-05/MCO-03), usando o gasto TOTAL da conta via `adsSpendMes` (`ml_ads_daily_cache`, D-12).

Purpose: Materializa MCO-02 e MCO-03 — o lojista vê, por anúncio, quanto a publicidade come da margem; e vê o MCO agregado da operação no DRE mensal.
Output: 1 hook novo + 3 arquivos frontend modificados; `tsc --noEmit` + `npm run build` limpos.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-mco-com-ads/48-CONTEXT.md
@.planning/phases/48-mco-com-ads/48-RESEARCH.md
@.planning/phases/48-mco-com-ads/48-PATTERNS.md

# Análogo direto do hook:
@src/hooks/useMLMarginAnalysis.ts
# Superfícies a modificar:
@src/pages/mercadolivre/MLAnuncios.tsx
@src/components/mercadolivre/MLCostCard.tsx
@src/pages/MercadoLivre.tsx
</context>

<interface_context>
A RPC consumida (criada em 48-01, depends_on) tem o contrato:
`get_margin_with_ads_by_product(p_org_id UUID, p_user_ids TEXT[], p_from DATE, p_to DATE)` retorna por item:
`item_id, titulo, sku, listing_type, receita, cmv, comissao, frete, impostos, lucro (operacional), lucro_pct (operacional), pedidos, unidades, has_cmv, ads_spend, ads_attributed_orders, lucro_pos_ads, lucro_pct_pos_ads, ads_no_sale`.
Os tipos já existem em `src/integrations/supabase/types.ts` (adicionados em 48-01).
</interface_context>

<artifacts_this_phase_produces>
Símbolos NOVOS / modificados por este plano:

- **Hook** `useMLMarginWithAds(dateFrom: string, dateTo: string)` em `src/hooks/useMLMarginWithAds.ts`
- **Interface** `ProductMarginWithAds` (export) — 19 campos espelhando a RPC
- **Colunas** em MLAnuncios visão "financeiro": `Mg. Op.` e `Mg. Pós-Ads` (TableHead + TableCell)
- **Prop** `adsTotalMes?: number | null` em `MLCostCardProps`; nova linha DRE "Publicidade (ads ML)"; cálculo de `lucro` incluindo `(adsTotalMes ?? 0)`
- **Wiring** em MercadoLivre.tsx: `adsTotalMes={dreFonte !== "estimado" ? adsSpendMes : null}` na chamada de MLCostCard
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Hook useMLMarginWithAds + colunas em MLAnuncios</name>
  <files>src/hooks/useMLMarginWithAds.ts, src/pages/mercadolivre/MLAnuncios.tsx</files>
  <read_first>
    - @src/hooks/useMLMarginAnalysis.ts — análogo EXATO do hook: imports (`useQuery`, `supabase`, `useMLStore`, `useOrganization`), uso de `resolvedMLUserIds` + `currentOrg`, `queryKey`, `.rpc()` com `p_from/p_to` via `.substring(0,10)`, mapeamento com `Number()/String()/Boolean()` em TODOS os campos (coerção obrigatória — Supabase retorna números como string), `enabled`, `staleTime: 2*60*1000`.
    - 48-RESEARCH.md §"Padrão do hook useMLMarginWithAds" (linhas 689-743) e 48-PATTERNS.md §"useMLMarginWithAds.ts" (linhas 246-332) — interface `ProductMarginWithAds` (19 campos) e corpo completo do hook.
    - @src/pages/mercadolivre/MLAnuncios.tsx linhas 565, 606-660 — `columnView` state; `rankingPeriod`/`rankingRange` e como `fromDate`/`toDate` são derivados (formato `yyyy-MM-dd`, TOTAL_PERIOD, ranking 0=Hoje). Reusar o mesmo from/to do ranking para o hook (D-13: /anuncios segue a janela já usada na tela).
    - @src/pages/mercadolivre/MLAnuncios.tsx linhas 1226-1247 — cabeçalho da visão "financeiro" (5 TableHead: Custo/Impostos/Comissão ML/Mg. Bruta/Mg. Líq.) e o padrão Tooltip (`cursor-help border-b border-dashed border-muted-foreground/40`).
    - @src/pages/mercadolivre/MLAnuncios.tsx linha 1348+ e ~1463 (`colSpan={columnView === "financeiro" ? 11 : 9}`) — onde as células financeiras por linha são renderizadas; o colSpan precisa subir de 11 para 13 com 2 colunas novas.
    - 48-PATTERNS.md §"MLAnuncios.tsx" (linhas 336-384) — TableHead das 2 colunas novas (Mg. Op. / Mg. Pós-Ads) com Tooltip, e a instrução de criar mapa `item_id → ProductMarginWithAds` via useMemo.
  </read_first>
  <action>
    Criar `src/hooks/useMLMarginWithAds.ts` copiando o molde de `useMLMarginAnalysis.ts`: exportar a interface `ProductMarginWithAds` (19 campos do contrato, com `sku`/`listing_type`/`lucro_pct`/`lucro_pct_pos_ads` nullable) e `useMLMarginWithAds(dateFrom, dateTo)` que chama `supabase.rpc("get_margin_with_ads_by_product", { p_org_id: currentOrg.id, p_user_ids: resolvedMLUserIds, p_from: dateFrom.substring(0,10), p_to: dateTo.substring(0,10) })`. Mapear cada campo com `Number()/String()/Boolean()` e nullables com `r.campo != null ? Number(r.campo) : null` (coerção crítica). `queryKey: ["ml_margin_with_ads", currentOrg?.id, resolvedMLUserIds, dateFrom, dateTo]`, `enabled: !!currentOrg?.id && resolvedMLUserIds.length > 0`, `staleTime: 2*60*1000`.

    Em MLAnuncios.tsx:
    - Derivar `rankingFrom`/`rankingTo` no formato `yyyy-MM-dd` a partir do mesmo cálculo já existente do ranking (`rankingPeriod`/`rankingRange`); chamar `const { data: marginWithAds } = useMLMarginWithAds(rankingFrom, rankingTo);`. Construir `const marginByItem = useMemo(() => new Map(marginWithAds?.map(m => [m.item_id, m])), [marginWithAds])` para lookup O(1).
    - No cabeçalho da visão "financeiro" (após o TableHead "Mg. Líq."), adicionar 2 TableHead: "Mg. Op." e "Mg. Pós-Ads", cada um com Tooltip explicando (Mg. Op. = margem operacional real dos pedidos do período, sem publicidade; Mg. Pós-Ads = após deduzir o gasto de ads do produto). Reusar o padrão Tooltip existente.
    - Nas células por linha (visão financeiro), após a célula "Mg. Líq.", adicionar 2 TableCell que leem `marginByItem.get(item.id)`: exibir `lucro_pct` (Mg. Op.) e `lucro_pct_pos_ads` (Mg. Pós-Ads), formatados como `%` com classe de cor `text-kpi-positive`/`text-kpi-negative` conforme sinal; quando o item não está no mapa (sem dados na janela) exibir "—". Sinalizar visualmente `ads_no_sale` (ex.: badge/cor) quando aplicável é opcional.
    - Atualizar o `colSpan` da linha expandida de `11` para `13` na visão financeiro (e qualquer outro colSpan que conte as colunas financeiras).

    Pitfall 4: NÃO alterar as colunas Mg. Bruta/Mg. Líq. existentes (baseadas em preço atual) — as novas colunas são conceitualmente distintas (margem real dos pedidos no período). NÃO reusar `useMLProductMargins` (que mistura operacional+ads) — usar o hook novo.
  </action>
  <verify>
    <automated>test -f src/hooks/useMLMarginWithAds.ts && grep -q "export function useMLMarginWithAds" src/hooks/useMLMarginWithAds.ts && grep -q "export interface ProductMarginWithAds" src/hooks/useMLMarginWithAds.ts && grep -q "get_margin_with_ads_by_product" src/hooks/useMLMarginWithAds.ts && grep -q "useMLMarginWithAds" src/pages/mercadolivre/MLAnuncios.tsx && grep -q "Pós-Ads\|Pos-Ads\|pos-ads" src/pages/mercadolivre/MLAnuncios.tsx && npx tsc --noEmit 2>&1 | grep -v node_modules | (! grep -qE "useMLMarginWithAds|MLAnuncios") && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Hook existe, exporta `useMLMarginWithAds` + `ProductMarginWithAds`, consome a RPC com coerção de tipos.
    - MLAnuncios chama o hook com o range do ranking e exibe 2 colunas (Mg. Op. + Mg. Pós-Ads) na visão financeiro via mapa O(1).
    - colSpan ajustado; colunas existentes intactas; tsc limpo nesses arquivos.
  </acceptance_criteria>
  <done>Hook criado e MLAnuncios mostra margem operacional e pós-ads lado a lado na visão financeiro.</done>
</task>

<task type="auto">
  <name>Task 2: Linha Publicidade/MCO agregado no MLCostCard + wiring em MercadoLivre.tsx</name>
  <files>src/components/mercadolivre/MLCostCard.tsx, src/pages/MercadoLivre.tsx</files>
  <read_first>
    - @src/components/mercadolivre/MLCostCard.tsx linhas 20-47 (interface `MLCostCardProps`), linhas 67-74 (cálculo de `lucro = receitaMes − totalTarifas − (cmvMes ?? 0) − (impostosMes ?? 0)` e `margemPct`), e o helper `fmt`/`pct` (topo do arquivo, ~linhas 7-10).
    - @src/components/mercadolivre/MLCostCard.tsx linhas ~196-242 — padrão de linha do DRE (estrutura `flex items-center justify-between text-xs py-1`, prefixo `(−)`, `pct(valor, receitaMes)`, `fmt(valor)`); a linha "Impostos próprios" é o modelo exato.
    - @src/pages/MercadoLivre.tsx linhas 255-258 (`adsSpendMes` já calculado de `adsDaily` em `ml_ads_daily_cache`, filtrado por `billingMonthFrom/To` — D-12/D-13 mês-calendário), linha 262 (`dreFonte`), linhas 266-285 (`gruposTarifasEfetivos` que no fallback estimado já inclui "Campanhas de publicidade" com `adsSpendMes` — Pitfall 7), linhas 714-729 (chamada de `<MLCostCard ... />`).
    - 48-PATTERNS.md §"MLCostCard.tsx" (linhas 425-476) e §"MercadoLivre.tsx" (linhas 388-421) — prop nova, linha DRE, cálculo de lucro com ads, e a regra `adsTotalMes={dreFonte !== "estimado" ? adsSpendMes : null}`.
    - 48-RESEARCH.md §Pitfall 7 (linhas 577-580) — por que a linha só aparece quando fonte !== "estimado".
  </read_first>
  <action>
    Em `MLCostCard.tsx`:
    - Adicionar à interface `MLCostCardProps`: `adsTotalMes?: number | null;` (comentar: gasto total de ads do mês via ml_ads_daily_cache; null = não exibir, fallback estimado já inclui).
    - Adicionar a linha "Publicidade (ads ML)" no DRE entre a linha "Impostos próprios" e o separador de Lucro, copiando a estrutura exata da linha de impostos (prefixo `(−)`, `pct(adsTotalMes, receitaMes)`, `fmt(adsTotalMes)`). Renderizar a linha apenas quando `adsTotalMes != null && adsTotalMes > 0`.
    - Atualizar o cálculo: `const lucro = receitaMes − totalTarifas − (cmvMes ?? 0) − (impostosMes ?? 0) − (adsTotalMes ?? 0);` (MCO agregado = Σ contribuição − ads total, D-05). `margemPct`/`lucroPositivo` derivam do novo `lucro` automaticamente.

    Em `MercadoLivre.tsx`:
    - Na chamada `<MLCostCard ... />` (linhas ~714-729), adicionar a prop `adsTotalMes={dreFonte !== "estimado" ? adsSpendMes : null}`. NÃO recalcular `adsSpendMes` (já existe nas linhas 255-258). Quando `dreFonte === "estimado"`, passar `null` porque `gruposTarifasEfetivos` já contém "Campanhas de publicidade" com o mesmo valor (Pitfall 7 — evitar duplicação).

    NÃO incorporar ads em `gruposTarifas` (PADS é débito separado do faturamento ML); é linha própria. NÃO mexer no ciclo de billing/DRE mês-calendário (já correto via quick 260613-2p6).
  </action>
  <verify>
    <automated>grep -q "adsTotalMes" src/components/mercadolivre/MLCostCard.tsx && grep -q "Publicidade" src/components/mercadolivre/MLCostCard.tsx && grep -q "(adsTotalMes ?? 0)" src/components/mercadolivre/MLCostCard.tsx && grep -q 'adsTotalMes={dreFonte !== "estimado" ? adsSpendMes : null}' src/pages/MercadoLivre.tsx && npx tsc --noEmit 2>&1 | grep -v node_modules | (! grep -qE "MLCostCard|MercadoLivre") && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - MLCostCard tem prop `adsTotalMes`, exibe a linha "Publicidade" só quando não-null e > 0, e o lucro subtrai ads.
    - MercadoLivre.tsx passa `adsTotalMes` com a guarda de fonte (null no estimado — Pitfall 7).
    - tsc limpo nesses arquivos.
  </acceptance_criteria>
  <done>DRE mostra linha Publicidade e MCO agregado (lucro inclui ads total) sem duplicar no fallback estimado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificação visual MCO com Ads</name>
  <action>Rodar npm run build e conduzir a verificacao visual descrita abaixo com o Wesley.</action>
  <what-built>Frontend completo: hook useMLMarginWithAds, 2 colunas (Mg. Op. + Mg. Pós-Ads) em /anuncios visão financeiro, e linha Publicidade + MCO agregado no card Custos/DRE de /vendas. Depende de 48-01 aplicado (RPC no banco).</what-built>
  <how-to-verify>
    Pré-condição: `npm run build` limpo (verificar antes do checkpoint visual).
    1. Abrir /anuncios, alternar para a visão "financeiro", selecionar um range no ranking (ex.: últimos 30 dias). Conferir as colunas "Mg. Op." e "Mg. Pós-Ads" lado a lado por anúncio, com valores plausíveis (pós-ads ≤ operacional onde há gasto de ads). Itens sem dados no período mostram "—".
    2. Abrir /vendas. No card "Custos"/DRE, confirmar a linha "Publicidade (ads ML)" com valor real (não-nulo) quando a fonte é billing/competência, e que o Lucro/MCO do mês desconta esse valor. Navegar meses e confirmar coerência.
    3. Confirmar que no fallback "estimado" a publicidade NÃO aparece duplicada (uma vez em "Campanhas de publicidade", não duas).
  </how-to-verify>
  <resume-signal>Wesley: digite "aprovado" após ver as 2 colunas em /anuncios e a linha Publicidade no DRE; ou descreva ajustes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→RPC (JWT) | Hook chama get_margin_with_ads_by_product com org/user_ids da org corrente |
| render→dados de outra org | Mapas/colunas renderizam apenas dados retornados pela RPC escopada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03-01 | Information Disclosure | useMLMarginWithAds | mitigate | Usa `currentOrg.id` + `resolvedMLUserIds` (OrganizationContext/MLStore) — escopo org+loja; RPC re-filtra server-side. Sem dados cross-org no cliente. |
| T-48-03-02 | Tampering | adsTotalMes duplicado no DRE | mitigate | Guarda `dreFonte !== "estimado"` evita somar ads duas vezes (Pitfall 7). |
| T-48-03-03 | Information Disclosure | colunas exibem item de outra loja | accept | `resolvedMLUserIds` já consolida as lojas da org (D-domain: v1 consolida por org); sem vazamento entre orgs. |
| T-48-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote externo novo (RESEARCH: sem novas dependências). |
</threat_model>

<verification>
- `npm run build` e `tsc --noEmit` limpos.
- /anuncios visão financeiro mostra Mg. Op. e Mg. Pós-Ads (MCO-02).
- /vendas DRE mostra linha Publicidade e MCO agregado inclui ads (MCO-03).
- Sem duplicação de ads no fallback estimado (Pitfall 7).
</verification>

<success_criteria>
MCO-02 e MCO-03 satisfeitos: margem operacional E pós-ads lado a lado por produto em /anuncios; MCO agregado (Σ contribuição − ads total) visível no card Custos/DRE.
</success_criteria>

<output>
Create `.planning/phases/48-mco-com-ads/48-03-SUMMARY.md` when done
</output>
