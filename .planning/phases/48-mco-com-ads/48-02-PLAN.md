---
phase: 48-mco-com-ads
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - supabase/functions/consultor-insights/index.ts
autonomous: false
requirements: [MCO-04, MCO-05]
must_haves:
  truths:
    - "Produto com lucro operacional > 0 mas pós-ads ≤ limiar gera insight ads_eating_margin (por item, MCO-04)"
    - "O insight ads_eating_margin é SEPARADO do alerta de prejuízo operacional margin_critical (D-07)"
    - "ads_no_sale passa a ser por produto: spend > 0 E attributed_orders = 0 por item_id (MCO-05)"
    - "rule_key permanece 'ads_no_sale' com ml_user_id_key=item_id, auto-resolvendo o insight org-level antigo (D-09/D-10/Pitfall 3)"
    - "ads_eating_margin penaliza o pilar Ads do score de saúde (peso 25)"
    - "EF consultor-insights deployada em ckcdevcxgvueywivefgx"
  artifacts:
    - path: "supabase/functions/consultor-insights/index.ts"
      provides: "RULE ads_eating_margin (nova) + RULE 3 ads_no_sale upgradada para item-level + pilar Ads ajustado"
      contains: "ads_eating_margin"
  key_links:
    - from: "RULE ads_eating_margin"
      to: "get_margin_with_ads_by_product"
      via: "sb.rpc na janela de 30 dias (marginFrom/marginTo)"
      pattern: "get_margin_with_ads_by_product"
    - from: "RULE 3 ads_no_sale"
      to: "ml_ads_products_cache"
      via: "select item_id,spend,attributed_orders,title agrupado por item_id"
      pattern: "ml_ads_products_cache"
    - from: "notaAds (pilar Ads do score)"
      to: "activeRuleKeys"
      via: "includes('ads_eating_margin') → penalidade adicional"
      pattern: "ads_eating_margin"
---

<objective>
Estender o engine `consultor-insights` (Phase 45) com duas mudanças: (1) nova regra `ads_eating_margin` por produto que dispara quando o lucro operacional é positivo mas a publicidade derruba a margem pós-ads abaixo dos limiares de `consultor_config` (D-06/D-08); (2) upgrade da RULE 3 `ads_no_sale` de org-level para item-level usando `ml_ads_products_cache` (D-09/MCO-05). Incluir `ads_eating_margin` na penalidade do pilar Ads do score de saúde, registrar os defaults no DEFAULT_CONFIG, e deployar a EF.

Purpose: Transforma os dois números da RPC (48-01) em alertas acionáveis e separados — "ads comendo a margem" (MCO-04) distinto do prejuízo operacional, e "gasta sem vender" por produto (MCO-05).
Output: `consultor-insights/index.ts` atualizada e deployada em `ckcdevcxgvueywivefgx`; insights gravados em `insights` por (org, rule_key, item_id).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-mco-com-ads/48-CONTEXT.md
@.planning/phases/48-mco-com-ads/48-RESEARCH.md
@.planning/phases/48-mco-com-ads/48-PATTERNS.md

# Arquivo a modificar (é o próprio análogo — RULE 1/2 e RULE 3 já existentes):
@supabase/functions/consultor-insights/index.ts
</context>

<artifacts_this_phase_produces>
Símbolos NOVOS / modificados por este plano:

- **Tipo** `ConsultorConfig` ganha `ads_eating_critical_pct: number` e `ads_eating_alert_pct: number`; `DEFAULT_CONFIG` ganha `ads_eating_critical_pct: 0`, `ads_eating_alert_pct: 10`.
- **Regra nova** `rule_key: "ads_eating_margin"` (category "Ads", per-item via `ml_user_id_key = item_id`) — usa RPC `get_margin_with_ads_by_product` (de 48-01).
- **Regra modificada** RULE 3 `ads_no_sale`: passa de org-level (`ml_ads_daily_cache`, `ml_user_id_key=""`) para per-item (`ml_ads_products_cache`, `ml_user_id_key=item_id`); mesmo `rule_key="ads_no_sale"`.
- **Score** `notaAds` (pilar Ads, peso 25) ganha penalidade quando `activeRuleKeys.includes("ads_eating_margin")`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Config + nova RULE ads_eating_margin</name>
  <files>supabase/functions/consultor-insights/index.ts</files>
  <read_first>
    - @supabase/functions/consultor-insights/index.ts linhas 85-109 — tipo `ConsultorConfig` e `DEFAULT_CONFIG` (adicionar 2 chaves após `paused_ads_lookback_days`).
    - @supabase/functions/consultor-insights/index.ts linhas 178-248 — RULE 1 `margin_critical` / RULE 2 `margin_alert`: molde exato de uma regra (carrega via `sb.rpc("get_consultor_margin_by_product", {p_org_id, p_user_ids, p_from: marginFrom, p_to: marginTo})`, filtra, push em `activeRuleKeys` + `candidates`). Reusar `marginFrom = thirtyDaysAgo` / `marginTo = today` (linhas 183-184) — mesma janela de 30 dias (D-domain: Consultor usa 30 dias).
    - @supabase/functions/consultor-insights/index.ts — interface `InsightCandidate` e helper `fmt` (formatação R$ leiga). Localizar via grep `interface InsightCandidate` e `const fmt`.
    - 48-RESEARCH.md §"Code Examples → Padrão da RULE ads_eating_margin no engine" (linhas 746-797) — corpo TS verificado da nova regra, incluindo cálculo `erosao = lucro − lucro_pos_ads` e o texto do body em linguagem leiga com R$ e os dois percentuais.
    - 48-PATTERNS.md §"index.ts — Padrão 1 e Padrão 2" (linhas 132-188) — diferenças para per-item: `ml_user_id_key: item.item_id`, loop `for (const item of ...)`, `category: "Ads"`.
  </read_first>
  <action>
    Adicionar ao tipo `ConsultorConfig` (após `paused_ads_lookback_days`): `ads_eating_critical_pct: number;` e `ads_eating_alert_pct: number;`. Adicionar ao `DEFAULT_CONFIG`: `ads_eating_critical_pct: 0,` e `ads_eating_alert_pct: 10,` (per D-06). O spread existente `{ ...DEFAULT_CONFIG, ...cfgRow }` (linha ~173) já mescla os valores reais do banco (colunas criadas em 48-01).

    Inserir a nova RULE `ads_eating_margin` APÓS a RULE 2 (margin_alert) e ANTES da RULE 3 (ads_no_sale), seguindo o molde do 48-RESEARCH.md linhas 746-797:
    - Ler `adsEatingCriticalPct = cfg.ads_eating_critical_pct ?? 0` e `adsEatingAlertPct = cfg.ads_eating_alert_pct ?? 10`.
    - Chamar `sb.rpc("get_margin_with_ads_by_product", { p_org_id: orgId, p_user_ids: mlUserIds, p_from: marginFrom, p_to: marginTo })` (mesma janela 30 dias das regras de margem).
    - Filtrar itens CRÍTICOS: `lucro > 0 && lucro_pct_pos_ads !== null && lucro_pct_pos_ads <= adsEatingCriticalPct` (operacional positivo mas pós-ads zerado, D-06).
    - Filtrar itens ALERTA: `lucro > 0 && lucro_pct_pos_ads !== null && lucro_pct_pos_ads > adsEatingCriticalPct && lucro_pct_pos_ads <= adsEatingAlertPct` (margem pós-ads abaixo de 10% sem zerar). Severidade `"high"` para alerta, `"critical"` para crítico.
    - Para cada item: `erosao = item.lucro − item.lucro_pos_ads`; `activeRuleKeys.push("ads_eating_margin")` (uma vez por execução basta para o score — pode-se usar um flag/Set para não duplicar a key no array, mas a presença é o que importa); `candidates.push({ organization_id: orgId, ml_user_id: null, ml_user_id_key: item.item_id, rule_key: "ads_eating_margin", category: "Ads", severity, title, body, action_label: "Ver anúncio", action_href: "/anuncios?items=" + item.item_id, impact_brl: Math.round(erosao), status: "active", updated_at: nowIso })`.
    - `body` em linguagem leiga com R$ e os dois percentuais (padrão D-domain): ex. "O produto X tem lucro operacional de Z%, mas a publicidade está consumindo R$ {erosao}/mês — a margem cai de Z% para W%." Usar o helper `fmt` existente.

    D-07/MCO-04: NÃO mesclar com `margin_critical`. Um produto pode disparar `ads_eating_margin` sem estar em `margin_critical` (operacional > 0). São rule_keys distintos.
    Pitfall 6: usar a RPC (não `.from(...).select()` paginado) — a RPC já é SECURITY DEFINER e não trunca.
  </action>
  <verify>
    <automated>grep -q "ads_eating_critical_pct" supabase/functions/consultor-insights/index.ts && grep -q "ads_eating_margin" supabase/functions/consultor-insights/index.ts && grep -q "get_margin_with_ads_by_product" supabase/functions/consultor-insights/index.ts && grep -q 'ml_user_id_key: item.item_id\|ml_user_id_key:item.item_id' supabase/functions/consultor-insights/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ConsultorConfig + DEFAULT_CONFIG têm as 2 chaves novas.
    - Nova regra usa a RPC de 48-01, filtra crítico (≤0%) e alerta (≤10%) sobre `lucro_pct_pos_ads` apenas quando `lucro > 0`.
    - Insight per-item com `ml_user_id_key = item_id`, rule_key `ads_eating_margin`, category `Ads`, body leigo em R$ com os dois percentuais, action_href `/anuncios?items=<item_id>`.
    - Regra inserida entre RULE 2 e RULE 3.
  </acceptance_criteria>
  <done>EF contém os defaults novos e a regra ads_eating_margin per-item consumindo a RPC, separada de margin_critical.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade RULE 3 ads_no_sale para item-level + pilar Ads do score</name>
  <files>supabase/functions/consultor-insights/index.ts</files>
  <read_first>
    - @supabase/functions/consultor-insights/index.ts linhas 252-291 — RULE 3 atual (org-level via `ml_ads_daily_cache`, `ml_user_id_key=""`, `rule_key="ads_no_sale"`, dispara quando Σspend>0 E Σorders===0). `adsNoSaleDays = cfg.ads_no_sale_days` (default 7) e `adsNoSaleFrom`.
    - @supabase/functions/consultor-insights/index.ts linhas 779-788 — cálculo de `notaAds`: `hasCampanhaSemVenda = activeRuleKeys.includes("ads_no_sale")`, `notaAds = clamp(Math.round(100 - tacosOver15*5 - (hasCampanhaSemVenda ? 20 : 0)))`.
    - 48-RESEARCH.md §MCO-04 (linhas 427-450) — query item-level e a decisão D-10 (substituir, manter `rule_key="ads_no_sale"`, mudar `ml_user_id_key` de "" para item_id → auto-resolve o org-level antigo).
    - 48-PATTERNS.md §"index.ts — Padrão 3 e Padrão 4" (linhas 189-234) — o que substituir e a penalidade nova do score.
    - 48-RESEARCH.md §Pitfall 3 (linhas 557-560) e §Pitfall 6 (linhas 572-575) — manter mesmo rule_key para auto-resolver; cuidado com truncamento 1000 linhas em `ml_ads_products_cache` (~6000 linhas/30 dias).
  </read_first>
  <action>
    Substituir o corpo da RULE 3 (org-level) por versão per-item (D-10: substituir, não complementar):
    - Buscar `ml_ads_products_cache` por `item_id, spend, attributed_orders, title` filtrando `organization_id = orgId`, `date >= adsNoSaleFrom`, `date <= today`. Como `ml_ads_products_cache` para 30 dias com ~200 itens chega a ~6000 linhas e o EF usa `.from().select()` (sujeito ao limite de 1000 do PostgREST — Pitfall 6): paginar com loop `.range(offset, offset+999)` até retornar menos de 1000, OU reduzir a janela de busca a `adsNoSaleFrom..today` (default 7 dias, ~1400 linhas → ainda exige paginação). Implementar paginação via `.range()` em loop (regra `feedback_postgrest_pagination`).
    - Agregar no cliente por `item_id`: somar `spend` e `attributed_orders`, guardar `title`. Filtrar itens com `Σspend > 0 AND Σattributed_orders === 0`.
    - Para cada item filtrado: `candidates.push({ organization_id: orgId, ml_user_id: null, ml_user_id_key: item_id, rule_key: "ads_no_sale", category: "Ads", severity: "high", title: "Publicidade sem venda no produto", body: "<leigo, R$ gasto sem retorno>", action_label: "Ver anúncio", action_href: "/anuncios?items=" + item_id, impact_brl: Math.round(spend), status: "active", updated_at: nowIso })`. Push `activeRuleKeys.push("ads_no_sale")` se houver ao menos um item.
    - MANTER `rule_key = "ads_no_sale"` (NÃO renomear para "ads_no_sale_product"): o índice único `(organization_id, rule_key, ml_user_id_key)` diferencia org-level antigo (`ml_user_id_key=""`) de item-level novo (`item_id`). O auto-resolver (filtra `NOT IN activeRuleKeys` por linha existente) resolverá o insight org-level antigo com `ml_user_id_key=""` quando ele deixar de ser gerado. Documentar essa justificativa no SUMMARY (Pitfall 3 / D-10).

    Score (pilar Ads): adicionar `const hasErosaoAds = activeRuleKeys.includes("ads_eating_margin");` e incluir `- (hasErosaoAds ? 15 : 0)` no cálculo de `notaAds`, preservando os termos `tacosOver15*5` e `hasCampanhaSemVenda ? 20 : 0`. Mantém coerência do pilar Ads peso 25 (D-09 Phase 45).

    NÃO alterar o upsert idempotente (onConflict `organization_id,rule_key,ml_user_id_key`) nem a lógica de dismissed (T-45-08) — aplicam-se automaticamente.
  </action>
  <verify>
    <automated>grep -q "ml_ads_products_cache" supabase/functions/consultor-insights/index.ts && grep -q "hasErosaoAds" supabase/functions/consultor-insights/index.ts && grep -q '\.range(' supabase/functions/consultor-insights/index.ts && grep -c 'rule_key: "ads_no_sale"\|rule_key:"ads_no_sale"' supabase/functions/consultor-insights/index.ts | grep -qv '^0$' && ! grep -q "ads_no_sale_product" supabase/functions/consultor-insights/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - RULE 3 lê `ml_ads_products_cache` com paginação `.range()`, agrega por item_id, dispara per-item.
    - `rule_key` permanece `"ads_no_sale"` (sem `ads_no_sale_product`); `ml_user_id_key = item_id`.
    - `notaAds` inclui penalidade de `ads_eating_margin` sem remover as penalidades existentes.
  </acceptance_criteria>
  <done>RULE 3 é item-level via products_cache paginado, mantém rule_key ads_no_sale, e o pilar Ads penaliza erosão de ads.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Deploy EF consultor-insights</name>
  <action>Deployar a EF consultor-insights em ckcdevcxgvueywivefgx e rodar o smoke descrito abaixo (executado pelo orquestrador, nao pelo executor).</action>
  <what-built>EF consultor-insights atualizada com a regra ads_eating_margin, ads_no_sale per-item e penalidade no pilar Ads. Precisa deploy em ckcdevcxgvueywivefgx (executor não tem CLI/MCP Supabase) e smoke confirmando geração de insights.</what-built>
  <how-to-verify>
    Deploy (orquestrador): `supabase functions deploy consultor-insights --project-ref ckcdevcxgvueywivefgx` (exige SUPABASE_ACCESS_TOKEN em non-TTY) — ou MCP equivalente. Depende de 48-01 já aplicado (a RPC get_margin_with_ads_by_product precisa existir no banco).

    Smoke (via MCP no ckcdevcxgvueywivefgx):
    1. `supabase functions list --project-ref ckcdevcxgvueywivefgx` → consultor-insights ACTIVE com nova versão.
    2. Invocar a EF on-demand para a org Pé Vermeio (JWT de usuário ou modo service-role conforme o auth dual da Phase 45) e verificar `insights_count` no retorno.
    3. `SELECT rule_key, ml_user_id_key, severity, count(*) FROM insights WHERE rule_key='ads_eating_margin' GROUP BY 1,2,3;` → linhas com `ml_user_id_key != ''` (per-item). (MCO-04)
    4. `SELECT count(*) FROM insights WHERE rule_key='ads_no_sale' AND ml_user_id_key != '';` → ≥0 itens per-produto; e confirmar que insights antigos `ads_no_sale` com `ml_user_id_key=''` foram resolvidos (status resolved) ou não re-ativados. (MCO-05 / Pitfall 3)
    5. Confirmar separação D-07: um produto pode aparecer em `ads_eating_margin` sem aparecer em `margin_critical` (consultar os dois rule_keys e checar item_ids distintos quando aplicável).
  </how-to-verify>
  <resume-signal>Digite "deployado" após confirmar EF ACTIVE + insights ads_eating_margin per-item gravados + ads_no_sale item-level + score com penalidade; ou descreva o erro.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/JWT→EF | consultor-insights roda por cron (service-role) e on-demand (JWT); processa dados por org |
| EF→insights table | Grava insights por (org, rule_key, item_id) — não pode cruzar orgs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02-01 | Information Disclosure | ads_eating_margin / ads_no_sale per-item | mitigate | RPC e selects filtram `organization_id = orgId` e `p_user_ids` da org; insight gravado com `organization_id = orgId`. Nenhum item_id de outra org entra nos candidates. |
| T-48-02-02 | Denial of Service | Upsert per-item explode linhas em insights | mitigate | Dedup por `onConflict (organization_id, rule_key, ml_user_id_key)`; dismissed não re-ativado (T-45-08); paginação `.range()` evita carregar tudo em memória sem limite. |
| T-48-02-03 | Spoofing | EF invocada sem auth | accept | Auth dual da Phase 45 (service-role + JWT) inalterado; EF rejeita sem auth (padrão existente). |
| T-48-02-04 | Tampering | Score penalizado por item de outra org | mitigate | `activeRuleKeys` é por execução escopada à org corrente; nenhum dado cross-org alimenta notaAds. |
| T-48-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote externo novo (RESEARCH §Package Legitimacy: N/A). |
</threat_model>

<verification>
- EF consultor-insights ACTIVE em ckcdevcxgvueywivefgx (nova versão).
- insights com rule_key `ads_eating_margin` e `ml_user_id_key != ''` (per-item, MCO-04).
- insights com rule_key `ads_no_sale` e `ml_user_id_key != ''` (item-level, MCO-05); org-level antigo auto-resolvido.
- notaAds reduzido quando ads_eating_margin ativo.
- ads_eating_margin independente de margin_critical (D-07).
</verification>

<success_criteria>
MCO-04 e MCO-05 satisfeitos: alerta separado por produto "ads comendo a margem" (independente do prejuízo operacional) e ads_no_sale por produto, ambos gravados em insights e refletidos no score de saúde, com a EF deployada.
</success_criteria>

<output>
Create `.planning/phases/48-mco-com-ads/48-02-SUMMARY.md` when done
</output>
