---
phase: 90-dre-imposto-real-cmv-fechamento
plan: 04
type: execute
wave: 3
depends_on: ["90-03"]
files_modified:
  - src/pages/MercadoLivre.tsx
  - src/components/mercadolivre/MLCostCard.tsx
autonomous: false
requirements: [SC1, SC2, SC3, SC5, SC6]
user_setup: []

must_haves:
  truths:
    - "MercadoLivre.tsx consulta a guia da competência S+1 (billingMonth deslocado +1 mês) e compõe cmvMes/impostosMes via resolveTaxAndCmv"
    - "No mês FECHADO o card mostra imposto real da guia + CMV cheio; no mês ABERTO tudo idêntico a hoje (zero regressão visual)"
    - "O card exibe selo 'imposto real (guia)' vs 'estimado (provisão)' e uma nota de base de CMV (cheio vs médio)"
    - "Abril/2026 (fechado via guia Maio 16.015) reconcilia com a DRE do Wesley; Junho segue em provisão"
    - "Legível em light+dark, responsivo no mobile"
  artifacts:
    - path: "src/pages/MercadoLivre.tsx"
      provides: "composição real/provisão + cálculo da competência S+1 + wiring do useImpostoGuia"
      contains: "useImpostoGuia"
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "selo/popover de base do imposto e nota de base do CMV"
      contains: "impostoFonte"
  key_links:
    - from: "MercadoLivre.tsx"
      to: "useImpostoGuia (S+1)"
      via: "guiaCompetenceFrom = billingMonth + 1 mês (YYYY-MM-01)"
      pattern: "useImpostoGuia"
    - from: "MercadoLivre.tsx"
      to: "MLCostCard"
      via: "props impostoFonte/cmvFonte alimentam o selo"
      pattern: "impostoFonte"
---

<objective>
Wirar o cérebro do Plan 90-03 na página e mostrar o resultado com selo explicativo — fechando o milestone "DRE de Resultado".

Na `MercadoLivre.tsx`: (1) calcular a **competência da guia = S+1** deslocando `billingMonth` em +1 mês (a régua deslocada — decisão LOCKED em 90-DATA-FINDINGS); (2) `useImpostoGuia(guiaCompetenceFrom)`; (3) substituir as atribuições diretas de `cmvMes`/`impostosMes` (linhas 258-260) por uma chamada a `resolveTaxAndCmv(...)`; (4) passar `impostoFonte`/`cmvFonte` ao `MLCostCard`.

No `MLCostCard.tsx`: selo/popover ao lado de "Impostos próprios" — "imposto real (guia)" (emerald) vs "estimado (provisão)" (amber) — e uma nota discreta na linha do CMV (base "custo cheio" vs "custo médio"), reusando os padrões já existentes (pill lines 131-141, Popover+HelpCircle lines 381-411). Sem cores novas (só tokens semânticos), light+dark, mobile (padrão Phase 78).

Fecha com reconciliação (Abril → guia Maio 16.015 ≈ planilha do Wesley), prova de zero-regressão do mês aberto, e checkpoint visual do Wesley.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-dre-imposto-real-cmv-fechamento/90-CONTEXT.md
@.planning/phases/90-dre-imposto-real-cmv-fechamento/90-DATA-FINDINGS.md
@src/pages/MercadoLivre.tsx
@src/components/mercadolivre/MLCostCard.tsx
@src/lib/dreOperational.ts
@src/hooks/useImpostoGuia.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composição real/provisão + competência S+1 em MercadoLivre.tsx</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <read_first>
    - `src/pages/MercadoLivre.tsx` linhas 182-241 — `billingMonth`, `billingMonthFrom`, `shiftDreMonth` (o idioma `Date.UTC(y, m-1+delta, 1)` para deslocar mês é o modelo para calcular S+1), `dreWaterfall`, `useDreOperational(billingMonthFrom)`.
    - Linhas 257-260 — as atribuições atuais de `receitaMes`/`cmvMes`/`impostosMes` (o alvo da substituição).
    - Linhas 780-797 — o JSX `<MLCostCard ... cmvMes impostosMes fonte dreOperational .../>` (onde passar as novas props).
    - `src/lib/dreOperational.ts` — assinatura de `resolveTaxAndCmv`.
    - `src/hooks/useImpostoGuia.ts` — assinatura/forma de retorno do hook.
    - `90-DATA-FINDINGS.md` — régua: DRE do mês de venda S usa a guia de competência S+1.
  </read_first>
  <action>
    1. Calcular `guiaCompetenceFrom` = primeiro dia do mês SEGUINTE a `billingMonth`: derivar de `billingMonth.split('-').map(Number)` e `new Date(Date.UTC(y, m, 1))` (m já = mês humano; Date.UTC com mês m dá o mês seguinte 0-based) → formatar `YYYY-MM-01`. Usar um useMemo dependente de billingMonth, espelhando o padrão de billingMonthFrom.
    2. `const guia = useImpostoGuia(guiaCompetenceFrom);` — usar o valor com default seguro enquanto carrega (`guia.data ?? { hasGuiaReal: false, totalReal: 0 }`).
    3. Substituir as linhas 258-260: manter `receitaMes = dreWaterfall?.paid_revenue ?? 0;`. Trocar as duas linhas cmvMes/impostosMes por uma chamada a `resolveTaxAndCmv` passando: estimatedTax `dreWaterfall?.total_tax ?? null`, hasTaxData `!!dreWaterfall?.has_tax_data`, custoMedio `dreWaterfall?.cmv ?? null`, hasCmv `!!dreWaterfall?.has_cmv`, cmvCheio `dreWaterfall?.cmv_cheio ?? null`, hasCmvCheio `!!dreWaterfall?.has_cmv_cheio`, guia (o objeto acima). Desestruturar `{ impostosMes, cmvMes, impostoFonte, cmvFonte }`.
    4. Passar ao `<MLCostCard>` as novas props `impostoFonte={impostoFonte}` e `cmvFonte={cmvFonte}`.
    Zero-regressão (SC1): quando `hasGuiaReal === false`, resolveTaxAndCmv já reproduz as expressões antigas — o mês aberto/corrente não pode mudar. NÃO alterar o cálculo de `lucro` no MLCostCard nem a fonte "competencia/billing/estimado" das TARIFAS (essa fonte é das tarifas ML, ortogonal ao imposto próprio).
  </action>
  <verify>
    <automated>cd /root/garment-glow-dre && grep -q "useImpostoGuia" src/pages/MercadoLivre.tsx && grep -q "resolveTaxAndCmv" src/pages/MercadoLivre.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - guiaCompetenceFrom = billingMonth + 1 mês; useImpostoGuia consumido com essa competência.
    - cmvMes/impostosMes derivam de resolveTaxAndCmv; impostoFonte/cmvFonte passados ao card.
    - tsc limpo; nenhuma mudança no cálculo de lucro/resultado nem na fonte de tarifas.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Selo imposto real/provisão + nota de base do CMV no MLCostCard</name>
  <files>src/components/mercadolivre/MLCostCard.tsx</files>
  <read_first>
    - `src/components/mercadolivre/MLCostCard.tsx` linhas 21-54 (interface MLCostCardProps), 76-93 (lucro + useState do popover), 131-141 (pill de fonte emerald/amber), 215-261 (linhas CMV e Impostos próprios), 381-411 (padrão Popover+HelpCircle "aproximado").
  </read_first>
  <action>
    1. Estender `MLCostCardProps` com `impostoFonte?: "real" | "provisao"` e `cmvFonte?: "cheio" | "medio" | "medio_fallback"` (opcionais, default "provisao"/"medio" para não quebrar chamadas existentes). Aceitar no destructuring de props com os defaults.
    2. Na linha "Impostos próprios" (bloco 239-261), ao lado do rótulo renderizar um pill 2-estados reusando o idioma das linhas 131-141: `impostoFonte === "real"` → texto "imposto real (guia)" com classes emerald (`bg-emerald-500/15 text-emerald-400`); senão "estimado (provisão)" com amber (`bg-amber-500/15 text-amber-400`). Mostrar só quando `impostosMes != null`.
    3. Acrescentar um Popover+HelpCircle (novo `useState` local `impostoTooltipOpen`) reusando EXATAMENTE o padrão das linhas 381-411, com texto explicando: base real = "Imposto da guia de ICMS/PIS/COFINS já apurada — competência do mês seguinte à venda"; provisão = "Estimativa automática por item pelo regime fiscal, enquanto a guia real não é lançada".
    4. Na linha "CMV do mês" (215-237): nota discreta (span `text-[9px] italic text-muted-foreground/80`) — `cmvFonte === "cheio"` → "custo cheio"; `cmvFonte === "medio_fallback"` → "custo médio (sem preço cheio)"; caso "medio" → nada (default silencioso). Sem cores novas; só tokens semânticos. Manter os spans pequenos para não quebrar o layout mobile (o card usa flex+gap).
    Consistência light/dark: reusar as MESMAS classes emerald/amber do pill de fonte (já validadas nos 2 temas).
  </action>
  <verify>
    <automated>cd /root/garment-glow-dre && grep -q "impostoFonte" src/components/mercadolivre/MLCostCard.tsx && grep -q "imposto real (guia)" src/components/mercadolivre/MLCostCard.tsx && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Props impostoFonte/cmvFonte aceitas com defaults; pill 2-estados na linha de impostos com Popover explicativo.
    - Nota de base do CMV (cheio/medio_fallback) quando aplicável.
    - tsc limpo + vitest verde (sem regressão da suíte da Phase 88).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reconciliação Abril + zero-regressão + checkpoint visual Wesley</name>
  <what-built>
    DRE do `/vendas` agora distingue mês ABERTO (provisão) vs FECHADO (imposto real da guia + CMV cheio), com selo explicativo. Build + preview Vercel para o Wesley validar.
  </what-built>
  <action>Executor roda tsc/vitest/build; navega o card DRE por Abril (fechado, reconciliação vs planilha), mês corrente (aberto, zero-regressão) e Junho (segue provisão); gera preview Vercel e aguarda o ok visual do Wesley em light+dark+mobile. Bloqueia até "aprovado".</action>
  <how-to-verify>
    Provas automáticas (executor/orquestrador):
    1. `npx tsc --noEmit` limpo + `npx vitest run` verde + `npm run build` ok.
    2. Reconciliação ABRIL/2026 (mês fechado): navegar o card DRE (‹ ›) até Abril/2026. Esperado: selo "imposto real (guia)" na linha de Impostos, valor ≈ R$16.015,06 (guia competência Maio), CMV com nota "custo cheio". O resultado do mês deve aproximar a DRE do Wesley (planilha) — muito melhor que a estimativa ~20% (que dava −R$29k). Registrar os números.
    3. Zero-regressão MÊS ABERTO: no mês corrente (guia S+1 ainda não existe → hasGuiaReal false) o card mostra selo "estimado (provisão)", imposto pela estimativa e CMV custo médio — idêntico ao comportamento pré-fase.
    4. JUNHO/2026: navegar até Junho → deve permanecer "estimado (provisão)" (a guia de competência Julho é placeholder/pending; o R$4.793 de junho é a guia de MAIO deslocada e não afeta junho). Confirmar que junho NÃO vira fechado.

    Checkpoint visual do Wesley (preview Vercel, dados reais):
    5. Abrir `/vendas`, navegar Abril (fechado) vs mês corrente (aberto), em light E dark, no mobile. Confirmar: selos legíveis, valores coerentes, nenhum layout quebrado.
  </how-to-verify>
  <resume-signal>Wesley responde "aprovado" (com os números de abril conferidos vs planilha) ou descreve ajustes (ex.: selo, casas decimais, régua de algum mês específico).</resume-signal>
</task>

</tasks>

<verification>
- tsc limpo + vitest verde + build ok.
- Abril fechado reconcilia (imposto real ≈16.015 + CMV cheio); mês aberto sem regressão; junho segue em provisão.
- Selo real/provisão + nota de CMV legíveis em light/dark/mobile.
</verification>

<success_criteria>
- SC1: mês aberto sem regressão (visual + numérico).
- SC2/SC3: mês fechado usa imposto real + CMV cheio; selo determinístico na UI.
- SC5: Abril reconcilia; junho fica em provisão (placeholder tratado).
- SC6: anti-IDOR herdado do backend; light+dark+mobile; validação visual Wesley.
</success_criteria>

## Artifacts this phase produces (Plan 90-04)
- `src/pages/MercadoLivre.tsx` — composição real/provisão + régua S+1 + wiring do useImpostoGuia.
- `src/components/mercadolivre/MLCostCard.tsx` — selo imposto real/provisão + nota de base do CMV.

<output>
Create `.planning/phases/90-dre-imposto-real-cmv-fechamento/90-04-SUMMARY.md` when done.
</output>
