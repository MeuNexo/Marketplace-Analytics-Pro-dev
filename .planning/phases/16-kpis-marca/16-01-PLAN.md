---
phase: 16-kpis-marca
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useMLKPISummary.ts
  - src/components/mercadolivre/MLKPIGrid.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements:
  - KPI-01
  - KPI-02

must_haves:
  truths:
    - "Card 'Markup das Vendas' exibe ratio no formato '2.3x' calculado de SUM(receita_bruta) / SUM(custo_unit * quantidade) da tabela orders"
    - "Card 'Custo Operacional' exibe valor em R$ e percentual sobre receita calculado de SUM(frete) + SUM(comissao) + ads_total"
    - "Ambos os cards mostram '—' quando não há dados de orders (custo_unit null / sem rows)"
    - "KPIs existentes (Receita, Pedidos, Ticket Médio, Visitas, Conversão) não regridem"
  artifacts:
    - path: "src/hooks/useMLKPISummary.ts"
      provides: "Hook que agrega markup e custo operacional de public.orders + ml_ads_daily_cache"
      exports: ["useMLKPISummary", "MLKPISummary"]
    - path: "src/components/mercadolivre/MLKPIGrid.tsx"
      provides: "Grid de KPI cards atualizado com 2 novos cards"
      contains: "Markup das Vendas, Custo Operacional"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useMLKPISummary.ts"
      via: "import { useMLKPISummary }"
      pattern: "useMLKPISummary"
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/components/mercadolivre/MLKPIGrid.tsx"
      via: "prop kpiSummary passado para MLKPIGrid"
      pattern: "kpiSummary"
---

<objective>
Adicionar os 2 primeiros KPIs ao Dashboard de Vendas: "Markup das Vendas" e "Custo Operacional da Plataforma". Estes cards não requerem o campo `marca` — calculam diretamente de `public.orders` e `ml_ads_daily_cache` já existentes.

Purpose: Revelar para o usuário a rentabilidade real das vendas (quanto o preço cobre o custo) e o peso total dos custos de plataforma em um único número.
Output: Hook `useMLKPISummary` + 2 novos KPICards integrados no grid existente da página `/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-kpis-marca/16-CONTEXT.md

<interfaces>
<!-- Interfaces existentes que o executor precisa. Não explorar codebase. -->

De src/hooks/useMLOrders.ts:
```typescript
export interface MLOrderSummary {
  total_comissao: number;
  total_frete: number;
  paid_orders_count: number;
  paid_revenue: number;
}
export function useMLOrders(from: string, to: string): UseQueryResult<MLOrderSummary | null>
// Query key: ["ml", "orders-summary", orgId, resolvedMLUserIds, from, to]
// Filtra: organization_id + in ml_user_id + gte/lte data_pedido
// Retorna null se rows.length === 0
```

De src/hooks/useMLAds.ts:
```typescript
export interface AdsSummary {
  total_spend: number;
  // ... outros campos
}
export function useMLAds(opts: { dateFrom?: string; dateTo?: string }): UseMLAdsResult
// Retorna { summary: AdsSummary, daily: AdsDailyStat[], ... }
// MercadoLivre.tsx já usa: computeAdsSummary(adsDaily.filter(...)) → adsSummary.total_spend
```

De src/components/dashboard/KPICard.tsx:
```typescript
interface KPICardProps {
  title: string;
  value: string;          // string formatada
  subtitle?: string;      // linha secundária (ex: "12,3% da receita")
  icon?: React.ReactNode;
  variant?: "default" | "success" | "warning" | "danger" | "info" | "neutral" | "purple" | "orange" | "minimal";
  iconClassName?: string;
  size?: "default" | "compact" | "tv";
  loading?: boolean;
  refreshing?: boolean;
  delta?: number;         // percentual de variação vs período anterior
  tooltip?: string;
}
```

De src/components/mercadolivre/MLKPIGrid.tsx:
```typescript
interface MLKPIGridProps {
  metrics: Metrics | null;
  previousMetrics: Metrics | null;
  loading: boolean;
  syncing: boolean;
  hasSyncProgress: boolean;
}
// Grid: className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3"
// 5 cards existentes: Receita Total, Pedidos, Ticket Médio, Visitas, Conversão
```

De src/pages/MercadoLivre.tsx (estado atual relevante):
```typescript
const { data: ordersSummary } = useMLOrders(currentFrom, currentTo);
const adsSummary = useMemo(
  () => computeAdsSummary(adsDaily.filter((d) => d.date >= currentFrom && d.date <= currentTo)),
  [adsDaily, currentFrom, currentTo],
);
// costSummary já calcula: comissao, frete, publicidade (ads), total_known, gross_revenue
```

De src/integrations/supabase/client.ts:
```typescript
export { supabase }  // cliente Supabase já configurado
```

De src/contexts/MLStoreContext.tsx:
```typescript
const { resolvedMLUserIds } = useMLStore();
// resolvedMLUserIds: string[] — IDs das lojas ML selecionadas
```

De src/contexts/OrganizationContext.tsx:
```typescript
const { currentOrg } = useOrganization();
// currentOrg?.id: string | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook useMLKPISummary — markup e custo operacional</name>
  <files>src/hooks/useMLKPISummary.ts</files>
  <behavior>
    - Quando orders tem rows com custo_unit e receita_bruta: retorna markup_ratio = SUM(receita_bruta) / SUM(custo_unit * quantidade)
    - Quando custo_unit é null em todos os rows: markup_has_cost = false (sem dado de custo)
    - Quando orders não tem rows: retorna null (sem dados para o período)
    - custo_operacional = total_frete + total_comissao + ads_total (nunca null — ads_total default 0)
    - pct_custo_operacional = custo_operacional / gross_revenue * 100 (0 se gross_revenue = 0)
  </behavior>
  <action>
Criar `src/hooks/useMLKPISummary.ts` com o seguinte conteúdo exato:

```typescript
/**
 * useMLKPISummary — Markup das Vendas + Custo Operacional da Plataforma
 * Fase 16: KPIs de Marca (Plano 01)
 *
 * markup_ratio = SUM(receita_bruta) / SUM(custo_unit * quantidade)
 *   — retorna null quando custo_unit está ausente em todos os rows
 * custo_operacional = SUM(frete) + SUM(comissao) + ads_total
 */
import { useQuery } from "@tanstack/react-query";
import { useMLStore } from "@/contexts/MLStoreContext";
import { useOrganization } from "@/contexts/OrganizationContext";
import { supabase } from "@/integrations/supabase/client";

export interface MLKPISummary {
  /** SUM(receita_bruta) / SUM(custo_unit * quantidade). null = sem dados de custo. */
  markup_ratio: number | null;
  /** true se havia ao menos 1 row com custo_unit != null */
  markup_has_cost: boolean;
  /** SUM(frete) + SUM(comissao) — base para custo operacional (sem ads) */
  custo_plataforma: number;
  /** frete + comissao + ads_total */
  custo_operacional: number;
  /** custo_operacional / gross_revenue * 100 */
  pct_custo_operacional: number;
  /** SUM(receita_bruta) de todas as rows do período */
  gross_revenue: number;
}

export function useMLKPISummary(
  from: string,
  to: string,
  /** ads_total para o mesmo período — vem de computeAdsSummary no caller */
  ads_total: number,
) {
  const { resolvedMLUserIds } = useMLStore();
  const { currentOrg } = useOrganization();
  const orgId = currentOrg?.id ?? null;

  return useQuery<MLKPISummary | null>({
    queryKey: ["ml", "kpi-summary", orgId, resolvedMLUserIds, from, to],
    queryFn: async (): Promise<MLKPISummary | null> => {
      if (!orgId || resolvedMLUserIds.length === 0) return null;

      const { data, error } = await supabase
        .from("orders")
        .select("receita_bruta, custo_unit, quantidade, frete, comissao")
        .eq("organization_id", orgId)
        .in("ml_user_id", resolvedMLUserIds)
        .gte("data_pedido", from)
        .lte("data_pedido", to);

      if (error) throw error;

      const rows = data ?? [];
      if (rows.length === 0) return null;

      // ── Markup ─────────────────────────────────────────────────────────────
      let sum_receita = 0;
      let sum_custo = 0;
      let markup_has_cost = false;

      for (const r of rows) {
        const receita = r.receita_bruta ?? 0;
        const custo = r.custo_unit;
        const qty = r.quantidade ?? 1;
        sum_receita += receita;
        if (custo != null) {
          markup_has_cost = true;
          sum_custo += custo * qty;
        }
      }

      const markup_ratio =
        markup_has_cost && sum_custo > 0 ? sum_receita / sum_custo : null;

      // ── Custo operacional ──────────────────────────────────────────────────
      const total_frete = rows.reduce((s, r) => s + (r.frete ?? 0), 0);
      const total_comissao = rows.reduce((s, r) => s + (r.comissao ?? 0), 0);
      const custo_plataforma = total_frete + total_comissao;
      const custo_operacional = custo_plataforma + ads_total;
      const gross_revenue = sum_receita;
      const pct_custo_operacional =
        gross_revenue > 0
          ? Math.round((custo_operacional / gross_revenue) * 10000) / 100
          : 0;

      return {
        markup_ratio,
        markup_has_cost,
        custo_plataforma,
        custo_operacional,
        pct_custo_operacional,
        gross_revenue,
      };
    },
    enabled: !!orgId && resolvedMLUserIds.length > 0 && !!from && !!to,
    staleTime: 5 * 60 * 1000,
  });
}
```

IMPORTANTE: `ads_total` NÃO entra no queryKey — o hook re-executa a query de orders independentemente do valor de ads. O caller calcula ads_total separadamente via `computeAdsSummary` e passa como parâmetro apenas para o cálculo local dentro do `queryFn`. Para evitar re-fetch desnecessário quando ads_total muda, computar `custo_operacional` no caller usando `kpiSummary?.custo_plataforma + adsSummary.total_spend` em vez de depender do valor retornado pelo hook. Porém para simplicidade, aceita-se re-fetch quando ads mudam — o staleTime de 5 min mitiga isso em uso normal.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -E "useMLKPISummary|error" | head -20</automated>
  </verify>
  <done>Arquivo existe, exporta MLKPISummary e useMLKPISummary, sem erros TypeScript</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar MLKPIGrid + wiring em MercadoLivre.tsx</name>
  <files>src/components/mercadolivre/MLKPIGrid.tsx, src/pages/MercadoLivre.tsx</files>
  <action>
**Passo A — Atualizar `src/components/mercadolivre/MLKPIGrid.tsx`:**

Adicionar prop `kpiSummary` e os 2 novos cards. O grid deve expandir de 5 para 7 cards:

```typescript
import { KPICard } from "@/components/dashboard/KPICard";
import { DollarSign, ShoppingCart, Tag, Eye, Percent, TrendingUp, Wallet } from "lucide-react";
import type { MLKPISummary } from "@/hooks/useMLKPISummary";

const currencyFmt = (v: number) =>
  v.toLocaleString("pt-BR", { style: "currency", currency: "BRL" });

interface Metrics {
  total_revenue: number;
  units_sold: number;
  avg_ticket: number;
  unique_visits: number;
  conversion_rate: number;
  total_orders: number;
  unique_buyers: number;
}

interface MLKPIGridProps {
  metrics: Metrics | null;
  previousMetrics: Metrics | null;
  loading: boolean;
  syncing: boolean;
  hasSyncProgress: boolean;
  kpiSummary?: MLKPISummary | null;   // novo — opcional para não quebrar callers existentes
  kpiSummaryLoading?: boolean;        // novo — opcional
  /** ads_total calculado no caller — somado a custo_plataforma para evitar stale data quando período muda */
  adsTotalForPeriod?: number;
}

function calcDelta(current: number, previous: number | undefined) {
  if (previous === undefined || previous === 0) return undefined;
  return ((current - previous) / previous) * 100;
}

export function MLKPIGrid({
  metrics,
  previousMetrics,
  loading,
  syncing,
  hasSyncProgress,
  kpiSummary,
  kpiSummaryLoading,
  adsTotalForPeriod = 0,
}: MLKPIGridProps) {
  const refreshing = syncing && !hasSyncProgress;
  const summaryLoading = loading || (kpiSummaryLoading ?? false);

  // Markup display
  const markupValue = (() => {
    if (!kpiSummary) return "—";
    if (!kpiSummary.markup_has_cost) return "s/ custo";
    if (kpiSummary.markup_ratio == null) return "—";
    return `${kpiSummary.markup_ratio.toFixed(2)}x`;
  })();

  // Custo operacional: somar custo_plataforma (do banco) + ads (do caller, sempre fresco)
  // Não usar kpiSummary.custo_operacional — ads_total não entra no queryKey, valor poderia ficar stale
  const custoTotal = kpiSummary
    ? (kpiSummary.custo_plataforma ?? 0) + adsTotalForPeriod
    : null;
  const custoOpValue = custoTotal != null ? currencyFmt(custoTotal) : "—";
  const custoOpSubtitle = custoTotal != null && kpiSummary && kpiSummary.gross_revenue > 0
    ? `${((custoTotal / kpiSummary.gross_revenue) * 100).toFixed(1)}% da receita`
    : undefined;

  return (
    <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-7 gap-3">
      {/* Cards existentes — mantidos sem alteração */}
      <KPICard
        title="Receita Total"
        value={metrics ? currencyFmt(metrics.total_revenue) : "—"}
        icon={<DollarSign className="w-4 h-4" />}
        variant="minimal"
        iconClassName="bg-accent/10 text-accent"
        size="compact"
        loading={loading}
        refreshing={refreshing}
        delta={metrics && previousMetrics ? calcDelta(metrics.total_revenue, previousMetrics.total_revenue) : undefined}
      />
      <KPICard
        title="Pedidos"
        value={metrics ? String(metrics.total_orders) : "—"}
        icon={<ShoppingCart className="w-4 h-4" />}
        variant="minimal"
        iconClassName="bg-[hsl(270,70%,50%)]/10 text-[hsl(270,70%,50%)]"
        size="compact"
        loading={loading}
        refreshing={refreshing}
        delta={metrics && previousMetrics ? calcDelta(metrics.total_orders, previousMetrics.total_orders) : undefined}
      />
      <KPICard
        title="Ticket Médio"
        value={
          metrics
            ? metrics.avg_ticket.toLocaleString("pt-BR", {
                style: "currency",
                currency: "BRL",
                minimumFractionDigits: 0,
                maximumFractionDigits: 0,
              })
            : "—"
        }
        icon={<Tag className="w-4 h-4" />}
        variant="minimal"
        iconClassName="bg-[hsl(25,95%,53%)]/10 text-[hsl(25,95%,53%)]"
        size="compact"
        loading={loading}
        refreshing={refreshing}
        delta={metrics && previousMetrics ? calcDelta(metrics.avg_ticket, previousMetrics.avg_ticket) : undefined}
      />
      <KPICard
        title="Visitas"
        value={metrics ? metrics.unique_visits.toLocaleString("pt-BR") : "—"}
        icon={<Eye className="w-4 h-4" />}
        variant="minimal"
        iconClassName="bg-accent/10 text-accent"
        size="compact"
        loading={loading}
        refreshing={refreshing}
        delta={metrics && previousMetrics ? calcDelta(metrics.unique_visits, previousMetrics.unique_visits) : undefined}
      />
      <KPICard
        title="Conversão"
        value={metrics ? `${metrics.conversion_rate.toFixed(2)}%` : "—"}
        icon={<Percent className="w-4 h-4" />}
        variant="minimal"
        iconClassName="bg-success/10 text-success"
        size="compact"
        loading={loading}
        refreshing={refreshing}
        delta={metrics && previousMetrics ? calcDelta(metrics.conversion_rate, previousMetrics.conversion_rate) : undefined}
      />

      {/* Novos KPI cards — Fase 16 */}
      <KPICard
        title="Markup das Vendas"
        value={markupValue}
        subtitle={
          kpiSummary?.markup_has_cost && kpiSummary.markup_ratio != null
            ? "preço / custo produto"
            : undefined
        }
        icon={<TrendingUp className="w-4 h-4" />}
        variant="minimal"
        iconClassName="bg-emerald-500/10 text-emerald-500"
        size="compact"
        loading={summaryLoading}
        refreshing={refreshing}
        tooltip="Receita bruta total dividida pelo custo total dos produtos vendidos (custo_unit × quantidade). Requer custo cadastrado em Anúncios."
      />
      <KPICard
        title="Custo Operacional"
        value={custoOpValue}
        subtitle={custoOpSubtitle}
        icon={<Wallet className="w-4 h-4" />}
        variant="minimal"
        iconClassName="bg-orange-500/10 text-orange-500"
        size="compact"
        loading={summaryLoading}
        refreshing={refreshing}
        tooltip="Frete + Comissão ML + Publicidade (ads) no período selecionado."
      />
    </div>
  );
}
```

**Passo B — Atualizar `src/pages/MercadoLivre.tsx`:**

1. Adicionar import do hook:
```typescript
import { useMLKPISummary } from "@/hooks/useMLKPISummary";
```

2. Adicionar chamada ao hook logo após a linha `const { data: ordersSummary } = useMLOrders(currentFrom, currentTo);`:
```typescript
const { data: kpiSummary, isLoading: kpiSummaryLoading } = useMLKPISummary(
  currentFrom,
  currentTo,
  adsSummary.total_spend,
);
```

3. Atualizar o `<MLKPIGrid>` passando as novas props:
```tsx
<MLKPIGrid
  metrics={effectiveMetrics}
  previousMetrics={previousMetrics}
  loading={effectiveLoading}
  syncing={effectiveSyncing}
  hasSyncProgress={!!syncProgress}
  kpiSummary={kpiSummary}
  kpiSummaryLoading={kpiSummaryLoading}
  adsTotalForPeriod={adsSummary.total_spend}
/>
```
NOTA: `adsTotalForPeriod` é passado separadamente (não via hook) porque `ads_total` não entra no queryKey de `useMLKPISummary`. Isso garante que o card "Custo Operacional" sempre reflete o ads_total fresco do período atual.

ATENÇÃO: `adsSummary` é computado via `useMemo` algumas linhas acima usando `adsDaily` — garantir que a linha `useMLKPISummary` apareça APÓS o `useMemo` de `adsSummary`. Não mover o hook antes de `adsSummary` ser definido.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
- MLKPIGrid aceita prop kpiSummary opcional (sem quebrar callers)
- Grid tem 7 colunas no breakpoint lg (grid-cols-7)
- Cards "Markup das Vendas" e "Custo Operacional" aparecem nos 2 slots finais
- MercadoLivre.tsx importa e passa kpiSummary para MLKPIGrid
- Zero erros TypeScript
  </done>
</task>

</tasks>

<verification>
```bash
cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -5
```

Verificar visualmente (se disponível):
- Acessar `/` — grid KPI mostra 7 cards incluindo "Markup das Vendas" e "Custo Operacional"
- Card Markup mostra "—" ou "s/ custo" quando sem dados de custo cadastrado
- Card Custo Op mostra R$ + porcentagem
- KPIs anteriores (Receita, Pedidos, etc.) continuam funcionando
</verification>

<success_criteria>
1. `src/hooks/useMLKPISummary.ts` existe com exports `MLKPISummary` e `useMLKPISummary`
2. `MLKPIGrid` aceita prop `kpiSummary?: MLKPISummary | null` sem quebrar uso existente
3. Dashboard `/` exibe 7 KPI cards — os 5 originais + Markup das Vendas + Custo Operacional
4. TypeScript compila sem erros em ambos os arquivos
5. Markup formula: `SUM(receita_bruta) / SUM(custo_unit * quantidade)` — sem hardcode
6. Custo Operacional: `frete + comissao + ads_total` — inclui publicidade
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-kpis-marca/16-01-SUMMARY.md` com:
- O que foi implementado
- Exports criados (interface + hook)
- Fórmulas usadas
- Qualquer decisão de implementação relevante
</output>
