---
phase: 16-kpis-marca
plan: "03"
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - src/hooks/useMLOrdersByBrand.ts
  - src/components/mercadolivre/BrandRevenueChart.tsx
  - src/components/mercadolivre/BrandMarkupChart.tsx
  - src/components/mercadolivre/CustoOperacionalChart.tsx
  - src/components/mercadolivre/BrandSharePieChart.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements:
  - KPI-03
  - KPI-04
  - KPI-05
  - KPI-06
  - KPI-07

must_haves:
  truths:
    - "Gráfico de área empilhado exibe faturamento por marca ao longo do tempo (top 5 marcas)"
    - "Gráfico de linha exibe markup por marca ao longo do tempo (1 linha por marca)"
    - "Gráfico de linha exibe custo operacional total (frete+comissao+ads) por dia"
    - "2 donuts exibem participação de receita por marca e volume por marca — soma = 100%"
    - "Todos os gráficos mostram skeleton/empty state quando marca não está populada"
    - "Gráficos aparecem na tab 'Vendas' abaixo dos componentes existentes"
  artifacts:
    - path: "src/hooks/useMLOrdersByBrand.ts"
      provides: "Hook de time series e aggregados por marca"
      exports: ["useMLOrdersByBrand", "BrandTimeSeries", "BrandAggregate"]
    - path: "src/components/mercadolivre/BrandRevenueChart.tsx"
      provides: "AreaChart empilhado de faturamento por marca"
    - path: "src/components/mercadolivre/BrandMarkupChart.tsx"
      provides: "LineChart de markup por marca"
    - path: "src/components/mercadolivre/CustoOperacionalChart.tsx"
      provides: "LineChart de custo operacional total diário"
    - path: "src/components/mercadolivre/BrandSharePieChart.tsx"
      provides: "Dois donuts: participação de receita e volume por marca"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useMLOrdersByBrand.ts"
      via: "import { useMLOrdersByBrand }"
      pattern: "useMLOrdersByBrand"
    - from: "src/components/mercadolivre/BrandRevenueChart.tsx"
      to: "src/components/ui/chart.tsx"
      via: "ChartContainer, ChartTooltip, ChartLegend"
      pattern: "ChartContainer"
---

<objective>
Criar os 5 gráficos de breakdown por marca: AreaChart empilhado (faturamento), LineChart (markup), LineChart (custo operacional), e 2 donuts (participação de receita e volume). Requerem que o Plano 16-02 tenha sido executado (coluna `marca` populada em `public.orders`).

Purpose: Revelar quais marcas puxam faturamento, qual tem melhor markup e qual consome mais custo de plataforma.
Output: Hook `useMLOrdersByBrand` + 4 componentes de gráfico + wiring em MercadoLivre.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kpis-marca/16-CONTEXT.md
@.planning/phases/16-kpis-marca/16-02-SUMMARY.md

<interfaces>
<!-- Contratos que o executor precisa. Não explorar codebase. -->

De src/hooks/useMLOrders.ts (padrão a seguir):
```typescript
// Query pattern: supabase.from("orders").select(...).eq("organization_id", orgId)
//   .in("ml_user_id", resolvedMLUserIds).gte("data_pedido", from).lte("data_pedido", to)
// staleTime: 5 * 60 * 1000
// enabled: !!orgId && resolvedMLUserIds.length > 0 && !!from && !!to
```

De src/contexts/MLStoreContext.tsx:
```typescript
const { resolvedMLUserIds } = useMLStore();
```

De src/contexts/OrganizationContext.tsx:
```typescript
const { currentOrg } = useOrganization();
// currentOrg?.id: string | null
```

De src/components/ui/chart.tsx:
```typescript
export type ChartConfig = {
  [k in string]: {
    label?: React.ReactNode;
    icon?: React.ComponentType;
  } & ({ color?: string } | { theme: Record<"light" | "dark", string> });
};
export { ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent }
// ChartContainer: <ChartContainer config={chartConfig} className="h-[280px]">
//   <AreaChart data={data}> ... </AreaChart>
// </ChartContainer>
```

De recharts (já instalado v2.15.4):
```typescript
import {
  AreaChart, Area, LineChart, Line, PieChart, Pie, Cell,
  XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer
} from "recharts";
// Usar via ChartContainer (wrapper já configurado) — não usar ResponsiveContainer diretamente
```

De src/hooks/useMLAds.ts (para custo operacional diário):
```typescript
// ml_ads_daily_cache tem campo: date (string), spend (number), ml_user_id (string)
// Query direta: supabase.from("ml_ads_daily_cache").select("date, spend").in("ml_user_id", ...).gte/lte
```

Tabela public.orders após Plano 16-02:
```sql
-- Campos relevantes para os gráficos:
-- data_pedido TEXT (YYYY-MM-DD)
-- marca TEXT (nullable — null = sem marca sincronizada)
-- receita_bruta NUMERIC
-- custo_unit NUMERIC (nullable)
-- quantidade INTEGER
-- frete NUMERIC
-- comissao NUMERIC
-- ml_user_id TEXT
-- organization_id UUID
```

Cores para marcas (definir no hook, reutilizar nos componentes):
```typescript
export const BRAND_COLORS = [
  "hsl(217, 70%, 50%)",   // azul
  "hsl(142, 70%, 45%)",   // verde
  "hsl(25, 95%, 53%)",    // laranja
  "hsl(270, 70%, 50%)",   // roxo
  "hsl(0, 70%, 55%)",     // vermelho
  "hsl(185, 70%, 45%)",   // ciano
  "hsl(45, 95%, 55%)",    // amarelo
];
```

De src/pages/MercadoLivre.tsx (estado atual após Plano 16-01):
```typescript
// Já disponível no componente:
const { daily: adsDaily } = useMLAds({ dateFrom: adsChartFrom, dateTo: currentTo });
const adsSummary = useMemo(() => computeAdsSummary(adsDaily.filter(...)), [adsDaily, currentFrom, currentTo]);
// currentFrom, currentTo: strings YYYY-MM-DD para o período selecionado
// adsDaily: AdsDailyStat[] com { date, spend, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook useMLOrdersByBrand — time series e aggregados por marca</name>
  <files>src/hooks/useMLOrdersByBrand.ts</files>
  <action>
Criar `src/hooks/useMLOrdersByBrand.ts` com o conteúdo exato abaixo.

O hook faz UMA query em `public.orders` e computa tudo em memória: time series diária por marca, top marcas, dados para pizza.

```typescript
/**
 * useMLOrdersByBrand — Gráficos de marca (Phase 16, Plano 03)
 *
 * Retorna:
 *  - brandTimeSeries: dados diários por marca para AreaChart e LineChart de markup
 *  - custoTimeSeries: custo operacional total por dia (frete+comissao, sem ads)
 *    — ads são passados pelo caller via adsDaily e mesclados nos componentes
 *  - brandAggregates: totais por marca para os donuts
 *  - topBrands: lista das N marcas mais relevantes por receita
 *  - hasData: true se há rows com marca populada
 */
import { useQuery } from "@tanstack/react-query";
import { useMLStore } from "@/contexts/MLStoreContext";
import { useOrganization } from "@/contexts/OrganizationContext";
import { supabase } from "@/integrations/supabase/client";

export const BRAND_COLORS = [
  "hsl(217, 70%, 50%)",
  "hsl(142, 70%, 45%)",
  "hsl(25, 95%, 53%)",
  "hsl(270, 70%, 50%)",
  "hsl(0, 70%, 55%)",
  "hsl(185, 70%, 45%)",
  "hsl(45, 95%, 55%)",
];

/** Um ponto no tempo para um gráfico de série temporal */
export interface BrandTimeSeries {
  /** YYYY-MM-DD */
  date: string;
  /** chave dinâmica: nome_da_marca → receita do dia */
  [brand: string]: string | number;
}

/** Totais por marca para os donuts */
export interface BrandAggregate {
  marca: string;
  receita: number;
  unidades: number;
  /** markup_ratio = SUM(receita_bruta) / SUM(custo_unit * quantidade). null = sem custo */
  markup_ratio: number | null;
  color: string;
}

export interface BrandMarkupSeries {
  /** YYYY-MM-DD */
  date: string;
  /** chave dinâmica: nome_da_marca → markup_ratio do dia */
  [brand: string]: string | number | null;
}

export interface CustoOperacionalSeries {
  /** YYYY-MM-DD */
  date: string;
  /** frete + comissao (sem ads — ads são somados pelo componente via adsDaily) */
  custo_plataforma: number;
}

export interface MLOrdersByBrandResult {
  /** Série temporal de receita por marca (para AreaChart empilhado) */
  brandRevenueSeries: BrandTimeSeries[];
  /** Série temporal de markup por marca (para LineChart de markup) */
  brandMarkupSeries: BrandMarkupSeries[];
  /** Série temporal de custo de plataforma por dia (para CustoOperacionalChart) */
  custoSeries: CustoOperacionalSeries[];
  /** Top N marcas por receita */
  topBrands: string[];
  /** Aggregados por marca para os 2 donuts */
  brandAggregates: BrandAggregate[];
  /** true se há rows com marca não-null */
  hasData: boolean;
}

const MAX_BRANDS = 7; // top N marcas + "Outros"

export function useMLOrdersByBrand(from: string, to: string) {
  const { resolvedMLUserIds } = useMLStore();
  const { currentOrg } = useOrganization();
  const orgId = currentOrg?.id ?? null;

  return useQuery<MLOrdersByBrandResult>({
    queryKey: ["ml", "orders-by-brand", orgId, resolvedMLUserIds, from, to],
    queryFn: async (): Promise<MLOrdersByBrandResult> => {
      const empty: MLOrdersByBrandResult = {
        brandRevenueSeries: [],
        brandMarkupSeries: [],
        custoSeries: [],
        topBrands: [],
        brandAggregates: [],
        hasData: false,
      };

      if (!orgId || resolvedMLUserIds.length === 0) return empty;

      const { data, error } = await supabase
        .from("orders")
        .select("data_pedido, marca, receita_bruta, custo_unit, quantidade, frete, comissao")
        .eq("organization_id", orgId)
        .in("ml_user_id", resolvedMLUserIds)
        .gte("data_pedido", from)
        .lte("data_pedido", to);

      if (error) throw error;

      const rows = data ?? [];
      const rowsWithBrand = rows.filter((r) => r.marca);
      if (rowsWithBrand.length === 0) return empty;

      // ── Aggregados por marca ───────────────────────────────────────────────
      const brandMap = new Map<
        string,
        { receita: number; unidades: number; sumCusto: number; hasCusto: boolean }
      >();

      for (const r of rowsWithBrand) {
        const marca = r.marca as string;
        const receita = r.receita_bruta ?? 0;
        const qty = r.quantidade ?? 1;
        const custo = r.custo_unit;
        const existing = brandMap.get(marca) ?? {
          receita: 0,
          unidades: 0,
          sumCusto: 0,
          hasCusto: false,
        };
        existing.receita += receita;
        existing.unidades += qty;
        if (custo != null) {
          existing.hasCusto = true;
          existing.sumCusto += custo * qty;
        }
        brandMap.set(marca, existing);
      }

      // Sort por receita desc, top MAX_BRANDS
      const sorted = Array.from(brandMap.entries()).sort(
        ([, a], [, b]) => b.receita - a.receita,
      );
      const topBrands = sorted.slice(0, MAX_BRANDS).map(([marca]) => marca);
      const topSet = new Set(topBrands);

      // Aggregados finais (incluindo "Outros" se necessário)
      const brandAggregates: BrandAggregate[] = [];
      let outrosReceita = 0;
      let outrosUnidades = 0;

      sorted.forEach(([marca, vals], idx) => {
        const markup_ratio =
          vals.hasCusto && vals.sumCusto > 0
            ? vals.receita / vals.sumCusto
            : null;
        if (topSet.has(marca)) {
          brandAggregates.push({
            marca,
            receita: vals.receita,
            unidades: vals.unidades,
            markup_ratio,
            color: BRAND_COLORS[topBrands.indexOf(marca) % BRAND_COLORS.length],
          });
        } else {
          outrosReceita += vals.receita;
          outrosUnidades += vals.unidades;
        }
      });

      if (outrosReceita > 0) {
        brandAggregates.push({
          marca: "Outros",
          receita: outrosReceita,
          unidades: outrosUnidades,
          markup_ratio: null,
          color: "hsl(220, 10%, 60%)",
        });
      }

      // ── Time series por dia ────────────────────────────────────────────────
      // Agrupar por data + marca
      type DayBrandKey = string; // "YYYY-MM-DD|marca"
      const dayBrandRevMap = new Map<DayBrandKey, number>();
      const dayBrandCostMap = new Map<DayBrandKey, { sumR: number; sumC: number; hasC: boolean }>();
      const dayCustoMap = new Map<string, number>(); // date → frete+comissao

      for (const r of rowsWithBrand) {
        const date = r.data_pedido as string;
        if (!date) continue;
        const marcaRaw = r.marca as string;
        const marca = topSet.has(marcaRaw) ? marcaRaw : "Outros";
        const receita = r.receita_bruta ?? 0;
        const qty = r.quantidade ?? 1;
        const custo = r.custo_unit;

        // Revenue series
        const revKey = `${date}|${marca}`;
        dayBrandRevMap.set(revKey, (dayBrandRevMap.get(revKey) ?? 0) + receita);

        // Markup series
        const existing = dayBrandCostMap.get(revKey) ?? {
          sumR: 0,
          sumC: 0,
          hasC: false,
        };
        existing.sumR += receita;
        if (custo != null) {
          existing.hasC = true;
          existing.sumC += custo * qty;
        }
        dayBrandCostMap.set(revKey, existing);

        // Custo plataforma series
        const frete = r.frete ?? 0;
        const comissao = r.comissao ?? 0;
        dayCustoMap.set(date, (dayCustoMap.get(date) ?? 0) + frete + comissao);
      }

      // Collect all dates in range with data
      const allDates = Array.from(
        new Set(rowsWithBrand.map((r) => r.data_pedido as string).filter(Boolean)),
      ).sort();

      // Build brand revenue series
      const brandRevenueSeries: BrandTimeSeries[] = allDates.map((date) => {
        const row: BrandTimeSeries = { date };
        for (const marca of topBrands) {
          row[marca] = dayBrandRevMap.get(`${date}|${marca}`) ?? 0;
        }
        if (outrosReceita > 0) {
          row["Outros"] = dayBrandRevMap.get(`${date}|Outros`) ?? 0;
        }
        return row;
      });

      // Build markup series
      const brandMarkupSeries: BrandMarkupSeries[] = allDates.map((date) => {
        const row: BrandMarkupSeries = { date };
        for (const marca of topBrands) {
          const key = `${date}|${marca}`;
          const vals = dayBrandCostMap.get(key);
          if (!vals || !vals.hasC || vals.sumC === 0) {
            row[marca] = null;
          } else {
            row[marca] = Math.round((vals.sumR / vals.sumC) * 100) / 100;
          }
        }
        return row;
      });

      // Build custo series
      const custoSeries: CustoOperacionalSeries[] = allDates.map((date) => ({
        date,
        custo_plataforma: Math.round((dayCustoMap.get(date) ?? 0) * 100) / 100,
      }));

      return {
        brandRevenueSeries,
        brandMarkupSeries,
        custoSeries,
        topBrands,
        brandAggregates,
        hasData: true,
      };
    },
    enabled: !!orgId && resolvedMLUserIds.length > 0 && !!from && !!to,
    staleTime: 5 * 60 * 1000,
  });
}
```
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "useMLOrdersByBrand" || echo "no errors for this file"</automated>
  </verify>
  <done>
- Arquivo existe com exports: useMLOrdersByBrand, BrandTimeSeries, BrandAggregate, BrandMarkupSeries, CustoOperacionalSeries, MLOrdersByBrandResult, BRAND_COLORS
- Zero erros TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: Componentes de gráfico (4 arquivos) + wiring em MercadoLivre.tsx</name>
  <files>
    src/components/mercadolivre/BrandRevenueChart.tsx,
    src/components/mercadolivre/BrandMarkupChart.tsx,
    src/components/mercadolivre/CustoOperacionalChart.tsx,
    src/components/mercadolivre/BrandSharePieChart.tsx,
    src/pages/MercadoLivre.tsx
  </files>
  <action>
**Arquivo 1 — `src/components/mercadolivre/BrandRevenueChart.tsx`:**

```typescript
import { AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from "recharts";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { ChartContainer, ChartTooltipContent } from "@/components/ui/chart";
import type { BrandTimeSeries } from "@/hooks/useMLOrdersByBrand";
import { BRAND_COLORS } from "@/hooks/useMLOrdersByBrand";

interface BrandRevenueChartProps {
  data: BrandTimeSeries[];
  topBrands: string[];
  loading?: boolean;
}

const currencyFmt = (v: number) =>
  v.toLocaleString("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 0 });

export function BrandRevenueChart({ data, topBrands, loading }: BrandRevenueChartProps) {
  const chartConfig = Object.fromEntries(
    topBrands.map((brand, i) => [
      brand,
      { label: brand, color: BRAND_COLORS[i % BRAND_COLORS.length] },
    ]),
  );

  if (loading) {
    return (
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium">Faturamento por Marca</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="h-[280px] bg-muted/20 animate-pulse rounded" />
        </CardContent>
      </Card>
    );
  }

  if (!data.length || !topBrands.length) {
    return (
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium">Faturamento por Marca</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="h-[280px] flex items-center justify-center text-xs text-muted-foreground">
            Sem dados de marca para o período. Sincronize orders para popular.
          </div>
        </CardContent>
      </Card>
    );
  }

  // Format date labels: "YYYY-MM-DD" → "dd/MM"
  const formattedData = data.map((d) => ({
    ...d,
    label: d.date.slice(5).replace("-", "/"),
  }));

  return (
    <Card>
      <CardHeader className="pb-2">
        <CardTitle className="text-sm font-medium">Faturamento por Marca</CardTitle>
      </CardHeader>
      <CardContent>
        <ChartContainer config={chartConfig} className="h-[280px]">
          <AreaChart data={formattedData} margin={{ top: 4, right: 8, left: 0, bottom: 0 }}>
            <CartesianGrid strokeDasharray="3 3" className="stroke-border/30" />
            <XAxis dataKey="label" tick={{ fontSize: 10 }} tickLine={false} axisLine={false} />
            <YAxis
              tick={{ fontSize: 10 }}
              tickLine={false}
              axisLine={false}
              tickFormatter={(v) => `R$${(v / 1000).toFixed(0)}k`}
              width={48}
            />
            <Tooltip
              content={
                <ChartTooltipContent
                  formatter={(value, name) => [currencyFmt(Number(value)), name]}
                />
              }
            />
            <Legend wrapperStyle={{ fontSize: 11 }} />
            {topBrands.map((brand, i) => (
              <Area
                key={brand}
                type="monotone"
                dataKey={brand}
                stackId="1"
                stroke={BRAND_COLORS[i % BRAND_COLORS.length]}
                fill={BRAND_COLORS[i % BRAND_COLORS.length]}
                fillOpacity={0.6}
              />
            ))}
            {/* "Outros" — aparece apenas quando há marcas além do top MAX_BRANDS */}
            {data[0]?.["Outros"] !== undefined && (
              <Area
                key="Outros"
                type="monotone"
                dataKey="Outros"
                stackId="1"
                stroke="hsl(220, 10%, 60%)"
                fill="hsl(220, 10%, 60%)"
                fillOpacity={0.4}
              />
            )}
          </AreaChart>
        </ChartContainer>
      </CardContent>
    </Card>
  );
}
```

---

**Arquivo 2 — `src/components/mercadolivre/BrandMarkupChart.tsx`:**

```typescript
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from "recharts";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { ChartContainer, ChartTooltipContent } from "@/components/ui/chart";
import type { BrandMarkupSeries } from "@/hooks/useMLOrdersByBrand";
import { BRAND_COLORS } from "@/hooks/useMLOrdersByBrand";

interface BrandMarkupChartProps {
  data: BrandMarkupSeries[];
  topBrands: string[];
  loading?: boolean;
}

export function BrandMarkupChart({ data, topBrands, loading }: BrandMarkupChartProps) {
  const chartConfig = Object.fromEntries(
    topBrands.map((brand, i) => [
      brand,
      { label: brand, color: BRAND_COLORS[i % BRAND_COLORS.length] },
    ]),
  );

  if (loading) {
    return (
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium">Markup por Marca</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="h-[280px] bg-muted/20 animate-pulse rounded" />
        </CardContent>
      </Card>
    );
  }

  // Check if any brand has markup data (requires custo_unit)
  const hasMarkupData = data.some((d) =>
    topBrands.some((b) => d[b] != null && Number(d[b]) > 0),
  );

  if (!data.length || !topBrands.length || !hasMarkupData) {
    return (
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium">Markup por Marca</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="h-[280px] flex items-center justify-center text-xs text-muted-foreground">
            {!data.length ? "Sem dados de marca para o período." : "Requer custo cadastrado nos anúncios."}
          </div>
        </CardContent>
      </Card>
    );
  }

  const formattedData = data.map((d) => ({
    ...d,
    label: d.date.slice(5).replace("-", "/"),
  }));

  return (
    <Card>
      <CardHeader className="pb-2">
        <CardTitle className="text-sm font-medium">Markup por Marca</CardTitle>
      </CardHeader>
      <CardContent>
        <ChartContainer config={chartConfig} className="h-[280px]">
          <LineChart data={formattedData} margin={{ top: 4, right: 8, left: 0, bottom: 0 }}>
            <CartesianGrid strokeDasharray="3 3" className="stroke-border/30" />
            <XAxis dataKey="label" tick={{ fontSize: 10 }} tickLine={false} axisLine={false} />
            <YAxis
              tick={{ fontSize: 10 }}
              tickLine={false}
              axisLine={false}
              tickFormatter={(v) => `${v.toFixed(1)}x`}
              width={40}
            />
            <Tooltip
              content={
                <ChartTooltipContent
                  formatter={(value, name) => [`${Number(value).toFixed(2)}x`, name]}
                />
              }
            />
            <Legend wrapperStyle={{ fontSize: 11 }} />
            {topBrands.map((brand, i) => (
              <Line
                key={brand}
                type="monotone"
                dataKey={brand}
                stroke={BRAND_COLORS[i % BRAND_COLORS.length]}
                strokeWidth={2}
                dot={false}
                connectNulls={false}
              />
            ))}
          </LineChart>
        </ChartContainer>
      </CardContent>
    </Card>
  );
}
```

---

**Arquivo 3 — `src/components/mercadolivre/CustoOperacionalChart.tsx`:**

```typescript
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip } from "recharts";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { ChartContainer, ChartTooltipContent } from "@/components/ui/chart";
import type { CustoOperacionalSeries } from "@/hooks/useMLOrdersByBrand";
import type { AdsDailyStat } from "@/hooks/useMLAds";

interface CustoOperacionalChartProps {
  custoSeries: CustoOperacionalSeries[];
  adsDaily: AdsDailyStat[];
  loading?: boolean;
}

const currencyFmt = (v: number) =>
  v.toLocaleString("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 0 });

const chartConfig = {
  custo_total: { label: "Custo Operacional", color: "hsl(25, 95%, 53%)" },
};

export function CustoOperacionalChart({
  custoSeries,
  adsDaily,
  loading,
}: CustoOperacionalChartProps) {
  if (loading) {
    return (
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium">Custo Operacional Diário</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="h-[220px] bg-muted/20 animate-pulse rounded" />
        </CardContent>
      </Card>
    );
  }

  if (!custoSeries.length) {
    return (
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium">Custo Operacional Diário</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="h-[220px] flex items-center justify-center text-xs text-muted-foreground">
            Sem dados para o período.
          </div>
        </CardContent>
      </Card>
    );
  }

  // Merge ads spend into custo series
  const adsMap = new Map(adsDaily.map((d) => [d.date, d.spend]));
  const chartData = custoSeries.map((d) => ({
    label: d.date.slice(5).replace("-", "/"),
    custo_total: Math.round((d.custo_plataforma + (adsMap.get(d.date) ?? 0)) * 100) / 100,
  }));

  return (
    <Card>
      <CardHeader className="pb-2">
        <CardTitle className="text-sm font-medium">Custo Operacional Diário</CardTitle>
        <p className="text-xs text-muted-foreground">Frete + Comissão + Publicidade</p>
      </CardHeader>
      <CardContent>
        <ChartContainer config={chartConfig} className="h-[220px]">
          <LineChart data={chartData} margin={{ top: 4, right: 8, left: 0, bottom: 0 }}>
            <CartesianGrid strokeDasharray="3 3" className="stroke-border/30" />
            <XAxis dataKey="label" tick={{ fontSize: 10 }} tickLine={false} axisLine={false} />
            <YAxis
              tick={{ fontSize: 10 }}
              tickLine={false}
              axisLine={false}
              tickFormatter={(v) => `R$${(v / 1000).toFixed(0)}k`}
              width={48}
            />
            <Tooltip
              content={
                <ChartTooltipContent
                  formatter={(value) => [currencyFmt(Number(value)), "Custo Operacional"]}
                />
              }
            />
            <Line
              type="monotone"
              dataKey="custo_total"
              stroke="hsl(25, 95%, 53%)"
              strokeWidth={2}
              dot={false}
            />
          </LineChart>
        </ChartContainer>
      </CardContent>
    </Card>
  );
}
```

---

**Arquivo 4 — `src/components/mercadolivre/BrandSharePieChart.tsx`:**

```typescript
import { PieChart, Pie, Cell, Tooltip, Legend } from "recharts";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { ChartContainer } from "@/components/ui/chart";
import type { BrandAggregate } from "@/hooks/useMLOrdersByBrand";

interface BrandSharePieChartProps {
  brandAggregates: BrandAggregate[];
  loading?: boolean;
}

const currencyFmt = (v: number) =>
  v.toLocaleString("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 0 });

const chartConfig = {};

export function BrandSharePieChart({ brandAggregates, loading }: BrandSharePieChartProps) {
  if (loading) {
    return (
      <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
        {[0, 1].map((i) => (
          <Card key={i}>
            <CardHeader className="pb-2">
              <CardTitle className="text-sm font-medium">
                {i === 0 ? "Receita por Marca" : "Volume por Marca"}
              </CardTitle>
            </CardHeader>
            <CardContent>
              <div className="h-[260px] bg-muted/20 animate-pulse rounded" />
            </CardContent>
          </Card>
        ))}
      </div>
    );
  }

  if (!brandAggregates.length) {
    return (
      <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
        {[0, 1].map((i) => (
          <Card key={i}>
            <CardHeader className="pb-2">
              <CardTitle className="text-sm font-medium">
                {i === 0 ? "Receita por Marca" : "Volume por Marca"}
              </CardTitle>
            </CardHeader>
            <CardContent>
              <div className="h-[260px] flex items-center justify-center text-xs text-muted-foreground">
                Sem dados de marca para o período.
              </div>
            </CardContent>
          </Card>
        ))}
      </div>
    );
  }

  const revData = brandAggregates.map((b) => ({
    name: b.marca,
    value: Math.round(b.receita),
    color: b.color,
  }));

  const volData = brandAggregates.map((b) => ({
    name: b.marca,
    value: b.unidades,
    color: b.color,
  }));

  const totalReceita = revData.reduce((s, d) => s + d.value, 0);
  const totalUnidades = volData.reduce((s, d) => s + d.value, 0);

  const renderCustomLabel = ({
    cx, cy, midAngle, innerRadius, outerRadius, percent,
  }: any) => {
    if (percent < 0.04) return null; // skip tiny slices
    const RADIAN = Math.PI / 180;
    const radius = innerRadius + (outerRadius - innerRadius) * 0.5;
    const x = cx + radius * Math.cos(-midAngle * RADIAN);
    const y = cy + radius * Math.sin(-midAngle * RADIAN);
    return (
      <text x={x} y={y} fill="white" textAnchor="middle" dominantBaseline="central" fontSize={10} fontWeight={600}>
        {`${(percent * 100).toFixed(0)}%`}
      </text>
    );
  };

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
      {/* Donut 1 — Receita */}
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium">Receita por Marca</CardTitle>
          <p className="text-xs text-muted-foreground">Total: {currencyFmt(totalReceita)}</p>
        </CardHeader>
        <CardContent>
          <ChartContainer config={chartConfig} className="h-[260px]">
            <PieChart>
              <Pie
                data={revData}
                cx="50%"
                cy="50%"
                innerRadius={60}
                outerRadius={100}
                dataKey="value"
                labelLine={false}
                label={renderCustomLabel}
              >
                {revData.map((entry, i) => (
                  <Cell key={i} fill={entry.color} />
                ))}
              </Pie>
              <Tooltip
                formatter={(value, name) => [currencyFmt(Number(value)), name]}
              />
              <Legend
                iconSize={10}
                wrapperStyle={{ fontSize: 11 }}
                formatter={(value) => value}
              />
            </PieChart>
          </ChartContainer>
        </CardContent>
      </Card>

      {/* Donut 2 — Volume */}
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium">Volume por Marca (un.)</CardTitle>
          <p className="text-xs text-muted-foreground">Total: {totalUnidades.toLocaleString("pt-BR")} un.</p>
        </CardHeader>
        <CardContent>
          <ChartContainer config={chartConfig} className="h-[260px]">
            <PieChart>
              <Pie
                data={volData}
                cx="50%"
                cy="50%"
                innerRadius={60}
                outerRadius={100}
                dataKey="value"
                labelLine={false}
                label={renderCustomLabel}
              >
                {volData.map((entry, i) => (
                  <Cell key={i} fill={entry.color} />
                ))}
              </Pie>
              <Tooltip
                formatter={(value, name) => [`${Number(value).toLocaleString("pt-BR")} un.`, name]}
              />
              <Legend
                iconSize={10}
                wrapperStyle={{ fontSize: 11 }}
                formatter={(value) => value}
              />
            </PieChart>
          </ChartContainer>
        </CardContent>
      </Card>
    </div>
  );
}
```

---

**Wiring em `src/pages/MercadoLivre.tsx`:**

Adicionar imports no topo (junto com os outros imports de hooks/componentes):
```typescript
import { useMLOrdersByBrand } from "@/hooks/useMLOrdersByBrand";
import { BrandRevenueChart } from "@/components/mercadolivre/BrandRevenueChart";
import { BrandMarkupChart } from "@/components/mercadolivre/BrandMarkupChart";
import { CustoOperacionalChart } from "@/components/mercadolivre/CustoOperacionalChart";
import { BrandSharePieChart } from "@/components/mercadolivre/BrandSharePieChart";
```

Adicionar chamada ao hook após `useMLKPISummary` (ou após `useMLOrders`):
```typescript
const {
  data: brandData,
  isLoading: brandLoading,
} = useMLOrdersByBrand(currentFrom, currentTo);
```

Na seção JSX da `TabsContent value="vendas"`, após o bloco existente:
```tsx
<div className="grid grid-cols-1 lg:grid-cols-6 gap-3">
  <MLCostCard costSummary={costSummary} />
  <MLTopProducts products={effectiveProducts} />
</div>
```

Adicionar a nova seção de gráficos de marca:
```tsx
{/* ── Gráficos de Marca — Phase 16 ── */}
<div className="space-y-3">
  <div className="grid grid-cols-1 lg:grid-cols-2 gap-3">
    <BrandRevenueChart
      data={brandData?.brandRevenueSeries ?? []}
      topBrands={brandData?.topBrands ?? []}
      loading={brandLoading}
    />
    <BrandMarkupChart
      data={brandData?.brandMarkupSeries ?? []}
      topBrands={brandData?.topBrands ?? []}
      loading={brandLoading}
    />
  </div>
  <CustoOperacionalChart
    custoSeries={brandData?.custoSeries ?? []}
    adsDaily={adsDaily}
    loading={brandLoading}
  />
  <BrandSharePieChart
    brandAggregates={brandData?.brandAggregates ?? []}
    loading={brandLoading}
  />
</div>
```

NOTA: `adsDaily` já está disponível em MercadoLivre.tsx:
```typescript
const { daily: adsDaily } = useMLAds({ dateFrom: adsChartFrom, dateTo: currentTo });
```
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
- 4 componentes de gráfico existem em src/components/mercadolivre/
- MercadoLivre.tsx importa useMLOrdersByBrand e os 4 componentes
- Gráficos aparecem em TabsContent "vendas" abaixo de MLCostCard
- Todos os componentes tratam o estado loading com skeleton e empty state com mensagem
- Zero erros TypeScript
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript limpo
cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -5

# Verificar todos os arquivos criados
ls /root/garment-glow-test/src/components/mercadolivre/Brand*.tsx
ls /root/garment-glow-test/src/components/mercadolivre/CustoOperacionalChart.tsx
ls /root/garment-glow-test/src/hooks/useMLOrdersByBrand.ts
```

Verificação visual (após deploy do Plano 16-02 e sincronização de orders com marca):
- Acessar `/` tab Vendas — gráficos aparecem abaixo dos cards existentes
- BrandRevenueChart: curvas coloridas por marca empilhadas
- BrandMarkupChart: linhas por marca (ou mensagem "Requer custo cadastrado")
- CustoOperacionalChart: linha única de custo total diário
- BrandSharePieChart: 2 donuts com fatias por marca, labels percentuais
</verification>

<success_criteria>
1. `src/hooks/useMLOrdersByBrand.ts` exporta hook e todos os tipos
2. Quatro componentes de gráfico criados em `src/components/mercadolivre/`
3. MercadoLivre.tsx renderiza os gráficos abaixo de MLCostCard na tab Vendas
4. Todos os gráficos têm empty state correto quando `marca` não está populada
5. Zero erros TypeScript
6. Recharts usado via ChartContainer (sem novas dependências)
7. Paleta de cores consistente com BRAND_COLORS do hook
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-kpis-marca/16-03-SUMMARY.md` com:
- Componentes criados
- Estrutura de dados do hook
- Como os ads são mesclados no CustoOperacionalChart
- Empty states implementados
</output>
