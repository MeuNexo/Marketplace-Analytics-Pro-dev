---
phase: 16-kpis-marca
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260521200000_orders_add_marca.sql
  - supabase/functions/sync-ml-orders/index.ts
autonomous: true
requirements:
  - KPI-03
  - KPI-04
  - KPI-05
  - KPI-06
  - KPI-07

must_haves:
  truths:
    - "Coluna marca TEXT NULLABLE existe em public.orders"
    - "sync-ml-orders v6 popula marca em novas sincronizações sem quebrar orders existentes"
    - "Função faz batch de no máximo 20 item_ids por chamada à ML API /items?ids=..."
    - "Falha ao buscar marca de um item não aborta o sync — marca fica null para esse item"
    - "Upsert de orders inclui campo marca no payload"
  artifacts:
    - path: "supabase/migrations/20260521200000_orders_add_marca.sql"
      provides: "Migration que adiciona coluna marca TEXT nullable"
      contains: "ALTER TABLE public.orders ADD COLUMN IF NOT EXISTS marca TEXT"
    - path: "supabase/functions/sync-ml-orders/index.ts"
      provides: "Edge function v6 que extrai marca dos itens via ML API"
      contains: "fetchItemBrands, /items?ids="
  key_links:
    - from: "supabase/functions/sync-ml-orders/index.ts"
      to: "ML API /items?ids=..."
      via: "fetchItemBrands() — batches de 20 IDs"
      pattern: "fetchItemBrands"
    - from: "supabase/functions/sync-ml-orders/index.ts"
      to: "public.orders"
      via: "upsert com campo marca incluído"
      pattern: "marca:"
---

<objective>
Adicionar infraestrutura de dados para os gráficos de marca: (1) migration que cria coluna `marca TEXT` nullable em `public.orders`, e (2) atualizar `sync-ml-orders` v5→v6 para extrair e persistir a marca de cada produto via ML API `/items?ids=...`.

Purpose: Habilitar os 5 gráficos de marca do Plano 16-03. Sem essa coluna populada, não há dado para os charts.
Output: Migration SQL + edge function v6 em produção.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kpis-marca/16-CONTEXT.md

<interfaces>
<!-- Estrutura atual de sync-ml-orders que o executor DEVE preservar -->

Arquivo: supabase/functions/sync-ml-orders/index.ts

Funções existentes (NÃO modificar assinaturas):
```typescript
// Helper HTTP autenticado — reutilizar para /items?ids=...
async function mlFetch(path: string, accessToken: string, timeoutMs = 15_000): Promise<any>

// Expande um order ML em rows de banco de dados
function expandOrder(
  order: any,
  mlUserId: string,
  sellerId: string | null,
  userId: string,
  organizationId: string | null,
  syncAt: string,
  shipmentMap: Map<number, ShipmentDetail>,
  costMap: Map<string, number>,
  taxConfig: any | null,
): Array<Record<string, unknown>>
// Retorna objetos com: ml_order_id, item_id, titulo, quantidade, receita_bruta, frete, comissao, etc.
```

Fluxo atual do handler principal:
1. Auth + body validation
2. Token lookup → accessToken, organizationId
3. fetchOrdersPage() → rawOrders (paginado)
4. Dedup por order.id
5. fetchShipmentDetails() → shipmentMap (frete + endereço)
6. Load taxConfig + costMap (ml_product_costs)
7. expandOrder() em cada order → records[]
8. supabaseAdmin.from("orders").upsert(records, { onConflict: "ml_order_id,ml_user_id,item_id,variation_id" })
9. Log em ml_sync_log

A nova função fetchItemBrands() deve ser chamada ENTRE os passos 6 e 7.

ML API para marcas:
- Endpoint: GET /items?ids=MLB123,MLB456,...  (até 20 IDs por request)
- Resposta: array de objetos item
- Campo marca: item.attributes[] onde { id: "BRAND", value_name: "Pralana" }
  Se não houver atributo BRAND ou item retornar erro: marca = null
- Exemplo de extração:
  const brandAttr = item.attributes?.find((a: any) => a.id === "BRAND");
  const marca = brandAttr?.value_name ?? null;

Ids dos items nos orders:
- order.order_items[].item.id → String (ex: "MLB4851068132")
- Já coletados em: const itemIds = Array.from(new Set(...))  (linha ~462 do arquivo atual)
```

Última migration:
```
20260521190000_orders_rls_and_unique_fix.sql
```
Próxima deve ser: `20260521200000_orders_add_marca.sql`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — ADD COLUMN marca em public.orders</name>
  <files>supabase/migrations/20260521200000_orders_add_marca.sql</files>
  <action>
Criar arquivo `supabase/migrations/20260521200000_orders_add_marca.sql` com conteúdo exato:

```sql
-- Phase 16: KPIs de Marca — adiciona coluna marca em public.orders
-- Nullable: não quebra orders existentes nem o upsert atual.
-- Populada pelo sync-ml-orders v6 via ML API /items?ids=...

ALTER TABLE public.orders
  ADD COLUMN IF NOT EXISTS marca TEXT;

COMMENT ON COLUMN public.orders.marca IS
  'Marca do produto extraída de ML API /items?ids=... (atributo BRAND). Populada a partir de sync-ml-orders v6.';
```

Depois aplicar a migration no Supabase remoto com:
```bash
cd /root/garment-glow-test && supabase db push --project-ref ckcdevcxgvueywivefgx
```

Se `supabase db push` não estiver disponível (CLI não instalado), aplicar via Supabase MCP tool `apply_migration` com o SQL acima.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && supabase db push --project-ref ckcdevcxgvueywivefgx --dry-run 2>&1 | grep -E "marca|error|success" || echo "check migration file exists"</automated>
  </verify>
  <done>
- Arquivo `supabase/migrations/20260521200000_orders_add_marca.sql` existe com o ADD COLUMN
- Migration aplicada no Supabase remoto (coluna `marca TEXT` visível na tabela orders)
  </done>
</task>

<task type="auto">
  <name>Task 2: sync-ml-orders v6 — extrair marca via ML API /items</name>
  <files>supabase/functions/sync-ml-orders/index.ts</files>
  <action>
Modificar `supabase/functions/sync-ml-orders/index.ts` para adicionar busca de marcas. Fazer 3 mudanças cirúrgicas:

**Mudança 1 — Adicionar função `fetchItemBrands` após a função `fetchShipmentDetails` (antes do comentário `// ── Expand one ML order object`)**:

```typescript
// ── Batch-fetch brand names from /items?ids=... ───────────────────────────────
// ML API allows up to 20 IDs per request.
// Returns Map<item_id, marca_name | null>

async function fetchItemBrands(
  itemIds: string[],
  accessToken: string,
): Promise<Map<string, string | null>> {
  const brandMap = new Map<string, string | null>();
  if (itemIds.length === 0) return brandMap;

  const BATCH_SIZE = 20;
  for (let i = 0; i < itemIds.length; i += BATCH_SIZE) {
    const batch = itemIds.slice(i, i + BATCH_SIZE);
    try {
      const items = await mlFetch(
        `/items?ids=${batch.join(",")}`,
        accessToken,
        15_000,
      );
      // Response is an array of { code, body } when using multi-get endpoint
      // Some ML API versions return plain array of items; handle both.
      const results: any[] = Array.isArray(items) ? items : [];
      for (const entry of results) {
        // Multi-get format: { code: 200, body: { id, attributes, ... } }
        // Single-item format: { id, attributes, ... }
        const item = entry.body ?? entry;
        if (!item?.id) continue;
        const itemId = String(item.id);
        const brandAttr = (item.attributes ?? []).find(
          (a: any) => a.id === "BRAND",
        );
        brandMap.set(itemId, brandAttr?.value_name ?? null);
      }
    } catch (err) {
      // Non-fatal: marca fica null para os IDs deste batch
      console.warn(`fetchItemBrands batch ${i}-${i + BATCH_SIZE} failed:`, err);
      for (const id of batch) {
        if (!brandMap.has(id)) brandMap.set(id, null);
      }
    }
  }

  return brandMap;
}
```

**Mudança 2 — Atualizar a assinatura de `expandOrder` para aceitar `brandMap`**:

Localizar a linha:
```typescript
function expandOrder(
  order:          any,
  mlUserId:       string,
  sellerId:       string | null,
  userId:         string,
  organizationId: string | null,
  syncAt:         string,
  shipmentMap:    Map<number, ShipmentDetail>,
  costMap:        Map<string, number>,
  taxConfig:      any | null,
): Array<Record<string, unknown>> {
```

Substituir por:
```typescript
function expandOrder(
  order:          any,
  mlUserId:       string,
  sellerId:       string | null,
  userId:         string,
  organizationId: string | null,
  syncAt:         string,
  shipmentMap:    Map<number, ShipmentDetail>,
  costMap:        Map<string, number>,
  taxConfig:      any | null,
  brandMap:       Map<string, string | null>,
): Array<Record<string, unknown>> {
```

Dentro de `expandOrder`, no objeto `return` do `.map((item: any) => { ... })`, adicionar o campo `marca` logo após `receita_liquida`:

Localizar:
```typescript
      receita_liquida: precoUnit != null
        ? precoUnit * quantidade
          - (item.sale_fee != null ? Number(item.sale_fee) : 0)
          - (frete ?? 0)
          - (taxAmount ?? 0)
        : null,
    };
  });
```

Substituir por:
```typescript
      receita_liquida: precoUnit != null
        ? precoUnit * quantidade
          - (item.sale_fee != null ? Number(item.sale_fee) : 0)
          - (frete ?? 0)
          - (taxAmount ?? 0)
        : null,
      marca: brandMap.get(itemId) ?? null,
    };
  });
```

**Mudança 3 — No handler principal, chamar `fetchItemBrands` e passar `brandMap` para `expandOrder`**:

Localizar o bloco (após o costMap estar populado, antes do expandOrder):
```typescript
    // ── Expand + upsert ───────────────────────────────────────────────────────
    const records = orders.flatMap((o) =>
      expandOrder(o, ml_user_id, effectiveSellerId, userId, organizationId, syncAt, shipmentMap, costMap, taxConfig),
    );
```

Substituir por:
```typescript
    // ── Fetch brand names for all unique item IDs ─────────────────────────────
    console.log(`Fetching brands for ${itemIds.length} unique items…`);
    const brandMap = await fetchItemBrands(itemIds, accessToken);
    console.log(`Brand map populated: ${brandMap.size} entries`);

    // ── Expand + upsert ───────────────────────────────────────────────────────
    const records = orders.flatMap((o) =>
      expandOrder(o, ml_user_id, effectiveSellerId, userId, organizationId, syncAt, shipmentMap, costMap, taxConfig, brandMap),
    );
```

NOTA: `itemIds` já está definido na linha ~462 do arquivo atual:
```typescript
    const itemIds = Array.from(new Set(
      orders.flatMap((o) => (o.order_items ?? []).map((i: any) => String(i.item?.id ?? ""))).filter(Boolean),
    ));
```

Após as 3 mudanças, fazer deploy da edge function:
```bash
cd /root/garment-glow-test && supabase functions deploy sync-ml-orders --project-ref ckcdevcxgvueywivefgx
```
  </action>
  <verify>
    <automated>grep -n "fetchItemBrands\|brandMap\|marca:" /root/garment-glow-test/supabase/functions/sync-ml-orders/index.ts | head -20</automated>
  </verify>
  <done>
- `fetchItemBrands` existe na função com batches de 20 IDs
- `expandOrder` aceita parâmetro `brandMap: Map<string, string | null>`
- Campo `marca: brandMap.get(itemId) ?? null` incluído no objeto de retorno
- Handler chama `fetchItemBrands(itemIds, accessToken)` antes de `expandOrder`
- Edge function deployed com sucesso (sem erros de compilação Deno)
  </done>
</task>

</tasks>

<verification>
```bash
# Verificar que a migration existe
ls /root/garment-glow-test/supabase/migrations/20260521200000_orders_add_marca.sql

# Verificar os 3 pontos de mudança na edge function
grep -c "fetchItemBrands\|brandMap\|/items?ids=" /root/garment-glow-test/supabase/functions/sync-ml-orders/index.ts
```

Verificação funcional (após próxima sync de orders):
- Trigger manual via UI ou chamar edge function diretamente:
  ```
  curl -X POST https://ckcdevcxgvueywivefgx.supabase.co/functions/v1/sync-ml-orders \
    -H "Authorization: Bearer SERVICE_ROLE_KEY" \
    -d '{"ml_user_id":"...", "date_from":"2026-05-20", "date_to":"2026-05-21"}'
  ```
- Verificar no Supabase: `SELECT item_id, titulo, marca FROM orders WHERE marca IS NOT NULL LIMIT 10`
</verification>

<success_criteria>
1. `supabase/migrations/20260521200000_orders_add_marca.sql` existe com `ADD COLUMN IF NOT EXISTS marca TEXT`
2. Migration aplicada — coluna `marca` existe em `public.orders` no Supabase remoto
3. `sync-ml-orders/index.ts` contém `fetchItemBrands` com loop de batches de 20
4. `expandOrder` inclui `marca: brandMap.get(itemId) ?? null` no payload
5. Edge function deployada sem erros
6. Orders sincronizados após deploy têm campo `marca` populado (ex: "Pralana", "Zebu", "TXC")
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-kpis-marca/16-02-SUMMARY.md` com:
- Status da migration (aplicada ou pendente)
- Mudanças feitas na edge function
- Como testar (curl ou link)
</output>
