---
phase: 92-anexos-nas-mensagens-de-reclama-o-exibi-o
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - src/lib/claimAttachments.ts
  - src/lib/claimAttachments.test.ts
  - src/hooks/useMLClaimMessages.ts
  - src/hooks/useClaimAttachment.ts
  - src/components/mercadolivre/ClaimAttachment.tsx
  - src/components/mercadolivre/ClaimDetailSheet.tsx
autonomous: true
requirements: [ATTACH-01, ATTACH-02]

must_haves:
  truths:
    - "Anexo de imagem em uma mensagem aparece como miniatura clicável no thread; clicar abre zoom em Dialog (ATTACH-01)"
    - "Anexo não-imagem aparece como botão Baixar; clicar baixa o arquivo (blob) no navegador (ATTACH-02)"
    - "O binário do anexo é buscado via EF ml-claim-attachment sob demanda (JWT), cacheado por attachment_id"
    - "Cada anexo tem seu próprio estado de loading/erro, sem quebrar o thread"
    - "A decisão imagem-vs-arquivo é uma lib pura testada (type image/* ou fallback por extensão)"
  artifacts:
    - path: "src/lib/claimAttachments.ts"
      provides: "normalizeClaimAttachments, isImageAttachment, tipo ClaimAttachmentMeta"
      exports: ["normalizeClaimAttachments", "isImageAttachment", "ClaimAttachmentMeta"]
      min_lines: 20
    - path: "src/lib/claimAttachments.test.ts"
      provides: "vitest: normalização (string/objeto/vazio) + decisão imagem-vs-arquivo"
    - path: "src/hooks/useClaimAttachment.ts"
      provides: "hook lazy React Query que invoca ml-claim-attachment e devolve data URI + tipo"
      exports: ["useClaimAttachment"]
    - path: "src/components/mercadolivre/ClaimAttachment.tsx"
      provides: "componente que renderiza imagem (thumb+zoom) ou botão Baixar por anexo"
      exports: ["ClaimAttachment"]
  key_links:
    - from: "src/components/mercadolivre/ClaimDetailSheet.tsx"
      to: "src/components/mercadolivre/ClaimAttachment.tsx"
      via: "render de ClaimAttachment por anexo abaixo do texto de cada mensagem"
      pattern: "ClaimAttachment"
    - from: "src/hooks/useClaimAttachment.ts"
      to: "supabase.functions.invoke('ml-claim-attachment')"
      via: "invoke com { claim_id, ml_user_id, attachment_id } (JWT automático)"
      pattern: "ml-claim-attachment"
---

<objective>
Camada de frontend da exibição de anexos: uma lib pura testável para tipar/normalizar attachments e decidir imagem-vs-arquivo, um hook lazy que busca o binário via a EF proxy (cache por attachment_id), um componente `ClaimAttachment` (imagem → miniatura+zoom / não-imagem → botão Baixar) e a fiação no `ClaimDetailSheet` abaixo do texto de cada mensagem.

Purpose: fechar o loop de exibição — cliente e vendedor veem as imagens/arquivos anexados sem sair do dashboard.
Output: `claimAttachments.ts` (+test), `useClaimAttachment.ts`, `ClaimAttachment.tsx`, `ClaimDetailSheet.tsx` fiado, tipo `MLClaimMessage.attachments` tipado.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-anexos-nas-mensagens-de-reclama-o-exibi-o/92-CONTEXT.md

@src/hooks/useMLClaimMessages.ts
@src/components/mercadolivre/ClaimDetailSheet.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lib pura claimAttachments (normalização + imagem-vs-arquivo) + tipo, com vitest</name>
  <files>src/lib/claimAttachments.ts, src/lib/claimAttachments.test.ts, src/hooks/useMLClaimMessages.ts</files>
  <read_first>
    - src/hooks/useMLClaimMessages.ts (interface MLClaimMessage com `attachments?: unknown[]` a ser tipado)
    - src/lib/claimStatus.ts (padrão de lib pura + named exports colocada com .test.ts)
    - .planning/phases/92-anexos-nas-mensagens-de-reclama-o-exibi-o/92-CONTEXT.md (shape { id, filename, type }; heurística image/* ou extensão)
  </read_first>
  <behavior>
    - normalizeClaimAttachments(undefined) === [] ; normalizeClaimAttachments([]) === []
    - normalizeClaimAttachments(["abc_1.jpeg"]) === [{ id: "abc_1.jpeg", filename: "abc_1.jpeg", type: null }] (tolera item string)
    - normalizeClaimAttachments([{ id: "x", filename: "nota.pdf", type: "application/pdf" }]) preserva os três campos
    - itens sem id resolvível são descartados
    - isImageAttachment({ type: "image/jpeg" }) === true ; isImageAttachment({ type: "application/pdf" }) === false
    - isImageAttachment({ type: null, filename: "foto.PNG" }) === true (fallback por extensão, case-insensitive: jpg/jpeg/png/gif/webp)
    - isImageAttachment({ type: null, filename: "nota.pdf" }) === false
  </behavior>
  <action>
    Criar `src/lib/claimAttachments.ts` com named exports: tipo `ClaimAttachmentMeta = { id: string; filename: string | null; type: string | null }`; função pura `normalizeClaimAttachments(raw: unknown): ClaimAttachmentMeta[]` (mesma tolerância string/objeto da EF — belt-and-suspenders defensivo sobre o retorno já normalizado do backend); função pura `isImageAttachment(a: { type?: string | null; filename?: string | null }): boolean` (true se `type` começa com `image/`; fallback por extensão do filename para jpg/jpeg/png/gif/webp, case-insensitive). Escrever PRIMEIRO `src/lib/claimAttachments.test.ts` cobrindo todos os casos do bloco <behavior> (RED), depois implementar (GREEN). Em `src/hooks/useMLClaimMessages.ts`, importar `ClaimAttachmentMeta` e trocar `attachments?: unknown[]` por `attachments?: ClaimAttachmentMeta[]` em `MLClaimMessage`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/claimAttachments.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - claimAttachments.ts exporta `normalizeClaimAttachments`, `isImageAttachment`, `ClaimAttachmentMeta`.
    - Todos os casos do <behavior> passam no vitest.
    - `MLClaimMessage.attachments` tipado como `ClaimAttachmentMeta[]`; tsc limpo.
  </acceptance_criteria>
  <done>Lib pura testada e tipo aplicado; vitest verde e tsc 0.</done>
</task>

<task type="auto">
  <name>Task 2: Hook useClaimAttachment (lazy) + componente ClaimAttachment (imagem/zoom + Baixar)</name>
  <files>src/hooks/useClaimAttachment.ts, src/components/mercadolivre/ClaimAttachment.tsx</files>
  <read_first>
    - src/hooks/useMLClaimMessages.ts (padrão useQuery + supabase.functions.invoke com JWT; queryKey; enabled)
    - src/lib/claimAttachments.ts (ClaimAttachmentMeta, isImageAttachment — do Task 1)
    - src/components/mercadolivre/ClaimDetailSheet.tsx (Dialog não usado ainda aqui; usar shadcn Dialog para zoom; tokens/estilo do thread)
  </read_first>
  <action>
    Criar `src/hooks/useClaimAttachment.ts`: hook `useClaimAttachment(claimId, mlUserId, attachmentId, enabled)` com React Query — `queryKey: ["ml-claim-attachment", attachmentId]` (cache por attachment_id, staleTime alto ex. 5min — binário imutável), `enabled` controla busca lazy, `queryFn` chama `supabase.functions.invoke("ml-claim-attachment", { body: { claim_id, ml_user_id, attachment_id } })`; monta e retorna a data URI `data:{content_type};base64,{data_base64}` + `content_type` + `filename`. Propaga erro para o componente exibir estado de erro.
    Criar `src/components/mercadolivre/ClaimAttachment.tsx` (named export `ClaimAttachment`), props `{ attachment: ClaimAttachmentMeta; claimId: string; mlUserId: string }`. Decidir via `isImageAttachment(attachment)`:
    - Imagem: buscar via hook (eager para imagem — `enabled` true ao montar, conforme preferência do CONTEXT: eager só p/ imagens); enquanto carrega, mostrar placeholder/spinner pequeno; ao ter data URI, mostrar miniatura clicável (`<img>` com tamanho contido, ex. max-h-40 rounded border); clique abre `Dialog` (shadcn `@/components/ui/dialog`) com a imagem em tamanho maior (zoom). Erro → ícone + texto curto "não foi possível carregar".
    - Não-imagem: NÃO buscar ao montar; botão "Baixar" (lucide `Download` + nome do arquivo). Ao clicar, disparar a busca (invoke), montar `Blob` a partir do base64, criar object URL e acionar download programático com `filename` (link temporário `a.download`), revogar o object URL depois. Estados loading/erro no botão.
    Usar named exports, Tailwind/tokens existentes, sem novas deps. Cite ATTACH-01 (imagem) e ATTACH-02 (download) em comentário.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'ml-claim-attachment' src/hooks/useClaimAttachment.ts && grep -q 'isImageAttachment' src/components/mercadolivre/ClaimAttachment.tsx && grep -q 'Dialog' src/components/mercadolivre/ClaimAttachment.tsx</automated>
  </verify>
  <acceptance_criteria>
    - useClaimAttachment invoca ml-claim-attachment com JWT, cacheia por attachment_id, devolve data URI.
    - ClaimAttachment: imagem → thumbnail + Dialog zoom; não-imagem → botão Baixar com download por blob.
    - Estados loading/erro por anexo; tsc 0; sem novas deps.
  </acceptance_criteria>
  <done>Hook e componente prontos; imagem com zoom e arquivo com download funcionando por anexo.</done>
</task>

<task type="auto">
  <name>Task 3: Fiar ClaimAttachment no thread do ClaimDetailSheet</name>
  <files>src/components/mercadolivre/ClaimDetailSheet.tsx</files>
  <read_first>
    - src/components/mercadolivre/ClaimDetailSheet.tsx (linha ~188 `detail!.messages.map`; o `<p>` do texto em ~198; claimId/mlUserId já disponíveis no componente)
    - src/components/mercadolivre/ClaimAttachment.tsx (props do Task 2)
  </read_first>
  <action>
    Em `ClaimDetailSheet.tsx`, dentro do `.map` das mensagens, ABAIXO do `<p>` que renderiza `htmlToText(m.message)`, se `m.attachments?.length`, renderizar um container (ex. `mt-2 space-y-1.5`) com um `ClaimAttachment` por anexo (`key={att.id}`), passando `attachment={att}`, `claimId={claimId!}`, `mlUserId={mlUserId!}` (ambos existem no escopo do componente e o thread só renderiza com claim selecionada). Import nomeado de `ClaimAttachment`. Não alterar a lógica de ações/resposta nem o layout existente das bolhas — apenas adicionar os anexos dentro da bolha da mensagem. Cite ATTACH-01/ATTACH-02.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'ClaimAttachment' src/components/mercadolivre/ClaimDetailSheet.tsx && npx vitest run src/lib/claimAttachments.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cada mensagem com anexos renderiza um ClaimAttachment por anexo abaixo do texto.
    - Layout/ações existentes intactos; tsc 0; vitest verde.
  </acceptance_criteria>
  <done>Anexos aparecem no thread por mensagem; build/tsc/vitest ok.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (frontend) → ml-claim-attachment EF | o front só passa identificadores; toda autorização é server-side (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-06 | Information Disclosure | data URI base64 em memória do cliente | accept | Anexo só é buscado após o gate anti-IDOR da EF (Plan 01) autorizar; conteúdo já é da org do usuário; sem persistência (só cache React Query em memória) |
| T-92-07 | Tampering | download por blob (filename) | mitigate | filename vem do backend/normalização; `a.download` não executa conteúdo; object URL revogado após uso |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova (CLAUDE.md: sem novas deps); usa shadcn Dialog e lucide já presentes |
</threat_model>

<verification>
- `npx vitest run src/lib/claimAttachments.test.ts` verde (normalização + imagem-vs-arquivo).
- `npx tsc --noEmit` limpo (tipo ClaimAttachmentMeta aplicado em MLClaimMessage).
- `ClaimAttachment` referenciado em `ClaimDetailSheet.tsx`; hook invoca `ml-claim-attachment`.
- Build ok (`npx vite build` ou script de build do projeto) — sem novas deps.
</verification>

<success_criteria>
- Imagem anexada aparece como miniatura clicável com zoom em Dialog (ATTACH-01).
- Arquivo não-imagem aparece como botão Baixar que baixa o blob (ATTACH-02).
- Busca lazy via EF proxy, cache por attachment_id, estados loading/erro por anexo.
- tsc 0, vitest verde, build ok.
</success_criteria>

<output>
Create `.planning/phases/92-anexos-nas-mensagens-de-reclama-o-exibi-o/92-02-SUMMARY.md` when done
</output>
