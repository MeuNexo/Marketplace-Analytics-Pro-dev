---
phase: 90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260690000000_ml_claims_seller_action.sql
  - supabase/functions/_shared/claimActions.ts
  - supabase/functions/_shared/claimActions.test.ts
  - supabase/functions/ml-webhook/index.ts
  - supabase/functions/sync-ml-claims/index.ts
autonomous: false
requirements: [TRIAGE-01, TRIAGE-02]
user_setup: []

must_haves:
  truths:
    - "ml_claims has columns seller_action_required, pending_action_type, action_due_date, available_actions, stage"
    - "A shared pure function derives seller_action_required/pending_action_type/action_due_date from the respondent player's available_actions per the locked 'Pende você' rule"
    - "ml-webhook writes the derived triage columns on every claim upsert (real time)"
    - "sync-ml-claims GETs the individual claim only for OPEN claims and enriches the same triage columns"
  artifacts:
    - path: "supabase/migrations/20260690000000_ml_claims_seller_action.sql"
      provides: "New ml_claims triage columns + partial index"
      contains: "seller_action_required"
    - path: "supabase/functions/_shared/claimActions.ts"
      provides: "deriveSellerAction pure helper"
      exports: ["deriveSellerAction"]
    - path: "supabase/functions/_shared/claimActions.test.ts"
      provides: "Unit tests proving the locked derivation rule"
    - path: "supabase/functions/ml-webhook/index.ts"
      provides: "claimRow writes derived triage columns"
      contains: "deriveSellerAction"
    - path: "supabase/functions/sync-ml-claims/index.ts"
      provides: "Individual GET for open claims + triage enrichment"
      contains: "deriveSellerAction"
  key_links:
    - from: "supabase/functions/ml-webhook/index.ts"
      to: "supabase/functions/_shared/claimActions.ts"
      via: "import { deriveSellerAction } from '../_shared/claimActions.ts'"
      pattern: "deriveSellerAction"
    - from: "supabase/functions/sync-ml-claims/index.ts"
      to: "supabase/functions/_shared/claimActions.ts"
      via: "import { deriveSellerAction } from '../_shared/claimActions.ts'"
      pattern: "deriveSellerAction"
---

<objective>
Block 1 (backend/data): make the "de quem é a vez" triage data exist and stay fresh.

Add derived triage columns to `ml_claims`, a single shared pure helper that computes them from the respondent player's `available_actions` following the LOCKED "Pende você" rule, and wire that helper into both write paths: `ml-webhook` (real time — the per-claim GET already carries `players`) and `sync-ml-claims` (cron — must GET each OPEN claim individually because the resumed search omits `available_actions`).

Purpose: today "aberta" ≠ "pende de você". This plan produces the field the whole triage UI (Plan 90-03) reads.
Output: migration file, `_shared/claimActions.ts` + tests, and the two EFs updated to populate the columns.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md
@.planning/phases/90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-/90-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — ml_claims triage columns + partial index</name>
  <read_first>
    - supabase/migrations/20260614100000_ml_questions_claims.sql (ml_claims table + existing indexes idx_ml_claims_scope/status/data)
    - supabase/migrations/20260689000200_ml_claims_motivo_texto.sql (most recent ADD COLUMN pattern on ml_claims)
  </read_first>
  <files>supabase/migrations/20260690000000_ml_claims_seller_action.sql</files>
  <action>
    Create the migration adding five nullable/defaulted columns to public.ml_claims with ADD COLUMN IF NOT EXISTS (idempotent, same style as the motivo_texto migration):
    - seller_action_required boolean NOT NULL DEFAULT false — respondent has at least one actionable item per the locked rule.
    - pending_action_type text NULL — one of 'reply' | 'return' | 'refund' | 'dispute' (no CHECK constraint; keep flexible, documented in a comment).
    - action_due_date timestamptz NULL — due_date of the action that set pending_action_type.
    - available_actions jsonb NULL — raw respondent available_actions list (for the sheet + audit).
    - stage text NULL — claim stage ('claim' | 'dispute' | …).
    Add a PARTIAL index: CREATE INDEX IF NOT EXISTS idx_ml_claims_seller_action ON public.ml_claims (organization_id, ml_user_id) WHERE seller_action_required. This backs the "Pende você" counters (bell + KPI) without scanning resolved claims.
    Do NOT alter RLS (the existing org_member_claims FOR ALL policy already covers the new columns). Do NOT backfill values — existing rows keep seller_action_required=false until the next webhook/sync GET re-derives them (documented in a header comment).
    This migration is applied to project ckcdevcxgvueywivefgx by the orchestrator via Supabase MCP apply_migration (see Task 4 checkpoint) — the executor only writes the .sql file.
  </action>
  <acceptance_criteria>
    - File supabase/migrations/20260690000000_ml_claims_seller_action.sql exists.
    - Grep (excluding comment lines) shows all five: seller_action_required, pending_action_type, action_due_date, available_actions, stage.
    - File contains the exact string `idx_ml_claims_seller_action` and the substring `WHERE seller_action_required`.
    - File contains `ADD COLUMN IF NOT EXISTS` for each column and `CREATE INDEX IF NOT EXISTS`.
    - File does NOT contain `DROP TABLE` or `DROP COLUMN`.
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260690000000_ml_claims_seller_action.sql | grep -q 'seller_action_required' && grep -v '^--' supabase/migrations/20260690000000_ml_claims_seller_action.sql | grep -q 'idx_ml_claims_seller_action' && grep -q 'WHERE seller_action_required' supabase/migrations/20260690000000_ml_claims_seller_action.sql && echo OK</automated>
  </verify>
  <done>Migration file present, all five columns + partial index declared idempotently, no destructive statements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared pure derivation helper + unit tests (claimActions.ts)</name>
  <read_first>
    - supabase/functions/reply-ml-claim/index.ts (how respondent is found: detail.players.find(p => p.role === 'respondent'), and how available_actions items map to {action})
    - supabase/functions/ml-claim-action/index.ts (ALLOWED_ACTIONS decision list: refund, allow_return, open_dispute)
    - supabase/functions/nexo-chat/tools.test.ts (vitest import style used for EF-dir tests; vitest.config include already covers supabase/functions/**)
    - docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md (section "Derivação (função compartilhada nas EFs)" — the LOCKED rule)
  </read_first>
  <behavior>
    - Input: a claim detail object's players array (each respondent action item has shape { action: string, mandatory?: boolean, due_date?: string }).
    - A mandatory send_message_to_* (mandatory=true) alone → seller_action_required=true, pending_action_type='reply', action_due_date = that action's due_date ?? null.
    - An OPTIONAL send_message_to_* (mandatory=false) alone → seller_action_required=false, pending_action_type=null, action_due_date=null (buyer/ML turn — "Aguardando").
    - allow_return present → contributes 'return'; refund or allow_partial_refund present → 'refund'; open_dispute present → 'dispute'; any of these decision actions → seller_action_required=true regardless of message mandatory flag.
    - Priority when several apply: 'reply' (mandatory message) > 'return' (allow_return) > 'refund' (refund/allow_partial_refund) > 'dispute' (open_dispute). action_due_date follows the chosen type's action.
    - No respondent / empty available_actions / null players → seller_action_required=false, pending_action_type=null, action_due_date=null, available_actions=[].
    - available_actions returned is the RAW respondent action objects (array), safe to store as jsonb.
  </behavior>
  <files>supabase/functions/_shared/claimActions.ts, supabase/functions/_shared/claimActions.test.ts</files>
  <action>
    Create supabase/functions/_shared/claimActions.ts as a PURE TypeScript module — NO Deno globals, NO https:// imports — so it is importable by both the Deno EFs (relative import) and vitest (node). Export:
    - a typed result interface (fields: seller_action_required boolean, pending_action_type 'reply'|'return'|'refund'|'dispute'|null, action_due_date string|null, available_actions unknown[]).
    - `deriveSellerAction(players: unknown): <that interface>` — finds the respondent player defensively (array guard, p?.role === 'respondent'), reads its available_actions (array guard), then applies the LOCKED rule above. Define the decision-action set and the mandatory-message predicate as named consts so the rule is auditable. Keep the raw respondent action objects for the returned available_actions.
    Create supabase/functions/_shared/claimActions.test.ts with vitest (import { describe, it, expect } from "vitest"; import { deriveSellerAction } from "./claimActions") covering every bullet in <behavior>: mandatory message → pende/reply(+due_date); optional message alone → aguardando; allow_return → return; refund → refund; allow_partial_refund → refund; open_dispute → dispute; combined priority (mandatory message + refund → reply wins); no respondent / empty / null → not required. Prove action_due_date is picked from the action that set the type.
    Do NOT import this file into the EFs yet — that is Task 3.
  </action>
  <acceptance_criteria>
    - supabase/functions/_shared/claimActions.ts exports `deriveSellerAction`.
    - claimActions.ts contains NO `https://` import and NO `Deno.` reference.
    - `npm test -- src/../supabase/functions/_shared/claimActions.test.ts` (i.e. the claimActions test file) passes with at least 8 test cases, all green.
    - A test asserts optional-message-alone yields seller_action_required=false AND pending_action_type=null.
    - A test asserts mandatory-message + refund yields pending_action_type='reply' (priority).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- claimActions 2>&1 | tail -5</automated>
  </verify>
  <done>Pure helper exported; vitest suite green covering all behavior bullets including the optional-vs-mandatory distinction and priority ordering.</done>
</task>

<task type="auto">
  <name>Task 3: Wire deriveSellerAction into ml-webhook and sync-ml-claims</name>
  <read_first>
    - supabase/functions/ml-webhook/index.ts (claimRow at line ~102; the claims branch at ~140 already does mlGet /v1/claims/{id} with /post-purchase fallback → the result `c` carries players)
    - supabase/functions/sync-ml-claims/index.ts (syncUser: search returns resumed items WITHOUT players; existing mlGet with retry + 120-200ms sleeps; open/closed upsert split; MAX_PAGES bound)
    - supabase/functions/_shared/claimActions.ts (the helper from Task 2)
    - docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md (section "Onde preencher")
  </read_first>
  <files>supabase/functions/ml-webhook/index.ts, supabase/functions/sync-ml-claims/index.ts</files>
  <action>
    ml-webhook: import deriveSellerAction from "../_shared/claimActions.ts". Extend claimRow(c, orgId, mlUserId) so it also returns seller_action_required, pending_action_type, action_due_date, available_actions (from deriveSellerAction(c.players)) and stage (c.stage ?? null). The claims branch already fetches the full claim `c` (with players) via the /v1 → /post-purchase fallback, so no extra request is needed. Preserve every existing field and the existing upsert onConflict target unchanged.
    sync-ml-claims: import deriveSellerAction from "../_shared/claimActions.ts". The resumed search does NOT return players, so after building allRows, for the OPEN claims ONLY (status !== 'closed'), GET the individual claim (reuse the existing dual-URL pattern: mlGet ML_API + "/v1/claims/" + claim_id, falling back to "/post-purchase/v1/claims/" + claim_id) to obtain players + stage, then set the same five columns via deriveSellerAction. Closed claims keep seller_action_required=false / null (no individual GET — matches design and keeps the EF inside its wall-clock budget). Reuse the existing inter-request sleep (~120-200ms) and the mlGet retry helper; do not remove the MAX_PAGES bound. Never log access_token (T-42-04). Closed-vs-open upsert split stays as-is.
    Frontend contract of reply-ml-claim / ml-claim-action is untouched by this task — do not edit those files.
  </action>
  <acceptance_criteria>
    - Both EFs contain `import { deriveSellerAction }` from "../_shared/claimActions.ts".
    - ml-webhook claimRow returns an object whose keys include seller_action_required, pending_action_type, action_due_date, available_actions, stage (grep the five keys in the file).
    - sync-ml-claims performs an individual claim GET guarded to open claims (grep shows deriveSellerAction AND a "/v1/claims/" individual GET added inside syncUser, not just the search).
    - `deno check supabase/functions/ml-webhook/index.ts` and `deno check supabase/functions/sync-ml-claims/index.ts` both pass.
    - Neither diff adds a `console.log`/`console.error` that prints access_token or refresh_token.
    - reply-ml-claim/index.ts and ml-claim-action/index.ts are NOT modified.
  </acceptance_criteria>
  <verify>
    <automated>deno check supabase/functions/ml-webhook/index.ts && deno check supabase/functions/sync-ml-claims/index.ts && grep -q deriveSellerAction supabase/functions/ml-webhook/index.ts && grep -q deriveSellerAction supabase/functions/sync-ml-claims/index.ts && echo OK</automated>
  </verify>
  <done>Both write paths import and use the shared helper; webhook derives from the GET it already does; sync GETs individually only for open claims; deno check clean; tokens never logged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>
    Migration 20260690000000_ml_claims_seller_action.sql and updated EFs ml-webhook + sync-ml-claims (both importing _shared/claimActions.ts).
  </what-built>
  <how-to-verify>
    These are OUTWARD steps the executor cannot perform (no Supabase token / MCP access). The orchestrator applies them to project ckcdevcxgvueywivefgx:
    1. Apply the migration via Supabase MCP apply_migration (name: ml_claims_seller_action, SQL = contents of the new migration file). Confirm the five columns and idx_ml_claims_seller_action exist (list_tables / a SELECT on information_schema).
    2. Deploy both edge functions via Supabase MCP deploy_edge_function: ml-webhook and sync-ml-claims (include the shared file — deploy the function bundle so ../_shared/claimActions.ts is present).
    3. SAFE post-deploy check only (no destructive ML action, no message send): after the next webhook/cron cycle, run a read-only SELECT counting rows where seller_action_required = true for org 7f615df7 and confirm open claims that require a seller turn are flagged, and that closed claims stay false. Do NOT trigger refund/allow_return/open_dispute or send any message to verify.
  </how-to-verify>
  <resume-signal>Type "aplicado" once the migration is applied and both EFs are deployed (or report the MCP error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ML API → EF | Claim detail (players/available_actions) is external input parsed by the derivation helper |
| EF → ml_claims (service role) | Writes bypass RLS; org scoping must be preserved on every row |
| ml_claims → dashboard (authenticated) | New columns readable only by org members via existing RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering | deriveSellerAction parsing malformed players | mitigate | Defensive array/role guards; unknown shapes → not required, empty available_actions (unit-tested) |
| T-90-02 | Information Disclosure | access_token logged during individual GET in sync/webhook | mitigate | Reuse existing pattern; no token in any console.* (acceptance-checked) |
| T-90-03 | Elevation of Privilege | cross-org row write via service role | accept | organization_id carried from the event/token row unchanged; existing onConflict scope preserved |
| T-90-04 | Denial of Service | per-open-claim GET blows the EF wall-clock | mitigate | GET only OPEN claims, keep 120-200ms sleeps + MAX_PAGES bound; closed claims skipped |
</threat_model>

<verification>
- `npm test -- claimActions` green (pure rule proven).
- `deno check` clean on both EFs.
- Migration grep gates pass.
- Orchestrator confirms migration applied + both EFs deployed to ckcdevcxgvueywivefgx (checkpoint).
</verification>

<success_criteria>
ml_claims exposes the five triage columns; the locked "Pende você" rule lives in one tested pure function used by both the real-time (webhook) and cron (sync) write paths; open claims that require a seller turn get flagged, closed claims stay false.
</success_criteria>

<output>
Create `.planning/phases/90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-/90-01-SUMMARY.md` when done
</output>
