---
phase: 90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260690000100_ml_claim_templates.sql
  - supabase/functions/ml-claim-detail/index.ts
autonomous: false
requirements: [TPL-01, TPL-02]
user_setup: []

must_haves:
  truths:
    - "A shared org-scoped table ml_claim_templates exists with org-first RLS (SELECT/INSERT/UPDATE/DELETE restricted to org members)"
    - "ml-claim-detail returns buyer_first_name (capitalized) for the claim's order, with a null fallback"
  artifacts:
    - path: "supabase/migrations/20260690000100_ml_claim_templates.sql"
      provides: "ml_claim_templates table + org-first RLS policies"
      contains: "is_org_member"
    - path: "supabase/functions/ml-claim-detail/index.ts"
      provides: "buyer_first_name in the JSON response"
      contains: "buyer_first_name"
  key_links:
    - from: "supabase/migrations/20260690000100_ml_claim_templates.sql"
      to: "public.is_org_member"
      via: "RLS USING (is_org_member(auth.uid(), organization_id))"
      pattern: "is_org_member\\(auth\\.uid\\(\\), organization_id\\)"
    - from: "supabase/functions/ml-claim-detail/index.ts"
      to: "ML orders API"
      via: "GET /orders/{order_id} → buyer.first_name"
      pattern: "buyer_first_name"
---

<objective>
Block 2 (backend/data): the storage + data that "Mensagens rápidas" needs.

Create the shared `ml_claim_templates` table (org-first RLS — CRUD happens directly through the Supabase client, no new EF), and enrich `ml-claim-detail` so it returns `buyer_first_name` (real buyer name from the order) used to fill the `{{nome}}` template variable, with a safe fallback.

Purpose: templates are shared across the store's users and the `{{nome}}` variable must resolve to the real buyer.
Output: one migration + one EF response field. No new dependency, no new EF.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md
@.planning/phases/90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-/90-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — ml_claim_templates table + org-first RLS</name>
  <read_first>
    - supabase/migrations/20260665000000_purchase_orders.sql (org-first table + RLS: ENABLE ROW LEVEL SECURITY, is_org_member(auth.uid(), organization_id))
    - supabase/migrations/20260614100000_ml_questions_claims.sql (org_member_claims FOR ALL policy shape; organization_id REFERENCES organizations(id))
    - supabase/migrations/20260515120000_ml_tax_config.sql (created_at/updated_at + gen_random_uuid() pk style)
  </read_first>
  <files>supabase/migrations/20260690000100_ml_claim_templates.sql</files>
  <action>
    Create public.ml_claim_templates with columns: id uuid PK DEFAULT gen_random_uuid(); organization_id uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE; title text NOT NULL; body text NOT NULL; created_by uuid NULL (references the auth user who created it — store auth.uid(); no FK needed); created_at timestamptz NOT NULL DEFAULT now(); updated_at timestamptz NOT NULL DEFAULT now().
    ENABLE ROW LEVEL SECURITY. Because CRUD is done directly by authenticated users (not a service-role EF), create org-first policies for ALL four verbs, each gated by is_org_member(auth.uid(), organization_id):
    - SELECT policy USING is_org_member(...).
    - INSERT policy WITH CHECK is_org_member(...).
    - UPDATE policy USING is_org_member(...) WITH CHECK is_org_member(...).
    - DELETE policy USING is_org_member(...).
    (A single FOR ALL policy with USING + WITH CHECK both = is_org_member is acceptable and mirrors org_member_claims; prefer that unless splitting reads clearer.) Add an index on (organization_id). Optionally add an updated_at BEFORE UPDATE trigger (reuse a set-updated-at helper if one already exists in an earlier migration; otherwise a small trigger fn is fine) — not mandatory for correctness.
    Applied to ckcdevcxgvueywivefgx by the orchestrator via Supabase MCP apply_migration (Task 3 checkpoint) — executor only writes the file.
  </action>
  <acceptance_criteria>
    - File supabase/migrations/20260690000100_ml_claim_templates.sql exists.
    - Contains `CREATE TABLE public.ml_claim_templates` with columns id, organization_id, title, body, created_by, created_at, updated_at.
    - Contains `ENABLE ROW LEVEL SECURITY`.
    - RLS grants exist for read AND write: grep shows is_org_member used for SELECT and for INSERT/UPDATE/DELETE (either a FOR ALL policy with USING + WITH CHECK, or four verb policies).
    - organization_id is `NOT NULL REFERENCES public.organizations(id)`.
    - File does NOT contain `DROP TABLE public.ml_claim_templates` before the CREATE (idempotent guard via IF NOT EXISTS is fine; no destructive drop of existing data).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'CREATE TABLE public.ml_claim_templates' supabase/migrations/20260690000100_ml_claim_templates.sql && grep -q 'ENABLE ROW LEVEL SECURITY' supabase/migrations/20260690000100_ml_claim_templates.sql && grep -c 'is_org_member' supabase/migrations/20260690000100_ml_claim_templates.sql && grep -q 'WITH CHECK' supabase/migrations/20260690000100_ml_claim_templates.sql && echo OK</automated>
  </verify>
  <done>Table created with org-first RLS covering read and write; anti-IDOR by is_org_member on every verb.</done>
</task>

<task type="auto">
  <name>Task 2: ml-claim-detail returns buyer_first_name</name>
  <read_first>
    - supabase/functions/ml-claim-detail/index.ts (existing gates JWT → token by ml_user_id → org membership; Promise.all detail+messages; final jsonResponse shape; mlGetJson helper)
    - supabase/functions/ml-webhook/index.ts (how order GET is used elsewhere; ML_API base)
    - docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md (section "Nome do comprador" — buyer.first_name, capitalized, fallback "cliente" handled on the frontend)
  </read_first>
  <files>supabase/functions/ml-claim-detail/index.ts</files>
  <action>
    After the org-membership gate (all existing security gates stay exactly as they are — JWT, token by ml_user_id, org membership anti-IDOR, access_token never logged), and using the same authenticated access_token `at`, fetch the buyer name for the claim's order. Derive the order_id from the claim detail (detail.resource_id) — the claim GET already runs. GET `${ML_API}/orders/${order_id}` via the existing mlGetJson helper, read order.buyer.first_name, and capitalize it (first letter upper, rest lower — the ML value comes uppercased, e.g. "ISADELLE" → "Isadelle"). Add `buyer_first_name: <capitalized> | null` to the returned JSON. When order_id is missing or the order GET returns nothing / no buyer.first_name, return buyer_first_name: null (the frontend maps null → "cliente"). Do this without adding a second claim GET — reuse the detail you already have; only the order GET is new, and only when an order_id exists. Keep the response otherwise byte-for-byte compatible (all current keys unchanged).
  </action>
  <acceptance_criteria>
    - ml-claim-detail/index.ts returns a `buyer_first_name` key in the final jsonResponse (grep).
    - The order fetch uses `${ML_API}/orders/` and reads `buyer.first_name`.
    - Capitalization is applied (grep shows a toUpperCase/toLowerCase or a capitalize helper touching the name).
    - All pre-existing security gates remain: grep still shows `supabase.auth.getUser`, `is_org_member`, and the `ml_tokens` token lookup by ml_user_id — none removed.
    - No `console.log`/`console.error` prints access_token.
    - `deno check supabase/functions/ml-claim-detail/index.ts` passes.
    - The existing keys messages, reason, available_actions, stage, type, status are still present in the response object.
  </acceptance_criteria>
  <verify>
    <automated>deno check supabase/functions/ml-claim-detail/index.ts && grep -q buyer_first_name supabase/functions/ml-claim-detail/index.ts && grep -q 'is_org_member' supabase/functions/ml-claim-detail/index.ts && grep -q 'available_actions' supabase/functions/ml-claim-detail/index.ts && echo OK</automated>
  </verify>
  <done>ml-claim-detail adds capitalized buyer_first_name (null fallback), one extra order GET only, all security gates and existing response keys intact.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>
    Migration 20260690000100_ml_claim_templates.sql and updated EF ml-claim-detail (now returns buyer_first_name).
  </what-built>
  <how-to-verify>
    Outward steps for the orchestrator on project ckcdevcxgvueywivefgx:
    1. Apply the migration via Supabase MCP apply_migration (name: ml_claim_templates). Confirm the table exists and that RLS is enabled with org-first policies (an anonymous/foreign-org SELECT returns 0 rows).
    2. Deploy ml-claim-detail via Supabase MCP deploy_edge_function.
    3. SAFE read-only check: open one existing claim in /devolucoes (org 7f615df7) and confirm the ml-claim-detail response now includes buyer_first_name (capitalized real name when the order has a buyer, else null). No message is sent and no template is created in this check.
  </how-to-verify>
  <resume-signal>Type "aplicado" once the migration is applied and ml-claim-detail is deployed (or report the MCP error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ml_claim_templates (authenticated) | Direct CRUD by authenticated users — RLS is the only guard |
| ML orders API → EF | buyer.first_name is external PII returned only inside an already-authorized detail call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-05 | Elevation of Privilege | reading/writing another org's templates (IDOR) | mitigate | org-first RLS on all four verbs via is_org_member(auth.uid(), organization_id); WITH CHECK on writes |
| T-90-06 | Information Disclosure | buyer name leaked to a non-member | mitigate | buyer_first_name only returned by ml-claim-detail after JWT + is_org_member gate; unchanged gates asserted in acceptance |
| T-90-07 | Information Disclosure | access_token logged during order GET | mitigate | reuse mlGetJson; no token in any console.* (acceptance-checked) |
| T-90-08 | Tampering | forged organization_id on INSERT | mitigate | INSERT WITH CHECK is_org_member; created_by = auth.uid() server-side, not client-trusted for authz |
</threat_model>

<verification>
- Migration grep gates pass (table + org-first RLS read & write).
- `deno check supabase/functions/ml-claim-detail/index.ts` clean; buyer_first_name present; all gates intact.
- Orchestrator confirms migration applied + ml-claim-detail deployed + foreign-org SELECT returns 0 rows (checkpoint).
</verification>

<success_criteria>
Shared org-scoped templates table exists with anti-IDOR RLS on read and write; ml-claim-detail hands the frontend a real, capitalized buyer_first_name (null → "cliente") without weakening any Phase 89 security gate.
</success_criteria>

<output>
Create `.planning/phases/90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-/90-02-SUMMARY.md` when done
</output>
