---
phase: 90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-
plan: 03
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - src/hooks/useMLClaims.ts
  - src/lib/claimStatus.ts
  - src/lib/claimStatus.test.ts
  - src/pages/mercadolivre/MLDevolucoes.tsx
  - src/hooks/useAtendimentoPendencias.ts
autonomous: true
requirements: [TRIAGE-03, TRIAGE-04]
user_setup: []

must_haves:
  truths:
    - "The claims row type exposes seller_action_required, pending_action_type, action_due_date, stage"
    - "/devolucoes shows three buckets (🔴 Pende você default / 🟡 Aguardando / ✅ Resolvida) using the locked criterion"
    - "Each pending row shows a pendency-type badge (Responder/Decidir devolução/Decidir reembolso/Falar com o ML) and a due-date label"
    - "The KPI reads 'Pendem você' and the navbar bell counts by seller_action_required"
  artifacts:
    - path: "src/lib/claimStatus.ts"
      provides: "bucket + pendency-badge + due-date helpers"
      exports: ["claimBucket", "pendingActionLabel", "dueDateLabel"]
    - path: "src/hooks/useMLClaims.ts"
      provides: "MLClaimRow with new triage columns"
      contains: "seller_action_required"
    - path: "src/pages/mercadolivre/MLDevolucoes.tsx"
      provides: "3-bucket tabs + badges + Pendem você KPI"
      contains: "Pende você"
    - path: "src/hooks/useAtendimentoPendencias.ts"
      provides: "bell counts by seller_action_required"
      contains: "seller_action_required"
  key_links:
    - from: "src/pages/mercadolivre/MLDevolucoes.tsx"
      to: "src/lib/claimStatus.ts"
      via: "claimBucket / pendingActionLabel / dueDateLabel"
      pattern: "claimBucket"
    - from: "src/hooks/useAtendimentoPendencias.ts"
      to: "ml_claims.seller_action_required"
      via: "supabase select + filter on seller_action_required"
      pattern: "seller_action_required"
---

<objective>
Block 1 (frontend): turn the triage columns from Plan 90-01 into the "de quem é a vez" experience on /devolucoes.

Replace the open/closed status filter with three buckets (🔴 Pende você default / 🟡 Aguardando / ✅ Resolvida), add a pendency-type badge + due-date label per pending row, rename the KPI to "Pendem você", and switch the navbar bell to the same `seller_action_required` criterion. The Tipo (reclamações/devoluções) filter stays.

Purpose: the lojista sees exactly what is on them, prioritized by deadline.
Output: updated row type, status helpers (+tests), the page, and the bell hook.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md
@.planning/phases/90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-/90-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MLClaimRow + claimStatus helpers (buckets, badge, due date) + tests</name>
  <read_first>
    - src/hooks/useMLClaims.ts (MLClaimRow interface; select("*") already returns new columns once the migration ran)
    - src/lib/claimStatus.ts (claimStatusConfig, isClaimOpen, claimTipoLabel — existing helpers to extend, not break)
    - src/lib/mcoHealth.test.ts (a representative src/lib test for import + describe/it style)
    - docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md (section "UI (/devolucoes)" — bucket definitions, badge labels, prazo wording)
  </read_first>
  <behavior>
    - claimBucket(row): returns 'pende' when seller_action_required === true AND the claim is open (isClaimOpen(status)); 'aguardando' when open AND not seller_action_required; 'resolvida' when the status is not open. Missing/unknown status that is not open → 'resolvida'.
    - pendingActionLabel(pending_action_type): 'reply'→"Responder", 'return'→"Decidir devolução", 'refund'→"Decidir reembolso", 'dispute'→"Falar com o ML"; null/unknown → null.
    - dueDateLabel(action_due_date, now): null due date → null; due date today (same calendar day) → "vence hoje"; past → "atrasada"; future → "vence em N dias" (N = ceil days). Deterministic given an injected `now` for testing.
  </behavior>
  <files>src/hooks/useMLClaims.ts, src/lib/claimStatus.ts, src/lib/claimStatus.test.ts</files>
  <action>
    useMLClaims.ts: add to the MLClaimRow interface: seller_action_required: boolean | null; pending_action_type: string | null; action_due_date: string | null; stage: string | null. The query already selects "*", so no query change needed — only the type. (available_actions jsonb is not needed by this page; do not add it here.)
    claimStatus.ts: add three exported pure helpers — claimBucket, pendingActionLabel, dueDateLabel — per <behavior>. Give each a small tone/emoji-free return where practical; the page owns emoji/color. Keep the existing exports (claimStatusConfig, isClaimOpen, claimTipoLabel) intact. dueDateLabel must accept an optional `now: Date` param (default new Date()) so it is testable deterministically. Type the bucket as a union 'pende' | 'aguardando' | 'resolvida'.
    claimStatus.test.ts: vitest suite covering claimBucket (pende / aguardando / resolvida incl. unknown status), pendingActionLabel (all four + null), dueDateLabel (null, today→"vence hoje", past→"atrasada", future→"vence em N dias" with an injected now).
  </action>
  <acceptance_criteria>
    - MLClaimRow includes seller_action_required, pending_action_type, action_due_date, stage.
    - claimStatus.ts exports claimBucket, pendingActionLabel, dueDateLabel AND still exports claimStatusConfig, isClaimOpen, claimTipoLabel.
    - `npm test -- claimStatus` passes with cases for all three buckets, all four badge labels, and the three due-date branches (today/past/future) using an injected now.
    - A test asserts claimBucket returns 'pende' only when seller_action_required && open.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- claimStatus 2>&1 | tail -5</automated>
  </verify>
  <done>Row type carries triage fields; three tested pure helpers added without breaking existing status helpers.</done>
</task>

<task type="auto">
  <name>Task 2: MLDevolucoes — 3 buckets, per-row pendency badge + due date, "Pendem você" KPI</name>
  <read_first>
    - src/pages/mercadolivre/MLDevolucoes.tsx (current tipoFilter/statusFilter, summary useMemo, KPI grid, table rows, action cell)
    - src/lib/claimStatus.ts (claimBucket, pendingActionLabel, dueDateLabel from Task 1)
    - src/components/ui/tabs — Radix Tabs available; or reuse the Select pattern already in the file for segments (choose Tabs for the 3 buckets for clarity)
    - docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md (section "UI (/devolucoes)")
  </read_first>
  <files>src/pages/mercadolivre/MLDevolucoes.tsx</files>
  <action>
    Replace the status Select with three buckets (Tabs or segmented control): 🔴 Pende você (N) · 🟡 Aguardando · ✅ Resolvida. Default selection = "pende". Compute each row's bucket with claimBucket and filter the list by the active bucket (Tipo filter stays and composes with it). Show the live count N of pending claims in the "Pende você" tab label.
    In the "Pende você" bucket, order rows by action_due_date ascending with NULLs last (shortest deadline first). For each pending row, render a pendency-type badge from pendingActionLabel(pending_action_type) and, when action_due_date is present, a due-date label from dueDateLabel (color it: atrasada → destructive, vence hoje → amber/orange, future → muted). Non-pending buckets keep the existing status badge.
    KPI: rename the first KPI from "Abertas" to "Pendem você" and set its value to the count of claims with claimBucket === 'pende'. Keep the other KPIs (taxa de resolução, resolvidas, total) working — recompute "resolvidas" as bucket 'resolvida'. Keep the ClaimDetailSheet wiring and row-click behavior unchanged. Do not alter reply-ml-claim / ml-claim-action contracts.
    Keep it mobile-safe (the file already uses overflow-x-auto tables and compact KPIs).
  </action>
  <acceptance_criteria>
    - MLDevolucoes.tsx contains the three bucket labels "Pende você", "Aguardando", "Resolvida" and uses claimBucket to filter.
    - Default active bucket is the pending one (grep shows a useState initialized to the pende bucket value).
    - The first KPI title string is "Pendem você" (not "Abertas").
    - Rows in the pending bucket render pendingActionLabel output and dueDateLabel output (grep shows both helpers imported and used).
    - Pending rows are sorted by action_due_date ascending with nulls last (grep shows a sort referencing action_due_date).
    - `npx tsc --noEmit` passes and `npm run build` succeeds.
    - reply-ml-claim / ml-claim-action invoke bodies are unchanged (no edits to those calls).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'Pendem você' src/pages/mercadolivre/MLDevolucoes.tsx && grep -q claimBucket src/pages/mercadolivre/MLDevolucoes.tsx && grep -q pendingActionLabel src/pages/mercadolivre/MLDevolucoes.tsx && grep -q action_due_date src/pages/mercadolivre/MLDevolucoes.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Three buckets with pending default, per-row pendency badge + due-date label, deadline-first ordering, and a "Pendem você" KPI — tsc + build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Navbar bell counts pendências by seller_action_required</name>
  <read_first>
    - src/hooks/useAtendimentoPendencias.ts (current CLAIM_ABERTA status filter + claims select; questions branch unchanged)
    - src/components/layout/AtendimentoBell.tsx (consumes { items, count }; no change needed unless the item shape changes)
    - docs/superpowers/specs/2026-07-07-atendimento-reclamacoes-design.md (section "UI (/devolucoes)" — "O sininho da navbar ... passa a contar por seller_action_required")
  </read_first>
  <files>src/hooks/useAtendimentoPendencias.ts</files>
  <action>
    Change the claims side of the pendências query so a claim counts as pending only when it truly pends the seller. Select seller_action_required (and pending_action_type for the row label) and filter claims with .eq("seller_action_required", true) instead of the current .in("status", CLAIM_ABERTA) open/under_review filter. Keep the org + multi-loja scoping (.eq organization_id, .in ml_user_id) and the questions branch exactly as they are. Update the claim item titulo to reflect the pendency type when available (e.g. use pendingActionLabel from claimStatus for a clearer label such as "Responder reclamação"), falling back to the current "Reclamação aberta" wording. Keep href "/devolucoes" and the 45s refetch. The bell component itself (AtendimentoBell.tsx) needs no change because it only reads count/items — do not edit it unless the PendenciaItem shape must change.
  </action>
  <acceptance_criteria>
    - useAtendimentoPendencias.ts filters claims by `seller_action_required` = true (grep shows `.eq("seller_action_required", true)`), and no longer relies on `.in("status", CLAIM_ABERTA)` for the claims branch.
    - The questions branch (UNANSWERED) is unchanged.
    - org + multi-loja scoping (organization_id + ml_user_id) preserved on the claims query.
    - `npx tsc --noEmit` passes.
    - AtendimentoBell.tsx is either unchanged or still compiles and renders count correctly.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'seller_action_required' src/hooks/useAtendimentoPendencias.ts && echo OK</automated>
  </verify>
  <done>Bell counts by seller_action_required (same criterion as the "Pende você" bucket), scoping and questions branch intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ml_claims → client (authenticated) | Triage columns read via existing org-first RLS; no new server surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-09 | Information Disclosure | cross-org claims shown in buckets/bell | accept | reads go through existing org_member_claims RLS + org/ml_user scoping in the hooks; no client-trusted org id |
| T-90-10 | Tampering | client filtering hides a real pendency | mitigate | bucket = pure claimBucket over server columns; deadline sort and counts derived from the same seller_action_required field the bell uses (consistency asserted by shared helper) |
</threat_model>

<verification>
- `npm test -- claimStatus` green (bucket/badge/due-date rules).
- `npx tsc --noEmit` + `npm run build` clean.
- Grep gates confirm buckets, "Pendem você" KPI, deadline sort, and bell criterion.
</verification>

<success_criteria>
On /devolucoes the "Pende você" bucket (default) shows exactly the claims where the seller must act, each with a pendency-type badge and a due-date label, sorted shortest-deadline-first; the KPI reads "Pendem você" and the navbar bell counts by the same seller_action_required criterion — counters and bell agree.
</success_criteria>

<output>
Create `.planning/phases/90-atendimento-de-reclama-es-triagem-de-pend-ncias-mensagens-r-/90-03-SUMMARY.md` when done
</output>