---
phase: 42-zero-mock
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - supabase/functions/sync-ml-questions/index.ts
  - supabase/functions/sync-ml-claims/index.ts
  - supabase/functions/reply-ml-question/index.ts
  - supabase/functions/ml-reputation/index.ts
  - supabase/migrations/20260614110000_pg_cron_questions_claims.sql
autonomous: false
requirements: [MOCK-01, MOCK-02, MOCK-03, MOCK-04]
user_setup: []

must_haves:
  truths:
    - "sync-ml-questions and sync-ml-claims fetch ML data for every ml_tokens row and upsert into the new tables"
    - "reply-ml-question validates org membership + ≤2000 chars before POSTing to ML /answers"
    - "ml-reputation now returns a feedbacks[] array (empty allowed) for the daily chart"
    - "pg_cron schedules invoke the sync EFs with Bearer service_role_key (Pattern B) — every 15min questions, every 30min claims"
  artifacts:
    - path: "supabase/functions/sync-ml-questions/index.ts"
      provides: "Cron-invoked questions sync EF"
      contains: "ml_questions"
    - path: "supabase/functions/sync-ml-claims/index.ts"
      provides: "Cron-invoked claims sync EF with dual-URL fallback"
      contains: "ml_claims"
    - path: "supabase/functions/reply-ml-question/index.ts"
      provides: "User-invoked reply EF, org-membership gated"
      contains: "is_org_member"
    - path: "supabase/functions/ml-reputation/index.ts"
      provides: "Expanded reputation EF returning feedbacks[]"
      contains: "feedbacks"
    - path: "supabase/migrations/20260614110000_pg_cron_questions_claims.sql"
      provides: "pg_cron schedules (Pattern B vault auth)"
      contains: "vault.decrypted_secrets"
  key_links:
    - from: "supabase/migrations/20260614110000_pg_cron_questions_claims.sql"
      to: "functions/v1/sync-ml-questions"
      via: "net.http_post with Bearer service_role_key from vault"
      pattern: "net.http_post"
    - from: "supabase/functions/sync-ml-questions/index.ts"
      to: "ml_questions"
      via: "upsert onConflict organization_id,ml_user_id,question_id"
      pattern: "upsert"
    - from: "supabase/functions/reply-ml-question/index.ts"
      to: "api.mercadolibre.com/answers"
      via: "POST after org-membership check"
      pattern: "/answers"
---

<objective>
Build the four Edge Functions that move real ML data into the system and the pg_cron schedules that drive periodic sync. Port logic already validated in production by the Nexo MCP (`/root/nexo-mcp/ml_client.py`): question/claim/feedback fetch, answer POST, status parsing, dual-URL claims fallback, rate limits. Reuse the in-project sync-ads structure (auth guard, token refresh, retrying ML GET, multi-seller loop) and the ml-reputation user-JWT pattern for the reply EF. Covers MOCK-01, MOCK-02, MOCK-03, MOCK-04.

Purpose: the data-producing tier — without these EFs and the cron, the plan-01 tables stay empty and the pages in plans 03/04 have nothing real to show.
Output: 3 new EFs + 1 expanded EF deployed; pg_cron schedules applied and smoke-verified (D-01, D-02).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/42-zero-mock/42-CONTEXT.md
@.planning/phases/42-zero-mock/42-RESEARCH.md
@.planning/phases/42-zero-mock/42-PATTERNS.md
@.planning/phases/42-zero-mock/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sync-ml-questions and sync-ml-claims EFs</name>
  <files>supabase/functions/sync-ml-questions/index.ts, supabase/functions/sync-ml-claims/index.ts</files>
  <read_first>
    - supabase/functions/sync-ads/index.ts (EXACT structural analog — CORS, json(), requireServiceRole guard, getAccessToken token refresh, mlGet retry, main serve() loop over ml_tokens, upsert)
    - 42-RESEARCH.md "ML API Reference" (Questions /questions/search; Claims /v1/claims/search + /post-purchase/v1/claims/search fallback; status parsing; rate limit 0.2s)
    - 42-PATTERNS.md "sync-ml-questions + sync-ml-claims" section + "Shared Patterns → Auth Guard (cron-invoked)"
    - /root/nexo-mcp/ml_client.py:945 (fetch_questions), :1002 (fetch_claims) — canonical endpoint/param/status spec
  </read_first>
  <action>
    Create both EFs following sync-ads/index.ts verbatim for shared scaffolding: CORS map, json() helper, requireServiceRole(req) guard (reject unless authorization === "Bearer " + SERVICE_KEY → 401), getAccessToken(sb, mlUserId) with expires_at refresh via POST /oauth/token, mlGet(url, token) with 429/5xx retry.
    Main serve(): OPTIONS handling, requireServiceRole gate, createClient(SUPABASE_URL, SERVICE_KEY), select ml_user_id,user_id,organization_id,seller_id from ml_tokens WHERE refresh_token is not null, loop per token calling syncUser, collect results, return json({ok:true, results}).
    sync-ml-questions syncUser: GET https://api.mercadolibre.com/questions/search?seller_id={ml_user_id}&status={s}&limit=50&offset={n}. Per D-03 backfill fetch status=unanswered first then status=answered (lowercase in request per RESEARCH.md open-question 3). Paginate (offset += items.length; stop when items empty or offset>=paging.total) with a 200ms sleep between pages. Normalize each question status to UPPERCASE when saving. Upsert into ml_questions: question_id=q.id (bigint), item_id, texto=q.text, status (UPPERCASE), comprador_id=String(q.from?.id ?? ""), data_pergunta=q.date_created, resposta=q.answer?.text ?? null, data_resposta=q.answer?.date_created ?? null, organization_id+ml_user_id from the token row, synced_at=now; onConflict "organization_id,ml_user_id,question_id".
    sync-ml-claims syncUser: try GET /v1/claims/search?role=respondent&status={opened|closed}&limit=50&offset={n}; if non-200 fall back to /post-purchase/v1/claims/search (Pitfall 5). Items under data ?? claims ?? results. Map claim_id=String(id), order_id=resource_id, tipo=type (mediations|returns — the D-09 discriminator), status, motivo=reason_id, data_abertura=date_created (date), data_limite=resolution_due_date (date), solucao=resolution?.type. Per D-03 fetch last 90 days (filter by date_created client-side if API lacks a date param). Upsert onConflict "organization_id,ml_user_id,claim_id". Same 200ms page sleep.
    Do NOT log access_token or refresh_token (T-42-04). Per-ml_user_id errors are caught and pushed into results without aborting the run.
  </action>
  <verify>
    <automated>test -f supabase/functions/sync-ml-questions/index.ts; test -f supabase/functions/sync-ml-claims/index.ts; grep -q "questions/search" supabase/functions/sync-ml-questions/index.ts; grep -q "onConflict" supabase/functions/sync-ml-questions/index.ts; grep -q "post-purchase/v1/claims/search" supabase/functions/sync-ml-claims/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both EFs reject requests without Bearer SERVICE_KEY (401) — requireServiceRole present.
    - sync-ml-questions paginates /questions/search, normalizes status to UPPERCASE, upserts onConflict (organization_id,ml_user_id,question_id).
    - sync-ml-claims implements the dual-URL fallback and maps `type` → `tipo`.
    - No access_token/refresh_token printed in any console statement.
  </acceptance_criteria>
  <done>Both sync EFs written matching the sync-ads analog + Nexo MCP endpoint spec; grep assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build reply-ml-question EF + expand ml-reputation with feedbacks[]</name>
  <files>supabase/functions/reply-ml-question/index.ts, supabase/functions/ml-reputation/index.ts</files>
  <read_first>
    - supabase/functions/ml-reputation/index.ts (EXACT analog — user-JWT via supabase.auth.getUser, ml_tokens lookup, is_org_member rpc, ML /users/{id} fetch; lines 66–88 are the expansion site)
    - 42-RESEARCH.md "Answer Question API" (POST /answers, body {question_id, text}, 2000 limit, irreversible) + "Feedback API" (3-URL fallback, fulfilled normalization, [] on failure)
    - 42-PATTERNS.md "reply-ml-question" + "ml-reputation (expand)" + "Shared Patterns → Auth Guard (user-invoked)/Org Membership Check/ML Token Lookup"
    - /root/nexo-mcp/ml_client.py:2003 (answer_question), :1065 (fetch_feedback)
  </read_first>
  <action>
    reply-ml-question (verify_jwt=true): OPTIONS/CORS. Validate user JWT (supabase.auth.getUser(token) → 401 if invalid). Parse + validate body with zod (deno.land/x/zod@v3.22.4): { question_id: number int positive, text: string min 1 max 2000, ml_user_id: string min 1 } → 400 on failure (server-side enforcement of the 2000-char limit, D-05/D-06). Look up ml_tokens by ml_user_id (access_token not null, limit 1, maybeSingle) → 404 if none. Verify is_org_member(_user_id=userId, _org_id=tokenRow.organization_id) → 403 if not a member (T-42-05: irreversible write on behalf of user requires ownership). POST https://api.mercadolibre.com/answers with Authorization Bearer {access_token}, body {question_id, text}. On non-2xx return the ML status + a sanitized message (no token). On success UPDATE ml_questions SET status='ANSWERED', resposta=text, data_resposta=now, synced_at=now WHERE question_id=question_id AND ml_user_id=ml_user_id. Return {ok:true}.
    ml-reputation expansion (Opção A): keep the existing seller_reputation/power_seller_status return. After the /users/{id} fetch, attempt the three feedback URLs in sequence (/users/{id}/feedbacks/received, /users/{id}/feedbacks?as=seller, /users/{id}/feedback?as=seller); on the first 200, set feedbacks = fbData.feedbacks ?? fbData.results ?? [] and break. On all failures feedbacks = [] (NEVER throw — Pitfall 7, D-07). Add feedbacks to the JSON response. Do not normalize fulfilled here — the hook does that in plan 03.
  </action>
  <verify>
    <automated>test -f supabase/functions/reply-ml-question/index.ts; grep -q "is_org_member" supabase/functions/reply-ml-question/index.ts; grep -q "/answers" supabase/functions/reply-ml-question/index.ts; grep -q "max(2000)" supabase/functions/reply-ml-question/index.ts; grep -q "feedbacks" supabase/functions/ml-reputation/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - reply-ml-question returns 401 (no JWT), 400 (invalid/text>2000), 403 (not org member), 404 (no token), 2xx (success) — distinct paths.
    - On success it updates the ml_questions row to ANSWERED with resposta + data_resposta.
    - ml-reputation returns feedbacks[] (empty array when all feedback endpoints fail; never throws).
    - No ML token printed in either EF's responses or logs.
  </acceptance_criteria>
  <done>reply EF written with full validation chain; ml-reputation returns feedbacks[]; grep assertions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Write pg_cron schedule migration (Pattern B, vault auth)</name>
  <files>supabase/migrations/20260614110000_pg_cron_questions_claims.sql</files>
  <read_first>
    - supabase/migrations/20260521300000_recalc_tax_and_cron_custos.sql:154-184 (EXACT Pattern B analog — cron.schedule + net.http_post + Bearer from vault.decrypted_secrets WHERE name='service_role_key')
    - 42-RESEARCH.md "pg_cron Schedule Pattern (migration)" + "pg_cron Auth Pattern (CRÍTICO — pitfall D-02)"
    - 42-PATTERNS.md "pg_cron Pattern B" (CRITICAL: do NOT use X-Cron-Secret Pattern A)
  </read_first>
  <action>
    Create the migration scheduling two cron jobs. For each: idempotent unschedule first (DO $$ BEGIN PERFORM cron.unschedule('<jobname>'); EXCEPTION WHEN OTHERS THEN NULL; END $$;), then cron.schedule.
    Job sync-ml-questions-every-15min: schedule '*/15 * * * *' (D-01 — questions are urgent); net.http_post to https://ckcdevcxgvueywivefgx.supabase.co/functions/v1/sync-ml-questions with headers Content-Type application/json AND Authorization 'Bearer ' || (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'service_role_key' LIMIT 1); body '{}'::jsonb.
    Job sync-ml-claims-every-30min: schedule '*/30 * * * *' (D-01); same net.http_post pattern to /functions/v1/sync-ml-claims.
    Use Pattern B (Bearer service_role_key from vault) — explicitly NOT Pattern A (X-Cron-Secret), per D-02 / Pitfall 1. The EFs check token === SUPABASE_SERVICE_ROLE_KEY, so the anon sb_secret_ key would 401.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260614110000_pg_cron_questions_claims.sql; grep -q "sync-ml-questions-every-15min" supabase/migrations/20260614110000_pg_cron_questions_claims.sql; grep -q "name = 'service_role_key'" supabase/migrations/20260614110000_pg_cron_questions_claims.sql; grep -cL "X-Cron-Secret" supabase/migrations/20260614110000_pg_cron_questions_claims.sql</automated>
  </verify>
  <acceptance_criteria>
    - Two cron jobs scheduled at */15 (questions) and */30 (claims), both idempotent (unschedule-then-schedule).
    - Both use Bearer service_role_key from vault.decrypted_secrets (Pattern B); neither uses X-Cron-Secret.
    - URLs point at ckcdevcxgvueywivefgx functions endpoints.
  </acceptance_criteria>
  <done>Cron migration written using Pattern B; grep assertions pass; Pattern A absent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Deploy EFs, apply cron migration, smoke-verify auth + first sync</name>
  <action>
    Deploy the four EFs and apply the cron migration to ckcdevcxgvueywivefgx, then run the smoke checks D-02 demands (the Nexo MCP cron-auth pitfall must be caught now, not in production). Do NOT use `supabase db push`. Steps are enumerated in how-to-verify; report the results back. This task pauses for human verification because it deploys to and mutates the live project and depends on the plan-01 vault check.
  </action>
  <what-built>
    Four EFs (sync-ml-questions, sync-ml-claims, reply-ml-question, expanded ml-reputation) and the pg_cron schedule migration. This task deploys them to ckcdevcxgvueywivefgx and runs the smoke checks.
  </what-built>
  <how-to-verify>
    Prereq: plan-01 Task 3 confirmed vault service_role_key exists. Do NOT use `supabase db push` (local CLI linked to wrong project).
    1. Deploy the four EFs to ckcdevcxgvueywivefgx (Supabase MCP deploy or dashboard). Confirm all four ACTIVE.
    2. Apply the cron migration via Supabase MCP apply_migration (project ckcdevcxgvueywivefgx, name="pg_cron_questions_claims", SQL from 20260614110000_pg_cron_questions_claims.sql).
    3. Auth smoke (anon must 401): curl -s -o /dev/null -w "%{http_code}" -X POST https://ckcdevcxgvueywivefgx.supabase.co/functions/v1/sync-ml-questions -H "Authorization: Bearer <ANON_KEY>" → expect 401 (proves requireServiceRole works, the inverse of the Nexo MCP bug).
    4. Real sync smoke: trigger sync-ml-questions once with the service_role_key Bearer (or wait for the */15 tick), then execute_sql SELECT count(*) FROM ml_questions; and SELECT count(*) FROM ml_claims; — at least one should be > 0 for Pé Vermeio (1639558873) if real ML data exists. Zero is acceptable only if the account genuinely has none — note which.
    5. Cron health: SELECT jobname, schedule, active FROM cron.job WHERE jobname LIKE 'sync-ml-%'; → both present, active=true, schedules */15 and */30. Then SELECT status FROM cron.job_run_details WHERE jobname='sync-ml-questions-every-15min' ORDER BY start_time DESC LIMIT 1; → status must NOT be 'failed' with an Unauthorized body (the sb_secret_ pitfall).
    Report: 4 EFs ACTIVE (yes/no), anon→401 (yes/no), row counts, cron jobs active (yes/no), latest run status.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual checkpoint; smoke performed via curl (anon→401) + Supabase MCP execute_sql (row counts, cron.job, cron.job_run_details) as described in how-to-verify</automated>
  </verify>
  <acceptance_criteria>
    - All four EFs ACTIVE in ckcdevcxgvueywivefgx.
    - Anon-key POST to sync-ml-questions returns 401.
    - cron.job shows both jobs active with */15 and */30 schedules.
    - Latest cron run is not a 401/Unauthorized failure (Pattern B auth works).
  </acceptance_criteria>
  <done>Four EFs ACTIVE; anon→401; cron jobs active; latest cron run not an Unauthorized failure.</done>
  <resume-signal>Type "approved" once all four EFs are ACTIVE, anon→401 confirmed, cron jobs active, and the latest cron run did not fail with Unauthorized. Otherwise describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → sync EF | Service-role-authenticated server-to-server call |
| browser → reply-ml-question | User-JWT authenticated; performs irreversible write to ML |
| sync EF → ML API | Outbound with ml_tokens access_token (secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Information Disclosure | ml_tokens access_token in EF logs/responses | mitigate | No console statement prints access_token/refresh_token (grep-verified); EF responses return only counts/status |
| T-42-05 | Elevation of Privilege | reply-ml-question posting on behalf of another org | mitigate | is_org_member(userId, tokenRow.organization_id) → 403 before POST /answers; zod-validated ml_user_id |
| T-42-06 | Spoofing | cron call without proper auth (sb_secret_ pitfall) | mitigate | requireServiceRole compares to SUPABASE_SERVICE_ROLE_KEY; cron passes Bearer service_role_key from vault (Pattern B); Task 4 smoke confirms anon→401 |
| T-42-07 | Denial of Service | ML rate-limit ban from aggressive paging | mitigate | 200ms sleep between pages; cron capped at */15 (questions) and */30 (claims) by design |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none); no install tasks |
</threat_model>

<verification>
- `npm run build` passes (EF Deno files are not Vite-bundled, but shared import type errors would surface).
- All four EFs deploy ACTIVE to ckcdevcxgvueywivefgx.
- Anon→401 smoke passes; cron jobs active; latest cron run not an Unauthorized failure.
</verification>

<success_criteria>
- sync-ml-questions / sync-ml-claims populate the live tables for active ml_tokens (D-01, D-03).
- reply-ml-question enforces JWT + org membership + 2000-char limit before the irreversible POST (MOCK-02, D-05).
- ml-reputation returns feedbacks[] (empty allowed) for the daily chart (MOCK-04, D-07).
- pg_cron runs on Pattern B auth without the sb_secret_ 401 pitfall (D-02).
</success_criteria>

<output>
Create `.planning/phases/42-zero-mock/42-02-SUMMARY.md` when done.
</output>
