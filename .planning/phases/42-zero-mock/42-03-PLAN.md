---
phase: 42-zero-mock
plan: 03
type: execute
wave: 3
depends_on: ["42-01", "42-02"]
files_modified:
  - src/hooks/useMLQuestions.ts
  - src/hooks/useMLClaims.ts
  - src/hooks/useMLReputation.ts
  - src/pages/mercadolivre/MLPerguntas.tsx
  - src/pages/mercadolivre/MLDevolucoes.tsx
  - src/pages/mercadolivre/MLReputacao.tsx
  - src/data/perguntasMockData.ts
  - src/data/devolucoesMockData.ts
  - src/data/reputacaoMockData.ts
autonomous: true
requirements: [MOCK-01, MOCK-02, MOCK-03, MOCK-04]
user_setup: []

must_haves:
  truths:
    - "/perguntas lists real ml_questions and the user replies inline with confirmation (MOCK-01, MOCK-02)"
    - "/devolucoes lists real ml_claims with tipo + status filters, read-only (MOCK-03)"
    - "/reputacao shows real feedback; 30d chart derived from real feedback dates (MOCK-04)"
    - "No getMock* imported by any product page/hook; no 'dados simulados' badge renders"
  artifacts:
    - path: "src/hooks/useMLQuestions.ts"
      provides: "Hook reading ml_questions, CR-01 scope"
      exports: ["useMLQuestions"]
    - path: "src/hooks/useMLClaims.ts"
      provides: "Hook reading ml_claims"
      exports: ["useMLClaims"]
    - path: "src/pages/mercadolivre/MLPerguntas.tsx"
      provides: "Real questions + inline reply + confirm + char counter"
    - path: "src/pages/mercadolivre/MLReputacao.tsx"
      provides: "Real reputation + derived daily feedback chart"
  key_links:
    - from: "src/hooks/useMLQuestions.ts"
      to: "ml_questions"
      via: "eq(organization_id).in(ml_user_id, resolvedMLUserIds)"
      pattern: "ml_questions"
    - from: "src/pages/mercadolivre/MLPerguntas.tsx"
      to: "reply-ml-question"
      via: "functions.invoke after confirm"
      pattern: "reply-ml-question"
    - from: "src/hooks/useMLReputation.ts"
      to: "ml-reputation feedbacks"
      via: "parse data.feedbacks"
      pattern: "feedbacks"
---

<objective>
Replace simulated data on /perguntas, /devolucoes, /reputacao with real data. Build useMLQuestions + useMLClaims (CR-01 scope) on the useMLBilling analog; rewrite the three pages; wire inline reply (D-04/05/06) through reply-ml-question; derive the daily feedback chart from real dates (D-07/D-08); delete every getMock* and "dados simulados" badge in the perguntas/devolucoes/reputacao surface, turning the plan-01 scaffolds GREEN.

Purpose: user-facing payoff — three of four pages show only real ML data.
Output: two hooks, three rewritten pages, mocks removed (types kept), tests pass.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-zero-mock/42-CONTEXT.md
@.planning/phases/42-zero-mock/42-RESEARCH.md
@.planning/phases/42-zero-mock/42-PATTERNS.md
@.planning/phases/42-zero-mock/42-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build useMLQuestions + useMLClaims hooks</name>
  <files>src/hooks/useMLQuestions.ts, src/hooks/useMLClaims.ts</files>
  <behavior>
    - useMLQuestions: ml_questions scoped to currentOrg.id, .in("ml_user_id", resolvedMLUserIds) (CR-01), order data_pergunta desc, optional status; disabled until orgId and resolvedMLUserIds.length greater than 0.
    - useMLClaims: ml_claims scoped same way, order data_abertura desc; page may filter tipo/status.
    - Both return [] (never throw) when disabled; staleTime about 2min.
    - plan-01 scaffolds useMLQuestions.test.ts and useMLClaims.test.ts pass after these exist.
  </behavior>
  <read_first>
    - src/hooks/useMLBilling.ts (EXACT analog ~149-205: useMLStore resolvedMLUserIds, useOrganization currentOrg, useQuery, enabled guard, .eq/.in)
    - src/contexts/MLStoreContext.tsx (resolvedMLUserIds export)
    - 42-PATTERNS.md "useMLQuestions.ts + useMLClaims.ts" and "Multi-loja Hook Scope (CR-01)"
    - src/hooks/useMLQuestions.test.ts, src/hooks/useMLClaims.test.ts (RED scaffolds from plan 01 = the contract)
  </read_first>
  <action>
    Build useMLQuestions(status?) and useMLClaims(filters?) following useMLBilling: resolvedMLUserIds from useMLStore(), currentOrg from useOrganization(), orgId = currentOrg?.id ?? null. useQuery queryKey ["ml_questions", orgId, resolvedMLUserIds, status] (ml_claims analog), enabled !!orgId and resolvedMLUserIds.length, staleTime 2 min. queryFn returns [] if disabled else supabase.from("ml_questions").select("*").eq("organization_id", orgId).in("ml_user_id", resolvedMLUserIds).order("data_pergunta", desc).limit(200) plus optional .eq("status", status). useMLClaims uses ml_claims and order data_abertura desc; fetching all and filtering in the page is acceptable (D-09). Export explicit MLQuestionRow / MLClaimRow interfaces matching the table columns. No separate merge — .in(resolvedMLUserIds) IS the CR-01 merge.
  </action>
  <verify>
    <automated>npm test -- src/hooks/useMLQuestions.test.ts src/hooks/useMLClaims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both hooks exported with explicit row interfaces.
    - Query scoped by organization_id and .in(ml_user_id, resolvedMLUserIds), ordered by correct date column desc.
    - plan-01 test files PASS (GREEN).
  </acceptance_criteria>
  <done>Hooks built on useMLBilling analog; scaffold tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite MLPerguntas (inline reply) and MLDevolucoes (filters)</name>
  <files>src/pages/mercadolivre/MLPerguntas.tsx, src/pages/mercadolivre/MLDevolucoes.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLPerguntas.tsx (self; getMock* imports ~22-25, mock useMemos ~47-49, badge ~76-82, fake setSyncing ~86-88)
    - src/pages/mercadolivre/MLDevolucoes.tsx (self; getMock* imports; keep STATUS_CONFIG)
    - 42-PATTERNS.md "MLPerguntas.tsx" and "MLDevolucoes.tsx" (AlertDialog block, char counter, empty state, filter useMemo)
    - src/components/ui/alert-dialog.tsx (shadcn AlertDialog for D-05)
    - 42-CONTEXT.md D-04/D-05/D-06/D-09 and empty-state discretion note
  </read_first>
  <action>
    MLPerguntas: remove the 3 getMock* imports, their useMemos, the "Dados simulados" badge/tooltip, and the fake setSyncing timeout. Add useMLQuestions() returning {data:questions=[], isLoading, refetch}; derive pending/answered/daily stats from questions[] (real rows only). Inline reply (D-04): state answeringId:number|null, answerText, confirmingId:number|null; "Responder" on an UNANSWERED row expands a textarea. Char counter (D-06): answerText.length over 2000, disable submit when length is 0 or over 2000. Confirm (D-05): shadcn AlertDialog when confirmingId is set, copy states irreversible, AlertDialogAction runs handleSendReply. handleSendReply: supabase.functions.invoke("reply-ml-question", body {question_id, text, ml_user_id: the row ml_user_id}); on success toast.success (sonner) + refetch() + clear; on error toast.error and keep textarea. Empty state: when not isLoading and questions.length is 0, show "Sincronizando perguntas - volte em alguns minutos" (NO on-demand sync; model is cron). Refresh button calls refetch(). Store filter respected via the hook (D-10).
    MLDevolucoes: remove getMock* and the "dados simulados" badge. Add useMLClaims() returning {data:claims=[], isLoading}; keep STATUS_CONFIG. Add tipoFilter (all|mediations|returns) and statusFilter (all|opened|closed) via Tabs/Select; filtered = useMemo applying both (D-09). Read-only. Same empty state. Map tipo to a readable label (reclamacao / devolucao).
    Neither page uses dangerouslySetInnerHTML on ML text (T-42-08).
  </action>
  <verify>
    <automated>test -f src/pages/mercadolivre/MLPerguntas.tsx; grep -q "useMLQuestions" src/pages/mercadolivre/MLPerguntas.tsx; grep -q "reply-ml-question" src/pages/mercadolivre/MLPerguntas.tsx; grep -q "useMLClaims" src/pages/mercadolivre/MLDevolucoes.tsx; grep -cL "getMock" src/pages/mercadolivre/MLPerguntas.tsx src/pages/mercadolivre/MLDevolucoes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No getMock* import or "dados simulados" badge in either page.
    - MLPerguntas uses useMLQuestions, an AlertDialog confirm (D-05), a 0/2000 char counter (D-06), and invokes reply-ml-question + sonner toast (D-06).
    - MLDevolucoes uses useMLClaims, has tipo + status filters (D-09), and is read-only.
  </acceptance_criteria>
  <done>Both pages consume real hooks; reply flow + filters wired; badges gone.</done>
</task>

<task type="auto">
  <name>Task 3: Real reputation feedback + remove all reputation/perguntas/devolucoes mocks</name>
  <files>src/hooks/useMLReputation.ts, src/pages/mercadolivre/MLReputacao.tsx, src/data/reputacaoMockData.ts, src/data/perguntasMockData.ts, src/data/devolucoesMockData.ts</files>
  <read_first>
    - src/hooks/useMLReputation.ts (self; getMockReputationSummary import ~8, mockReputation field ~33, useMemo ~88-91, return field)
    - src/pages/mercadolivre/MLReputacao.tsx (self; getMockFeedbackDaily/getMockFeedbackEntries/mockReputation usage)
    - 42-PATTERNS.md "MLReputacao.tsx" + "useMLReputation.ts" (buildDailyFeedback, fulfilled normalization)
    - 42-RESEARCH.md "Existing Code to Modify -> useMLReputation.ts", Pitfall 3 (fulfilled string|bool), Pitfall 7 ([] valid), D-07/D-08
    - src/data/reputacaoMockData.ts (keep type exports; remove getMock*)
  </read_first>
  <action>
    useMLReputation.ts: remove the getMockReputationSummary import, the mockReputation field from UseMLReputationResult, its useMemo, and the return field. Add feedbacks: FeedbackEntry[] to the interface and state. In fetchReputation, parse data.feedbacks (returned by the plan-02 expanded EF) into FeedbackEntry[] with rating normalized per Pitfall 3: (f.fulfilled === "positive" || f.fulfilled === true) ? "positive" : "negative"; message = f.message ?? ""; date = f.date_created ?? "". Treat missing/empty data.feedbacks as [] (Pitfall 7 — never throw). Update any consumer of the removed mockReputation field.
    MLReputacao.tsx: remove getMockFeedbackDaily/getMockFeedbackEntries calls and mockReputation usage. Add buildDailyFeedback(feedbacks) grouping by date.substring(0,10) into {positive, negative} counts sorted ascending (D-07); feed into the existing recharts chart. A sparse/empty series is valid — when feedbacks is [], render empty, never fabricated. Remove the "dados simulados" badge.
    Mock files: in reputacaoMockData.ts delete the getMock* functions but KEEP the type exports (ReputationSummary, ReputationLevel, FeedbackEntry). In perguntasMockData.ts and devolucoesMockData.ts (unimported after Task 2) delete the getMock* functions; keep only types still consumed, else empty the file. Confirm nothing imports the removed functions.
    SCOPE NOTE: src/data/pedidosMockData.ts and src/data/financialMockData.ts are OUT OF SCOPE (pedidos/anuncios) — do not touch; the grep gate targets only the perguntas/devolucoes/reputacao surface.
  </action>
  <verify>
    <automated>grep -q "feedbacks" src/hooks/useMLReputation.ts; grep -cL "getMock" src/hooks/useMLReputation.ts src/pages/mercadolivre/MLReputacao.tsx src/data/reputacaoMockData.ts; grep -c "getMock" src/data/reputacaoMockData.ts</automated>
  </verify>
  <acceptance_criteria>
    - useMLReputation returns feedbacks[]; no getMock* remains in the hook, MLReputacao, or reputacaoMockData functions.
    - Daily chart derived from real feedback dates; empty series renders without error.
    - No getMock* import in any of the three product pages or the reputation hook.
  </acceptance_criteria>
  <done>Reputation uses real feedback; all in-scope mocks removed; types preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → reply-ml-question | User-JWT call performing irreversible ML write |
| ml_questions/ml_claims text → JSX | Buyer-authored strings rendered in the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08 | Tampering | rendering buyer question/answer/claim text | mitigate | React escapes JSX by default; no dangerouslySetInnerHTML on ML-sourced text |
| T-42-09 | Elevation of Privilege | client supplies a question_id from another org | mitigate | reply-ml-question (plan 02) re-validates org membership + ml_user_id ownership server-side; client cannot bypass RLS or the EF check |
| T-42-10 | Information Disclosure | cross-org rows leaking into a page | mitigate | Hooks scope every query by organization_id + .in(ml_user_id, resolvedMLUserIds); RLS org_member is the backstop |
</threat_model>

<verification>
- `npm run build` passes (tsc + bundle clean).
- `npm test` passes (plan-01 scaffolds GREEN).
- `grep -rn "getMock" src/pages/mercadolivre/MLPerguntas.tsx src/pages/mercadolivre/MLDevolucoes.tsx src/pages/mercadolivre/MLReputacao.tsx src/hooks/useMLReputation.ts` returns nothing.
- `grep -rin "dados simulados" src/pages/mercadolivre/MLPerguntas.tsx src/pages/mercadolivre/MLDevolucoes.tsx src/pages/mercadolivre/MLReputacao.tsx` returns nothing.
- Manual: /perguntas reply flow (textarea + confirm + toast), /devolucoes filters, /reputacao chart with real (possibly sparse) data.
</verification>

<success_criteria>
- /perguntas, /devolucoes, /reputacao read real ML data (MOCK-01, MOCK-02, MOCK-03, MOCK-04).
- Inline reply works through reply-ml-question with confirmation + char limit (MOCK-02, D-04/05/06).
- All getMock* removed from the in-scope surface; no "dados simulados" badge.
</success_criteria>

<output>
Create `.planning/phases/42-zero-mock/42-03-SUMMARY.md` when done.
</output>
