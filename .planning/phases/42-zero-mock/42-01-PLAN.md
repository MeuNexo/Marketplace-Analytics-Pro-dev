---
phase: 42-zero-mock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260614100000_ml_questions_claims.sql
  - supabase/config.toml
  - src/hooks/useMLQuestions.test.ts
  - src/hooks/useMLClaims.test.ts
autonomous: false
requirements: [MOCK-01, MOCK-03]
user_setup: []

must_haves:
  truths:
    - "Tables ml_questions and ml_claims exist in the live database (ckcdevcxgvueywivefgx) with RLS enabled"
    - "vault.secrets has a service_role_key entry (verified before any cron migration in plan 02)"
    - "Test scaffolds exist so hook behavior can be asserted in later waves"
  artifacts:
    - path: "supabase/migrations/20260614100000_ml_questions_claims.sql"
      provides: "CREATE TABLE ml_questions + ml_claims, RLS org_member policies, scope/status/data indexes"
      contains: "CREATE TABLE public.ml_questions"
    - path: "supabase/config.toml"
      provides: "verify_jwt entries for the three new EFs"
      contains: "[functions.sync-ml-questions]"
    - path: "src/hooks/useMLQuestions.test.ts"
      provides: "Failing scaffold for MOCK-01/MOCK-02 hook behavior"
    - path: "src/hooks/useMLClaims.test.ts"
      provides: "Failing scaffold for MOCK-03 hook behavior"
  key_links:
    - from: "supabase/migrations/20260614100000_ml_questions_claims.sql"
      to: "public.organizations"
      via: "organization_id FK + is_org_member RLS"
      pattern: "REFERENCES public.organizations"
---

<objective>
Lay the data foundation for Phase 42: create the `ml_questions` and `ml_claims` tables (RLS, indexes, unique constraints) following the `ml_billing_monthly` analog, apply them to the LIVE database, verify the `vault.secrets` `service_role_key` entry exists (required by the plan-02 cron migration), register `verify_jwt` for the three new Edge Functions, and create the failing test scaffolds that close the Wave 0 validation gaps (MOCK-01, MOCK-03).

Purpose: Hooks and EFs in later waves query these tables. TypeScript type-checks pass WITHOUT the live tables existing, so the migration MUST be applied to the live DB now to avoid a false-positive verification state.
Output: Two new tables live in ckcdevcxgvueywivefgx, config.toml updated, vault entry confirmed, two test files scaffolded.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/42-zero-mock/42-CONTEXT.md
@.planning/phases/42-zero-mock/42-RESEARCH.md
@.planning/phases/42-zero-mock/42-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ml_questions + ml_claims migration</name>
  <files>supabase/migrations/20260614100000_ml_questions_claims.sql</files>
  <read_first>
    - supabase/migrations/20260612140000_ml_billing_monthly.sql (exact analog — CREATE TABLE + ENABLE RLS + single org_member FOR ALL policy)
    - supabase/migrations/20260613020000 (ml_billing_daily — composite index pattern)
    - 42-RESEARCH.md "Table Schemas" section (full DDL for both tables)
    - 42-PATTERNS.md "RLS Policy (org-scoped tables)" shared pattern
  </read_first>
  <action>
    Create the migration creating two tables in schema public.
    ml_questions: id (uuid pk default gen_random_uuid), organization_id (uuid not null REFERENCES public.organizations(id)), ml_user_id (text not null), question_id (bigint not null), item_id (text), item_title (text), texto (text not null), status (text not null default 'UNANSWERED'), comprador_id (text), data_pergunta (timestamptz), resposta (text), data_resposta (timestamptz), synced_at (timestamptz default now()); UNIQUE (organization_id, ml_user_id, question_id).
    ml_claims: id (uuid pk), organization_id (uuid not null REFERENCES public.organizations(id)), ml_user_id (text not null), claim_id (text not null), order_id (text), tipo (text not null default 'mediations'), status (text not null default 'opened'), motivo (text), data_abertura (date), data_limite (date), solucao (text), synced_at (timestamptz default now()); UNIQUE (organization_id, ml_user_id, claim_id).
    For BOTH: ENABLE ROW LEVEL SECURITY; single policy org_member_questions / org_member_claims, FOR ALL, USING (public.is_org_member(auth.uid(), organization_id)) — match the ml_billing_monthly analog exactly.
    Indexes (CREATE INDEX IF NOT EXISTS): idx_ml_questions_scope (organization_id, ml_user_id); idx_ml_questions_status (organization_id, ml_user_id, status); idx_ml_questions_data (organization_id, ml_user_id, data_pergunta DESC); and the three ml_claims equivalents using data_abertura DESC for the data index.
    Per Pitfall 6 the unique constraint MUST include organization_id + ml_user_id (not question_id/claim_id alone).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260614100000_ml_questions_claims.sql; grep -q "CREATE TABLE public.ml_questions" supabase/migrations/20260614100000_ml_questions_claims.sql; grep -q "CREATE TABLE public.ml_claims" supabase/migrations/20260614100000_ml_questions_claims.sql; grep -q "is_org_member(auth.uid(), organization_id)" supabase/migrations/20260614100000_ml_questions_claims.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with both CREATE TABLE statements.
    - Both tables have ENABLE ROW LEVEL SECURITY and an org_member FOR ALL policy using public.is_org_member(auth.uid(), organization_id).
    - Unique constraints are (organization_id, ml_user_id, question_id) and (organization_id, ml_user_id, claim_id).
    - Six indexes created (3 per table), all IF NOT EXISTS.
  </acceptance_criteria>
  <done>Migration written matching the RESEARCH.md DDL and ml_billing_monthly analog; grep assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Register verify_jwt for the three new EFs + scaffold failing hook tests</name>
  <files>supabase/config.toml, src/hooks/useMLQuestions.test.ts, src/hooks/useMLClaims.test.ts</files>
  <read_first>
    - supabase/config.toml (existing [functions.*] tail — sync-tiny-costs/sync-ml-orders use verify_jwt=false)
    - 42-RESEARCH.md "config.toml para novas EFs" section
    - 42-RESEARCH.md "Validation Architecture" → Wave 0 Gaps + Phase Requirements → Test Map
    - src/hooks/useMLBilling.ts (hook shape these tests assert against)
  </read_first>
  <action>
    In supabase/config.toml append three entries: [functions.sync-ml-questions] verify_jwt = false; [functions.sync-ml-claims] verify_jwt = false; [functions.reply-ml-question] verify_jwt = true. Rationale: sync EFs are cron-invoked with service_role_key (Pattern B), the reply EF is user-invoked with a user JWT.
    Create src/hooks/useMLQuestions.test.ts and src/hooks/useMLClaims.test.ts as vitest scaffolds closing the Nyquist Wave 0 gap. Each imports the (not-yet-existing) hook and asserts the contract plan 03 will satisfy: scopes the Supabase query by organization_id, filters ml_user_id via resolvedMLUserIds (CR-01), orders by the date column descending, applies the optional status filter. Mock the supabase client + the MLStore/Organization contexts. These tests are expected to FAIL now (hook missing) — the RED state. Add a top-of-file comment naming the requirements (MOCK-01/MOCK-02 for questions, MOCK-03 for claims). Do NOT write the hooks here.
  </action>
  <verify>
    <automated>grep -q "\[functions.sync-ml-questions\]" supabase/config.toml; grep -q "\[functions.reply-ml-question\]" supabase/config.toml; test -f src/hooks/useMLQuestions.test.ts; test -f src/hooks/useMLClaims.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - config.toml contains the three new [functions.*] entries with correct verify_jwt (false/false/true).
    - Both test files exist and assert org_id + resolvedMLUserIds scoping and status filter.
    - `npm test -- src/hooks/useMLQuestions.test.ts` fails because the hook module does not exist (RED — not a syntax error).
  </acceptance_criteria>
  <done>Three config entries present; two failing test scaffolds created asserting the future hook contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply table migration to live DB + verify vault service_role_key</name>
  <action>
    Apply the table migration to the LIVE database and confirm the vault prerequisite for plan 02's cron migration. Do NOT use `supabase db push` (the local CLI is linked to the wrong project per STATE.md). Use the Supabase MCP against project ckcdevcxgvueywivefgx. Steps are enumerated in how-to-verify; report the results back. This task pauses for human verification because it mutates the live database and gates plan 02.
  </action>
  <what-built>
    The ml_questions + ml_claims migration (Task 1) and the verify_jwt config (Task 2). This task applies the table migration to the LIVE database and confirms the vault prerequisite for plan-02's cron migration.
  </what-built>
  <how-to-verify>
    Apply/verify against ckcdevcxgvueywivefgx (NOT `supabase db push`):
    1. Supabase MCP apply_migration with project_id=ckcdevcxgvueywivefgx, name="ml_questions_claims", SQL from supabase/migrations/20260614100000_ml_questions_claims.sql.
    2. Supabase MCP list_tables (schema public) shows ml_questions and ml_claims, both rls_enabled=true.
    3. Verify vault prereq (RESEARCH.md A1 — most critical assumption): execute_sql SELECT name FROM vault.secrets WHERE name = 'service_role_key'; MUST return exactly one row. If ZERO rows, STOP (plan-02 cron will 401): insert via SELECT vault.create_secret('<service_role_key>', 'service_role_key', 'Service role key para pg_cron'); then re-verify. Wesley must supply/confirm the key value.
    4. Supabase MCP get_advisors (type=security) — confirm no NEW critical advisor on the two tables (RLS present).
    Report: tables created (yes/no), rls_enabled (yes/yes), vault service_role_key present (yes/no), advisor result.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual checkpoint; verification performed via Supabase MCP list_tables + execute_sql + get_advisors as described in how-to-verify</automated>
  </verify>
  <acceptance_criteria>
    - list_tables shows ml_questions and ml_claims with rls_enabled=true.
    - SELECT name FROM vault.secrets WHERE name='service_role_key' returns 1 row.
    - get_advisors security shows no critical RLS-missing finding on the two new tables.
  </acceptance_criteria>
  <done>Tables live with RLS in ckcdevcxgvueywivefgx; vault service_role_key confirmed present.</done>
  <resume-signal>Type "approved" once tables are live with RLS and the vault service_role_key entry is confirmed present, or describe the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ml_questions/ml_claims | Authenticated browser reads org-scoped rows via RLS |
| migration → live DB | DDL applied via Supabase MCP under service-role context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information Disclosure | ml_questions / ml_claims (buyer PII: question text, claim details, buyer ids) | mitigate | RLS FOR ALL USING public.is_org_member(auth.uid(), organization_id) on both tables; scope organization_id + ml_user_id |
| T-42-02 | Tampering | unique constraint missing org scope | mitigate | UNIQUE (organization_id, ml_user_id, question_id/claim_id) prevents cross-org row collision on reconnect (Pitfall 6) |
| T-42-03 | Elevation of Privilege | RLS disabled by oversight | mitigate | Task 3 verifies rls_enabled=true via list_tables + get_advisors security before sign-off |
</threat_model>

<verification>
- `npm run build` passes (config.toml + test files do not break the build).
- Supabase MCP list_tables shows both tables with RLS enabled in ckcdevcxgvueywivefgx.
- vault.secrets contains service_role_key (1 row).
</verification>

<success_criteria>
- ml_questions and ml_claims live in ckcdevcxgvueywivefgx with RLS + indexes + unique constraints.
- config.toml registers the three new EFs with correct verify_jwt.
- Two failing hook test scaffolds exist (RED), closing the Wave 0 Nyquist gap.
- vault service_role_key confirmed present — plan-02 cron migration unblocked.
</success_criteria>

<output>
Create `.planning/phases/42-zero-mock/42-01-SUMMARY.md` when done.
</output>
