---
phase: 42-zero-mock
plan: 04
type: execute
wave: 3
depends_on: ["42-01"]
files_modified:
  - src/pages/TVModeVendas.tsx
autonomous: true
requirements: [MOCK-05]
user_setup: []

must_haves:
  truths:
    - "/tv loads sellers from the sellers table filtered by organization_id, only ML-connected ones (MOCK-05, D-11)"
    - "No hardcoded seller UUIDs remain in TVModeVendas.tsx"
    - "Logo + initials come from the sellers row with ML fallback; cycling is alphabetical by name (D-12)"
  artifacts:
    - path: "src/pages/TVModeVendas.tsx"
      provides: "Dynamic org-scoped seller list replacing the SELLERS constant"
      contains: "from(\"sellers\")"
  key_links:
    - from: "src/pages/TVModeVendas.tsx"
      to: "sellers + ml_tokens"
      via: "query sellers by organization_id, filter by ml_tokens.seller_id with access_token"
      pattern: "ml_tokens"
---

<objective>
Remove the hardcoded `SELLERS` array (TVModeVendas.tsx:16) and load sellers dynamically from the `sellers` table scoped to `currentOrg.id`, keeping only sellers that have an `ml_tokens` row with an access_token (D-11). Logo and initials come from the sellers row with a fallback (D-12), and the TV cycle iterates alphabetically by name. This plan is independent of the EFs/hooks (it only reads `sellers` + `ml_tokens`), so it runs in Wave 3 in parallel with plan 03 — no file overlap.

Purpose: /tv currently shows two hardcoded UUIDs that only exist for the Pé Vermeio org — broken for any other tenant. This makes /tv multi-tenant correct.
Output: TVModeVendas reads real org sellers; zero hardcoded UUIDs.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-zero-mock/42-CONTEXT.md
@.planning/phases/42-zero-mock/42-RESEARCH.md
@.planning/phases/42-zero-mock/42-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SELLERS constant with dynamic org-scoped query</name>
  <files>src/pages/TVModeVendas.tsx</files>
  <read_first>
    - src/pages/TVModeVendas.tsx (self — SELLERS const lines 16-19; fetchSellerData ml_tokens query pattern lines 111-128; cycle useEffects referencing SELLERS.length / SELLERS[sellerIdx] ~83/97)
    - 42-PATTERNS.md "TVModeVendas.tsx" section (exact seller state + useEffect block, reference updates, sellerIdx re-init)
    - 42-RESEARCH.md "Sellers Table Schema (MOCK-05)" (columns: id, name, initials nullable, logo_url, is_active, organization_id; NO ml_user_id column — join via ml_tokens.seller_id -> sellers.id) and Pitfall 2 (cycle effect deps)
    - src/types/seller.ts (generateInitials export for the initials fallback)
    - src/contexts/OrganizationContext.tsx (currentOrg)
  </read_first>
  <action>
    Remove the SELLERS constant (lines 16-19, both hardcoded UUIDs). Import useOrganization and read currentOrg. Add a SellerEntry interface {id; name; initials; logo: string|null} and a sellers state array. Add a useEffect keyed on currentOrg?.id (with a cancelled guard) that: (1) queries supabase.from("sellers").select("id, name, initials, logo_url").eq("organization_id", currentOrg.id).eq("is_active", true).order("name") — alphabetical (D-12); (2) queries supabase.from("ml_tokens").select("seller_id").eq("organization_id", currentOrg.id).in("seller_id", sellerIds).not("access_token", "is", null); (3) builds a Set of connected seller_ids and keeps only sellers in it (D-11 — only ML-connected); (4) maps to SellerEntry using initials ?? generateInitials(name) and logo_url ?? null (D-12 fallback). Replace ALL SELLERS references with the sellers state: seller = sellers[sellerIdx]; cycle modulo Math.max(sellers.length, 1); guard rendering when sellers[sellerIdx] is undefined (loading spinner). Add a useEffect that resets sellerIdx to 0 when sellers.length changes, and include sellers in the dependency arrays of the cycle effects (Pitfall 2). Logo/initials fallback to the ML ficha is acceptable where the sellers row lacks them, per D-12.
  </action>
  <verify>
    <automated>grep -q "from(\"sellers\")" src/pages/TVModeVendas.tsx; grep -q "ml_tokens" src/pages/TVModeVendas.tsx; grep -q "useOrganization" src/pages/TVModeVendas.tsx; grep -c "8c57110c\|52a7ed04" src/pages/TVModeVendas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "8c57110c\|52a7ed04" src/pages/TVModeVendas.tsx` returns nothing (zero hardcoded UUIDs) — the count check above must be 0.
    - sellers loaded via sellers table scoped to currentOrg.id, filtered to ML-connected via ml_tokens (D-11).
    - initials/logo use the sellers row with generateInitials/ML fallback; list ordered alphabetically (D-12).
    - sellerIdx resets on list change and cycle effects depend on sellers (Pitfall 2 fixed).
  </acceptance_criteria>
  <done>SELLERS constant gone; /tv reads real org sellers; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → sellers / ml_tokens | Authenticated client reads org-scoped rows via RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11 | Information Disclosure | TV showing sellers from another org | mitigate | Query scoped .eq("organization_id", currentOrg.id) on both sellers and ml_tokens; RLS org_member is the backstop (RESEARCH A4 — verify sellers RLS allows authenticated org reads) |
| T-42-12 | Tampering | ml_tokens.access_token exposed to client | accept | Only seller_id is selected from ml_tokens (no token columns); access_token never reaches the client |
</threat_model>

<verification>
- `npm run build` passes.
- `grep -n "8c57110c\|52a7ed04" src/pages/TVModeVendas.tsx` returns nothing.
- Manual: /tv cycles through the org's ML-connected sellers alphabetically with correct logos/initials; an org with one connected seller shows just that one without crashing.
</verification>

<success_criteria>
- /tv reads sellers from the sellers table by organization_id, ML-connected only (MOCK-05).
- Zero hardcoded seller UUIDs in TVModeVendas.tsx.
- Logo/initials from sellers with fallback; alphabetical cycle (D-11, D-12).
</success_criteria>

<output>
Create `.planning/phases/42-zero-mock/42-04-SUMMARY.md` when done.
</output>
