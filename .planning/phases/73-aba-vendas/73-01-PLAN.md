---
phase: 73-aba-vendas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mercadolivre/anuncios/listingSalesAgg.ts
  - src/components/mercadolivre/anuncios/listingSalesAgg.test.ts
  - src/components/mercadolivre/anuncios/useMLListingSales.ts
  - src/components/mercadolivre/anuncios/ListingSalesTab.tsx
  - src/components/mercadolivre/anuncios/ListingDetailModal.tsx
autonomous: true
requirements: [ADM-73]

must_haves:
  truths:
    - "A aba Vendas do modal deixa de ficar desabilitada e renderiza um grafico recharts com as vendas do anuncio aberto (SC-1)"
    - "Um toggle alterna entre unidades vendidas/dia e receita (R$)/dia (SC-2)"
    - "Um seletor alterna a janela entre 30 e 90 dias, refazendo a consulta (SC-2)"
    - "Os dados vem de orders via query direta filtrada por item_id + status='paid', agregados por dia (SC-3)"
    - "Estados de loading (skeleton), vazio e erro sao tratados sem quebrar o modal nem as outras abas (SC-4)"
    - "A query usa o client autenticado (respeita RLS org-scoped de orders); lazy: so busca com modal aberto / aba ativa (SC-5)"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/listingSalesAgg.ts"
      provides: "Utilitario puro de bucketizacao por dia + soma de quantidade/receita + preenchimento de dias vazios"
      min_lines: 25
    - path: "src/components/mercadolivre/anuncios/listingSalesAgg.test.ts"
      provides: "Testes vitest do utilitario de agregacao"
      contains: "describe"
    - path: "src/components/mercadolivre/anuncios/useMLListingSales.ts"
      provides: "Hook lazy de query direta a orders por item_id, janela 30/90d, estados idle/loading/success/empty/error"
      min_lines: 40
    - path: "src/components/mercadolivre/anuncios/ListingSalesTab.tsx"
      provides: "Componente da aba Vendas: grafico recharts + toggle metrica + seletor janela + estados"
      min_lines: 50
  key_links:
    - from: "src/components/mercadolivre/anuncios/ListingDetailModal.tsx"
      to: "src/components/mercadolivre/anuncios/ListingSalesTab.tsx"
      via: "TabsTrigger value=vendas ativo + TabsContent renderiza <ListingSalesTab item={item} />"
      pattern: "ListingSalesTab"
    - from: "src/components/mercadolivre/anuncios/useMLListingSales.ts"
      to: "orders (tabela Supabase)"
      via: "supabase.from('orders') filtrado por item_id + status='paid'"
      pattern: "from\\([\"']orders[\"']\\)"
    - from: "src/components/mercadolivre/anuncios/ListingSalesTab.tsx"
      to: "src/components/mercadolivre/anuncios/listingSalesAgg.ts"
      via: "agrega as linhas brutas em series diarias antes de plotar"
      pattern: "aggregateListingSales|listingSalesAgg"
---

<objective>
Ligar a aba "Vendas" do `ListingDetailModal` (hoje desabilitada com tooltip "em breve") a um grafico real do historico de vendas do anuncio aberto, lido da tabela `orders` via query direta no client.

Implementa a Fase C do milestone "Modal de Detalhe do Anuncio" (spec secao 4) e o requisito ADM-73.

Proposito: dar ao usuario visibilidade do desempenho de vendas de cada anuncio direto no modal, com toggle unidades/receita e janela 30/90 dias, sem criar backend novo (RLS org-scoped de `orders` ja garante o isolamento multi-tenant).

Output: utilitario puro de agregacao (+ testes vitest), hook lazy de consulta, componente da aba com grafico recharts, e o wiring no modal trocando a aba "vendas" de desabilitada para ativa.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-aba-vendas/73-CONTEXT.md
@.planning/ROADMAP.md

# Canonicos (LER ANTES DE IMPLEMENTAR)
@src/pages/mercadolivre/MLPedidos.tsx          # linhas ~700-760: padrao de query direta em `orders` (filtros/status/datas, paginacao .range)
@src/components/mercadolivre/MLRevenueChart.tsx # padrao de grafico recharts (ComposedChart, eixos, tooltip, tokens hsl(var(--...)))
@src/components/mercadolivre/anuncios/ListingDetailModal.tsx     # onde a aba "vendas" e hoje DisabledTabTrigger
@src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx   # padrao de componente de aba
@src/components/mercadolivre/anuncios/ListingIssues.tsx          # padrao de estados loading(skeleton)/vazio/erro a espelhar
@src/components/mercadolivre/anuncios/useMLListingHealth.ts      # padrao de hook lazy (guard de id, flag cancelled no cleanup)
@src/components/mercadolivre/anuncios/listingHelpers.ts          # currencyFmt(v: number): string
@src/contexts/MLInventoryContext.tsx            # ProductItem.id (= item_id) e ProductItem._ml_user_id
@src/integrations/supabase/client.ts            # client autenticado para a query
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Utilitario puro de agregacao de vendas por dia + testes vitest</name>
  <files>src/components/mercadolivre/anuncios/listingSalesAgg.ts, src/components/mercadolivre/anuncios/listingSalesAgg.test.ts</files>
  <read_first>
    - src/pages/mercadolivre/MLPedidos.tsx (linha 61: `data_pedido: string | null`; e o trecho ~714 que documenta data_pedido como ISO/meia-noite UTC) — confirma que o valor chega como string ISO ordenavel.
    - src/components/mercadolivre/anuncios/listingHelpers.ts (so para alinhar estilo de modulo util/named export).
  </read_first>
  <behavior>
    Funcao pura `aggregateListingSales(rows, windowDays, today?)` em `listingSalesAgg.ts`:
    - Entrada `rows`: array de `{ data_pedido: string; quantidade: number; receita_bruta: number }` (linhas cruas de `orders`, ja filtradas por item_id+status='paid').
    - Entrada `windowDays`: 30 ou 90. `today` opcional (Date injetavel para teste deterministico; default `new Date()`).
    - Retorno: array com UM ponto por dia da janela (windowDays pontos, do mais antigo ao mais recente), cada ponto `{ date: string (YYYY-MM-DD); label: string (DD/MM); unidades: number; receita: number }`.
    - Decisao travada (Phase 63): `data_pedido` e TEXT → derivar a chave do dia de `row.data_pedido.slice(0,10)` (porcao YYYY-MM-DD), NAO `new Date(...)` cego.
    - Dias sem venda preenchidos com `unidades:0, receita:0` (a serie nao "pula" datas).
    - Linhas fora da janela ou com `data_pedido` vazio/invalido sao ignoradas (nao quebram).
    - Soma agrega TODAS as variacoes do mesmo dia (somatorio simples de quantidade e receita_bruta).
    Casos de teste (vitest) cobrindo:
    - Test 1: janela de 30 dias com `today` fixo gera exatamente 30 pontos contiguos.
    - Test 2: multiplas linhas no mesmo dia somam quantidade e receita.
    - Test 3: dia sem venda no meio da janela aparece com unidades=0 e receita=0.
    - Test 4: linha com data fora da janela e ignorada.
    - Test 5: array vazio retorna windowDays pontos todos zerados.
    - Test 6: `label` formata DD/MM em pt-BR.
  </behavior>
  <action>
    Criar `listingSalesAgg.ts` exportando o tipo do ponto de serie (ex.: `ListingSalesPoint`) e a funcao pura `aggregateListingSales`. Sem imports de React, Supabase ou rede — modulo 100% puro para ser testavel isoladamente. Derivar a chave de dia pela porcao YYYY-MM-DD de `data_pedido` (slice(0,10)); montar o esqueleto da janela iterando `windowDays` dias a partir de `today` para tras e indexar as somas por chave de dia. Usar somatorio acumulado por dia. Escrever `listingSalesAgg.test.ts` com os 6 casos acima injetando `today` fixo (ex.: data canonica) para determinismo. Named exports, padrao do projeto.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run src/components/mercadolivre/anuncios/listingSalesAgg.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os 6 testes passam.
    - `aggregateListingSales([], 30, fixedDate)` retorna 30 pontos todos com unidades=0 e receita=0.
    - O modulo nao importa React/Supabase (`grep -E "react|supabase" src/components/mercadolivre/anuncios/listingSalesAgg.ts` retorna vazio).
  </acceptance_criteria>
  <done>Util puro de bucketizacao por dia com soma e preenchimento de dias vazios existe e tem testes vitest verdes; cobre SC-3 (agregacao por dia, cast data_pedido TEXT) e parte de SC-6.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Hook lazy useMLListingSales (query direta a orders por item_id, janela 30/90d)</name>
  <files>src/components/mercadolivre/anuncios/useMLListingSales.ts</files>
  <read_first>
    - src/components/mercadolivre/anuncios/useMLListingHealth.ts — copiar o padrao: guard quando falta id, flag `cancelled` no cleanup do useEffect, estado tipado por union de string.
    - src/pages/mercadolivre/MLPedidos.tsx (~700-760) — padrao de query direta `supabase.from("orders")` com `.eq`/`.gte`/`.range`; observar paginacao de 1000 (PostgREST trunca).
    - src/integrations/supabase/client.ts — import do `supabase` (client autenticado, NAO service role).
  </read_first>
  <action>
    Criar `useMLListingSales(item: ProductItem | null, windowDays: 30 | 90)` retornando `{ status, points }` onde `status` e union `"idle" | "loading" | "success" | "empty" | "error"` e `points` e `ListingSalesPoint[]` (do util da Tarefa 1).

    Comportamento:
    - Guard (lazy): se `!item?.id` → status `"idle"`, points `[]`, sem fetch. So consulta quando ha item (modal aberto / aba ativa).
    - Calcular a data inicial da janela: `today - windowDays` dias, formatada como ISO `YYYY-MM-DD`.
    - Query: `supabase.from("orders").select("data_pedido, quantidade, receita_bruta").eq("item_id", item.id).eq("status", "paid").gte("data_pedido", startIso).order("data_pedido", { ascending: true })`. NAO passar `organization_id` nem usar service role — o RLS org-scoped de `orders` ja isola por org sobre o client autenticado (anti-IDOR pelo RLS). Paginar com `.range(from, from+999)` em loop de 1000 ate a ultima pagina (PostgREST trunca em 1000 — ver MEMORY: sempre .range()).
    - Em erro do supabase → status `"error"`.
    - Sucesso: chamar `aggregateListingSales(rows, windowDays)`; se a soma total de unidades e receita for 0 (nenhuma venda no periodo) → status `"empty"` (mas ainda expor os points zerados para o componente decidir); senao status `"success"`.
    - Cleanup com flag `cancelled` para nao chamar setState apos desmontar (padrao do useMLListingHealth).
    - Reexecutar o efeito quando `item.id` ou `windowDays` mudarem (trocar 30↔90 refaz a consulta — SC-2).
    - Importar tipo `ProductItem` de `@/contexts/MLInventoryContext` e o util da Tarefa 1.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` sem erros.
    - O hook usa `supabase.from("orders")` filtrando por `item_id` e `status='paid'` e nao referencia `service_role` nem injeta `organization_id` manualmente (`grep -nE "service_role|organization_id" src/components/mercadolivre/anuncios/useMLListingSales.ts` retorna vazio).
    - Troca de `windowDays` esta no array de dependencias do `useEffect`.
  </acceptance_criteria>
  <done>Hook lazy retorna series diarias agregadas por item_id com estados idle/loading/success/empty/error, refaz a consulta ao trocar a janela, e respeita o RLS (client autenticado). Cobre SC-3 e SC-5.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Componente ListingSalesTab (grafico recharts + toggle + seletor) e wiring no modal</name>
  <files>src/components/mercadolivre/anuncios/ListingSalesTab.tsx, src/components/mercadolivre/anuncios/ListingDetailModal.tsx</files>
  <read_first>
    - src/components/mercadolivre/MLRevenueChart.tsx — padrao recharts do projeto: `ResponsiveContainer`, `ComposedChart`/`Area`/`Bar`, eixos com `tick={{ fontSize: 11, fill: "hsl(var(--muted-foreground))" }}`, tooltip com `contentStyle` e tokens `hsl(var(--card))`. Reusar essa estetica.
    - src/components/mercadolivre/anuncios/ListingIssues.tsx — espelhar os estados: loading com `Skeleton` (aria-label), vazio com mensagem, erro com texto destructive.
    - src/components/mercadolivre/anuncios/listingHelpers.ts — `currencyFmt` para o eixo/tooltip de receita.
    - src/components/mercadolivre/anuncios/ListingDetailModal.tsx — estrutura atual das Tabs (linhas ~149-167): `DisabledTabTrigger value="vendas"` na TabsList e `<TabsContent value="vendas" />` vazio.
  </read_first>
  <action>
    Parte A — `ListingSalesTab.tsx` (named export, props inline `{ item: ProductItem }`):
    - Estado local: `metric` (`"unidades" | "receita"`, default `"unidades"`) e `windowDays` (`30 | 90`, default `30`).
    - Chamar `useMLListingSales(item, windowDays)`.
    - Controles no topo: um toggle metrica (usar `ToggleGroup`/`Tabs` do shadcn, ja disponiveis) "Unidades" vs "Receita (R$)"; e um seletor de janela "30 dias" vs "90 dias". Layout limpo no mobile (controles em flex-wrap).
    - Grafico recharts dentro de `ResponsiveContainer` height ~220: quando `metric="unidades"` plotar Bar de unidades (eixo Y inteiro, `allowDecimals={false}`); quando `metric="receita"` plotar Area/Line de receita com `tickFormatter`/tooltip usando `currencyFmt`. `dataKey="label"` no XAxis (DD/MM). Discricao do tipo de grafico conforme CONTEXT (barras p/ unidades, area/linha p/ receita) — usar tokens `hsl(var(--accent))`/`hsl(var(--primary))` do projeto.
    - Estados (espelhar ListingIssues): `status==="loading"` → Skeleton com aria-label "Carregando vendas do anuncio"; `status==="empty"` → mensagem "Sem vendas no periodo"; `status==="error"` → "Nao foi possivel carregar as vendas" em texto destructive; `status==="success"` → grafico. NUNCA quebrar o modal (sem throw).
    - Importar tipo `ProductItem` de `@/contexts/MLInventoryContext`.

    Parte B — wiring em `ListingDetailModal.tsx`:
    - Importar `ListingSalesTab`.
    - Na `TabsList`, trocar `<DisabledTabTrigger value="vendas" label="Vendas" />` por `<TabsTrigger value="vendas">Vendas</TabsTrigger>` ativo. NAO mexer nas demais (`precificacao`/`avaliacoes`/`historico` continuam `DisabledTabTrigger`).
    - Trocar o `<TabsContent value="vendas" />` vazio por `<TabsContent value="vendas" className="mt-4"><ListingSalesTab item={item} /></TabsContent>`.
    - NAO regredir a aba Indicadores (71/72) nem as outras abas desabilitadas — diff cirurgico apenas na linha da aba "vendas".
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` sem erros e `npm run build` ok.
    - `ListingDetailModal.tsx` renderiza `<ListingSalesTab` em `TabsContent value="vendas"` e a aba "vendas" usa `TabsTrigger` ativo (nao `DisabledTabTrigger`): `grep -n "DisabledTabTrigger value=\"vendas\"" src/components/mercadolivre/anuncios/ListingDetailModal.tsx` retorna vazio.
    - As abas `precificacao`/`avaliacoes`/`historico` permanecem `DisabledTabTrigger` (Indicadores intacta) — `grep -c "DisabledTabTrigger" src/components/mercadolivre/anuncios/ListingDetailModal.tsx` retorna 3.
    - `ListingSalesTab` trata os 4 estados (loading/empty/error/success) e tem toggle de metrica + seletor de janela.
  </acceptance_criteria>
  <done>A aba "Vendas" do modal esta ativa e renderiza o grafico recharts com toggle unidades/receita e seletor 30/90d, com estados loading/vazio/erro, sem regredir as outras abas. Cobre SC-1, SC-2, SC-4 e fecha SC-6 (build).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → Supabase PostgREST | Usuario autenticado le `orders` direto via client; nenhuma EF/RPC nova nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Information Disclosure | `useMLListingSales` query direta a `orders` | accept (mitigado por controle existente) | Anti-IDOR vem do RLS org-scoped de `orders` (2 policies ja em prod). A query usa o client autenticado (`@/integrations/supabase/client`), NUNCA service role — portanto o filtro de org e aplicado pelo Postgres, nao confiavel no front. Nenhum novo endpoint criado. |
| T-73-02 | Tampering | filtro `item_id` vindo de `ProductItem.id` | accept | `item.id` vem do `MLInventoryContext`, ja filtrado por org/seller; ainda que adulterado, o RLS limita o resultado a linhas da org do usuario. |
| T-73-03 | Denial of Service | janela 90d sem teto de linhas | mitigate | Paginacao `.range()` de 1000 ja limita por pagina; consulta filtrada por `item_id` (1 anuncio) mantem o volume baixo. Lazy: so dispara com a aba ativa. |

Nota: nao ha instalacao de pacotes nesta fase (recharts ja presente — CLAUDE.md) → sem checkpoint de legitimidade de pacote.
</threat_model>

<verification>
- `npx vitest run src/components/mercadolivre/anuncios/listingSalesAgg.test.ts` — testes do util puro verdes.
- `npx tsc --noEmit` — zero erros de tipo.
- `npm run build` — build ok.
- Inspecao: aba "Vendas" ativa no modal, toggle unidades/receita e seletor 30/90d funcionando, estados loading/vazio/erro tratados.
- `grep` confirma query usa client autenticado em `orders` (sem service role / sem organization_id manual) — RLS preserva multi-tenant.
</verification>

<success_criteria>
1. Aba "Vendas" deixa de ficar desabilitada e renderiza grafico recharts do anuncio aberto (SC-1).
2. Toggle alterna unidades/dia ↔ receita/dia; seletor alterna 30↔90 dias refazendo a consulta (SC-2).
3. Dados vem de `orders` via query direta por `item_id` + `status='paid'`, agregados por dia (cast `data_pedido` TEXT→date), sem EF/RPC nova (SC-3).
4. Estados loading (skeleton), vazio e erro tratados sem quebrar o modal nem as outras abas (SC-4).
5. Multi-tenant respeitado pelo RLS org-scoped de `orders`; lazy ao abrir o anuncio/ativar a aba (SC-5).
6. `tsc` 0 erros + `build` ok + testes do util puro passando (SC-6).
</success_criteria>

<output>
Criar `.planning/phases/73-aba-vendas/73-01-SUMMARY.md` ao concluir.
</output>
