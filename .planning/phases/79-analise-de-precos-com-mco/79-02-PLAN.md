---
phase: 79-analise-de-precos-com-mco
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified: []
autonomous: false
requirements: ["APM-RPC-DEPLOY"]
user_setup: []

must_haves:
  truths:
    - "A migration 20260679000000 está aplicada em produção (ckcdevcxgvueywivefgx) via MCP apply_migration"
    - "A RPC estendida retorna as 6 colunas novas para um item real da Pé Vermeio"
    - "2-3 buckets batem contra a soma manual em SQL (receita, cmv, comissao, frete, impostos)"
    - "Chamada como role authenticated de uma org alheia retorna 0 linhas (anti-IDOR)"
  artifacts: []
  key_links:
    - from: "MCP apply_migration"
      to: "orders_price_timeseries (prod)"
      via: "aplicação da migration 20260679000000 no projeto ckcdevcxgvueywivefgx"
      pattern: "orders_price_timeseries"
---

<objective>
Aplicar a migration escrita em 79-01 ao banco de produção e provar (smoke) que a RPC
estendida retorna números corretos e continua isolada por org. Este é um plano de
CHECKPOINT executado pelo ORQUESTRADOR — o gsd-executor NÃO tem acesso ao MCP claude.ai
do Supabase; a aplicação e o smoke são feitos pelo orquestrador via MCP
`apply_migration` / `execute_sql` (padrão estabelecido nas Phases 62/63/69).

Purpose: [BLOCKING] a phase não pode passar verificação sem a migration aplicada — build e
testes passam sem o push (os tipos vêm de config, não do banco vivo), criando um
falso-positivo. Este plano fecha essa lacuna.

Output: RPC em prod com 6 colunas novas + evidência de reconciliação e anti-IDOR.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/79-analise-de-precos-com-mco/79-CONTEXT.md
@supabase/migrations/20260679000000_orders_price_timeseries_mco.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING]: Aplicar migration via MCP + smoke como role authenticated + anti-IDOR</name>
  <what-built>
    A migration `supabase/migrations/20260679000000_orders_price_timeseries_mco.sql`
    (escrita em 79-01) estende `orders_price_timeseries` com cmv, comissao, frete,
    qtd_sem_custo, impostos, qtd_sem_imposto. Ainda NÃO aplicada em prod.
  </what-built>
  <how-to-verify>
    O ORQUESTRADOR executa (não o gsd-executor):
    1. Aplicar a migration no projeto `ckcdevcxgvueywivefgx` via MCP
       `mcp__claude_ai_Supabase__apply_migration` (nome: orders_price_timeseries_mco;
       conteúdo = o arquivo .sql do 79-01). NUNCA via SQL Editor, NUNCA `supabase db push`.
    2. Smoke de existência: `SELECT * FROM orders_price_timeseries('<MLB de item real da Pé Vermeio>', NULL, NULL, NULL, 'day') LIMIT 3;`
       via `execute_sql` — deve retornar as 13 colunas sem erro "function does not exist".
    3. Reconciliação (2-3 buckets): rodar como role `authenticated` impersonando a org
       Pé Vermeio (SET LOCAL ROLE authenticated + set_config request.jwt.claims em transação
       ROLLBACK — método das Phases 43/63). Para 2-3 buckets do resultado, comparar contra a
       soma manual direta em `orders` para o mesmo item/dia:
       SUM(receita_bruta) ≡ total; SUM(custo_unit*quantidade) ≡ cmv; SUM(comissao) ≡ comissao;
       SUM(frete) ≡ frete; SUM(tax_amount) ≡ impostos; contagem de qtd com custo_unit NULL ≡ qtd_sem_custo.
       (Rodar como authenticated, NÃO postgres — postgres bypassa RLS e mascara IDOR.)
    4. Anti-IDOR: repetir a chamada da RPC impersonando uma org ALHEIA (ex.: Thales
       `e4150d57-...`) para um item da Pé Vermeio → deve retornar 0 linhas.
    5. (Opcional, calibra expectativa de ads) `SELECT count(*), min(date), max(date) FROM ml_ads_products_cache WHERE organization_id = '<pe-vermeio-org-id>';`
       para saber a cobertura do cache de ads dos itens que o Wesley vai analisar.
  </how-to-verify>
  <resume-signal>
    Confirmar: "migration aplicada em prod; smoke retornou 13 colunas; 2-3 buckets reconciliaram
    (total/cmv/comissao/frete/impostos batem); anti-IDOR = 0 linhas para org alheia." Se algum
    bucket não bater, investigar (status filter, cast ::date) antes de prosseguir.
  </resume-signal>
  <action>
    [ORQUESTRADOR — o gsd-executor não tem MCP claude.ai] Aplicar a migration 79-01 no projeto
    ckcdevcxgvueywivefgx via MCP `apply_migration` (nunca SQL Editor, nunca `supabase db push`),
    depois rodar os passos 2-5 de "how-to-verify" via `execute_sql`: smoke de existência (13 colunas),
    reconciliação de 2-3 buckets como role authenticated (Pé Vermeio) contra soma manual em `orders`,
    e o teste anti-IDOR (org alheia → 0 linhas). Registrar os números no SUMMARY. Bloqueante: só
    liberar o wave 3 (UI, 79-03) após reconciliação e anti-IDOR OK.
  </action>
  <files>supabase/migrations/20260679000000_orders_price_timeseries_mco.sql (aplicada em prod, não editada)</files>
  <verify>Smoke via execute_sql como role authenticated: 13 colunas retornadas; 2-3 buckets ≡ soma manual; org alheia → 0 linhas.</verify>
  <done>Migration aplicada em prod; reconciliação de buckets OK; anti-IDOR = 0 linhas cross-org; números no SUMMARY.</done>
</task>

</tasks>

<artifacts_produced>
- RPC `orders_price_timeseries` em produção (ckcdevcxgvueywivefgx) com 6 colunas novas ativas.
- Evidência de reconciliação (buckets ✓) e anti-IDOR (0 linhas cross-org) registrada no SUMMARY.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador (MCP) → banco prod | aplicação de DDL e execução de SQL de smoke |
| role authenticated (org X) → RPC | leitura agregada de orders sob RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-04 | Elevation of Privilege (IDOR) | orders_price_timeseries em prod | mitigate | smoke anti-IDOR obrigatório: org alheia → 0 linhas, provado como role authenticated (não postgres) |
| T-79-05 | Repudiation/erro silencioso | DDL aplicada | mitigate | reconciliação de 2-3 buckets contra soma manual antes de liberar a UI (79-03) |
| T-79-SC | Tampering | supply chain | accept | nenhum pacote/instalação nesta phase |
</threat_model>

<verification>
- Migration aplicada via MCP apply_migration (não SQL Editor).
- Smoke retorna 13 colunas; 2-3 buckets reconciliam; anti-IDOR = 0 linhas cross-org.
</verification>

<success_criteria>
RPC estendida viva em prod, reconciliada e provadamente isolada por org — pronta para a UI (79-03) consumir dados reais.
</success_criteria>

<output>
Create `.planning/phases/79-analise-de-precos-com-mco/79-02-SUMMARY.md` when done (registrar os números do smoke e o resultado anti-IDOR).
</output>
