---
phase: 79-analise-de-precos-com-mco
plan: 03
type: execute
wave: 3
depends_on: ["79-01", "79-02"]
files_modified:
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
autonomous: false
requirements: ["APM-UI"]
user_setup: []

must_haves:
  truths:
    - "O gráfico mostra linha de preço praticado × linha de break-even (tracejada) em R$/un, eixo esquerdo"
    - "O colchão entre as linhas é verde quando preço ≥ break-even e vermelho quando preço < break-even"
    - "A linha de MCO% aparece no eixo direito"
    - "As barras de volume e o toggle Qtd/Receita foram removidos"
    - "O toggle 'incluir ads' (Switch, default ON) recalcula a série ao alternar"
    - "Os 6 KPIs são Preço médio, Break-even médio, MCO R$, MCO % (verde/vermelho pelo sinal), Qtd vendida, Receita"
    - "Aviso de custo ausente e de regime fiscal não configurado aparecem quando aplicável, sem inventar número"
    - "Gráfico e toggles funcionam em mobile e desktop (paridade — lição Phase 78)"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      provides: "Gráfico preço×break-even com colchão MCO + MCO% + toggle ads + 6 KPIs novos + tooltip decomposto"
      contains: "computePrecoMcoSeries"
      min_lines: 250
  key_links:
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/lib/precoMcoSeries.ts"
      via: "computePrecoMcoSeries(rows, { adsDaily, incluirAds, granularity })"
      pattern: "computePrecoMcoSeries"
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "ml_ads_products_cache"
      via: "query direta RLS-scoped por item_id + date range para o spend diário de ads"
      pattern: "ml_ads_products_cache"
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "orders_price_timeseries (RPC estendida)"
      via: "mapeamento das 6 colunas novas no resultado da RPC"
      pattern: "qtd_sem_custo"
---

<objective>
Refazer o gráfico e os KPIs de `PrecoPraticadoReport.tsx` para responder "o preço praticado
deu MCO?": linha de preço × linha de break-even (R$/un) com colchão verde/vermelho entre
elas + linha de MCO% no eixo direito; toggle "incluir ads"; 6 KPIs novos; tooltip com a
decomposição por unidade; avisos de custo/imposto ausente.

Purpose: transformar dado firme (RPC 79-01/79-02) + cálculo puro (util 79-01) na visão que o
Wesley aprovou. Depende do util (79-01) e da RPC viva em prod (79-02).

Output: componente refeito + checkpoint visual do Wesley em preview.

REGRA OBRIGATÓRIA: ler a skill `dataviz` ANTES de escrever qualquer código de gráfico.
DADO DE ADS: série diária REAL de `ml_ads_products_cache` bucketizada pela granularidade
(NÃO rateio por receita) — conforme CONTEXT.md/spec adendo (fonte de verdade pós-research).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
Branch: `gsd/phase-79-analise-precos-mco`.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/79-analise-de-precos-com-mco/79-CONTEXT.md
@.planning/phases/79-analise-de-precos-com-mco/79-RESEARCH.md
@.planning/phases/79-analise-de-precos-com-mco/79-PATTERNS.md
@src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
@src/lib/precoMcoSeries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar PrecoPraticadoReport — dados (RPC+ads), série MCO, gráfico de bandas, KPIs, tooltip, avisos</name>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx</files>
  <read_first>
    - Skill `dataviz` (OBRIGATÓRIO antes de qualquer código de chart — regra do repo)
    - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx (versão atual — preservar a casca: fetch com cancelled-guard, estados vazios/loading, seletor de anúncio, granularidade, grid de KPIs)
    - src/lib/precoMcoSeries.ts (computePrecoMcoSeries + tipos — assinatura do que consumir)
    - .planning/phases/79-analise-de-precos-com-mco/79-RESEARCH.md (Pattern 2 — técnica da banda/colchão com código Recharts completo linhas ~300-372; query de ads linhas ~594-612; Pattern 3 KPI por sinal)
    - .planning/phases/79-analise-de-precos-com-mco/79-PATTERNS.md (seção do componente — imports a adicionar, mapeamento RPC, Switch, KPI variant, substituição do chart, tooltip, rodapé)
    - src/components/dashboard/KPICard.tsx (variant success/danger, size="compact")
    - src/components/mercadolivre/ReplenishmentPanel.tsx (linhas ~135-144 — padrão Switch + Label)
  </read_first>
  <action>
    Refatorar o componente inteiro (edição estrutural, preservando a casca de fetch/estados):

    DADOS:
    - Estender o `SeriesRow` local (ou trocar por `PrecoSeriesRow` importado) para incluir as 6
      colunas novas da RPC: cmv, comissao, frete, qtd_sem_custo, impostos, qtd_sem_imposto.
      Mapear esses campos no `.map((r:any)=>...)` do resultado da RPC (Number(r.x ?? 0)).
      Manter o comportamento de erro atual (console.warn + setRows([])).
    - Adicionar uma busca do spend diário de ads: query direta a `ml_ads_products_cache`
      selecionando `spend, date`, `.eq("item_id", selectedId)`, `.in("ml_user_id", mlUserIds)`,
      `.gte("date", fromDate)`, `.lte("date", toDate)`. RLS já restringe por org (is_org_member) —
      sem filtro explícito de organization_id. Guardar como `adsDaily: {date,spend}[]` em estado.
      Cobertura ausente → array vazio → ads=0 (silencioso). Fazer no mesmo useEffect (chaveado por
      selectedId/mlUserIds/fromDate/toDate) ou um sibling useEffect com o mesmo cancelled-guard.
      NÃO chavear pela granularidade (a bucketização de ads é feita no util) para evitar refetch à toa.
    - Adicionar estado `incluirAds` (boolean, default true).
    - Computar a série via `computePrecoMcoSeries(rows ?? [], { adsDaily, incluirAds, granularity })`
      num useMemo; derivar `chartData` = série + label (bucketLabel existente).
    - REMOVER o estado/uso de `volumeMetric` (o toggle Qtd/Receita e a Bar saem).

    KPIs (6, mesmo grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6):
    - Preço médio = receitaTotal/qtdTotal. Break-even médio = (Σcmv+Σcomissao+Σfrete+ΣadsBucket+Σimpostos)/qtdTotal
      (ads só entra se incluirAds). MCO R$ e MCO % via computeMco sobre os totais do período
      (grossRevenue=receitaTotal, cmv=Σcmv, platformCost=Σcomissao+Σfrete, ads=ΣadsBucket se incluirAds senão 0,
      tax=Σimpostos) — reconcilia com a série. MCO % com KPICard variant success quando ≥0, danger quando <0.
      Qtd vendida = Σqtd. Receita = Σtotal. REMOVER: Faixa de preço, Média diária (Qtd), Receita média diária.

    GRÁFICO (ComposedChart, seguir a skill dataviz + Pattern 2 do RESEARCH):
    - Eixo esquerdo yAxisId="preco" (R$/un); eixo direito yAxisId="mco" orientation="right" (%).
    - 3 Areas empilhadas no yAxisId="preco" com stackId comum: `base` (fill transparent, stroke none),
      `gainBand` (fill hsl(var(--success)) fillOpacity ~0.25), `lossBand` (fill hsl(var(--destructive))
      fillOpacity ~0.25). Todas com `type="linear"` e `isAnimationActive={false}`.
    - 2 Lines no yAxisId="preco": `precoUnit` (stroke accent, sólida) e `breakevenUnit` (stroke
      muted-foreground, `strokeDasharray="5 4"`, dot={false}). Ambas `type="linear"`.
    - 1 Line no yAxisId="mco": `mcoPct` (stroke primary). type pode ser monotone.
    - Usar tokens de cor via hsl(var(--...)); detalhes de opacidade/gradiente ficam a critério
      seguindo dataviz. Preservar ResponsiveContainer/CartesianGrid/XAxis wrapper.

    TOOLTIP: substituir o conteúdo pela decomposição por unidade — Preço, Break-even, MCO R$/un
    (mco/qtd do bucket ou precoUnit-breakevenUnit), MCO %, e por unidade: custo (cmvUnit),
    comissão (comissaoUnit), frete (freteUnit), ads (adsUnit), imposto (impostoUnit). Transparência total.

    AVISOS: quando algum bucket exibido tem custoAusente, mostrar aviso "custo ausente em N un —
    break-even subestimado" (N = Σ qtd_sem_custo dos buckets); quando algum tem impostoAusente,
    mostrar "regime fiscal não configurado" (não inventar número). Colocar como texto discreto
    perto do rodapé/gráfico.

    RODAPÉ: trocar a legenda por (ajustada ao dado real de ads): "Ads = relatório diário de
    publicidade (melhor esforço; ausente = 0) · imposto pelo regime configurado · linha tracejada
    = break-even". (Nota: o CONTEXT.md trava a intenção; o texto exato de ads reflete o adendo
    pós-research — série diária real, não rateio.)

    TOGGLE "incluir ads": Switch + Label (padrão ReplenishmentPanel), default ON, ao lado dos
    controles (granularidade). Alternar recomputa a série (dep do useMemo).

    PARIDADE MOBILE (lição Phase 78): garantir que gráfico, toggle de ads e granularidade sejam
    utilizáveis nos dois tamanhos — sem controle escondido/desktop-only. O componente é renderizado
    uma vez em MLAnalisePrecos (não há ramo mobile/desktop separado); manter os controles com wrap
    responsivo (flex-wrap) e o Switch acessível no mobile.

    Preservar: seletor de anúncio, granularidade dia/semana/mês, estados de loading/sem-seleção/sem-dados.
  </action>
  <verify>
    <automated>npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "computePrecoMcoSeries" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` ≥ 1.
    - `grep -c "ml_ads_products_cache" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` ≥ 1.
    - `grep -c "gainBand" src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` ≥ 1 e `grep -c "lossBand" ...` ≥ 1.
    - `grep -c "breakevenUnit" ...` ≥ 1 e `grep -c "mcoPct" ...` ≥ 1.
    - `grep -c "Switch" ...` ≥ 1 (toggle incluir ads).
    - `grep -c "volumeMetric" ...` retorna 0 (toggle Qtd/Receita removido).
    - As Areas de banda usam `type="linear"` (não monotone): `grep -c 'type="monotone"' ...` não deve aparecer nas Areas de banda (mcoPct pode ser monotone).
    - `npm run build` verde e `npx vitest run` verde (toda a suíte, incl. precoMcoSeries.test.ts).
    - (NÃO usar `npx tsc -p tsconfig.app.json` como gate — erros pré-existentes.)
  </acceptance_criteria>
  <done>Componente refeito: gráfico preço×break-even com colchão + MCO% + toggle ads + 6 KPIs + tooltip decomposto + avisos; build e testes verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual do Wesley em preview</name>
  <what-built>
    O gráfico da Análise de Preços refeito (colchão verde/vermelho entre preço e break-even,
    MCO% no eixo direito, toggle incluir ads, 6 KPIs novos, tooltip decomposto) na branch
    `gsd/phase-79-analise-precos-mco`.
  </what-built>
  <how-to-verify>
    1. Publicar preview Vercel da branch (padrão do projeto: branch → preview apontando p/ Supabase prod).
    2. Wesley abre `/analise-precos` logado (dados reais Pé Vermeio), seleciona um anúncio com vendas.
    3. Conferir: colchão verde quando o preço está acima do break-even e vermelho quando afunda;
       linha tracejada = break-even; linha de MCO% no eixo direito acompanha o colchão.
    4. Alternar "incluir ads" ON/OFF → o break-even e o MCO% mudam de forma coerente (ads entra/sai).
    5. Trocar granularidade dia/semana/mês → buckets e ads se realinham.
    6. Passar o mouse no tooltip → ver a decomposição por unidade (custo, comissão, frete, ads, imposto).
    7. Conferir os 6 KPIs (Preço médio, Break-even médio, MCO R$, MCO % verde/vermelho, Qtd, Receita).
    8. Testar em mobile (aparelho real ou responsive) — gráfico e toggles utilizáveis (lição Phase 78).
    9. Se houver item com custo/imposto ausente, ver o aviso correspondente (sem número inventado).
  </how-to-verify>
  <resume-signal>Wesley digita "aprovado" ou descreve ajustes (cor/opacidade do colchão, formato do tooltip, cópia do rodapé).</resume-signal>
  <action>
    [ORQUESTRADOR + WESLEY] Publicar preview Vercel da branch `gsd/phase-79-analise-precos-mco`
    (padrão do projeto: branch → preview apontando p/ Supabase prod) e conduzir os passos 2-9 de
    "how-to-verify" com o Wesley logado em `/analise-precos`. Coletar aprovação ou lista de ajustes;
    aplicar ajustes na Task 1 se houver e repetir o preview.
  </action>
  <files>src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx (revisão visual, sem edição durante o checkpoint salvo ajustes solicitados)</files>
  <verify>Wesley confirma em preview: colchão verde/vermelho coerente, toggle ads recalcula, tooltip decomposto, 6 KPIs, paridade mobile.</verify>
  <done>Checkpoint visual aprovado pelo Wesley (ou ajustes aplicados e reaprovados).</done>
</task>

</tasks>

<artifacts_produced>
- `src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx` refeito:
  - Consome `computePrecoMcoSeries` (util do 79-01) e a RPC estendida (79-02).
  - Nova query a `ml_ads_products_cache` (spend diário por item).
  - Estado/flag de UI `incluirAds` (Switch default ON).
  - Gráfico: 3 Areas (base/gainBand/lossBand) + 2 Lines (precoUnit/breakevenUnit) + 1 Line mcoPct (eixo direito).
  - 6 KPIs novos; tooltip decomposto; avisos de custo/imposto ausente; rodapé atualizado.
  - Removidos: `volumeMetric`, Bar de volume, toggle Qtd/Receita, KPIs de faixa/média diária.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client autenticado → ml_ads_products_cache | query direta do spend diário por item |
| client autenticado → orders_price_timeseries | RPC de série de preço/custos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-06 | Information Disclosure | query direta ml_ads_products_cache | mitigate | RLS org-first (is_org_member) já ativa na tabela; sem filtro de org no client — RLS isola; nenhum dado de ads de outra org retorna |
| T-79-07 | Elevation of Privilege (IDOR) | RPC via UI | mitigate | RPC SECURITY INVOKER (79-01/79-02); item_id/ml_user_id vêm do estado da app já escopado por org |
| T-79-SC | Tampering | supply chain | accept | nenhum pacote novo (recharts/date-fns/shadcn já no projeto) |
</threat_model>

<verification>
- `npm run build` verde; `npx vitest run` verde (suíte inteira).
- Greps de acceptance da Task 1 satisfeitos.
- Checkpoint visual do Wesley aprovado.
</verification>

<success_criteria>
A Análise de Preços responde visualmente "o preço deu MCO?" com colchão preço×break-even, MCO%, toggle de ads e KPIs firmes, aprovado pelo Wesley em preview, com paridade mobile/desktop.
</success_criteria>

<output>
Create `.planning/phases/79-analise-de-precos-com-mco/79-03-SUMMARY.md` when done.
</output>
