---
phase: 23-dashboard-granular
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useDashboardLayout.ts
  - src/hooks/useMLProductMargins.ts
  - src/components/mercadolivre/MLTopProducts.tsx
  - src/components/mercadolivre/CustoOperacionalChart.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Top Anúncios tem coluna 'Margem' com %, calculada de orders.custo_unit × quantidade vs receita — exibe '—' se sem custo"
    - "CustoOperacionalChart exibe duas linhas: R$ (eixo Y esquerdo) e % da receita (eixo Y direito)"
    - "Tooltip do CustoOperacional mostra R$ e % simultaneamente"
    - "Personalizar lista 8 widgets individuais — BrandRevenueChart, BrandMarkupChart, CustoOperacionalChart e BrandSharePieChart são toggles separados"
    - "Não há seção 'Análises Detalhadas' ou 'Gráficos de Marca' como grupo fixo — cada card renderiza individualmente"
    - "Configuração de layout salva em ml_dashboard_layout_v2 (não conflita com v1)"
  artifacts:
    - path: "src/hooks/useMLProductMargins.ts"
      provides: "Hook React Query que retorna Map<item_id, margin_pct> para o período filtrado"
    - path: "src/hooks/useDashboardLayout.ts"
      provides: "8 widgets individuais, storage key v2"
    - path: "src/components/mercadolivre/MLTopProducts.tsx"
      provides: "Coluna Margem com fallback '—' para produtos sem custo"
    - path: "src/components/mercadolivre/CustoOperacionalChart.tsx"
      provides: "Dual-axis chart: R$ esquerdo, % direito; tooltip dual"
    - path: "src/pages/MercadoLivre.tsx"
      provides: "Renderização granular por widget; dailyRevenue passado ao CustoOperacionalChart; marginMap passado ao MLTopProducts"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useMLProductMargins.ts"
      via: "useMLProductMargins(currentFrom, currentTo) → marginMap passado como prop"
      pattern: "useMLProductMargins"
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/components/mercadolivre/CustoOperacionalChart.tsx"
      via: "dailyRevenue={allDaily.map(d => ({ date: d.date, revenue: d.approved_revenue ?? 0 }))}"
      pattern: "dailyRevenue"
    - from: "src/hooks/useDashboardLayout.ts"
      to: "src/pages/MercadoLivre.tsx"
      via: "8 widget IDs individuais; widget.id switch no render loop"
      pattern: "ml_dashboard_layout_v2"
---

<objective>
Três melhorias na página de Vendas: (1) coluna Margem Média no Top Anúncios calculada de orders reais,
(2) dual-axis no Custo Operacional Diário mostrando R$ e % por dia, (3) granularidade total no Personalizar
— cada card é um toggle independente, sem agrupamentos fixos.

Purpose: Dar ao usuário análise de margem por produto no período filtrado, visualização de eficiência de custo
relativa à receita, e controle fino de layout sem necessitar ver blocos inteiros.

Output:
- Hook `useMLProductMargins` (query Supabase, Map<item_id, margin_pct>)
- `MLTopProducts` com coluna Margem
- `CustoOperacionalChart` com eixo Y duplo
- `useDashboardLayout` com 8 widgets individuais (key v2)
- `MercadoLivre.tsx` renderizando cada widget granularmente
</objective>

## Tarefas

### Tarefa 1 — Hook `useMLProductMargins` (novo arquivo)

**Arquivo:** `src/hooks/useMLProductMargins.ts`

Criar hook React Query que consulta `orders` agrupando por `item_id` e calcula margem bruta:

```typescript
import { useQuery } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
import { useMLStore } from "@/contexts/MLStoreContext";
import { useOrganization } from "@/contexts/OrganizationContext";

const PAID_STATUSES = ["paid", "shipped", "delivered"];

export function useMLProductMargins(dateFrom: string, dateTo: string) {
  const { resolvedMLUserIds } = useMLStore();
  const { currentOrg } = useOrganization();

  return useQuery({
    queryKey: ["ml_product_margins", currentOrg?.id, resolvedMLUserIds, dateFrom, dateTo],
    queryFn: async (): Promise<Map<string, number>> => {
      if (!currentOrg?.id || !resolvedMLUserIds.length) return new Map();

      const { data, error } = await supabase
        .from("orders")
        .select("item_id, receita_bruta, custo_unit, quantidade, comissao, frete")
        .eq("organization_id", currentOrg.id)
        .in("ml_user_id", resolvedMLUserIds)
        .in("status", PAID_STATUSES)
        .gte("data_pedido", dateFrom)
        .lte("data_pedido", dateTo)
        .not("item_id", "is", null)
        .not("custo_unit", "is", null);

      if (error) throw error;

      // Agrupa por item_id
      const acc = new Map<string, { receita: number; lucro: number }>();
      for (const r of data ?? []) {
        const receita = r.receita_bruta ?? 0;
        const custo = (r.custo_unit ?? 0) * (r.quantidade ?? 1);
        const comissao = r.comissao ?? 0;
        const frete = r.frete ?? 0;
        const lucro = receita - custo - comissao - frete;
        const existing = acc.get(r.item_id) ?? { receita: 0, lucro: 0 };
        acc.set(r.item_id, {
          receita: existing.receita + receita,
          lucro: existing.lucro + lucro,
        });
      }

      const result = new Map<string, number>();
      for (const [id, { receita, lucro }] of acc) {
        if (receita > 0) result.set(id, (lucro / receita) * 100);
      }
      return result;
    },
    enabled: !!currentOrg?.id && resolvedMLUserIds.length > 0,
    staleTime: 2 * 60 * 1000,
  });
}
```

---

### Tarefa 2 — Coluna Margem em `MLTopProducts`

**Arquivo:** `src/components/mercadolivre/MLTopProducts.tsx`

Adicionar prop `marginMap?: Map<string, number>` e nova coluna na tabela:

1. Adicionar ao `MLTopProductsProps`:
   ```typescript
   marginMap?: Map<string, number>;
   ```

2. Adicionar ao cabeçalho após `% Part.`:
   ```tsx
   <span className="w-16 text-right">Margem</span>
   ```

3. Na linha de cada produto, após o span de `% Part.`:
   ```tsx
   {(() => {
     const margin = marginMap?.get(p.item_id);
     if (margin == null) return <span className="w-16 text-right text-[10px] text-muted-foreground">—</span>;
     const positive = margin >= 0;
     return (
       <span className={`w-16 text-right text-xs font-semibold tabular-nums ${positive ? "text-emerald-500" : "text-red-500"}`}>
         {margin.toFixed(1)}%
       </span>
     );
   })()}
   ```

4. Exportar `MLTopProducts` com `marginMap` opcional — componente funciona normalmente sem ele.

---

### Tarefa 3 — Dual-axis em `CustoOperacionalChart`

**Arquivo:** `src/components/mercadolivre/CustoOperacionalChart.tsx`

Adicionar prop `dailyRevenue?: Array<{ date: string; revenue: number }>` e segundo eixo Y:

1. Adicionar ao `CustoOperacionalChartProps`:
   ```typescript
   dailyRevenue?: Array<{ date: string; revenue: number }>;
   ```

2. No build de `chartData`, adicionar campo `pct_custo`:
   ```typescript
   const revenueMap = new Map((dailyRevenue ?? []).map((d) => [d.date, d.revenue]));
   const chartData = custoSeries.map((d) => {
     const custo = Math.round((d.custo_plataforma + (adsMap.get(d.date) ?? 0)) * 100) / 100;
     const receita = revenueMap.get(d.date) ?? 0;
     const pct_custo = receita > 0 ? Math.round((custo / receita) * 1000) / 10 : null;
     return {
       label: d.date.slice(5).replace("-", "/"),
       date: d.date,
       custo_total: custo,
       pct_custo,
     };
   });
   ```

3. Adicionar ao `chartConfig`:
   ```typescript
   pct_custo: { label: "% da Receita", color: "hsl(220, 70%, 50%)" },
   ```

4. No `LineChart`, adicionar `YAxis` direito e linha de %:
   ```tsx
   <YAxis
     yAxisId="right"
     orientation="right"
     tick={{ fontSize: 10 }}
     tickLine={false}
     axisLine={false}
     tickFormatter={(v) => `${v}%`}
     width={36}
     domain={[0, 'auto']}
   />
   // YAxis existente recebe yAxisId="left"
   // Line existente recebe yAxisId="left"
   <Line
     yAxisId="right"
     type="monotone"
     dataKey="pct_custo"
     stroke="hsl(220, 70%, 50%)"
     strokeWidth={1.5}
     strokeDasharray="4 2"
     dot={false}
     connectNulls
   />
   ```

5. Atualizar Tooltip para mostrar ambos:
   ```tsx
   <div className="flex items-center justify-between gap-4 text-xs">
     <span className="text-muted-foreground">Custo Operacional</span>
     <span className="font-mono font-medium">{currencyFmt(Number(payload[0]?.value))}</span>
   </div>
   {payload[1]?.value != null && (
     <div className="flex items-center justify-between gap-4 text-xs">
       <span className="text-muted-foreground">% da Receita</span>
       <span className="font-mono font-medium">{Number(payload[1].value).toFixed(1)}%</span>
     </div>
   )}
   ```

---

### Tarefa 4 — `useDashboardLayout` com 8 widgets individuais

**Arquivo:** `src/hooks/useDashboardLayout.ts`

Substituir `DEFAULT_WIDGETS` e bumpar storage key:

```typescript
const DEFAULT_WIDGETS: WidgetConfig[] = [
  { id: "kpi_grid",         label: "KPIs de Vendas",           description: "Cards com receita, pedidos, ticket, visitas e mais",    visible: true },
  { id: "revenue_chart",    label: "Gráfico de Receita",       description: "Evolução diária ou horária da receita + metas",          visible: true },
  { id: "cost_waterfall",   label: "Custos & Top Anúncios",    description: "Waterfall financeiro e produtos mais vendidos",          visible: true },
  { id: "brand_revenue",    label: "Receita por Marca",        description: "Faturamento diário por marca ao longo do período",       visible: true },
  { id: "brand_markup",     label: "Markup por Marca",         description: "Evolução do markup médio por marca",                    visible: true },
  { id: "operational_cost", label: "Custo Operacional Diário", description: "Frete + Comissão + Publicidade em R$ e % da receita",   visible: true },
  { id: "brand_share",      label: "Share de Marca",           description: "Participação de receita e volume por marca",            visible: true },
  { id: "analytics",        label: "Análises Detalhadas",      description: "Venda por hora, ticket médio, estados e funil",         visible: true },
];

const STORAGE_KEY = "ml_dashboard_layout_v2";
```

A função `loadLayout` já faz merge correto — novos IDs (`brand_revenue`, `brand_markup`, `operational_cost`, `brand_share`) serão adicionados aos layouts existentes que não os têm.

---

### Tarefa 5 — `MercadoLivre.tsx`: renderização granular + dados

**Arquivo:** `src/pages/MercadoLivre.tsx`

**5a. Imports e hooks novos:**
```typescript
import { useMLProductMargins } from "@/hooks/useMLProductMargins";
// BrandRevenueChart, BrandMarkupChart, CustoOperacionalChart, BrandSharePieChart
// já importados via brand_charts
```

Adicionar chamada do hook:
```typescript
const { data: marginMap } = useMLProductMargins(currentFrom, currentTo);
```

**5b. Construir `dailyRevenue` para o chart:**
```typescript
const dailyRevenue = useMemo(
  () => effectiveDailyRows.map((d) => ({ date: d.date, revenue: d.approved_revenue ?? 0 })),
  [effectiveDailyRows],
);
```
*(Se `effectiveDailyRows` não existir com esse nome, usar o array daily filtrado equivalente já presente na página.)*

**5c. Substituir o bloco de renderização de widgets:**

Remover os blocos `if (widget.id === "brand_charts")` e `if (widget.id === "analytics")`.
Adicionar cases individuais:

```typescript
if (widget.id === "brand_revenue") return (
  <BrandRevenueChart
    key="brand_revenue"
    data={brandData?.brandRevenueSeries ?? []}
    topBrands={brandData?.topBrands ?? []}
    loading={brandLoading}
  />
);
if (widget.id === "brand_markup") return (
  <BrandMarkupChart
    key="brand_markup"
    data={brandData?.brandMarkupSeries ?? []}
    topBrands={brandData?.topBrands ?? []}
    loading={brandLoading}
  />
);
if (widget.id === "operational_cost") return (
  <CustoOperacionalChart
    key="operational_cost"
    custoSeries={brandData?.custoSeries ?? []}
    adsDaily={adsDaily}
    dailyRevenue={dailyRevenue}
    loading={brandLoading}
  />
);
if (widget.id === "brand_share") return (
  <BrandSharePieChart
    key="brand_share"
    brandAggregates={brandData?.brandAggregates ?? []}
    loading={brandLoading}
  />
);
if (widget.id === "analytics") return (
  <MLSalesAnalytics key="analytics" from={currentFrom} to={currentTo} />
);
```

**5d. Passar `marginMap` ao `MLTopProducts`:**
```tsx
<MLTopProducts products={effectiveProducts} marginMap={marginMap} />
```

**5e. Verificar nome correto da variável de daily rows filtradas:**
- Em MercadoLivre.tsx procurar como `effectiveDailyRows` ou equivalente está definido (pode ser `filteredDailyRows`, `currentDailyRows` etc.)
- Usar o array correto que corresponde ao período `currentFrom/currentTo` filtrado por loja

---

## Verificação

Após implementação, confirmar:
- [ ] Top Anúncios mostra coluna "Margem" — produtos com custo têm %, sem custo mostram "—"
- [ ] CustoOperacionalChart tem duas linhas visivelmente distintas (tracejada = %)
- [ ] Tooltip exibe "Custo Operacional R$X" e "% da Receita Y%"
- [ ] Painel Personalizar lista 8 itens separados (não 5)
- [ ] Ocultar "Receita por Marca" não oculta "Markup por Marca" — toggles independentes
- [ ] `tsc --noEmit` sem erros
- [ ] Build (`npm run build`) sem erros
