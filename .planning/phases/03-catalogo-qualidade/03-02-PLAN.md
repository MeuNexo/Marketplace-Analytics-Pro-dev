---
phase: 03-catalogo-qualidade
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/pages/mercadolivre/MLProdutos.tsx
autonomous: true
requirements:
  - CATALOG-01
  - CATALOG-02
  - CATALOG-03
must_haves:
  truths:
    - "Impostos column shows R$ X,XX (Y,Y%) for products whose store has a fiscal config"
    - "Impostos column shows the manual tax_rate fallback when no fiscal config exists for that store"
    - "Impostos column shows — when neither fiscal config nor manual tax_rate is available"
    - "Variation rows show tax computed from parent item's store and variation price"
    - "Amber banner appears when at least one active store has no ml_tax_config"
    - "Banner contains a Link to /fiscal"
    - "Impostos column header tooltip shows the disclaimer text"
    - "marginLiq calculation uses effectiveTaxRate (not stale taxRate)"
    - "InlineEditCell is removed from the Impostos cell — column is read-only"
  artifacts:
    - path: "src/pages/mercadolivre/MLProdutos.tsx"
      provides: "Updated catalog table with fiscal-aware Impostos column and banner"
      contains: "useMLTaxConfig"
  key_links:
    - from: "src/pages/mercadolivre/MLProdutos.tsx"
      to: "@/hooks/useMLTaxConfig"
      via: "named import + hook call"
      pattern: "useMLTaxConfig"
    - from: "src/pages/mercadolivre/MLProdutos.tsx"
      to: "@/contexts/OrganizationContext"
      via: "named import + hook call"
      pattern: "useOrganization"
    - from: "effectiveTaxRate"
      to: "taxMap.get(item._ml_user_id)"
      via: "taxEntry?.effective_rate with productCost?.tax_rate fallback"
      pattern: "effectiveTaxRate"
---

<objective>
Edit MLProdutos.tsx to display fiscal-regime-derived tax amounts in the Impostos column, add an amber banner for unconfigured stores, and update the column header tooltip.

Purpose: Surface the fiscal configuration from Phase 2 directly in the product catalog so users see tax estimates without manual entry. The InlineEditCell is replaced with a read-only display; the manual tax_rate becomes a silent fallback.
Output: Updated MLProdutos.tsx where the Impostos column is driven by useMLTaxConfig with a manual fallback.
</objective>

<execution_context>
@C:/Users/thales.martins/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/thales.martins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc/.planning/ROADMAP.md
@C:/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc/.planning/phases/03-catalogo-qualidade/03-01-SUMMARY.md

<interfaces>
From src/lib/tax/index.ts (created in Plan 01):
  export type TaxRegime = "simples_nacional" | "lucro_presumido" | "lucro_real"
  export interface TaxInput { regime: TaxRegime; sn_aliquota_efetiva?: number | null; ... }
  export function calculateEffectiveRate(input: TaxInput): number
  export function clampEffectiveRate(rate: number): number
  export function computeTaxAmount(price: number, effectiveRatePct: number): number

From src/pages/mercadolivre/MLProdutos.tsx (verified in codebase):
  Line 29 — AlertCircle already imported from lucide-react
  Line 33 — Link already imported from react-router-dom
  Line 36 — Tooltip, TooltipContent, TooltipTrigger already imported
  Line 69 — const currencyFmt = (v: number) => v.toLocaleString("pt-BR", { style: "currency", currency: "BRL" })
  Line 546 — const { selectedStore, stores, sellerId, resolvedMLUserIds, scopeKey } = useMLStore()
  Line 1013 — KPI grid opens: <div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
  Line 1026 — KPI grid closes: </div>
  Line 1029 — Filters Card opens: <Card>
  Line 1141–1147 — Impostos column header with Tooltip
  Line 1232–1243 — per-row IIFE with taxRate derivation
  Line 1255–1261 — Impostos cell (main row) with InlineEditCell
  Line 1381–1396 — variation IIFE with taxRate + two "↑ item" cells

Hooks available (from Phase 2):
  import { useMLTaxConfig } from "@/hooks/useMLTaxConfig"
  useMLTaxConfig(mlUserIds: string[], orgId: string) → { data: Map<string, { effective_rate: number }> | null }

  import { useOrganization } from "@/contexts/OrganizationContext"
  useOrganization() → { currentOrg: { id: string } | null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3: Add imports and hook calls to MLProdutos.tsx</name>
  <files>src/pages/mercadolivre/MLProdutos.tsx</files>
  <action>
    Apply three surgical edits to MLProdutos.tsx. Use the Edit tool (old_string / new_string) for each sub-step. Do not modify any other lines.

    SUB-STEP 3a — Add missing imports after the existing import block.
    Find the exact line (verified at line 37):
      import { useMLProductCosts } from "@/hooks/useMLProductCosts";
    Replace with:
      import { useMLProductCosts } from "@/hooks/useMLProductCosts";
      import { useOrganization } from "@/contexts/OrganizationContext";
      import { useMLTaxConfig } from "@/hooks/useMLTaxConfig";

    SUB-STEP 3b — Add hook calls and derived constant after the useMLStore() call.
    Find the exact line (verified at line 546):
      const { selectedStore, stores, sellerId, resolvedMLUserIds, scopeKey } = useMLStore();
    Replace with:
      const { selectedStore, stores, sellerId, resolvedMLUserIds, scopeKey } = useMLStore();
      const { currentOrg } = useOrganization();
      const orgId = currentOrg?.id ?? null;
      const { data: taxMap } = useMLTaxConfig(resolvedMLUserIds, orgId ?? "");
      const showTaxBanner = !!taxMap && stores.some((s) => !taxMap.has(s.ml_user_id));

    Verify after this step: the file compiles without "useOrganization is not defined" or "useMLTaxConfig is not defined" errors.
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc'; npx tsc --noEmit 2>&1 | Select-String 'useOrganization|useMLTaxConfig|taxMap|showTaxBanner'"</automated>
  </verify>
  <done>No TypeScript errors referencing useOrganization, useMLTaxConfig, taxMap, or showTaxBanner</done>
</task>

<task type="auto">
  <name>Task 4: Update per-row tax derivation, main Impostos cell, variation cells, tooltip, and banner</name>
  <files>src/pages/mercadolivre/MLProdutos.tsx</files>
  <action>
    Apply five surgical edits in order. Use the Edit tool for each sub-step with exact old_string matches (verified in codebase).

    SUB-STEP 4a — Update per-row tax derivation in the main-row IIFE (line ~1235).
    old_string (exact, verified):
              const taxRate = productCost?.tax_rate ?? null;
    new_string:
              const taxEntry = item._ml_user_id ? taxMap?.get(item._ml_user_id) : undefined;
              const effectiveTaxRate = taxEntry != null
                ? Math.max(0, taxEntry.effective_rate)
                : (productCost?.tax_rate ?? null);

    Also update the taxAmount line immediately below (line ~1239).
    old_string (exact, verified):
              const taxAmount = taxRate != null ? item.price * (taxRate / 100) : null;
    new_string:
              const taxAmount = effectiveTaxRate != null ? item.price * (effectiveTaxRate / 100) : null;

    SUB-STEP 4b — Replace the main-row Impostos cell (lines ~1255–1261). Replace the entire InlineEditCell block.
    old_string (exact, verified):
                                  <TableCell className="text-right" onClick={(e) => e.stopPropagation()}>
                                    <InlineEditCell
                                      value={taxRate}
                                      format="percent"
                                      onSave={async (v) => { const prev = costs.get(item.id); await upsertCost(item.id, prev?.cost ?? null, v); }}
                                    />
                                  </TableCell>
    new_string:
                                  <TableCell className="text-right" onClick={(e) => e.stopPropagation()}>
                                    {effectiveTaxRate != null ? (
                                      <span className="text-xs font-mono tabular-nums">
                                        {currencyFmt(item.price * (effectiveTaxRate / 100))}{" "}
                                        ({(effectiveTaxRate).toLocaleString("pt-BR", { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%)
                                      </span>
                                    ) : (
                                      <span className="text-xs text-muted-foreground/40">—</span>
                                    )}
                                  </TableCell>

    SUB-STEP 4c — Update variation rows: replace taxRate derivation and the two "↑ item" Impostos cells.

    In the variation IIFE (line ~1384), update the taxRate derivation.
    old_string (exact, verified):
                                              const taxRate = productCost?.tax_rate ?? null;
    new_string (uses parent item._ml_user_id per architecture decision 7):
                                              const taxEntry = item._ml_user_id ? taxMap?.get(item._ml_user_id) : undefined;
                                              const effectiveTaxRate = taxEntry != null
                                                ? Math.max(0, taxEntry.effective_rate)
                                                : (productCost?.tax_rate ?? null);

    Also update the variation taxAmount line (line ~1388).
    old_string (exact, verified):
                                              const taxAmount = taxRate != null ? v.price * (taxRate / 100) : null;
    new_string:
                                              const taxAmount = effectiveTaxRate != null ? v.price * (effectiveTaxRate / 100) : null;

    Replace the two "↑ item" cells in the variation return block (lines ~1395–1396).
    old_string (exact, verified — both cells on consecutive lines):
                                                  <TableCell className="py-2 text-right text-xs text-muted-foreground italic">↑ item</TableCell>
                                                  <TableCell className="py-2 text-right text-xs text-muted-foreground italic">↑ item</TableCell>
    new_string (first cell is Cost — stays as ↑ item; second cell is Impostos — replaced with computed value):
                                                  <TableCell className="py-2 text-right text-xs text-muted-foreground italic">↑ item</TableCell>
                                                  <TableCell className="py-2 text-right">
                                                    {effectiveTaxRate != null ? (
                                                      <span className="text-xs font-mono tabular-nums text-muted-foreground">
                                                        {currencyFmt(v.price * (effectiveTaxRate / 100))}{" "}
                                                        ({(effectiveTaxRate).toLocaleString("pt-BR", { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%)
                                                      </span>
                                                    ) : (
                                                      <span className="text-xs text-muted-foreground/40">—</span>
                                                    )}
                                                  </TableCell>

    SUB-STEP 4d — Update Impostos column header tooltip (line ~1146). Replace tooltip content text only.
    old_string (exact, verified):
                              <TooltipContent className="text-xs max-w-[200px]">Alíquota de imposto sobre a receita (ex: 15%). Clique para editar.</TooltipContent>
    new_string:
                              <TooltipContent className="text-xs max-w-[200px]">Estimativa baseada no regime tributário configurado em Fiscal. Não considere créditos de entrada. Consulte seu contador.</TooltipContent>

    SUB-STEP 4e — Add amber banner between the KPI grid close tag and the Filters Card open tag.
    old_string (exact, verified — lines 1026–1029 including the comment):
        </div>

        {/* Filters + Table */}
        <Card>
    new_string:
        </div>

        {showTaxBanner && (
          <div className="rounded-xl border border-amber-500/20 bg-amber-500/5 p-4 flex items-start gap-3">
            <AlertCircle className="w-5 h-5 text-amber-600 shrink-0 mt-0.5" />
            <div>
              <p className="text-sm font-medium text-amber-800">
                Regime tributário não configurado
              </p>
              <p className="text-xs text-amber-700 mt-1">
                Uma ou mais contas não têm regime configurado. A coluna Impostos pode não refletir os valores corretos.{" "}
                <Link to="/fiscal" className="underline font-medium">
                  Configurar agora →
                </Link>
              </p>
            </div>
          </div>
        )}

        {/* Filters + Table */}
        <Card>

    After all five sub-steps, run tsc --noEmit to confirm zero errors.
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc'; npx tsc --noEmit 2>&1; exit $LASTEXITCODE"</automated>
  </verify>
  <done>
    - tsc --noEmit exits 0
    - MLProdutos.tsx contains "effectiveTaxRate" (grep confirms)
    - MLProdutos.tsx contains "showTaxBanner" (grep confirms)
    - MLProdutos.tsx contains "Estimativa baseada no regime" (grep confirms)
    - MLProdutos.tsx does NOT contain the old tooltip text "Clique para editar" on the Impostos header
    - MLProdutos.tsx does NOT contain InlineEditCell in the Impostos cell
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| taxMap (hook data) → render | Data from useMLTaxConfig crosses into JSX display logic |
| effective_rate → display | DB-computed rate rendered directly; could be null or negative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | effective_rate display | mitigate | Math.max(0, taxEntry.effective_rate) clamps negative lucro_real rates to 0 before display — matches clampEffectiveRate from index.ts |
| T-03-04 | Information Disclosure | showTaxBanner | accept | Banner reveals which stores lack fiscal config; this is intentional UX for the account owner only — no external exposure |
| T-03-05 | Denial of Service | taxMap?.get() in render | accept | Map lookup is O(1); called once per rendered row; no risk of blocking |
| T-03-06 | Spoofing | Link to="/fiscal" | accept | Internal React Router link; no external URL involved |
</threat_model>

<verification>
- npx tsc --noEmit exits 0 from project root
- npx vitest run src/lib/tax/index.test.ts exits 0 with 15/15 passing (from Plan 01)
- Grep confirms "effectiveTaxRate" appears in MLProdutos.tsx
- Grep confirms "showTaxBanner" appears in MLProdutos.tsx
- Grep confirms "Estimativa baseada no regime tributário configurado em Fiscal" appears in MLProdutos.tsx
- Grep confirms "InlineEditCell" does NOT appear in the Impostos cell (the value/onSave/format="percent" pattern is gone)
- Grep confirms "↑ item" appears exactly once in the variation financial block (Cost cell only; Impostos cell is replaced)
</verification>

<success_criteria>
- tsc --noEmit exits 0 — no type errors in the updated file
- Impostos column renders R$ X,XX (Y,Y%) when fiscal config exists for the product's store (per CATALOG-01)
- Impostos column renders the manual tax_rate fallback value when no fiscal config exists (per CATALOG-01)
- Impostos column renders — when neither fiscal config nor manual tax_rate is set (per CATALOG-01)
- Variation rows compute tax from parent store's effective_rate against variation price (per architecture decision 7)
- Amber banner is shown when taxMap is loaded and at least one store has no entry (per CATALOG-02)
- Banner Link navigates to /fiscal (per CATALOG-02)
- Impostos tooltip shows disclaimer text (per CATALOG-03)
- marginLiq reflects effectiveTaxRate (per architecture decision 8)
- InlineEditCell is gone from the Impostos cell (per architecture decision 1)
</success_criteria>

<output>
After completion, create .planning/phases/03-catalogo-qualidade/03-02-SUMMARY.md recording:
- Files modified: src/pages/mercadolivre/MLProdutos.tsx
- Sub-steps applied: 3a, 3b, 4a, 4b, 4c, 4d, 4e
- tsc --noEmit result
- Any lines that differed from the expected old_string and how you resolved them
- Any deviations from this plan
</output>
