---
phase: 19-imposto-custo-real
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/hooks/useMLKPISummary.ts
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements:
  - TAX-03
  - TAX-04

must_haves:
  truths:
    - "useMLKPISummary retorna total_tax: SUM(orders.tax_amount) para o período"
    - "Menu Vendas exibe imposto calculado de SUM(orders.tax_amount) — não de effective_rate * receita"
    - "KPIs existentes (markup, custo operacional, receita, pedidos) não regridem"
    - "Quando tax_amount = 0 em todos os orders do período: imposto mostra null/— (não R$0 falso)"
  artifacts:
    - path: "src/hooks/useMLKPISummary.ts"
      provides: "Hook com total_tax adicionado ao MLKPISummary"
      exports: ["useMLKPISummary", "MLKPISummary"]
    - path: "src/pages/MercadoLivre.tsx"
      provides: "impostosTotal = kpiSummary?.total_tax ?? null"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useMLKPISummary.ts"
      via: "kpiSummary?.total_tax"
      pattern: "total_tax"
---

<objective>
Mudar o menu Vendas para exibir imposto calculado dos dados reais dos pedidos (`SUM(orders.tax_amount)`) em vez de `effective_rate × receita` — que retorna 0% com a config atual de Lucro Real.

Purpose: Com o Plano 19-01, todos os orders agora têm `tax_amount` correto (~20.14%). O menu Vendas precisa LER esses valores do banco em vez de recalcular com a taxa efetiva.
Output: Hook atualizado + MercadoLivre.tsx usando dado real.
</objective>

<execution_context>
@.planning/phases/19-imposto-custo-real/19-CONTEXT.md
@.planning/phases/19-imposto-custo-real/19-01-SUMMARY.md
</execution_context>

<context>
<interfaces>
De src/hooks/useMLKPISummary.ts (estado após Plano 16-01, agora a ser expandido):
```typescript
export interface MLKPISummary {
  markup_ratio: number | null;
  markup_has_cost: boolean;
  custo_plataforma: number;        // frete + comissao
  custo_operacional: number;       // custo_plataforma + ads_total
  pct_custo_operacional: number;
  gross_revenue: number;
  // A ADICIONAR:
  // total_tax: number;            // SUM(orders.tax_amount) — 0 se todos null
  // has_tax_data: boolean;        // true se ao menos 1 row com tax_amount > 0
}

export function useMLKPISummary(from: string, to: string, ads_total: number)
// Query: supabase.from("orders").select("receita_bruta, custo_unit, quantidade, frete, comissao")
// PRECISA ADICIONAR: ", tax_amount" no select
```

De src/pages/MercadoLivre.tsx (estado atual):
```typescript
// Linha existente — REMOVER após a mudança:
const { data: taxMap } = useMLTaxConfig(resolvedMLUserIds, currentOrg?.id ?? "");
const impostosTotal = useMemo(() => {
  if (!costWaterfall?.revenue_per_store || !taxMap || taxMap.size === 0) return null;
  let total = 0;
  for (const [mlUserId, rev] of Object.entries(costWaterfall.revenue_per_store)) {
    const taxEntry = taxMap.get(mlUserId);
    if (taxEntry?.effective_rate != null && taxEntry.effective_rate > 0) {
      total += (rev * taxEntry.effective_rate) / 100;
    }
  }
  return total > 0 ? total : null;
}, [costWaterfall, taxMap]);

// SUBSTITUIR por:
const impostosTotal = kpiSummary?.has_tax_data ? (kpiSummary.total_tax || null) : null;
```

De src/components/mercadolivre/MLCostCard.tsx:
```typescript
interface MLCostCardProps {
  impostos: number | null;  // já aceita null → mostra "—"
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar total_tax e has_tax_data ao useMLKPISummary</name>
  <files>src/hooks/useMLKPISummary.ts</files>
  <action>
Fazer 3 mudanças cirúrgicas em `src/hooks/useMLKPISummary.ts`:

**Mudança 1 — Adicionar campos à interface MLKPISummary:**
```typescript
export interface MLKPISummary {
  markup_ratio: number | null;
  markup_has_cost: boolean;
  custo_plataforma: number;
  custo_operacional: number;
  pct_custo_operacional: number;
  gross_revenue: number;
  total_tax: number;          // SUM(orders.tax_amount) para o período
  has_tax_data: boolean;      // true se ao menos 1 order com tax_amount > 0
}
```

**Mudança 2 — Adicionar `tax_amount` ao select na queryFn:**
```typescript
const { data, error } = await supabase
  .from("orders")
  .select("receita_bruta, custo_unit, quantidade, frete, comissao, tax_amount")
  // ...
```

**Mudança 3 — Calcular total_tax na queryFn (antes do return):**
```typescript
const total_tax = rows.reduce((s, r) => s + (r.tax_amount ?? 0), 0);
const has_tax_data = rows.some((r) => (r.tax_amount ?? 0) > 0);

return {
  markup_ratio,
  markup_has_cost,
  custo_plataforma,
  custo_operacional,
  pct_custo_operacional,
  gross_revenue,
  total_tax,
  has_tax_data,
};
```
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "useMLKPISummary\|error TS" | head -10</automated>
  </verify>
  <done>MLKPISummary tem total_tax e has_tax_data; hook compila sem erros</done>
</task>

<task type="auto">
  <name>Task 2: MercadoLivre.tsx — usar kpiSummary.total_tax em vez de effective_rate*receita</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <action>
Fazer 3 mudanças em `src/pages/MercadoLivre.tsx`:

**Mudança 1 — Remover useMLTaxConfig e impostosTotal calculado com effective_rate:**

Localizar e REMOVER o bloco:
```typescript
  // Tax config para calcular impostos por loja
  const { data: taxMap } = useMLTaxConfig(resolvedMLUserIds, currentOrg?.id ?? "");

  const impostosTotal = useMemo(() => {
    if (!costWaterfall?.revenue_per_store || !taxMap || taxMap.size === 0) return null;
    let total = 0;
    for (const [mlUserId, rev] of Object.entries(costWaterfall.revenue_per_store)) {
      const taxEntry = taxMap.get(mlUserId);
      if (taxEntry?.effective_rate != null && taxEntry.effective_rate > 0) {
        total += (rev * taxEntry.effective_rate) / 100;
      }
    }
    return total > 0 ? total : null;
  }, [costWaterfall, taxMap]);
```

**Mudança 2 — Adicionar impostosTotal derivado de kpiSummary, logo após a linha do kpiSummary hook:**
```typescript
  // Imposto: usa SUM(orders.tax_amount) para o período (dados reais, não effective_rate)
  const impostosTotal = kpiSummary?.has_tax_data ? (kpiSummary.total_tax || null) : null;
```

**Mudança 3 — Remover o import de useMLTaxConfig** se não for mais usado em nenhum outro lugar do arquivo:
```typescript
// Remover: import { useMLTaxConfig } from "@/hooks/useMLTaxConfig";
```

NOTA: Antes de remover o import, verificar se `useMLTaxConfig` é usado em outro lugar do arquivo. Se sim, manter o import mas não usar para `impostosTotal`.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <done>
- impostosTotal = kpiSummary?.has_tax_data ? kpiSummary.total_tax : null
- useMLTaxConfig removido do arquivo (se não usado em outro lugar)
- TypeScript limpo
  </done>
</task>

</tasks>

<verification>
```bash
cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -5

# Verificar que total_tax está sendo calculado
grep -n "total_tax\|has_tax_data" /root/garment-glow-test/src/hooks/useMLKPISummary.ts

# Verificar que Vendas usa kpiSummary.total_tax
grep -n "impostosTotal\|total_tax" /root/garment-glow-test/src/pages/MercadoLivre.tsx
```

Verificação visual (após 19-01 deployado e orders recalculados):
- Menu Vendas → card Impostos mostra valor > 0 (não "—" nem R$0.00)
- Valor deve ser coerente: ~20% × receita do período
</verification>

<success_criteria>
1. `MLKPISummary` tem `total_tax: number` e `has_tax_data: boolean`
2. `MercadoLivre.tsx` calcula `impostosTotal = kpiSummary?.has_tax_data ? kpiSummary.total_tax : null`
3. `useMLTaxConfig` removido de `MercadoLivre.tsx` (não mais necessário para imposto no Vendas)
4. TypeScript compila sem erros
5. Quando orders têm tax_amount > 0: imposto aparece no card Vendas
6. KPIs existentes (markup, custo operacional, receita) não regridem
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/19-imposto-custo-real/19-02-SUMMARY.md` com:
- Campos adicionados ao hook
- Como impostosTotal é calculado agora
- Valor do imposto visível no Vendas (se já há dados recalculados)
</output>
