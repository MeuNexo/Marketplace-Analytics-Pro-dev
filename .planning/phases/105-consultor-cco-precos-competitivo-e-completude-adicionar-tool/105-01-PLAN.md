---
phase: 105-consultor-cco-precos-competitivo-e-completude-adicionar-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/nexo-chat/tools.ts
  - supabase/functions/nexo-chat/tools.test.ts
  - supabase/functions/nexo-chat/playbooks.ts
  - supabase/functions/nexo-chat/prompt.ts
  - supabase/functions/nexo-chat/prompt.test.ts
autonomous: true
requirements:
  - CCO-PRICE          # tool get_price_practiced (RPC orders_sold_products_agg + tabela ml_mco_targets) — preço praticado × meta MCO
  - CCO-COMPETITIVE    # tool get_competitive_price (EF ml-precos-custos type=references, JWT real) — pilar competitivo Rafael
  - CCO-GAPS           # tool get_cost_gaps (RPC get_cmv_cheio_gaps) — QUAIS SKUs sem custo
  - CCO-CANCELLED      # tool get_cancelled_revenue (RPC get_cancelled_revenue) — receita cancelada
  - CCO-PLAYBOOK-R     # playbook Rafael ampliado (4.3 dado competitivo real: preço total, quando reagir, sugestão ML = sinal)
  - CCO-PERSONA-FINAL  # persona FINAL da milestone: USO DAS FERRAMENTAS cita TODAS as tools 103/104/105 + rótulos de veracidade
  - CCO-TESTS-105      # testes espelhando 103/104 (3 padrões anti-IDOR, EF-mock, div/0, filtro sku='', contagem 31→35, greps novos)

must_haves:
  truths:
    - "O Consultor responde 'qual foi meu preço praticado × minha meta de MCO em [item]' via get_price_practiced — o preço é DERIVADO (receita_bruta/quantidade), com guarda de divisão por zero (quantidade=0 → null), e a meta vem de ml_mco_targets só para o anúncio inteiro (sku='')"
    - "O Consultor tem, pela PRIMEIRA vez, um sinal competitivo REAL via get_competitive_price (EF ml-precos-custos, type=references) — rotulado como SUGESTÃO do ML, nunca garantia; sem JWT real do usuário ele declara a limitação em vez de inventar concorrente"
    - "O Consultor responde 'QUAIS SKUs estão sem custo' via get_cost_gaps (não só a contagem), rotulando que custo ausente pode ser LEGÍTIMO em conta de revenda (não é necessariamente erro)"
    - "O Consultor responde 'quanto de receita foi cancelada' via get_cancelled_revenue, rotulando que cancelado ≠ faturamento"
    - "Uma org NUNCA vê preço/competitivo/custo/cancelado de outra: get_cost_gaps/get_cancelled_revenue passam p_org_id+p_user_ids do servidor; get_price_practiced passa SÓ _ml_user_ids do servidor (a RPC não tem p_org_id) e o select em ml_mco_targets filtra .eq('organization_id', orgId); get_competitive_price faz loop só sobre mlUserIds do servidor. args de org/seller do modelo são sempre ignorados"
    - "ctx.userJwt do usuário é usado EXCLUSIVAMENTE no header Authorization da chamada à EF ml-precos-custos e NUNCA é logado nem exposto no retorno da tool"
    - "A persona FINAL da milestone cita TODAS as tools novas (103: replenishment/suppliers; 104: dre_result/dre_cash/projected_balance/taxes_paid; 105: price_practiced/competitive_price/cost_gaps/cancelled_revenue) com quando usar, e os rótulos de veracidade (preço competitivo=sugestão; custo ausente legítimo em revenda; cancelado≠faturamento) — sem quebrar nenhum grep/ordem de prompt.test.ts"
  artifacts:
    - path: "supabase/functions/nexo-chat/tools.ts"
      provides: "4 declarações (get_price_practiced, get_competitive_price, get_cost_gaps, get_cancelled_revenue), 4 cases no dispatchTool (3 padrões anti-IDOR distintos), helper puro exportado buildPricePracticed, comentário de mapeamento atualizado"
      contains: "get_competitive_price"
      exports: ["TOOL_DECLARATIONS", "dispatchTool", "buildPricePracticed"]
    - path: "supabase/functions/nexo-chat/tools.test.ts"
      provides: "Testes anti-IDOR dos 3 padrões, EF-mock de get_competitive_price (type=references + Bearer do usuário), guarda div/0 e filtro sku='' de buildPricePracticed, contagem 35"
      contains: "get_competitive_price"
    - path: "supabase/functions/nexo-chat/playbooks.ts"
      provides: "Bloco 4. RAFAEL ampliado com 4.3 (dado competitivo real: preço total preço+frete, quando reagir vs manter margem, sugestão ML = sinal não ordem)"
      contains: "get_competitive_price"
    - path: "supabase/functions/nexo-chat/prompt.ts"
      provides: "PERSONA FINAL da milestone: USO DAS FERRAMENTAS citando TODAS as tools 103/104/105, FONTE CERTA + PARCIAL É ROTULADO com os rótulos de 105"
      contains: "get_competitive_price"
    - path: "supabase/functions/nexo-chat/prompt.test.ts"
      provides: "Greps novos provando as 4 tools de 105 e os rótulos (sugestão competitiva, custo ausente legítimo, cancelado≠faturamento) na PERSONA, sem quebrar ordens/greps existentes"
      contains: "get_competitive_price"
  key_links:
    - from: "supabase/functions/nexo-chat/tools.ts (case get_price_practiced)"
      to: "RPC orders_sold_products_agg (_ml_user_ids do servidor) + tabela ml_mco_targets (.eq organization_id, .eq sku '')"
      via: "sb.rpc sem p_org_id + select org-scoped; join por item_id só no sentinela sku=''; preço = receita_bruta/quantidade com guarda div/0"
      pattern: "orders_sold_products_agg"
    - from: "supabase/functions/nexo-chat/tools.ts (case get_competitive_price)"
      to: "EF ml-precos-custos ?type=references via fetch com Authorization: Bearer ctx.userJwt"
      via: "loop sobre mlUserIds do servidor; guarda !ctx.userJwt → {error:'sem_jwt'}; ctx.userJwt nunca logado"
      pattern: "type=references"
    - from: "supabase/functions/nexo-chat/tools.ts (cases get_cost_gaps/get_cancelled_revenue)"
      to: "RPCs get_cmv_cheio_gaps + get_cancelled_revenue (p_org_id, p_user_ids, p_from, p_to)"
      via: "sb.rpc org+mlUserIds do servidor, reutilizando from/to do topo do dispatchTool"
      pattern: "get_cmv_cheio_gaps"
    - from: "supabase/functions/nexo-chat/prompt.ts (buildSystemPrompt)"
      to: "playbooks.ts (STRATEGIC, bloco 4. RAFAEL)"
      via: "concatenação já existente de STRATEGIC no system prompt"
      pattern: "STRATEGIC"
---

<objective>
Fechar a milestone "Consultor CCO Completo" (Grupos 3+4 do spec) adicionando ao Consultor de IA
(edge function `nexo-chat`) as 4 últimas tools read-only, acendendo o **pilar competitivo (Rafael) —
hoje fantasma** (a persona promete concorrentes/preço total/Buy Box mas NENHUMA tool tem dado
competitivo real), o **preço praticado × meta de MCO**, e a **completude de leitura** (quais SKUs sem
custo, receita cancelada). Concretamente:

1. Quatro tools read-only novas em `tools.ts`, cada uma com o padrão anti-IDOR CORRETO para a sua
   fonte (são TRÊS padrões distintos — respeitar cada um):
   - `get_price_practiced` → RPC `orders_sold_products_agg(_ml_user_ids, _from, _to)` (SEM `p_org_id`
     — padrão "só mlUserIds do servidor", molde `get_goals`) + select em `ml_mco_targets`
     (`.eq('organization_id', orgId).eq('sku','')`). É item-level e NÃO tem coluna de preço → o preço
     praticado é DERIVADO `receita_bruta/quantidade` com GUARDA de divisão por zero. A meta só casa no
     sentinela `sku=''` (anúncio inteiro); metas por variação não têm equivalente no agregado.
   - `get_competitive_price` → EF `ml-precos-custos` com query `?type=references` (NÃO `mode`),
     exigindo JWT REAL do usuário (`ctx.userJwt`) — molde `get_reputation` EXATO. Loop sobre
     `mlUserIds` do servidor, `Authorization: Bearer ${ctx.userJwt}`, guarda `!ctx.userJwt`. Input
     não-sensível do modelo: `item_id` (opcional, sanitizado). ctx.userJwt NUNCA logado/exposto.
   - `get_cost_gaps` → RPC `get_cmv_cheio_gaps(p_org_id, p_user_ids, p_from, p_to)` — padrão
     org+mlUserIds (molde `get_margin_by_product`).
   - `get_cancelled_revenue` → RPC `get_cancelled_revenue(p_org_id, p_user_ids, p_from, p_to)` — idem.
2. Playbook Rafael ampliado em `playbooks.ts` (bloco "## 4. RAFAEL", subseção 4.3 nova com dado
   competitivo real), estilo DADO→Diagnóstico→Ação→Métrica, sem remover 4.1/4.2.
3. Persona FINAL da milestone em `prompt.ts`: garantir que "USO DAS FERRAMENTAS" cite TODAS as tools
   novas da milestone (103/104/105) e que a veracidade cubra os rótulos de 105.
4. Testes espelhando 103/104 (3 padrões anti-IDOR, EF-mock, div/0, filtro sku='', contagem 31→35,
   greps novos em prompt.test.ts).

Purpose: acender o pilar competitivo (hoje a persona promete o que nenhuma tool entrega) e completar
a leitura de dados — o último passo para o Consultor ser um CCO de verdade. Todas as RPCs e a EF já
existem e são consumidas em produção por páginas do dashboard; esta fase só as embrulha em tools
rotuladas corretamente, sem computar nenhum número novo.
Output: 4 tools novas + helper `buildPricePracticed`, base de conhecimento (Rafael) e persona FINAL
ampliadas, e suíte de testes verde (contagem de tools 31→35).

FORA de escopo: RAG/embeddings (Fase 2); qualquer mutação; novas RPCs/migrations de dados; expor
preço por VARIAÇÃO (o agregado `orders_sold_products_agg` é por `item_id`, não por SKU — declarar a
limitação, não contornar). Read-only estrito.

NOTA DE DEPLOY (não é task do executor): após o merge, o **orquestrador** faz o deploy da EF
`nexo-chat` no projeto `ckcdevcxgvueywivefgx` via MCP `deploy_edge_function` (preservando
`verify_jwt=true`) ou CLI `supabase functions deploy nexo-chat`. Nenhuma migration nova é necessária.
O executor NÃO deploya. A EF `ml-precos-custos` já está deployada — não é tocada.

NOTA DE DRIFT (para o orquestrador, não bloqueia o executor): confirmar ao vivo via Supabase MCP,
antes do deploy final, (a) o setting `verify_jwt` de `ml-precos-custos` (Assumption A1 do research —
o requisito prático de JWT real é HIGH confidence por leitura de código, só o gateway é inferido) e
(b) que as assinaturas de `orders_sold_products_agg`, `ml_mco_targets`, `get_cmv_cheio_gaps` e
`get_cancelled_revenue` não sofreram drift desde o research (o repo tem histórico de divergência
entre migrations locais e o DB vivo). Não corrigir silenciosamente — reportar se divergir.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-consultor-cco-precos-competitivo-e-completude-adicionar-tool/105-CONTEXT.md
@.planning/phases/105-consultor-cco-precos-competitivo-e-completude-adicionar-tool/105-RESEARCH.md

# Moldes validados das fases anteriores (mesma estrutura de tool + testes + playbook + persona)
@.planning/phases/103-consultor-cco-ferramentas-de-compra-vs-venda-adicionar-tools/103-01-PLAN.md
@.planning/phases/104-consultor-cco-dre-real-e-caixa-adicionar-tools-read-only-get/104-01-PLAN.md

# Código a modificar (LER na íntegra as partes citadas em cada task)
@supabase/functions/nexo-chat/tools.ts
@supabase/functions/nexo-chat/tools.test.ts
@supabase/functions/nexo-chat/playbooks.ts
@supabase/functions/nexo-chat/prompt.ts
@supabase/functions/nexo-chat/prompt.test.ts

# A EF consumida por get_competitive_price (LER para confirmar o contrato type=references + JWT)
@supabase/functions/ml-precos-custos/index.ts
</context>

## Artifacts this phase produces

- `get_price_practiced` — tool read-only (declaração + case) sobre `orders_sold_products_agg`
  (padrão "só _ml_user_ids do servidor", SEM p_org_id) + select em `ml_mco_targets`
  (`.eq('organization_id', orgId).eq('sku','')`). Retorno `{ label, rows }` onde cada row tem
  `item_id, titulo, marca, quantidade, receita_bruta, preco_medio_praticado (receita/qtd, null se
  qtd=0), meta_mco_pct (null se sem meta cadastrada)`.
- `get_competitive_price` — tool read-only sobre a EF `ml-precos-custos` `?type=references` via
  `fetch` com `Authorization: Bearer ctx.userJwt` (molde get_reputation). Retorno `{ label, data }`
  onde `data` = por-loja `{ ml_user_id, reference|references, no_suggestion?, error? }`. Sem JWT →
  `{ error: 'sem_jwt', label }`.
- `get_cost_gaps` — tool read-only sobre `get_cmv_cheio_gaps` (org+mlUserIds). Retorno
  `{ label, rows }` com `{ sku, marca, linhas, unidades, receita, tem_custo_medio }`.
- `get_cancelled_revenue` — tool read-only sobre `get_cancelled_revenue` (org+mlUserIds). Retorno
  `{ label, cancelled_revenue, cancelled_orders }`.
- `buildPricePracticed(soldRows, targetRows)` — **helper PURO exportado** de `tools.ts`: filtra
  targetRows por `sku===''` (defensivo), monta `Map<item_id, target_mco_pct>`, deriva
  `preco_medio_praticado = quantidade>0 ? receita_bruta/quantidade : null` (guarda div/0), anexa
  `meta_mco_pct ?? null`. É a unidade determinística testada na Task 2.
- Bloco `## 4. RAFAEL` ampliado em `playbooks.ts` (subseção 4.3).
- `PERSONA` FINAL da milestone em `prompt.ts` (todas as tools 103/104/105 + rótulos de 105).

<tasks>

<task type="auto">
  <name>Task 1: Implementar as 4 tools de preços/competitivo/completude em tools.ts (3 padrões anti-IDOR)</name>

  <read_first>
    - `supabase/functions/nexo-chat/tools.ts` — LER na íntegra os pontos:
      - cabeçalho (linhas 1-44): bloco de comentário "Mapeamento confirmado em 57-RESEARCH" — precisa
        ganhar 4 linhas novas (uma por tool de 105).
      - `MAX_ROWS` (linha 47), helpers `today`/`daysAgo` (50-58), `clampDate` (60-68), `cap` (70-72),
        `sumGuiaReal` (74+, referência de estilo de helper puro exportado), `DATE_PROPS` (linha 108).
      - `TOOL_DECLARATIONS`: molde de declaração SEM param de org/seller. As 31 entradas atuais
        terminam em `get_taxes_paid` (fecha na linha 438, `];` na 439). As 4 novas entram ANTES do
        `];` fechando o array, mantendo-o coeso.
      - `dispatchTool` (assinatura linhas 574-581; `ctx: { userJwt?: string } = {}`): `from`/`to` já
        computados no topo (584-585). Moldes a copiar:
        - **org+mlUserIds**: `get_margin_by_product` (589-594) — `p_org_id: orgId, p_user_ids:
          mlUserIds, p_from: from, p_to: to`. É o molde de `get_cost_gaps` e `get_cancelled_revenue`.
        - **só mlUserIds, sem p_org_id + select org-scoped**: `get_goals` (1086-1146) — RPC scopada
          só por mlUserIds do servidor + select direto com `.eq`/`.in` do servidor. É o molde da parte
          RPC de `get_price_practiced`.
        - **EF via ctx.userJwt**: `get_reputation` (1037-1080) — guarda `!ctx.userJwt` → `{error:
          'sem_jwt', label}`; `Deno.env.get("SUPABASE_URL")`; loop sobre `mlUserIds` (servidor);
          `fetch(efUrl, { headers: { Authorization: `Bearer ${ctx.userJwt}` } })`; try/catch por loja
          com `error: ef_status_${res.status}`/`ef_fetch_error`. É o molde EXATO de
          `get_competitive_price`. NUNCA logar ctx.userJwt.
        - sanitização de input livre do modelo: `get_inventory` (buscar o `search` sanitizado —
          `.replace(/[%,()*\\]/g, "").trim().slice(0, 60)`) é o molde para sanitizar `item_id`.
        - `clampMonth` (1340-1346, privado) e o `default` (1288, `{ error: "unknown_tool" }`).
      - Os 4 cases novos entram ANTES do `default:` (linha 1288), depois do case `get_taxes_paid`
        (fecha na 1286).
    - `105-RESEARCH.md` — LER na íntegra: "Summary", "Architecture Patterns" (Pattern 1 org-only,
      Pattern 2 só-mlUserIds+join, Pattern 3 EF-via-ctx.userJwt — os exemplos de código com
      comentários inline são o CONTRATO desta task, seguir literalmente, NÃO especular),
      "Anti-Patterns to Avoid", "Don't Hand-Roll", "Common Pitfalls 1-5", e "Security Domain" (V5:
      sanitização de item_id). Assinaturas EXATAS confirmadas por leitura das migrations (respeitar;
      NÃO inventar params):
      `orders_sold_products_agg(_ml_user_ids text[], _from date, _to date)` — **SEM p_org_id**;
      `ml_mco_targets` (colunas usadas: `item_id, sku, target_mco_pct, organization_id`);
      `get_cmv_cheio_gaps(p_org_id uuid, p_user_ids text[], p_from date, p_to date)`;
      `get_cancelled_revenue(p_org_id uuid, p_user_ids text[], p_from date, p_to date)`.
    - `supabase/functions/ml-precos-custos/index.ts` — LER: `getUserAndToken` (linha 20, chama
      `supabase.auth.getUser` → exige JWT real, service-role falha), o dispatch por `type` (linha 322
      `url.searchParams.get("type") ?? "prices"`; linha 337 `if (type === "references")`), o 400 de
      `ml_user_id required` (linha 326), e `handlePriceReferences` (229+): item único → `{ reference,
      no_suggestion }`; bulk → `{ references }`.
  </read_first>

  <files>supabase/functions/nexo-chat/tools.ts</files>

  <action>
    Adicionar as 4 tools novas. CADA UMA usa o padrão anti-IDOR CORRETO da sua fonte (são 3 padrões
    diferentes — não uniformizar). NENHUM parâmetro de org/seller é exposto ao modelo. Não remover
    nem alterar nenhuma tool/case existente. NÃO computar nenhum número novo — só invocar as
    RPCs/EF e rotular.

    (a) Em `TOOL_DECLARATIONS`, adicionar 4 entradas `FnDecl` (após `get_taxes_paid`, antes do `];`).
    Params expostos ao modelo, todos opcionais e NÃO-sensíveis:
    - `get_price_practiced`: `parameters: { type: "object", properties: { ...DATE_PROPS } }` (janela
      de datas, default 30d no dispatch — Open Question 1 do research: expor DATE_PROPS por
      consistência, NUNCA rodar unbounded). Description PT-BR: preço MÉDIO praticado (histórico,
      DERIVADO de receita_bruta/quantidade de pedidos pagos no período — não é o preço ATUAL do
      anúncio) por anúncio (item_id), cruzado com a meta de MCO cadastrada (ml_mco_targets); a meta
      só existe para o anúncio INTEIRO (metas por variação/SKU não aparecem — o agregado é por
      item_id); meta_mco_pct=null significa SEM meta cadastrada (não 0%).
    - `get_competitive_price`: `parameters` com `item_id` (string, opcional). Description PT-BR:
      SUGESTÃO de preço competitiva do próprio Mercado Livre (endpoint de referência de preços) +
      custos ML embutidos — é SUGESTÃO/indicativo, NÃO garantia de venda nem preço mínimo
      obrigatório, e NÃO é o preço do concorrente; sem item_id retorna sugestões em lote por loja,
      com item_id detalha 1 anúncio; nem todo item tem sugestão disponível.
    - `get_cost_gaps`: `parameters: { type: "object", properties: { ...DATE_PROPS } }`. Description
      PT-BR: QUAIS SKUs estão sem custo CHEIO cadastrado no período (não só a contagem) — custo
      ausente pode ser LEGÍTIMO em conta de revenda (custo não está no Tiny), não necessariamente
      erro; tem_custo_medio=true distingue "tem custo médio, falta o cheio" de "sem custo nenhum".
    - `get_cancelled_revenue`: `parameters: { type: "object", properties: { ...DATE_PROPS } }`.
      Description PT-BR: receita de pedidos cancelados/estornados no período — NÃO é faturamento;
      complementa get_sales_kpis (que só soma pagos).

    (b) Adicionar o helper PURO exportado (estilo `sumGuiaReal`/`buildReplenishmentResult`):
    `export function buildPricePracticed(soldRows, targetRows)` — recebe as rows cruas de
    `orders_sold_products_agg` e as rows de `ml_mco_targets`; monta `Map<item_id, target_mco_pct>`
    SOMENTE a partir das targetRows com `sku === ''` (defensivo, Pitfall 4 — mesmo que o select já
    filtre, o helper filtra de novo para ser determinístico em teste com fixture mista); mapeia cada
    soldRow para `{ item_id, titulo, marca, quantidade: Number(...)||0, receita_bruta: Number(...)||0,
    preco_medio_praticado: quantidade>0 ? Math.round((receita_bruta/quantidade)*100)/100 : null,
    meta_mco_pct: map.get(item_id) ?? null }`. GUARDA de divisão por zero obrigatória (Pitfall 3:
    quantidade=0 → null, NUNCA NaN/Infinity). Retorna o array enriquecido (o case aplica label+cap).

    (c) Em `dispatchTool`, adicionar 4 `case` ANTES do `default` (seguir os exemplos do research
    literalmente, ajustando só o estilo do arquivo):
    - `case "get_price_practiced"` (padrão SÓ mlUserIds + select org-scoped):
      `sb.rpc("orders_sold_products_agg", { _ml_user_ids: mlUserIds, _from: from, _to: to })` —
      **NÃO passar p_org_id** (a RPC não tem esse param; passá-lo causa erro Postgres "function does
      not exist" — Anti-Pattern do research). Depois select
      `sb.from("ml_mco_targets").select("item_id, target_mco_pct, sku").eq("organization_id",
      orgId).eq("sku", "")` (anti-IDOR: select via service_role bypassa RLS → `.eq('organization_id')`
      obrigatório; `.eq('sku','')` = só o sentinela do anúncio inteiro — Pitfall 4). Chamar
      `buildPricePracticed(soldRows, targetRows)`. Retornar `{ label, rows: cap(enriched) }` com o
      `label` explicando: preço praticado é HISTÓRICO derivado (receita/qtd), não o preço atual do
      anúncio; meta_mco_pct vem de ml_mco_targets só para o anúncio inteiro (sku=''); meta null = sem
      meta cadastrada (não 0% — declarar limitação, não inventar); metas por variação (SKU) não
      aparecem (agregado é por item_id).
    - `case "get_competitive_price"` (padrão EF via ctx.userJwt — molde get_reputation EXATO):
      guarda `if (!ctx.userJwt) return { error: "sem_jwt", label: "não foi possível consultar preço
      competitivo agora" }`. `const supabaseUrl = Deno.env.get("SUPABASE_URL")`. Sanitizar item_id:
      `typeof args.item_id === "string"` → strip para alfanumérico e `.slice(0, 30)` (molde da
      sanitização de `search` em get_inventory; V5 do research); senão undefined. Loop sobre
      `mlUserIds` (servidor — NUNCA o item/seller do modelo escolhe a loja): montar
      `new URLSearchParams({ ml_user_id: mlUserId, type: "references" })` (**type**, não mode —
      Pitfall 1) + `item_id` só se presente; `fetch(`${supabaseUrl}/functions/v1/ml-precos-custos?${qs}`,
      { headers: { Authorization: `Bearer ${ctx.userJwt}` } })`; try/catch por loja distinguindo
      `error: ef_status_${res.status}` (JWT presente mas rejeitado) de `ef_fetch_error`; no sucesso
      push `{ ml_user_id, reference: data.reference ?? null, references: Array.isArray(data.references)
      ? data.references : undefined, no_suggestion: data.no_suggestion === true }`. Retornar
      `{ label, data: cap(results) }`. O `label`: SUGESTÃO do ML (não garantia, não preço do
      concorrente, não obrigatório); custos ML (selling_fees/shipping_fees) embutidos; nem todo item
      tem sugestão (no_suggestion / omitido no bulk). **NUNCA** colocar ctx.userJwt no retorno nem em
      log.
    - `case "get_cost_gaps"` (padrão org+mlUserIds — molde get_margin_by_product):
      `sb.rpc("get_cmv_cheio_gaps", { p_org_id: orgId, p_user_ids: mlUserIds, p_from: from, p_to: to })`.
      Retornar `{ label, rows: cap(data ?? []) }` com o label: SKUs sem custo cheio no período; custo
      ausente pode ser LEGÍTIMO em revenda (não é erro); tem_custo_medio distingue os casos.
    - `case "get_cancelled_revenue"` (padrão org+mlUserIds):
      `sb.rpc("get_cancelled_revenue", { p_org_id: orgId, p_user_ids: mlUserIds, p_from: from, p_to: to })`;
      `row = Array.isArray(data) ? data[0] : data`. Retornar `{ label, cancelled_revenue:
      Number(row?.cancelled_revenue ?? 0), cancelled_orders: Number(row?.cancelled_orders ?? 0) }`
      com o label: cancelado ≠ faturamento; complementa get_sales_kpis.

    (d) Atualizar o bloco de comentário de mapeamento no topo (linhas 23-44): acrescentar 4 linhas
    descrevendo cada tool→fonte com a nota do padrão anti-IDOR:
    `get_price_practiced → orders_sold_products_agg(_ml_user_ids SÓ, sem p_org_id) + ml_mco_targets(.eq org, sku='') [só-mlUserIds]`;
    `get_competitive_price → EF ml-precos-custos ?type=references via ctx.userJwt [EF-com-JWT, molde get_reputation]`;
    `get_cost_gaps → get_cmv_cheio_gaps(p_org_id,p_user_ids,p_from,p_to) [INVOKER, org+mlUserIds]`;
    `get_cancelled_revenue → get_cancelled_revenue(idem) [INVOKER, org+mlUserIds]`.

    `cap(rows)` simples (slice 50) é suficiente — as 4 tools retornam listas pequenas/objetos fixos;
    NÃO replicar o summary/sample estratificado de `buildReplenishmentResult` (over-engineering).
  </action>

  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run supabase/functions/nexo-chat/tools.test.ts 2>&1 | tail -20</automated>
    <manual>grep -n "orders_sold_products_agg\|ml_mco_targets\|type: \"references\"\|get_cmv_cheio_gaps\|get_cancelled_revenue\|buildPricePracticed\|ctx.userJwt" supabase/functions/nexo-chat/tools.ts</manual>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function buildPricePracticed" supabase/functions/nexo-chat/tools.ts` retorna 1.
    - O case get_price_practiced chama `orders_sold_products_agg` com `_ml_user_ids: mlUserIds` e NÃO passa `p_org_id` a essa RPC (verificar por leitura); o select em `ml_mco_targets` tem `.eq("organization_id", orgId)` e `.eq("sku", "")`.
    - O case get_competitive_price constrói a URL da EF com `type` (não `mode`) — `grep -q 'type: "references"' supabase/functions/nexo-chat/tools.ts` encontra — e usa `Authorization: Bearer ${ctx.userJwt}` com guarda `!ctx.userJwt`; `ctx.userJwt` não aparece em nenhum objeto de retorno (verificar por leitura).
    - Os cases get_cost_gaps e get_cancelled_revenue passam `p_org_id: orgId` E `p_user_ids: mlUserIds` (org+mlUserIds).
    - A guarda de divisão por zero existe em buildPricePracticed (quantidade=0 → null, nunca NaN/Infinity).
    - `npx vitest run supabase/functions/nexo-chat/tools.test.ts` compila (sem erro de tipo) e fica verde após a Task 2.
  </acceptance_criteria>

  <done>
    tools.ts tem 35 tools declaradas (31 + get_price_practiced + get_competitive_price + get_cost_gaps
    + get_cancelled_revenue), 4 cases novos com os 3 padrões anti-IDOR corretos (só-mlUserIds+select
    org-scoped; EF-via-ctx.userJwt; org+mlUserIds ×2), o helper exportado `buildPricePracticed` com
    guarda div/0 e filtro sku='', e o comentário de mapeamento atualizado. ctx.userJwt nunca logado
    nem exposto. Nenhuma tool existente foi alterada; nenhum número novo foi computado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Testes das 4 tools em tools.test.ts (3 padrões anti-IDOR, EF-mock type=references, div/0, filtro sku='', contagem 35)</name>

  <read_first>
    - `supabase/functions/nexo-chat/tools.test.ts` — LER na íntegra:
      - `makeStub` (26-60): stub encadeável; `sb.rpc(fn, params)` grava em `rpcCalls`; selects gravam
        em `selectCalls` com `eqs`/`ins`; `maybeSingle` default null; `.eq`/`.in` gravam mas o stub
        SEMPRE devolve `rows` (não filtra) — por isso o filtro sku='' é testado por (i) asserir
        `selectCalls` de `ml_mco_targets` tem `eqs.sku === ""` e (ii) testar `buildPricePracticed`
        DIRETO com fixture mista. REUTILIZAR sem alteração.
      - constantes `ORG_SERVER` (62), `ML_IDS_SERVER` (63), `EVIL_ARGS` (65).
      - teste "declara as 31 tools esperadas" (68-111): **array exato** de nomes + texto "31 tools" —
        VAI QUEBRAR ao adicionar 4 tools; precisa virar "35 tools" e ganhar as 4 novas.
      - teste "NENHUMA declaration expõe org_id/seller_id/ml_user_id" (113-125): as tools novas expõem
        só `from`/`to`/`item_id` (não-sensíveis) — confirmar que continua passando.
      - blocos anti-IDOR org+mlUserIds existentes (molde `get_margin_by_product`, 137+) e o
        **teste EF-mock de `get_reputation`** (947-1020): shim de `globalThis.Deno.env` +
        `globalThis.fetch`, captura `{ url, authHeader }`, asserts de URL/param/Bearer, `finally`
        restaura fetch e apaga Deno. É o molde EXATO do teste de `get_competitive_price`.
    - `105-RESEARCH.md` — seções "Code Examples" (traz os 2 testes-molde já prontos: anti-IDOR
      só-mlUserIds de get_price_practiced, e EF-via-JWT de get_competitive_price com `type=references`)
      e "Common Pitfalls" (as warning signs são os asserts a escrever: div/0 → null; sku='' match;
      type≠mode).
  </read_first>

  <files>supabase/functions/nexo-chat/tools.test.ts</files>

  <action>
    Importar `buildPricePracticed` de `./tools` (além do que já é importado). Adicionar:

    (a) ATUALIZAR o teste da contagem: mudar "31 tools" para "35 tools" e acrescentar
    `"get_price_practiced"`, `"get_competitive_price"`, `"get_cost_gaps"`, `"get_cancelled_revenue"`
    ao array esperado (o `.sort()` normaliza a ordem). Este é o único teste EXISTENTE que precisa
    mudar — não enfraquecer nenhum outro.

    (b) Teste anti-IDOR `get_price_practiced` (padrão só-mlUserIds): `dispatchTool(sb, ORG_SERVER,
    ML_IDS_SERVER, "get_price_practiced", { ...EVIL_ARGS })` sobre stub com uma soldRow; localizar
    rpcCall `orders_sold_products_agg`; asserir `params._ml_user_ids === ML_IDS_SERVER`, que `params`
    **NÃO tem `p_org_id`** (essa RPC não tem esse param), e `JSON.stringify(params)` não contém
    "888"/"999"/"ORG-ALHEIA". Além disso, localizar o `selectCalls` de `ml_mco_targets` e asserir
    `eqs.organization_id === ORG_SERVER` e `eqs.sku === ""` (só o sentinela do anúncio inteiro).

    (c) Teste DIRETO de `buildPricePracticed` (unidade determinística — 2 asserts):
    - div/0: passar soldRows `[{item_id:"MLB1", titulo:"T", marca:"M", quantidade:0, receita_bruta:0},
      {item_id:"MLB2", quantidade:10, receita_bruta:1000}]` e targetRows `[]`; asserir que a row de
      MLB1 tem `preco_medio_praticado === null` (não NaN, não Infinity) e a de MLB2 `=== 100`.
    - filtro sku='' (Pitfall 4): passar soldRows `[{item_id:"MLB1", quantidade:10, receita_bruta:1000}]`
      e targetRows MISTA `[{item_id:"MLB1", sku:"", target_mco_pct:15},{item_id:"MLB1", sku:"SKU-A",
      target_mco_pct:99}]`; asserir que a row resultante tem `meta_mco_pct === 15` (o valor do sku='',
      NUNCA 99 da variação).

    (d) Teste EF-mock `get_competitive_price` (molde get_reputation, linhas 947-1020) — o ponto mais
    crítico desta task: shimar `globalThis.Deno.env` (SUPABASE_URL fixo) e `globalThis.fetch`
    (captura `{ url, authHeader }`, devolve `new Response(JSON.stringify({ references: [] }),
    { status: 200 })`); chamar `dispatchTool(sb, ORG_SERVER, ML_IDS_SERVER, "get_competitive_price",
    EVIL_ARGS, { userJwt: "JWT-REAL" })`; asserir `fetchCalls.length === ML_IDS_SERVER.length`; para
    cada call: `url` contém `/functions/v1/ml-precos-custos`, `url` contém `type=references` (NÃO
    `mode=references` — um teste que aceitasse `mode` seria a chamada ERRADA), `new
    URL(url).searchParams.get("ml_user_id") ∈ ML_IDS_SERVER` e `!== "888"`, e `authHeader === "Bearer
    JWT-REAL"`. `finally` restaura fetch e apaga o shim de Deno.

    (e) Teste guarda sem-JWT de `get_competitive_price`: chamar SEM ctx (ctx default `{}`); asserir
    `result.error === "sem_jwt"` e `result` tem `label`, e que NENHUM fetch foi feito.

    (f) Teste anti-IDOR org+mlUserIds de `get_cost_gaps`: chamar com `EVIL_ARGS`; localizar rpcCall
    `get_cmv_cheio_gaps`; asserir `params.p_org_id === ORG_SERVER`, `params.p_user_ids ===
    ML_IDS_SERVER`, `JSON.stringify(params)` sem "ORG-ALHEIA"/"888".

    (g) Teste anti-IDOR org+mlUserIds de `get_cancelled_revenue`: idem (f) na rpcCall
    `get_cancelled_revenue`; asserir também que o resultado expõe `cancelled_revenue`/`cancelled_orders`
    numéricos (default 0, nunca null/NaN) sobre um stub `[{cancelled_revenue:1234, cancelled_orders:5}]`.

    (h) (opcional, se fácil) Teste de que o retorno de `get_competitive_price` NUNCA contém o JWT:
    após o teste (d), asserir `JSON.stringify(result)` não contém `"JWT-REAL"`.

    Não remover nem enfraquecer testes existentes.
  </action>

  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run supabase/functions/nexo-chat/tools.test.ts 2>&1 | tail -25</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run supabase/functions/nexo-chat/tools.test.ts` passa (0 falhas), incluindo o teste de contagem agora com 35 tools.
    - Existe um teste que asserta que `orders_sold_products_agg` recebe `_ml_user_ids` do servidor e NÃO recebe `p_org_id`, e que o select em `ml_mco_targets` usa `eqs.sku === ""` e `eqs.organization_id === ORG_SERVER`.
    - Existe um teste DIRETO de `buildPricePracticed` provando (i) quantidade=0 → `preco_medio_praticado === null` e (ii) numa fixture com sku='' e sku='SKU-A' o `meta_mco_pct` resultante é o do sku='' (não o da variação).
    - Existe um teste EF-mock que asserta `type=references` (NÃO `mode`) e `authHeader === "Bearer JWT-REAL"` para cada `ml_user_id ∈ ML_IDS_SERVER` (nunca "888") — `grep -c "type=references" supabase/functions/nexo-chat/tools.test.ts` retorna ≥1.
    - Existe um teste que asserta `{error:'sem_jwt'}` de get_competitive_price sem ctx.userJwt e nenhum fetch.
    - Existem testes anti-IDOR org+mlUserIds para get_cost_gaps e get_cancelled_revenue (p_org_id + p_user_ids do servidor).
    - O array de nomes esperados no teste de contagem inclui as 4 tools novas.
  </acceptance_criteria>

  <done>
    A suíte tools.test.ts prova os 3 padrões anti-IDOR das 4 tools (só-mlUserIds+select org-scoped
    com sku=''; EF-via-JWT com type=references e Bearer do usuário; org+mlUserIds ×2), a guarda de
    divisão por zero e o filtro sku='' de buildPricePracticed, a guarda sem-JWT de
    get_competitive_price, e que o JWT nunca vaza no retorno. Contagem de tools = 35.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ampliar playbook Rafael (playbooks.ts) + persona FINAL da milestone (prompt.ts) + greps (prompt.test.ts)</name>

  <read_first>
    - `supabase/functions/nexo-chat/playbooks.ts` — LER:
      - `export const STRATEGIC` (linha 13) e o bloco `## 4. RAFAEL — Inteligência Competitiva`
        (260-310): estrutura DADO→Ação validada (passos numerados); já existem `### 4.1 Monitoramento
        de Concorrentes` (262) e `### 4.2 Análise de Categoria` (277). O `---` + `## 5. REGRAS
        TRANSVERSAIS` começa na linha 285 — inserir a nova subseção 4.3 ANTES do `---`/`## 5`, dentro
        do bloco 4.
    - `supabase/functions/nexo-chat/prompt.ts` — LER na íntegra a string `PERSONA` (19-66):
      - o parágrafo de competências (25) já cita "Inteligência competitiva (preço total, concorrentes,
        categoria, Buy Box)" — hoje SEM tool por trás; agora ganha tool.
      - `FONTE CERTA POR PERGUNTA` (subseção 1, linha 40): já cita as tools de 103 e 104 no formato
        `"<pergunta>" — use <tool>`; terminar de citar as de 105.
      - `PARCIAL É ROTULADO, NUNCA ABSOLUTO` (subseção 2, linha 42): termina com a frase fixa `NUNCA
        afirme "0 em estoque..."` — inserir os pares novos ANTES dela; NÃO alterar essa frase final.
      - `USO DAS FERRAMENTAS` (linha 48-49): o 1º parágrafo é a lista corrida de domínios que HOJE já
        cita reposição/compra e DRE/caixa/imposto (103/104); acrescentar o domínio competitivo/preço
        praticado/completude (105). Esta é a FINALIZAÇÃO — a lista precisa cobrir TODAS as tools da
        milestone.
    - `supabase/functions/nexo-chat/prompt.test.ts` — LER na íntegra: greps por literais e por ORDEM
      (`idxVerac > idxAnti`; `idxVerac < idxUso`; blocos de 103 linhas 93-111 e 104 linhas 113-140).
      NÃO remover nenhuma string testada nem quebrar essas ordens. As frases literais que os testes de
      104 checam (ex.: `get_dre_result sozinho NÃO é o resultado completo`) devem permanecer intactas.
    - `105-RESEARCH.md` — seções "State of the Art" (o pilar Rafael era fantasma; agora tem
      get_competitive_price) e "Security Domain" (prompt injection já coberto — não criar regra nova).
  </read_first>

  <files>supabase/functions/nexo-chat/playbooks.ts, supabase/functions/nexo-chat/prompt.ts, supabase/functions/nexo-chat/prompt.test.ts</files>

  <action>
    (A) `playbooks.ts` — INSERIR a subseção `### 4.3` no bloco `## 4. RAFAEL` (após 4.2, antes do
    `---`/`## 5. REGRAS TRANSVERSAIS`), mantendo o estilo DADO→Diagnóstico→Ação→Métrica e citando a
    fonte real. NÃO remover 4.1/4.2. Conteúdo de `### 4.3 Sinal Competitivo Real (sugestão de preço do ML)`:
    - DADO "vale reagir ao concorrente neste anúncio?": usar `get_competitive_price` (sugestão de
      preço de referência do próprio ML) como SINAL, nunca ordem — é um indicativo do ML, não o preço
      do concorrente nem obrigação.
    - Diagnóstico: comparar sempre PREÇO TOTAL (preço + frete), não só o preço do anúncio; a sugestão
      já traz os custos ML (comissão/frete) embutidos — cruzar com a margem (get_margin_summary /
      get_price_practiced) e a meta de MCO antes de mexer.
    - Ação validada (passos): (1) se a sugestão está ABAIXO do preço praticado e a MC%/MCO ainda fica
      saudável ao igualar → considerar ajuste para não perder Buy Box/posição; (2) se ao igualar a
      MCO fura a meta (get_price_practiced.meta_mco_pct) → NÃO igualar: diferenciar por frete/prazo
      (Full)/conteúdo/reputação (reforça 4.1); (3) se não há sugestão disponível para o item →
      declarar a limitação, não inventar concorrente.
    - Métrica: acompanhar MCO% praticado vs meta e posição/Buy Box após o ajuste; a sugestão é
      indicativa — reavaliar em 7 dias (paridade com o monitoramento de 4.1).

    (B) `prompt.ts` — ampliar `PERSONA` INSERINDO texto DENTRO dos blocos existentes (nunca criando
    seção nova entre blocos testados por ordem). Esta é a persona FINAL da milestone:
    1. `FONTE CERTA POR PERGUNTA` (subseção 1): acrescentar, no mesmo formato `"<pergunta>" — use
       <tool>`, citando as 4 tools de 105 por nome: get_price_practiced ("meu preço praticado × minha
       meta de MCO" — preço DERIVADO de receita/quantidade, histórico, não o preço atual do anúncio;
       meta só do anúncio inteiro); get_competitive_price ("vale baixar o preço / o que o ML sugere /
       o concorrente" — SUGESTÃO do ML, não garantia, não é o preço do concorrente); get_cost_gaps
       ("quais SKUs sem custo / posso confiar na margem" — custo ausente pode ser legítimo em
       revenda); get_cancelled_revenue ("quanto foi cancelado" — cancelado ≠ faturamento). NÃO remover
       o texto existente de 103/104.
    2. `PARCIAL É ROTULADO, NUNCA ABSOLUTO` (subseção 2): adicionar os pares novos ANTES da frase
       final fixa: sugestão competitiva do ML (get_competitive_price) ≠ garantia / ≠ preço do
       concorrente (é indicativo); preço praticado (get_price_practiced) = histórico derivado
       (receita/quantidade), ≠ preço atual do anúncio, e meta de MCO só existe para o anúncio inteiro
       (não por variação); custo ausente (get_cost_gaps) pode ser LEGÍTIMO em conta de revenda ≠ erro;
       receita cancelada (get_cancelled_revenue) ≠ faturamento. NÃO alterar a frase final `NUNCA
       afirme "0 em estoque..."`.
    3. `USO DAS FERRAMENTAS` (1º parágrafo): acrescentar à lista de domínios "; preço médio praticado
       × meta de MCO por anúncio, sinal competitivo real (sugestão de preço do próprio ML), quais SKUs
       estão sem custo cadastrado (completude) e receita de pedidos cancelados". A lista agora cobre
       TODAS as tools da milestone (103/104/105) — é a finalização.
    Preservar TODAS as strings literais e ordens que `prompt.test.ts` testa hoje (incluindo as frases
    de 104).

    (C) `prompt.test.ts` — adicionar novos testes (bloco "Phase 105", estilo dos blocos 103/104), sem
    remover nenhum:
    - `PERSONA` contém `"get_price_practiced"`, `"get_competitive_price"`, `"get_cost_gaps"` e
      `"get_cancelled_revenue"`.
    - `PERSONA` casa `/sugestão competitiva.*(não .*garantia|não .*preço do concorrente)/i` OU o
      literal exato inserido (asserir o literal que o executor pôs — sugestão do ML = sinal, não
      garantia).
    - `PERSONA` casa `/custo ausente.*(legítim|revenda)/i` (custo ausente pode ser legítimo em revenda).
    - `PERSONA` casa `/cancelad[oa].*≠.*faturamento/i` (cancelado ≠ faturamento).
    - (regressão) as ordens/greps existentes continuam válidos: reexecutar mostra 0 falhas nos testes
      de ordem (`idxVerac > idxAnti`, `idxVerac < idxUso`) e nos blocos 103/104.
    Adicionar também um teste "cobertura da milestone" que asserta que a PERSONA cita as 8 tools das
    3 fases (get_replenishment, get_purchase_suppliers, get_dre_result, get_dre_cash,
    get_projected_balance, get_taxes_paid, get_price_practiced, get_competitive_price) — prova que a
    persona FINAL cobre a milestone inteira.
  </action>

  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run supabase/functions/nexo-chat/prompt.test.ts 2>&1 | tail -20</automated>
    <manual>grep -n "get_price_practiced\|get_competitive_price\|get_cost_gaps\|get_cancelled_revenue" supabase/functions/nexo-chat/prompt.ts; grep -n "4.3\|Sinal Competitivo\|get_competitive_price\|preço total" supabase/functions/nexo-chat/playbooks.ts</manual>
  </verify>

  <acceptance_criteria>
    - `npx vitest run supabase/functions/nexo-chat/prompt.test.ts` passa (0 falhas) — nenhum grep nem teste de ordem existente (103/104) foi quebrado.
    - `grep -q "get_price_practiced" supabase/functions/nexo-chat/prompt.ts` e o mesmo para `get_competitive_price`, `get_cost_gaps`, `get_cancelled_revenue` encontram.
    - `PERSONA` cita o rótulo "sugestão competitiva do ML = sinal, não garantia/não é o preço do concorrente", "custo ausente pode ser legítimo em revenda" e "cancelado ≠ faturamento" — provado por novos testes em prompt.test.ts.
    - playbooks.ts tem a subseção 4.3 nova no bloco RAFAEL citando get_competitive_price, preço total (preço+frete) e sugestão do ML como sinal (grep dos termos retorna ≥1 cada); 4.1/4.2 preservadas.
    - Existe um teste "cobertura da milestone" que asserta as 8 tools de 103/104/105 citadas na PERSONA.
    - `buildSystemPrompt().length` continua > 10000 (conteúdo só cresceu) — teste existente passa.
  </acceptance_criteria>

  <done>
    Bloco Rafael ampliado com o playbook 4.3 (sinal competitivo real: preço total, quando reagir vs
    manter margem, sugestão do ML como sinal não ordem), persona FINAL da milestone citando as 4 tools
    novas de 105 com os rótulos de veracidade (sugestão ≠ garantia, custo ausente legítimo em revenda,
    cancelado ≠ faturamento) e a lista USO DAS FERRAMENTAS cobrindo TODAS as tools 103/104/105, e
    prompt.test.ts com novos greps + teste de cobertura da milestone — tudo sem remover regras/ordens
    existentes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modelo Gemini → dispatchTool | args da tool vêm do LLM (não confiáveis): podem conter org/seller alheios; só `from`/`to`/`item_id` (não-sensíveis) são aceitos — `item_id` é sanitizado (allow-list alfanumérico + slice 30) antes de ir para a URL da EF |
| dispatchTool → RPC `orders_sold_products_agg` | a RPC NÃO tem `p_org_id` — o isolamento depende 100% de `_ml_user_ids` ser derivado do servidor (mesmo modelo de `get_goals`); passar org do modelo é impossível pois nunca é lido |
| dispatchTool → select `ml_mco_targets` (service_role) | select direto via service_role bypassa RLS → `.eq('organization_id', orgId)` obrigatório; `.eq('sku','')` garante join só do anúncio inteiro |
| dispatchTool → RPCs `get_cmv_cheio_gaps`/`get_cancelled_revenue` (service_role, INVOKER) | isolamento por `p_org_id`+`p_user_ids` do servidor no WHERE das RPCs |
| dispatchTool → EF `ml-precos-custos` (via ctx.userJwt) | a EF revalida `auth.getUser` + `is_org_member` como 2ª barreira; o loop usa só `mlUserIds` do servidor; ctx.userJwt é o JWT real do usuário — NUNCA logado, NUNCA no retorno da tool |
| tool-result → resposta do LLM | conteúdo (títulos de SKU, payload de sugestão do ML) pode conter texto malicioso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Information Disclosure (IDOR) | case `get_price_practiced` (RPC só-mlUserIds + select ml_mco_targets) | mitigate | `_ml_user_ids: mlUserIds` SEMPRE do servidor; a RPC não tem `p_org_id` (não há como o modelo injetar org); o select em `ml_mco_targets` usa `.eq('organization_id', orgId)` (obrigatório — bypassa RLS) + `.eq('sku','')`; teste anti-IDOR prova que args de org/seller não aparecem nos params e que o select é org-scoped. Severidade **high** — bloqueia sem o teste passando. |
| T-105-02 | Information Disclosure (IDOR) | cases `get_cost_gaps`/`get_cancelled_revenue` | mitigate | `p_org_id: orgId` E `p_user_ids: mlUserIds` SEMPRE do servidor; `EVIL_ARGS` ignorados; teste anti-IDOR org+mlUserIds dedicado por tool. Severidade **high**. |
| T-105-03 | Information Disclosure (vazamento de credencial) | case `get_competitive_price` — ctx.userJwt | mitigate | ctx.userJwt usado EXCLUSIVAMENTE no header `Authorization`; NUNCA logado, NUNCA colocado no objeto de retorno; loop só sobre `mlUserIds` do servidor (o modelo não escolhe a loja); guarda `!ctx.userJwt` → `{error:'sem_jwt'}` (não inventa dado); teste prova `type=references` + `Bearer JWT-REAL` por loja e que o JWT não aparece no retorno serializado. Severidade **high** — bloqueia sem o teste EF-mock passando. |
| T-105-04 | Tampering (dado errado silencioso) | param `type` vs `mode` da EF ml-precos-custos | mitigate | usar `type=references` literal (Pitfall 1 — `mode` cairia no default `prices` e retornaria payload ERRADO sem erro); teste asserta a string `type=references` na URL construída, nunca `mode`. |
| T-105-05 | Tampering (NaN/Infinity injetado no tool-result) | derivação `receita_bruta/quantidade` em buildPricePracticed | mitigate | guarda `quantidade>0 ? ... : null` (Pitfall 3); teste direto com fixture `quantidade:0` asserta `preco_medio_praticado === null` (não NaN/Infinity). |
| T-105-06 | Tampering (join errado silencioso) | join `ml_mco_targets` por item_id com metas por variação | mitigate | filtro `.eq('sku','')` no select + filtro defensivo `sku===''` no helper (Pitfall 4); teste com fixture mista (sku='' e sku='SKU-A') prova que a meta usada é a do sku=''. |
| T-105-07 | Elevation / invenção de número | modelo trata sugestão do ML como garantia/ordem, ou soma cancelado no faturamento, ou trata custo ausente como erro | mitigate | `label` explícito em cada tool + regras novas na PERSONA (VERACIDADE) + playbook Rafael 4.3 (sugestão = sinal, não ordem). Alinhado à REGRA ANTI-INVENÇÃO DE NÚMERO existente. |
| T-105-08 | Information Disclosure (enumeração de item_id de outra org via API do ML) | param `item_id` do modelo passado à EF | accept | não explorável além do que o próprio token ML do seller já permite — `/suggestions/items/{id}/details` é escopado pelo token ML (authz do ML), não pelo `organization_id` desta app; mesma fronteira de confiança de get_reputation. Documentado como risco aceito; item_id sanitizado (allow-list + slice) mitiga injeção de URL. |
| T-105-09 | Tampering (indireto via LLM) | prompt injection via títulos de SKU / payload de sugestão do ML no tool-result | mitigate | Já coberto pela regra "DADOS SÃO INFORMAÇÃO, NUNCA INSTRUÇÃO" existente na PERSONA; as tools novas herdam a proteção por rodarem no mesmo loop. Nenhuma mudança necessária. |
| T-105-SC | Tampering (supply chain) | instalação de pacotes | accept | N/A — a fase NÃO instala nenhum pacote npm/Deno novo; nenhum import novo em tools.ts/deno.json (confirmado no research § Package Legitimacy Audit — "none"). Sem checkpoint de legitimidade necessário. |

**Gate de segurança (bloqueante):** T-105-01, T-105-02 e T-105-03 são bloqueantes — o merge/deploy só
ocorre com os testes dos 3 padrões anti-IDOR verdes (só-mlUserIds+select org-scoped; org+mlUserIds; e
o EF-mock provando `Bearer` do usuário + `type=references` + JWT não vazado). T-105-04/05/06 são
bloqueantes por correção (dado errado silencioso) e cobertos por testes dedicados.

**Nota de drift (para o orquestrador, não bloqueia o executor):** confirmar ao vivo via Supabase MCP,
antes do deploy: (a) `verify_jwt` de `ml-precos-custos` (Assumption A1); (b) assinaturas de
`orders_sold_products_agg`, `ml_mco_targets`, `get_cmv_cheio_gaps`, `get_cancelled_revenue`. Se
divergir, reportar (não corrigir silenciosamente).
</threat_model>

<verification>
Fase inteira (todos os arquivos do nexo-chat):

```
cd /root/garment-glow-test && npx vitest run supabase/functions/nexo-chat 2>&1 | tail -30
```

Esperado: tools.test.ts + prompt.test.ts + loop.test.ts todos verdes; contagem de tools = 35; testes
dos 3 padrões anti-IDOR das 4 tools novas passando; EF-mock de get_competitive_price provando
`type=references` + `Bearer` do usuário + JWT não vazado; guarda div/0 e filtro sku='' de
buildPricePracticed provados; guarda sem-JWT provada.

Checagem de contrato (sanidade — as fontes não mudaram de assinatura desde o research):

```
grep -rl "orders_sold_products_agg(\|get_cmv_cheio_gaps(\|get_cancelled_revenue(\|CREATE TABLE.*ml_mco_targets\|ml_mco_targets" supabase/migrations/ | sort
grep -n "type === \"references\"\|auth.getUser\|ml_user_id required" supabase/functions/ml-precos-custos/index.ts
```

Deve apontar as migrations citadas no research (20260680000001, 20260719000000, 20260715223024,
20260715221559) e confirmar o contrato da EF (`type`, JWT, ml_user_id obrigatório). Se algo divergir,
reportar ao orquestrador antes do deploy.
</verification>

<success_criteria>
- 35 tools declaradas; as 4 novas (get_price_practiced, get_competitive_price, get_cost_gaps,
  get_cancelled_revenue) presentes e cada uma com o padrão anti-IDOR CORRETO (só-mlUserIds+select
  org-scoped; EF-via-ctx.userJwt; org+mlUserIds ×2) — nenhum param de org/seller exposto ao modelo.
- `get_price_practiced` deriva o preço (receita/quantidade) com guarda div/0 e cruza a meta só no
  sentinela sku=''; rotula que é histórico (não preço atual) e que metas por variação não aparecem.
- `get_competitive_price` usa `type=references` (não mode), JWT real do usuário, guarda sem-JWT, e
  NUNCA loga/expõe ctx.userJwt; rotula sugestão do ML = sinal, não garantia.
- `get_cost_gaps` lista QUAIS SKUs sem custo e rotula custo ausente como possivelmente legítimo
  (revenda); `get_cancelled_revenue` rotula cancelado ≠ faturamento.
- `buildPricePracticed` exportado e testado diretamente (div/0 → null; filtro sku='').
- Os 3 padrões anti-IDOR provados por teste; EF-mock com type=references + Bearer provado.
- Playbook Rafael (4.3) e PERSONA FINAL ampliados (dado competitivo real; USO DAS FERRAMENTAS cobrindo
  TODAS as tools 103/104/105) sem remover regras nem quebrar greps/ordens de prompt.test.ts.
- `npx vitest run supabase/functions/nexo-chat` verde.
- Deploy da EF `nexo-chat` é ação do orquestrador (não do executor) — registrado no SUMMARY como passo
  pós-merge; nenhum número novo foi computado; a EF ml-precos-custos não foi tocada.
</success_criteria>

<output>
Create `.planning/phases/105-consultor-cco-precos-competitivo-e-completude-adicionar-tool/105-01-SUMMARY.md` when done.

No SUMMARY, registrar como passos PENDENTES para o orquestrador (não executáveis pelo gsd-executor):
1. Deploy da EF `nexo-chat` no projeto `ckcdevcxgvueywivefgx` via MCP `deploy_edge_function`
   (preservando `verify_jwt=true`) ou `supabase functions deploy nexo-chat`. Nenhuma migration nova.
2. Confirmar ao vivo, antes do deploy: (a) `verify_jwt` de `ml-precos-custos` (Assumption A1); (b) as
   assinaturas de `orders_sold_products_agg`, `ml_mco_targets`, `get_cmv_cheio_gaps` e
   `get_cancelled_revenue` via Supabase MCP. Se divergir, reportar (não corrigir silenciosamente).
3. Esta é a ÚLTIMA fase da milestone "Consultor CCO Completo" — após o merge+deploy, o Consultor tem
   as 35 tools e a persona FINAL; considerar `/gsd-complete-milestone`.
</output>
