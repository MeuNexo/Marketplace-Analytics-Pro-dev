---
phase: 100-break-even-de-caixa-do-m-s-quanto-falta-vender-para-fechar-n
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - src/lib/dreCashForecast.ts
  - src/lib/dreCashForecast.test.ts
  - src/hooks/useDreCashForecast.ts
  - src/components/mercadolivre/DreCashForecastCard.tsx
  - src/pages/mercadolivre/MLDreCaixa.tsx
autonomous: false
requirements: [BEC-02, BEC-03, BEC-04]
must_haves:
  truths:
    - "Na /dre-caixa com o mês corrente selecionado, o dono vê no topo quanto falta entrar para fechar no zero (gap) em linguagem leiga"
    - "O dono vê quanto precisa vender (~R$ Y) e até que dia (dia-limite = fim do mês − lag), com o aviso de que venda depois do dia-limite só vira caixa no mês seguinte"
    - "Editar a meta (ex. sobrar R$ 20.000) recalcula gap e venda necessária na hora, sem chamada de rede e sem persistir em banco"
    - "Ritmo necessário × ritmo real 7d com semáforo: verde (real ≥ necessário), vermelho (abaixo), neutro (gap ≤ 0 — mês já fecha na meta)"
    - "Selecionar um mês passado esconde o painel por completo"
    - "Quando a RPC devolve alertas de recorrência suspeita, o painel exibe o aviso com categoria, valor e nº de meses futuros"
    - "Cada número do painel é explicável: a composição do cálculo (componentes de saída prevista e entrada garantida) é visível num drill/collapsible"
  artifacts:
    - path: "src/lib/dreCashForecast.ts"
      provides: "Matemática pura do painel: gap, meta, venda necessária, dia-limite, ritmos, semáforo, alertas"
      exports: ["buildDreCashForecast"]
      min_lines: 80
    - path: "src/lib/dreCashForecast.test.ts"
      provides: "Testes vitest da lib pura (TDD)"
      min_lines: 80
    - path: "src/hooks/useDreCashForecast.ts"
      provides: "Hook TanStack Query da RPC get_dre_cash_forecast"
      exports: ["useDreCashForecast"]
    - path: "src/components/mercadolivre/DreCashForecastCard.tsx"
      provides: "Card 'Fechar o mês' com gap, venda necessária, dia-limite, meta editável, ritmos e alerta de recorrência"
    - path: "src/pages/mercadolivre/MLDreCaixa.tsx"
      provides: "Card montado no topo, condicional ao mês corrente"
      contains: "DreCashForecastCard"
  key_links:
    - from: "src/components/mercadolivre/DreCashForecastCard.tsx"
      to: "src/hooks/useDreCashForecast.ts"
      via: "hook TanStack Query alimenta o card"
      pattern: "useDreCashForecast"
    - from: "src/hooks/useDreCashForecast.ts"
      to: "public.get_dre_cash_forecast (RPC do 100-01)"
      via: "supabase.rpc num único round-trip"
      pattern: "rpc\\(\"get_dre_cash_forecast\""
    - from: "src/components/mercadolivre/DreCashForecastCard.tsx"
      to: "src/lib/dreCashForecast.ts"
      via: "toda a matemática vem da lib pura (card não calcula gap/ritmo por conta própria)"
      pattern: "buildDreCashForecast"
    - from: "src/pages/mercadolivre/MLDreCaixa.tsx"
      to: "src/components/mercadolivre/DreCashForecastCard.tsx"
      via: "render condicional isCurrentMonth no topo da página"
      pattern: "isCurrentMonth && "
---

<objective>
Entregar o painel "Fechar o mês" no topo da `/dre-caixa` (mês corrente): lib pura `dreCashForecast.ts` testada (BEC-03), hook `useDreCashForecast` e card `DreCashForecastCard` (BEC-02), com meta editável client-side, semáforo de ritmo, aviso de recorrência suspeita (BEC-04, lado exibição) e ok visual do Wesley como última task.

Purpose: responder a pergunta do dono (D-01): "quanto falta entrar para o mês fechar no zero (ou na meta), e quanto preciso vender até que dia?".
Output: 4 arquivos novos + 1 edição mínima em MLDreCaixa.tsx.

Mapa de decisões do 100-CONTEXT.md (numeração atribuída no plano 100-01, repetida aqui): D-01 pergunta do painel · D-02 matemática do gap · D-03 conversão/dia-limite · D-04 meta client-side · D-05 ritmo/semáforo · D-06 guarda anti-fantasma · D-07 backend · D-08 frontend · D-09 separações.

Este plano implementa D-01, D-02 (composição client-side), D-03 (dia-limite/aviso), D-04, D-05, D-06 (aviso), D-08 e D-09. Cobertura de requirements: BEC-02, BEC-03, BEC-04 (exibição).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-break-even-de-caixa-do-m-s-quanto-falta-vender-para-fechar-n/100-CONTEXT.md
@.planning/phases/100-break-even-de-caixa-do-m-s-quanto-falta-vender-para-fechar-n/100-01-PLAN.md
@src/lib/dreCashCascade.ts
@src/lib/dreCashCascade.test.ts
@src/hooks/useDreCash.ts
@src/pages/mercadolivre/MLDreCaixa.tsx
</context>

## Interface herdada do plano 100-01 (contrato da RPC)

`get_dre_cash_forecast(p_org_id uuid, p_month date)` → linhas `(secao, categoria, total, n)`. Categorias canônicas (nomes exatos — ver tabela completa no 100-01-PLAN.md): secao `saida_prevista` → saidas_pagas, estornos_ocorridos, saidas_pendentes, estornos_previstos, imposto_previsto_restante; secao `entrada` → entradas_liberadas, entradas_agendadas; secao `taxa` → taxa_liquido_bruto, taxa_estornos, taxa_venda_para_caixa (frações 0-1, podem ser null), lag_liberacao_dias; secao `ritmo` → vendas_7d_media_diaria; secao `alerta_recorrencia` → 0..N linhas (categoria = categoria Tiny, total = valor repetido, n = meses futuros).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lib pura dreCashForecast.ts (TDD RED→GREEN)</name>
  <files>src/lib/dreCashForecast.ts, src/lib/dreCashForecast.test.ts</files>
  <read_first>
    - .planning/phases/100-break-even-de-caixa-do-m-s-quanto-falta-vender-para-fechar-n/100-01-PLAN.md (seção "Contrato de saída da RPC" — nomes exatos das categorias)
    - src/lib/dreCashCascade.ts + src/lib/dreCashCascade.test.ts (padrão de lib pura: interfaces + round2 + helpers de row nos testes — espelhar estrutura)
    - .planning/phases/100-break-even-de-caixa-do-m-s-quanto-falta-vender-para-fechar-n/100-CONTEXT.md (D-02 matemática, D-03 dia-limite, D-04 meta, D-05 ritmo/semáforo, D-06 aviso)
  </read_first>
  <behavior>
    - Test 1 (gap, D-02): rows completas → saidasPrevistas.total = pagas + estornosOcorridos + pendentes + estornosPrevistos + impostoPrevistoRestante; entradasGarantidas.total = liberadas + agendadas; gap = saídas − entradas (SEM projeção de vendas novas)
    - Test 2 (meta, D-04): meta 20000 sobre gap 0 → gapComMeta 20000 e vendaNecessaria = 20000 ÷ taxaVendaParaCaixa; mudar meta muda o resultado sem tocar nas rows
    - Test 3 (mês já fecha, D-05): gapComMeta ≤ 0 → semaforo "neutro", vendaNecessaria 0, ritmoNecessario null
    - Test 4 (null-safety, D-03): taxa_venda_para_caixa null ou ≤ 0 → vendaNecessaria null, sem divisão por zero nem NaN em nenhum campo
    - Test 5 (dia-limite, D-03): hoje 2026-07-17, lag 14 → diaLimite = 2026-07-17 do fim do mês (31/07) − 14 dias = 17/07; diasRestantes = 0 → prazoEsgotado true; lag 7 → diaLimite 24/07, diasRestantes 7
    - Test 6 (semáforo, D-05): ritmoReal7d ≥ ritmoNecessario → "verde"; abaixo → "vermelho"
    - Test 7 (alertas, D-06): linhas secao alerta_recorrencia viram alertasRecorrencia[{categoria, valor, mesesFuturos}] ordenadas por valor desc; sem linhas → []
    - Test 8 (vazio): rows [] → hasData false, gap 0, semaforo "neutro", nenhum NaN
    - Test 9 (arredondamento): todos os valores monetários com 2 casas (padrão round2 do dreCashCascade)
  </behavior>
  <action>
    TDD RED→GREEN em 2 commits (test primeiro, falhando; depois implementação — padrão do projeto desde a Phase 62).

    Criar `src/lib/dreCashForecast.ts` — módulo 100% puro, sem imports de React/Supabase/TanStack/date-fns pesado (aritmética de datas com Date nativo, como faz o dreCashCascade com números). Exports:
    - `interface DreCashForecastRow { secao: string; categoria: string; total: number | null; n: number }` (mesmo shape cru da RPC, análogo a DreCashRow)
    - `interface ForecastAlertaRecorrencia { categoria: string; valor: number; mesesFuturos: number }`
    - `interface DreCashForecast` com: `saidasPrevistas { pagas; estornosOcorridos; pendentes; estornosPrevistos; impostoPrevistoRestante; total }`, `entradasGarantidas { liberadas; agendadas; total }`, `gap`, `gapComMeta`, `taxaVendaParaCaixa: number | null`, `lagLiberacaoDias: number`, `diaLimite: Date`, `diasRestantes: number`, `prazoEsgotado: boolean`, `vendaNecessaria: number | null`, `ritmoNecessario: number | null`, `ritmoReal7d: number`, `semaforo: "verde" | "vermelho" | "neutro"`, `alertasRecorrencia: ForecastAlertaRecorrencia[]`, `hasData: boolean`
    - `function buildDreCashForecast(rows: DreCashForecastRow[], opts: { meta: number; hoje: Date }): DreCashForecast`

    Regras (todas travadas no 100-CONTEXT):
    - Particionar rows por secao/categoria na ENTRADA da função, antes de qualquer soma (mesmo guardrail do buildDreCashCascade). Categoria ausente → 0 (taxas ausentes → null).
    - gap = saidasPrevistas.total − entradasGarantidas.total (D-02 — projeção de vendas novas NUNCA entra no gap). gapComMeta = gap + meta (meta ≥ 0, default 0 = zero a zero, D-04).
    - vendaNecessaria = gapComMeta ≤ 0 → 0; taxaVendaParaCaixa null/≤0 → null; senão gapComMeta ÷ taxaVendaParaCaixa (D-02).
    - lagLiberacaoDias = valor da row (fallback 14 se null — mesmo fallback documentado na RPC). diaLimite = último dia do mês de `opts.hoje` − lag dias (D-03). diasRestantes = dias de calendário de hoje até diaLimite, mínimo 0 (comparar por dia-calendário local, meia-noite a meia-noite — padrão da decisão da Phase 90-03, não delta bruto de ms). prazoEsgotado = diasRestantes === 0 e gapComMeta > 0.
    - ritmoNecessario = vendaNecessaria ÷ diasRestantes quando vendaNecessaria > 0 e diasRestantes > 0; senão null. ritmoReal7d = row vendas_7d_media_diaria (0 se ausente).
    - semaforo (D-05): "neutro" se gapComMeta ≤ 0; "verde" se ritmoReal7d ≥ ritmoNecessario (ambos definidos); "vermelho" nos demais casos com gap aberto (inclui prazoEsgotado e ritmoNecessario null — sem prova de que fecha, não fica verde; documentar essa escolha no comentário).
    - round2 (Math.round(x*100)/100) em todos os monetários, igual dreCashCascade.

    Criar `src/lib/dreCashForecast.test.ts` cobrindo os 9 comportamentos do behavior, com helpers de construção de row por secao (espelho dos helpers entradaRow/saidaRow do dreCashCascade.test.ts).
  </action>
  <verify>
    <automated>npx vitest run src/lib/dreCashForecast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/dreCashForecast.test.ts` verde com ≥ 9 testes cobrindo os comportamentos listados
    - Pureza: `grep -v '^\s*//' src/lib/dreCashForecast.ts | grep -cE 'from "(react|@tanstack|@/integrations|@/hooks|@/contexts)' ` == 0
    - `grep -q "export function buildDreCashForecast" src/lib/dreCashForecast.ts`
    - Guardrail D-02 auditável: `grep -v '^\s*//' src/lib/dreCashForecast.ts | grep -c "vendas_7d_media_diaria"` ≥ 1 e o cálculo de `gap` referencia apenas os agregados de saidasPrevistas/entradasGarantidas (ritmo real nunca soma no gap)
    - 2 commits TDD: o primeiro contém só o test file (RED), o segundo a implementação (GREEN)
    - `npx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Lib pura testada com a matemática completa do painel: gap, meta, venda necessária, dia-limite, ritmos, semáforo e alertas — sem rede, sem React.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hook useDreCashForecast + card DreCashForecastCard + montagem na /dre-caixa</name>
  <files>src/hooks/useDreCashForecast.ts, src/components/mercadolivre/DreCashForecastCard.tsx, src/pages/mercadolivre/MLDreCaixa.tsx</files>
  <read_first>
    - src/hooks/useDreCash.ts (padrão a clonar: useOrganization + useQuery + normalização de numeric-string + regra pMonth "YYYY-MM-01")
    - src/pages/mercadolivre/MLDreCaixa.tsx (estrutura de seções: onde ficam pMonth/isCurrentMonth/BadgeResposta; padrão KPICard/Collapsible/Alert já importados)
    - src/lib/dreCashForecast.ts (API da Task 1)
    - .planning/phases/100-break-even-de-caixa-do-m-s-quanto-falta-vender-para-fechar-n/100-CONTEXT.md (D-08 microcopy leiga, D-09 separações, Specifics: explicabilidade drill/tooltip)
  </read_first>
  <action>
    **Hook `src/hooks/useDreCashForecast.ts`** — clone estrutural de useDreCash: `useDreCashForecast(pMonth: string, enabled: boolean)`; queryKey `["dre-cash-forecast", orgId, pMonth]`; `enabled: !!orgId && !!pMonth && enabled`; staleTime 5min; `supabase.rpc("get_dre_cash_forecast", { p_org_id: orgId, p_month: pMonth })`; normalizar cada linha para `DreCashForecastRow` (numeric pode chegar string — Number(); total null preservado). pMonth SEMPRE "YYYY-MM-01" (mesma regra documentada do useDreCash). O parâmetro `enabled` existe porque o painel é só do mês corrente — a página passa `isCurrentMonth` e o hook nem dispara para mês passado.

    **Card `src/components/mercadolivre/DreCashForecastCard.tsx`** — componente novo (não inflar MLDreCaixa.tsx). Props: `{ pMonth: string }`. Internamente: `useDreCashForecast(pMonth, true)` + `useState` para a meta (number, default 0 — zero a zero, D-04, SEM persistência) + `useMemo(() => buildDreCashForecast(rows ?? [], { meta, hoje: new Date() }), [rows, meta])`. Toda a matemática vem da lib — o card NUNCA recalcula gap/ritmo por conta própria.

    Layout (Card do shadcn, título "Fechar o mês", reutilizar tokens kpi-positive/kpi-negative/muted e formatação BRL da página):
    1. **Headline do gap** em destaque, linguagem leiga (D-01/D-08): gapComMeta > 0 → "Faltam R$ {gapComMeta} para fechar {meta > 0 ? "na meta" : "no zero"}" (tom negativo); gapComMeta ≤ 0 → "O mês já fecha {na meta|no zero} — sobra prevista de R$ {abs}" (tom positivo).
    2. **Venda necessária + dia-limite** (D-02/D-03): "Você precisa vender ~R$ {vendaNecessaria} até dia {diaLimite dd/MM}" + aviso permanente "vendas a partir de {diaLimite+1 dd/MM} só viram caixa no mês seguinte". vendaNecessaria null → "—" com nota "sem dados suficientes para medir a conversão venda→caixa". prazoEsgotado → aviso âmbar "Vendas novas não viram mais caixa este mês — o gap só fecha com as liberações já agendadas".
    3. **Ritmos + semáforo** (D-05): "Ritmo atual: R$ {ritmoReal7d}/dia · Necessário: R$ {ritmoNecessario}/dia" com pill/ícone verde (real ≥ necessário), vermelho (abaixo) ou neutro ("mês já fecha na meta").
    4. **Meta editável** (D-04): Input numérico rotulado "Quero que sobre (R$)" que atualiza o useState na hora — recálculo instantâneo client-side, zero rede, zero escrita em banco.
    5. **Explicabilidade** (Specifics do CONTEXT — todo número explicável): Collapsible "Como esse número é calculado" listando cada componente com valor: saídas já pagas, estornos ocorridos, a pagar até o fim do mês, estornos previstos (taxa {taxa_estornos}% × agendadas), imposto previsto restante, entradas liberadas, agendadas a liberar, conversão venda→caixa ({taxa}%), prazo médio de liberação ({lag} dias).
    6. **Alerta de recorrência** (D-06/BEC-04): quando alertasRecorrencia.length > 0, Alert destacado: "Contas pendentes deste mês se repetem em meses futuros — confira se não é recorrência acidental no Tiny:" + lista "{categoria} — R$ {valor} (aparece em {n} meses futuros)".
    7. Estados: loading → Skeleton compacto; erro → Alert discreto com retry; hasData false → texto neutro "Sem dados de previsão para este mês".

    Regras duras: renderizar texto de categoria SEMPRE como texto React puro (nunca HTML injetado — categorias vêm do Tiny). Zero acoplamento com a página de tesouraria/projeção de saldo (D-09): não importar nada daquela página nem citar as RPCs/tabelas de saldo dela em código ou comentários — as fontes deste card são exclusivamente a RPC nova (via hook) e a lib pura.

    **Montagem em `src/pages/mercadolivre/MLDreCaixa.tsx`** — edição mínima: importar DreCashForecastCard e renderizar `{isCurrentMonth && <DreCashForecastCard pMonth={pMonth} />}` imediatamente após o BadgeResposta (topo da página, D-08). Mês passado selecionado → painel some por completo. NENHUMA outra seção da página muda (cascata/KPIs/histórico intocados — previsão é informativa, nunca altera os números apurados).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 0 erros; `npx vitest run` toda a suíte verde; `npm run build` ok
    - `grep -q 'rpc("get_dre_cash_forecast"' src/hooks/useDreCashForecast.ts`
    - `grep -q "buildDreCashForecast" src/components/mercadolivre/DreCashForecastCard.tsx` e `grep -q "useDreCashForecast" src/components/mercadolivre/DreCashForecastCard.tsx`
    - `grep -q "isCurrentMonth && " src/pages/mercadolivre/MLDreCaixa.tsx` na linha que monta DreCashForecastCard (painel só no mês corrente)
    - Zero acoplamento com Fluxo de Caixa (D-09): `grep -rn "get_cashflow\|get_treasury_panel\|initial_balance\|financial_settings\|useTreasury" src/lib/dreCashForecast.ts src/hooks/useDreCashForecast.ts src/components/mercadolivre/DreCashForecastCard.tsx | grep -cv '^\s*$'` == 0
    - Meta 100% client-side (D-04): `grep -v '^\s*//' src/components/mercadolivre/DreCashForecastCard.tsx | grep -cE '\.(insert|upsert|update)\('` == 0 e `grep -c "localStorage" src/components/mercadolivre/DreCashForecastCard.tsx` == 0
    - Anti-XSS: `grep -c "dangerouslySetInnerHTML" src/components/mercadolivre/DreCashForecastCard.tsx` == 0
    - Diff de MLDreCaixa.tsx restrito a import + render condicional (nenhuma linha da cascata/KPIs/histórico alterada)
  </acceptance_criteria>
  <done>Painel "Fechar o mês" funcional no topo da /dre-caixa, mês corrente apenas, com meta editável instantânea, semáforo, explicabilidade e alerta de recorrência — suíte, tsc e build verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ok visual do Wesley no preview</name>
  <action>Checkpoint humano final da phase: orquestrador gera preview Vercel da branch atual (workflow reference_vercel_preview_workflow: branch + .env.production + share link) e conduz o roteiro de validação visual 1-8 com o Wesley. Iterar ajustes de microcopy/layout neste mesmo plano até o ok.</action>
  <what-built>Painel "Fechar o mês" no topo da /dre-caixa: gap para fechar no zero/meta, venda necessária com dia-limite, meta editável, ritmo com semáforo, composição explicável e alerta anti-fantasma. Backend (RPC) já aplicado e provado no plano 100-01.</what-built>
  <how-to-verify>
    Orquestrador gera o preview (branch atual `gsd/phase-99-dre-caixa-mp` — workflow Vercel de referência: reference_vercel_preview_workflow) e manda o link ao Wesley. Roteiro:
    1. Abrir /dre-caixa no mês corrente → painel "Fechar o mês" no topo, logo abaixo do badge-resposta.
    2. Conferir o gap contra a leitura dele: "Faltam R$ X para fechar no zero" faz sentido vs entradas/saídas do mês em andamento (mesmos números da cascata abaixo — reconciliados na prova do 100-01).
    3. Conferir a frase "Você precisa vender ~R$ Y até dia D" e o aviso do dia-limite (venda depois de D só vira caixa no mês seguinte).
    4. Digitar uma meta (ex. 20000) no campo "Quero que sobre (R$)" → gap e venda necessária recalculam na hora.
    5. Conferir "Ritmo atual × Necessário" e a cor do semáforo.
    6. Abrir "Como esse número é calculado" → composição legível (pagas, estornos, a pagar, imposto previsto, liberadas, agendadas, taxa, prazo de liberação).
    7. Se houver aviso de recorrência suspeita, validar que as contas listadas fazem sentido (lição ads/full de 17/07).
    8. Voltar um mês no seletor → painel some; cascata/KPIs/histórico da Phase 99 idênticos ao que ele validou ontem.
  </how-to-verify>
  <verify>
    <automated>MISSING — validação é visual/humana em preview com dados reais; gates automatizados já cobertos na Task 2 (tsc/vitest/build/greps).</automated>
    <human-check>Roteiro 1-8 percorrido: gap coerente, venda necessária + dia-limite, meta recalcula na hora, semáforo, composição explicável, alerta de recorrência (se houver), painel oculto em mês passado, Phase 99 intocada.</human-check>
  </verify>
  <done>Ok visual do Wesley registrado; ajustes pedidos (se houver) implementados e reaprovados.</done>
  <resume-signal>Wesley aprova ("ok visual") ou descreve ajustes de microcopy/layout — iterar no mesmo plano até o ok.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC → card (dados do Tiny/MP) | categorias e valores vindos de sistemas externos renderizados na UI |
| input do usuário (meta) | número digitado pelo dono, usado só em cálculo local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-04 | Tampering (XSS) | DreCashForecastCard (categorias do Tiny nos alertas) | mitigate | render como texto React puro; gate: zero dangerouslySetInnerHTML no card |
| T-100-05 | Information Disclosure | hook useDreCashForecast | mitigate | anti-IDOR é server-side (INVOKER+RLS, provado no 100-01); frontend só passa currentOrg.id do contexto, nunca re-filtra |
| T-100-06 | Denial of Service | recálculo da meta a cada tecla | accept | cálculo puro O(n) sobre ~15 linhas — custo desprezível, sem rede |
| T-100-SC | Tampering | dependências novas | accept | zero pacote novo (React/shadcn/TanStack já no projeto) |
</threat_model>

## Artifacts this phase produces (plano 02)

- **Lib pura:** `src/lib/dreCashForecast.ts` (export `buildDreCashForecast`, tipos `DreCashForecastRow`/`DreCashForecast`/`ForecastAlertaRecorrencia`)
- **Testes:** `src/lib/dreCashForecast.test.ts` (≥9 comportamentos)
- **Hook:** `src/hooks/useDreCashForecast.ts` (export `useDreCashForecast`)
- **Componente:** `src/components/mercadolivre/DreCashForecastCard.tsx`
- **Página:** `src/pages/mercadolivre/MLDreCaixa.tsx` (montagem condicional do card)

<verification>
- Suíte vitest completa + tsc + build verdes
- Greps de acoplamento zero com tesouraria/saldo, meta client-side e anti-XSS passam
- Painel visível só no mês corrente; Phase 99 intocada abaixo dele
- Ok visual do Wesley registrado (Task 3)
</verification>

<success_criteria>
- BEC-02: card no topo com gap em destaque + venda necessária + dia-limite + ritmo 7d + meta editável client-side
- BEC-03: lib pura testada com a matemática de gap/conversão/dia-limite (9+ testes)
- BEC-04: alerta de recorrência exibido quando a RPC detecta pendentes-fantasma
- D-09: nenhuma leitura de saldo/projeção da página de tesouraria; cascata apurada da Phase 99 inalterada
</success_criteria>

<output>
Criar `.planning/phases/100-break-even-de-caixa-do-m-s-quanto-falta-vender-para-fechar-n/100-02-SUMMARY.md` ao concluir, registrando: decisões de microcopy tomadas, escolha do semáforo em casos-limite e o veredito do ok visual.
</output>
