---
phase: 61-enriquecer-fornecedor-categoria-do-contas-a-pagar
plan: 03
type: execute
wave: 3
depends_on: ["61-02"]
files_modified: []
autonomous: false
requirements: [CASHFIX-07, CASHFIX-08]
tags: [validation, charts, regression, treasury, cash_outflows, phase-60]

must_haves:
  truths:
    - "Em prod, Composição de Custos por Mês mostra ≥3 categorias reais e Exposição por Fornecedor mostra ≥2 fornecedores reais — CASHFIX-08"
    - "Um sync executado APÓS o backfill NÃO derruba a contagem de category/supplier não-nulos (estabilidade pós-fix) — CASHFIX-07"
    - "Sem regressão da Phase 60: get_cashflow (toggle OFF/ON), reconciliação R$87.105,79 e a Exposição por Fornecedor seguem o comportamento da Phase 60"
  artifacts: []
  key_links:
    - from: "cash_outflows.category"
      to: "get_cost_by_month → CostCompositionChart"
      via: "RPC já lê a coluna; mais dados = mais categorias"
      pattern: "get_cost_by_month"
    - from: "cash_outflows.supplier"
      to: "get_supplier_exposure → SupplierExposureChart"
      via: "RPC já filtra supplier IS NOT NULL; mais dados = mais barras"
      pattern: "get_supplier_exposure"
---

<objective>
Validar que a phase atingiu o objetivo de negócio e não regrediu a Phase 60 (CASHFIX-07 + CASHFIX-08). Três frentes: (1) provar estabilidade — rodar mais um sync após o backfill e confirmar que as contagens de category/supplier NÃO caem; (2) validar os dois gráficos em prod (≥3 categorias, ≥2 fornecedores); (3) validar que a DFC da Phase 60 segue intacta (toggle OFF/ON, reconciliação R$87.105,79, Exposição por Fornecedor não some/zera com o toggle).

Purpose: O risco real do fix é introduzir regressão na Phase 60 ou que o sync continue derrubando dados. Esta é a prova final, end-to-end, contra o banco e a UI de prod.
Output: Critérios de sucesso 1, 4, 5 e 6 do ROADMAP confirmados.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-enriquecer-fornecedor-categoria-do-contas-a-pagar/61-RESEARCH.md
@.planning/phases/61-enriquecer-fornecedor-categoria-do-contas-a-pagar/61-02-SUMMARY.md
@.planning/phases/60-alinhamento-da-dfc-fluxo-de-caixa
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Estabilidade pós-fix — sync após backfill não derruba contagens + diversidade</name>
  <read_first>
    - 61-RESEARCH.md "Arquitetura de Validação" (SQL de contagem e diversidade) e §5 (comportamento das RPCs)
    - ROADMAP Phase 61, Success Criteria 1 e 4
  </read_first>
  <action>
    Ação executada pelo orquestrador via `mcp__supabase__execute_sql` + disparo de um sync real: capturar snapshot de contagens, rodar mais um `sync-tiny-payables`, e confirmar que com_cat/com_sup NÃO caem e que DISTINCT category > 1 e DISTINCT supplier > 1 (estabilidade pós-fix, CASHFIX-07/08).
  </action>
  <what-built>
    Backfill drenado (≥90%) no plano 61-02. Falta provar que o sync NÃO regride mais.
  </what-built>
  <how-to-verify>
    Via `mcp__supabase__execute_sql` no projeto `ckcdevcxgvueywivefgx`:
    1. Snapshot ANTES: `SELECT count(*) FILTER (WHERE category IS NOT NULL) AS com_cat, count(*) FILTER (WHERE supplier IS NOT NULL) AS com_sup FROM cash_outflows WHERE source='tiny';`
    2. Disparar um sync real da `sync-tiny-payables` (chamada cron-style, como na Phase 59).
    3. Snapshot DEPOIS: mesma query. CRITÉRIO 1: com_cat e com_sup NÃO caem (≥ snapshot anterior).
    4. Diversidade (CRITÉRIO 4): `SELECT count(DISTINCT category) AS cats, count(DISTINCT supplier) AS sups FROM cash_outflows WHERE category IS NOT NULL AND supplier IS NOT NULL;` → cats > 1 E sups > 1. E confirmar pct ≥ 90 (query do 61-02).
  </how-to-verify>
  <verify>
    <automated>MANUAL — execute_sql: contagens com_cat/com_sup não caem após sync; DISTINCT category > 1 e DISTINCT supplier > 1; pct ≥ 90</automated>
  </verify>
  <done>Sync pós-backfill não derruba com_cat/com_sup; DISTINCT category > 1 e DISTINCT supplier > 1; pct ≥ 90.</done>
  <resume-signal>Digite "estável" quando contagens não caírem e diversidade > 1, ou reporte a queda</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validação visual dos 2 gráficos em /fluxo-de-caixa (prod)</name>
  <read_first>
    - 61-RESEARCH.md §5 (CostCompositionChart / SupplierExposureChart — fonte e hooks; nenhuma mudança de frontend)
    - ROADMAP Phase 61, Success Criteria 5
  </read_first>
  <action>
    Validação visual em prod pelo Wesley: abrir `/fluxo-de-caixa` logado na org Pé Vermeio e confirmar que a Composição de Custos por Mês mostra ≥3 categorias reais e a Exposição por Fornecedor mostra ≥2 fornecedores reais. Nenhuma mudança de código — só observação do estado da UI após o backfill.
  </action>
  <what-built>
    Os dois gráficos já liam as colunas corretas; agora os dados existem. Nenhuma mudança de frontend foi feita.
  </what-built>
  <how-to-verify>
    Em produção, logado na org Pé Vermeio, abrir `/fluxo-de-caixa`:
    1. "Composição de Custos por Mês": mostra ≥3 categorias reais do plano de contas do Tiny (além de "Outros"/"Previsões de compra").
    2. "Exposição por Fornecedor": mostra ≥2 fornecedores reais (não só Pralana).
    3. Confirmar que os números batem com o esperado do backfill (ordem de grandeza plausível, sem barra única "Outros").
  </how-to-verify>
  <verify>
    <human-check>Wesley confirma visualmente em prod: ≥3 categorias na Composição e ≥2 fornecedores na Exposição</human-check>
  </verify>
  <done>Em prod: Composição de Custos por Mês mostra ≥3 categorias reais; Exposição por Fornecedor mostra ≥2 fornecedores reais.</done>
  <resume-signal>Digite "gráficos ok" ou descreva o que aparece de errado</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: No-regressão da Phase 60 (get_cashflow toggle + reconciliação R$87.105,79)</name>
  <read_first>
    - 61-RESEARCH.md §9 Pitfall 5 (get_cashflow usa o valor de category, não NULL — sem regressão) e §5 (toggle não move a Exposição por Fornecedor)
    - .planning/phases/60-alinhamento-da-dfc-fluxo-de-caixa — RPC get_cashflow 4-arg, toggle de previsões, regra da Exposição
    - ROADMAP Phase 61, Success Criteria 6
  </read_first>
  <action>
    Ação executada pelo orquestrador via `mcp__supabase__execute_sql` + observação em prod: validar que get_cashflow (toggle OFF/ON) mantém a reconciliação R$87.105,79 (invariante Phase 60), que as ~20 OCs seguem corretamente excluídas no toggle OFF (lógica por valor de category, Pitfall 5) e que a Exposição por Fornecedor não some/zera ao alternar o toggle.
  </action>
  <what-built>
    O backfill preencheu as ~20 OCs e as 1991 linhas. Precisa provar que a DFC da Phase 60 não mudou de comportamento.
  </what-built>
  <how-to-verify>
    1. Via `mcp__supabase__execute_sql`: chamar `get_cashflow` com o toggle de previsões OFF e confirmar a invariante de reconciliação da Phase 60 (saída/total = R$87.105,79 conforme Phase 60). Repetir com toggle ON e confirmar o comportamento esperado (previsões entram só onde a Phase 60 manda).
    2. Confirmar que as ~20 OCs com category preenchido pelo enriquecimento continuam corretamente excluídas no toggle OFF (a lógica filtra pelo valor de category, ex.: 'Previsões de compra' — Pitfall 5).
    3. Em prod (/fluxo-de-caixa): a Exposição por Fornecedor NÃO some nem zera ao alternar o toggle (comportamento Phase 60 preservado).
  </how-to-verify>
  <verify>
    <automated>MANUAL — execute_sql: get_cashflow toggle OFF reconcilia R$87.105,79 (invariante Phase 60); toggle ON sem mudança de comportamento; Exposição estável ao toggle</automated>
  </verify>
  <done>get_cashflow toggle OFF reconcilia R$87.105,79; toggle ON sem mudança; OCs excluídas no OFF; Exposição por Fornecedor estável ao toggle (Phase 60 intacta).</done>
  <resume-signal>Digite "sem regressão" quando a reconciliação bater e o toggle se comportar como na Phase 60</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador → prod (MCP) | Sync de validação e queries de leitura em prod |
| Frontend (org-scoped) → RPCs | get_cost_by_month / get_supplier_exposure / get_cashflow lidas pela UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | Tampering | Regressão na DFC da Phase 60 | mitigate | Task 3 reconcilia R$87.105,79 (invariante) e valida toggle OFF/ON antes de fechar a phase |
| T-61-08 | Information Disclosure | RPCs de leitura org-scoped | accept | Não alteradas; SECURITY INVOKER org-first (Phase 51/60) |
| T-61-SC | Tampering | Supply chain | accept | Nenhum pacote novo instalado |
</threat_model>

<artifacts>
## Artefatos que este plano produz
- Confirmação dos critérios de sucesso 1, 4, 5 e 6 do ROADMAP (registro no SUMMARY).
- Nenhum arquivo de código novo (validação pura sobre prod).
</artifacts>

<verification>
- Sync pós-backfill não derruba contagens; DISTINCT category > 1 e DISTINCT supplier > 1; pct ≥ 90.
- Gráficos em prod: ≥3 categorias, ≥2 fornecedores.
- Phase 60 intacta: reconciliação R$87.105,79 e toggle OFF/ON sem regressão.
</verification>

<success_criteria>
- CASHFIX-07 e CASHFIX-08 confirmados end-to-end em prod.
- Phase 61 pronta para fechamento sem regressão da Phase 60.
</success_criteria>

<output>
Criar `.planning/phases/61-enriquecer-fornecedor-categoria-do-contas-a-pagar/61-03-SUMMARY.md` ao concluir (registrar números finais + confirmação dos 6 critérios do ROADMAP).
</output>
