---
phase: 61-enriquecer-fornecedor-categoria-do-contas-a-pagar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sync-tiny-payables/index.ts
  - supabase/migrations/20260661000000_enrich_supplier_category.sql
autonomous: true
requirements: [CASHFIX-07]
tags: [supabase, edge-function, migration, treasury, cash_outflows]

must_haves:
  truths:
    - "O objeto rows.push de sync-tiny-payables não contém mais as chaves de fornecedor nem de categoria (enriquecimento vira fonte única) — opção A (CONTEXT) / CASHFIX-07"
    - "A migration 20260661000000 define enrich_payable_step gravando supplier = contato.nome além de category = categoria.descricao"
    - "enrich_enqueue_new passa a re-enfileirar (todo) toda linha com category IS NULL OR supplier IS NULL e usa ON CONFLICT DO UPDATE em vez de DO NOTHING"
  artifacts:
    - path: "supabase/functions/sync-tiny-payables/index.ts"
      provides: "Upsert de cash_outflows que NÃO escreve category/supplier (preserva no ON CONFLICT)"
      contains: "fonte única"
    - path: "supabase/migrations/20260661000000_enrich_supplier_category.sql"
      provides: "CREATE OR REPLACE de enrich_payable_step + enrich_enqueue_new + enrich_harvest"
      contains: "enrich_payable_step"
  key_links:
    - from: "supabase/functions/sync-tiny-payables/index.ts"
      to: "cash_outflows (ON CONFLICT)"
      via: "objeto de upsert sem as chaves enriquecidas → UPDATE não toca category/supplier"
      pattern: "onConflict"
    - from: "supabase/migrations/20260661000000_enrich_supplier_category.sql"
      to: "cash_outflows.supplier"
      via: "UPDATE SET category=..., supplier=... no harvest de enrich_payable_step"
      pattern: "supplier"
---

<objective>
Implementar a correção de código da arquitetura "fonte única" para `cash_outflows.category` e `cash_outflows.supplier` (opção A travada no CONTEXT, CASHFIX-07): (1) remover as duas chaves enriquecidas do objeto de upsert da Edge Function `sync-tiny-payables` para que o `ON CONFLICT DO UPDATE` pare de zerá-las; (2) criar a migration que torna o enriquecimento-detalhe (Phase 51) a única fonte desses campos — `enrich_payable_step` passa a gravar `supplier`, e `enrich_enqueue_new` passa a re-enfileirar qualquer linha com `category IS NULL OR supplier IS NULL` (incluindo as já `done`).

Este plano é SÓ implementação (escrita de arquivos). O deploy da EF e o apply da migration acontecem no plano 61-02 (ações de plataforma via MCP, feitas pelo orquestrador — não pelo executor; padrão do projeto).

Purpose: Sem essas duas mudanças de código, cada `sync-tiny-payables` continua sobrescrevendo o enriquecimento para NULL e a fila nunca recupera as linhas `done`.
Output: `index.ts` modificado + migration `20260661000000_enrich_supplier_category.sql`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-enriquecer-fornecedor-categoria-do-contas-a-pagar/61-CONTEXT.md
@.planning/phases/61-enriquecer-fornecedor-categoria-do-contas-a-pagar/61-RESEARCH.md
@supabase/functions/sync-tiny-payables/index.ts
@supabase/migrations/20260650000300_cr01_backfill_pipeline_multitenant.sql
@supabase/migrations/20260650000100_treasury_category_backfill.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover as 2 chaves enriquecidas do upsert de sync-tiny-payables</name>
  <files>supabase/functions/sync-tiny-payables/index.ts</files>
  <read_first>
    - supabase/functions/sync-tiny-payables/index.ts — função processLoja(), objeto rows.push() (~linhas 273-287) e a chamada .upsert() (~linhas 299-304)
    - 61-RESEARCH.md §1 ("sync-tiny-payables — Upsert Exato e Mudança Mínima") e §10 (diff antes/depois) — caminhos e linhas exatas
  </read_first>
  <action>
    No objeto passado a `rows.push({...})` dentro de `processLoja()`, remover as DUAS propriedades que mapeiam os campos enriquecidos (a que deriva do contato e a que deriva do tipo da lista — linhas ~278-279 conforme RESEARCH §1/§10). NÃO atribuir o valor como `undefined`: a chave deve ficar AUSENTE do objeto literal (ver Pitfall 1 do RESEARCH — `undefined` pode ser interpretado como NULL por algumas versões do PostgREST). Deixar uma linha de comentário curta no lugar indicando que o enriquecimento-detalhe é a fonte única desses dois campos (opção A, CASHFIX-07).
    Manter INTOCADAS todas as demais chaves legítimas da lista: outflow_date, amount, description, status, document_number, source, tiny_payable_id, synced_at, updated_at. Manter a chamada `.upsert(rows, { onConflict: "organization_id,tiny_payable_id", ignoreDuplicates: false })` exatamente como está. NÃO mexer na interface TinyPayable. NÃO criar nenhum outro escritor desses campos.
  </action>
  <verify>
    <automated>cd supabase/functions/sync-tiny-payables && deno check index.ts && grep -c 'item.nomeFornecedor' index.ts | grep -qx 0 && grep -c 'item.tipoOrdem' index.ts | grep -qx 0 && grep -q 'fonte única' index.ts && grep -q 'onConflict' index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno check index.ts` passa sem erros de tipo.
    - As expressões-fonte exclusivas das linhas removidas (`item.nomeFornecedor` e `item.tipoOrdem`) não aparecem mais no arquivo (contagem 0).
    - O comentário "fonte única" está presente no lugar das chaves removidas.
    - A chamada `.upsert(... onConflict ...)` permanece presente e inalterada (ainda contém `onConflict`).
    - Nenhuma chave do objeto rows.push é atribuída como `undefined`.
  </acceptance_criteria>
  <done>EF sync-tiny-payables não inclui mais category/supplier no objeto de upsert; deno check passa; tokens-fonte removidos (contagem 0); .upsert/onConflict intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration 20260661000000 — enrich grava supplier + enqueue re-enfileira</name>
  <files>supabase/migrations/20260661000000_enrich_supplier_category.sql</files>
  <read_first>
    - 61-RESEARCH.md §2 (correção de enrich_enqueue_new e enrich_payable_step) e §10 (SQL completo verificado das 2 funções) — usar a estrutura dali
    - supabase/migrations/20260650000300_cr01_backfill_pipeline_multitenant.sql — definições atuais de enrich_enqueue_new (~linhas 19-47) e enrich_payable_step (~linhas 50-116)
    - supabase/migrations/20260650000100_treasury_category_backfill.sql — definição atual de enrich_harvest (~linha 79) para atualizar por consistência
    - 61-RESEARCH.md §4 (convenções de migration: naming, SECURITY DEFINER, REVOKE) e Pitfalls 2/3/4
  </read_first>
  <action>
    Criar `supabase/migrations/20260661000000_enrich_supplier_category.sql` com TRÊS `CREATE OR REPLACE` (sem recriar crons — eles já existem em prod como DRIFT e passam a usar o novo código automaticamente, Pitfall 3):

    1) `public.enrich_enqueue_new()` (SECURITY DEFINER, SET search_path TO 'public'): manter o SELECT/JOIN LATERAL atuais, mas (a) mudar o WHERE para incluir também `co.supplier IS NULL` na disjunção (`category IS NULL OR TRIM(category)='' OR supplier IS NULL`); (b) trocar a cláusula de conflito de "não fazer nada" por `ON CONFLICT (tiny_payable_id) DO UPDATE SET status='todo', updated_at=now() WHERE cat_backfill_queue.status IN ('done','error')` — re-marcar SÓ `done`/`error` (não resetar `todo`/`sent` para não perder requisições em voo; usar o nome completo da tabela na cláusula WHERE do ON CONFLICT, não alias — Pitfall 2). Retornar o jsonb de status (enqueued_now / queue_open / done_total) como no RESEARCH §10.

    2) `public.enrich_payable_step(p_batch integer DEFAULT 12)` (SECURITY DEFINER, SET search_path TO 'public'): declarar a variável adicional de fornecedor no bloco DECLARE (Pitfall 4 — sem isso é erro de sintaxe). Na seção de harvest do status 200, derivar o fornecedor de `v_content->'contato'->>'nome'` via `NULLIF(TRIM(COALESCE(...,'')),'')` e incluí-lo no `UPDATE public.cash_outflows SET category=..., supplier=...`. Manter intactas a derivação de categoria (COALESCE ... 'Outros'), o tratamento de 429 (volta a todo) e de erro (>4 tentativas → error), e o disparo de novas requisições via net.http_get. Retornar o mesmo jsonb de status atual.

    3) `public.enrich_harvest()` (mesma assinatura/segurança atual da migration 20260650000100): por consistência (critério de sucesso 2 cita explicitamente `enrich_harvest`), aplicar a MESMA adição de `supplier = contato.nome` no UPDATE de cash_outflows. Não alterar mais nada da função.

    Encerrar com `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated;` nas três funções (pipeline server-side chamado por pg_cron — RESEARCH §4). NÃO usar `supabase db push`/CLI; o apply é via MCP no plano 61-02.
  </action>
  <verify>
    <automated>F=supabase/migrations/20260661000000_enrich_supplier_category.sql; test -f $F && grep -q 'enrich_payable_step' $F && grep -q 'enrich_enqueue_new' $F && grep -q 'enrich_harvest' $F && grep -q 'DO UPDATE' $F && grep -qi 'supplier' $F && grep -c 'DO NOTHING' $F | grep -qx 0 && grep -q 'REVOKE' $F</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo de migration existe com o nome exato `20260661000000_enrich_supplier_category.sql`.
    - Contém os três `CREATE OR REPLACE`: enrich_enqueue_new, enrich_payable_step e enrich_harvest.
    - `DO NOTHING` não aparece mais (contagem 0) e existe ao menos um `DO UPDATE` (re-marcação da fila).
    - O termo `supplier` aparece no UPDATE de cash_outflows das funções de harvest.
    - A migration NÃO recria os crons treasury_cat_tick/treasury_cat_enqueue (Pitfall 3).
    - Há `REVOKE EXECUTE ... FROM PUBLIC` nas funções de pipeline.
  </acceptance_criteria>
  <done>Migration 20260661000000 criada com enrich_enqueue_new (DO UPDATE re-marca todo + supplier IS NULL no WHERE), enrich_payable_step e enrich_harvest gravando supplier; sem DO NOTHING; com REVOKE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tiny API → pg_net → cash_outflows | Resposta do detalhe `/contas-pagar/{id}` (categoria.descricao, contato.nome) é gravada em colunas de texto |
| pg_cron → funções SECURITY DEFINER | enrich_payable_step/enrich_enqueue_new/enrich_harvest rodam com privilégio elevado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | Conteúdo do detalhe Tiny gravado em category/supplier | mitigate | Valores extraídos via acesso JSON `->>` (sem concatenação SQL), TRIM/NULLIF; colunas são text nullable — sem risco de injeção |
| T-61-02 | Elevation of Privilege | Funções SECURITY DEFINER do pipeline | mitigate | `REVOKE EXECUTE FROM PUBLIC, anon, authenticated` nas três funções; só pg_cron as chama |
| T-61-03 | Information Disclosure | RPCs de leitura get_cost_by_month/get_supplier_exposure | accept | Não alteradas nesta phase; já são SECURITY INVOKER org-scoped (Phase 51/60) |
| T-61-SC | Tampering | Supply chain (npm/pip/cargo) | accept | Nenhum pacote novo instalado nesta phase — sem superfície de supply-chain |
</threat_model>

<artifacts>
## Artefatos que este plano produz
- `supabase/functions/sync-tiny-payables/index.ts` — modificado: upsert sem as chaves category/supplier (preserva no ON CONFLICT).
- `supabase/migrations/20260661000000_enrich_supplier_category.sql` — novo: CREATE OR REPLACE de enrich_enqueue_new (re-enfileira + DO UPDATE), enrich_payable_step (grava supplier) e enrich_harvest (grava supplier).

> NÃO deployado/aplicado neste plano. Deploy da EF + apply da migration acontecem no plano 61-02 via MCP (orquestrador).
</artifacts>

<verification>
- `deno check` passa na EF modificada.
- Tokens-fonte das linhas removidas (`item.nomeFornecedor`, `item.tipoOrdem`) não aparecem mais.
- Migration contém as três funções, troca DO NOTHING→DO UPDATE, e grava supplier.
</verification>

<success_criteria>
- Código da fonte única escrito e type-checado, pronto para deploy/apply no plano 61-02 (CASHFIX-07).
- Nenhuma outra parte do upsert de sync-tiny-payables alterada.
</success_criteria>

<output>
Criar `.planning/phases/61-enriquecer-fornecedor-categoria-do-contas-a-pagar/61-01-SUMMARY.md` ao concluir.
</output>