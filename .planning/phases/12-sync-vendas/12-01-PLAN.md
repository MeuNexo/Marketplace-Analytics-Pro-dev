---
phase: 12-sync-vendas
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260521170000_cache_tables_add_units_sold_seller_id.sql
autonomous: true

must_haves:
  truths:
    - As tabelas ml_daily_cache e ml_hourly_cache têm colunas units_sold e seller_id
    - As tabelas ml_product_daily_cache e ml_user_cache têm coluna seller_id
    - O schema do banco é compatível com o que a edge function mercado-libre-integration escreve
  artifacts:
    - supabase/migrations/20260521170000_cache_tables_add_units_sold_seller_id.sql (já aplicado e commitado)
  key_links:
    - Edge function tenta upsert com units_sold/seller_id → sem as colunas → erro 400 → sync falha silenciosamente
---

<objective>
Corrigir incompatibilidade de schema entre a edge function mercado-libre-integration
e as tabelas de cache no Supabase Dev.

A edge function escreve campos `units_sold` (integer) e `seller_id` (uuid) em
ml_daily_cache e ml_hourly_cache, e `seller_id` em ml_product_daily_cache e
ml_user_cache. Essas colunas não existiam no schema do dev, causando falha silenciosa
no upsert e cache sempre vazio.

Purpose: Garantir que o sync de vendas possa popular as tabelas de cache corretamente.
Output: Migration aplicada + colunas presentes em todas as 4 tabelas.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar migration aplicada e colunas presentes</name>
  <files>supabase/migrations/20260521170000_cache_tables_add_units_sold_seller_id.sql</files>
  <action>
    A migration já foi aplicada ao Supabase Dev (ckcdevcxgvueywivefgx) e o arquivo
    já está commitado. Executar SQL de verificação para confirmar que todas as colunas
    existem:

    ```sql
    SELECT table_name, column_name
    FROM information_schema.columns
    WHERE table_schema = 'public'
    AND table_name IN ('ml_daily_cache','ml_hourly_cache','ml_product_daily_cache','ml_user_cache')
    AND column_name IN ('units_sold','seller_id')
    ORDER BY table_name, column_name;
    ```

    Deve retornar 6 linhas:
    - ml_daily_cache: units_sold, seller_id
    - ml_hourly_cache: units_sold, seller_id
    - ml_product_daily_cache: seller_id
    - ml_user_cache: seller_id
  </action>
  <verify>
    SQL retorna exatamente 6 linhas com as combinações table_name/column_name esperadas.
  </verify>
  <done>
    Todas as 4 tabelas de cache têm o schema compatível com a edge function.
  </done>
</task>

</tasks>

<verification>
SQL de verificação retorna 6 linhas — todas as colunas presentes em todas as tabelas.
</verification>

<success_criteria>
- ml_daily_cache tem units_sold e seller_id
- ml_hourly_cache tem units_sold e seller_id
- ml_product_daily_cache tem seller_id
- ml_user_cache tem seller_id
</success_criteria>

<output>
Após completion, criar `.planning/phases/12-sync-vendas/12-01-SUMMARY.md`.
</output>
