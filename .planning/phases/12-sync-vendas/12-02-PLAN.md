---
phase: 12-sync-vendas
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - O dashboard exibe faturamento, pedidos e compradores com valores reais da Pé Vermeio
    - ml_daily_cache tem linhas para ml_user_id=1639558873 após o sync
    - ml_user_cache tem o nickname da conta Pé Vermeio
    - O botão de sync manual no dashboard completa sem erro
  artifacts:
    - Linhas em ml_daily_cache com ml_user_id=1639558873
    - Linha em ml_user_cache com ml_user_id=1639558873 e nickname preenchido
  key_links:
    - useMLSync.syncFromAPI() → mercado-libre-integration → ml_daily_cache → useMLDailyQuery → dashboard KPIs
    - HeaderScopeContext lê ml_tokens por seller_id → resolve ml_user_ids → sync usa esses IDs
---

<objective>
Disparar o primeiro sync de vendas da Pé Vermeio e verificar que o dashboard
exibe dados reais — sem zeros, sem dados mockados.

O sync acontece automaticamente quando o usuário abre o dashboard (auto-sync on mount
se dados estiverem stale). O cache está vazio, então o sync vai disparar imediatamente
ao visitar `/`.

Purpose: Validar SYNC-01, SYNC-02, SYNC-03, SYNC-04 do milestone v4.0.
Output: ml_daily_cache populado + dashboard com dados reais da Pé Vermeio.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sync-vendas/12-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Abrir dashboard e aguardar sync automático</name>
  <action>
    VERIFICAÇÃO MANUAL — o sync requer sessão autenticada no browser.

    Passo 1 — Acesse https://marketplace-analytics-pro-dev.vercel.app/
    - Faça login como wesleysantos@pevermeio.com (senha: Wesley6873*) se necessário
    - Certifique-se que o seller "Pé Vermeio" está selecionado no header

    Passo 2 — Observe o sync automático:
    - Ao abrir o dashboard, um indicador de sincronização deve aparecer (ícone de loading)
    - Aguarde até completar (pode levar 30-60 segundos para o primeiro sync)
    - Se não disparar automaticamente, clique no botão de sync manual (ícone de reload)

    Passo 3 — Verifique os dados:
    - Os KPIs de faturamento devem mostrar valores reais (não R$ 0,00)
    - O período padrão é os últimos 30 dias
    - Verifique que "Pedidos Aprovados" e "Compradores Únicos" têm valores

    Informe: sync completou com sucesso? Quais valores aparecem (aprox)?
    Se der erro, copie a mensagem de erro visível na tela.
  </action>
  <verify>
    Após confirmação, executar SQL:
    ```sql
    SELECT date, total_revenue, approved_revenue, qty_orders, unique_buyers
    FROM ml_daily_cache
    WHERE ml_user_id = '1639558873'
    ORDER BY date DESC
    LIMIT 5;
    ```
    Deve retornar linhas com valores > 0.
  </verify>
  <done>
    - Usuário confirma que dashboard mostra dados reais (não zeros)
    - SQL confirma linhas em ml_daily_cache para ml_user_id=1639558873
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar ml_user_cache com dados da Pé Vermeio</name>
  <action>
    Executar SQL para confirmar que ml_user_cache foi populado com o nickname
    da conta Pé Vermeio:

    ```sql
    SELECT ml_user_id, nickname, active_listings, synced_at
    FROM ml_user_cache
    WHERE ml_user_id = '1639558873';
    ```

    Deve retornar 1 linha com nickname preenchido (nome da loja no ML).

    Também verificar ml_sync_log:
    ```sql
    SELECT ml_user_id, date_from, date_to, days_synced, synced_at
    FROM ml_sync_log
    WHERE ml_user_id = '1639558873'
    ORDER BY synced_at DESC
    LIMIT 1;
    ```
  </action>
  <verify>
    ml_user_cache tem linha para 1639558873 com nickname não nulo.
  </verify>
  <done>
    SYNC-01 a SYNC-04 validados:
    - SYNC-01: edge function populou ml_daily_cache com dados reais
    - SYNC-02: dashboard exibe faturamento real
    - SYNC-03: sync manual completou sem erro
    - SYNC-04: ml_user_cache tem nickname da Pé Vermeio
  </done>
</task>

</tasks>

<verification>
1. ml_daily_cache tem linhas para ml_user_id=1639558873 com valores > 0
2. ml_user_cache tem nickname para ml_user_id=1639558873
3. Dashboard mostra dados reais confirmados pelo usuário
</verification>

<success_criteria>
- SYNC-01: ml_daily_cache populado com dados reais via mercado-libre-integration
- SYNC-02: Dashboard KPIs mostram faturamento/pedidos/compradores reais
- SYNC-03: Sync manual completou em < 60s sem erro
- SYNC-04: ml_user_cache tem nickname da conta Pé Vermeio
</success_criteria>

<output>
Após completion, criar `.planning/phases/12-sync-vendas/12-02-SUMMARY.md` com:
- Valores confirmados no dashboard (faturamento aproximado do período)
- Nickname da Pé Vermeio em ml_user_cache
- Status de cada SYNC-0X
</output>
