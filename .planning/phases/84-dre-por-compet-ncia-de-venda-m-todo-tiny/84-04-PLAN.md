---
phase: 84-dre-por-compet-ncia-de-venda-m-todo-tiny
plan: 04
type: execute
wave: 2
depends_on: [84-01]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Confirmado via MCP que existe exatamente UMA UNIQUE constraint em ml_billing_daily antes de aplicar"
    - "A migration 20260685000000 foi aplicada em ckcdevcxgvueywivefgx via MCP apply_migration"
    - "Após aplicar, TODAS as linhas existentes têm competence_date NOT NULL (= charge_date); 0 nulls"
    - "A nova UNIQUE (6 colunas) existe e a antiga (5 colunas) não existe mais"
  artifacts:
    - path: "N/A (ação MCP — sem arquivo de repo)"
      provides: "Schema competence_date aplicado em produção + verificação"
  key_links:
    - from: "supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql"
      to: "ckcdevcxgvueywivefgx (prod)"
      via: "MCP apply_migration"
      pattern: "competence_date"
---

<objective>
GATE DE APLICAÇÃO DO SCHEMA (executado pelo ORQUESTRADOR via MCP Supabase — o gsd-executor não tem token). Verifica a assunção da constraint, aplica a migration 84-01 em produção `ckcdevcxgvueywivefgx`, confirma o backfill das linhas existentes e roda o diagnóstico barato de `sales_info`. É o segundo elo da ordem estrita: NADA de EF/backfill real (84-05) roda antes deste gate passar. Se a verificação da constraint revelar algo diferente do esperado (ex.: 2 UNIQUE constraints), PARAR e revisar a migration em 84-01 antes de aplicar.

Purpose: Aplicar a coluna + constraint alargada com segurança, provando gap-zero (nenhuma linha NULL) antes de o frontend trocar o filtro.
Output: migration aplicada em prod + evidências (SQL) coladas no SUMMARY. Sem arquivos de repo.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-RESEARCH.md
@supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Gate MCP — verificar constraint, aplicar migration, provar backfill</name>
  <action>Ação do ORQUESTRADOR via MCP Supabase (executor não tem token). Executar os passos do bloco what-built em ordem no projeto ckcdevcxgvueywivefgx: diagnóstico sales_info (não-bloqueante) → verificar 1 UNIQUE constraint → apply_migration da 20260685000000 → provar backfill (0 nulls, competence=charge 100%) + UNIQUE 6-col + índice. Se a verificação de constraint divergir, PARAR e revisar 84-01.</action>
  <what-built>
    Gate de schema via MCP no projeto Supabase `ckcdevcxgvueywivefgx` (NÃO gionpsuunfkkzzjdubfy). Passos, em ordem, pelo ORQUESTRADOR:
    1. **Diagnóstico barato (não-bloqueante):** medir a frequência de `sales_info` multi-entrada na origem antes de finalizar. Como o dado bruto não persiste, usar o proxy pós-nada: registrar a intenção e, se viável, disparar 1 run debug da EF ATUAL (pré-84-05) com log temporário de `sales_info.length` OU simplesmente documentar que será checado no smoke (84-05) via fallback-por-tipo. Resultado só gera follow-up se `length > 1` for frequente (Pitfall 4 / Assumption A3). NÃO bloqueia.
    2. **Verificar a constraint (BLOQUEANTE):** `SELECT conname, contype, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'public.ml_billing_daily'::regclass;`. Confirmar que existe EXATAMENTE UMA constraint UNIQUE (contype='u') e que é a de 5 colunas `(organization_id, ml_user_id, charge_date, charge_type, source_invoice_key)`. Se houver 2+ UNIQUE ou shape diferente → PARAR e revisar 84-01.
    3. **Aplicar a migration:** `apply_migration` com o conteúdo de `20260685000000_ml_billing_daily_competence_date.sql`.
    4. **Verificar backfill + constraint:** rodar (a) `SELECT count(*) AS total, count(*) FILTER (WHERE competence_date IS NULL) AS nulos FROM ml_billing_daily;` → `nulos` DEVE ser 0; (b) `SELECT count(*) FROM ml_billing_daily WHERE competence_date <> charge_date;` → nesta etapa DEVE ser 0 (backfill = charge_date; só muda após o re-sync do 84-05); (c) re-rodar o SELECT de pg_constraint e confirmar que a UNIQUE agora tem 6 colunas incl. competence_date e a antiga sumiu; (d) confirmar `idx_ml_billing_daily_competence` existe (`SELECT indexname FROM pg_indexes WHERE tablename='ml_billing_daily';`).
  </what-built>
  <how-to-verify>
    Orquestrador cola no SUMMARY as saídas SQL provando: (1) 1 UNIQUE antes; (2) migration aplicada sem erro; (3) nulos=0; (4) competence_date=charge_date em 100% das linhas nesta etapa; (5) nova UNIQUE de 6 colunas presente + antiga ausente; (6) índice de competência presente. Advisors (`get_advisors` security+performance) sem issue novo introduzido pela migration.
  </how-to-verify>
  <acceptance_criteria>
    - Exatamente 1 UNIQUE constraint confirmada ANTES da aplicação (senão: parar + revisar 84-01).
    - Migration 20260685000000 aplicada em ckcdevcxgvueywivefgx sem erro.
    - `count(*) FILTER (WHERE competence_date IS NULL)` == 0.
    - `count(*) WHERE competence_date <> charge_date` == 0 nesta etapa.
    - `pg_get_constraintdef` da UNIQUE inclui `competence_date` e tem 6 colunas; constraint antiga de 5 colunas não existe mais.
    - `idx_ml_billing_daily_competence` listado em pg_indexes.
    - get_advisors sem novo ERROR/WARN atribuível a esta migration.
  </acceptance_criteria>
  <verify>Evidências SQL coladas no SUMMARY provam: 1 UNIQUE antes; migration aplicada; nulos=0; competence=charge 100%; UNIQUE 6-col + antiga ausente; índice presente; advisors limpos.</verify>
  <done>Migration 20260685000000 aplicada em ckcdevcxgvueywivefgx com backfill provado (0 nulls) e UNIQUE alargada; gate liberado para 84-05.</done>
  <resume-signal>Digite "aplicado" após colar as evidências SQL, ou descreva o desvio (ex.: 2 UNIQUE constraints → revisar 84-01).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orquestrador (MCP, token privilegiado) → prod ckcdevcxgvueywivefgx | Aplicação de DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-08 | Tampering | DROP/ADD da UNIQUE constraint | mitigate | Verificação prévia de exatamente 1 UNIQUE; DO-block por nome dinâmico; org+ml_user_id permanecem 1ª/2ª colunas (isolamento cross-org preservado) |
| T-84-09 | Denial of Service | migration com backfill em tabela grande | accept | Backfill é UPDATE simples (competence_date := charge_date) em tabela de billing agregado (baixa cardinalidade); janela de aplicação curta |
</threat_model>

<verification>
Evidências SQL no SUMMARY: 1 UNIQUE antes, nulos=0, competence=charge em 100%, UNIQUE 6-col, índice presente, advisors limpos.
</verification>

<success_criteria>
Schema competence_date em prod com backfill provado (0 nulls) e UNIQUE alargada; gate liberado para 84-05.
</success_criteria>

<artifacts>
## Artifacts que este plano produz
- **Estado em prod:** coluna `competence_date` + constraint `ml_billing_daily_uniq` (6 col) + índice `idx_ml_billing_daily_competence` aplicados em `ckcdevcxgvueywivefgx`
- **Evidência:** bloco SQL de verificação no SUMMARY (não-código)
</artifacts>

<output>
Create `.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-04-SUMMARY.md` when done
</output>
