---
phase: 84-dre-por-compet-ncia-de-venda-m-todo-tiny
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sync-ml-billing/index.ts
  - supabase/functions/sync-ml-billing/aggregate.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "aggregateInvoice agrega por (competence_date, charge_date, charge_type); competence_date = saleDate ?? charge_date"
    - "A exclusão `within` é REMOVIDA na trilha de competência: estorno (B*) sempre conta como -amount, mesmo de venda fora da janela da fatura"
    - "O payload gravado em ml_billing_daily inclui competence_date; delete-by-source_invoice_key + insert continua idempotente"
    - "A trilha ml_billing_monthly (fetchBillingPeriod) permanece 100% intacta"
  artifacts:
    - path: "supabase/functions/sync-ml-billing/index.ts"
      provides: "aggregateInvoice com competência + remoção do within + payload competence_date"
      contains: "competence_date"
    - path: "supabase/functions/sync-ml-billing/aggregate.test.ts"
      provides: "Testes unitários do core puro de agregação (competência + sinal + remoção do within)"
      contains: "competence"
  key_links:
    - from: "supabase/functions/sync-ml-billing/index.ts (aggregateInvoice)"
      to: "ml_billing_daily.competence_date"
      via: "runDailySync payload inclui competence_date"
      pattern: "competence_date"
---

<objective>
Autoria (sem deploy) da mudança de regime na EF `sync-ml-billing`: `aggregateInvoice()` passa a alocar cada movimento pela COMPETÊNCIA DA VENDA (`competence_date = saleDate ?? charge_date`), muda o grão do agregado para `(competence_date, charge_date, charge_type)` e REMOVE a exclusão `within` na trilha de competência (estornos de venda antiga passam a contar, sempre com sinal negativo). `runDailySync()` grava `competence_date` no payload. A trilha `ml_billing_monthly` (`fetchBillingPeriod`) fica intacta — é a visão "igual à fatura ML". Extrai um core puro de agregação para permitir teste unitário automatizado (a EF hoje não tem testes).

Purpose: Segundo elo da ordem estrita (migration → EF → frontend → backfill). Deploy real da EF é o plano 84-05 (via MCP, verify_jwt=false).
Output: edição de `index.ts` + novo `aggregate.test.ts`. NENHUM deploy.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-RESEARCH.md
@supabase/functions/sync-ml-billing/index.ts
</context>

<tasks>

<!-- planner-discipline-allow: signed = null -->

<task type="auto" tdd="true">
  <name>Task 1: Extrair core puro de agregação por competência + testes (RED→GREEN)</name>
  <files>supabase/functions/sync-ml-billing/index.ts, supabase/functions/sync-ml-billing/aggregate.test.ts</files>
  <read_first>
    - supabase/functions/sync-ml-billing/index.ts linhas 90-183 (RawMove, fetchGroupMoves, aggregateInvoice, regra within/sinal)
    - vitest.config.ts (include já cobre `supabase/functions/**/*.test.ts`)
    - 84-RESEARCH.md Pitfall 4 (sales_info[0] — só o primeiro item é lido; comportamento pré-existente mantido)
  </read_first>
  <behavior>
    - Dado um movimento não-bonus com saleDate="2026-06-10" e charge_date="2026-07-03" → competence_date="2026-06-10", amount positivo, agrupado sob competência junho.
    - Dado um movimento bonus (B*) com saleDate="2026-05-20" (venda antiga, fora da janela da fatura de julho) → competence_date="2026-05-20", amount NEGATIVO, e NÃO é descartado (within removido).
    - Dado um movimento sem saleDate (null) — ex.: PADS/mensalidade → competence_date = charge_date (fallback).
    - Dois movimentos mesmo charge_date + charge_type mas competence_date diferente → DUAS linhas distintas no agregado (grão novo).
    - Movimento sem date ou sem type → ignorado (igual hoje).
    - Arredondamento: amount final = Math.round(x*100)/100.
  </behavior>
  <action>
    Extrair de `aggregateInvoice` a lógica PURA (sem fetch/rede) para uma função exportada, ex. `export function aggregateMoves(moves: RawMove[]): Array<{ competence_date: string; charge_date: string; charge_type: string; charge_label: string; amount: number }>`. Regras:
    - `const competence = m.saleDate ?? m.date;` (fallback charge_date quando não há venda associada).
    - Sinal: não-bonus → `+m.amount`; bonus → `-m.amount` SEMPRE (remover o `else if (within(...)) ... else signed = null` — sem mais janela na trilha de competência).
    - Chave do agregado: `` `${competence}|${m.date}|${m.type}` `` (era `${m.date}|${m.type}`).
    - Cada linha do agregado carrega `competence_date: competence` além de `charge_date: m.date`.
    - Manter o guard `if (!m.date || !m.type) continue;` e o arredondamento final.
    `aggregateInvoice` passa a: buscar os moves (ML+MP via fetchGroupMoves) e retornar `aggregateMoves([...ML, ...MP])`. O tipo de retorno de `aggregateInvoice` ganha `competence_date`. Exportar `RawMove` e `aggregateMoves` para o teste. NÃO alterar `fetchGroupMoves`, `resolveInvoice`, `fetchBillingPeriod`, `runAllAccountsDailySync` nem o modo monthly.
    Criar `supabase/functions/sync-ml-billing/aggregate.test.ts` cobrindo os 6 casos do bloco <behavior>. Escrever o teste ANTES da implementação passar (commit test RED, depois GREEN).
  </action>
  <acceptance_criteria>
    - `supabase/functions/sync-ml-billing/aggregate.test.ts` existe e importa `aggregateMoves` de `./index.ts`.
    - `npx vitest run supabase/functions/sync-ml-billing/aggregate.test.ts` — todos os testes verdes.
    - Um teste prova que um bonus com saleDate fora da janela produz linha com amount negativo (within removido).
    - Um teste prova competência = charge_date quando saleDate é null.
    - Um teste prova 2 linhas distintas para mesmo (charge_date, charge_type) e competence_date diferentes.
    - `grep -c "signed = null" supabase/functions/sync-ml-billing/index.ts` == 0 (branch de descarte por janela removido da trilha de competência).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run supabase/functions/sync-ml-billing/aggregate.test.ts</automated>
  </verify>
  <done>Core puro de agregação por competência testado; within removido; fallback provado.</done>
</task>

<task type="auto">
  <name>Task 2: runDailySync grava competence_date no payload de ml_billing_daily</name>
  <files>supabase/functions/sync-ml-billing/index.ts</files>
  <read_first>
    - supabase/functions/sync-ml-billing/index.ts linhas 226-259 (runDailySync: delete-by-source_invoice_key + insert em lotes de 500)
  </read_first>
  <action>
    Em `runDailySync`, incluir `competence_date: r.competence_date` no objeto de `payload.map(...)` (ao lado de charge_date/charge_type/charge_label/amount/source_invoice_key/organization_id/ml_user_id). Manter o delete-by-`source_invoice_key` + insert em lotes de 500 e o `if (error) throw`. Nenhuma outra mudança em runDailySync. A idempotência permanece: a competência de um mês pode receber linhas de múltiplas faturas — o delete é por fatura, não por competência.
  </action>
  <acceptance_criteria>
    - `grep -c "competence_date: r.competence_date" supabase/functions/sync-ml-billing/index.ts` == 1
    - O payload de insert de ml_billing_daily inclui competence_date.
    - `fetchBillingPeriod` e o upsert em `ml_billing_monthly` permanecem sem diff (`grep -c "ml_billing_monthly" index.ts` inalterado; a trilha de fatura não muda).
    - `npx tsc --noEmit` não introduz erros novos no repo (nota: index.ts é Deno; a checagem principal do repo é o vitest do core puro).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "competence_date: r.competence_date" supabase/functions/sync-ml-billing/index.ts && npx vitest run supabase/functions/sync-ml-billing/aggregate.test.ts</automated>
  </verify>
  <done>Payload de ml_billing_daily grava competence_date; trilha de fatura intacta; idempotência preservada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ML Billing API → EF | Dados externos (movimentos de billing); competence_date derivada server-side de sales_info, nunca do cliente |
| EF (service_role) → ml_billing_daily | Escrita bypassa RLS por design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03 | Tampering | competence_date derivada de sales_info | mitigate | Derivada exclusivamente do payload da ML Billing API (sale_date_time), nunca de input do cliente; fallback explícito para charge_date |
| T-84-04 | Spoofing/Elevation | modo daily fan-out (sem ml_user_id) | accept | Já exige service-role (código existente inalterado); usuário comum recebe 400 |
| T-84-05 | Denial of Service | re-sync/backfill dispara chamadas ML | mitigate | Backoff 429/5xx e cursor from_id já existentes (EF v10); backfill sequencial é enforçado no plano 84-05, não aqui |
</threat_model>

<verification>
- Testes do core puro verdes (competência + within removido + fallback).
- `grep "signed = null"` == 0; payload inclui competence_date.
- Trilha ml_billing_monthly sem diff.
</verification>

<success_criteria>
EF autorada (não deployada): aggregateInvoice por competência, within removido, competence_date no payload, testes unitários verdes; trilha de fatura intacta.
</success_criteria>

<artifacts>
## Artifacts que este plano produz
- **Função nova exportada:** `aggregateMoves(moves: RawMove[])` (core puro, testável) em `sync-ml-billing/index.ts`
- **Campo novo no agregado/payload:** `competence_date` em `aggregateInvoice`/`runDailySync`
- **Arquivo novo:** `supabase/functions/sync-ml-billing/aggregate.test.ts`
- **Comportamento removido:** exclusão `within` na trilha de competência (estornos antigos agora contam)
</artifacts>

<output>
Create `.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-02-SUMMARY.md` when done
</output>
