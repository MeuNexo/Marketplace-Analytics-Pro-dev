---
phase: 84-dre-por-compet-ncia-de-venda-m-todo-tiny
plan: 05
type: execute
wave: 3
depends_on: [84-02, 84-04]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "A EF sync-ml-billing (com aggregateMoves por competência) está deployada em prod com verify_jwt=false"
    - "As faturas de 2026 (jan → mês corrente) foram re-sincronizadas SEQUENCIALMENTE, sem gaps, uma por vez"
    - "Invariante provado: nenhum movimento perdido/duplicado (dedup por detail_id); soma por competência = soma por lançamento + estornos antes descartados"
    - "Reconciliação junho/Pé Vermeio ao centavo contra o esperado"
    - "Anti-IDOR: consulta como org alheia retorna 0 linhas"
    - "Prova de cross-month: um cancelamento de venda de mês anterior agora reduz o lucro do mês da venda"
  artifacts:
    - path: "N/A (ações MCP — sem arquivo de repo)"
      provides: "EF deployada + backfill 2026 + smoke completo"
  key_links:
    - from: "sync-ml-billing EF (competência)"
      to: "ml_billing_daily.competence_date (prod)"
      via: "backfill via net.http_post Pattern B, sequencial por mês"
      pattern: "competence_date"
---

<objective>
DEPLOY DA EF + BACKFILL 2026 + SMOKE (executado pelo ORQUESTRADOR via MCP — o gsd-executor não deploya EF nem tem token). Terceiro/quarto elo da ordem estrita: só roda depois do gate de schema (84-04) e com a EF já autorada (84-02). Deploya `sync-ml-billing`, dispara o re-sync das faturas de 2026 SEQUENCIALMENTE (uma por vez, evitando o bug de rate-limit corrigido em 2026-07-03), e prova o invariante de auditoria, a reconciliação ao centavo, anti-IDOR e o efeito cross-month.

Purpose: Popular `competence_date` REAL (venda) e provar que a mudança de regime não perde/duplica movimento nem vaza cross-org.
Output: EF deployada + faturas 2026 re-sincronizadas + evidências SQL no SUMMARY. Sem arquivos de repo.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-RESEARCH.md
@supabase/functions/sync-ml-billing/index.ts
@supabase/migrations/20260684000000_sync_ml_billing_cron.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy EF + backfill 2026 sequencial + smoke (MCP)</name>
  <action>Ação do ORQUESTRADOR via MCP Supabase (executor não deploya EF nem tem token). Executar os passos do bloco what-built em ordem em ckcdevcxgvueywivefgx: deploy da EF sync-ml-billing (verify_jwt=false) → backfill de 2026 SEQUENCIAL (um period_month por vez, verificar antes do próximo, nunca em paralelo) → smoke de invariante (dedup detail_id; competência = lançamento + estornos) → reconciliação junho/Pé Vermeio ao centavo → anti-IDOR (0 linhas cross-org) → prova cross-month → diagnóstico fallback-por-tipo.</action>
  <what-built>
    Pelo ORQUESTRADOR, em ordem, no projeto `ckcdevcxgvueywivefgx`:
    1. **Deploy da EF:** `deploy_edge_function` de `sync-ml-billing` (código do 84-02), MANTENDO `verify_jwt=false` (auth própria). Smoke rápido: OPTIONS → 200; POST sem auth → 401.
    2. **Backfill 2026 SEQUENCIAL (crítico):** para cada `period_month` de `2026-01` até o mês corrente, um de cada vez, disparar via `net.http_post` (Pattern B — lê `service_role_key` do vault, body `{"mode":"daily","period_month":"YYYY-MM"}`, fan-out multi-conta pois SEM ml_user_id), reusando a receita de `20260684000000_sync_ml_billing_cron.sql`. Entre um mês e o próximo, VERIFICAR que o sync do mês anterior terminou (row count / synced_at avançando em ml_billing_daily) ANTES de disparar o próximo. NUNCA disparar todos os meses em paralelo/rajada (reabriria o bug de rate-limit). Cobrir TODO o intervalo sem gaps (um estorno atrasado pode cair em qualquer fatura futura — Pitfall 3).
    3. **Smoke — invariante (BLOQUEANTE):** provar que nenhum movimento é perdido/duplicado. Query de reconciliação: para um `source_invoice_key` de referência, a soma de `amount` em ml_billing_daily (competência, todas as competências daquela fatura) = soma dos mesmos movimentos por lançamento + os estornos antes descartados. Confirmar dedup por detail_id (sem duplicatas).
    4. **Reconciliação ao centavo:** junho/Pé Vermeio (org `7f615df7-7bac-45e5-8a93-827fb9ddeec7`, ml_user_id `1639558873`): `SELECT count(DISTINCT competence_date) meses, sum(amount) total, sum(amount) FILTER (WHERE charge_type='PADS') pads FROM ml_billing_daily WHERE organization_id='7f615df7-...' AND ml_user_id='1639558873' AND competence_date BETWEEN '2026-06-01' AND '2026-06-30';` — comparar com o esperado documentado (ex.: referência de tarifas de junho). Diferença DEVE ser R$0,00 na conta esperada (ou explicada pelos estornos que agora entram).
    5. **Anti-IDOR:** impersonar (SET LOCAL ROLE authenticated + request.jwt.claims de uma org alheia, ex.: Thales `e4150d57-...`) numa transação ROLLBACK e confirmar 0 linhas de ml_billing_daily da Pé Vermeio.
    6. **Prova cross-month:** localizar um par venda-mês-anterior/estorno-mês-seguinte (B*) e mostrar que o estorno agora tem `competence_date` no mês da VENDA (reduz o lucro daquele mês), não no mês do lançamento.
    7. **Diagnóstico fallback-por-tipo (não-bloqueante, Assumption A2/A3):** `SELECT charge_type, count(*) total, count(*) FILTER (WHERE competence_date = charge_date) fallback FROM ml_billing_daily GROUP BY charge_type;` — confirmar que tipos de venda (CVVML, CFFE, CFONPN...) majoritariamente usam competência real e PADS/mensalidade caem no fallback. Se um tipo de venda usar fallback com frequência alta inesperada → flag para follow-up (não bloqueia esta fase).
  </what-built>
  <how-to-verify>
    Orquestrador cola no SUMMARY: status do deploy (versão ACTIVE, verify_jwt=false, smoke 401/200), lista dos meses re-sincronizados em ordem com row counts, o invariante (soma competência = lançamento + estornos), a reconciliação junho ao centavo, o resultado anti-IDOR (0 linhas cross-org), a prova cross-month (linha do estorno com competence_date no mês da venda) e a tabela fallback-por-tipo.
  </how-to-verify>
  <acceptance_criteria>
    - EF `sync-ml-billing` ACTIVE em nova versão, verify_jwt=false; POST sem auth → 401; OPTIONS → 200.
    - Todos os `period_month` de 2026-01 → mês corrente re-sincronizados, em ordem, sem gaps; cada um verificado antes do próximo (sem disparo em paralelo).
    - Invariante: nenhuma perda/duplicação de movimento (dedup por detail_id); soma por competência reconcilia com lançamento + estornos antes descartados.
    - Reconciliação junho/Pé Vermeio: diferença R$0,00 (ou explicada por estornos que agora entram).
    - Anti-IDOR: 0 linhas retornadas para org alheia; own > 0.
    - Prova cross-month: ao menos 1 estorno com competence_date no mês da venda (≠ mês do charge_date) demonstrado.
    - get_advisors sem novo issue.
  </acceptance_criteria>
  <verify>Evidências no SUMMARY: deploy ACTIVE (401/200), meses re-sincronizados em ordem com row counts, invariante, reconciliação junho ao centavo, anti-IDOR 0 linhas cross-org, prova cross-month, fallback-por-tipo; advisors limpos.</verify>
  <done>EF por competência em prod + faturas 2026 re-sincronizadas sequencialmente + invariante/reconciliação/anti-IDOR/cross-month provados.</done>
  <resume-signal>Digite "smoke ok" após colar todas as evidências, ou descreva a divergência (ex.: reconciliação não bate, gap de mês, rate-limit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ML Billing API → EF | Dados externos re-ingeridos no backfill |
| Orquestrador (service_role via vault) → EF/prod | Disparo de backfill + queries privilegiadas |
| org alheia (JWT simulado) → ml_billing_daily | Deve retornar 0 linhas (RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-10 | Information Disclosure | consulta cross-org em ml_billing_daily | mitigate | Prova anti-IDOR via impersonação SET LOCAL ROLE + request.jwt.claims (transação ROLLBACK); RLS `is_org_member` inalterada |
| T-84-11 | Denial of Service | backfill re-sync de 2026 | mitigate | Disparo SEQUENCIAL (1 mês por vez, verificar antes do próximo); cursor from_id + backoff 429/5xx da EF v10; nunca rajada paralela |
| T-84-12 | Tampering/Repudiation | perda/duplicação na mudança de grão | mitigate | Invariante de auditoria obrigatório (dedup por detail_id; soma competência = lançamento + estornos); delete-by-source_invoice_key idempotente |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo nesta fase (RESEARCH: Package Legitimacy N/A) — sem superfície de supply-chain |
</threat_model>

<verification>
Evidências no SUMMARY: deploy (401/200), meses re-sincronizados em ordem, invariante, reconciliação junho ao centavo, anti-IDOR 0 linhas, prova cross-month, fallback-por-tipo.
</verification>

<success_criteria>
EF por competência em prod + faturas 2026 re-sincronizadas sequencialmente + invariante/reconciliação/anti-IDOR/cross-month provados. Pronto para validação visual (84-06).
</success_criteria>

<artifacts>
## Artifacts que este plano produz
- **Estado em prod:** EF `sync-ml-billing` nova versão ACTIVE (verify_jwt=false)
- **Dados em prod:** `ml_billing_daily.competence_date` REAL (venda) para todas as faturas de 2026
- **Evidência:** blocos SQL de invariante/reconciliação/anti-IDOR/cross-month no SUMMARY
</artifacts>

<output>
Create `.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-05-SUMMARY.md` when done
</output>
