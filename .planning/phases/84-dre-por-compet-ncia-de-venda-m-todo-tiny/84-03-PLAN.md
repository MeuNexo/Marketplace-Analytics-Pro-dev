---
phase: 84-dre-por-compet-ncia-de-venda-m-todo-tiny
plan: 03
type: execute
wave: 2
depends_on: [84-01]
files_modified:
  - src/hooks/useMLBilling.ts
  - src/components/mercadolivre/MLCostCard.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "useMLBillingDaily filtra por competence_date (não mais charge_date) no range 01–fim do mês"
    - "coverageTo passa a rastrear MAX(competence_date), mantendo o guard de mês fechado correto"
    - "MLCostCard tem um dropdown de mês (jan/2026 → mês corrente, trava no futuro) ao lado das setas ◄ ►; setas continuam funcionando"
    - "Selecionar um mês no dropdown troca o período do DRE via o mesmo estado dreMonthOverride que as setas usam"
  artifacts:
    - path: "src/hooks/useMLBilling.ts"
      provides: "Filtro DRE por competence_date + coverageTo por competence_date"
      contains: "competence_date"
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "Dropdown de mês (Select shadcn) + prop onSelectMonth + prop months"
      contains: "onSelectMonth"
    - path: "src/pages/MercadoLivre.tsx"
      provides: "Lista de meses 2026→corrente + wiring do onSelectMonth ao dreMonthOverride"
      contains: "onSelectMonth"
  key_links:
    - from: "src/components/mercadolivre/MLCostCard.tsx"
      to: "src/pages/MercadoLivre.tsx (setDreMonthOverride)"
      via: "prop onSelectMonth chamada pelo Select"
      pattern: "onSelectMonth"
    - from: "src/hooks/useMLBilling.ts (useMLBillingDaily)"
      to: "ml_billing_daily.competence_date"
      via: ".gte/.lte('competence_date', ...)"
      pattern: "competence_date"
---

<objective>
Trocar a DRE do card `MLCostCard` para operar por competência de venda e adicionar o dropdown de mês. (1) `useMLBillingDaily` filtra o range mensal por `competence_date` (era `charge_date`) e passa a computar `coverageTo` a partir de `MAX(competence_date)` (senão o guard de mês fechado de `useMLBillingDailyWithSync` quebra — charge_date e competence_date divergem exatamente por causa desta fase). (2) `MLCostCard` ganha um `<Select>` (shadcn) de mês ao lado das setas, com trava no futuro; nova prop `onSelectMonth` + `months`. (3) `MercadoLivre.tsx` monta a lista de meses (2026-01 → mês corrente) e liga `onSelectMonth` ao `setDreMonthOverride` já existente (o mesmo estado que `shiftDreMonth` escreve).

Purpose: Terceiro elo da ordem estrita. Depende do 84-01 (coluna competence_date tipada em types.ts). A troca de filtro é segura porque a migration faz backfill `competence_date := charge_date` na mesma transação — nunca há NULL. NÃO faz merge para prod aqui; o go-live visual/merge é o plano 84-06 (após backfill + smoke).
Output: edições de 3 arquivos de frontend.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-RESEARCH.md
@src/hooks/useMLBilling.ts
@src/components/mercadolivre/MLCostCard.tsx
@src/components/mercadolivre/MLStoreSelector.tsx
</context>

<tasks>

<!-- planner-discipline-allow: gte("charge_date" -->
<!-- planner-discipline-allow: lte("charge_date" -->

<task type="auto">
  <name>Task 1: useMLBillingDaily — filtro e coverageTo por competence_date</name>
  <files>src/hooks/useMLBilling.ts</files>
  <read_first>
    - src/hooks/useMLBilling.ts linhas 292-338 (useMLBillingDaily: select, range charge_date, montagem de coverageTo)
    - src/hooks/useMLBilling.ts linhas 358-426 (useMLBillingDailyWithSync: expectedCoverage/isIncomplete usam coverageTo)
    - 84-RESEARCH.md Pitfall 2 (coverageTo silenciosamente quebra se ficar em charge_date)
  </read_first>
  <action>
    Em `useMLBillingDaily`:
    - No `.select(...)` (linha ~311), incluir `competence_date` na lista de colunas e no tipo `Row`.
    - Trocar `.gte("charge_date", from).lte("charge_date", to)` por `.gte("competence_date", from).lte("competence_date", to)`.
    - Na montagem do resultado, computar `coverageTo` a partir de `r.competence_date` (era `r.charge_date`): `if (!coverageTo || r.competence_date > coverageTo) coverageTo = r.competence_date;`.
    - Manter a paginação `.range()`, a agregação por `charge_type`, o arredondamento e o retorno `{ charges, coverageTo }`.
    NÃO alterar `useMLBilling`/`useMLBillingWithSync` (trilha ml_billing_monthly) nem `groupBillingCharges`. `useMLBillingDailyWithSync` não muda de código — ele já consome `coverageTo`, que agora reflete competência.
    Atualizar o comentário do bloco (linhas ~287-291) para dizer "competência = data da venda (sale_date_time), fallback data de lançamento" em vez de "competência = data de lançamento".
  </action>
  <acceptance_criteria>
    - `grep -c 'gte("competence_date"' src/hooks/useMLBilling.ts` == 1 e `grep -c 'lte("competence_date"' src/hooks/useMLBilling.ts` == 1
    - `grep -c 'gte("charge_date"' src/hooks/useMLBilling.ts` == 0 (dentro de useMLBillingDaily) e `grep -c 'lte("charge_date"' src/hooks/useMLBilling.ts` == 0
    - `coverageTo` é derivado de `competence_date` (grep confirma `r.competence_date > coverageTo`).
    - `competence_date` aparece na lista de `.select(` de useMLBillingDaily.
    - `npx tsc --noEmit` limpo (depende de 84-01 ter tipado a coluna).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'gte("competence_date"' src/hooks/useMLBilling.ts && ! grep -q 'lte("charge_date"' src/hooks/useMLBilling.ts && echo OK</automated>
  </verify>
  <done>DRE filtra por competência; guard de mês fechado usa MAX(competence_date); trilha de fatura intacta.</done>
</task>

<task type="auto">
  <name>Task 2: MLCostCard — dropdown de mês (Select shadcn) + prop onSelectMonth</name>
  <files>src/components/mercadolivre/MLCostCard.tsx</files>
  <read_first>
    - src/components/mercadolivre/MLCostCard.tsx (cabeçalho linhas 84-124: setas ChevronLeft/Right + mesLabel + badge fonte)
    - src/components/mercadolivre/MLStoreSelector.tsx (padrão de import e uso do Select shadcn)
  </read_first>
  <action>
    Adicionar props opcionais em `MLCostCardProps`: `months?: Array<{ value: string; label: string }>` (lista jan/2026 → corrente, do mais recente para o mais antigo), `selectedMonth?: string` (o billingMonth atual, ex.: "2026-06"), `onSelectMonth?: (m: string) => void`.
    Importar `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` de `@/components/ui/select`. Renderizar um `<Select value={selectedMonth} onValueChange={onSelectMonth} disabled={syncing}>` no cabeçalho, ENTRE o `mesLabel` e a badge de fonte (ou substituindo o `<span>{mesLabel}</span>` central por um trigger compacto). Manter as setas ◄ ► funcionando exatamente como hoje (não removê-las). Estilo compacto alinhado ao cabeçalho (ex.: `SelectTrigger` `h-6 text-[10px]` w ~[104px]). Só renderizar o Select se `months` e `onSelectMonth` vierem definidos (fallback: continua exibindo o `mesLabel` textual quando não há dropdown — retrocompatível).
    A trava no futuro é garantida pela própria lista `months` (não inclui meses > corrente); as setas continuam usando `canGoNext`.
  </action>
  <acceptance_criteria>
    - `grep -c "onSelectMonth" src/components/mercadolivre/MLCostCard.tsx` >= 2 (prop na interface + uso)
    - `grep -c "@/components/ui/select" src/components/mercadolivre/MLCostCard.tsx` == 1
    - As setas (`ChevronLeft`/`ChevronRight` com `onPrevMonth`/`onNextMonth`) permanecem no arquivo (`grep -c "ChevronLeft" ...` >= 1).
    - `<Select>` desabilita durante `syncing`.
    - `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "onSelectMonth" src/components/mercadolivre/MLCostCard.tsx && grep -q "ChevronLeft" src/components/mercadolivre/MLCostCard.tsx && echo OK</automated>
  </verify>
  <done>Dropdown de mês presente ao lado das setas; setas preservadas; desabilita em sync; trava no futuro via lista.</done>
</task>

<task type="auto">
  <name>Task 3: MercadoLivre.tsx — lista de meses + wiring do onSelectMonth</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <read_first>
    - src/pages/MercadoLivre.tsx linhas 180-260 (dreMonthOverride, billingMonth, shiftDreMonth, currentCalendarMonth, mesLabel)
    - src/pages/MercadoLivre.tsx linhas 773-792 (uso de MLCostCard)
  </read_first>
  <action>
    Montar via `useMemo` uma lista `dreMonths: Array<{ value: string; label: string }>` de `2026-01` até `currentCalendarMonth` (inclusive), ordenada do mais recente para o mais antigo, com label pt-BR ("Junho/2026") reusando a mesma formatação de `mesLabel`. Passar ao `MLCostCard` as novas props: `months={dreMonths}`, `selectedMonth={billingMonth}`, `onSelectMonth={(m) => setDreMonthOverride(m === filterMonth ? null : m)}` (espelha a regra de `shiftDreMonth`: quando o mês escolhido == filterMonth, volta a seguir o filtro em vez de fixar override). Manter `onPrevMonth`/`onNextMonth`/`canGoNext`/`syncing`/`faturaFrom`/`faturaTo` como estão. Não mexer em nenhuma outra lógica de billing.
  </action>
  <acceptance_criteria>
    - `grep -c "onSelectMonth=" src/pages/MercadoLivre.tsx` == 1 e `grep -c "months={dreMonths}" src/pages/MercadoLivre.tsx` == 1
    - A lista de meses começa em "2026-01" e termina no mês corrente (não inclui meses futuros).
    - `onSelectMonth` chama `setDreMonthOverride`, com colapso para `null` quando o mês == filterMonth.
    - As setas e `canGoNext` continuam ligadas (sem regressão).
    - `npx tsc --noEmit` limpo; `npm run build` limpo.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "onSelectMonth=" src/pages/MercadoLivre.tsx && grep -q "months={dreMonths}" src/pages/MercadoLivre.tsx && echo OK</automated>
  </verify>
  <done>Dropdown ligado ao dreMonthOverride; meses 2026→corrente; setas intactas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (org member JWT) → ml_billing_daily | Leitura via RLS `is_org_member`; consulta escopada por organization_id + ml_user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-06 | Information Disclosure | filtro por competence_date | mitigate | Consulta continua `.eq("organization_id", orgId).in("ml_user_id", resolvedMLUserIds)`; RLS inalterada; nenhuma coluna nova amplia escopo de tenant |
| T-84-07 | Tampering | seleção de mês pelo dropdown | accept | Mês é só filtro de leitura client-side; não dispara escrita; backfill/re-sync já gated por service-role na EF |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run build` limpos.
- Filtro do DRE em competence_date; coverageTo em competence_date; dropdown presente; setas preservadas.
</verification>

<success_criteria>
DRE por competência com dropdown de mês funcional (autorado, não merged); guard de mês fechado correto; retrocompatível.
</success_criteria>

<artifacts>
## Artifacts que este plano produz
- **Props novas em MLCostCard:** `months`, `selectedMonth`, `onSelectMonth`
- **Componente novo (inline):** dropdown `<Select>` de mês no cabeçalho do MLCostCard
- **Memo novo em MercadoLivre.tsx:** `dreMonths` (lista 2026-01 → corrente)
- **Mudança de comportamento:** `useMLBillingDaily` filtra/coverageTo por `competence_date`
</artifacts>

<output>
Create `.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-03-SUMMARY.md` when done
</output>
