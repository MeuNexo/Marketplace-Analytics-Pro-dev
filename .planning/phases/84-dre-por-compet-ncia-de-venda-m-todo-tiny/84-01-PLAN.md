---
phase: 84-dre-por-compet-ncia-de-venda-m-todo-tiny
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql
  - src/integrations/supabase/types.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A coluna competence_date existe em ml_billing_daily e nenhuma linha existente fica NULL (backfill = charge_date na mesma migration)"
    - "A UNIQUE constraint passa a incluir competence_date, sem quebrar o isolamento por org/loja (organization_id e ml_user_id permanecem as duas primeiras colunas)"
    - "ml_billing_daily passa a existir em types.ts (antes ausente) com competence_date tipada"
  artifacts:
    - path: "supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql"
      provides: "ADD COLUMN competence_date + backfill + widen UNIQUE + index de suporte"
      contains: "competence_date"
    - path: "src/integrations/supabase/types.ts"
      provides: "Definição completa da tabela ml_billing_daily incl. competence_date"
      contains: "ml_billing_daily"
  key_links:
    - from: "supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql"
      to: "ml_billing_daily (Postgres)"
      via: "ALTER TABLE ADD COLUMN + widen UNIQUE via DO-block dynamic lookup"
      pattern: "competence_date"
---

<objective>
Autoria (sem aplicar) da migration de schema que habilita a DRE por competência de venda: adiciona a coluna `competence_date` em `ml_billing_daily`, faz backfill imediato `competence_date := charge_date` das linhas existentes na MESMA migration (gap zero-width — nenhuma consulta vê NULL), alarga a UNIQUE constraint para incluir `competence_date` (senão o backfill do re-sync colide) e adiciona o índice de suporte para o novo filtro. Também registra a tabela `ml_billing_daily` inteira (hoje AUSENTE) em `types.ts`, incluindo a nova coluna.

Purpose: É o primeiro elo da ordem estrita de deploy (migration → EF → frontend → backfill). Sem a coluna + constraint alargada, o deploy da EF (grão novo) falha com violação de UNIQUE.
Output: 1 arquivo de migration novo + edição de `types.ts`. NENHUMA aplicação em prod (isso é o plano 84-04, via MCP).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-RESEARCH.md
@supabase/migrations/20260613020000_ml_billing_daily.sql
@supabase/migrations/20260666000000_fornecedor_scope.sql
</context>

<tasks>

<!-- planner-discipline-allow: ml_billing_monthly -->

<task type="auto">
  <name>Task 1: Migration — ADD COLUMN competence_date + backfill + widen UNIQUE + index</name>
  <files>supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql</files>
  <read_first>
    - supabase/migrations/20260613020000_ml_billing_daily.sql (CREATE TABLE original: UNIQUE inline de 5 colunas com nome auto-gerado/truncado; índices idx_ml_billing_daily_lookup/_invoice)
    - supabase/migrations/20260666000000_fornecedor_scope.sql (padrão ADD COLUMN IF NOT EXISTS + backfill na mesma migration)
    - 84-RESEARCH.md Pattern 1 (backfill same-migration) e Pattern 2 (widen UNIQUE via DO-block sem hardcodar nome)
  </read_first>
  <action>
    Criar `supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql`, idempotente, na seguinte ordem exata:
    (1) `ALTER TABLE public.ml_billing_daily ADD COLUMN IF NOT EXISTS competence_date DATE;`
    (2) Backfill na MESMA migration: `UPDATE public.ml_billing_daily SET competence_date = charge_date WHERE competence_date IS NULL;` — garante que nenhuma linha existente fique NULL no instante em que o frontend trocar o filtro (evita DRE em branco).
    (3) `ALTER TABLE public.ml_billing_daily ALTER COLUMN competence_date SET NOT NULL;`
    (4) Alargar a UNIQUE: bloco `DO $$` que faz `SELECT conname INTO ...` de `pg_constraint WHERE conrelid = 'public.ml_billing_daily'::regclass AND contype = 'u'`, e se encontrado faz `EXECUTE format('ALTER TABLE public.ml_billing_daily DROP CONSTRAINT %I', c_name)`. NÃO hardcodar o nome (é auto-gerado e provavelmente truncado). Em seguida `ADD CONSTRAINT ml_billing_daily_uniq UNIQUE (organization_id, ml_user_id, competence_date, charge_date, charge_type, source_invoice_key)`. `organization_id` e `ml_user_id` DEVEM permanecer as duas primeiras colunas (mantém isolamento cross-org estruturalmente impossível — anti-IDOR).
    (5) Índice de suporte para o novo shape de consulta: `CREATE INDEX IF NOT EXISTS idx_ml_billing_daily_competence ON public.ml_billing_daily (organization_id, ml_user_id, competence_date);`
    Cabeçalho em comentário: projeto Supabase `ckcdevcxgvueywivefgx` (NÃO gionpsuunfkkzzjdubfy); DEPLOY só via MCP apply_migration (sem token CLI); NUNCA via SQL Editor. NÃO tocar em `ml_billing_monthly`, políticas RLS ou nos índices existentes.
    Observação: o plano 84-04 (MCP) DEVE confirmar via `SELECT conname, contype FROM pg_constraint` que existe exatamente UMA UNIQUE constraint antes de aplicar — o DO-block é escrito para ser robusto, mas a verificação vive no gate de aplicação.
  </action>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql` existe.
    - `grep -c "ADD COLUMN IF NOT EXISTS competence_date DATE" <arquivo>` == 1
    - `grep -c "SET competence_date = charge_date" <arquivo>` == 1
    - `grep -c "SET NOT NULL" <arquivo>` == 1
    - Contém um bloco `DO $$` que lê `pg_constraint` (`grep -c "pg_constraint" <arquivo>` >= 1) e um `ADD CONSTRAINT ... UNIQUE (organization_id, ml_user_id, competence_date, charge_date, charge_type, source_invoice_key)`.
    - `grep -c "idx_ml_billing_daily_competence" <arquivo>` >= 1
    - `grep -c "ml_billing_monthly" <arquivo>` == 0 (não toca a trilha de fatura).
    - Nenhum nome de constraint hardcodado num DROP (o DROP usa `format('... %I', c_name)`).
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql && grep -q "competence_date, charge_date, charge_type, source_invoice_key" supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql && echo OK</automated>
  </verify>
  <done>Migration autorada, idempotente, com backfill same-transaction, UNIQUE alargada por DO-block e índice; pronta para o gate de aplicação MCP (84-04).</done>
</task>

<task type="auto">
  <name>Task 2: types.ts — registrar ml_billing_daily (hoje ausente) com competence_date</name>
  <files>src/integrations/supabase/types.ts</files>
  <read_first>
    - src/integrations/supabase/types.ts (bloco `ml_billing_monthly` ~linhas 673-710 como molde de shape Row/Insert/Update/Relationships)
    - supabase/migrations/20260613020000_ml_billing_daily.sql (colunas reais: id, organization_id, ml_user_id, charge_date, charge_type, charge_label, amount, source_invoice_key, synced_at)
  </read_first>
  <action>
    Adicionar a definição COMPLETA da tabela `ml_billing_daily` em `Database.public.Tables` de `types.ts` (imediatamente antes ou depois de `ml_billing_monthly`, mantendo ordem alfabética se possível). Colunas: `id: string`, `organization_id: string`, `ml_user_id: string`, `charge_date: string`, `charge_type: string`, `charge_label: string | null`, `amount: number`, `source_invoice_key: string`, `synced_at: string | null`, e a NOVA `competence_date: string` (NOT NULL após a migration → obrigatória em Row; opcional em Insert/Update `competence_date?: string`). Seguir o mesmo shape Row/Insert/Update/Relationships de `ml_billing_monthly` (Relationships: FK `organization_id` → `organizations.id`). Precedente: Phase 52 atualizou types.ts manualmente após migration.
  </action>
  <acceptance_criteria>
    - `grep -c "ml_billing_daily:" src/integrations/supabase/types.ts` >= 1
    - `grep -c "competence_date" src/integrations/supabase/types.ts` >= 3 (Row + Insert + Update)
    - `npx tsc --noEmit` retorna 0 erros.
    - Row de `ml_billing_daily` inclui todas as 10 colunas (9 originais + competence_date).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "ml_billing_daily:" src/integrations/supabase/types.ts && echo OK</automated>
  </verify>
  <done>Tabela ml_billing_daily tipada com competence_date; tsc limpo; consultas do hook (84-03) passam a ter checagem de coluna em tempo de compilação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role (EF) → ml_billing_daily | Escrita só por service-role (bypassa RLS); cliente nunca escreve |
| org member (browser JWT) → ml_billing_daily | Leitura via RLS `is_org_member(auth.uid(), organization_id)` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering | UNIQUE constraint alargada | mitigate | organization_id + ml_user_id permanecem as 2 primeiras colunas da UNIQUE → duplicação cross-org continua estruturalmente impossível; só aumenta a granularidade intra-org/intra-fatura (intencional) |
| T-84-02 | Information Disclosure | nova coluna competence_date | accept | RLS é row-level (não column-level); `competence_date` não introduz novo escopo de tenant; policy `org_member_billing_daily_select` já cobre a linha inteira |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo.
- Migration existe e contém coluna, backfill, UNIQUE alargada (6 colunas), índice; não toca ml_billing_monthly.
- Nenhum nome de constraint hardcodado.
</verification>

<success_criteria>
Migration de schema autorada (não aplicada) + types.ts com ml_billing_daily incl. competence_date; tsc limpo; base pronta para o gate MCP 84-04.
</success_criteria>

<artifacts>
## Artifacts que este plano produz
- **Arquivo novo:** `supabase/migrations/20260685000000_ml_billing_daily_competence_date.sql`
- **Coluna nova:** `ml_billing_daily.competence_date DATE NOT NULL`
- **Constraint nova:** `ml_billing_daily_uniq` UNIQUE (organization_id, ml_user_id, competence_date, charge_date, charge_type, source_invoice_key)
- **Índice novo:** `idx_ml_billing_daily_competence`
- **Tipo novo:** entrada `ml_billing_daily` em `Database.public.Tables` (types.ts)
</artifacts>

<output>
Create `.planning/phases/84-dre-por-compet-ncia-de-venda-m-todo-tiny/84-01-SUMMARY.md` when done
</output>
