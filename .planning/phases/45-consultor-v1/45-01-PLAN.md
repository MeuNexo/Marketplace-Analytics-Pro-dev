---
phase: 45-consultor-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260645000000_consultor_tables.sql
  - supabase/migrations/20260645010000_consultor_engine_rpcs.sql
  - src/integrations/supabase/types.ts
autonomous: false   # contém task [BLOCKING] de apply via MCP (orquestrador, fora do executor)
requirements: [CONSUL-01, CONSUL-04]
must_haves:
  truths:
    - "Tabela insights existe com RLS org-first (SELECT is_org_member; UPDATE dismiss is_org_member; INSERT/DELETE service role)"
    - "Tabela consultor_config existe com defaults dos 12 limiares e RLS (SELECT membro; ALL owner)"
    - "Tabela consultor_health_snapshots existe com 1 linha por org+mês e RLS (SELECT membro)"
    - "Engine consegue ler margem por produto e cobertura por produto via RPC org-scoped sem truncar em 1000 linhas"
  artifacts:
    - path: "supabase/migrations/20260645000000_consultor_tables.sql"
      provides: "3 tabelas do Consultor v1 + RLS org-first + índice único funcional de dedup"
      contains: "create table"
    - path: "supabase/migrations/20260645010000_consultor_engine_rpcs.sql"
      provides: "RPCs SECURITY DEFINER que o engine usa para margem/cobertura/no-cost por org (agregados, sem limite PostgREST)"
      contains: "create or replace function"
    - path: "src/integrations/supabase/types.ts"
      provides: "Tipos TS das 3 tabelas novas para o hook do frontend"
      contains: "insights"
  key_links:
    - from: "insights"
      to: "is_org_member / get_org_role"
      via: "RLS policies"
      pattern: "is_org_member"
---

<objective>
Criar a fundação de dados do Consultor v1: as 3 tabelas novas (`insights`, `consultor_config`, `consultor_health_snapshots`) com RLS org-first, e as RPCs SECURITY DEFINER que o engine (Plan 02) usará para avaliar as regras de margem/estoque/sem-custo por org sem esbarrar no truncamento de 1000 linhas do PostgREST. Atualizar `types.ts` para o hook do frontend (Plan 03).

Purpose: Sem essas tabelas e RPCs, o engine não tem onde gravar nem como ler agregados org-scoped de forma confiável. As migrations DEVEM ser aplicadas no banco vivo (`ckcdevcxgvueywivefgx`) antes da verificação — `tsc`/`build` passam sem o apply e criariam falso-positivo.
Output: 2 migrations SQL + types.ts atualizado + tabelas/RPCs aplicadas no projeto correto.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/45-consultor-v1/45-CONTEXT.md
@.planning/phases/45-consultor-v1/45-RESEARCH.md
@.planning/phases/45-consultor-v1/45-PATTERNS.md
@supabase/migrations/20260614123000_tenant04_onboarding_progress.sql
@supabase/migrations/20260527110000_margin_aggregate_rpcs.sql
</context>

<critical_constraints>
- **Supabase project correto: `ckcdevcxgvueywivefgx`** (o `gionpsuunfkkzzjdubfy` em CLAUDE.md está desatualizado — NUNCA usar). O CLI local está linkado no projeto errado → `supabase db push` NÃO funciona.
- **Apply de migration é feito via MCP `apply_migration` pelo orquestrador (não pelo executor).** O executor SÓ escreve os arquivos `.sql` e `types.ts`. A Task de apply é `[BLOCKING]` e manual.
- **`UNIQUE` com `COALESCE` não pode ser constraint inline** (Pitfall 7) — usar `CREATE UNIQUE INDEX` funcional separado.
- RLS org-first segue exatamente o analog `20260614123000_tenant04_onboarding_progress.sql`: `is_org_member` para SELECT, `get_org_role(...) = 'owner'::public.org_role` para escrita restrita a owner.
- Migrations idempotentes: `CREATE TABLE IF NOT EXISTS`, `DROP POLICY IF EXISTS` antes de recriar, constraint via `DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;`.
- **NÃO criar dependência de runtime nova.**
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Migration das 3 tabelas do Consultor v1 (RLS org-first)</name>
  <files>supabase/migrations/20260645000000_consultor_tables.sql</files>
  <read_first>
    - supabase/migrations/20260614123000_tenant04_onboarding_progress.sql (analog EXATO de migration RLS org-first idempotente — copiar estrutura de policies e DO/EXCEPTION)
    - .planning/phases/45-consultor-v1/45-RESEARCH.md §"Schema das Tabelas Novas" (colunas exatas de insights/consultor_config/consultor_health_snapshots)
    - .planning/phases/45-consultor-v1/45-PATTERNS.md §"supabase/migrations/20260645_consultor_tables.sql" (diferenças de RLS por tabela)
  </read_first>
  <action>
    Criar `insights` conforme schema do RESEARCH §Schema: colunas id (uuid PK default gen_random_uuid), organization_id (uuid NOT NULL FK organizations ON DELETE CASCADE), ml_user_id (text NULL), rule_key (text NOT NULL), category (text NOT NULL), severity (text NOT NULL), title/body/action_label/action_href (text NOT NULL), impact_brl (numeric NULL), status (text NOT NULL DEFAULT 'active'), created_at/updated_at (timestamptz default now), resolved_at/dismissed_at (timestamptz NULL). Adicionar CHECK idempotente (via DO/EXCEPTION) para severity IN ('critical','high','medium') e status IN ('active','resolved','dismissed').
    Para dedup, NÃO usar constraint UNIQUE inline com COALESCE (Pitfall 7). Em vez disso adicionar coluna helper `ml_user_id_key text NOT NULL DEFAULT ''` (o engine preenche com ml_user_id ou '') E criar `CREATE UNIQUE INDEX IF NOT EXISTS insights_dedup_idx ON public.insights (organization_id, rule_key, ml_user_id_key)`. Esta coluna helper resolve o open question A4/Q3 do RESEARCH (mais robusta que índice funcional com COALESCE). Criar também `CREATE INDEX IF NOT EXISTS insights_org_status_idx ON public.insights (organization_id, status, severity)`.
    RLS de insights: ENABLE ROW LEVEL SECURITY; policy SELECT `insights_select` TO authenticated USING is_org_member(auth.uid(), organization_id); policy UPDATE `insights_dismiss` TO authenticated USING is_org_member(...) WITH CHECK is_org_member(...). NÃO criar policy de INSERT/DELETE para authenticated (só service role escreve — service role ignora RLS).
    Criar `consultor_config` (organization_id uuid PK FK ON DELETE CASCADE) com TODOS os limiares default do RESEARCH §consultor_config: margin_critical_pct DEFAULT 0, margin_alert_pct DEFAULT 10, tacos_alert_pct DEFAULT 15, acos_alert_pct DEFAULT 30, roas_min DEFAULT 3, ads_no_sale_days DEFAULT 7, stock_critical_days DEFAULT 7, stock_alert_days DEFAULT 15, ticket_drop_pct DEFAULT 10, claims_spike_pct DEFAULT 20, goal_risk_pct DEFAULT 10, paused_ads_lookback_days DEFAULT 30, updated_at timestamptz default now. RLS: SELECT is_org_member; ALL (escrita) `get_org_role(auth.uid(), organization_id) = 'owner'::public.org_role` (D-02: editável só via SQL/owner no v1).
    Criar `consultor_health_snapshots` conforme RESEARCH §snapshots: id uuid PK, organization_id uuid NOT NULL FK ON DELETE CASCADE, score integer NOT NULL, score_margin/score_ads/score_estoque/score_reputacao/score_completude integer NOT NULL DEFAULT 0, insights_total/insights_critical integer NOT NULL DEFAULT 0, snapshot_month char(7) NOT NULL ('YYYY-MM'), created_at timestamptz default now. Constraint UNIQUE (organization_id, snapshot_month) — esta PODE ser inline (sem COALESCE). Índice `snapshots_org_month_idx (organization_id, snapshot_month DESC)`. RLS: SELECT is_org_member; sem policy de escrita para authenticated (service role escreve).
    Toda a migration idempotente (IF NOT EXISTS, DROP POLICY IF EXISTS antes de cada CREATE POLICY, DO/EXCEPTION nos CHECK/constraints). Cabeçalho com comentário explicando cada tabela (sem `#`; SQL usa `--`).
  </action>
  <verify>
    <automated>grep -c "create policy" supabase/migrations/20260645000000_consultor_tables.sql | grep -qE "[5-9]|[0-9]{2}" && grep -q "ml_user_id_key" supabase/migrations/20260645000000_consultor_tables.sql && grep -q "is_org_member" supabase/migrations/20260645000000_consultor_tables.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - As 3 tabelas estão definidas com `CREATE TABLE IF NOT EXISTS public.<t>` e FK para organizations ON DELETE CASCADE.
    - `insights` tem coluna `ml_user_id_key text NOT NULL DEFAULT ''` + `CREATE UNIQUE INDEX insights_dedup_idx (organization_id, rule_key, ml_user_id_key)` (não constraint COALESCE inline).
    - `consultor_config` tem TODOS os 12 limiares com os defaults exatos do RESEARCH.
    - Policy SELECT de cada tabela usa `is_org_member(auth.uid(), organization_id)`; consultor_config tem policy ALL com `get_org_role(...) = 'owner'`.
    - `insights` NÃO tem policy de INSERT/DELETE para `authenticated`; snapshots NÃO tem policy de escrita para `authenticated`.
    - Migration é idempotente (grep mostra `if not exists` e `drop policy if exists`).
  </acceptance_criteria>
  <done>Arquivo SQL escrito, idempotente, com 3 tabelas + RLS org-first + índice de dedup por coluna helper.</done>
</task>

<task type="auto">
  <name>Task 2: RPCs SECURITY DEFINER para o engine (margem/cobertura/sem-custo por org)</name>
  <files>supabase/migrations/20260645010000_consultor_engine_rpcs.sql</files>
  <read_first>
    - supabase/migrations/20260527110000_margin_aggregate_rpcs.sql (analog de RPC SQL agregado org-scoped: assinatura p_org_id/p_user_ids, LANGUAGE sql STABLE, fórmula de lucro com COALESCE)
    - .planning/phases/45-consultor-v1/45-RESEARCH.md §"Fontes de Dados por Regra" (queries SQL de cada regra: margem por item_id, cobertura, sem custo, pausado com venda)
    - .planning/phases/45-consultor-v1/45-PATTERNS.md §"RPCs de margem" e §"Cobertura de estoque"
  </read_first>
  <action>
    Criar RPCs que o engine invocará via `supabase.rpc(...)` dentro da EF (Plan 02) para evitar o truncamento de 1000 linhas do PostgREST (Pitfall 1/5) — RPCs retornam linhas agregadas por produto, não a tabela bruta. Todas `LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public` (DEFINER para o engine ler sob service role e para a função aplicar filtro org explícito; o engine SEMPRE passa org_id+ml_user_ids).
    1. `get_consultor_margin_by_product(p_org_id uuid, p_user_ids text[], p_from date, p_to date)` RETURNS TABLE(item_id text, receita numeric, lucro numeric, lucro_pct numeric) — replicar a fórmula de lucro do analog (receita_bruta - custo_unit*quantidade - comissao - frete - tax_amount) agrupada por item_id, filtrando organization_id = p_org_id, ml_user_id = ANY(p_user_ids), status IN ('paid','shipped','delivered'), data_pedido no range, custo_unit IS NOT NULL, HAVING SUM(receita_bruta) > 0. (Regra margem D-03.)
    2. `get_consultor_coverage(p_org_id uuid, p_from date)` RETURNS TABLE(item_id text, title text, price numeric, coverage_days integer, avg_daily numeric) — portar a lógica de useMLCoverage: avg_daily = SUM(qty_sold)/30 de ml_product_daily_cache (date >= p_from), JOIN com ml_inventory_cache (status='active') por item_id+organization_id; coverage_days = FLOOR(available_quantity / NULLIF(avg_daily,0)). (Regra estoque D-05.)
    3. `get_consultor_paused_with_sales(p_org_id uuid, p_from date)` RETURNS TABLE(item_id text, title text, price numeric, vendas_30d numeric) — ml_inventory_cache status='paused' JOIN ml_product_daily_cache (date >= p_from) HAVING SUM(qty_sold) > 0. (Regra anúncio pausado D-08.)
    4. `get_consultor_no_cost_count(p_org_id uuid)` RETURNS integer — COUNT de ml_inventory_cache status='active' LEFT JOIN ml_product_costs ON (item_id OR seller_sku) WHERE custo ausente. (Regra sem custo.)
    Confirmar os nomes de coluna reais antes de escrever: `orders` tem receita_bruta/custo_unit/quantidade/comissao/frete/tax_amount/status/data_pedido/item_id/ml_user_id/organization_id (verificado no analog de margem); `ml_inventory_cache` tem available_quantity/price/title/status/item_id/organization_id/seller_custom_field; `ml_product_daily_cache` tem item_id/qty_sold/date/organization_id. Se algum nome divergir, o orquestrador valida via MCP no apply (Task 4) — escrever com os nomes do RESEARCH e sinalizar dúvida no SUMMARY.
    Idempotente: `CREATE OR REPLACE FUNCTION`. Conceder `GRANT EXECUTE ... TO service_role` em cada RPC.
    NOTA importante (documentar no topo do arquivo como comentário `--`): TACoS (regra 3) e meta (regra 10) NÃO viram RPC aqui — são queries agregadas pequenas que o engine roda inline na EF (Plan 02). A revenue org-scoped do TACoS usa `orders.receita_bruta` (NÃO `ml_daily_cache.approved_revenue`, que é scoped por user_id e não por organization_id — descoberta desta fase, corrige o A5 do RESEARCH).
  </action>
  <verify>
    <automated>grep -c "create or replace function" supabase/migrations/20260645010000_consultor_engine_rpcs.sql | grep -qE "[4-9]" && grep -q "get_consultor_margin_by_product" supabase/migrations/20260645010000_consultor_engine_rpcs.sql && grep -q "security definer" supabase/migrations/20260645010000_consultor_engine_rpcs.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 4 RPCs criadas com `CREATE OR REPLACE FUNCTION` e `SECURITY DEFINER SET search_path = public`.
    - `get_consultor_margin_by_product` recebe (p_org_id, p_user_ids, p_from, p_to) e filtra por organization_id + ml_user_id ANY + status pagos.
    - `get_consultor_coverage` calcula coverage_days = FLOOR(available_quantity / avg_daily) com JOIN org-scoped.
    - `GRANT EXECUTE ... TO service_role` presente para cada RPC.
    - Comentário no topo documenta que TACoS usa `orders.receita_bruta` (não ml_daily_cache.approved_revenue).
  </acceptance_criteria>
  <done>Migration de RPCs escrita, idempotente, com 4 funções agregadas org-scoped.</done>
</task>

<task type="auto">
  <name>Task 3: Tipos TS das 3 tabelas novas em types.ts</name>
  <files>src/integrations/supabase/types.ts</files>
  <read_first>
    - src/integrations/supabase/types.ts (ver o padrão de definição de uma tabela existente, ex: onboarding_progress, para Row/Insert/Update)
  </read_first>
  <action>
    Adicionar manualmente as entradas das 3 tabelas novas (`insights`, `consultor_config`, `consultor_health_snapshots`) ao bloco `public.Tables` de types.ts, seguindo o formato Row/Insert/Update já usado pelas outras tabelas. Tipar conforme as colunas da Task 1 (incluir `ml_user_id_key` em insights; impact_brl/resolved_at/dismissed_at como `number | null`/`string | null`). Convenção do projeto (STATE.md: "tiny token columns added to types.ts manually") — types.ts é mantido à mão, NÃO regenerado do banco. NÃO remover nem reordenar entradas existentes.
  </action>
  <verify>
    <automated>grep -q "insights:" src/integrations/supabase/types.ts && grep -q "consultor_config:" src/integrations/supabase/types.ts && grep -q "consultor_health_snapshots:" src/integrations/supabase/types.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - types.ts tem as 3 tabelas com Row/Insert/Update tipados.
    - `npx tsc --noEmit` passa sem novos erros relacionados às tabelas.
    - Nenhuma entrada existente foi removida.
  </acceptance_criteria>
  <done>types.ts atualizado à mão com as 3 tabelas; tsc limpo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 [BLOCKING]: Aplicar as 2 migrations no banco via MCP apply_migration</name>
  <what-built>2 migrations escritas (20260645000000_consultor_tables.sql + 20260645010000_consultor_engine_rpcs.sql). Elas NÃO foram aplicadas — o gsd-executor não tem acesso ao Supabase MCP.</what-built>
  <how-to-verify>
    O ORQUESTRADOR (não o executor) deve, via Supabase MCP no projeto **`ckcdevcxgvueywivefgx`**:
    1. `mcp__supabase__apply_migration` com o conteúdo de `20260645000000_consultor_tables.sql`.
    2. `mcp__supabase__apply_migration` com o conteúdo de `20260645010000_consultor_engine_rpcs.sql`.
    3. Validar via `mcp__supabase__execute_sql`:
       - `SELECT to_regclass('public.insights'), to_regclass('public.consultor_config'), to_regclass('public.consultor_health_snapshots');` → 3 não-nulos.
       - `SELECT COUNT(*) FROM pg_proc WHERE proname LIKE 'get_consultor_%';` → ≥ 4.
       - `SELECT indexname FROM pg_indexes WHERE tablename='insights';` → contém `insights_dedup_idx`.
       - Conferir nomes de coluna reais de `orders`/`ml_inventory_cache`/`ml_product_daily_cache` contra as RPCs; se houver divergência, corrigir o .sql e reaplicar.
    4. `mcp__supabase__get_advisors` (security) → sem erro crítico novo nas 3 tabelas (RLS habilitado).
  </how-to-verify>
  <resume-signal>Digite "aplicado" após confirmar as 3 tabelas + 4 RPCs no projeto ckcdevcxgvueywivefgx, ou descreva o erro.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend (authenticated) → insights/config/snapshots | Membro de uma org não pode ler/alterar dados de outra org |
| service role (engine) → tabelas | Engine escreve livremente (ignora RLS); gap a cobrir é a leitura por authenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | SELECT em insights/config/snapshots | mitigate | Policy SELECT `is_org_member(auth.uid(), organization_id)` nas 3 tabelas (Task 1) |
| T-45-02 | Tampering | dismiss de insight de outra org | mitigate | Policy UPDATE de insights com USING + WITH CHECK `is_org_member` (Task 1) |
| T-45-03 | Elevation of Privilege | membro não-owner editar consultor_config | mitigate | Policy ALL de consultor_config restrita a `get_org_role = 'owner'` (Task 1) |
| T-45-04 | Tampering | authenticated inserir/forjar insight ou snapshot | mitigate | Ausência de policy INSERT/DELETE para authenticated → só service role escreve (Task 1) |
| T-45-05 | Information Disclosure | RPC SECURITY DEFINER vazar cross-org | mitigate | RPCs sempre filtram `organization_id = p_org_id`; GRANT EXECUTE só a service_role (Task 2) |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NÃO instala pacotes (constraint: zero deps novas) — sem vetor de supply-chain |
</threat_model>

<verification>
- 3 tabelas existem no projeto `ckcdevcxgvueywivefgx` com RLS habilitado (Task 4).
- 4 RPCs `get_consultor_*` existem e executáveis por service_role.
- `npx tsc --noEmit` limpo após types.ts.
- `get_advisors` security sem erro crítico nas tabelas novas.
</verification>

<success_criteria>
- Migrations idempotentes escritas e aplicadas no projeto correto.
- RLS org-first confirmado (T-45-01..04 mitigados).
- types.ts pronto para o hook do Plan 03.
</success_criteria>

<output>
Create `.planning/phases/45-consultor-v1/45-01-SUMMARY.md` when done
</output>
