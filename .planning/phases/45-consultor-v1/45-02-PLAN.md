---
phase: 45-consultor-v1
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - supabase/functions/consultor-insights/index.ts
  - supabase/config.toml
  - supabase/migrations/20260645020000_pg_cron_consultor.sql
autonomous: false   # contém task [BLOCKING] de deploy de EF + apply de cron via MCP (orquestrador)
requirements: [CONSUL-01, CONSUL-04, CONSUL-05]
must_haves:
  truths:
    - "EF consultor-insights avalia as 12 regras por org e grava em insights com upsert idempotente (sem duplicar por run)"
    - "EF calcula score 0-100 ponderado (Margem30/Ads25/Estoque20/Reputação15/Completude10) e grava snapshot por mês"
    - "Insights cujas condições cessaram viram status='resolved'; insights 'dismissed' nunca são re-ativados"
    - "EF aceita auth dual: service role (cron, mode all_orgs) e user JWT (on-demand, mode org_only — só a própria org)"
    - "cron diário Pattern B 08:30 UTC invoca a EF; org Pé Vermeio gera ≥5 insights no 1º run"
  artifacts:
    - path: "supabase/functions/consultor-insights/index.ts"
      provides: "Engine de regras determinístico: 12 regras + score + upsert + auto-resolve + snapshot, auth dual"
      min_lines: 300
      contains: "runConsultorForOrg"
    - path: "supabase/migrations/20260645020000_pg_cron_consultor.sql"
      provides: "cron diário Pattern B que invoca a EF com mode all_orgs"
      contains: "cron.schedule"
    - path: "supabase/config.toml"
      provides: "verify_jwt=false para consultor-insights"
      contains: "consultor-insights"
  key_links:
    - from: "consultor-insights/index.ts"
      to: "insights / consultor_health_snapshots"
      via: "supabase.upsert (service role)"
      pattern: "from\\([\"']insights"
    - from: "pg_cron consultor-insights-daily"
      to: "consultor-insights EF"
      via: "net.http_post + vault service_role_key"
      pattern: "net.http_post"
---

<objective>
Implementar o motor de regras determinístico do Consultor v1 como uma EF Deno única (`consultor-insights`) que avalia as 12 regras (D-01) sobre dados reais por org, calcula o score de saúde 0-100 ponderado (D-09), gera os textos por template (D-22), faz upsert idempotente em `insights` com auto-resolve + preservação de dismiss (D-18), grava snapshot mensal do score (D-12), e expõe auth dual (cron service role + on-demand user JWT, D-20). Agendar o cron diário Pattern B (D-20).

Purpose: É o coração da fase — sem o engine não há insights nem score. A meta CONSUL-05 (≥5 insights reais no 1º run da Pé Vermeio) só é validável após deploy + invocação real.
Output: EF `consultor-insights` + config.toml + migration de cron, deployados/aplicados no projeto `ckcdevcxgvueywivefgx` e smoke-testados.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-consultor-v1/45-CONTEXT.md
@.planning/phases/45-consultor-v1/45-RESEARCH.md
@.planning/phases/45-consultor-v1/45-PATTERNS.md
@supabase/functions/sync-ml-claims/index.ts
@supabase/functions/sync-ml-inventory/index.ts
@supabase/migrations/20260614110000_pg_cron_questions_claims.sql
@.planning/phases/45-consultor-v1/45-01-SUMMARY.md
</context>

<critical_constraints>
- **Projeto correto: `ckcdevcxgvueywivefgx`.** Deploy de EF e apply de cron via MCP pelo ORQUESTRADOR (o executor não tem MCP/deploy). As tasks de deploy/apply são `[BLOCKING]` e manuais.
- **pg_cron Pattern B:** vault `service_role_key` = chave `sb_secret_*` (NÃO JWT legacy). Seguir EXATAMENTE `20260614110000_pg_cron_questions_claims.sql`.
- **verify_jwt=false ≠ sem auth** (Pitfall 4): auth interna dual obrigatória — padrão `sync-ml-inventory/authenticate()`.
- **PostgREST trunca em 1000 linhas** (Pitfall 1): dentro da EF, ler margem/cobertura/pausados/sem-custo via as RPCs `get_consultor_*` do Plan 01 (agregam em SQL), NÃO via `.from().select()` em tabelas grandes.
- **`ml_targets` sem organization_id** (Pitfall 6): regra de meta resolve via join `ml_tokens`; fallback = não dispara se sem meta na org.
- **`ml_ads_campaigns_cache` sem coluna date** (Pitfall 5): regra "campanha sem venda" opera em nível de org/dia via `ml_ads_daily_cache` (texto do insight deve refletir isso).
- **Revenue org-scoped (TACoS/meta/ticket)** usa `orders.receita_bruta` — NÃO `ml_daily_cache.approved_revenue` (esse é scoped por user_id, não organization_id; descoberta do Plan 01).
- **Limite 150s da EF** (Pitfall 8): em mode all_orgs, serializar orgs (não paralelo), com error isolation por org.
- **Zero dependência de runtime nova.**
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: EF consultor-insights — auth dual + loop por org + score + snapshot + upsert</name>
  <files>supabase/functions/consultor-insights/index.ts, supabase/config.toml</files>
  <read_first>
    - supabase/functions/sync-ml-inventory/index.ts (analog EXATO de auth dual service role + user JWT: função authenticate(), linhas ~60-92)
    - supabase/functions/sync-ml-claims/index.ts (analog de loop por org com error isolation + lookup ml_tokens + upsert)
    - .planning/phases/45-consultor-v1/45-RESEARCH.md §"Arquitetura do Engine", §"Score de Saúde 0-100 por Pilar", §"Ciclo de Vida dos Insights"
    - .planning/phases/45-consultor-v1/45-PATTERNS.md §"consultor-insights/index.ts" (boilerplate, authenticate, loop, upsert idempotente, auto-resolve)
  </read_first>
  <action>
    Criar `supabase/functions/consultor-insights/index.ts` com a estrutura do PATTERNS §consultor-insights:
    - Boilerplate de imports (deno std@0.168.0/http/server + @supabase/supabase-js@2.49.1 via esm.sh), CORS, helper json(). SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY de Deno.env.
    - `authenticate(req)`: porta do `sync-ml-inventory` — retorna { userId: null } se Bearer == service role key (cron); { userId } se Bearer for JWT válido (sb.auth.getUser); { error: 401 } caso contrário. (Mitiga T-45-06.)
    - `serve`: trata OPTIONS; chama authenticate; lê body.mode. Se userId === null → mode all_orgs: buscar todas as orgs distintas com ml_tokens (`.from('ml_tokens').select('organization_id').not('refresh_token','is',null)`) e iterar serializado. Se userId !== null → mode org_only: resolver a org do usuário; ANTES de rodar, validar `is_org_member(userId, orgId)` via rpc — se false, retornar 403 (mitiga T-45-07: on-demand só recalcula a própria org).
    - Loop por org com try/catch por org (error isolation, padrão sync-ml-claims): `runConsultorForOrg(sb, orgId)`. Coletar resultados; retornar { ok:true, results }.
    - `runConsultorForOrg`: (a) resolver ml_user_ids da org via ml_tokens (`ORDER BY updated_at DESC` determinístico, padrão ME-04); (b) ler consultor_config da org, com fallback para os defaults hardcoded (não criar row); (c) executar as 12 regras (Task 2 as implementa como funções/queries) e montar array de candidatos; (d) calcular score por pilar e score final (Task 2); (e) upsert idempotente dos candidatos em `insights` `onConflict: 'organization_id,rule_key,ml_user_id_key'`, preenchendo `ml_user_id_key` (= ml_user_id ?? ''), e NUNCA sobrescrevendo linhas com status='dismissed' (filtrar candidatos/usar update condicional `WHERE status != 'dismissed'`); (f) auto-resolve: `update insights set status='resolved', resolved_at=now() where organization_id=orgId and status='active' and rule_key NOT IN (activeRuleKeys) and ml_user_id_key ...` (não tocar dismissed); (g) upsert snapshot em consultor_health_snapshots `onConflict: 'organization_id,snapshot_month'` com snapshot_month = YYYY-MM corrente, score + por-pilar + insights_total/critical; (h) retornar { insights_count, score }.
    - config.toml: adicionar bloco `[functions.consultor-insights]` com `verify_jwt = false`.
    Manter a EF em um único arquivo; pode dividir a lógica em funções internas. Comentar no topo a estratégia de auth dual e os pitfalls aplicados.
  </action>
  <verify>
    <automated>test -f supabase/functions/consultor-insights/index.ts && grep -q "runConsultorForOrg" supabase/functions/consultor-insights/index.ts && grep -q "function authenticate" supabase/functions/consultor-insights/index.ts && grep -q "consultor-insights" supabase/config.toml && grep -q "verify_jwt = false" supabase/config.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `authenticate()` distingue service role (userId null) de user JWT (userId string) e rejeita não-autorizado com 401.
    - mode org_only valida `is_org_member` e retorna 403 para org alheia.
    - Loop por org tem try/catch por org (error isolation).
    - Upsert usa onConflict org+rule_key+ml_user_id_key e NÃO sobrescreve status='dismissed'.
    - Auto-resolve marca status='resolved' para regras que não dispararam, sem tocar dismissed.
    - Snapshot é upsert por (organization_id, snapshot_month).
    - config.toml tem `[functions.consultor-insights]` verify_jwt = false.
  </acceptance_criteria>
  <done>EF com auth dual, loop por org, upsert idempotente, auto-resolve e snapshot escrita; config.toml atualizado.</done>
</task>

<task type="auto">
  <name>Task 2: As 12 regras + cálculo de score por pilar + templates de texto e impacto R$</name>
  <files>supabase/functions/consultor-insights/index.ts</files>
  <read_first>
    - supabase/functions/consultor-insights/index.ts (o arquivo da Task 1 — adicionar as regras dentro de runConsultorForOrg)
    - .planning/phases/45-consultor-v1/45-RESEARCH.md §"Fontes de Dados por Regra" (as 12 queries), §"Templates de Texto por Regra" (tabela rule_key→title/body/action), §"Cálculo de Impacto R$ Detalhado", §"Score de Saúde 0-100 por Pilar"
    - supabase/migrations/20260645010000_consultor_engine_rpcs.sql (assinaturas das RPCs get_consultor_* do Plan 01)
  </read_first>
  <action>
    Implementar as 12 regras (D-01) dentro de runConsultorForOrg, cada uma produzindo zero ou mais insight candidatos com os campos: rule_key, category, severity, title, body, action_label, action_href, impact_brl (ou null), ml_user_id (ou null = org), ml_user_id_key. Impacto R$ (D-13: fórmula específica por regra onde há R$ óbvio — margem perdida × volume, spend desperdiçado, venda perdida por ruptura = giro × preço; impact_brl=null/qualitativo onde não há, ex: pergunta/reputação; D-15: período base = projeção mensal, extrapolar para 30d por regra). Usar os templates do RESEARCH §Templates (variáveis substituídas: {{N}}, {{impact}}, {{lista}}, {{spend}}, {{atual}}, {{meta}}, {{projecao}}, {{dias}}). CORRIGIR os deep-links (action_href) para as ROTAS REAIS deste projeto (descoberta desta fase, divergem do RESEARCH): produto/custos → `/precificacao` (NÃO `/precos-custos`); fiscal → `/fiscal` (NÃO `/organizacao`); ads → `/publicidade`; estoque → `/estoque`; anúncios → `/anuncios`; perguntas → `/perguntas`; devoluções → `/devolucoes`; metas → `/metas`; vendas → `/`.

    Regras e fontes (RESEARCH §Fontes):
    1. margin_critical (lucro_pct ≤ config.margin_critical_pct) e 2. margin_alert (≤ margin_alert_pct) — via RPC `get_consultor_margin_by_product` (janela 30d). severity critical/high. impact = SUM(ABS(lucro))×(30/dias) (critical) / SUM((meta-lucro_pct)×receita/100)×(30/dias) (alert).
    3. ads_no_sale — `ml_ads_daily_cache` SUM(spend)>0 AND SUM(attributed_orders)=0 nos últimos config.ads_no_sale_days (7). severity high. impact = SUM(spend)×(30/7). Texto deve dizer "nível de org" (Pitfall 5).
    4. tacos_high — SUM(spend de ml_ads_daily_cache) / SUM(orders.receita_bruta) ×100 > config.tacos_alert_pct (15) no mês. severity medium. impact = excesso vs meta ×(30/dias).
    5. acos_high / roas_low — ACoS = spend/attributed_revenue×100 > config.acos_alert_pct; ROAS < config.roas_min. severity medium. (entra via D-01; pode emitir como rule_key `ads_efficiency`.)
    6. stock_critical (coverage_days < config.stock_critical_days, 7) e 7. stock_alert (< stock_alert_days, 15) — via RPC `get_consultor_coverage`. impact = avg_daily×preço×(7-coverage_days). severity critical/high.
    8. no_cost — via RPC `get_consultor_no_cost_count` > 0. severity medium. impact null (qualitativo).
    9. no_fiscal — COUNT(ml_tax_config WHERE organization_id=org)=0. severity high. impact null.
    10. ticket_drop — orders ticket médio mês atual vs anterior; dispara se queda > config.ticket_drop_pct (10%). impact = (ticket_ant-ticket_atual)×pedidos_mes. severity medium.
    11. claims_spike — ml_claims COUNT mês atual vs anterior; dispara se > config.claims_spike_pct (20%). severity medium. impact null.
    12. paused_with_sales — via RPC `get_consultor_paused_with_sales` (config.paused_ads_lookback_days, 30). impact = vendas_30d×preço. severity high.
    13. goal_at_risk — join ml_targets via ml_tokens (Pitfall 6); run-rate projeção; dispara se projecao < meta×(1 - goal_risk_pct/100). FALLBACK: se não houver meta para a org, NÃO emitir (sem insight). impact = meta - projecao. severity high.
    14. questions_old — ml_questions status='UNANSWERED' AND data_pergunta ≤ now()-24h. severity critical se >5, senão high. impact null.
    (As 12 "regras candidatas" do CONSUL mapeiam para esses rule_keys; ROAS/ACoS é o item extra de D-01.)

    Score por pilar (RESEARCH §Score, D-09): implementar nota_margem (peso 30), nota_ads (25), nota_estoque (20), nota_reputacao (15, via proxy ml_claims cancellation_rate — sem chamar API ML, conforme recomendação concreta do RESEARCH), nota_completude (10, via onboarding_progress completed_steps ∩ ['connect_ml','costs','fiscal'] / 3 × 100 — D-11: reusa onboarding_progress, zero infra nova). Cada nota 0-100, clamp. score final = Σ(nota_pilar × peso/100), arredondado para integer. Gravar por-pilar no snapshot.

    Severidades = critical|high|medium (D-21); categorias = 'Margem'|'Ads'|'Estoque'|'Reputação'|'Config'|'Vendas' (D-21).
    Para insights por loja (ex: margem por produto numa loja específica), preencher ml_user_id e prefixar o título com a loja quando a org tiver >1 ml_user_id (D-23).
  </action>
  <verify>
    <automated>grep -oE "rule_key:\s*[\"'][a-z_]+" supabase/functions/consultor-insights/index.ts | sort -u | wc -l | awk '{print ($1>=10)?"OK "$1:"FAIL "$1}' && grep -q "get_consultor_margin_by_product" supabase/functions/consultor-insights/index.ts && grep -q "score" supabase/functions/consultor-insights/index.ts && grep -q "/precificacao" supabase/functions/consultor-insights/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - ≥ 10 rule_keys distintos implementados (cobrindo as 12 regras candidatas + ROAS/ACoS).
    - Regras de margem/cobertura/pausados/sem-custo usam as RPCs `get_consultor_*` (não `.from().select()` em tabela grande).
    - goal_at_risk faz join via ml_tokens e NÃO dispara se a org não tem meta.
    - tacos/ticket usam `orders.receita_bruta` como receita org-scoped.
    - Deep-links usam rotas reais (`/precificacao`, `/fiscal`, `/publicidade`, etc.).
    - Score final é Σ ponderado (30/25/20/15/10) e cada pilar é gravado no snapshot.
    - Pilar Reputação usa proxy via ml_claims (sem chamada síncrona à API ML).
  </acceptance_criteria>
  <done>As 12 regras + score por pilar + templates + impacto R$ implementados na EF.</done>
</task>

<task type="auto">
  <name>Task 3: Migration pg_cron Pattern B (diário 08:30 UTC)</name>
  <files>supabase/migrations/20260645020000_pg_cron_consultor.sql</files>
  <read_first>
    - supabase/migrations/20260614110000_pg_cron_questions_claims.sql (analog EXATO Pattern B: unschedule idempotente + cron.schedule + net.http_post + vault decrypted_secret service_role_key)
    - .planning/phases/45-consultor-v1/45-PATTERNS.md §"pg_cron Pattern B"
  </read_first>
  <action>
    Criar a migration de cron seguindo o analog: bloco `DO $$ ... PERFORM cron.unschedule('consultor-insights-daily'); EXCEPTION WHEN OTHERS THEN NULL; END $$;` seguido de `SELECT cron.schedule('consultor-insights-daily', '30 8 * * *', $cmd$ ... $cmd$)`. Dentro do comando: `net.http_post` com url `https://ckcdevcxgvueywivefgx.supabase.co/functions/v1/consultor-insights`, header Authorization `'Bearer ' || (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name='service_role_key' LIMIT 1)`, body `'{"mode":"all_orgs"}'::jsonb`. Horário 08:30 UTC (após sync 07:03 + scores 07:30, D-20). Comentário `--` no topo explicando Pattern B e o horário.
  </action>
  <verify>
    <automated>grep -q "cron.schedule" supabase/migrations/20260645020000_pg_cron_consultor.sql && grep -q "consultor-insights-daily" supabase/migrations/20260645020000_pg_cron_consultor.sql && grep -q "service_role_key" supabase/migrations/20260645020000_pg_cron_consultor.sql && grep -q "all_orgs" supabase/migrations/20260645020000_pg_cron_consultor.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - unschedule idempotente antes do schedule.
    - cron '30 8 * * *' invoca a EF via net.http_post com vault `service_role_key`.
    - body `{"mode":"all_orgs"}`.
  </acceptance_criteria>
  <done>Migration de cron escrita seguindo Pattern B.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 [BLOCKING]: Deploy da EF + apply do cron + smoke (CONSUL-05) via MCP</name>
  <what-built>EF consultor-insights (auth dual, 12 regras, score, snapshot) + config.toml + migration de cron. Nada foi deployado/aplicado — o gsd-executor não tem MCP/deploy.</what-built>
  <how-to-verify>
    O ORQUESTRADOR, via Supabase MCP no projeto **`ckcdevcxgvueywivefgx`**:
    1. `mcp__supabase__deploy_edge_function` para `consultor-insights` (verify_jwt=false). Confirmar ACTIVE.
    2. Confirmar vault: `SELECT name FROM vault.secrets WHERE name='service_role_key';` → 1 linha (Pattern B pré-requisito). Se ausente, parar e sinalizar Wesley.
    3. `mcp__supabase__apply_migration` de `20260645020000_pg_cron_consultor.sql`. Validar: `SELECT jobname, schedule FROM cron.job WHERE jobname='consultor-insights-daily';`.
    4. SMOKE / CONSUL-05: invocar a EF para a org Pé Vermeio (`7f615df7-...`) em mode all_orgs ou via net.http_post com service_role. Depois:
       - `SELECT COUNT(*) FROM insights WHERE organization_id='7f615df7-...' AND status='active';` → **≥ 5** (meta CONSUL-05).
       - `SELECT rule_key, severity, impact_brl FROM insights WHERE organization_id='7f615df7-...' AND status='active' ORDER BY severity, impact_brl DESC NULLS LAST;` → conferir variedade de regras e textos coerentes.
       - `SELECT score, score_margin, score_ads, score_estoque, score_reputacao, score_completude, snapshot_month FROM consultor_health_snapshots WHERE organization_id='7f615df7-...' ORDER BY created_at DESC LIMIT 1;` → score 0-100 e pilares preenchidos.
    5. Idempotência: invocar a EF de novo e confirmar que o COUNT de active NÃO duplica (upsert por rule_key).
    6. `mcp__supabase__get_logs` (edge function) sem erro.
  </how-to-verify>
  <resume-signal>Digite "deployado" após EF ACTIVE + cron agendado + ≥5 insights ativos + score gravado na Pé Vermeio. Se &lt;5 insights, descreva quais regras dispararam para ajuste de limiares.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron / frontend → EF consultor-insights | EF tem verify_jwt=false; precisa distinguir service role (cron) de user JWT (on-demand) e rejeitar anônimo |
| user JWT (on-demand) → org recalc | Usuário só pode disparar recalc da própria org |
| EF (service role) → tabelas insights/snapshots | Escrita irrestrita; controlada pela lógica da EF (dedup, não tocar dismissed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06 | Elevation of Privilege | invocação anônima da EF (verify_jwt=false) | mitigate | `authenticate()` exige Bearer service-role-key OU JWT válido; senão 401 (Task 1) |
| T-45-07 | Information Disclosure / Tampering | on-demand recalcula/lê org alheia via JWT | mitigate | mode org_only valida `is_org_member(userId, orgId)` → 403 se não-membro (Task 1) |
| T-45-08 | Tampering | re-ativação de insight dismissed pelo engine | mitigate | upsert/auto-resolve com cláusula `WHERE status != 'dismissed'` (Task 1/2) |
| T-45-09 | Information Disclosure | ml_user_id enumeration na EF | mitigate | ml_tokens lookup sempre filtrado por organization_id da org em escopo; mode org_only restrito ao membro (Task 1) |
| T-45-10 | Denial of Service | timeout 150s em all_orgs | mitigate | orgs serializadas + error isolation por org (Task 1); cron diário fora de pico (Task 3) |
| T-45-SC | Tampering | supply-chain de pacotes | accept | EF importa só deno std + supabase-js já usados no projeto; zero pacote novo |
</threat_model>

<verification>
- EF ACTIVE no projeto correto; cron agendado.
- ≥5 insights ativos + score gravado para a Pé Vermeio (CONSUL-05).
- Reinvocação não duplica insights (idempotência).
- get_logs sem erro.
</verification>

<success_criteria>
- Engine roda por org (cron + on-demand), 12 regras avaliadas, insights + score persistidos.
- Auth dual e isolamento org confirmados (T-45-06..09 mitigados).
- Meta CONSUL-05 atingida no smoke.
</success_criteria>

<output>
Create `.planning/phases/45-consultor-v1/45-02-SUMMARY.md` when done
</output>
