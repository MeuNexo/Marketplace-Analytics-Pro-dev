---
phase: 45-consultor-v1
plan: 03
type: execute
wave: 3
depends_on: ["45-01", "45-02"]
files_modified:
  - src/hooks/useConsultorInsights.ts
  - src/components/mercadolivre/ConsultorCard.tsx
  - src/pages/mercadolivre/MLConsultor.tsx
  - src/pages/MercadoLivre.tsx
  - src/App.tsx
  - src/components/layout/ApiSidebar.tsx
  - src/components/layout/routeMeta.ts
autonomous: false   # contém checkpoint:human-verify (visual Wesley)
requirements: [CONSUL-02, CONSUL-03, CONSUL-04]
must_haves:
  truths:
    - "Card 'O que fazer agora' aparece no topo de /vendas com score de saúde colorido + Top 3 insights em linguagem leiga (D-16)"
    - "Hook useConsultorInsights lê insights+score por org via RLS, dispara on-demand quando vazio (D-20), e permite dismiss (D-18)"
    - "Página /consultor lista todos os insights com 'por que importa'/'como resolver' + deep-link filtrado por categoria (D-03/D-19)"
    - "Insights ordenados por severidade depois impacto R$ (D-17); score mostra faixa/cor (D-10) e tendência ▲/▼ vs mês anterior (D-12)"
  artifacts:
    - path: "src/hooks/useConsultorInsights.ts"
      provides: "Leitura org-scoped de insights + snapshots (score atual+anterior) + on-demand invoke + dismiss mutation"
      exports: ["useConsultorInsights"]
      min_lines: 60
    - path: "src/components/mercadolivre/ConsultorCard.tsx"
      provides: "Card top-3 + score de saúde para /vendas"
      exports: ["ConsultorCard"]
    - path: "src/pages/mercadolivre/MLConsultor.tsx"
      provides: "Painel completo /consultor com explicação por insight + dismiss + deep-links"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "ConsultorCard"
      via: "render após OnboardingBanner"
      pattern: "ConsultorCard"
    - from: "src/hooks/useConsultorInsights.ts"
      to: "insights / consultor_health_snapshots"
      via: "supabase.from(...).eq(organization_id)"
      pattern: "from\\([\"']insights"
    - from: "src/App.tsx"
      to: "MLConsultor"
      via: "Route path=/consultor"
      pattern: "/consultor"
---

<objective>
Expor o Consultor v1 na UI: hook `useConsultorInsights` (leitura org-scoped + on-demand quando vazio + dismiss), o card "O que fazer agora" com score de saúde e Top 3 insights no topo de `/vendas`, e a página/painel `/consultor` com a lista completa e explicação leiga por insight + deep-links filtrados. Registrar rota e item de sidebar.

Purpose: Sem a UI, o engine (Plan 02) grava insights que ninguém vê. Este plano entrega CONSUL-02 (card), CONSUL-03 (painel com explicação) e a exibição de CONSUL-04 (score visível).
Output: hook + 2 componentes/páginas + 4 wirings (página /vendas, App route, sidebar, routeMeta).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-consultor-v1/45-CONTEXT.md
@.planning/phases/45-consultor-v1/45-RESEARCH.md
@.planning/phases/45-consultor-v1/45-PATTERNS.md
@src/hooks/useMLBilling.ts
@src/hooks/useMLQuestions.ts
@src/hooks/useOnboardingProgress.ts
@src/components/onboarding/OnboardingBanner.tsx
@src/components/dashboard/KPICard.tsx
@.planning/phases/45-consultor-v1/45-01-SUMMARY.md
@.planning/phases/45-consultor-v1/45-02-SUMMARY.md
</context>

<critical_constraints>
- **Zero dependência nova.** Reutilizar shadcn/ui (Card, Badge, Button, Sheet), lucide-react, TanStack Query v5, tokens `kpi.positive/negative/neutral`.
- **Rotas reais deste projeto** (verificado em src/App.tsx + ApiSidebar.tsx): custos → `/precificacao`; fiscal → `/fiscal`; ads → `/publicidade`; estoque → `/estoque`; anúncios → `/anuncios`; perguntas → `/perguntas`; devoluções → `/devolucoes`; metas → `/metas`; vendas → `/`. O `action_href` já vem do engine (Plan 02) — o frontend só renderiza via `<Link to={insight.action_href}>`.
- Card só aparece quando onboarding completo: `{onboardingComplete && <ConsultorCard .../>}`, inserido APÓS `<OnboardingBanner>` e ANTES de `<MLPeriodPicker>` em MercadoLivre.tsx (ponto verificado).
- Página `/consultor` segue o padrão de lazy import + `<Route>` dentro da árvore `RoleRoute`/`ErrorBoundary` de App.tsx; sidebar via `children` em ApiSidebar.tsx; subtítulo em routeMeta.ts.
- Ordenação D-17: severidade (critical → high → medium) e depois impact_brl desc (nulls last). Mapear severity→rank no client para garantir a ordem independente da string; o card pega os 3 primeiros.
</critical_constraints>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hook useConsultorInsights (leitura + score + tendência + on-demand + dismiss)</name>
  <files>src/hooks/useConsultorInsights.ts</files>
  <read_first>
    - src/hooks/useMLBilling.ts (analog EXATO de on-demand invoke quando vazio: useMLBillingWithSync, linhas ~213-255)
    - src/hooks/useMLQuestions.ts (analog de useQuery org-scoped + staleTime)
    - src/hooks/useOnboardingProgress.ts (analog de mutation com invalidateQueries — para o dismiss)
    - src/integrations/supabase/types.ts (tipos das tabelas insights/consultor_health_snapshots do Plan 01)
    - .planning/phases/45-consultor-v1/45-PATTERNS.md §"useConsultorInsights.ts"
  </read_first>
  <action>
    Criar `src/hooks/useConsultorInsights.ts` exportando `useConsultorInsights()` e os tipos `InsightRow` e `HealthScore`.
    - useQuery `["consultor_insights", orgId]`, enabled !!orgId, staleTime 4h: `supabase.from('insights').select('*').eq('organization_id', orgId).in('status', ['active'])`. Reordenar no client por severity-rank (critical=0, high=1, medium=2) e depois impact_brl desc (nulls last) via useMemo, para garantir D-17 independente da ordenação textual.
    - useQuery `["consultor_score", orgId]`: ler as 2 linhas mais recentes de `consultor_health_snapshots` (`.order('snapshot_month',{ascending:false}).limit(2)`) → expor score atual + delta vs mês anterior (▲/▼, D-12) + breakdown por pilar (score_margin/score_ads/score_estoque/score_reputacao/score_completude).
    - on-demand: padrão useMLBillingWithSync — se !isLoading && (data?.length===0) && !attempted(orgId): `supabase.functions.invoke('consultor-insights', { body:{ mode:'org_only' } })` então refetch; expor `syncing`; liberar retry em erro de rede. (D-20.)
    - dismiss mutation: `supabase.from('insights').update({ status:'dismissed', dismissed_at: new Date().toISOString() }).eq('id', id).eq('organization_id', orgId)`, onSuccess invalidateQueries(['consultor_insights', orgId]). (D-18.)
    - Retornar { insights, score, scoreDelta, scoreBand, pillars, loading, syncing, dismiss }. scoreBand derivado: ≥75 'healthy', ≥50 'attention', else 'critical' (D-10).
  </action>
  <verify>
    <automated>test -f src/hooks/useConsultorInsights.ts && grep -q "useConsultorInsights" src/hooks/useConsultorInsights.ts && grep -q "consultor-insights" src/hooks/useConsultorInsights.ts && grep -q "dismissed" src/hooks/useConsultorInsights.ts && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Hook lê insights ativos org-scoped, ordenados severidade→impacto (D-17).
    - Lê snapshot atual + anterior e expõe score + delta + band + pilares (D-10/D-12).
    - On-demand invoke quando vazio com guard de tentativa por org (D-20).
    - dismiss faz update status='dismissed' org-scoped e invalida a query (D-18).
    - `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>Hook completo e tipado; tsc limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ConsultorCard no topo de /vendas (score + Top 3) + wiring em MercadoLivre.tsx</name>
  <files>src/components/mercadolivre/ConsultorCard.tsx, src/pages/MercadoLivre.tsx</files>
  <read_first>
    - src/components/onboarding/OnboardingBanner.tsx (analog de Card no topo de /vendas + estrutura CardContent)
    - src/components/dashboard/KPICard.tsx (analog de variantes de cor success/warning/danger por tokens kpi.*)
    - src/pages/MercadoLivre.tsx (ponto de inserção: após <OnboardingBanner>, antes de <MLPeriodPicker>; var onboardingComplete já existe linha ~98)
    - .planning/phases/45-consultor-v1/45-PATTERNS.md §"ConsultorCard.tsx" e §"Cores do score"
  </read_first>
  <action>
    Criar `ConsultorCard.tsx` exportando `ConsultorCard`. Props: { insights, score, scoreDelta, scoreBand, syncing, onDismiss }. Renderizar:
    - Linha de score: ícone Activity + "Saúde do negócio" + Badge com `{score} — {label}` onde label = Saudável/Atenção/Crítico e cor pela band (≥75 token kpi.positive/success, ≥50 kpi.neutral/warning, <50 kpi.negative/destructive — D-10). Mostrar seta ▲/▼ com scoreDelta (D-12).
    - Top 3 insights (D-16): para cada um, ícone por severity (critical→XCircle text-destructive; high→AlertTriangle text-warning; medium→Info text-muted-foreground), title leigo, impact_brl formatado como "Você está perdendo ~R$ X/mês" quando não-nulo (framing perda, D-14), botão dismiss (X) chamando onDismiss(id), e o título/linha clicável via `<Link to={insight.action_href}>` (deep-link filtrado, D-19).
    - Botão "Ver todos →" `<Link to="/consultor">`.
    - Estado de syncing: skeleton/spinner discreto. Estado sem insights ativos: mensagem positiva curta ("Tudo certo por aqui") — manter o score visível, não renderizar lista vazia ruidosa.
    Em MercadoLivre.tsx: importar ConsultorCard e useConsultorInsights; chamar o hook; inserir `{onboardingComplete && <ConsultorCard insights={...} score={...} scoreDelta={...} scoreBand={...} syncing={...} onDismiss={dismiss} />}` exatamente após `<OnboardingBanner .../>` e antes de `<MLPeriodPicker ...>`. Não alterar outra lógica da página.
  </action>
  <verify>
    <automated>test -f src/components/mercadolivre/ConsultorCard.tsx && grep -q "ConsultorCard" src/components/mercadolivre/ConsultorCard.tsx && grep -q "ConsultorCard" src/pages/MercadoLivre.tsx && grep -q "useConsultorInsights" src/pages/MercadoLivre.tsx && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Card mostra score com cor/label por faixa (D-10) e tendência (D-12).
    - Lista no máximo 3 insights com ícone por severidade, framing de perda R$ (D-14), dismiss e deep-link (D-19).
    - "Ver todos" linka para /consultor.
    - Inserido após OnboardingBanner, antes do MLPeriodPicker, condicionado a onboardingComplete.
    - tsc limpo.
  </acceptance_criteria>
  <done>ConsultorCard renderiza no topo de /vendas com dados reais do hook.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Página /consultor (painel completo) + rota + sidebar + routeMeta</name>
  <files>src/pages/mercadolivre/MLConsultor.tsx, src/App.tsx, src/components/layout/ApiSidebar.tsx, src/components/layout/routeMeta.ts</files>
  <read_first>
    - src/pages/MercadoLivre.tsx (estrutura de página com hook + render condicional + MLPageHeader)
    - src/App.tsx (linhas ~25-46 lazy imports; linhas ~122-127 padrão de <Route> dentro de RoleRoute/ErrorBoundary)
    - src/components/layout/ApiSidebar.tsx (estrutura children dos sections — adicionar item Consultor)
    - src/components/layout/routeMeta.ts (formato { "/path": { title, subtitle } })
    - .planning/phases/45-consultor-v1/45-PATTERNS.md §"MLConsultor.tsx"
  </read_first>
  <action>
    Criar `src/pages/mercadolivre/MLConsultor.tsx` (default export) usando `useConsultorInsights`. Layout:
    - Cabeçalho com score de saúde grande (band/cor D-10) + breakdown por pilar (Margem/Ads/Estoque/Reputação/Completude) lendo os campos `pillars` do hook; se ausentes, mostrar só o score total.
    - Lista de TODOS os insights ativos ordenados por severidade→impacto. Cada card de insight mostra: severity badge, title, body (explicação leiga = "por que isso importa", CONSUL-03), impact_brl com framing de perda (D-14), botão "como resolver" = `<Link to={insight.action_href}>{insight.action_label}</Link>` (deep-link filtrado, D-19), e botão dispensar (dismiss). Estado vazio acionável ("Nenhum alerta no momento").
    - Estado de loading/syncing com skeleton.
    Registrar a rota em App.tsx: adicionar `const MLConsultor = React.lazy(() => import("./pages/mercadolivre/MLConsultor"));` junto aos outros e `<Route path="/consultor" element={<RoleRoute><ErrorBoundary fallbackTitle="Erro no Consultor"><MLConsultor /></ErrorBoundary></RoleRoute>} />` dentro da mesma árvore das demais páginas ML (ALL roles, como /reputacao).
    Adicionar item de sidebar em ApiSidebar.tsx: no section "Dashboard", `{ icon: Lightbulb, label: "Consultor", path: "/consultor" }` (importar `Lightbulb` de lucide-react). Posicionar logo após "Vendas".
    Adicionar em routeMeta.ts: `"/consultor": { title: "Consultor", subtitle: "O que fazer agora — alertas e saúde do negócio" }`.
  </action>
  <verify>
    <automated>test -f src/pages/mercadolivre/MLConsultor.tsx && grep -q "/consultor" src/App.tsx && grep -q "MLConsultor" src/App.tsx && grep -q "/consultor" src/components/layout/ApiSidebar.tsx && grep -q "/consultor" src/components/layout/routeMeta.ts && npx tsc --noEmit 2>&1 | tail -3 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MLConsultor.tsx lista TODOS os insights ativos com title + body (por que importa) + action link (como resolver) + dismiss (CONSUL-03/D-19).
    - Score grande com faixa/cor (D-10) no cabeçalho do painel.
    - Rota /consultor registrada em App.tsx dentro de RoleRoute/ErrorBoundary (ALL roles).
    - Item "Consultor" na sidebar e subtítulo em routeMeta.ts.
    - `npx tsc --noEmit` e `npm run build` limpos.
  </acceptance_criteria>
  <done>Página /consultor acessível via rota e sidebar, listando insights com explicação leiga e deep-links; build limpo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificação visual do Consultor v1 na UI (Wesley)</name>
  <what-built>Card "O que fazer agora" + score de saúde no topo de /vendas, e painel completo em /consultor com explicação leiga por insight + deep-links. Dados reais vindos do engine (Plan 02) para a Pé Vermeio.</what-built>
  <how-to-verify>
    1. Abrir `/` (Vendas) logado na org Pé Vermeio com onboarding completo. Confirmar que o card "Saúde do negócio" aparece logo abaixo do banner de onboarding e acima do seletor de período, com:
       - Score 0-100 colorido (verde/amarelo/vermelho) + rótulo (Saudável/Atenção/Crítico) + seta de tendência.
       - Até 3 insights em linguagem leiga, com "Você está perdendo ~R$ X/mês" onde aplicável.
    2. Clicar num insight → confirmar que leva à página certa já filtrada (ex: Ads → /publicidade).
    3. Clicar "Ver todos" → abrir /consultor; confirmar lista completa com "por que importa" + "como resolver" + botão dispensar.
    4. Dispensar um insight → confirmar que ele some da lista e do card (não reaparece no reload).
    5. Conferir que há ≥5 insights reais (CONSUL-05) e que os textos fazem sentido para um lojista leigo.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva ajustes de texto/layout/limiares.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (authenticated) → insights | leitura e dismiss só da própria org (RLS do Plan 01) |
| browser → EF consultor-insights (on-demand) | invocação com user JWT; EF valida is_org_member (Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-11 | Information Disclosure | hook ler insight de outra org | mitigate | query sempre `.eq('organization_id', orgId)` + RLS `is_org_member` (Plan 01) garante no servidor mesmo se o client errar |
| T-45-12 | Tampering | dismiss de insight de outra org via client forjado | mitigate | RLS UPDATE policy `is_org_member` no servidor (Plan 01); client filtra por org |
| T-45-13 | Information Disclosure | deep-link expõe dado sensível | accept | links apontam para rotas internas já protegidas por RoleRoute; sem segredo no href |
| T-45-SC | Tampering | supply-chain de pacotes | accept | Nenhuma dependência nova — só shadcn/lucide/query já presentes |
</threat_model>

<verification>
- `npx tsc --noEmit` e `npm run build` limpos.
- Card visível no topo de /vendas; /consultor acessível por rota e sidebar.
- Insights ordenados severidade→impacto; dismiss persiste; score com faixa/cor + tendência.
- Checkpoint visual aprovado por Wesley (≥5 insights reais, textos leigos).
</verification>

<success_criteria>
- CONSUL-02 (card top-3), CONSUL-03 (painel com explicação) e exibição de CONSUL-04 (score visível) entregues.
- Isolamento org confirmado pela RLS do Plan 01 (T-45-11/12 mitigados).
</success_criteria>

<output>
Create `.planning/phases/45-consultor-v1/45-03-SUMMARY.md` when done
</output>
