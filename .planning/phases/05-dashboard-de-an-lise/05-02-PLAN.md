---
phase: 05-dashboard-de-an-lise
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/mercadolivre/analise/AnalysisProductCard.tsx
  - src/components/mercadolivre/analise/AnalisePrecosTable.tsx
  - src/components/mercadolivre/analise/AnaliseDashboard.tsx
  - src/pages/mercadolivre/MLPrecificacao.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "O usuário vê um card por produto analisado com três preços (GMV, Neutro, Margem) e a frase de elasticidade"
    - "O usuário vê uma tabela com colunas Produto, Marca, Preço GMV, Preço Neutro, Preço Margem, Impacto Comercial e Estratégia"
    - "O usuário escolhe Estratégia por linha em um dropdown; a célula de preço correspondente é destacada visualmente"
    - "Mudar Estratégia persiste no Supabase via updateStrategy(snapshotId, strategy)"
    - "Clicar 'Analisar' busca pedidos, calcula um snapshot e adiciona-o à lista exibida"
  artifacts:
    - path: "src/components/mercadolivre/analise/AnalysisProductCard.tsx"
      provides: "Card por produto com 3 preços + badge de classe + frase de elasticidade (DASH-01)"
      exports: ["AnalysisProductCard"]
    - path: "src/components/mercadolivre/analise/AnalisePrecosTable.tsx"
      provides: "Tabela interativa com destaque de coluna por estratégia + Select por linha (DASH-02, DASH-03)"
      exports: ["AnalisePrecosTable"]
    - path: "src/components/mercadolivre/analise/AnaliseDashboard.tsx"
      provides: "Orquestrador: controles, fetch, save, lista de snapshots, render de card + tabela"
      exports: ["AnaliseDashboard"]
    - path: "src/pages/mercadolivre/MLPrecificacao.tsx"
      provides: "Substitui placeholder pela montagem real de AnaliseDashboard"
      contains: "AnaliseDashboard"
  key_links:
    - from: "AnaliseDashboard"
      to: "useMLOrdersByItem.fetchOrders"
      via: "chamada na ação 'Analisar' antes de saveSnapshot"
      pattern: "fetchOrders\\("
    - from: "AnaliseDashboard"
      to: "useAnalysisSnapshots.saveSnapshot"
      via: "input com orders + periodDays + periodStart/End + mlUserId + orgId + itemId + productTitle"
      pattern: "saveSnapshot\\("
    - from: "AnalisePrecosTable Select"
      to: "useAnalysisSnapshots.updateStrategy"
      via: "onValueChange → onStrategyChange prop → updateStrategy"
      pattern: "updateStrategy\\("
    - from: "AnalisePrecosTable cells"
      to: "snapshot.strategy"
      via: "className condicional aplicando bg + ring por coluna ativa"
      pattern: "snapshot\\.strategy"
---

<objective>
Entregar o Dashboard de Análise completo: card de produto, tabela interativa com seleção de estratégia e destaque visual, integrado dentro da aba 'Análise' criada em 05-01.

Propósito: cumprir DASH-01 (card com 3 preços + frase de elasticidade), DASH-02 (tabela com colunas Produto/Marca/Preços/Impacto Comercial) e DASH-03 (dropdown de estratégia por linha + highlight visual da coluna correspondente). Esta entrega fecha o objetivo do Fase 5.

Saída: três novos componentes em `src/components/mercadolivre/analise/` e substituição do placeholder em `MLPrecificacao.tsx` por `<AnaliseDashboard />`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-de-an-lise/05-RESEARCH.md
@.planning/phases/05-dashboard-de-an-lise/05-01-SUMMARY.md

@src/pages/mercadolivre/MLPrecificacao.tsx
@src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx
@src/hooks/useMLOrdersByItem.ts
@src/hooks/useAnalysisSnapshots.ts
@src/hooks/useMLPrecosCustos.ts
@src/lib/analysis/types.ts
@src/lib/pricing/calculator.ts
@src/components/ui/table.tsx
@src/components/ui/select.tsx
@src/components/ui/badge.tsx
@src/components/ui/card.tsx
@src/components/ui/skeleton.tsx
@src/lib/utils.ts

<interfaces>
<!-- Contratos consumidos (de 05-01 e Fase 4) -->

useMLOrdersByItem (de 05-01):
- fetchOrders(itemId, orgId, mlUserId, dateFrom, dateTo) → Promise<OrderRecord[]>
- loading: boolean

useAnalysisSnapshots (de Fase 4):
- saveSnapshot(input: SnapshotInput) → Promise<AnalysisSnapshot>
- fetchSnapshots(itemId, orgId) → Promise<AnalysisSnapshot[]>
- updateStrategy(snapshotId, strategy: 'gmv'|'neutral'|'margin') → Promise<void>
- saving, loading: boolean

AnalysisSnapshot (de src/hooks/useAnalysisSnapshots.ts):
- { id, createdAt, mlUserId, organizationId, itemId, productTitle, brand,
    periodStart, periodEnd, priceCurve, priceGmv, priceNeutral, priceMargin,
    elasticityPct, elasticityClass, strategy }

formatBRL (de src/lib/pricing/calculator.ts):
- formatBRL(value: number) → string  // "R$ 12,34"

<!-- Contratos PRODUZIDOS por este plano -->

AnalysisProductCard props:
- { snapshot: AnalysisSnapshot }

AnalisePrecosTable props:
- {
    snapshots: AnalysisSnapshot[];
    onStrategyChange: (id: string, strategy: 'gmv'|'neutral'|'margin') => void;
  }

AnaliseDashboard props:
- (sem props — lê orgId e mlUserId via mesmos contextos usados por SimuladorPrecificacao)

<!-- Mapeamento estratégia → coluna destacada -->

Strategy → coluna destacada → classes Tailwind:
- gmv     → Preço GMV     → bg-emerald-500/10 ring-1 ring-inset ring-emerald-500/30 text-emerald-700 font-semibold
- neutral → Preço Neutro  → bg-blue-500/10    ring-1 ring-inset ring-blue-500/30    text-blue-700    font-semibold
- margin  → Preço Margem  → bg-amber-500/10   ring-1 ring-inset ring-amber-500/30   text-amber-700   font-semibold

<!-- ElasticityClass → Badge -->

- baixa   → Label "Baixa"   → bg-emerald-500/15 text-emerald-700 border-emerald-500/30
- media   → Label "Média"   → bg-blue-500/15    text-blue-700    border-blue-500/30
- alta    → Label "Alta"    → bg-amber-500/15   text-amber-700   border-amber-500/30
- extrema → Label "Extrema" → bg-red-500/15     text-red-700     border-red-500/30

<!-- Frase de elasticidade (DASH-01) -->

"A cada R$1,00 de subida a partir de {formatBRL(priceGmv)}, perde aproximadamente {elasticityPct com 2 decimais e vírgula}% em volume"

<!-- Impacto Comercial (coluna da tabela) -->

Coluna "Impacto Comercial" = Badge da ElasticityClass (mesma cor/label acima).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar AnalysisProductCard (DASH-01)</name>
  <files>src/components/mercadolivre/analise/AnalysisProductCard.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (API de Card, CardHeader, CardContent, CardTitle)
    - src/components/ui/badge.tsx (variantes de Badge)
    - src/hooks/useAnalysisSnapshots.ts (tipo AnalysisSnapshot)
    - src/lib/pricing/calculator.ts (export de formatBRL — confirmar nome exato)
    - src/lib/utils.ts (helper cn)
  </read_first>
  <action>
    Criar `src/components/mercadolivre/analise/AnalysisProductCard.tsx` (per DASH-01).

    1. Imports: Card components de "@/components/ui/card"; Badge de "@/components/ui/badge"; cn de "@/lib/utils"; formatBRL de "@/lib/pricing/calculator"; tipo AnalysisSnapshot de "@/hooks/useAnalysisSnapshots".

    2. Definir e exportar componente `AnalysisProductCard({ snapshot }: { snapshot: AnalysisSnapshot })`.

    3. Estrutura visual:
       - `<Card>` raiz.
       - `<CardHeader>` com `<CardTitle>` exibindo `snapshot.productTitle` e um `<p className="text-sm text-muted-foreground">` exibindo `snapshot.brand ?? "Sem marca"`.
       - `<CardContent>` contendo:
         a) Grid `grid grid-cols-1 md:grid-cols-3 gap-3` com 3 sub-blocos (um por preço). Cada sub-bloco é um `<div className="rounded-md border p-3">` com um label pequeno (`text-xs uppercase text-muted-foreground`) e o valor (`text-lg font-semibold tabular-nums`) formatado por `formatBRL`. Aplicar accent border-left de 3px nas cores:
            - Preço GMV    → `border-l-emerald-500 border-l-4` + texto valor em `text-emerald-700`
            - Preço Neutro → `border-l-blue-500 border-l-4`    + texto valor em `text-blue-700`
            - Preço Margem → `border-l-amber-500 border-l-4`   + texto valor em `text-amber-700`
         b) Bloco de elasticidade abaixo do grid:
            - Badge com label e classes da tabela ElasticityClass→Badge em `<interfaces>` (usar `variant="outline"` + className custom via cn).
            - Parágrafo com a frase exata: `"A cada R$1,00 de subida a partir de {formatBRL(snapshot.priceGmv)}, perde aproximadamente {pct}% em volume"` onde `pct = snapshot.elasticityPct.toFixed(2).replace(".", ",")`.

    4. Sem estado local. Componente puramente apresentacional.

    NÃO inventar variantes novas do Badge — usar `variant="outline"` + className.
  </action>
  <verify>
    <automated>cd C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc && npx tsc --noEmit 2>&1 | grep "AnalysisProductCard" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe e exporta `AnalysisProductCard`.
    - Renderiza três blocos de preço (GMV emerald / Neutro blue / Margem amber) com valores formatados via `formatBRL`.
    - Renderiza Badge da `elasticityClass` com label PT-BR e cor correta.
    - Renderiza a frase de elasticidade exatamente no formato especificado, com porcentagem em duas casas decimais e vírgula como separador decimal.
    - Sem erros TypeScript referentes ao arquivo.
  </acceptance_criteria>
  <done>Card de produto pronto e exportado, consumindo AnalysisSnapshot sem dependência adicional.</done>
</task>

<task type="auto">
  <name>Task 2: Criar AnalisePrecosTable (DASH-02 + DASH-03)</name>
  <files>src/components/mercadolivre/analise/AnalisePrecosTable.tsx</files>
  <read_first>
    - src/components/ui/table.tsx (Table, TableHeader, TableBody, TableRow, TableHead, TableCell)
    - src/components/ui/select.tsx (Select, SelectTrigger, SelectContent, SelectItem, SelectValue)
    - src/components/ui/badge.tsx
    - src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx (padrão de uso de Select — referência)
    - src/hooks/useAnalysisSnapshots.ts (tipo AnalysisSnapshot)
    - src/lib/pricing/calculator.ts (formatBRL)
    - src/lib/utils.ts (cn)
  </read_first>
  <action>
    Criar `src/components/mercadolivre/analise/AnalisePrecosTable.tsx` (per DASH-02 + DASH-03).

    1. Imports: Table/TableHeader/TableBody/TableRow/TableHead/TableCell de "@/components/ui/table"; Select/SelectTrigger/SelectContent/SelectItem/SelectValue de "@/components/ui/select"; Badge; cn; formatBRL; tipo AnalysisSnapshot.

    2. Definir type local `Strategy = 'gmv' | 'neutral' | 'margin'`.

    3. Props: `{ snapshots: AnalysisSnapshot[]; onStrategyChange: (id: string, strategy: Strategy) => void; }`.

    4. Definir constante interna `STRATEGY_CELL_CLASSES: Record<Strategy, string>` com os 3 conjuntos de classes listados em `<interfaces>` (emerald/blue/amber).

    5. Definir constante interna `ELASTICITY_BADGE: Record<ElasticityClass, { label: string; className: string }>` com os 4 itens da tabela ElasticityClass→Badge em `<interfaces>`.

    6. Estrutura visual:
       - `<Table>` com 7 colunas: Produto, Marca, Preço GMV, Preço Neutro, Preço Margem, Impacto Comercial, Estratégia.
       - `<TableHead>` para colunas de preço com `className="text-right"`.
       - Por linha (`snapshots.map(snapshot => ...)`):
         a) Produto: `snapshot.productTitle`.
         b) Marca: `snapshot.brand ?? "—"`.
         c) Preço GMV TableCell: `text-right tabular-nums`. Se `snapshot.strategy === 'gmv'` aplicar também `STRATEGY_CELL_CLASSES.gmv` via `cn`. Renderizar `formatBRL(snapshot.priceGmv)`.
         d) Preço Neutro TableCell: análogo para `neutral`.
         e) Preço Margem TableCell: análogo para `margin`.
         f) Impacto Comercial: Badge `variant="outline"` com label e className de `ELASTICITY_BADGE[snapshot.elasticityClass]`.
         g) Estratégia: `<Select value={snapshot.strategy ?? ""} onValueChange={(v) => onStrategyChange(snapshot.id, v as Strategy)}>` com `<SelectTrigger className="h-8 w-[110px] text-xs">`, placeholder "Estratégia", e três `<SelectItem>` para `gmv`/`neutral`/`margin` com labels `GMV`/`Neutro`/`Margem`.

    7. Empty state: se `snapshots.length === 0`, renderizar uma única linha com `<TableCell colSpan={7}>` exibindo `Nenhum snapshot disponível. Execute uma análise.` em `text-center text-muted-foreground py-6`.

    8. Não chamar `updateStrategy` diretamente — apenas invocar `onStrategyChange` (o orquestrador AnaliseDashboard liga ao hook). Não manter estado local de strategy — `value` lê direto de `snapshot.strategy`.
  </action>
  <verify>
    <automated>cd C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc && npx tsc --noEmit 2>&1 | grep "AnalisePrecosTable" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe e exporta `AnalisePrecosTable`.
    - Tabela com exatamente 7 colunas na ordem especificada.
    - Cada célula de preço aplica condicionalmente as classes de destaque (emerald/blue/amber) quando `snapshot.strategy` aponta para aquela coluna.
    - Coluna Estratégia usa shadcn Select com 3 opções GMV/Neutro/Margem em PT-BR.
    - Coluna Impacto Comercial renderiza Badge colorido por ElasticityClass.
    - Empty state colSpan 7 quando `snapshots` vazio.
    - Sem erros TypeScript referentes ao arquivo.
  </acceptance_criteria>
  <done>Tabela interativa com seleção de estratégia + highlight de coluna ativa, sem persistência interna (delegada ao orquestrador).</done>
</task>

<task type="auto">
  <name>Task 3: Criar AnaliseDashboard orquestrador</name>
  <files>src/components/mercadolivre/analise/AnaliseDashboard.tsx</files>
  <read_first>
    - src/components/mercadolivre/precificacao/SimuladorPrecificacao.tsx (padrão completo: como obter orgId/mlUserId, como usar useMLPrecosCustos para search de produtos, padrão de Card + controles)
    - src/hooks/useMLPrecosCustos.ts (items list + resolveMLUserId)
    - src/hooks/useMLOrdersByItem.ts (criado em 05-01)
    - src/hooks/useAnalysisSnapshots.ts (saveSnapshot + fetchSnapshots + updateStrategy + tipo SnapshotInput)
    - src/components/ui/skeleton.tsx
    - src/components/ui/input.tsx
    - src/components/ui/button.tsx
    - date-fns (usar differenceInCalendarDays para periodDays)
  </read_first>
  <action>
    Criar `src/components/mercadolivre/analise/AnaliseDashboard.tsx` (per DASH-01/02/03 — orquestração).

    1. Imports: useState/useEffect/useCallback de "react"; useMLOrdersByItem; useAnalysisSnapshots + tipos SnapshotInput, AnalysisSnapshot; useMLPrecosCustos (para listar produtos da loja); AnalysisProductCard; AnalisePrecosTable; Card/CardHeader/CardContent/CardTitle; Input; Button; Skeleton; toast de "@/hooks/use-toast" ou caminho equivalente confirmado em SimuladorPrecificacao; differenceInCalendarDays de "date-fns".

    2. Contexto org + store: reproduzir EXATAMENTE o mesmo padrão de obtenção de `orgId` e `mlUserId` que SimuladorPrecificacao usa (ler o arquivo e copiar os hooks de contexto correspondentes — `useOrganization`, `useMLStore`, com fallback para `stores[0].ml_user_id` se `selectedStore === "all"`). Não inventar novos contextos.

    3. Estado local:
       - `dateFrom`, `dateTo`: strings YYYY-MM-DD (default: últimos 30 dias).
       - `itemId`: string.
       - `productTitle`: string (preenchido junto com itemId pelo seletor).
       - `brand`: string | null.
       - `snapshots`: AnalysisSnapshot[].
       - `running`: boolean derivado de `saving || loadingOrders`.

    4. Controles bar (dentro de um `<Card>` no topo):
       - Linha 1: três `<Input type="date">` para dateFrom/dateTo, e um seletor/Input de produto (pode usar `<Input>` simples + um seletor baseado no padrão de useMLPrecosCustos.items — preencher itemId, productTitle, brand a partir do item escolhido). Mantenha alinhamento horizontal com flex-wrap.
       - Linha 2: `<Button onClick={handleAnalyze} disabled={running || !itemId}>Analisar</Button>`.

    5. handleAnalyze:
       - Validar `itemId`, `dateFrom`, `dateTo` não vazios.
       - `periodDays = differenceInCalendarDays(new Date(dateTo), new Date(dateFrom)) + 1` — usar try/catch para datas inválidas.
       - `orders = await fetchOrders(itemId, orgId, mlUserId, dateFrom, dateTo)`.
       - Se `orders.length === 0`: chamar `toast({ title: "Sem pedidos", description: "Nenhum pedido confirmado encontrado para este produto no período." })` e abortar.
       - Construir `SnapshotInput` com `orders, periodDays, periodStart: dateFrom, periodEnd: dateTo, mlUserId, organizationId: orgId, itemId, productTitle, brand`.
       - `snapshot = await saveSnapshot(input)`.
       - `setSnapshots(prev => [snapshot, ...prev.filter(s => s.id !== snapshot.id)])`.
       - Em catch: `toast({ variant: "destructive", title: "Erro na análise", description: err.message })`.

    6. handleStrategyChange (passado para AnalisePrecosTable):
       - Atualização otimista: `setSnapshots(prev => prev.map(s => s.id === id ? { ...s, strategy } : s))`.
       - `await updateStrategy(id, strategy)`.
       - Em catch: reverter e exibir toast destrutivo.

    7. useEffect: ao mudar `itemId` (truthy) e `orgId`, chamar `fetchSnapshots(itemId, orgId)` e setar resultado em `snapshots`. Skeleton enquanto `loading`.

    8. Render:
       - Card de controles (sempre visível).
       - Se `running`: renderizar 3 `<Skeleton className="h-32 w-full" />`.
       - Se `snapshots.length > 0`: renderizar `<AnalysisProductCard snapshot={snapshots[0]} />` (snapshot mais recente) + `<AnalisePrecosTable snapshots={snapshots} onStrategyChange={handleStrategyChange} />`.
       - Se vazio e não rodando: mensagem `Configure produto e período acima e clique em "Analisar" para começar.` (`text-center text-muted-foreground py-8`).

    9. NÃO criar context novo, NÃO criar selector global novo — apenas reutilizar exatamente os mesmos hooks de contexto já usados em SimuladorPrecificacao.
  </action>
  <verify>
    <automated>cd C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc && npx tsc --noEmit 2>&1 | grep "AnaliseDashboard" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe e exporta `AnaliseDashboard`.
    - Obtenção de orgId e mlUserId via os MESMOS hooks usados em SimuladorPrecificacao (verificável via grep dos imports/contexts).
    - handleAnalyze chama em sequência: validação → fetchOrders → saveSnapshot → setSnapshots prepend.
    - useEffect com `[itemId, orgId]` chama `fetchSnapshots` quando ambos truthy.
    - handleStrategyChange aplica otimismo + chama `updateStrategy` + reverte em erro.
    - Renderiza `<AnalysisProductCard>` para o snapshot mais recente e `<AnalisePrecosTable>` para a lista.
    - Skeleton durante running.
    - Sem erros TypeScript referentes ao arquivo.
  </acceptance_criteria>
  <done>Orquestrador completo: controles, fetch, save, lista persistida com mudança de estratégia funcional.</done>
</task>

<task type="auto">
  <name>Task 4: Wire AnaliseDashboard em MLPrecificacao</name>
  <files>src/pages/mercadolivre/MLPrecificacao.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLPrecificacao.tsx (estado pós-05-01: TABS já contém 'analise' com placeholder)
  </read_first>
  <action>
    Editar `src/pages/mercadolivre/MLPrecificacao.tsx` (per DASH-01/02/03 — montagem final).

    1. Adicionar import: `import { AnaliseDashboard } from "@/components/mercadolivre/analise/AnaliseDashboard";`.

    2. Substituir o placeholder criado em 05-01 (`<div className="py-8 text-center text-muted-foreground">Carregando módulo de análise…</div>`) pela montagem real: `{tab === "analise" && <AnaliseDashboard />}`.

    3. Não alterar mais nada: TABS permanece com 2 entradas, animação inalterada, sticky header inalterado, branch do simulador inalterada.
  </action>
  <verify>
    <automated>cd C:\Users\thales.martins\AppData\Local\Temp\garment-glow-app-0cc0fcfc && npx tsc --noEmit 2>&1 | grep "MLPrecificacao" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Import de `AnaliseDashboard` presente.
    - Branch `tab === "analise"` renderiza `<AnaliseDashboard />` (não o placeholder de string).
    - Branch do simulador continua intacta.
    - `npx tsc --noEmit` sem erros referentes a MLPrecificacao.tsx.
  </acceptance_criteria>
  <done>Aba 'Análise' agora renderiza o Dashboard de Análise funcional; objetivo da Fase 5 atendido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase (orders SELECT) | Já mitigado em 05-01 via RLS organization_id |
| Browser → Supabase (commercial_analysis_snapshots INSERT/UPDATE) | RLS existente (Fase 4) restringe por organization_id |
| User input (dateFrom/dateTo/itemId) | Strings vindas de Input HTML; consumidas em queries parametrizadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | AnaliseDashboard handleStrategyChange | mitigate | Atualização otimista revertida em erro; updateStrategy roda via RLS-protected Supabase client; valor restrito a union 'gmv'/'neutral'/'margin' no TypeScript e no Select |
| T-05-04 | Information Disclosure | AnalisePrecosTable | accept | Snapshots já estão sob RLS por organization_id; tabela só renderiza o que `fetchSnapshots` retorna para o org atual |
| T-05-05 | Denial of Service | handleAnalyze (loop de paginação) | accept | Volume típico de orders por item < 1000 rows; paginação implementada em 05-01 é defensiva, não exposta a input do usuário além do par item+período |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit` sem novos erros
- [ ] Aba 'Análise' renderiza controles + card + tabela após uma execução de "Analisar"
- [ ] Mudar Estratégia em uma linha destaca visualmente a coluna correspondente (emerald/blue/amber)
- [ ] Mudar Estratégia persiste após reload da página (verificável via `fetchSnapshots`)
- [ ] Card mostra três preços formatados em BRL e a frase de elasticidade exata
- [ ] Tabela mostra 7 colunas com Impacto Comercial colorido por ElasticityClass
- [ ] Sem pedidos no período: toast informativo, sem snapshot criado
</verification>

<success_criteria>
- DASH-01 cumprido: card com Preço GMV, Preço Neutro, Preço Margem e frase de elasticidade no formato exato.
- DASH-02 cumprido: tabela com colunas Produto, Marca, Preço GMV, Preço Neutro, Preço Margem, Impacto Comercial (e Estratégia adicional para suportar DASH-03).
- DASH-03 cumprido: dropdown de Estratégia por linha (GMV/Neutro/Margem) com persistência via `updateStrategy` e destaque visual da coluna correspondente.
- Objetivo do Fase 5 atingido: o usuário consegue visualizar resultados em cards e tabela interativa com seleção de estratégia.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard-de-an-lise/05-02-SUMMARY.md`
</output>
