---
phase: 83-produtos-vendidos-mco-redesign
plan: 03
type: execute
wave: 3
depends_on: ["83-01", "83-02"]
files_modified:
  - src/hooks/useMLMarginWithAds.ts
  - src/pages/mercadolivre/MLProdutosVendidos.tsx
autonomous: false
requirements: ["MCO-PV-UI"]
user_setup: []

must_haves:
  truths:
    - "A página /produtos-vendidos passa a se alimentar de get_margin_with_ads_by_product (via useMLMarginWithAds) e não mais de orders_sold_products_agg; filtra unidades > 0 (produtos vendidos, sem linhas ads-only)"
    - "O painel direito ganha coluna MCO% com bolinha de cor por faixa (🔴≤5/🟡6–8/🟢≥9, via classifyMcoHealth) + rótulo % sempre visível, e coluna % Ads (ACoS = ads_spend÷receita); a tabela é ordenável por qualquer coluna"
    - "MCO% de cada anúncio usa lucro_pct_pos_ads (com ads); quando has_cmv=false o MCO% aparece como '—' com aviso de custo ausente, nunca zerado/estimado"
    - "O painel esquerdo mostra, por marca, o MCO% da marca (Σlucro_pos_ads÷Σreceita) com bolinha de cor ao lado da receita"
    - "Ao selecionar uma marca, um cabeçalho-resumo mostra Receita total · MCO% médio da marca · nº de anúncios no vermelho (destacado quando > 0)"
    - "A versão mobile (cards) mostra as mesmas métricas (MCO% com cor, % Ads); tsc 0, build e a suíte de testes verdes, sem regressão"
  artifacts:
    - path: "src/pages/mercadolivre/MLProdutosVendidos.tsx"
      provides: "Página Produtos Vendidos com MCO por anúncio/marca, semáforo, %Ads, tabela ordenável e cabeçalho-resumo"
      contains: "aggregateMcoGroups"
    - path: "src/hooks/useMLMarginWithAds.ts"
      provides: "Hook de margem pós-ads por anúncio, agora expondo marca"
      contains: "marca"
  key_links:
    - from: "src/pages/mercadolivre/MLProdutosVendidos.tsx"
      to: "src/hooks/useMLMarginWithAds.ts"
      via: "useMLMarginWithAds(currentFrom, currentTo) como fonte única da página"
      pattern: "useMLMarginWithAds"
    - from: "src/pages/mercadolivre/MLProdutosVendidos.tsx"
      to: "src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts"
      via: "aggregateMcoGroups / aggregateMcoItems para painéis esquerdo e direito"
      pattern: "aggregateMco(Groups|Items)"
---

<objective>
Reescrever a UI de `/produtos-vendidos` para exibir MCO, consumindo a RPC de margem pós-ads
(via `useMLMarginWithAds`, agora com `marca`) e a agregação pura do 83-01. Entregar: coluna
MCO% com semáforo CVD-safe + rótulo %, coluna % Ads (ACoS), tabela ordenável por qualquer
coluna, tooltip com quebra de custos, MCO% por marca no painel esquerdo, cabeçalho-resumo do
grupo, cards mobile coerentes e aviso de custo ausente. Fecha com checkpoint visual do Wesley
(light + dark).

Purpose: responder "esse anúncio/marca vende bem, mas sobra?". A ordenação por MCO% é o
caminho para achar "micos" (vende bem, margem ruim). MCO principal = COM ads (decisão travada).

Output: página reescrita + hook estendido; checkpoint visual aprovado.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/83-produtos-vendidos-mco-redesign/83-CONTEXT.md
@src/pages/mercadolivre/MLProdutosVendidos.tsx
@src/hooks/useMLMarginWithAds.ts
@src/lib/mcoHealth.ts
@src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts
@src/lib/kpi-glossary.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expor marca no hook useMLMarginWithAds</name>
  <files>src/hooks/useMLMarginWithAds.ts</files>
  <read_first>
    - src/hooks/useMLMarginWithAds.ts — interface ProductMarginWithAds + o mapper `.map((r) => ({...}))`.
    - Consumidores existentes (adicionar campo é seguro, mas confirme que nenhum faz destructuring exaustivo/exact): src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx, ListingDetailModal.tsx, src/pages/mercadolivre/MLAnuncios.tsx.
  </read_first>
  <behavior>
    - ProductMarginWithAds ganha `marca: string | null`.
    - O mapper devolve `marca: r.marca ? String(r.marca) : null`.
    - Nenhuma outra propriedade muda; os 3 consumidores existentes continuam compilando (campo apenas adicionado).
  </behavior>
  <action>
    Adicione o campo `marca: string | null` à interface `ProductMarginWithAds` (logo após
    `listing_type` ou no fim — escolha coerente com o estilo do arquivo) e, no mapper da queryFn,
    `marca: r.marca ? String(r.marca) : null`. A RPC (após 83-02) já projeta `marca`; o mapa é
    por nome, então basta ler `r.marca`. Não mexa em mais nada do hook (queryKey, enabled,
    staleTime, args permanecem). Isso é retrocompatível: adicionar um campo não quebra os 3
    consumidores atuais.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ProductMarginWithAds tem `marca: string | null`; o mapper preenche `marca` a partir de `r.marca`.
    - `npx tsc --noEmit` passa (os 3 consumidores existentes continuam válidos).
  </acceptance_criteria>
  <done>Hook expõe `marca` sem alterar o resto do contrato; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever MLProdutosVendidos.tsx com MCO, semáforo, %Ads, tabela ordenável e cabeçalho-resumo</name>
  <files>src/pages/mercadolivre/MLProdutosVendidos.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLProdutosVendidos.tsx — estrutura atual (sticky header, toggle marca/categoria, MLPeriodPicker, painel duplo, tabela desktop + cards mobile, itemsMap do inventário).
    - src/hooks/useMLMarginWithAds.ts — assinatura useMLMarginWithAds(dateFrom, dateTo) (usa useMLStore + useOrganization internamente; devolve ProductMarginWithAds[] com marca).
    - src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts — aggregateMcoGroups / aggregateMcoItems e as interfaces PvMcoGroup/PvMcoItem/McoProductRow.
    - src/lib/mcoHealth.ts — classifyMcoHealth, mcoHealthRole, MCO_SAUDAVEL_PCT.
    - src/lib/kpi-glossary.ts — KPI_GLOSSARY.mco (texto do tooltip do cabeçalho da coluna MCO).
    - Tokens de cor do projeto: --success, --warning, --destructive, --kpi-neutral (mapa role→token).
  </read_first>
  <action>
    Reescreva a página trocando a FONTE de dados e adicionando MCO. Passos:

    (1) FONTE: troque `useMLSoldProducts` por `useMLMarginWithAds(currentFrom, currentTo)`. Do
        resultado, monte `rows: McoProductRow[]` mapeando os campos (item_id, titulo, marca,
        receita, unidades, cmv, comissao, frete, impostos, ads_spend, lucro_pos_ads,
        lucro_pct_pos_ads, has_cmv — os 4 de custo alimentam o tooltip) e **filtre
        `unidades > 0`** (decisão LOCKED: página de produtos VENDIDOS, sem linhas ads-only). NÃO
        toque em useMLSoldProducts.ts nem em soldProductsAgg.ts (Análise de Preços os usa). O
        estado de loading vem do isLoading/isPending da query; o estado vazio = rows.length===0.
        Mantenha o toggle marca/categoria, o MLPeriodPicker e o itemsMap do MLInventoryContext
        exatamente como hoje (estoque e categoria continuam vindo do inventário por item_id).

    (2) GRUPOS: use `aggregateMcoGroups(rows, pvView, itemsMap)` para o painel esquerdo e
        `aggregateMcoItems(rows, pvSelected, pvView, itemsMap)` para o direito.

    (3) PAINEL ESQUERDO (marcas/categorias): ao lado da Receita de cada grupo, exiba o MCO% do
        grupo (`g.mcoPct`) com uma bolinha de cor pela faixa (classifyMcoHealth(g.mcoPct) →
        mcoHealthRole → token CSS). Rótulo % sempre visível ao lado da bolinha (cor nunca é
        sinal único). Se `g.mcoPct` é null (Σreceita=0) mostre "—".

    (4) CABEÇALHO-RESUMO DO GRUPO: ao selecionar um grupo, acima da tabela mostre uma faixa com
        3 números: Receita total do grupo · MCO% médio da marca (mcoPct do grupo, com bolinha de
        cor) · "N anúncios no vermelho" (g.redCount), destacado (cor critical) quando N > 0. Se
        `g.hasMissingCost`, mostre um aviso curto de que o MCO% inclui anúncios sem custo (nunca
        inventar número — decisão LOCKED "Dados ausentes").

    (5) PAINEL DIREITO — COLUNAS: Anúncio · Qtd · Receita · MCO% · % Ads · Estoque · % Grupo.
        - MCO%: bolinha de cor (mcoHealthRole(item.health)→token) + rótulo. Quando
          `item.hasCmv===false` (health 'indefinido'), renderize "—" + um ícone/aviso de custo
          ausente (tooltip "Sem custo cadastrado — MCO indefinido"). NUNCA zere/estime.
        - Tooltip do MCO% (hover) com a quebra, consumindo os campos do ITEM agregado (fonte
          única do 83-01, sem recalcular na UI): "MCO R$ {item.mcoReais} · Ads R$ {item.adsSpend} ·
          Comissão R$ {item.comissao} · Frete R$ {item.frete} · Imposto R$ {item.impostos}".
          (McoProductRow já carrega cmv/comissao/frete/impostos/ads_spend/lucro_pos_ads e
          aggregateMcoItems repassa-os para PvMcoItem — a página só formata em BRL.)
        - % Ads (ACoS = item.acosPct): formatar como %; "—" quando null (receita=0). Opcional:
          destacar em warning/critical quando ACoS alto (decisão de UI sua).
        - Estoque: continua de itemsMap (available_quantity), "—" quando ausente.
        - % Grupo: item.shareOfGroup × 100.
        No cabeçalho da coluna MCO% adicione o tooltip do glossário (KPI_GLOSSARY.mco.definition).

    (6) TABELA ORDENÁVEL: adicione estado local `sortKey` (uma de: qty | revenue | mcoPct |
        acosPct | estoque | share) e `sortDir` ('asc'|'desc'), default revenue/desc (comportamento
        de hoje). Clicar no header alterna asc/desc e troca a coluna ativa; mostre um indicador
        visual (seta) na coluna ativa. Ordene os `pvItems` por uma função de comparação estável;
        nulls (MCO% indefinido, ACoS null) vão para o fim independentemente da direção. A
        ordenação por MCO% é o caminho para achar "micos".

    (7) MOBILE (cards): mesmas métricas — inclua MCO% (com bolinha de cor) e % Ads nos cards,
        além de Qtd/Receita/Estoque/%Grupo já existentes. Mantenha o aviso de custo ausente.

    (8) Paleta: use os tokens de status do projeto mapeados à paleta CVD-safe da skill dataviz
        (good/warning/serious/critical). A cor nunca carrega significado sozinha — sempre há
        rótulo %/ícone. A validação visual em light + dark é o checkpoint da Task 3.

    Mantenha o cabeçalho sticky, o toggle e o period picker. Ajuste o texto do empty state
    ("Nenhum produto vendido no período.").
  </action>
  <verify>
    <automated>cd /root/garment-glow-test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - A página importa e usa useMLMarginWithAds + aggregateMcoGroups/aggregateMcoItems; NÃO importa mais useMLSoldProducts.
    - rows filtradas por unidades > 0.
    - Painel direito tem as 7 colunas (Anúncio, Qtd, Receita, MCO%, % Ads, Estoque, % Grupo); tabela ordenável por qualquer coluna com indicador da coluna ativa.
    - MCO% usa lucro_pct_pos_ads com bolinha de cor por faixa + rótulo; has_cmv=false → "—" + aviso; tooltip com quebra de custos.
    - Painel esquerdo mostra MCO% por marca com bolinha de cor; cabeçalho-resumo com Receita/MCO% médio/nº no vermelho.
    - Cards mobile mostram MCO% e % Ads.
    - `npx tsc --noEmit` e `npm run build` passam; suíte vitest existente sem regressão.
  </acceptance_criteria>
  <done>Página reescrita com MCO por anúncio/marca, semáforo CVD-safe, %Ads, tabela ordenável, cabeçalho-resumo, cards mobile e aviso de custo ausente; tsc + build verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING]: Checkpoint visual do Wesley (light + dark)</name>
  <what-built>
    A página /produtos-vendidos reescrita: painel esquerdo com MCO% por marca (bolinha de cor),
    cabeçalho-resumo do grupo (Receita · MCO% médio · nº no vermelho), painel direito com coluna
    MCO% (semáforo + rótulo + tooltip de quebra de custos), coluna % Ads, tabela ordenável por
    qualquer coluna, cards mobile e aviso de custo ausente. Fonte = get_margin_with_ads_by_product
    (critério paid+shipped+delivered).
  </what-built>
  <how-to-verify>
    1. Abrir /produtos-vendidos em produção/preview logado como Pé Vermeio.
    2. Conferir em tema CLARO e ESCURO: as 3 cores do semáforo (🔴≤5/🟡6–8/🟢≥9) são distinguíveis
       e legíveis, o rótulo % está sempre visível ao lado da bolinha (cor não é sinal único).
    3. Selecionar uma marca: o cabeçalho-resumo mostra Receita total, MCO% médio e nº de anúncios
       no vermelho (destacado quando > 0).
    4. Ordenar por MCO% (asc) e confirmar que aparecem os "micos" (vende bem, MCO baixo); testar
       ordenar por % Ads, Receita, Qtd, Estoque, % Grupo — indicador da coluna ativa aparece.
    5. Um anúncio sem custo mostra "—" no MCO% + aviso, nunca 0%.
    6. Hover no MCO% mostra a quebra (MCO R$, Ads, Comissão, Frete, Imposto).
    7. Mobile: cards mostram MCO% (com cor) e % Ads.
    8. (Esperado, não é bug) os números de Receita/Qtd mudam levemente vs. antes (critério passou
       de só 'paid' para paid+shipped+delivered).
  </how-to-verify>
  <resume-signal>Wesley digita "aprovado" (light+dark ok) ou descreve ajustes de cor/layout/rótulo.</resume-signal>
  <action>
    [ORQUESTRADOR/Wesley] Validação visual em light + dark. Se Wesley pedir ajustes de paleta,
    rodar o validador da skill dataviz nas cores usadas e ajustar. Só então liberar o merge/PR.
  </action>
  <files>src/pages/mercadolivre/MLProdutosVendidos.tsx (validado, ajustes conforme feedback)</files>
  <verify>
    <human-check>Wesley confirma semáforo legível em light+dark, cabeçalho-resumo correto, ordenação por MCO% revela micos, aviso de custo ausente e cards mobile OK.</human-check>
  </verify>
  <acceptance_criteria>
    - Wesley aprova em light e dark, ou os ajustes solicitados foram aplicados e re-aprovados.
    - Semáforo CVD-safe legível nos dois temas; rótulo % sempre presente.
    - Custo ausente exibe "—" + aviso; ordenação por MCO% funciona.
  </acceptance_criteria>
  <done>Checkpoint visual aprovado por Wesley (light + dark); ajustes de feedback aplicados; pronto para merge/PR.</done>
</task>

</tasks>

<artifacts_produced>
- `src/hooks/useMLMarginWithAds.ts` — expõe `marca`.
- `src/pages/mercadolivre/MLProdutosVendidos.tsx` — página com MCO por anúncio/marca, semáforo CVD-safe, %Ads, tabela ordenável, cabeçalho-resumo, cards mobile, aviso de custo ausente.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (org X, JWT) → useMLMarginWithAds → RPC | leitura agregada sob RLS org-first; nenhum org_id do cliente decide escopo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-05 | Information Disclosure | página consome margem por anúncio | mitigate | fonte é a RPC SECURITY INVOKER (RLS org-first); a página não passa organization_id arbitrário — usa currentOrg/resolvedMLUserIds do contexto autenticado |
| T-83-06 | Tampering (número enganoso) | MCO% com custo ausente | mitigate | has_cmv=false → "—" + aviso; nunca zera/estima (decisão LOCKED) |
| T-83-SC | Tampering | supply chain | accept | nenhum pacote/instalação nesta phase |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run build` verdes; suíte vitest sem regressão.
- Checkpoint visual do Wesley (light+dark) aprovado.
</verification>

<success_criteria>
/produtos-vendidos mostra MCO por anúncio (semáforo + rótulo + quebra no hover) e por marca,
com % Ads, tabela ordenável e cabeçalho-resumo — respondendo "vende bem, mas sobra?" — validado
por Wesley em light e dark, sem regressão de build/testes.
</success_criteria>

<output>
Create `.planning/phases/83-produtos-vendidos-mco-redesign/83-03-SUMMARY.md` when done.
</output>
