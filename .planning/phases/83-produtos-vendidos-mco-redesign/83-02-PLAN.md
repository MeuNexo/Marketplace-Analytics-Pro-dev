---
phase: 83-produtos-vendidos-mco-redesign
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified: []
autonomous: false
requirements: ["MCO-PV-DEPLOY"]
user_setup: []

must_haves:
  truths:
    - "A migration 20260683000000_margin_with_ads_marca está aplicada em produção (ckcdevcxgvueywivefgx) via MCP apply_migration (não SQL Editor, não db push)"
    - "get_margin_with_ads_by_product retorna a coluna marca preenchida (não-nula para anúncios com venda) além das 18 colunas originais, sem erro de tipo/retorno"
    - "Retrocompat: os 4 consumidores existentes (useMLMarginWithAds.ts, useMLProductMargins.ts, nexo-chat/tools.ts, consultor-insights/index.ts) continuam funcionando — a RPC devolve as 18 colunas antigas por nome, sem quebra"
    - "Reconciliação: para a org Pé Vermeio num período curto, Σreceita da RPC bate com a soma manual em orders (status IN paid,shipped,delivered) do mesmo período — confirmando o critério unificado"
    - "Anti-IDOR: chamada como role authenticated de org alheia para itens da Pé Vermeio retorna 0 linhas"
  artifacts: []
  key_links:
    - from: "MCP apply_migration"
      to: "get_margin_with_ads_by_product (prod, com marca)"
      via: "aplicação da migration 20260683000000 no projeto ckcdevcxgvueywivefgx"
      pattern: "get_margin_with_ads_by_product"
---

<objective>
Aplicar em produção a migration escrita em 83-01 (coluna `marca` na RPC
`get_margin_with_ads_by_product`) e provar por smoke que: a coluna `marca` vem preenchida, o
consumidor existente não quebrou, a receita reconcilia com `orders` sob o critério unificado
(paid+shipped+delivered) e a RPC continua isolada por org (anti-IDOR). Plano de CHECKPOINT
executado pelo ORQUESTRADOR — o gsd-executor NÃO tem acesso ao MCP claude.ai do Supabase; a
aplicação e o smoke são via MCP `apply_migration` / `execute_sql` (padrão Phases 79/82).

Purpose: [BLOCKING] build e testes do 83-01 passam sem o deploy (os tipos vêm de config, não
do banco vivo), criando falso-positivo. A UI (83-03) só pode consumir dados reais depois que a
coluna `marca` estiver viva e reconciliada. Este é o gate onde se prova retrocompat + anti-IDOR.

Output: RPC com `marca` viva em prod + evidência (marca preenchida, retrocompat, reconciliação
de receita, anti-IDOR) registrada no SUMMARY.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/83-produtos-vendidos-mco-redesign/83-CONTEXT.md
@supabase/migrations/20260683000000_margin_with_ads_marca.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING]: Aplicar migration via MCP + smoke (marca preenchida + retrocompat + reconciliação de receita + anti-IDOR)</name>
  <read_first>
    - supabase/migrations/20260683000000_margin_with_ads_marca.sql (escrita em 83-01) — o conteúdo a aplicar.
    - .planning/phases/82-an-lise-de-pre-os-por-varia-o-seletor-de-varia-o/82-02-PLAN.md — o padrão de smoke/reconciliação/anti-IDOR desta família de RPC (impersonar role authenticated via SET LOCAL + set_config, transação ROLLBACK).
    - Os 4 consumidores atuais da RPC `get_margin_with_ads_by_product` — TODOS acessam as colunas por NOME (não por posição), então adicionar `marca` no fim da RETURNS TABLE é seguro; a retrocompat precisa ser confirmada para todos: `src/hooks/useMLMarginWithAds.ts`, `src/hooks/useMLProductMargins.ts`, `supabase/functions/nexo-chat/tools.ts`, `supabase/functions/consultor-insights/index.ts`.
  </read_first>
  <what-built>
    A migration `supabase/migrations/20260683000000_margin_with_ads_marca.sql` (escrita em
    83-01) faz DROP+CREATE de `get_margin_with_ads_by_product` adicionando a coluna `marca`
    (MAX(o.marca)) no fim da RETURNS TABLE. Ainda NÃO aplicada em prod.
  </what-built>
  <how-to-verify>
    O ORQUESTRADOR executa (não o gsd-executor):
    1. Aplicar a migration no projeto `ckcdevcxgvueywivefgx` via MCP
       `mcp__claude_ai_Supabase__apply_migration` (name: margin_with_ads_marca; conteúdo = o
       arquivo .sql do 83-01). NUNCA via SQL Editor, NUNCA `supabase db push`.
    2. Smoke de retorno + marca: `SELECT item_id, marca, receita, unidades, lucro_pos_ads,
       lucro_pct_pos_ads, ads_spend FROM get_margin_with_ads_by_product('<org Pé Vermeio>',
       ARRAY['<ml_user_id>'], '<from>', '<to>') WHERE unidades > 0 LIMIT 10;` via `execute_sql`
       — deve retornar a coluna `marca` preenchida (não-nula) para anúncios com venda, sem erro
       de "function does not exist"/tipo.
    3. Retrocompat dos consumidores: rodar uma chamada equivalente (mesmos 4 args) e confirmar
       que TODAS as 18 colunas antigas (item_id, titulo, sku, listing_type, receita, cmv,
       comissao, frete, impostos, lucro, lucro_pct, pedidos, unidades, has_cmv, ads_spend,
       ads_attributed_orders, lucro_pos_ads, lucro_pct_pos_ads, ads_no_sale) continuam presentes
       por nome — garantindo que NENHUM dos 4 consumidores quebra: `useMLMarginWithAds.ts`,
       `useMLProductMargins.ts`, `nexo-chat/tools.ts`, `consultor-insights/index.ts` (todos leem
       por nome).
    4. Reconciliação de receita (role authenticated, NÃO postgres — impersonar a org Pé Vermeio
       via SET LOCAL ROLE authenticated + set_config request.jwt.claims em transação ROLLBACK,
       método Phases 43/79/82). Comparar Σreceita da RPC no período curto contra
       `SELECT SUM(receita_bruta) FROM orders WHERE organization_id=... AND ml_user_id=ANY(...)
       AND status IN ('paid','shipped','delivered') AND data_pedido::date BETWEEN ... AND ...`.
       Devem bater (critério unificado). Registrar os dois números no SUMMARY.
    5. Anti-IDOR: repetir a chamada impersonando uma org ALHEIA (ex.: Thales) para itens da Pé
       Vermeio → deve retornar 0 linhas.
  </how-to-verify>
  <resume-signal>
    Confirmar: "migration aplicada em prod; coluna marca preenchida para anúncios com venda;
    retrocompat OK (18 colunas antigas por nome); Σreceita da RPC ≡ soma manual em orders
    (paid+shipped+delivered); anti-IDOR = 0 linhas para org alheia." Se a receita não bater ou a
    marca vier nula, investigar (MAX(o.marca), status, cast ::date) antes de liberar o wave 3.
  </resume-signal>
  <action>
    [ORQUESTRADOR — o gsd-executor não tem MCP claude.ai] Aplicar a migration 83-01 no projeto
    ckcdevcxgvueywivefgx via MCP `apply_migration` (nunca SQL Editor, nunca `supabase db push`),
    depois rodar os passos 2-5 de "how-to-verify" via `execute_sql`: smoke de retorno+marca,
    retrocompat das 18 colunas antigas, reconciliação de Σreceita como role authenticated (Pé
    Vermeio) contra soma manual em `orders`, e o teste anti-IDOR (org alheia → 0 linhas).
    Registrar todos os números no SUMMARY. Bloqueante: só liberar o wave 3 (UI, 83-03) após
    marca preenchida + retrocompat + reconciliação + anti-IDOR OK.
  </action>
  <files>supabase/migrations/20260683000000_margin_with_ads_marca.sql (aplicada em prod, não editada)</files>
  <verify>
    <automated>MISSING — verificação é via MCP execute_sql pelo orquestrador (checkpoint BLOCKING); não há comando local. Evidência registrada no 83-02-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - Migration aplicada em prod via MCP apply_migration (não SQL Editor, não db push).
    - Coluna `marca` retorna preenchida (não-nula) para anúncios com venda.
    - Retrocompat: as 18 colunas originais continuam presentes por nome — nenhum dos 4 consumidores (useMLMarginWithAds.ts, useMLProductMargins.ts, nexo-chat/tools.ts, consultor-insights/index.ts) quebra.
    - Σreceita da RPC ≡ soma manual em orders (paid+shipped+delivered) no período curto — registrado no SUMMARY.
    - Anti-IDOR: org alheia → 0 linhas.
  </acceptance_criteria>
  <done>RPC com `marca` viva em prod; marca preenchida; retrocompat provada; receita reconciliada sob critério unificado; anti-IDOR = 0 linhas; números no SUMMARY.</done>
</task>

</tasks>

<artifacts_produced>
- RPC `get_margin_with_ads_by_product` em produção (ckcdevcxgvueywivefgx) com a coluna `marca` ativa.
- Evidência no SUMMARY: marca preenchida, retrocompat das 18 colunas, reconciliação de receita, anti-IDOR (0 linhas cross-org).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador (MCP) → banco prod | aplicação de DDL e SQL de smoke |
| role authenticated (org X) → RPC | leitura agregada de orders/ml_ads sob RLS, agora com marca |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Elevation of Privilege (IDOR) | RPC em prod (com marca) | mitigate | smoke anti-IDOR obrigatório: org alheia → 0 linhas, provado como role authenticated (não postgres) |
| T-83-04 | Repudiation/erro silencioso | DDL aplicada / retrocompat | mitigate | smoke de retrocompat (18 colunas por nome) + reconciliação de receita antes de liberar a UI (83-03) |
| T-83-SC | Tampering | supply chain | accept | nenhum pacote/instalação nesta phase |
</threat_model>

<verification>
- Migration aplicada via MCP apply_migration (não SQL Editor).
- Marca preenchida; retrocompat OK; Σreceita reconcilia; anti-IDOR = 0 linhas.
</verification>

<success_criteria>
RPC com `marca` viva em prod, retrocompatível, reconciliada por receita e isolada por org —
pronta para a UI (83-03) consumir.
</success_criteria>

<output>
Create `.planning/phases/83-produtos-vendidos-mco-redesign/83-02-SUMMARY.md` when done (registrar marca preenchida, retrocompat, reconciliação de receita e anti-IDOR).
</output>
