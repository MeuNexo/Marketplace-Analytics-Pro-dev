---
phase: 83-produtos-vendidos-mco-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260683000000_margin_with_ads_marca.sql
  - src/lib/mcoHealth.ts
  - src/lib/mcoHealth.test.ts
  - src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts
  - src/components/mercadolivre/anuncios/soldProductsMcoAgg.test.ts
autonomous: true
requirements: ["MCO-PV-RPC", "MCO-PV-LOGIC"]
user_setup: []

must_haves:
  truths:
    - "A RPC get_margin_with_ads_by_product passa a retornar a coluna marca (MAX(o.marca) por item_id), adicionada NO FIM da RETURNS TABLE — o consumidor existente useMLMarginWithAds (mapeia por nome) não quebra"
    - "A migration é DROP FUNCTION (assinatura de 4 args) + CREATE (não CREATE OR REPLACE, pois a RETURNS TABLE muda), preservando SECURITY INVOKER + GRANT authenticated + status IN ('paid','shipped','delivered') + GROUP BY o.item_id"
    - "classifyMcoHealth(pct) devolve 'vermelho' quando pct<=5, 'amarelo' quando 5<pct<9, 'verde' quando pct>=9, e 'indefinido' quando pct é null/undefined (custo ausente) — cortes centralizados na constante MCO_SAUDAVEL_PCT"
    - "aggregateMcoGroups devolve, por marca/categoria, receita=Σreceita, mcoPct=Σlucro_pos_ads÷Σreceita×100 (null se Σreceita=0), redCount=nº de anúncios com saúde 'vermelho', e hasMissingCost=true se algum anúncio do grupo tem has_cmv=false"
    - "aggregateMcoItems devolve por anúncio mcoPct=lucro_pct_pos_ads, acosPct=ads_spend÷receita×100 (null se receita=0), health=classifyMcoHealth(mcoPct quando has_cmv, senão indefinido), shareOfGroup=receita÷Σreceita do grupo e os campos do tooltip (cmv, comissao, frete, impostos, adsSpend, mcoReais) como fonte única da quebra de custos do 83-03"
  artifacts:
    - path: "supabase/migrations/20260683000000_margin_with_ads_marca.sql"
      provides: "RPC get_margin_with_ads_by_product com coluna marca"
      contains: "marca"
    - path: "src/lib/mcoHealth.ts"
      provides: "Constante MCO_SAUDAVEL_PCT + classifyMcoHealth + mapeamento saúde→role de cor CVD-safe"
      contains: "MCO_SAUDAVEL_PCT"
    - path: "src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts"
      provides: "Agregação pura por marca/categoria e por anúncio a partir das linhas da RPC de margem"
      contains: "aggregateMcoGroups"
  key_links:
    - from: "src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts"
      to: "src/lib/mcoHealth.ts"
      via: "classifyMcoHealth para contar anúncios no vermelho por grupo"
      pattern: "classifyMcoHealth"
    - from: "supabase/migrations/20260683000000_margin_with_ads_marca.sql"
      to: "orders (coluna marca)"
      via: "MAX(o.marca) AS marca no CTE orders_side"
      pattern: "MAX\\(o\\.marca\\)"
---

<objective>
Construir a FUNDAÇÃO da Phase 83 (backend + lógica pura, 100% testável, sem UI): (1) a
migration que adiciona a coluna `marca` à RPC `get_margin_with_ads_by_product` (a página
agrupa por marca, e a RPC hoje não devolve marca); (2) o helper puro de saúde de MCO
(`mcoHealth.ts`) com a constante de faixas `MCO_SAUDAVEL_PCT` e a função de classificação
(semáforo); (3) o módulo puro de agregação por marca/categoria e por anúncio
(`soldProductsMcoAgg.ts`) que a UI (83-03) vai consumir. Todos com suíte vitest.

Purpose: separar o que é determinístico e testável (fórmula de faixa, agregação pós-ads por
marca, coluna `marca` na RPC) da UI, para que a página só precise renderizar números já
provados. MCO principal = COM ads (`lucro_pos_ads` / `lucro_pct_pos_ads`), decisão travada.

Output: 1 arquivo de migration (aplicado em prod só no 83-02, via MCP pelo orquestrador — o
gsd-executor não tem token) + 2 módulos TS puros + 2 suítes de teste.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/83-produtos-vendidos-mco-redesign/83-CONTEXT.md
@supabase/migrations/20260615120000_margin_with_ads_rpc.sql
@src/lib/mco.ts
@src/hooks/useMLMarginWithAds.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — adicionar coluna marca à RPC get_margin_with_ads_by_product</name>
  <files>supabase/migrations/20260683000000_margin_with_ads_marca.sql</files>
  <read_first>
    - supabase/migrations/20260615120000_margin_with_ads_rpc.sql — versão ATUAL da RPC (18 colunas, FULL OUTER JOIN orders_side×ads_side, SECURITY INVOKER, GRANT authenticated). Copie-a como base e faça a MENOR mudança possível.
    - src/hooks/useMLMarginWithAds.ts — o OUTRO consumidor da RPC. Confirme que ele mapeia por NOME de coluna (r.item_id, r.receita, …), não por posição — por isso adicionar `marca` no FIM da RETURNS TABLE é retrocompatível.
    - .planning/phases/83-produtos-vendidos-mco-redesign/83-CONTEXT.md — seção Decisions, item "Fonte de dados — UMA RPC só".
  </read_first>
  <action>
    Crie a migration copiando integralmente a RPC de `20260615120000_margin_with_ads_rpc.sql`
    e aplicando exatamente estas mudanças (per decisão LOCKED "Fonte de dados — UMA RPC só" do
    CONTEXT):

    (1) DROP explícito da assinatura ATUAL antes do CREATE. Como a RETURNS TABLE muda (ganha
        uma coluna), `CREATE OR REPLACE` falha com "cannot change return type of existing
        function". Portanto:
        `DROP FUNCTION IF EXISTS public.get_margin_with_ads_by_product(UUID, TEXT[], DATE, DATE);`
        e depois `CREATE FUNCTION` (não OR REPLACE). A assinatura de argumentos NÃO muda
        (continua `p_org_id UUID, p_user_ids TEXT[], p_from DATE, p_to DATE`).

    (2) Na RETURNS TABLE, adicione `marca TEXT` como ÚLTIMA coluna (após `ads_no_sale`).
        Adicionar no FIM preserva o mapeamento-por-nome de useMLMarginWithAds.

    (3) No CTE `orders_side`, adicione a agregação `MAX(o.marca) AS marca` (junto dos outros
        MAX(o.titulo)/MAX(o.sku)). `orders.marca` é a mesma coluna que a página usa hoje via
        orders_sold_products_agg.

    (4) No SELECT final, adicione `o.marca AS marca` como última coluna projetada (do alias
        `orders_side o`). Para linhas ads-only do FULL OUTER JOIN (sem lado orders) `marca`
        vem NULL — aceitável, pois a página 83 filtra `unidades > 0` e essas linhas somem.

    NÃO mude mais nada: as 18 colunas existentes permanecem idênticas e na MESMA ordem; o
    FULL OUTER JOIN, o GROUP BY o.item_id, o status IN ('paid','shipped','delivered'), o
    cast `o.data_pedido::date`, `LANGUAGE sql STABLE SECURITY INVOKER SET search_path =
    public`, o ORDER BY por receita DESC e o `GRANT EXECUTE ... TO authenticated` permanecem.
    Nenhuma subquery correlacionada (lição RPC RLS timeout 8s). Sem parâmetro de org extra
    além do já existente (RLS org-first de orders/ml_ads_products_cache isola o tenant do
    chamador autenticado — anti-IDOR mantido).

    No topo do arquivo, comente: que o deploy é via MCP `apply_migration` no projeto
    `ckcdevcxgvueywivefgx` (NUNCA SQL Editor / NUNCA `supabase db push`), que a aplicação real
    + smoke acontecem no plano 83-02 (checkpoint do orquestrador), e o porquê do DROP (a
    RETURNS TABLE mudou). Atualize o comentário/rodapé de smoke para lembrar de checar
    retrocompat do consumidor useMLMarginWithAds e reconciliação de receita.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260683000000_margin_with_ads_marca.sql &amp;&amp; grep -qi 'DROP FUNCTION IF EXISTS public.get_margin_with_ads_by_product(UUID, TEXT\[\], DATE, DATE)' supabase/migrations/20260683000000_margin_with_ads_marca.sql &amp;&amp; grep -q 'MAX(o.marca)' supabase/migrations/20260683000000_margin_with_ads_marca.sql &amp;&amp; grep -q 'SECURITY INVOKER' supabase/migrations/20260683000000_margin_with_ads_marca.sql &amp;&amp; grep -q 'GRANT EXECUTE ON FUNCTION public.get_margin_with_ads_by_product' supabase/migrations/20260683000000_margin_with_ads_marca.sql</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe: DROP FUNCTION (4 args) seguido de CREATE FUNCTION (mesmos 4 args).
    - RETURNS TABLE ganha exatamente 1 coluna nova, `marca TEXT`, como última.
    - `orders_side` contém `MAX(o.marca) AS marca`; o SELECT final projeta `o.marca AS marca`.
    - As 18 colunas originais permanecem idênticas e na mesma ordem; SECURITY INVOKER + GRANT authenticated + status IN ('paid','shipped','delivered') + GROUP BY o.item_id preservados.
    - Diff contra `20260615120000_...` = só a coluna marca (3 pontos) + comentários; nenhuma outra linha de lógica alterada.
    - NÃO aplica a migration em prod (isso é 83-02).
  </acceptance_criteria>
  <done>Migration escrita, com `marca` no fim da RETURNS TABLE, `MAX(o.marca)` no CTE e projeção final; retrocompatível por nome; pronta para o orquestrador aplicar em 83-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helper puro de saúde de MCO (mcoHealth.ts) + testes</name>
  <files>src/lib/mcoHealth.ts, src/lib/mcoHealth.test.ts</files>
  <read_first>
    - src/lib/mco.ts — estilo do helper puro canônico (interface + função pura, JSDoc curto).
    - src/lib/mco.test.ts — estilo da suíte vitest deste diretório.
    - .planning/phases/83-produtos-vendidos-mco-redesign/83-CONTEXT.md — seção "Faixas do semáforo (MCO% por anúncio)".
  </read_first>
  <behavior>
    - MCO_SAUDAVEL_PCT = { red: 5, green: 9 } (corte do vermelho e do verde; centraliza os limiares).
    - classifyMcoHealth(4) === 'vermelho'; classifyMcoHealth(5) === 'vermelho' (≤5).
    - classifyMcoHealth(6) === 'amarelo'; classifyMcoHealth(8) === 'amarelo'; classifyMcoHealth(8.9) === 'amarelo' (>5 e <9).
    - classifyMcoHealth(9) === 'verde'; classifyMcoHealth(12.3) === 'verde' (≥9).
    - classifyMcoHealth(null) === 'indefinido'; classifyMcoHealth(undefined) === 'indefinido' (custo ausente).
    - mcoHealthRole('vermelho') === 'critical'; ('amarelo') === 'warning'; ('verde') === 'good'; ('indefinido') === 'neutral'.
  </behavior>
  <action>
    Crie `src/lib/mcoHealth.ts` PURO (zero React/Supabase). Exporte:
    - `export const MCO_SAUDAVEL_PCT = { red: 5, green: 9 } as const;` — os cortes travados por Wesley
      (🔴 MCO% ≤ 5 · 🟡 6–8 (>5 e <9) · 🟢 ≥ 9). Fonte única dos limiares.
    - `export type McoHealth = 'verde' | 'amarelo' | 'vermelho' | 'indefinido';`
    - `export function classifyMcoHealth(pct: number | null | undefined): McoHealth` — null/undefined
      (custo ausente) → 'indefinido'; pct <= MCO_SAUDAVEL_PCT.red → 'vermelho'; pct >=
      MCO_SAUDAVEL_PCT.green → 'verde'; caso contrário → 'amarelo'.
    - `export type McoColorRole = 'critical' | 'warning' | 'good' | 'neutral';`
    - `export function mcoHealthRole(h: McoHealth): McoColorRole` — mapeia saúde → role de cor
      CVD-safe (vermelho→critical, amarelo→warning, verde→good, indefinido→neutral). A UI (83-03)
      converte o role em token/hex da paleta de status dataviz; manter a decisão de faixa aqui,
      em um único lugar testável. A cor NUNCA é sinal único — o rótulo % é sempre renderizado
      pela UI ao lado da bolinha.
    Crie `src/lib/mcoHealth.test.ts` cobrindo os casos do bloco <behavior> (incluindo as bordas
    exatas 5, 6, 8.9, 9 e os dois nulos).
    Não coloque cor/hex/CSS neste arquivo — só a classificação e o role semântico.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test &amp;&amp; npx vitest run src/lib/mcoHealth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `MCO_SAUDAVEL_PCT` exportado com red=5 e green=9.
    - Todas as bordas do bloco <behavior> passam (5→vermelho, 6/8.9→amarelo, 9→verde, null/undefined→indefinido).
    - mcoHealthRole mapeia os 4 estados corretamente.
    - Nenhum import de React/Supabase/DOM; nenhum hex/CSS no arquivo.
  </acceptance_criteria>
  <done>mcoHealth.ts puro com constante de faixas + classificação + role de cor; suíte vitest verde cobrindo bordas e nulos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Agregação por marca/categoria e por anúncio (soldProductsMcoAgg.ts) + testes</name>
  <files>src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts, src/components/mercadolivre/anuncios/soldProductsMcoAgg.test.ts</files>
  <read_first>
    - src/components/mercadolivre/anuncios/soldProductsAgg.ts — padrão atual de aggregatePvGroups/aggregatePvItems (chave marca vs category_id via itemsMap, shareOfGroup, ordenação por receita). Reuse a MESMA lógica de agrupamento.
    - src/components/mercadolivre/anuncios/soldProductsAgg.test.ts — estilo da suíte deste diretório.
    - src/hooks/useMLMarginWithAds.ts — o shape ProductMarginWithAds (receita, unidades, ads_spend, lucro_pos_ads, lucro_pct_pos_ads, has_cmv, marca após 83-01).
    - src/lib/mcoHealth.ts — classifyMcoHealth (Task 2 deste plano).
  </read_first>
  <behavior>
    - aggregateMcoGroups agrupa por marca (row.marca ?? "") ou por categoria (itemsMap.category_id ?? "") e por grupo devolve: revenue=Σreceita, qty=Σunidades, mcoPct=Σlucro_pos_ads÷Σreceita×100 (null quando Σreceita=0), redCount=nº de anúncios do grupo cuja saúde é 'vermelho', hasMissingCost=true se algum anúncio do grupo tem has_cmv=false. Ordena por revenue desc.
    - Grupo com 2 anúncios receita 100 (lucro_pos_ads 3) e 200 (lucro_pos_ads 30): mcoPct = 33÷300×100 = 11 → grupo saudável; redCount conta o anúncio de mcoPct 3% (=3, vermelho) como 1.
    - aggregateMcoItems (do grupo selecionado) devolve por anúncio: title (itemsMap.title>titulo>item_id), qty=unidades, revenue=receita, mcoReais=lucro_pos_ads, mcoPct=lucro_pct_pos_ads, acosPct=ads_spend÷receita×100 (null quando receita=0), hasCmv=has_cmv, health=has_cmv? classifyMcoHealth(lucro_pct_pos_ads) : 'indefinido', shareOfGroup=receita÷Σreceita do grupo (0 quando Σ=0). Ordena por revenue desc.
    - CAMPOS DO TOOLTIP (fonte única, repassados do row para o item): cmv, comissao, frete, impostos, adsSpend=ads_spend, mcoReais=lucro_pos_ads. O 83-03 consome ESTES campos do item agregado (não recalcula na UI) para a quebra de custos no hover.
    - Anúncio com has_cmv=false → health='indefinido' e mcoPct pode ser exibido como "—" pela UI (não zerar).
  </behavior>
  <action>
    Crie `src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts` PURO (zero React/Supabase/rede).
    Defina a interface de entrada auto-contida `McoProductRow` com os campos que a UI passa a
    partir de ProductMarginWithAds: `{ item_id: string; titulo: string | null; marca: string | null;
    receita: number; unidades: number; cmv: number; comissao: number; frete: number; impostos: number;
    ads_spend: number; lucro_pos_ads: number; lucro_pct_pos_ads: number | null; has_cmv: boolean }`.
    (cmv/comissao/frete/impostos entram para a quebra de custos do tooltip ter fonte única.) Exporte:
    - `PvMcoGroup { key; name; revenue; qty; mcoPct: number | null; redCount: number; hasMissingCost: boolean }`
    - `PvMcoItem { item_id; title; qty; revenue; mcoReais; mcoPct: number | null; acosPct: number | null; hasCmv: boolean; health: McoHealth; shareOfGroup: number; cmv: number; comissao: number; frete: number; impostos: number; adsSpend: number }` (cmv/comissao/frete/impostos/adsSpend/mcoReais = fonte única do tooltip de quebra de custos do 83-03)
    - `aggregateMcoGroups(rows, pvView, itemsMap): PvMcoGroup[]`
    - `aggregateMcoItems(rows, pvSelected, pvView, itemsMap): PvMcoItem[]`
    Reuse EXATAMENTE o padrão de chave/grupo de soldProductsAgg.ts (marca=row.marca??"" ;
    categoria=itemsMap.get(item_id)?.category_id??"" ; name "Sem marca"/"Sem categoria" quando
    key vazia). mcoPct do grupo = Σlucro_pos_ads ÷ Σreceita × 100 (per decisão LOCKED #6: pós-ads,
    consistente com o painel direito) — NÃO a média dos mcoPct dos itens. Importe `classifyMcoHealth`
    de `@/lib/mcoHealth` para o redCount e para o health por item. `hasMissingCost` sinaliza que o
    MCO% do grupo inclui anúncios sem custo (a UI mostra um aviso; nunca inventamos número — decisão
    LOCKED "Dados ausentes"). NÃO usar get_margin_by_brand (pré-ads, inconsistente — decisão LOCKED).
    Crie `soldProductsMcoAgg.test.ts` cobrindo: agrupamento por marca e por categoria; mcoPct pós-ads
    do grupo; redCount; hasMissingCost quando há has_cmv=false; item com has_cmv=false → health
    'indefinido'; acosPct null quando receita=0; shareOfGroup somando 1 no grupo; ordenação por receita;
    e os CAMPOS DO TOOLTIP no item (cmv, comissao, frete, impostos, adsSpend, mcoReais) repassados
    fielmente do row (assert de igualdade dos 6 valores).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test &amp;&amp; npx vitest run src/components/mercadolivre/anuncios/soldProductsMcoAgg.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - aggregateMcoGroups: mcoPct = Σlucro_pos_ads÷Σreceita×100 (não média de %), redCount e hasMissingCost corretos, ordenado por revenue desc.
    - aggregateMcoItems: mcoPct=lucro_pct_pos_ads, acosPct=ads_spend÷receita×100 (null se receita=0), health via classifyMcoHealth, has_cmv=false→'indefinido', shareOfGroup correto.
    - PvMcoItem inclui os campos do tooltip (cmv, comissao, frete, impostos, adsSpend, mcoReais) repassados do row; teste assegura os 6 valores.
    - Agrupamento por marca e por categoria idêntico ao padrão de soldProductsAgg.ts.
    - Suíte vitest verde; módulo puro (sem React/Supabase).
  </acceptance_criteria>
  <done>Módulo de agregação puro com grupos (mcoPct pós-ads, redCount, hasMissingCost) e itens (mcoPct/acos/health/share); suíte vitest verde.</done>
</task>

</tasks>

<artifacts_produced>
- `supabase/migrations/20260683000000_margin_with_ads_marca.sql` — RPC `get_margin_with_ads_by_product` ganha coluna `marca` (aplicação em prod = plano 83-02).
- `src/lib/mcoHealth.ts` — `MCO_SAUDAVEL_PCT`, `classifyMcoHealth`, `mcoHealthRole` (semáforo CVD-safe centralizado).
- `src/components/mercadolivre/anuncios/soldProductsMcoAgg.ts` — `aggregateMcoGroups` / `aggregateMcoItems` (agregação pós-ads por marca/categoria e por anúncio).
- Suítes: `mcoHealth.test.ts`, `soldProductsMcoAgg.test.ts`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role authenticated (org X) → RPC get_margin_with_ads_by_product | leitura agregada de orders/ml_ads_products_cache sob RLS, agora projetando marca |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Elevation of Privilege (IDOR) | RPC com coluna marca | mitigate | SECURITY INVOKER + RLS org-first de orders/ml_ads_products_cache; marca é coluna do próprio orders já visível ao chamador. Prova anti-IDOR no smoke 83-02 |
| T-83-02 | Repudiation/erro silencioso | mudança de RETURNS TABLE | mitigate | DROP explícito da assinatura antiga + retrocompat de useMLMarginWithAds (mapa por nome) verificada no smoke 83-02 |
| T-83-SC | Tampering | supply chain | accept | nenhum pacote/instalação nesta phase |
</threat_model>

<verification>
- Migration com `marca` (DROP+CREATE, SECURITY INVOKER, GRANT authenticated) existe.
- `npx vitest run src/lib/mcoHealth.test.ts src/components/mercadolivre/anuncios/soldProductsMcoAgg.test.ts` verde.
</verification>

<success_criteria>
Fundação pronta: coluna `marca` na RPC (escrita, não aplicada), faixas de MCO centralizadas e
agregação pós-ads por marca/anúncio — tudo puro e provado por testes, pronto para o deploy
(83-02) e a UI (83-03).
</success_criteria>

<output>
Create `.planning/phases/83-produtos-vendidos-mco-redesign/83-01-SUMMARY.md` when done.
</output>
