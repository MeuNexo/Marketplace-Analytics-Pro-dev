---
phase: 10-inventory-cache
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260519150000_ml_inventory_cache.sql
  - supabase/functions/sync-ml-inventory/index.ts
  - supabase/functions/process-sync-job/index.ts
  - supabase/config.toml
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03

must_haves:
  truths:
    - "A migration file ml_inventory_cache.sql existe com a tabela, UNIQUE, RLS, dispatch_inventory_jobs() e pg_cron às 07:00 UTC"
    - "A edge function sync-ml-inventory existe e aceita ml_user_id, faz upsert na ml_inventory_cache"
    - "process-sync-job atualizado para tratar job_type=inventory chamando sync-ml-inventory"
    - "config.toml tem [functions.sync-ml-inventory] com verify_jwt = false"
  artifacts:
    - path: "supabase/migrations/20260519150000_ml_inventory_cache.sql"
      provides: "Schema ml_inventory_cache + dispatch_inventory_jobs() + pg_cron"
    - path: "supabase/functions/sync-ml-inventory/index.ts"
      provides: "Edge function que sincroniza inventário ML para cache DB"
    - path: "supabase/functions/process-sync-job/index.ts"
      provides: "Job drainer atualizado com suporte a inventory job_type"
---

<objective>
Criar toda a infraestrutura da Phase 10: migration SQL para ml_inventory_cache, edge function sync-ml-inventory e atualização de process-sync-job para suportar inventory job_type.
</objective>

<tasks>

<task type="code">
  <name>Task 1: Migration ml_inventory_cache</name>
  <file>supabase/migrations/20260519150000_ml_inventory_cache.sql</file>
  <spec>
    -- Tabela ml_inventory_cache
    -- PK e UNIQUE: (organization_id, ml_user_id, item_id)
    -- Colunas: item_id text, title text, status text, available_quantity int,
    --   sold_quantity int, price numeric, currency_id text DEFAULT 'BRL',
    --   thumbnail text, category_id text, listing_type_id text, health numeric,
    --   visits int DEFAULT 0, brand text, seller_custom_field text,
    --   has_variations bool DEFAULT false, variations jsonb DEFAULT '[]',
    --   logistic_type text, free_shipping bool DEFAULT false,
    --   catalog_product_id text, deal_ids jsonb DEFAULT '[]',
    --   synced_at timestamptz NOT NULL DEFAULT now()
    -- RLS: SELECT para authenticated org members; INSERT/UPDATE/DELETE: service_role only
    
    -- Função dispatch_inventory_jobs()
    -- Itera ml_tokens com access_token IS NOT NULL
    -- Para cada (organization_id, ml_user_id): insere job 'inventory' pending
    --   se não existir pending/running (mesmo guard do dispatch_sync_jobs SYNC-07)
    -- Retorna integer (total inserido)
    
    -- pg_cron: '0 7 * * *' (04:00 BRT = 07:00 UTC)
    -- Chama dispatch_inventory_jobs() via net.http_post para process-sync-job
    -- Ou diretamente: SELECT public.dispatch_inventory_jobs();
  </spec>
</task>

<task type="code">
  <name>Task 2: Edge function sync-ml-inventory</name>
  <file>supabase/functions/sync-ml-inventory/index.ts</file>
  <spec>
    -- Auth: Bearer service_role key OR X-Cron-Secret (via get_cron_secret() RPC)
    -- Input body: { ml_user_id: string }
    -- Lookup: busca access_token e organization_id de ml_tokens
    -- Fetch: busca itens active + paused da ML API (paginado, PAGE_SIZE=100)
    -- Multi-get em batches de 20 para detalhes (sem variation SKU enrichment — caro demais para cache)
    -- Upsert: INSERT INTO ml_inventory_cache ... ON CONFLICT DO UPDATE SET ...
    -- Retorna: { ok: true, upserted: N, ml_user_id }
  </spec>
</task>

<task type="code">
  <name>Task 3: Atualizar process-sync-job para inventory</name>
  <file>supabase/functions/process-sync-job/index.ts</file>
  <spec>
    -- Adicionar case 'inventory' no if/else de job_type
    -- Chamar sync-ml-inventory com { ml_user_id: job.ml_user_id }
    -- Mesmo padrão do case 'orders': check resp.ok, update status completed/failed
  </spec>
</task>

<task type="code">
  <name>Task 4: Atualizar config.toml</name>
  <file>supabase/config.toml</file>
  <spec>
    -- Adicionar [functions.sync-ml-inventory] com verify_jwt = false
  </spec>
</task>

</tasks>
