---
phase: 28-performance-scalability
plan: 01
type: execute
wave: 1
autonomous: true
files_modified:
  - supabase/migrations/20260527100000_batch_upsert_orders.sql
  - supabase/functions/sync-ml-orders/index.ts
---

## Objetivo

Substituir o loop N+1 de upserts individuais no `sync-ml-orders` por um único RPC batch.
Atualmente: `for (const r of records) { await supabaseAdmin.rpc("upsert_order_preserve_cost", ...) }` — para 200 pedidos = 200 round-trips sequenciais.
Depois: `await supabaseAdmin.rpc("batch_upsert_orders", { p_records: records })` — 1 round-trip, mesma lógica de preserve cost.

---

## TAREFA 1 — Migration: criar função `batch_upsert_orders`

**Arquivo:** `supabase/migrations/20260527100000_batch_upsert_orders.sql`

```sql
-- Batch upsert de orders preservando custo_unit histórico.
-- Substitui o loop de upsert_order_preserve_cost chamado N vezes.
CREATE OR REPLACE FUNCTION public.batch_upsert_orders(p_records JSONB)
RETURNS INTEGER
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
DECLARE
  v_count INTEGER := 0;
BEGIN
  INSERT INTO public.orders (
    ml_order_id, ml_user_id, item_id, variation_id, seller_id,
    user_id, organization_id, sku, titulo, listing_type,
    quantidade, preco_unit, comissao, frete, status,
    data_pedido, data_pagamento, estado, cidade, comprador,
    synced_at, custo_unit, tax_rate, tax_amount,
    uf_origem, receita_bruta, receita_liquida, marca
  )
  SELECT
    (r->>'ml_order_id'),
    (r->>'ml_user_id')::uuid,
    (r->>'item_id'),
    (r->>'variation_id'),
    (r->>'seller_id'),
    (r->>'user_id')::uuid,
    (r->>'organization_id')::uuid,
    (r->>'sku'),
    (r->>'titulo'),
    (r->>'listing_type'),
    (r->>'quantidade')::integer,
    (r->>'preco_unit')::numeric,
    (r->>'comissao')::numeric,
    (r->>'frete')::numeric,
    (r->>'status'),
    (r->>'data_pedido')::timestamptz,
    (r->>'data_pagamento')::timestamptz,
    (r->>'estado'),
    (r->>'cidade'),
    (r->>'comprador'),
    (r->>'synced_at')::timestamptz,
    NULLIF(r->>'custo_unit', '')::numeric,
    NULLIF(r->>'tax_rate', '')::numeric,
    NULLIF(r->>'tax_amount', '')::numeric,
    (r->>'uf_origem'),
    (r->>'receita_bruta')::numeric,
    (r->>'receita_liquida')::numeric,
    (r->>'marca')
  FROM jsonb_array_elements(p_records) AS r
  ON CONFLICT (ml_order_id, ml_user_id, item_id, variation_id)
  DO UPDATE SET
    seller_id       = EXCLUDED.seller_id,
    user_id         = EXCLUDED.user_id,
    organization_id = EXCLUDED.organization_id,
    sku             = EXCLUDED.sku,
    titulo          = EXCLUDED.titulo,
    listing_type    = EXCLUDED.listing_type,
    quantidade      = EXCLUDED.quantidade,
    preco_unit      = EXCLUDED.preco_unit,
    comissao        = EXCLUDED.comissao,
    frete           = EXCLUDED.frete,
    status          = EXCLUDED.status,
    data_pedido     = EXCLUDED.data_pedido,
    data_pagamento  = EXCLUDED.data_pagamento,
    estado          = EXCLUDED.estado,
    cidade          = EXCLUDED.cidade,
    comprador       = EXCLUDED.comprador,
    synced_at       = EXCLUDED.synced_at,
    -- Preserve: não sobrescreve custo_unit histórico se o novo for nulo
    custo_unit      = COALESCE(EXCLUDED.custo_unit, orders.custo_unit),
    tax_rate        = EXCLUDED.tax_rate,
    tax_amount      = EXCLUDED.tax_amount,
    uf_origem       = EXCLUDED.uf_origem,
    receita_bruta   = EXCLUDED.receita_bruta,
    receita_liquida = EXCLUDED.receita_liquida,
    marca           = EXCLUDED.marca;

  GET DIAGNOSTICS v_count = ROW_COUNT;
  RETURN v_count;
END;
$$;

-- Revogar acesso público; só service_role (edge functions) pode chamar
REVOKE ALL ON FUNCTION public.batch_upsert_orders(JSONB) FROM PUBLIC;
GRANT EXECUTE ON FUNCTION public.batch_upsert_orders(JSONB) TO service_role;
```

**Como aplicar:**
```bash
cd /root/garment-glow-test
npx supabase@2.100.1 db push --db-url "$SUPABASE_DB_URL"
```

Se `SUPABASE_DB_URL` não estiver disponível localmente, aplicar via Supabase SQL Editor ou usar:
```bash
npx supabase@2.100.1 migration up --db-url "postgresql://postgres.[PROJECT_ID]:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
```

O PROJECT_ID é `gionpsuunfkkzzjdubfy` (ver CLAUDE.md).

---

## TAREFA 2 — Atualizar `sync-ml-orders/index.ts`: substituir loop por batch

**Arquivo:** `supabase/functions/sync-ml-orders/index.ts`

### 2a. Localizar o bloco do loop N+1 (linhas 541–584)

```typescript
    let upserted = 0;
    if (records.length > 0) {
      // upsert_order_preserve_cost preserva custo_unit existente (não sobrescreve histórico)
      for (const r of records) {
        const { error: upsertErr } = await supabaseAdmin.rpc(
          "upsert_order_preserve_cost",
          {
            p_ml_order_id:     r.ml_order_id,
            // ... demais campos ...
          },
        );
        if (upsertErr) {
          console.warn(`upsert_order_preserve_cost failed for order ${r.ml_order_id}:`, upsertErr.message);
        } else {
          upserted++;
        }
      }
      console.log(`Upserted ${upserted}/${records.length} orders (cost preserved)`);
```

### 2b. Substituir por chamada batch

```typescript
    let upserted = 0;
    if (records.length > 0) {
      // Batch upsert via RPC — 1 round-trip para todos os pedidos do lote
      const { data: batchCount, error: batchErr } = await supabaseAdmin.rpc(
        "batch_upsert_orders",
        { p_records: JSON.stringify(records) },
      );

      if (batchErr) {
        console.error("batch_upsert_orders failed:", batchErr.message);
        // Fallback: marcar 0 upsertados mas continuar o fluxo
        upserted = 0;
      } else {
        upserted = (batchCount as number) ?? records.length;
      }
      console.log(`Batch upserted ${upserted}/${records.length} orders (cost preserved, 1 RPC)`);
```

**Importante:** Manter todo o código acima (construção de `records`, `expandOrder`, `costMap`, `brandMap`) sem alteração — apenas o bloco de upsert muda.

---

## TAREFA 3 — Deploy da edge function

```bash
cd /root/garment-glow-test
npx supabase@2.100.1 functions deploy sync-ml-orders --project-ref gionpsuunfkkzzjdubfy
```

---

## Verificação obrigatória

```bash
# 1. Sem erros de TypeScript
cd /root/garment-glow-test
npx tsc --noEmit

# 2. Confirmar que migration foi aplicada
# Via Supabase SQL Editor:
# SELECT proname FROM pg_proc WHERE proname = 'batch_upsert_orders';
# Deve retornar 1 row.

# 3. Smoke test manual (após deploy):
# Ir em /integracoes → clicar "Sincronizar" para a conta Thales
# Observar logs da edge function no Supabase Dashboard
# Deve aparecer: "Batch upserted X/X orders (cost preserved, 1 RPC)"
# NÃO deve aparecer: "upsert_order_preserve_cost failed"
```

## Commit

```
perf(sync): batch upsert de orders — 1 RPC ao invés de N

Substitui loop de upsert_order_preserve_cost (1 RPC/pedido) por
batch_upsert_orders que executa INSERT...ON CONFLICT em lote.
Para 200 pedidos: 200 round-trips → 1. Preserva custo_unit histórico.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

`git push origin main` após o commit.
