---
phase: 88-dre-frontend-resultado-completo-vendas
plan: 03
type: execute
wave: 3
depends_on: [88-02]
files_modified: []
autonomous: false
requirements: [DRE-88-04]

must_haves:
  truths:
    - "The production build succeeds (npm run build) with the new DRE section included"
    - "A Vercel preview of the branch is available for Wesley to open"
    - "Wesley confirms /vendas renders Resultado líquido in light + dark + mobile, and June/2026 shows ≈ −R$29k"
  artifacts:
    - path: ".planning/phases/88-dre-frontend-resultado-completo-vendas/88-03-SUMMARY.md"
      provides: "Record of the build result, preview URL, and Wesley's approval"
  key_links:
    - from: "preview build"
      to: "Wesley visual validation"
      via: "shared Vercel preview link → light/dark/mobile + June −R$29k check"
      pattern: "preview"
---

<objective>
Ship the DRE Resultado Completo to a Vercel preview and get Wesley's visual sign-off — the final acceptance gate for the milestone. This is the only place a human confirms what the unit tests already asserted mathematically: June/2026 ≈ −R$29k, and that the new section is legible in light + dark + mobile.

Purpose: Frontend phases close on a human visual check (project convention). The build must be green and the preview reachable before Wesley is asked to look.

Output: A green build, a preview URL, and a recorded approval in the SUMMARY.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-dre-frontend-resultado-completo-vendas/88-CONTEXT.md

Depends on Plan 02 (the UI section must be merged into the working branch).
Preview workflow reference: branch + Vercel preview + share link (project convention — see project memory reference_vercel_preview_workflow).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green build + push preview branch</name>
  <files></files>
  <read_first>
    - package.json (build script: `vite build`)
  </read_first>
  <action>
    Ensure deps are installed (`npm ci` if node_modules is missing). Run `npm run build` and confirm it exits 0 with the new DRE code compiled. Ensure the phase-88 branch is pushed so Vercel generates a preview deployment; capture the preview URL for the checkpoint below. Do NOT merge to main — merge waits for Wesley's approval in Task 2.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`npm run build` exits 0; branch pushed; Vercel preview URL captured for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Wesley visual validation of Resultado líquido (light/dark/mobile + June −R$29k)</name>
  <action>Present the Vercel preview URL to Wesley and walk him through the verification steps below. Do not merge to main until he replies "aprovado". If he reports issues, capture them for a gap-closure pass instead of merging.</action>
  <what-built>The /vendas "DRE do Mês" card now extends past "Lucro do mês" into (−) Pessoal / Estrutura / Serviços / Outros = Resultado operacional, then (−) Financeiro [aproximado ⓘ] = Resultado líquido. Values come from the deployed RPC get_dre_operational_by_competence, filtered to the five operational blocos (impostos_venda/excluido excluded), tracking the DRE card's own ‹ › month navigator.</what-built>
  <how-to-verify>
    1. Open the Vercel preview URL (from Task 1) and go to `/vendas`.
    2. Navigate the DRE card ‹ › to Junho/2026. Confirm Resultado líquido shows ≈ −R$29k (−R$29.094; margem +20.888 − pessoal 27.852 − financeiro 20.027 − serviços 1.953 − outros 150), colored as a negative (kpi-negative) with a down-trend icon.
    3. Confirm the row order reads: Lucro do mês → (−) Pessoal → Estrutura → Serviços → Outros → = Resultado operacional → (−) Financeiro → = Resultado líquido.
    4. Hover/tap the "aproximado" info icon on the Financeiro line — a tooltip explains juros não separados do principal (pendente tabela do banco).
    5. Toggle dark mode: confirm all rows + the positive/negative colors remain legible.
    6. Open on a phone (or narrow the window to mobile width): confirm the section stays readable — no overflow, values right-aligned, no broken layout (Phase 78 standard).
    7. Confirm the operational rows change month in lock-step with the margin rows when you use ‹ ›.
  </how-to-verify>
  <resume-signal>Wesley types "aprovado" (then the orchestrator may merge to main), or describes issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → preview deploy | compiled bundle served to Wesley's browser (no new runtime input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-05 | (validation gate) | June −R$29k reconciliation | mitigate | Human confirms the tested figure renders in the real UI before merge; a deviation from −R$29k signals a double-count or wrong-month regression to fix before shipping. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase. |
</threat_model>

<verification>
- `npm run build` exits 0.
- Preview URL opens `/vendas` with the new section.
- Wesley's "aprovado" recorded in the SUMMARY.
</verification>

<success_criteria>
- Green production build.
- Wesley confirms light + dark + mobile legibility and June ≈ −R$29k in the real UI.
- Approval (or fix list) recorded; merge to main only after approval.
</success_criteria>

<output>
Create `.planning/phases/88-dre-frontend-resultado-completo-vendas/88-03-SUMMARY.md` when done, including the preview URL and Wesley's decision.
</output>
