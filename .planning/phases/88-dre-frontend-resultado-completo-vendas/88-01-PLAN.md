---
phase: 88-dre-frontend-resultado-completo-vendas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dreOperational.ts
  - src/lib/dreOperational.test.ts
  - src/hooks/useDreOperational.ts
  - src/hooks/useDreOperational.test.ts
autonomous: true
requirements: [DRE-88-01, DRE-88-03, DRE-88-04]

must_haves:
  truths:
    - "The resultado-líquido math (margem − pessoal − estrutura − servicos − outros_operacionais − financeiro) is computed by one pure, tested function"
    - "The blocos impostos_venda and excluido are never included in the operational sum (would double-count and break the −R$29k June case)"
    - "financeiro_is_approximate is true when ANY financeiro row is approximate (derived via .some over financeiro rows)"
    - "The June/2026 fixture reconciles to ≈ −R$29k (−29.094)"
    - "The hook is disabled (no fetch) when orgId is null or month is null"
  artifacts:
    - path: "src/lib/dreOperational.ts"
      provides: "Pure aggregateOperationalBlocks + computeResultadoLiquido functions + DreOperationalRow/DreOperationalBlocks types"
      exports: ["aggregateOperationalBlocks", "computeResultadoLiquido", "DreOperationalRow", "DreOperationalBlocks", "OPERATIONAL_BLOCOS"]
      min_lines: 40
    - path: "src/lib/dreOperational.test.ts"
      provides: "Vitest coverage of the sum, the impostos_venda/excluido exclusion, the financeiro_is_approximate derivation, and the June −R$29k case"
      min_lines: 40
    - path: "src/hooks/useDreOperational.ts"
      provides: "react-query hook calling get_dre_operational_by_competence, reducing rows to DreOperationalBlocks"
      exports: ["useDreOperational"]
      min_lines: 30
    - path: "src/hooks/useDreOperational.test.ts"
      provides: "Vitest coverage of RPC args (p_org_id/p_month), enabled:false guards, and row aggregation through the hook"
      min_lines: 40
  key_links:
    - from: "src/hooks/useDreOperational.ts"
      to: "src/lib/dreOperational.ts"
      via: "hook imports aggregateOperationalBlocks to reduce RPC rows into DreOperationalBlocks"
      pattern: "aggregateOperationalBlocks"
    - from: "src/hooks/useDreOperational.ts"
      to: "supabase.rpc"
      via: "calls get_dre_operational_by_competence with p_org_id + p_month"
      pattern: "get_dre_operational_by_competence"
---

<objective>
Build the pure data + logic layer for the "Resultado líquido" DRE extension: a tested pure module (`src/lib/dreOperational.ts`) that filters/sums the operational blocos and computes resultado operacional + resultado líquido from an already-computed margin, plus a react-query hook (`src/hooks/useDreOperational.ts`) that calls the already-deployed RPC `get_dre_operational_by_competence` and reduces its rows through that pure module.

Purpose: Isolate the two failure-prone concerns of this milestone — (1) the double-count risk of `impostos_venda`/`excluido`, and (2) the resultado-líquido arithmetic — into a pure, unit-tested function BEFORE any UI is wired. The −R$29k June reconciliation is proven by a test, not by eyeballing the screen.

Output: Two new source files + two new vitest files. No UI changes in this plan.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-dre-frontend-resultado-completo-vendas/88-CONTEXT.md
@.planning/phases/88-dre-frontend-resultado-completo-vendas/88-RESEARCH.md

# Patterns to copy exactly:
@src/hooks/useCostByMonth.ts
@src/hooks/useMLClaims.test.ts

Note: if `node_modules` is missing (typescript/vitest not present), run `npm ci` once before the verify gates.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure dreOperational module + unit tests (June −R$29k proof)</name>
  <files>src/lib/dreOperational.ts, src/lib/dreOperational.test.ts</files>
  <read_first>
    - src/hooks/useCostByMonth.ts (row-mapping style: String()/Number() coercion, named-export function)
    - 88-RESEARCH.md "RPC row shape" (bloco/category/total/n/financeiro_is_approximate) and Pitfalls 1 & 4
    - 88-CONTEXT.md decisions (which blocos are used vs ignored; June validation math)
  </read_first>
  <behavior>
    - aggregateOperationalBlocks(rows): keeps ONLY blocos in OPERATIONAL_BLOCOS (pessoal, estrutura, servicos, outros_operacionais, financeiro); every returned key defaults to 0 even when no row exists for it; sums `total` per bloco.
    - aggregateOperationalBlocks excludes rows whose bloco is impostos_venda or excluido from every sum (allow-list, not deny-list).
    - financeiro_is_approximate = rows filtered to bloco==='financeiro' then .some(r => r.financeiro_is_approximate) (per-bloco intent, not incidental row order); false when there are no financeiro rows.
    - computeResultadoLiquido(margem, blocks): resultadoOperacional = margem − pessoal − estrutura − servicos − outros_operacionais; resultadoLiquido = resultadoOperacional − financeiro.
    - June/2026 case: margem 20888, pessoal 27852, financeiro 20027 (approximate), servicos 1953, outros_operacionais 150, estrutura 0 → resultadoLiquido rounds to ≈ −29094 (assert within ±1 or Math.round equality to -29094).
    - Exclusion proof: adding an impostos_venda row (e.g. 53000) and an excluido row (e.g. 9000) to the June fixture does NOT change resultadoLiquido.
  </behavior>
  <action>
    Create `src/lib/dreOperational.ts` as a pure module (no React, no supabase import). Define and export: type DreOperationalRow { bloco: string; category: string; total: number; n: number; financeiro_is_approximate: boolean }; interface DreOperationalBlocks { pessoal: number; estrutura: number; servicos: number; outros_operacionais: number; financeiro: number; financeiro_is_approximate: boolean }; a readonly OPERATIONAL_BLOCOS allow-list of the five wanted bloco names; aggregateOperationalBlocks(rows: DreOperationalRow[]): DreOperationalBlocks; and computeResultadoLiquido(margem: number, blocks: DreOperationalBlocks): { resultadoOperacional: number; resultadoLiquido: number }. Implement aggregate by initializing all five numeric keys to 0, iterating rows, skipping any bloco not in OPERATIONAL_BLOCOS, and adding Number(total ?? 0). Derive financeiro_is_approximate via the .some() rule above. Use named exports only (per CLAUDE.md — no default export).
    Create `src/lib/dreOperational.test.ts` (vitest) covering the six behaviors listed above, including a JUNE fixture matching 88-CONTEXT.md's validated numbers and the exclusion proof. Do NOT reference the RPC or supabase here — this is a pure-function test.
  </action>
  <verify>
    <automated>npx vitest run src/lib/dreOperational.test.ts</automated>
  </verify>
  <done>All tests in dreOperational.test.ts pass; the June fixture asserts resultadoLiquido ≈ −29094; the exclusion test proves impostos_venda/excluido rows do not affect the result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useDreOperational react-query hook + hook tests</name>
  <files>src/hooks/useDreOperational.ts, src/hooks/useDreOperational.test.ts</files>
  <read_first>
    - src/hooks/useCostByMonth.ts (EXACT hook shape to copy: useOrganization → orgId, useQuery, enabled:!!orgId, staleTime, supabase.rpc, row map)
    - src/hooks/useMLClaims.test.ts (test scaffold: QueryClientProvider wrapper, vi.mock for supabase + OrganizationContext, enabled-guard assertions)
    - src/lib/dreOperational.ts (aggregateOperationalBlocks + types created in Task 1)
  </read_first>
  <behavior>
    - useDreOperational(month: string | null) reads orgId from useOrganization().currentOrg?.id ?? null.
    - queryKey is ["dre","operational", orgId, month]; enabled is !!orgId && !!month; staleTime 3*60*1000 (mirrors useCostByMonth).
    - queryFn calls supabase.rpc("get_dre_operational_by_competence", { p_org_id: orgId, p_month: month }), throws on error, maps rows to DreOperationalRow[] (String/Number/Boolean coercion), and returns aggregateOperationalBlocks(rows).
    - Test: RPC is called with p_org_id === the mocked org id and p_month === the passed month string.
    - Test: hook is disabled (isLoading false, data undefined, rpc not called) when orgId is null.
    - Test: hook is disabled when month is null.
    - Test: given a mocked RPC resolving mixed rows (pessoal + financeiro approximate + impostos_venda + excluido), the hook's data equals the aggregated blocks with impostos_venda/excluido excluded and financeiro_is_approximate true.
  </behavior>
  <action>
    Create `src/hooks/useDreOperational.ts` copying the useCostByMonth.ts structure exactly, substituting the RPC name, args, queryKey, and returning `aggregateOperationalBlocks(mappedRows)` (typed as DreOperationalBlocks) instead of the raw array. Import aggregateOperationalBlocks + DreOperationalRow/DreOperationalBlocks from `@/lib/dreOperational`. Named export `useDreOperational`. If tsc rejects the new RPC name against generated Supabase types, mirror whatever the existing custom-RPC hooks in this repo already do (the sibling hooks call `.rpc("get_cost_by_month", …)` / `.rpc("get_cost_waterfall", …)` with the same shape and no cast — match that precedent; only add a minimal cast if tsc actually errors).
    Create `src/hooks/useDreOperational.test.ts` following the useMLClaims.test.ts scaffold but mocking `supabase.rpc` as a resolvable vi.fn (return `{ data, error: null }`) and mocking `@/contexts/OrganizationContext`. Cover the five test behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useDreOperational.test.ts</automated>
  </verify>
  <done>All useDreOperational.test.ts tests pass; the hook calls the RPC with session-derived p_org_id + the passed p_month, is disabled when either is null, and returns aggregated blocks with impostos_venda/excluido excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase RPC | orgId + month string cross into the DB RPC as p_org_id/p_month |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Elevation of Privilege (IDOR) | useDreOperational p_org_id | mitigate | orgId is always sourced from `useOrganization().currentOrg?.id` (session-derived), never from URL/user input; the RPC is SECURITY INVOKER + RLS-scoped on cash_outflows (verified anti-IDOR in Phase 87). Never accept an org id argument from a caller. |
| T-88-02 | Tampering (double-count) | aggregateOperationalBlocks | mitigate | Allow-list of five blocos filters out impostos_venda/excluido at the pure-function layer; unit-tested so a future refactor cannot silently reintroduce the double-count. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A — zero new deps). |
</threat_model>

<verification>
- `npx vitest run src/lib/dreOperational.test.ts src/hooks/useDreOperational.test.ts` — both suites green.
- `npx tsc --noEmit` — no new type errors introduced by the two new source files.
- Grep sanity: `grep -c "get_dre_operational_by_competence" src/hooks/useDreOperational.ts` ≥ 1; `grep -c "impostos_venda" src/lib/dreOperational.ts` — the allow-list approach means impostos_venda need not appear at all (allow-list names the wanted blocos).
</verification>

<success_criteria>
- Pure module computes resultado líquido and is proven ≈ −R$29k for June/2026 by a test.
- impostos_venda and excluido are provably excluded from the sum.
- Hook fetches the deployed RPC with session-derived org scope and reduces rows through the pure module.
- No UI touched; no new npm packages.
</success_criteria>

<output>
Create `.planning/phases/88-dre-frontend-resultado-completo-vendas/88-01-SUMMARY.md` when done.
</output>
