---
phase: 88-dre-frontend-resultado-completo-vendas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dreCascade.ts
  - src/lib/dreCascade.test.ts
  - src/hooks/useDreOperational.ts
  - src/hooks/useDreOperational.test.ts
  - src/components/mercadolivre/MLCostCard.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements: [SC-1, SC-2, SC-3]

must_haves:
  truths:
    - "Na /vendas, o card 'DRE do Mês' mostra a cascata completa: Margem de contribuição → Resultado operacional → Resultado líquido"
    - "O antigo subtotal 'Lucro do mês' passa a se chamar 'Margem de contribuição' (mesmo cálculo: receita − tarifas − CMV − impostos)"
    - "Cada bloco operacional (Pessoal, Estrutura, Serviços, Operacional, Não classificado) aparece como linha própria e visível"
    - "O bloco 'financeiro' (Empréstimo) desce depois do Resultado operacional para chegar ao Resultado líquido"
    - "O Resultado líquido tem sinal semântico verde/vermelho (TrendingUp/TrendingDown), como o subtotal atual"
    - "A linha operacional com double_count_risk (Cartão de crédito) exibe um tooltip discreto de aviso — não é escondida nem netada"
    - "Os blocos impostos_venda e excluido NUNCA são renderizados nem somados na cascata (evita dupla contagem do imposto)"
    - "Os números batem com a reconciliação de junho/2026 provada no backend da Phase 87 para o mesmo mês"
  artifacts:
    - path: "src/lib/dreCascade.ts"
      provides: "Função pura que agrupa as linhas da RPC 87 por bloco, exclui impostos_venda/excluido, soma deduções operacionais e financeiro, e deriva os três subtotais"
      exports: ["buildDreCascade", "OPERACIONAL_BLOCOS"]
      min_lines: 40
    - path: "src/lib/dreCascade.test.ts"
      provides: "Testes da cascata: exclusão de impostos_venda/excluido (SC-3), matemática dos subtotais, propagação de double_count_risk, fixture de reconciliação junho/2026"
    - path: "src/hooks/useDreOperational.ts"
      provides: "Hook TanStack Query que chama supabase.rpc('get_dre_operational_by_competence', {p_org_id, p_month}) escopado por currentOrg"
      exports: ["useDreOperational", "DreOperationalRow", "DreBloco"]
    - path: "src/hooks/useDreOperational.test.ts"
      provides: "Teste do hook com mock de supabase.rpc (não supabase.from) — estabelece o padrão de mock RPC no repo"
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "Card estendido com props dos blocos operacionais + financeiro e as novas linhas/subtotais"
    - path: "src/pages/MercadoLivre.tsx"
      provides: "Wiring do hook no mesmo eixo de mês do waterfall e passagem das novas props ao card"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useDreOperational.ts"
      via: "useDreOperational(billingMonthIsCurrentMonth ? monthlyFrom : billingMonthFrom) — mesmo eixo do useMLCostWaterfall"
      pattern: "useDreOperational\\("
    - from: "src/hooks/useDreOperational.ts"
      to: "get_dre_operational_by_competence (RPC Phase 87, prod)"
      via: "supabase.rpc com p_org_id=currentOrg.id, p_month=first-of-month"
      pattern: "get_dre_operational_by_competence"
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/lib/dreCascade.ts"
      via: "buildDreCascade(rows, margemContribuicao) antes de passar props ao MLCostCard"
      pattern: "buildDreCascade\\("
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/components/mercadolivre/MLCostCard.tsx"
      via: "novas props blocosOperacionais + financeiro + subtotais"
      pattern: "<MLCostCard"
---

<objective>
Estender o card "DRE do Mês" (`MLCostCard`) em `/vendas` para mostrar o **resultado completo**: hoje ele para em "Lucro do mês" (= receita − tarifas ML − CMV − impostos = margem de contribuição). A Phase 88 continua a cascata para baixo com os custos operacionais + financeiro vindos da RPC já em produção da Phase 87 (`get_dre_operational_by_competence`), chegando a **Resultado operacional** e **Resultado líquido**.

É pura extensão de frontend + wiring de um hook novo. NÃO mexer em RPC (86/87), na metodologia de receita/CMV/impostos (já correta, vinda de `orders`), nem em `get_cashflow`/DFC.

Purpose: dar ao Wesley visibilidade inline em `/vendas` do resultado do mês até a linha final (líquido), com Pessoal/Estrutura/Serviços/Operacional/Não classificado/Financeiro, reconciliado com os números do backend (Phase 87, delta R$0,00 em junho/2026).
Output: 1 função pura nova (`dreCascade.ts` + teste), 1 hook novo (`useDreOperational.ts` + teste), o `MLCostCard.tsx` estendido e o `MercadoLivre.tsx` religado.

DECISÃO DIFERIDA (hand-off ao Wesley, NÃO implementar nesta phase): o **toggle previsão × apuração** (trocar CMV médio↔cheio e imposto estimado↔guia real). CONTEXT manda "surface como pergunta, não decidir sozinho"; a pesquisa recomenda NÃO construir agora porque não é gratuito — exigiria religar `orders.custo_unit_cheio` e uma fonte de guia real, ambos FORA do escopo da RPC 87. Esta phase entrega só a cascata completa mantendo as fontes atuais do card (modo previsão). O toggle fica como follow-up para o Wesley decidir. Rationale numa linha: manter a phase estritamente aditiva e evitar creep para um pipeline de CMV cheio/guia real não pesquisado.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/88-dre-frontend-resultado-completo-vendas/88-CONTEXT.md
@.planning/phases/88-dre-frontend-resultado-completo-vendas/88-RESEARCH.md
@src/components/mercadolivre/MLCostCard.tsx
@src/pages/MercadoLivre.tsx
@src/hooks/useCostByMonth.ts
@src/hooks/useMLCostWaterfall.ts
@src/components/mercadolivre/ReplenishmentSkuTable.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Função pura dreCascade (grouping + guardrail anti dupla-contagem)</name>
  <files>src/lib/dreCascade.ts, src/lib/dreCascade.test.ts</files>
  <read_first>
    - src/hooks/useMLCostWaterfall.ts (shape das linhas numéricas + arredondamento Math.round(x*100)/100)
    - 88-RESEARCH.md seções "Pattern 3" e "Pitfall 1" (lista de blocos consumidos vs excluídos) e os números de reconciliação junho/2026
  </read_first>
  <behavior>
    - `OPERACIONAL_BLOCOS` = ["pessoal","estrutura","servicos","operacional","nao_classificado"] (as const).
    - `buildDreCascade(rows, margemContribuicao)` recebe as linhas cruas da RPC (bloco, category, total, n, double_count_risk) e a margem de contribuição já calculada pelo card; retorna:
        · `operacionalBlocos`: uma linha AGREGADA por bloco em OPERACIONAL_BLOCOS que tiver dados, na ordem fixa pessoal→estrutura→servicos→operacional→nao_classificado, cada uma com { bloco, label pt-BR, total (soma das categorias do bloco), doubleCountRisk (true se qualquer categoria do bloco tiver double_count_risk) };
        · `totalOperacionalDeducoes` = soma dos totais de operacionalBlocos;
        · `resultadoOperacional` = margemContribuicao − totalOperacionalDeducoes;
        · `financeiro`: linha agregada do bloco "financeiro" (label "Financeiro (Empréstimo)") ou null se ausente, com { total, doubleCountRisk };
        · `totalFinanceiro` = total do financeiro (0 se ausente);
        · `resultadoLiquido` = resultadoOperacional − totalFinanceiro.
    - Test 1 (SC-3, CRÍTICO): linhas com bloco "impostos_venda" e "excluido" NUNCA aparecem em operacionalBlocos nem em financeiro, e NÃO afetam nenhum dos totais/subtotais.
    - Test 2: matemática — resultadoOperacional = margem − Σ deduções; resultadoLiquido = resultadoOperacional − financeiro.
    - Test 3: double_count_risk propaga para doubleCountRisk do bloco correspondente (categoria "Cartão de crédito" em operacional).
    - Test 4: nao_classificado com dados sai como linha própria visível (não somado em operacional).
    - Test 5: fixture reconciliação junho/2026 — dado o conjunto {excluido 139968, pessoal 27852, financeiro 20027, operacional 15715 (double_count_risk=true), nao_classificado 7360, impostos_venda 4793, servicos 2103}, totalOperacionalDeducoes = 27852+2103+15715+7360 = 53030 e totalFinanceiro = 20027 (impostos_venda e excluido fora).
  </behavior>
  <action>
    Criar `src/lib/dreCascade.ts` como módulo puro (sem React/Supabase), seguindo a convenção de helpers puros do repo (ex.: precoFaixas.ts, soldProductsMcoAgg.ts). Definir e exportar `OPERACIONAL_BLOCOS` e a função `buildDreCascade(rows, margemContribuicao)` com o comportamento descrito em <behavior>. Mapear label pt-BR por bloco: pessoal→"Pessoal", estrutura→"Estrutura", servicos→"Serviços", operacional→"Operacional", nao_classificado→"Não classificado", financeiro→"Financeiro (Empréstimo)". Consumir SEMPRE o campo `bloco` das linhas — NUNCA re-derivar bloco a partir de `category` no frontend (o mapa categoria→bloco é fonte única no backend/Phase 87). Filtrar explicitamente impostos_venda e excluido logo na entrada, antes de qualquer soma, para que nunca contaminem os subtotais (Pitfall 1 do research). Usar arredondamento consistente com o resto do card (inteiros no display; manter precisão numérica no cálculo). Tipar as linhas de entrada reusando o tipo exportado pelo hook da Task 2 (importar `DreOperationalRow`/`DreBloco`) OU definir o tipo aqui e reexportar — escolher para não criar dependência circular (preferir tipos no hook e importar aqui). Escrever `src/lib/dreCascade.test.ts` cobrindo os 5 casos de <behavior>.
  </action>
  <verify>
    <automated>npx vitest run src/lib/dreCascade.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Testes de dreCascade verdes (5 casos, incluindo o guardrail de exclusão de impostos_venda/excluido e a fixture junho/2026); tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook useDreOperational (RPC 87, escopado por org)</name>
  <files>src/hooks/useDreOperational.ts, src/hooks/useDreOperational.test.ts</files>
  <read_first>
    - src/hooks/useCostByMonth.ts (padrão exato de hook RPC — clonar shape: queryKey, enabled:!!orgId, staleTime, queryFn com supabase.rpc + map)
    - src/hooks/useMLClaims.test.ts (shape do mock vi.mock de supabase/client e OrganizationContext — mas usar rpc, NÃO from/select, ver Pitfall 4)
    - 88-RESEARCH.md "Pattern 1" e "Pitfall 3/4"
  </read_first>
  <behavior>
    - `useDreOperational(pMonth: string)` usa `useOrganization().currentOrg?.id`, `enabled: !!orgId && !!pMonth`, `staleTime` de alguns minutos, `queryKey: ["dre","operational", orgId, pMonth]`.
    - queryFn chama `supabase.rpc("get_dre_operational_by_competence", { p_org_id: orgId, p_month: pMonth })`, lança em erro, e mapeia cada linha para `DreOperationalRow` { bloco (DreBloco), category (string), total (number), n (number), double_count_risk (boolean) }.
    - Exporta os tipos `DreBloco` (union dos 8 blocos: impostos_venda|pessoal|estrutura|servicos|operacional|financeiro|excluido|nao_classificado) e `DreOperationalRow`.
    - Test 1: chama supabase.rpc com exatamente {p_org_id, p_month} corretos e mapeia o shape das linhas (mock de rpc retornando {data:[...], error:null}).
    - Test 2 (error path): rpc retornando {data:null, error:{...}} faz a query rejeitar/entrar em erro.
    - Test 3: sem orgId, hook fica disabled (não chama rpc).
  </behavior>
  <action>
    Criar `src/hooks/useDreOperational.ts` clonando o padrão de `useCostByMonth.ts` verbatim, trocando a chamada para `supabase.rpc("get_dre_operational_by_competence", { p_org_id, p_month })`. Documentar no JSDoc que `p_month` DEVE ser uma string de primeiro-dia-do-mês ("YYYY-MM-01") — nunca "YYYY-MM" (Pitfall 3: cast de date do Postgres falha). Exportar `DreBloco` e `DreOperationalRow`. Escrever `src/hooks/useDreOperational.test.ts` estabelecendo o padrão de mock de `supabase.rpc` no repo: `vi.mock("@/integrations/supabase/client", ...)` expondo `rpc: vi.fn()` (NÃO o chain from/select/eq — ver Pitfall 4) e `vi.mock("@/contexts/OrganizationContext", ...)` retornando um currentOrg com id fixo; usar `renderHook` + `waitFor` e asserir os args exatos da chamada rpc. Cobrir os 3 casos de <behavior>.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useDreOperational.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Hook chama a RPC com {p_org_id, p_month} corretos, mapeia as linhas, respeita enabled; testes verdes (incl. error path e disabled sem org); tsc limpo. Mock de supabase.rpc estabelecido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Estender MLCostCard + religar MercadoLivre.tsx (cascata visível)</name>
  <files>src/components/mercadolivre/MLCostCard.tsx, src/pages/MercadoLivre.tsx</files>
  <read_first>
    - src/components/mercadolivre/MLCostCard.tsx (bloco "Lucro do mês" linhas ~244-264 = shape do subtotal a clonar/renomear; helper fmt/pct linhas 6-10; grupos de tarifas linhas ~158-194 = shape da linha "(−)" a clonar)
    - src/pages/MercadoLivre.tsx (linhas 195-266 = billingMonthFrom/monthlyFrom/billingMonthIsCurrentMonth/receitaMes/cmvMes/impostosMes; render do card linhas 773-793; currentOrg linha 96)
    - src/components/mercadolivre/ReplenishmentSkuTable.tsx (padrão Tooltip + HelpCircle a reusar verbatim)
  </read_first>
  <behavior>
    - "Lucro do mês" é RENOMEADO para "Margem de contribuição" (mesmo cálculo atual de `lucro`), mantendo TrendingUp/TrendingDown e as classes atuais.
    - Depois da Margem: N linhas "(−)" dos blocos operacionais (Pessoal, Estrutura, Serviços, Operacional, Não classificado), cada uma com % sobre receita e valor, no shape das linhas de tarifas existentes.
    - Subtotal "Resultado operacional" (border-t-2) = Margem − deduções operacionais.
    - Linha "(−) Financeiro (Empréstimo)".
    - Subtotal final "Resultado líquido" (border-t-2, maior/mais forte, sinal verde/vermelho via text-kpi-positive/negative + TrendingUp/Down) = Resultado operacional − financeiro.
    - A linha "Operacional" com doubleCountRisk mostra um HelpCircle + Tooltip discreto ("Pode conter fatura ML já contabilizada na margem"); as demais não.
    - "Não classificado" é sempre linha própria visível quando tiver valor.
    - Quando não há linhas operacionais (RPC vazia/loading), a cascata nova não quebra: o card continua exibindo até a Margem de contribuição sem as linhas novas (degradação graciosa).
  </behavior>
  <action>
    MLCostCard.tsx: estender `MLCostCardProps` com as novas props derivadas da cascata (ex.: `blocosOperacionais: Array<{bloco,label,total,doubleCountRisk}>`, `resultadoOperacional: number`, `financeiro: {total,doubleCountRisk} | null`, `resultadoLiquido: number`) — nomes à sua escolha (o research os trata como ilustrativos). Renomear o rótulo do subtotal atual de "Lucro do mês" para "Margem de contribuição" (NÃO mudar o cálculo de `lucro`/`margemPct`). Após esse subtotal, renderizar as linhas dos blocos operacionais clonando o shape da linha de tarifas (span "(−)" + label + pct(valor,receitaMes) + fmt(valor)), depois o subtotal "Resultado operacional" clonando o shape do subtotal atual (border-t-2), depois a linha "(−) Financeiro (Empréstimo)", e por fim o subtotal "Resultado líquido" clonando o subtotal atual mas com peso visual maior (ex.: manter text-base font-bold) e o sinal verde/vermelho baseado em resultadoLiquido>=0. Dar hierarquia visual entre os três subtotais (Margem = subtotal médio; Operacional = subtotal; Líquido = o mais destacado). Para a linha Operacional com doubleCountRisk, importar `HelpCircle` e Tooltip/TooltipContent/TooltipProvider/TooltipTrigger e inserir o ícone+tooltip inline no label, exatamente como em ReplenishmentSkuTable.tsx. Reusar `fmt`/`pct` já existentes; nada de novo formatter. Manter tokens light/dark, tabular-nums e o layout mobile (padrão Phase 78). Se `blocosOperacionais` vier vazio, não renderizar as linhas novas (guarda de degradação graciosa).

    MercadoLivre.tsx: importar `useDreOperational` e `buildDreCascade`. Chamar `useDreOperational(billingMonthIsCurrentMonth ? monthlyFrom : billingMonthFrom)` — MESMO ternário/eixo de mês já usado no `useMLCostWaterfall` (linhas 227-230), garantindo paridade de eixo calendário; NUNCA passar `billingMonth` cru (Pitfall 3). Calcular `margemContribuicao` = mesmo valor de `lucro` do card (receitaMes − totalTarifasEfetivo − (cmvMes ?? 0) − (impostosMes ?? 0)) via useMemo, e passar as linhas da RPC + essa margem para `buildDreCascade`. Passar o resultado da cascata como as novas props do `<MLCostCard>` no site de render (linhas 773-793). currentOrg já está disponível (linha 96) — o hook resolve a org internamente, então não é preciso passar org. Não tocar em nenhuma outra prop existente do card.

    Opcional/discricionário (só se trivial e sem custo de escopo): o bloco `excluido` num "ver detalhes" colapsado — CONTEXT marca como opcional; NÃO é requisito de SC-1/2/3. Preferir NÃO incluir em v1.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Card mostra Margem de contribuição → linhas operacionais (incl. Não classificado visível) → Resultado operacional → Financeiro (Empréstimo) → Resultado líquido (verde/vermelho); tooltip discreto na linha Operacional com doubleCountRisk; impostos_venda/excluido nunca aparecem; suíte completa verde (baseline 526+ testes) e tsc limpo; build ok.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase RPC | frontend envia p_org_id (currentOrg.id) para `get_dre_operational_by_competence` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Information Disclosure | useDreOperational → get_dre_operational_by_competence | accept | IDOR já mitigado server-side na Phase 87 (RPC SECURITY INVOKER + RLS is_org_member, provado com role authenticated de outra org → 0 linhas). Esta phase apenas adiciona um consumidor reusando currentOrg.id do OrganizationContext, idêntico a useCostByMonth/useMLCostWaterfall — nenhuma nova fronteira de confiança. Frontend não re-filtra por org (defesa redundante mascararia regressão de RLS). |
| T-88-02 | Tampering (integridade de dado financeiro) | dreCascade grouping | mitigate | Filtrar impostos_venda e excluido na entrada de buildDreCascade antes de qualquer soma (Pitfall 1); incluir/somar impostos_venda dupla-contaria o imposto que o card já subtrai em impostosMes. Coberto por teste dedicado (Test 1 da Task 1, SC-3). |

Sem instalação de pacotes nesta phase (zero dependências novas) — sem checkpoint de legitimidade de pacote.
</threat_model>

<verification>
- `npx tsc --noEmit` limpo em todo o repo.
- `npx vitest run` — suíte completa verde (baseline confirmado no research: 526 testes / 37 arquivos) + os 2 novos arquivos de teste (dreCascade, useDreOperational).
- `npm run build` (ou `npx vite build`) sem erros.
- Guardrail SC-3: teste de dreCascade prova que impostos_venda e excluido nunca entram em nenhum subtotal.
- Paridade de eixo: `useDreOperational` recebe o MESMO ternário de mês (monthlyFrom/billingMonthFrom) do `useMLCostWaterfall`, garantindo que as linhas novas estão no mesmo mês-calendário de receita/CMV/impostos (Pitfall 2 é pré-existente e fora de escopo).
- Reconciliação: para junho/2026, os subtotais devem bater com a Phase 87 (delta R$0,00) — validado por fixture no teste e, no ambiente real, pela validação visual do Wesley.
- Validação visual final é do Wesley EM PRODUÇÃO (deploy via Vercel a partir de `main`, frontend apenas — SEM MCP, SEM migration, SEM edge function). Conferir `/vendas` em light + dark e a cascata completa até o Resultado líquido no mês de junho/2026.
</verification>

<success_criteria>
- [ ] SC-1: cascata renderizada em ordem — Margem de contribuição → (Pessoal/Estrutura/Serviços/Operacional/Não classificado) → Resultado operacional → Financeiro (Empréstimo) → Resultado líquido.
- [ ] SC-2: consistência visual com o card atual (fmt BRL/toLocaleString, tabular-nums, tokens light/dark, mobile Phase 78); três subtotais com hierarquia visual; sinal verde/vermelho no líquido.
- [ ] SC-3: imposto sobre venda NÃO duplicado — bloco impostos_venda (e excluido) nunca renderizado nem somado na cascata.
- [ ] "Não classificado" visível como linha própria; linha Operacional com double_count_risk exibe tooltip discreto (não escondida, não netada).
- [ ] Números batem com a reconciliação junho/2026 da Phase 87.
- [ ] tsc limpo, vitest completo verde (incl. 2 testes novos), build ok.
- [ ] DECISÃO DIFERIDA registrada: toggle previsão×apuração NÃO implementado nesta phase (hand-off ao Wesley) — card mantém as fontes atuais (modo previsão).
</success_criteria>

<output>
Create `.planning/phases/88-dre-frontend-resultado-completo-vendas/88-01-SUMMARY.md` when done.
</output>
