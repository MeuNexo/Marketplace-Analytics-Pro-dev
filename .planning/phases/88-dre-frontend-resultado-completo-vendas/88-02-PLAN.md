---
phase: 88-dre-frontend-resultado-completo-vendas
plan: 02
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - src/pages/MercadoLivre.tsx
  - src/components/mercadolivre/MLCostCard.tsx
autonomous: true
requirements: [DRE-88-01, DRE-88-02, DRE-88-03]

must_haves:
  truths:
    - "The /vendas 'DRE do Mês' card renders (−) Pessoal, Estrutura, Serviços, Outros below 'Lucro do mês', then = Resultado operacional, then (−) Financeiro, then = Resultado líquido"
    - "The operational section uses the SAME month the rest of the card shows (billingMonthFrom, the DRE's own ‹ › navigator — not the page date-range filter)"
    - "Resultado líquido reuses the margin already computed in MLCostCard (existing `lucro`) — the margin is not re-derived from a second data source"
    - "The Financeiro line shows an 'aproximado' info tooltip when financeiro_is_approximate is true"
    - "Resultado líquido sign uses text-kpi-positive/text-kpi-negative (light+dark) and the single responsive JSX tree renders correctly on mobile"
  artifacts:
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "New MLCostCardProps fields + operational section JSX + Resultado operacional/líquido rows + Financeiro 'aproximado' Popover"
      contains: "Resultado líquido"
    - path: "src/pages/MercadoLivre.tsx"
      provides: "useDreOperational(billingMonthFrom) call + passing dreOperational/dreOperationalLoading props into MLCostCard"
      contains: "useDreOperational"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useDreOperational.ts"
      via: "calls useDreOperational(billingMonthFrom) and threads result into MLCostCard"
      pattern: "useDreOperational\\("
    - from: "src/components/mercadolivre/MLCostCard.tsx"
      to: "src/lib/dreOperational.ts"
      via: "computeResultadoLiquido(lucro, dreOperational) drives the Resultado líquido row"
      pattern: "computeResultadoLiquido"
---

<objective>
Wire the tested data layer (Plan 01) into the existing `/vendas` "DRE do Mês" card. `MercadoLivre.tsx` calls `useDreOperational(billingMonthFrom)` and passes the operational blocks into `MLCostCard`, which appends a new section after "Lucro do mês": (−) Pessoal / Estrutura / Serviços / Outros = Resultado operacional, then (−) Financeiro [aproximado ⓘ] = Resultado líquido.

Purpose: Deliver the milestone's visible outcome — the DRE now shows the real bottom line, using the exact same month + the exact same margin already on screen (no re-derivation, no double-count).

Output: Two modified files. No new files.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-dre-frontend-resultado-completo-vendas/88-CONTEXT.md
@.planning/phases/88-dre-frontend-resultado-completo-vendas/88-RESEARCH.md

# Files being modified + the patterns they must mirror:
@src/components/mercadolivre/MLCostCard.tsx
@src/components/dashboard/KPICard.tsx

Depends on Plan 01 artifacts: `src/lib/dreOperational.ts` (computeResultadoLiquido, DreOperationalBlocks) and `src/hooks/useDreOperational.ts`.
Note: if `node_modules` is missing, run `npm ci` once before the verify gates.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Call useDreOperational in MercadoLivre.tsx and pass props to MLCostCard</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <read_first>
    - src/pages/MercadoLivre.tsx lines 181–290 (billingMonth/billingMonthFrom derivation + existing MLCostCard inputs) and lines 773–793 (the MLCostCard render site)
    - src/hooks/useDreOperational.ts (Plan 01 — hook signature useDreOperational(month))
    - 88-RESEARCH.md Pattern 2 (month must come from billingMonthFrom, never currentFrom/currentTo)
  </read_first>
  <action>
    Import `useDreOperational` from `@/hooks/useDreOperational`. Near the existing DRE derivations (around line 227, alongside `filterMonthWaterfall`), call `const { data: dreOperational, isLoading: dreOperationalLoading } = useDreOperational(billingMonthFrom);` — pass `billingMonthFrom` (the already-computed first-of-month string, DRE-88-02), NOT `currentFrom`/`currentTo`. At the MLCostCard render site (around line 775), add two props: `dreOperational={dreOperational ?? null}` and `dreOperationalLoading={dreOperationalLoading}`. Do not add any new margin computation here — MLCostCard already computes `lucro` internally and will reuse it (per the "não re-derivar a margem" decision).
  </action>
  <verify>
    <automated>grep -c "useDreOperational(billingMonthFrom)" src/pages/MercadoLivre.tsx</automated>
  </verify>
  <done>MercadoLivre.tsx calls useDreOperational with billingMonthFrom and passes dreOperational + dreOperationalLoading into MLCostCard; grep returns ≥ 1.</done>
</task>

<task type="auto">
  <name>Task 2: Render the operational section + Resultado líquido + Financeiro badge in MLCostCard</name>
  <files>src/components/mercadolivre/MLCostCard.tsx</files>
  <read_first>
    - src/components/mercadolivre/MLCostCard.tsx (full file — existing fmt/pct helpers, the "Impostos próprios" row lines 220–242 as the row template, the "Lucro do mês" row lines 244–264 as the total-row template, `lucro` at line 68)
    - src/components/dashboard/KPICard.tsx lines 69, 96–119 (Popover + HelpCircle "info tooltip" pattern to copy for 'aproximado')
    - src/lib/dreOperational.ts (computeResultadoLiquido, DreOperationalBlocks)
  </read_first>
  <action>
    Extend `MLCostCardProps` with `dreOperational?: DreOperationalBlocks | null` and `dreOperationalLoading?: boolean` (import the type from `@/lib/dreOperational`). Destructure both in the component signature (default `dreOperational = null`). After the existing "Lucro do mês" block (closes at line 264), append a new section rendered only when `dreOperational` is present. Compute totals with the tested pure function: `const { resultadoOperacional, resultadoLiquido } = computeResultadoLiquido(lucro, dreOperational);` — reuse the existing `lucro` var (do NOT recompute the margin, do NOT call any waterfall hook again).
    Render, using the file-local `fmt()`/`pct()` helpers and the existing row JSX shape (muted label + (−) glyph + right-aligned pct chip + right-aligned bold value), one row each for: (−) Pessoal, (−) Estrutura, (−) Serviços, (−) Outros — values `dreOperational.pessoal/estrutura/servicos/outros_operacionais` (render R$ 0 honestly when a bloco is 0; no special empty state per CONTEXT scope). Then a "= Resultado operacional" subtotal row (copy the "Total de tarifas ML" row shape lines 180–193, `=` glyph). Then a "(−) Financeiro" row that, when `dreOperational.financeiro_is_approximate`, shows a small "aproximado" info trigger next to the label using the KPICard Popover+HelpCircle pattern (state via useState, tooltip copy explaining juros não separados do principal — pendente tabela do banco). Finally a "= Resultado líquido" total row copying the "Lucro do mês" row structure exactly (TrendingUp/TrendingDown swap on `resultadoLiquido >= 0`, `text-kpi-positive`/`text-kpi-negative` classes). When `dreOperationalLoading` and no data yet, render a couple of pulse skeleton rows in this section (mirror the existing skeleton style) rather than blank. Keep everything inside the single responsive JSX tree — do NOT add a mobile-only branch (RESEARCH anti-pattern: MLCostCard is a single Tailwind-breakpoint tree).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>tsc passes; MLCostCard renders Pessoal/Estrutura/Serviços/Outros, Resultado operacional, Financeiro (with 'aproximado' tooltip when approximate), and Resultado líquido with kpi-positive/negative coloring, all inside one responsive tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client state → MLCostCard props | dreOperational blocks (RLS-scoped) rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03 | Tampering (wrong month) | MercadoLivre.tsx → useDreOperational | mitigate | p_month sourced from `billingMonthFrom` (the DRE card's own navigator), the same value feeding the margin rows — guarantees the operational section and the margin above it are the same competence month (DRE-88-02). |
| T-88-04 | Information Disclosure (double-count leak) | MLCostCard Resultado líquido | mitigate | Base margin reuses the existing `lucro`; operational deductions come only from the five allow-listed blocos (Plan 01) — impostos_venda/excluido cannot re-enter here. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean.
- `npx vitest run` — full suite still green (Plan 01 tests + existing).
- Manual grep: `grep -c "computeResultadoLiquido" src/components/mercadolivre/MLCostCard.tsx` ≥ 1; `grep -c "Resultado líquido" src/components/mercadolivre/MLCostCard.tsx` ≥ 1; `grep -c "text-kpi-negative" src/components/mercadolivre/MLCostCard.tsx` ≥ 1.
- Confirm no second call to `useMLCostWaterfall` was added for the operational section (margin reused, not re-derived).
</verification>

<success_criteria>
- The DRE card shows the full waterfall down to Resultado líquido.
- Operational rows track the ‹ › month navigator in lock-step with the margin rows.
- Financeiro shows the 'aproximado' tooltip when the RPC flags it.
- Sign colors work in light and dark; layout holds on mobile (single responsive tree).
- tsc + vitest pass.
</success_criteria>

<output>
Create `.planning/phases/88-dre-frontend-resultado-completo-vendas/88-02-SUMMARY.md` when done.
</output>
