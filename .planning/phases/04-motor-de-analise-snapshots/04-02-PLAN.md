---
phase: 04-motor-de-analise-snapshots
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260515200000_commercial_analysis_snapshots.sql
autonomous: false
requirements:
  - HIST-01

must_haves:
  truths:
    - "A tabela commercial_analysis_snapshots existe no banco Supabase com todas as colunas especificadas"
    - "RLS permite SELECT/INSERT apenas para membros autenticados da organização"
    - "O schema push executa sem erros"
  artifacts:
    - path: "supabase/migrations/20260515200000_commercial_analysis_snapshots.sql"
      provides: "DDL da tabela commercial_analysis_snapshots com RLS"
      contains: "CREATE TABLE commercial_analysis_snapshots"
  key_links:
    - from: "supabase/migrations/20260515200000_commercial_analysis_snapshots.sql"
      to: "Supabase remote database"
      via: "supabase db push"
      pattern: "commercial_analysis_snapshots"
---

<objective>
Criar a migration SQL que define a tabela `commercial_analysis_snapshots` no Supabase e executar
o push para o banco remoto. Esta tabela é a infraestrutura de persistência consumida pelo hook
de snapshots (Plan 03) e por todas as fases seguintes.

Purpose: Sem esta tabela no banco, o hook useAnalysisSnapshots não consegue salvar nem buscar
análises — bloqueia Plans 03 e todas as fases downstream.

Output: Arquivo de migration SQL + tabela presente no banco Supabase remoto.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Schema que o hook (Plan 03) vai usar para INSERT/SELECT -->

Tabela: commercial_analysis_snapshots

Colunas obrigatórias:
  id                UUID PRIMARY KEY DEFAULT gen_random_uuid()
  created_at        TIMESTAMPTZ NOT NULL DEFAULT now()
  ml_user_id        TEXT NOT NULL REFERENCES ml_tokens(ml_user_id) ON DELETE CASCADE
  organization_id   UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE
  item_id           TEXT NOT NULL
  product_title     TEXT NOT NULL
  brand             TEXT
  period_start      DATE NOT NULL
  period_end        DATE NOT NULL
  price_curve       JSONB NOT NULL      -- array de PriceBucket
  price_gmv         NUMERIC(10,2) NOT NULL
  price_neutral     NUMERIC(10,2) NOT NULL
  price_margin      NUMERIC(10,2) NOT NULL
  elasticity_pct    NUMERIC(8,4) NOT NULL
  elasticity_class  TEXT NOT NULL CHECK (elasticity_class IN ('baixa','media','alta','extrema'))
  strategy          TEXT CHECK (strategy IN ('gmv','neutral','margin'))  -- nullable

Índices úteis:
  (organization_id, item_id, created_at DESC)
  (ml_user_id, item_id)

RLS:
  SELECT: autenticado + is_org_member(organization_id)   (mesma função usada em ml_tax_config)
  INSERT: autenticado + is_org_member(organization_id)
  UPDATE: autenticado + is_org_member(organization_id)   (para setar strategy depois)
  DELETE: não necessário (snapshots são append-only; owner pode deletar se quiser)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration SQL da tabela commercial_analysis_snapshots</name>
  <files>supabase/migrations/20260515200000_commercial_analysis_snapshots.sql</files>

  <read_first>
    - supabase/migrations/20260515120000_ml_tax_config.sql — padrão de migration com RLS, helper is_org_member, referências a organizations e ml_tokens
    - supabase/migrations/20260515133645_74fe9eff-0844-43a7-b4fc-7fef029b4916.sql — ver se há funções auxiliares que podem ser reutilizadas
  </read_first>

  <action>
    Criar supabase/migrations/20260515200000_commercial_analysis_snapshots.sql contendo:

    1. CREATE TABLE commercial_analysis_snapshots com as colunas listadas na seção interfaces acima.
       Usar NUMERIC(10,2) para preços e NUMERIC(8,4) para elasticity_pct.
       Coluna strategy é nullable (sem NOT NULL).
       CHECK constraint em elasticity_class e strategy conforme interfaces.

    2. CREATE INDEX em (organization_id, item_id, created_at DESC) e em (ml_user_id, item_id).

    3. ALTER TABLE ENABLE ROW LEVEL SECURITY.

    4. Políticas RLS (seguir exatamente o padrão das migrations existentes):
       - SELECT: "Members can view own org snapshots"
         USING (is_org_member(organization_id))  ou equivalente com auth.uid()
       - INSERT: "Members can insert own org snapshots"
         WITH CHECK (is_org_member(organization_id))
       - UPDATE: "Members can update strategy on own org snapshots"
         USING (is_org_member(organization_id))
         WITH CHECK (is_org_member(organization_id))

    Inspecionar as migrations existentes para descobrir se is_org_member já existe e qual é
    sua assinatura exata — usar a mesma chamada usada em ml_tax_config.sql.
    Não inventar funções; reutilizar o que existe.
  </action>

  <verify>
    <automated>grep -c "commercial_analysis_snapshots" /c/Users/thales.martins/AppData/Local/Temp/garment-glow-app-0cc0fcfc/supabase/migrations/20260515200000_commercial_analysis_snapshots.sql</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo supabase/migrations/20260515200000_commercial_analysis_snapshots.sql existe
    - Contém CREATE TABLE commercial_analysis_snapshots com todas as colunas especificadas
    - Contém ALTER TABLE ENABLE ROW LEVEL SECURITY
    - Contém ao menos 2 políticas RLS (SELECT e INSERT)
    - Contém ao menos 1 CREATE INDEX
    - Sintaxe SQL válida (sem erros de linting óbvios)
  </acceptance_criteria>

  <done>Arquivo de migration criado com DDL completo, índices e RLS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>[BLOCKING] Task 2: Executar supabase db push para aplicar a migration</name>

  <what-built>
    Migration SQL criada em Task 1 com a tabela commercial_analysis_snapshots completa.
    O push envia a migration para o banco Supabase remoto do projeto.
  </what-built>

  <how-to-verify>
    1. Execute no terminal (no diretório raiz do projeto):
       ```
       supabase db push
       ```
       Se solicitar login interativo, autentique com:
       ```
       supabase login
       supabase db push
       ```

    2. Confirme que a saída inclui a migration 20260515200000_commercial_analysis_snapshots.sql
       sem erros.

    3. Verifique no Supabase Dashboard (Table Editor) que a tabela
       `commercial_analysis_snapshots` aparece com todas as colunas esperadas:
       id, created_at, ml_user_id, organization_id, item_id, product_title, brand,
       period_start, period_end, price_curve, price_gmv, price_neutral, price_margin,
       elasticity_pct, elasticity_class, strategy.

    4. Confirme que RLS está habilitado na tabela (Dashboard → Authentication → Policies
       ou Table Editor → commercial_analysis_snapshots → RLS).
  </how-to-verify>

  <resume-signal>
    Digite "aprovado" se a tabela foi criada com sucesso no banco.
    Descreva o erro se o push falhar.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| app → Supabase RLS | Todos os acessos à tabela passam pela policy RLS; sem bypass possível via API anon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | commercial_analysis_snapshots RLS | mitigate | RLS com is_org_member garante isolamento por organização; validar que a mesma função usada em ml_tax_config é aplicada aqui |
| T-04-05 | Tampering | price_curve JSONB | accept | Dado inserido via hook autenticado; formato é validado pela camada TypeScript antes do INSERT; sem input direto do usuário externo |
| T-04-06 | Information Disclosure | SELECT policy | mitigate | Policy USING(is_org_member) garante que membros de org A não acessam snapshots de org B |
</threat_model>

<verification>
- supabase/migrations/20260515200000_commercial_analysis_snapshots.sql existe com DDL completo
- `supabase db push` executa sem erros (verificado pelo usuário via checkpoint)
- Tabela commercial_analysis_snapshots visível no Supabase Dashboard com RLS habilitado
</verification>

<success_criteria>
Tabela commercial_analysis_snapshots existe no banco remoto Supabase com todas as colunas
especificadas, índices criados e RLS habilitado com políticas de isolamento por organização.
</success_criteria>

<output>
Após conclusão, criar .planning/phases/04-motor-de-analise-snapshots/04-02-SUMMARY.md
</output>
