---
phase: 71-modal-de-detalhe-do-an-ncio-shell-indicadores
plan: 02
type: execute
wave: 2
depends_on:
  - 71-01
files_modified:
  - src/pages/mercadolivre/MLAnuncios.tsx
autonomous: true
requirements:
  - ADM-71

must_haves:
  truths:
    - "Clicar na miniatura de qualquer anúncio abre o modal com os dados daquele anúncio"
    - "Clicar no ícone 'Ver detalhes' (Eye) da linha abre o modal com os dados daquele anúncio"
    - "O expandir-variações (toggleRow) e o link do título para o ML continuam funcionando sem conflito de clique"
    - "A margem exibida no modal vem dos hooks que a página já usa (marginByItem), passada por prop — sem nova fonte"
    - "A página não infla: helpers de listing são importados de listingHelpers.ts (sem duplicação) e o modal vive em arquivo próprio"
  artifacts:
    - path: "src/pages/mercadolivre/MLAnuncios.tsx"
      provides: "Estado de abertura + gatilho (miniatura + ícone) + render do ListingDetailModal; helpers importados do módulo compartilhado"
      contains: "ListingDetailModal"
  key_links:
    - from: "src/pages/mercadolivre/MLAnuncios.tsx"
      to: "src/components/mercadolivre/anuncios/ListingDetailModal.tsx"
      via: "renderiza <ListingDetailModal item={selectedItem} open={modalOpen} onOpenChange={...} margin={marginByItem.get(selectedItem.id)} />"
      pattern: "ListingDetailModal"
    - from: "src/pages/mercadolivre/MLAnuncios.tsx"
      to: "src/components/mercadolivre/anuncios/listingHelpers.ts"
      via: "importa getCommissionRate/getListingLabel/currencyFmt/mlListingUrl do módulo compartilhado"
      pattern: "listingHelpers"
---

<objective>
Ligar o modal de detalhe na página de catálogo `MLAnuncios.tsx`: adicionar estado de abertura, os dois gatilhos (clique na miniatura + ícone "Ver detalhes") preservando o expandir-variações e o link do título, renderizar o `<ListingDetailModal>` passando a margem que a página JÁ calcula, e refatorar os helpers de listing para importá-los de `listingHelpers.ts` (criado no Plano 01) eliminando duplicação.

Implementa as decisões travadas de Gatilho de abertura e Estrutura de arquivos (71-CONTEXT.md). A página só ganha estado + gatilho + render — não infla (já tem ~2285 linhas).

Purpose: cobrir os Success Criteria 1, 4, 5 e 7 da Phase 71 (gatilho sem conflito, multi-tenant intacto, página enxuta).
Output: `MLAnuncios.tsx` com o modal integrado.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-modal-de-detalhe-do-an-ncio-shell-indicadores/71-CONTEXT.md
@src/pages/mercadolivre/MLAnuncios.tsx
@src/components/mercadolivre/anuncios/ListingDetailModal.tsx
@src/components/mercadolivre/anuncios/listingHelpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar helpers de listing para o módulo compartilhado (sem duplicação)</name>
  <files>src/pages/mercadolivre/MLAnuncios.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/listingHelpers.ts (Plano 01 — exporta getCommissionRate, getListingLabel, currencyFmt, mlListingUrl com assinaturas idênticas às locais)
    - src/pages/mercadolivre/MLAnuncios.tsx (linhas 68-91: definições locais de getCommissionRate/getListingLabel/currencyFmt/listingBadge; linha 1400: anchor do título usa o regex da URL do ML — pode passar a usar mlListingUrl)
  </read_first>
  <action>
    Remova as definições locais de `getCommissionRate`, `getListingLabel` e `currencyFmt` em MLAnuncios.tsx e passe a importá-las de `@/components/mercadolivre/anuncios/listingHelpers`. As assinaturas são idênticas (drop-in), então todos os call sites existentes continuam funcionando sem alteração. `listingBadge` (que retorna JSX) pode permanecer na página OU mover para o módulo compartilhado — decisão sua, desde que NÃO haja lógica duplicada de comissão/label entre página e módulo. Opcionalmente, substitua o regex inline da URL do ML (linha ~1400) por `mlListingUrl(item.id)` para consistência.

    Este refactor é puramente de organização: comportamento e renderização da página devem permanecer idênticos. Não alterar a lógica de cálculo.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sem erros
    - Nenhuma definição local de `getCommissionRate`/`getListingLabel`/`currencyFmt` permanece em MLAnuncios.tsx (importadas de listingHelpers)
    - Nenhuma lógica de comissão/label duplicada entre a página e `listingHelpers.ts`
    - Comportamento da página inalterado (mesmos call sites, mesmas assinaturas)
  </acceptance_criteria>
  <done>Helpers centralizados em listingHelpers.ts; página consome o módulo compartilhado sem duplicação.</done>
</task>

<task type="auto">
  <name>Task 2: Estado + gatilho (miniatura + ícone) + render do ListingDetailModal</name>
  <files>src/pages/mercadolivre/MLAnuncios.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/ListingDetailModal.tsx (Plano 01 — props: item, open, onOpenChange, margin)
    - src/pages/mercadolivre/MLAnuncios.tsx (linha 589: padrão de estado do PriceDetailSheet; linha 699-701: `marginByItem` Map keyed por item.id; linhas 1376-1409: TableRow com onClick toggleRow, célula da miniatura com stopPropagation, anchor do título com stopPropagation; linha 1363: coluna "Análise"; linha 1591: célula de ações onde já há botão com stopPropagation)
  </read_first>
  <action>
    Adicione estado de abertura do modal no componente da página: `selectedItem: ProductItem | null` e `modalOpen: boolean` (espelhar o padrão de `priceSheetItem` já existente). Crie um handler `openDetail(item)` que seta `selectedItem` e `modalOpen=true`.

    Gatilho 1 — miniatura: a célula da miniatura (linha ~1389) já tem `onClick={(e) => e.stopPropagation()}` para não disparar o `toggleRow`. Torne a `<img>`/wrapper clicável chamando `openDetail(item)` (manter o `stopPropagation` para não expandir variações; adicionar `cursor-pointer` e um `title`/aria-label "Ver detalhes").

    Gatilho 2 — ícone "Ver detalhes": adicione um botão com ícone `Eye` (lucide-react) na célula de ações da linha (coluna "Análise", ~linha 1363/1591) que chama `openDetail(item)` com `e.stopPropagation()`. NÃO remover/alterar o botão existente de abrir o PriceDetailSheet nem o anchor do título.

    Preservação (crítico): o `onClick={() => item.has_variations && toggleRow(item.id)}` da TableRow e o anchor do título (`rel="noopener noreferrer"`, com `stopPropagation`) permanecem intactos — usar alvos de clique distintos e `e.stopPropagation()` nos gatilhos novos para zero conflito.

    Render: ao final do JSX da página (junto ao `<PriceDetailSheet ... />`), renderize `<ListingDetailModal item={selectedItem} open={modalOpen} onOpenChange={setModalOpen} margin={selectedItem ? marginByItem.get(selectedItem.id) : null} />`. A margem vem do `marginByItem` que a página JÁ calcula via `useMLMarginWithAds` — nenhuma nova fonte de margem. Importe `ListingDetailModal` e o ícone `Eye`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sem erros e `npm run build` conclui
    - Clicar na miniatura abre o modal com o `ProductItem` daquela linha (sem expandir variações)
    - O ícone Eye na célula de ações abre o modal (com `e.stopPropagation()`)
    - O `toggleRow` (expandir variações) e o link do título continuam funcionando — gatilhos não conflitam
    - O modal recebe `margin={marginByItem.get(selectedItem.id)}` — sem nova chamada de margem; multi-tenant intacto (item já escopado por org/seller pelo contexto)
  </acceptance_criteria>
  <done>Página de catálogo abre o modal de detalhe por miniatura e por ícone, preservando interações existentes, com a margem reusada dos hooks atuais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MLInventoryContext → MLAnuncios → modal | O `ProductItem` e o `marginByItem` chegam já escopados por org/seller; a página só repassa por prop, sem novo fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04 | Information Disclosure | render do ListingDetailModal em MLAnuncios.tsx | accept | O modal recebe apenas o `ProductItem` já filtrado por org/seller pelo `MLInventoryContext` e a margem do `marginByItem` (RPC org-first). Nenhum dado novo cruza organização (Success Criterion 5). |
| T-71-05 | Denial of Service | gatilhos de clique na tabela | accept | Ações puramente locais de UI (set state); `e.stopPropagation()` evita disparos cruzados. Sem chamadas de rede adicionais por clique. |
</threat_model>

<verification>
- `npx tsc --noEmit` — sem erros de tipo
- `npm run build` — build de produção conclui
- Manual (executor confirma via leitura do diff): miniatura e ícone Eye chamam `openDetail` com `stopPropagation`; `toggleRow` e anchor do título inalterados; `<ListingDetailModal>` renderizado com margem do `marginByItem`
</verification>

<success_criteria>
- `MLAnuncios.tsx` integra o modal sem inflar (estado + 2 gatilhos + render + imports; helpers centralizados)
- Gatilhos não conflitam com expandir-variações nem com o link do título
- Margem reaproveitada do `marginByItem` (zero nova fonte / zero novo fetch)
- Multi-tenant intacto
</success_criteria>

<output>
Create `.planning/phases/71-modal-de-detalhe-do-an-ncio-shell-indicadores/71-02-SUMMARY.md` when done
</output>
