---
phase: 71-modal-de-detalhe-do-an-ncio-shell-indicadores
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mercadolivre/anuncios/listingHelpers.ts
  - src/components/mercadolivre/anuncios/listingIndicators.ts
  - src/components/mercadolivre/anuncios/listingIndicators.test.ts
  - src/components/mercadolivre/anuncios/ListingQualityScore.tsx
  - src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx
  - src/components/mercadolivre/anuncios/ListingDetailModal.tsx
autonomous: true
requirements:
  - ADM-71

must_haves:
  truths:
    - "O modal abre como Dialog central (max-w-4xl) com cabeçalho do anúncio e Tabs"
    - "A aba Indicadores mostra quality score, variações com estoque/vendido, breakdown de tipo logístico e KPIs (visitas/vendido/estoque/margem)"
    - "O scoreboard de qualidade trata health=null com estado 'sem dado'"
    - "As abas Vendas/Precificação/Avaliações/Histórico aparecem desabilitadas com tooltip 'em breve'"
    - "O botão 'Ver no ML' aponta para a URL correta do anúncio (formato MLB-<id>) abrindo em nova aba"
    - "Os utilitários puros (faixa de quality score e agregação de tipo logístico) têm testes unitários passando"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/listingIndicators.ts"
      provides: "Utilitários puros: qualityScoreBand (health 0-1 -> faixa/%) e aggregateLogisticType (logistic_type x estoque)"
      min_lines: 30
    - path: "src/components/mercadolivre/anuncios/listingIndicators.test.ts"
      provides: "Testes unitários dos utilitários puros"
      min_lines: 25
    - path: "src/components/mercadolivre/anuncios/listingHelpers.ts"
      provides: "Helpers compartilhados extraídos: getCommissionRate, getListingLabel, currencyFmt, mlListingUrl"
      min_lines: 20
    - path: "src/components/mercadolivre/anuncios/ListingQualityScore.tsx"
      provides: "Scoreboard de qualidade isolado a partir de health"
      min_lines: 25
    - path: "src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx"
      provides: "Conteúdo da aba Indicadores (grid 2 colunas)"
      min_lines: 60
    - path: "src/components/mercadolivre/anuncios/ListingDetailModal.tsx"
      provides: "Shell do modal: Dialog + cabeçalho + Tabs + Ver no ML"
      min_lines: 60
  key_links:
    - from: "src/components/mercadolivre/anuncios/ListingDetailModal.tsx"
      to: "src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx"
      via: "renderiza <ListingIndicatorsTab item={item} margin={margin} /> dentro da TabsContent 'indicadores'"
      pattern: "ListingIndicatorsTab"
    - from: "src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx"
      to: "src/components/mercadolivre/anuncios/ListingQualityScore.tsx"
      via: "renderiza <ListingQualityScore health={item.health} />"
      pattern: "ListingQualityScore"
    - from: "src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx"
      to: "src/components/mercadolivre/anuncios/listingIndicators.ts"
      via: "consome aggregateLogisticType(item) para o breakdown logístico"
      pattern: "aggregateLogisticType"
---

<objective>
Construir os 3 componentes novos do modal de detalhe do anúncio (Fase A do milestone) e os utilitários puros que eles consomem, todos isolados em `src/components/mercadolivre/anuncios/`. Tudo opera sobre o tipo `ProductItem` já existente — ZERO backend novo, ZERO fetch novo, ZERO dependência nova.

Implementa a decisão travada D (71-CONTEXT.md): modal central `Dialog max-w-4xl`, aba Indicadores em grid `md:grid-cols-5` (esq 2 / dir 3), abas futuras desabilitadas com tooltip "em breve", scoreboard de qualidade reutilizável (Phase 72).

Purpose: entregar o shell + a aba Indicadores como peças independentes e testáveis antes de tocar a página de catálogo (que tem ~2285 linhas). Mantém a página enxuta (Plano 02 só liga o gatilho).
Output: `listingHelpers.ts`, `listingIndicators.ts` (+ testes), `ListingQualityScore.tsx`, `ListingIndicatorsTab.tsx`, `ListingDetailModal.tsx`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-modal-de-detalhe-do-an-ncio-shell-indicadores/71-CONTEXT.md
@docs/superpowers/specs/2026-06-29-anuncio-detail-modal-design.md
@src/contexts/MLInventoryContext.tsx
@src/components/mercadolivre/anuncios/ImportacaoCustos.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Utilitários puros (faixa de quality score + agregação de tipo logístico) + testes</name>
  <files>src/components/mercadolivre/anuncios/listingHelpers.ts, src/components/mercadolivre/anuncios/listingIndicators.ts, src/components/mercadolivre/anuncios/listingIndicators.test.ts</files>
  <read_first>
    - src/contexts/MLInventoryContext.tsx (tipos `ProductItem`/`ProductVariation`; note que `ProductVariation` NÃO tem campo `logistic_type` — ele só existe no nível do item)
    - src/pages/mercadolivre/MLAnuncios.tsx (linhas 68-104: definições atuais de `getCommissionRate`, `getListingLabel`, `currencyFmt`, `healthBadge` — faixas 0.8 / 0.5; linha 96 e 819-823: rótulos de `LogisticFilter` = fulfillment/cross_docking/self_service/drop_off; linha 1400: padrão da URL do ML)
    - src/lib/mco.test.ts (estilo de teste vitest do projeto — `describe`/`it`/`expect`)
  </read_first>
  <behavior>
    qualityScoreBand(health: number | null):
      - health=null  -> { pct: null, band: "sem_dado", label: "Sem dado" }
      - health=0.92  -> { pct: 92, band: "bom",   label: "Boa" }   (>= 0.8)
      - health=0.6   -> { pct: 60, band: "medio", label: "Regular" } (>= 0.5 e < 0.8)
      - health=0.3   -> { pct: 30, band: "ruim",  label: "Baixa" }  (< 0.5)
      - arredonda pct para inteiro (Math.round(health*100)); thresholds idênticos ao `healthBadge` atual (0.8 / 0.5)
    aggregateLogisticType(item):
      - sem variações -> [{ type: item.logistic_type ?? "sem_info", label, stock: item.available_quantity }]
      - com variações -> agrupa por tipo logístico (todas as variações compartilham `item.logistic_type`) somando `available_quantity` de cada variação -> [{ type, label, stock: soma }]
      - logistic_type null/"" -> bucket type "sem_info", label "Sem info"
      - retorna array (estrutura preparada para múltiplos buckets), ordenado por stock desc
    logisticTypeLabel(type): mapeia fulfillment->"Full", cross_docking->"Cross-docking", self_service->"Flex", drop_off->"Agência/Correios", default->"Sem info"
  </behavior>
  <action>
    Crie `listingHelpers.ts` com helpers compartilhados PUROS extraídos conceitualmente de MLAnuncios.tsx (sem duplicar — o Plano 02 fará a página importar daqui e remover as cópias locais): exporte `getCommissionRate(listingTypeId)`, `getListingLabel(listingTypeId)`, `currencyFmt(v)` e adicione `mlListingUrl(id)` que retorna `https://produto.mercadolivre.com.br/${id.replace(/^(MLB)(\d+)$/, "$1-$2")}` (mesmo regex já usado na linha do título). Mantenha as assinaturas e o comportamento idênticos às definições atuais da página para que o refactor do Plano 02 seja drop-in. Importe `LISTING_TYPE_RATES` de `@/data/financialMockData` (mesma fonte que a página usa).

    Crie `listingIndicators.ts` com os utilitários puros descritos em <behavior>: `qualityScoreBand`, `aggregateLogisticType`, `logisticTypeLabel`. Defina e exporte os tipos de retorno (`QualityBand`, `LogisticBucket`) como interfaces nomeadas. `aggregateLogisticType` recebe um subconjunto de `ProductItem` (tipar via `Pick<ProductItem, "logistic_type" | "available_quantity" | "variations">`). NÃO faça fetch, NÃO importe React aqui — módulo puro.

    Crie `listingIndicators.test.ts` (vitest) cobrindo os casos de <behavior>: as 4 faixas de quality score (null/bom/médio/ruim) e a agregação logística (sem variações, com variações somando estoque, tipo null -> sem_info). Use `describe`/`it`/`expect` no estilo de `src/lib/mco.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/components/mercadolivre/anuncios/listingIndicators.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/mercadolivre/anuncios/listingIndicators.test.ts` passa com todos os casos verdes
    - `qualityScoreBand(null).band === "sem_dado"` e `qualityScoreBand(0.92).pct === 92`
    - `aggregateLogisticType` soma corretamente o estoque das variações em um único bucket quando todas compartilham o logistic_type do item
    - `listingHelpers.ts` exporta `getCommissionRate`, `getListingLabel`, `currencyFmt`, `mlListingUrl` com comportamento idêntico ao da página
  </acceptance_criteria>
  <done>Utilitários puros criados e testados; helpers compartilhados prontos para reuso pelo modal e (no Plano 02) pela página.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ListingQualityScore + ListingIndicatorsTab</name>
  <files>src/components/mercadolivre/anuncios/ListingQualityScore.tsx, src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/listingIndicators.ts (utilitários da Task 1: qualityScoreBand, aggregateLogisticType, logisticTypeLabel)
    - src/contexts/MLInventoryContext.tsx (tipo `ProductItem`; variações têm attribute_combinations/available_quantity/sold_quantity)
    - src/hooks/useMLMarginWithAds.ts (tipo `ProductMarginWithAds` — campos `lucro_pct`, `lucro_pct_pos_ads`, `has_cmv`; será passado por prop, NÃO chamar o hook aqui)
    - src/components/mercadolivre/anuncios/ImportacaoCustos.tsx (estilo da pasta: shadcn Card/Badge, named export, props interface inline)
    - src/pages/mercadolivre/MLAnuncios.tsx (linha 99-104: faixas/cores do `healthBadge` atual para manter consistência visual)
  </read_first>
  <action>
    Crie `ListingQualityScore.tsx` (named export, props interface inline `{ health: number | null }`): usa `qualityScoreBand(health)` para renderizar um scoreboard isolado — % grande, `Progress` (shadcn) e um `Badge` de faixa colorido (bom=emerald, médio=amber, ruim=destructive) reproduzindo a paleta do `healthBadge` atual; quando `band === "sem_dado"` mostra "Sem dado" em muted (a Phase 72 forçará refresh via EF). Este componente fica reutilizável pela Phase 72.

    Crie `ListingIndicatorsTab.tsx` (named export, props interface inline `{ item: ProductItem; margin?: ProductMarginWithAds | null }`): layout grid `md:grid-cols-5`.
    - Coluna esquerda (`md:col-span-2`): imagem `item.thumbnail` (aplicar `.replace("http://","https://")`, com fallback `Package` quando null, como na página); lista de variações (por variação: atributos de `attribute_combinations` como "Nome: Valor", estoque `available_quantity`, vendido `sold_quantity`) — quando `variations.length === 0`, omitir a lista; breakdown de tipo logístico via `aggregateLogisticType(item)` (cada bucket: `logisticTypeLabel(type)` + estoque).
    - Coluna direita (`md:col-span-3`): `<ListingQualityScore health={item.health} />` no topo; KPIs em grid — visitas (`item.visits`), vendido (`item.sold_quantity`), estoque (`item.available_quantity`) e margem (de `margin`: usar `margin.lucro_pct` com fallback "—"/"Sem vendas no período" quando `margin` for undefined/null, espelhando o tratamento da página; NÃO chamar hook de margem aqui — vem por prop); e os campos marca (`item.brand` ou "Sem marca"), status (`item.status`), frete grátis (`item.free_shipping` -> "Grátis"/"Pago"), catálogo (mostrar `item.catalog_product_id` só quando presente).
    Usar `currencyFmt` de `listingHelpers.ts` onde formatar moeda. Sem fetch, sem novas libs (apenas shadcn Card/Badge/Progress + lucide).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sem erros
    - `ListingQualityScore` renderiza % + badge de faixa e o estado "Sem dado" quando `health` é null
    - `ListingIndicatorsTab` renderiza coluna esquerda (thumbnail/variações/logístico) e direita (scoreboard/KPIs/marca/status/frete/catálogo) sem nenhuma chamada de rede
    - Margem é lida do prop `margin` (não há `useMLMarginWithAds`/`useMLProductCosts` dentro destes componentes)
  </acceptance_criteria>
  <done>Aba Indicadores e scoreboard prontos, consumindo apenas dados de `ProductItem` + margem por prop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ListingDetailModal (shell — Dialog + cabeçalho + Tabs + Ver no ML)</name>
  <files>src/components/mercadolivre/anuncios/ListingDetailModal.tsx</files>
  <read_first>
    - src/components/mercadolivre/anuncios/ListingIndicatorsTab.tsx (Task 2 — vai dentro da TabsContent "indicadores")
    - src/components/mercadolivre/anuncios/listingHelpers.ts (Task 1 — getListingLabel, getCommissionRate, mlListingUrl)
    - src/contexts/MLInventoryContext.tsx (tipo `ProductItem`)
    - src/hooks/useMLMarginWithAds.ts (tipo `ProductMarginWithAds` para o prop `margin`)
    - src/pages/mercadolivre/MLAnuncios.tsx (linhas 85-91: composição do `listingBadge`; padrão de uso de shadcn `Dialog`/`Tabs`/`Tooltip` no projeto)
  </read_first>
  <action>
    Crie `ListingDetailModal.tsx` (named export). Props interface inline: `{ item: ProductItem | null; open: boolean; onOpenChange: (open: boolean) => void; margin?: ProductMarginWithAds | null }`. Reusa o tipo `ProductItem` (não cria modelo novo).

    Estrutura: shadcn `Dialog` controlado por `open`/`onOpenChange`, `DialogContent` com `className="max-w-4xl max-h-[90vh] overflow-y-auto"`. Quando `item` é null, renderizar `null` (ou nada) dentro do content. Cabeçalho (`DialogHeader`): título (`line-clamp-1`), MLB id em mono (`item.id`), badge "N variações" quando `item.variations.length > 1`, badge de tipo de anúncio composto de `getListingLabel(item.listing_type_id)` + `getCommissionRate(item.listing_type_id)` (Clássico/Premium/Grátis + %), e botão "Ver no ML" (`<a href={mlListingUrl(item.id)} target="_blank" rel="noopener noreferrer">` — sempre incluir `rel="noopener noreferrer"`).

    `Tabs` com `defaultValue="indicadores"`. `TabsList` com 5 gatilhos: `indicadores` (ativo) e `vendas`, `precificacao`, `avaliacoes`, `historico` cada um com `disabled` e envolto em `Tooltip` cujo `TooltipContent` diz "Em breve". `TabsContent value="indicadores"` renderiza `<ListingIndicatorsTab item={item} margin={margin} />`. As 4 TabsContent futuras podem ficar vazias ou ausentes (o importante são os gatilhos desabilitados presentes para B–F encaixarem sem refazer layout).

    Não duplicar a lógica de `getCommissionRate`/`getListingLabel` — importar de `listingHelpers.ts`. Sem fetch, sem novas dependências.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sem erros e `npm run build` conclui com sucesso
    - `DialogContent` usa exatamente `max-w-4xl max-h-[90vh] overflow-y-auto`
    - As 4 abas futuras renderizam com `disabled` + tooltip "Em breve"; a aba "indicadores" é o `defaultValue`
    - Botão "Ver no ML" usa `mlListingUrl(item.id)` com `target="_blank"` e `rel="noopener noreferrer"`
    - O badge de tipo de anúncio deriva de `getListingLabel`/`getCommissionRate` (sem lógica duplicada)
  </acceptance_criteria>
  <done>Shell do modal completo e tipado, pronto para o Plano 02 ligar o gatilho na página de catálogo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MLInventoryContext → modal | O `ProductItem` chega já filtrado por org/seller pelo contexto; o modal NÃO faz fetch próprio nesta fase |
| app → produto.mercadolivre.com.br | Navegação externa em nova aba via link "Ver no ML" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Information Disclosure | ListingDetailModal / ListingIndicatorsTab | accept | Sem fetch novo; renderiza apenas o `ProductItem` já escopado por org/seller pelo `MLInventoryContext`. Nenhum dado novo cruza organização (Success Criterion 5). |
| T-71-02 | Tampering | link "Ver no ML" (target=_blank) | mitigate | Sempre `rel="noopener noreferrer"` no anchor para evitar tabnabbing/acesso a `window.opener`. |
| T-71-03 | Elevation of Privilege | dependências | accept | ZERO dependência nova instalada nesta fase — não há superfície de supply chain (sem npm install). |
</threat_model>

<verification>
- `npx vitest run src/components/mercadolivre/anuncios/listingIndicators.test.ts` — testes dos utilitários puros verdes
- `npx tsc --noEmit` — sem erros de tipo
- `npm run build` — build de produção conclui
- Inspeção: nenhum `supabase`/`fetch`/`useQuery` nos 3 componentes ou nos utilitários (zero rede)
</verification>

<success_criteria>
- 6 arquivos novos criados em `src/components/mercadolivre/anuncios/` (helpers, utils, teste, 3 componentes)
- Utilitários puros testados (faixa de quality score + agregação logística)
- Modal renderiza Dialog `max-w-4xl` com cabeçalho, aba Indicadores ativa e 4 abas desabilitadas "em breve"
- Zero backend novo, zero fetch novo, zero dependência nova
</success_criteria>

<output>
Create `.planning/phases/71-modal-de-detalhe-do-an-ncio-shell-indicadores/71-01-SUMMARY.md` when done
</output>
