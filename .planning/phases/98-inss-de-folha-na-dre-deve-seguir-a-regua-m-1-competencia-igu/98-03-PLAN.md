---
phase: 98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu
plan: 03
type: execute
wave: 2
depends_on: ["98-01", "98-02"]
files_modified:
  - src/pages/MercadoLivre.tsx
  - src/lib/dreCascade.test.ts
autonomous: false
requirements: ["INSS-03", "INSS-04"]

must_haves:
  truths:
    - "Em mês fechado (apuração), o card DRE do Mês mostra o bloco Pessoal com Salários/Pró-labore do mês corrente + INSS da guia real M+1 — não o valor cru do mês corrente"
    - "Em mês aberto (previsão), o bloco Pessoal continua exatamente como antes desta phase — nenhuma linha filtrada, nenhum valor M+1 somado"
    - "A decisão do dono sobre estender o gate de fechamento ao INSS foi perguntada explicitamente e registrada — não decidida pelo executor"
    - "get_dre_operational_by_competence, get_imposto_guia_by_competence, dreRegime.ts, dreCascade.ts e dreCloseGate.ts permanecem intocados"
  artifacts:
    - path: "src/pages/MercadoLivre.tsx"
      provides: "Orquestração: useInssGuiaReal + resolveInssForCascade + applyInssReal ligados ao card DRE do Mês, regime-gated"
      contains: "resolveInssForCascade"
    - path: "src/lib/dreCascade.test.ts"
      provides: "Describe do C11 reescrito para refletir a reversão da Phase 98, sem alterar dreCascade.ts nem os valores numéricos das asserções"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/lib/dreInss.ts"
      via: "dreCascade = applyInssReal(buildDreCascade(rowsFiltradas, margemContribuicao), inssReal), rowsFiltradas/inssReal vindos de resolveInssForCascade"
      pattern: "applyInssReal\\("
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useInssGuiaReal.ts"
      via: "mesmo eixo de mês (dreSaleMonth) já usado por guiaReal/guiaNudge — eixo diferente desalinha apuração e previsão"
      pattern: "useInssGuiaReal\\(dreSaleMonth\\)"
---

<objective>
Ligar a régua M+1 do INSS (módulos puros do Plano 98-02) na orquestração real do card "DRE do Mês" em `MercadoLivre.tsx`, condicionada ao regime (previsão nunca desloca; apuração usa o valor real M+1) — e capturar, via checkpoint, a decisão do dono sobre se a ausência da guia de INSS também deve bloquear o fechamento do mês (pergunta em aberto do RESEARCH.md, propositalmente NÃO decidida por este plano).

Purpose: É aqui que o comportamento observável muda para o usuário. Tudo o que este plano faz é COMPOSIÇÃO de peças já provadas (Planos 98-01/98-02) — nenhuma lógica de negócio nova nasce neste plano.

Output: bloco Pessoal do card DRE do Mês correto em apuração, byte-idêntico em previsão; decisão do gate de fechamento registrada; descrição do describe C11 em `dreCascade.test.ts` atualizada para não mais implicar que o INSS nunca desloca.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-CONTEXT.md
@.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decisão — o gate de fechamento deve bloquear com INSS ausente?</name>
  <decision>O gate de fechamento do mês (`resolveCloseGate`/`canApurarImposto`, hoje só olha ICMS/PIS/COFINS) deve GANHAR um terceiro sinal e bloquear "marcar mês como apurado" quando a guia de INSS da competência M+1 estiver ausente/pendente — no mesmo padrão do C6 (CMV cheio) e C7 (imposto de venda)?</decision>
  <action>Pausar a execução e apresentar as duas opções abaixo a Wesley antes de prosseguir para a Task 2. Nenhum arquivo é criado ou editado por esta task — é uma pausa de decisão pura, cujo único efeito é a resposta registrada na SUMMARY (e, se `option-a`, uma pendência explícita anotada para uma phase futura).</action>
  <context>
    Wesley confirmou explicitamente (98-CONTEXT.md) a régua de VALOR (M+1, cancelada não soma) para o INSS — isso este plano já implementa na Task 2, incondicionalmente. Ele NÃO confirmou se quer o mesmo comportamento de GATE (bloquear fechamento se ausente). O evento que originou esta phase foi justamente março travado por causa do COFINS ausente — replicar o mesmo gate para o INSS é o padrão mais consistente, mas cria um NOVO ponto de travamento que Wesley pode não esperar.

    Esta phase (98) NÃO implementa a extensão do gate em nenhum dos dois casos — o escopo combinado é só CAPTURAR a decisão. Se a resposta for estender, a implementação (`canApurarInss` em `dreCloseGate.ts`, mirror de `canApurarImposto`) fica para uma phase futura dedicada.
  </context>
  <options>
    <option id="option-a">
      <name>Estender o gate agora (futuro dedicado)</name>
      <pros>Consistência: mesma régua (M+1) implica no mesmo nível de confiança exigido antes de fechar o mês. Evita fechar um mês com o INSS ainda "pending"/placeholder, mesma dor que motivou o C7 original.</pros>
      <cons>Mais um ponto de bloqueio pode travar o fechamento (Wesley já sentiu isso com o COFINS de março). Semanticamente, INSS é encargo de folha (recorrente, previsível) — diferente de imposto de venda (varia com receita) — pode ser aceitável fechar com estimativa e corrigir depois.</cons>
    </option>
    <option id="option-b">
      <name>Não estender agora — só a régua de valor</name>
      <pros>Sem novo ponto de travamento; fechamento de mês continua exigindo só CMV cheio + guias de venda. Menor risco de fricção no fluxo que Wesley já usa toda semana.</pros>
      <cons>Um mês pode ser apurado com INSS ainda ausente (guia==null) — o bloco Pessoal, nesse caso, fica SEM o valor de INSS até a guia existir (silenciosamente, sem sinal de bloqueio) — risco documentado, não ainda mitigado.</cons>
    </option>
  </options>
  <resume-signal>Responda "option-a" ou "option-b". Qualquer resposta: NENHUM código de gate é escrito nesta phase — a resposta só é registrada na SUMMARY (e, se option-a, também deve virar um item pendente explícito no STATE.md/ROADMAP.md para uma phase futura). Prossiga para a Task 2 assim que a resposta for registrada.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Ligar a régua M+1 do INSS no card DRE do Mês (MercadoLivre.tsx)</name>

  <read_first>
    - `src/pages/MercadoLivre.tsx:53-63` — bloco de imports do domínio DRE (`useDreOperational`, `useCancelledRevenue`, `buildDreCascade`, `computeMargemContribuicao`, `resolveDreRegime`/`shouldNudgeClose`/`monthPlusOne`, `useDreMonthClose`, `useImpostoGuiaReal`/`useImpostoGuiaNudge`, `useCmvCheioGate`, `useNaoClassificadoItems`, `resolveCloseGate`) — é aqui que as duas novas importações entram.
    - `src/pages/MercadoLivre.tsx:286-335` — orquestração atual: `dreSaleMonth` (eixo de mês único, linha 289), `monthClose`/`guiaReal`/`guiaNudge` (290-292), `cmvGate` (297-300), `closeGate`/`closeBlocked` (309-313), `regimeResult`/`cmvMes`/`impostosMes` (322-333). `regimeResult.regime` é o sinal de previsão×apuração que a Task 2 precisa consumir.
    - `src/pages/MercadoLivre.tsx:414-435` — a montagem final: `margemContribuicao` (memo, 421-431) e o `dreCascade` atual (432-435): `buildDreCascade(dreOperationalRows ?? [], margemContribuicao)`. É este memo que muda.
    - `src/lib/dreInss.ts` e `src/hooks/useInssGuiaReal.ts` (Plano 98-02) — as peças já prontas: `resolveInssForCascade({regime, rows, guia})` devolve `{rows, inssReal}`; `applyInssReal(cascade, inssReal)` devolve a cascata ajustada.
  </read_first>

  <files>src/pages/MercadoLivre.tsx</files>

  <action>
    1. No bloco de imports do domínio DRE (perto da linha 62), adicione `import { useInssGuiaReal } from "@/hooks/useInssGuiaReal";` e `import { resolveInssForCascade, applyInssReal } from "@/lib/dreInss";`.
    2. Logo depois de `guiaNudge` (linha ~292), adicione `const inssGuiaReal = useInssGuiaReal(dreSaleMonth);` — MESMO eixo `dreSaleMonth` já usado por `guiaReal`/`guiaNudge`/`monthClose` (nunca um eixo de mês diferente, ou apuração e previsão passam a olhar meses diferentes e o deslocamento desalinha).
    3. Depois que `regimeResult` já foi calculado (depois da linha ~331, onde `cmvMes`/`impostosMes` são extraídos), adicione um `useMemo` novo que chama `resolveInssForCascade({ regime: regimeResult.regime, rows: dreOperationalRows ?? [], guia: inssGuiaReal.data ?? null })`, com dependências `[regimeResult.regime, dreOperationalRows, inssGuiaReal.data]`, destruturando o resultado em duas constantes, por exemplo `dreOperationalRowsParaCascata` e `inssRealApuracao`.
    4. Substitua o memo de `dreCascade` (linhas 432-435) para: montar a cascata com `buildDreCascade(dreOperationalRowsParaCascata, margemContribuicao)` (em vez de `dreOperationalRows ?? []` cru) e então envolver o resultado com `applyInssReal(base, inssRealApuracao)` antes de retornar. Atualize o array de dependências do memo para `[dreOperationalRowsParaCascata, margemContribuicao, inssRealApuracao]` (removendo a dependência direta em `dreOperationalRows`, que passa a ser consumida só indiretamente via `dreOperationalRowsParaCascata`).
    5. NÃO toque em `closeGate`/`resolveCloseGate`/`closeBlocked`/`canClose`/`handleCloseDreMonth`/`handleReopenDreMonth` neste plano — a decisão sobre estender o gate (Task 1) fica registrada, não implementada agora.
    6. NÃO toque em `dreOperationalRows` em si (a variável que vem direto de `useDreOperational` continua crua/sem filtro) — só a variável local que alimenta `buildDreCascade` muda.
    7. Comentário inline no ponto de integração explicando: em previsão, `resolveInssForCascade` devolve as rows sem alteração e `inssReal: null`, então `applyInssReal` é um no-op e a cascata fica byte-idêntica à legada; em apuração, a linha crua de INSS é removida e o valor real M+1 é somado no lugar dela — nunca os dois somados.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "useInssGuiaReal(dreSaleMonth)" src/pages/MercadoLivre.tsx` == 1.
    - `grep -c "resolveInssForCascade(" src/pages/MercadoLivre.tsx` == 1.
    - `grep -c "applyInssReal(" src/pages/MercadoLivre.tsx` == 1.
    - `npx tsc --noEmit` → 0 erros.
    - `git diff --name-only main -- src/lib/dreCascade.ts src/lib/dreRegime.ts src/lib/dreCloseGate.ts` → saída vazia (nenhum destes três módulos foi tocado).
    - `npx vitest run` (suíte completa) → verde, sem novas falhas.
  </acceptance_criteria>

  <done>Em mês fechado, o bloco Pessoal do card soma Salários/Pró-labore do mês corrente + INSS real da guia M+1; em mês aberto, o bloco permanece idêntico ao comportamento anterior a esta phase. Nenhuma lógica de gate foi alterada.</done>
</task>

<task type="auto">
  <name>Task 3: Reescrever o describe do C11 em dreCascade.test.ts (documentar a reversão da Phase 98)</name>

  <read_first>
    - `src/lib/dreCascade.test.ts:153-214` — o describe atual completo (4 testes + o comentário de cabeçalho que trava o comportamento antigo como definitivo). Ler linha a linha antes de editar.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-02-PLAN.md` (Task 2) — a origem do describe atual, para citar corretamente qual decisão está sendo revertida.
  </read_first>

  <files>src/lib/dreCascade.test.ts</files>

  <action>
    Este describe (linhas 153-214) fixa o comportamento onde `buildDreCascade` recebe a linha crua de INSS junto com Salários e soma as duas dentro do bloco Pessoal — e o comentário de cabeçalho hoje afirma que isso é definitivo e nunca deve mudar. A Phase 96 travou essa decisão; o dono reverteu a decisão DE NEGÓCIO nesta Phase 98 (o INSS agora segue a régua M+1 em apuração) — mas repare que `buildDreCascade` em si CONTINUA agnóstico a qualquer régua de competência: ele só soma o que recebe. A mudança de comportamento observável vive inteiramente na camada de orquestração (`resolveInssForCascade`/`applyInssReal` do Plano 98-02, ligados em `MercadoLivre.tsx` no Plano 98-03/Task 2) — nunca dentro deste módulo.

    Reescreva SÓ este describe (não toque nos outros 5 describes do arquivo, nem em `src/lib/dreCascade.ts`):

    1. Troque o título do describe para algo como "buildDreCascade — C11/Phase 98: o módulo continua somando o bloco Pessoal cru (a régua M+1 do INSS vive em dreInss.ts, fora deste módulo)".
    2. Reescreva o comentário de cabeçalho do describe removendo a linguagem de "trava definitiva/nunca mudar" e explicando: a Phase 96 fixou este comportamento como decisão de negócio; a Phase 98 (2026-07-16) reverteu essa decisão — o INSS passou a seguir a régua M+1 em apuração — mas a mudança acontece FORA deste módulo (ver `src/lib/dreInss.ts` e `src/lib/dreInss.test.ts` para a prova do comportamento novo). Os 4 testes abaixo continuam válidos porque testam exclusivamente a característica mecânica de `buildDreCascade` (somar categorias dentro de um bloco, sem saber o que cada categoria significa) — essa característica não mudou e não deveria mudar.
    3. Ajuste a descrição de cada um dos 4 `it(...)` para deixar claro que testam o comportamento PURO de soma de `buildDreCascade`, não uma afirmação sobre a régua de competência do INSS (ex.: prefixar com algo como "[buildDreCascade puro]").
    4. NÃO altere nenhum valor numérico nem nenhuma chamada de `buildDreCascade`/`row(...)` dentro dos 4 testes — os números (27852.19, 24117.74, etc.) continuam corretos porque a função somada não mudou.
    5. NÃO edite `src/lib/dreCascade.ts` — se qualquer um dos 4 testes falhar depois da sua edição, pare: significa que você alterou uma asserção por engano, não o comentário.
  </action>

  <verify>
    <automated>npx vitest run src/lib/dreCascade.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/lib/dreCascade.test.ts` → todos os testes passam, incluindo os 4 do describe reescrito, com as MESMAS asserções numéricas de antes.
    - `git diff --name-only main -- src/lib/dreCascade.ts` → saída vazia (módulo de produção intocado).
    - `grep -c "Phase 98" src/lib/dreCascade.test.ts` ≥ 1 (a reversão está documentada no arquivo).
    - `grep -c "dreInss" src/lib/dreCascade.test.ts` ≥ 1 (referência cruzada para onde a régua nova mora).
  </acceptance_criteria>

  <done>O describe do C11 documenta honestamente que a Phase 98 reverteu a decisão de negócio da Phase 96, sem implicar que `buildDreCascade` mudou — porque não mudou. Os 4 testes permanecem verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Este plano só orquestra hooks/funções puras já protegidas (RLS na RPC do Plano 98-01, módulos puros do Plano 98-02); nenhuma superfície de auth/input nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | Repudiation | Decisão do gate de fechamento (Task 1) não registrada em lugar nenhum | mitigate | Resume-signal exige registrar a resposta na SUMMARY (e, se `option-a`, também no STATE.md/ROADMAP.md como pendência explícita para phase futura) |
| T-98-05 | Tampering | Eixo de mês (`dreSaleMonth`) divergente entre `useInssGuiaReal` e `guiaReal`/`monthClose`, desalinhando apuração/previsão | mitigate | Task 2 exige literalmente o mesmo argumento `dreSaleMonth` já usado pelos hooks irmãos — acceptance criteria grepa a chamada exata |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- `npx tsc --noEmit` 0 erros.
- `npx vitest run` (suíte completa) verde, incluindo os 4 testes reescritos do C11.
- `git diff --name-only main -- src/lib/dreCascade.ts src/lib/dreRegime.ts src/lib/dreCloseGate.ts` vazio.
- Decisão da Task 1 registrada verbatim na SUMMARY.
</verification>

<success_criteria>
- INSS-03: bloco Pessoal do card DRE do Mês usa o INSS real M+1 em apuração; byte-idêntico em previsão.
- INSS-04: decisão do dono sobre estender o gate capturada e registrada — não implementada às pressas.
- Zero regressão: nenhuma RPC ou módulo puro pré-existente (`get_dre_operational_by_competence`, `get_imposto_guia_by_competence`, `dreRegime.ts`, `dreCascade.ts`, `dreCloseGate.ts`) foi modificado.
</success_criteria>

<output>
Create `.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-03-SUMMARY.md` when done. Registre verbatim a resposta da Task 1 (option-a/option-b) e, se `option-a`, adicione o item pendente ao STATE.md e a uma futura entrada no ROADMAP.md.
</output>
