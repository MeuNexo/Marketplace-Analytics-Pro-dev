---
phase: 98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dreInss.ts
  - src/lib/dreInss.test.ts
  - src/hooks/useInssGuiaReal.ts
autonomous: true
requirements: ["INSS-02"]

must_haves:
  truths:
    - "A soma real do INSS de uma competência ignora linhas canceladas e soma pagas+pendentes, igual à régua já usada para ICMS/PIS/COFINS"
    - "O caso real de abril (duas linhas na mesma competência: 1.550,00 cancelada + 2.652,31 paga) resolve para 2.652,31, sem código especial"
    - "Em regime de previsão (mês aberto), a régua NUNCA desloca — as linhas cruas da RPC 87 e o total do bloco Pessoal continuam byte-a-byte idênticos ao comportamento anterior a esta phase"
    - "Em regime de apuração (mês fechado), a linha crua de INSS some das linhas de entrada e o valor M+1 resolvido é o único a contar"
  artifacts:
    - path: "src/lib/dreInss.ts"
      provides: "Módulo puro: INSS_FOLHA_CATEGORY, resolveInssReal, filterRawInssRow, applyInssReal, resolveInssForCascade"
      exports: ["INSS_FOLHA_CATEGORY", "resolveInssReal", "filterRawInssRow", "applyInssReal", "resolveInssForCascade"]
      min_lines: 50
    - path: "src/lib/dreInss.test.ts"
      provides: "Prova dos 4 casos exigidos pelo CONTEXT: pago, cancelado, ausente, múltiplas linhas na mesma competência (abril real) e o gate previsão×apuração"
      min_lines: 90
    - path: "src/hooks/useInssGuiaReal.ts"
      provides: "Hook TanStack Query — mirror de useImpostoGuiaReal, chamando get_inss_guia_by_competence em p_competence = monthPlusOne(saleMonth)"
      exports: ["useInssGuiaReal"]
  key_links:
    - from: "src/hooks/useInssGuiaReal.ts"
      to: "src/lib/dreRegime.ts"
      via: "monthPlusOne(saleMonth) como p_competence — nunca aritmética de data manual"
      pattern: "monthPlusOne\\(saleMonth\\)"
    - from: "src/lib/dreInss.ts"
      to: "src/lib/dreCascade.ts"
      via: "applyInssReal recebe e devolve um DreCascade — nunca reimplementa a soma de blocos"
      pattern: "DreCascade"
---

<objective>
Construir, em módulo puro e testável, toda a lógica de negócio do deslocamento M+1 do INSS de folha: a soma real filtrando canceladas (`resolveInssReal`), a remoção da linha crua não-deslocada (`filterRawInssRow`), a fusão do valor real no bloco Pessoal já montado (`applyInssReal`), e o portão que decide SE a régua se aplica — nunca em previsão, só em apuração (`resolveInssForCascade`). Mais o hook que busca o dado real (`useInssGuiaReal`), mirror exato de `useImpostoGuiaReal`.

Purpose: Isolar 100% da regra de negócio em funções puras, sem tocar `dreCascade.ts`/`dreRegime.ts`/`MercadoLivre.tsx` neste plano — o Plano 98-03 só faz a fiação (wiring) usando estas peças já provadas por teste. Mesma disciplina de módulos puros já usada em `dreCascade.ts`/`dreCloseGate.ts`/`dreRegime.ts` (sem import de React/Supabase).

Output: `src/lib/dreInss.ts` + `src/lib/dreInss.test.ts` (TDD) + `src/hooks/useInssGuiaReal.ts`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dreInss.ts — resolver, filtro e merge da régua M+1 do INSS (TDD)</name>

  <read_first>
    - `src/lib/dreRegime.ts:27-57` — `DreRegime` (tipo `"previsao" | "apuracao"`), `GuiaRealCategoryTotal`, `monthPlusOne`. Não editar este arquivo.
    - `src/lib/dreRegime.ts:119-137` — o trecho `apuracaoImpostoReal` dentro de `resolveDreRegime`: a MESMA regra (`status !== 'cancelled'` soma, `round2`, ausente→null) que `resolveInssReal` clona para 1 categoria só.
    - `src/lib/dreCascade.ts:20-96` — `DreBloco`, `DreOperationalRow`, `OPERACIONAL_BLOCOS` (note que `"pessoal"` é o PRIMEIRO da lista — relevante para onde inserir uma linha nova), `DreCascadeBlocoLine`, `DreCascade`, e o `round2` local (linha 96) — cada módulo puro do repo define seu próprio `round2`, não importa de outro lugar.
    - `.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-CONTEXT.md` seção `<decisions>` (regra de negócio) e `98-RESEARCH.md` seção "Addendum" (o caso real de abril: `competence_date=2026-04-01` com DUAS linhas, `{1550, cancelled}` e `{2652.31, paid}`).
  </read_first>

  <files>src/lib/dreInss.ts (novo), src/lib/dreInss.test.ts (novo)</files>

  <behavior>
    `resolveInssReal(guia: GuiaRealCategoryTotal[] | null): number | null`
    - `null` → `null`.
    - `[]` → `null`.
    - `[{total:1550, status:'paid'}]` → `1550`.
    - Abril real: `[{total:1550, status:'cancelled'}, {total:2652.31, status:'paid'}]` → `2652.31` (a linha cancelada NUNCA soma).
    - Só cancelada (mês 100% crédito): `[{total:1550, status:'cancelled'}]` → `0` (NÃO `null` — mesma semântica de `apuracaoImpostoReal`: apuração aconteceu, deu crédito).
    - `pending` também soma: `[{total:3852.19, status:'pending'}]` → `3852.19`.

    `filterRawInssRow(rows: DreOperationalRow[]): DreOperationalRow[]`
    - Rows com Salários(24000, bloco pessoal) + Pessoal-INSS(3852.19, bloco pessoal) + Estrutura(500) → devolve só Salários + Estrutura (2 linhas), a linha de categoria `'Pessoal - INSS'` removida.
    - Rows sem nenhuma linha de INSS → devolve equivalente ao array de entrada (no-op), mesmo conteúdo.

    `applyInssReal(cascade: DreCascade, inssReal: number | null): DreCascade`
    - Cascata com bloco `pessoal` já existente (total 24000) + `inssReal=3852.19` → bloco `pessoal.total` vira `27852.19`; `totalOperacionalDeducoes`, `resultadoOperacional` e `resultadoLiquido` também se ajustam pelo mesmo delta (+3852.19 nas deduções, -3852.19 no resultado operacional e no líquido).
    - `inssReal=null` → devolve a cascata SEM NENHUMA alteração (no-op — nunca chamar isto fora de apuração).
    - `inssReal=0` (mês 100% crédito) → PRECISA processar mesmo assim (bloco `pessoal.total` inalterado, +0) — prova de que o código usa `== null`, nunca um check de falsy (`!inssReal` seria um bug aqui).
    - Cascata SEM bloco `pessoal` (ex.: mês sem Salários, só existia a linha crua de INSS que já foi filtrada antes de montar a cascata) + `inssReal=2652.31` → CRIA a linha `{bloco:'pessoal', label:'Pessoal', total:2652.31, doubleCountRisk:false}`, inserida na FRENTE do array `operacionalBlocos` (pessoal é o primeiro bloco em `OPERACIONAL_BLOCOS`), preservando a ordem dos blocos já existentes depois dela.

    `resolveInssForCascade(input: {regime: DreRegime; rows: DreOperationalRow[]; guia: GuiaRealCategoryTotal[] | null}): {rows: DreOperationalRow[]; inssReal: number | null}`
    - `regime: "previsao"` → devolve `{rows: <rows sem NENHUMA alteração>, inssReal: null}`, MESMO que `guia` tenha dado real presente — previsão nunca desloca, ponto.
    - `regime: "apuracao"` com a fixture de abril (rows com Salários 24000 + INSS cru 1550; guia = `[{1550,cancelled},{2652.31,paid}]`) → devolve `{rows: [só Salários], inssReal: 2652.31}`.
    - Prova de integração (encadeando `resolveInssForCascade` → `buildDreCascade` → `applyInssReal` na mesma fixture de abril, regime `"apuracao"`): o total final do bloco `pessoal` é `24000 + 2652.31 = 26652.31` — NUNCA `24000 + 1550 = 25550` (isso provaria que a linha crua não foi substituída, e sim somada em cima).
    - Mesma fixture com regime `"previsao"`: o total final do bloco `pessoal` é `24000 + 1550 = 25550` — idêntico ao comportamento anterior a esta phase, mesmo com `guia` populada.
  </behavior>

  <action>
    RED primeiro: escreva `src/lib/dreInss.test.ts` com os casos do bloco `<behavior>` acima (mínimo 14 `it(...)`), commit só do teste, rode e confirme o vermelho (o módulo ainda não existe). Depois implemente `src/lib/dreInss.ts`:

    1. Sem import de React nem Supabase — módulo puro, mesmo padrão de `dreCascade.ts`/`dreRegime.ts`/`dreCloseGate.ts`.
    2. Exporte `export const INSS_FOLHA_CATEGORY = "Pessoal - INSS" as const;` — fonte única da grafia da categoria (evita erro de digitação divergente entre o filtro e a RPC).
    3. Um `round2` local (`Math.round(v * 100) / 100`), igual ao padrão já usado em `dreCascade.ts:96`/`dreCloseGate.ts:49` — não importar de outro módulo.
    4. `resolveInssReal` — clone literal da regra `apuracaoImpostoReal` de `dreRegime.ts:130-137` (filtra `status !== "cancelled"`, soma, `round2`), mas para 1 categoria: nulo/vazio → `null`; senão soma (podendo dar `0` quando tudo é cancelado).
    5. `filterRawInssRow` — `rows.filter(r => r.category !== INSS_FOLHA_CATEGORY)`.
    6. `applyInssReal` — curto-circuita em `inssReal == null` (nunca `!inssReal`, que quebraria o caso `0`); localiza o bloco `pessoal` em `cascade.operacionalBlocos`; se existir, soma `inssReal` ao seu `total` (mantendo `label`/`doubleCountRisk`); se NÃO existir, insere uma nova linha `{bloco:"pessoal", label:"Pessoal", total: round2(inssReal), doubleCountRisk:false}` no INÍCIO do array (pessoal é o primeiro bloco de `OPERACIONAL_BLOCOS`); ajusta `totalOperacionalDeducoes`/`resultadoOperacional`/`resultadoLiquido` pelo mesmo delta; devolve um NOVO objeto `DreCascade` (nunca mutação in-place).
    7. `resolveInssForCascade` — se `regime !== "apuracao"`, devolve `{rows, inssReal: null}` sem tocar `rows`; senão devolve `{rows: filterRawInssRow(rows), inssReal: resolveInssReal(guia)}`.
    8. Comentário de cabeçalho do arquivo explicando: por que este módulo existe separado de `dreRegime.ts` (regra de folha, não de imposto sobre venda — RESEARCH.md Open Question 2); a regra "previsão nunca desloca, só apuração usa M+1" (mesmo padrão do imposto de venda); e que `applyInssReal`/`resolveInssForCascade` NUNCA mutam `dreCascade.ts` — só consomem seu tipo `DreCascade` já pronto.
  </action>

  <verify>
    <automated>npx vitest run src/lib/dreInss.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/lib/dreInss.test.ts` → todos os casos do `<behavior>` passam (mínimo 14).
    - `grep -ci "from \"react\"\|from 'react'\|@supabase" src/lib/dreInss.ts` retorna `0` (módulo puro).
    - `grep -c "INSS_FOLHA_CATEGORY" src/lib/dreInss.ts` ≥ 3 (definida e usada).
    - Teste do caso `inssReal=0` presente e verde (prova que `== null`, não falsy, decide o curto-circuito).
    - `npx tsc --noEmit` → 0 erros.
  </acceptance_criteria>

  <done>`resolveInssReal`, `filterRawInssRow`, `applyInssReal` e `resolveInssForCascade` provados por teste, cobrindo pago/cancelado/ausente/múltiplas-linhas-mesma-competência (abril real) e o gate previsão×apuração — sem tocar em `dreCascade.ts`/`dreRegime.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: useInssGuiaReal — hook mirror de useImpostoGuiaReal</name>

  <read_first>
    - `src/hooks/useImpostoGuiaReal.ts` inteiro — sobretudo `useImpostoGuiaReal` (linhas 41-66): mesma forma de `queryKey`, mesmo `staleTime`, mesmo guard `enabled`, mesmo mapeamento de linhas (`category`/`total`/`status`) via `String`/`Number`. NÃO clone `useImpostoGuiaNudge` — o INSS não precisa do empurrãozinho de 3 sinais nesta phase (fora de escopo, CONTEXT.md `<deferred>`).
    - `src/lib/dreRegime.ts:44-51` — `monthPlusOne`, já importável, NUNCA reimplementar aritmética de data com concatenação de string (Pitfall 3, documentado em todo o projeto).
    - `src/lib/dreInss.ts` (Task 1) — não é importado por este hook, mas fixa o nome da RPC que este hook chama.
  </read_first>

  <files>src/hooks/useInssGuiaReal.ts (novo)</files>

  <action>
    Crie `src/hooks/useInssGuiaReal(saleMonth: string)` — mirror 1:1 de `useImpostoGuiaReal`, com estas correspondências exatas: `queryKey: ["dre", "inss-guia", orgId, pCompetence] as const` (troca só o segundo elemento, de `"imposto-guia"` para `"inss-guia"`, para não colidir cache com o hook irmão); mesmo `enabled: !!orgId && !!saleMonth`; mesmo `staleTime: 5 * 60 * 1000`; `pCompetence = saleMonth ? monthPlusOne(saleMonth) : null`; dentro do `queryFn`, chame `supabase.rpc("get_inss_guia_by_competence", { p_org_id: orgId, p_competence: pCompetence })` e mapeie o retorno exatamente como o hook irmão faz (`category: String(r.category)`, `total: Number(r.total ?? 0)`, `status: String(r.status ?? "")`). Tipo de retorno do hook: `useQuery<GuiaRealCategoryTotal[]>` (mesmo tipo já exportado de `@/lib/dreRegime`, reaproveitado — não redeclarar um tipo novo).

    Não crie teste dedicado para este hook — segue o mesmo padrão do `useImpostoGuiaReal.ts`, que também não tem `.test.ts` companion neste repo (só hooks com lógica de mapeamento mais complexa, como `useDreOperational`, têm teste próprio).
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "get_inss_guia_by_competence" src/hooks/useInssGuiaReal.ts` == 1.
    - `grep -c "monthPlusOne" src/hooks/useInssGuiaReal.ts` ≥ 1 — nunca aritmética de data manual.
    - `grep -c "staleTime: 5 \* 60 \* 1000" src/hooks/useInssGuiaReal.ts` == 1 (mesmo cache do hook irmão).
    - `npx tsc --noEmit` → 0 erros.
  </acceptance_criteria>

  <done>`useInssGuiaReal(saleMonth)` busca a guia real de INSS em `p_competence = monthPlusOne(saleMonth)`, mesmo padrão de cache/guard do hook irmão de imposto de venda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | `dreInss.ts` é puro (sem I/O); `useInssGuiaReal` só chama uma RPC já protegida por RLS+SECURITY INVOKER (Plano 98-01) — nenhum novo trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-03 | Tampering | `applyInssReal` processar `inssReal=0` como ausência de dado | mitigate | curto-circuito explícito em `== null` (nunca falsy) — Teste dedicado no `<behavior>` prova isso |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo neste plano |
</threat_model>

<verification>
- `npx vitest run src/lib/dreInss.test.ts` verde (≥14 casos).
- `npx tsc --noEmit` 0 erros.
- `npx vitest run` (suíte completa) sem regressão — nenhum arquivo fora dos 3 listados em `files_modified` foi tocado.
</verification>

<success_criteria>
- INSS-02: régua M+1 do INSS provada em módulo puro (pago/cancelado/ausente/múltiplas-linhas-mesma-competência real + gate previsão×apuração), hook irmão do imposto de venda criado.
- Nenhuma edição em `dreCascade.ts`, `dreRegime.ts`, `dreCloseGate.ts`, `useImpostoGuiaReal.ts` ou `MercadoLivre.tsx` (fiação fica para o Plano 98-03).
</success_criteria>

<output>
Create `.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-02-SUMMARY.md` when done.
</output>
