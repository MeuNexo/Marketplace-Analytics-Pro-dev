---
phase: 98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260716230000_get_inss_guia_by_competence.sql
autonomous: false
requirements: ["INSS-01"]

must_haves:
  truths:
    - "Existe uma RPC que devolve, por competência, o total de 'Pessoal - INSS' agrupado por status (paid/pending/cancelled) — a matéria-prima crua para o frontend aplicar a régua M+1"
    - "Uma org alheia (Thales) não lê nenhuma linha de INSS da Pé Vermeio (anti-IDOR)"
    - "O dado real de março (INSS paga) e abril (2 linhas na mesma competência: uma cancelada, uma paga) bate exatamente com o que a RPC devolve"
  artifacts:
    - path: "supabase/migrations/20260716230000_get_inss_guia_by_competence.sql"
      provides: "RPC get_inss_guia_by_competence — clone de get_imposto_guia_by_competence para categoria única 'Pessoal - INSS'"
      contains: "CREATE OR REPLACE FUNCTION public.get_inss_guia_by_competence"
      min_lines: 25
  key_links:
    - from: "supabase/migrations/20260716230000_get_inss_guia_by_competence.sql"
      to: "public.cash_outflows"
      via: "SELECT ... WHERE co.organization_id = p_org_id AND co.category = 'Pessoal - INSS' AND co.competence_date dentro do mês de p_competence"
      pattern: "co\\.category = 'Pessoal - INSS'"
---

<objective>
Criar a RPC `get_inss_guia_by_competence(p_org_id, p_competence)` — clone estrutural exato de `get_imposto_guia_by_competence`, mas para a categoria única `'Pessoal - INSS'` em vez das 3 categorias de imposto sobre venda. É a matéria-prima crua (category × status × total × n por competência) que o frontend (Plano 98-02) usa para aplicar a régua M+1 já validada para ICMS/PIS/COFINS.

Purpose: Wesley confirmou ao vivo (2026-07-16) que o INSS de folha segue a MESMA régua de competência deslocada (M+1) que os 3 impostos de venda já seguem. O mecanismo de deslocamento já existe e é testado (`dreRegime.ts`/`useImpostoGuiaReal.ts`) — falta só a fonte crua de dados para o INSS, isolada em sua própria RPC pequena e dedicada (o padrão já estabelecido no repo: uma RPC por regra de negócio, nunca uma genérica por lista de categorias).

Output: `get_inss_guia_by_competence` viva em prod `ckcdevcxgvueywivefgx`, SECURITY INVOKER, anti-IDOR provado, com os dados reais de março/abril conferidos.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-CONTEXT.md
</context>

<deploy_note>
O gsd-executor NÃO tem token da Supabase CLI neste ambiente. O trabalho do executor (Task 1) é ESCREVER o arquivo de migration em `supabase/migrations/`. A aplicação em prod (Task 2) é feita pelo ORQUESTRADOR via Supabase MCP `apply_migration` — nunca via SQL Editor, nunca `supabase db push`.
</deploy_note>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration get_inss_guia_by_competence (clone de get_imposto_guia_by_competence)</name>
  <files>supabase/migrations/20260716230000_get_inss_guia_by_competence.sql</files>

  <read_first>
    - `/root/garment-glow-dre/supabase/migrations/20260690000000_get_imposto_guia_by_competence.sql` — o MOLDE EXATO a clonar (worktree irmão; esta RPC não existe em nenhuma migration deste repo, o drift já está documentado no CONTEXT/RESEARCH). Ler o arquivo inteiro.
    - `.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-RESEARCH.md` — seção "Pattern 1" (o mesmo SQL já reproduzido) e o "Addendum" no final (confirmação ao vivo via MCP: `max(version)` real em prod = `20260716172353`; a migration nova precisa de um número ACIMA disso).
    - `supabase/migrations/20260716210000_cancelled_payables_dre.sql:31-82` — a definição VIVA de `get_dre_operational_by_competence` (RPC 87), só para confirmar a grafia EXATA da categoria (`'Pessoal - INSS'`, com espaços ao redor do hífen) tal como usada no `CASE` de mapeamento categoria→bloco.
  </read_first>

  <action>
    Crie a migration `supabase/migrations/20260716230000_get_inss_guia_by_competence.sql` clonando literalmente a estrutura de `get_imposto_guia_by_competence` (arquivo lido acima), com estas e SOMENTE estas diferenças:

    1. Nome da função: `public.get_inss_guia_by_competence` (mesmos parâmetros `p_org_id uuid, p_competence date`; mesmo retorno `TABLE (category text, total numeric, status text, n integer)`; mesmo `LANGUAGE sql STABLE SECURITY INVOKER SET search_path TO 'public'`).
    2. A cláusula `WHERE` troca a lista de 3 categorias (`co.category IN (...)`) por uma igualdade de categoria ÚNICA: `co.category = 'Pessoal - INSS'` — grafia idêntica à usada no `CASE` da RPC 87 (espaço-hífen-espaço, letras exatas).
    3. Mantenha idênticos: o filtro de organização (`co.organization_id = p_org_id`), o filtro de mês por `date_trunc('month', p_competence)` (início inclusive, início do mês seguinte exclusive — NUNCA comparar string "YYYY-MM"), o `GROUP BY co.category, co.status`, o `ORDER BY co.category, co.status` (mesmo que só exista 1 categoria, mantenha a mesma forma do molde por consistência), e as duas linhas finais `REVOKE EXECUTE ... FROM PUBLIC, anon;` / `GRANT EXECUTE ... TO authenticated;` com a assinatura `(uuid, date)`.
    4. Escreva um comentário de cabeçalho (estilo do molde e de `20260716210000_cancelled_payables_dre.sql`) explicando: (a) esta RPC é IRMÃ de `get_imposto_guia_by_competence` — nunca modificar aquela; (b) devolve dado CRU (category×status×total×n) sem nenhum deslocamento de mês — a régua M+1 é aplicada pelo FRONTEND (hook `useInssGuiaReal`, Plano 98-02), nunca aqui; (c) decisão travada por Wesley em 2026-07-16: o INSS de folha segue a mesma régua de competência M+1 que ICMS/PIS/COFINS; (d) esta migration precisa ser numerada acima do `max(version)` real de prod — reconfirmar via MCP `list_migrations` antes de aplicar (Task 2 faz essa reconfirmação).
    5. Anti-IDOR: `SECURITY INVOKER` (nunca `SECURITY DEFINER`) — a RLS de `cash_outflows` (já org-scoped, já anti-IDOR provada nas Phases 87/94/96) faz o isolamento; `p_org_id` de uma org alheia devolve 0 linhas pela RLS, não por lógica desta função.
  </action>

  <verify>
    <automated>test -f supabase/migrations/20260716230000_get_inss_guia_by_competence.sql && grep -c "CREATE OR REPLACE FUNCTION public.get_inss_guia_by_competence" supabase/migrations/20260716230000_get_inss_guia_by_competence.sql | grep -qx 1 && grep -c "SECURITY INVOKER" supabase/migrations/20260716230000_get_inss_guia_by_competence.sql | grep -qE '^[1-9]' && grep -c "co.category = 'Pessoal - INSS'" supabase/migrations/20260716230000_get_inss_guia_by_competence.sql | grep -qx 1 && grep -c "SECURITY DEFINER" supabase/migrations/20260716230000_get_inss_guia_by_competence.sql | grep -qx 0 && grep -cE "REVOKE EXECUTE ON FUNCTION public\.get_inss_guia_by_competence" supabase/migrations/20260716230000_get_inss_guia_by_competence.sql | grep -qx 1 && grep -cE "GRANT +EXECUTE ON FUNCTION public\.get_inss_guia_by_competence" supabase/migrations/20260716230000_get_inss_guia_by_competence.sql | grep -qx 1 && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo existe no caminho exato, com `CREATE OR REPLACE FUNCTION public.get_inss_guia_by_competence` presente exatamente 1 vez.
    - `SECURITY INVOKER` presente; `SECURITY DEFINER` ausente (0 ocorrências).
    - `co.category = 'Pessoal - INSS'` presente exatamente 1 vez (igualdade simples, NÃO uma lista `IN (...)`— isso distingue esta RPC da irmã de 3 categorias).
    - `REVOKE EXECUTE`/`GRANT EXECUTE` presentes com a assinatura `(uuid, date)`, mesmo padrão do molde.
    - Retorno `TABLE (category text, total numeric, status text, n integer)` idêntico ao molde.
  </acceptance_criteria>

  <done>Migration escrita, estruturalmente idêntica a `get_imposto_guia_by_competence` exceto pela categoria única e nome de função — pronta para o checkpoint de aplicação (Task 2).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Orquestrador — reconfirmar numeração, aplicar via MCP, provar dado real + anti-IDOR</name>
  <what-built>Migration `20260716230000_get_inss_guia_by_competence.sql` escrita no repo, pronta para deploy via MCP.</what-built>
  <action>SÓ O ORQUESTRADOR (Supabase MCP). O gsd-executor não tem token de CLI para este passo.</action>
  <how-to-verify>
    No projeto `ckcdevcxgvueywivefgx`:

    1. `list_migrations` — reconfirme o `max(version)` real (o RESEARCH.md achou `20260716172353` numa sessão anterior; isso PODE ter mudado). Se `20260716230000` colidir ou ficar abaixo do máximo real, renomeie o arquivo (e o nome dentro do `apply_migration`) para um valor maior, mantendo o padrão `YYYYMMDDHHMMSS`, antes de aplicar.
    2. `apply_migration` com o conteúdo do arquivo da Task 1. Espere sucesso (sem colisão de nome de função/policy).
    3. `execute_sql` — confirme os 2 casos reais conhecidos para a org Pé Vermeio (`7f615df7-7bac-45e5-8a93-827fb9ddeec7`):
       - `get_inss_guia_by_competence('7f615df7-7bac-45e5-8a93-827fb9ddeec7', '2026-03-01')` → 1 linha, `category='Pessoal - INSS'`, `total=1550.00`, `status='paid'`.
       - `get_inss_guia_by_competence('7f615df7-7bac-45e5-8a93-827fb9ddeec7', '2026-04-01')` → 2 linhas na mesma competência: `{total:1550.00, status:'cancelled'}` e `{total:2652.31, status:'paid'}` (o caso real de múltiplas linhas confirmado no addendum do RESEARCH.md).
    4. ANTI-IDOR: impersonando um membro da org Thales (`e4150d57-1349-48c9-9a89-82b1774857b0`), chame a RPC passando `p_org_id` = Pé Vermeio. Espere 0 linhas (a RLS de `cash_outflows` filtra, não a RPC).
    5. `get_advisors` (security) — confirme que nenhum erro NOVO foi introduzido por esta função.
    6. NO-REGRESSION SPOT-CHECK: confirme que `get_imposto_guia_by_competence` e `get_dre_operational_by_competence` permanecem com o corpo intocado (nenhuma migration desta Task altera essas funções).
  </how-to-verify>
  <resume-signal>Digite "approved" quando a RPC estiver viva, os 2 casos reais (março/abril) baterem exatamente com os valores acima, o anti-IDOR devolver 0 linhas para a org alheia, e os advisors estiverem limpos. Registre o número de migration FINAL usado (caso tenha sido renumerado) e o resultado do anti-IDOR verbatim no SUMMARY.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT → get_inss_guia_by_competence(p_org_id, p_competence) | Qualquer sessão autenticada pode chamar a RPC com um `p_org_id` arbitrário — a RLS de `cash_outflows` (via SECURITY INVOKER) é o único guarda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Information Disclosure | `get_inss_guia_by_competence` chamada com `p_org_id` de outra org | mitigate | `SECURITY INVOKER` (nunca `DEFINER`) — a RLS org-scoped de `cash_outflows` (já provada anti-IDOR nas Phases 87/94/96) filtra as linhas; a função nunca bypassa RLS |
| T-98-02 | Tampering | Migration numerada abaixo do `max(version)` real de prod, colidindo com uma migration aplicada por outra sessão | mitigate | Task 2 reconfirma `list_migrations` ao vivo ANTES de `apply_migration` — nunca confia só no `ls` local (Pitfall 5 do 98-RESEARCH.md) |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo neste plano — só SQL |
</threat_model>

<verification>
- Migration escrita e estruturalmente correta (Task 1, grep automatizado).
- Checkpoint do orquestrador: RPC viva, march/abril batendo exatamente, anti-IDOR = 0 linhas, advisors limpos, `get_imposto_guia_by_competence`/`get_dre_operational_by_competence` intocadas.
</verification>

<success_criteria>
- INSS-01: `get_inss_guia_by_competence` em prod, SECURITY INVOKER, clone estrutural da RPC irmã de imposto, categoria única `'Pessoal - INSS'`.
- Anti-IDOR provado com dado real (org alheia = 0 linhas).
- Nenhuma RPC existente (`get_imposto_guia_by_competence`, `get_dre_operational_by_competence`) modificada.
</success_criteria>

<output>
Create `.planning/phases/98-inss-de-folha-na-dre-deve-seguir-a-regua-m-1-competencia-igu/98-01-SUMMARY.md` when done. Registre o número final da migration (se renumerada) e o resultado verbatim do anti-IDOR e dos 2 casos reais (março/abril).
</output>
