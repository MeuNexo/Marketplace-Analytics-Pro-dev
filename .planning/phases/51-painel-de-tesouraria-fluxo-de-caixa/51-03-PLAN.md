---
phase: 51-painel-de-tesouraria-fluxo-de-caixa
plan: 03
type: execute
wave: 3
depends_on: ["51-01", "51-02"]
files_modified:
  - src/components/financial/TreasuryPanel.tsx
  - src/components/financial/CostCompositionChart.tsx
  - src/components/financial/SupplierExposureChart.tsx
  - src/pages/mercadolivre/MLFluxoCaixa.tsx
autonomous: false
requirements: [TESO-01, TESO-02, TESO-05]
user_setup: []

must_haves:
  truths:
    - "Aba Caixa Real mostra 12 KPIs em 3 faixas (Saúde de Caixa, Realizado, Exposição a Fornecedor)"
    - "Os 3 cards antigos (TodayBalanceCard, ProjectedBalanceCard, CapacityCard) saíram da página (D-01)"
    - "Gráfico Saldo Projetado (CashFlowChart) permanece intocado (D-11)"
    - "Gráfico Composição de Custos por Mês (barras empilhadas por categoria) renderiza (D-12)"
    - "Gráfico Exposição por Fornecedor (barras agrupadas 30/60/90d) renderiza (D-13)"
    - "Aba Simulador (Phase 50 / CashFlowSimulator) permanece intocada (D-TESO-05)"
  artifacts:
    - path: "src/components/financial/TreasuryPanel.tsx"
      provides: "container dos 12 KPIs em 3 faixas"
      min_lines: 80
      exports: ["TreasuryPanel"]
    - path: "src/components/financial/CostCompositionChart.tsx"
      provides: "BarChart empilhado por categoria/mês"
      exports: ["CostCompositionChart"]
    - path: "src/components/financial/SupplierExposureChart.tsx"
      provides: "BarChart agrupado 30/60/90d por fornecedor"
      exports: ["SupplierExposureChart"]
  key_links:
    - from: "src/components/financial/TreasuryPanel.tsx"
      to: "useTreasuryPanel / useProjectedBalance(90) / useFinancialSettings"
      via: "hooks de dados"
      pattern: "useTreasuryPanel"
    - from: "src/pages/mercadolivre/MLFluxoCaixa.tsx"
      to: "TreasuryPanel / CostCompositionChart / SupplierExposureChart"
      via: "render na TabsContent value=real"
      pattern: "TreasuryPanel"
---

<objective>
Construir a UI do Painel de Tesouraria e fazer a troca na página: (1) TreasuryPanel
com os 12 KPIs em 3 faixas rotuladas, (2) CostCompositionChart (barras empilhadas por
categoria), (3) SupplierExposureChart (barras agrupadas 30/60/90d), e (4) edição
cirúrgica de MLFluxoCaixa.tsx removendo os 3 cards antigos (D-01) e adicionando o painel
+ os 2 gráficos novos — mantendo CashFlowChart (D-11), o botão "Ajustar saldo" e a aba
Simulador (D-TESO-05) intactos. Fecha com checkpoint visual do Wesley no preview Vercel.

Purpose: Entregar o painel orientado a saúde de caixa e exposição a fornecedor conforme
a referência visual do Wesley. Implementa TESO-01 (12 KPIs), TESO-02 (3 gráficos) e
TESO-05 (Simulador preservado).

Output: 3 componentes novos + página reconfigurada; aprovação visual antes de qualquer
merge para main.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-CONTEXT.md
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-RESEARCH.md
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-PATTERNS.md

# Analogs (verbatim patterns):
@src/components/financial/TodayBalanceCard.tsx
@src/pages/mercadolivre/MLFinanceiro.tsx
# Arquivo a editar cirurgicamente:
@src/pages/mercadolivre/MLFluxoCaixa.tsx
# Hooks consumidos (Wave 2):
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-02-SUMMARY.md
</context>

<artifacts_produced>
| Símbolo | Tipo | Papel |
|---------|------|-------|
| `TreasuryPanel` | componente | 3 faixas × 4 KPIs (12 KPIs) |
| `CostCompositionChart` | componente | BarChart empilhado por categoria (D-12) |
| `SupplierExposureChart` | componente | BarChart agrupado 30/60/90d (D-13) |
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TreasuryPanel — 12 KPIs em 3 faixas (TESO-01)</name>
  <files>src/components/financial/TreasuryPanel.tsx</files>
  <read_first>
    - src/components/financial/TodayBalanceCard.tsx (currFmt; padrão de coloração `v >= 0 ? "text-kpi-positive" : "text-kpi-negative"`; skeleton; Card/CardContent)
    - 51-PATTERNS.md seção TreasuryPanel.tsx (imports, currFmt, dateFmt, band header, KPI mini-card, Alerta special case com AlertTriangle, runway guard)
    - 51-RESEARCH.md §9 (regras de cor por KPI) e §4 (fórmulas: Runway = current_balance/burn_rate; Resultado = entrada−saida)
    - 51-CONTEXT.md D-02..D-09b (definição de cada KPI)
  </read_first>
  <action>
    Componente named export `TreasuryPanel`. Consome 3 hooks: useTreasuryPanel(),
    useProjectedBalance(90) (D-04/D-05: chamar com horizonte 90 — current_balance,
    min_balance, critical_date) e useFinancialSettings() (alert_threshold para o label do
    Alerta). Loading: skeleton de 3 linhas × 4 células (padrão do PATTERNS).

    Helpers locais: currFmt (toLocaleString pt-BR/BRL) e dateFmt (ISO "YYYY-MM-DD" → "DD/MM/AAAA",
    "—" se null).

    Faixa 1 — "Saúde de Caixa" (header amber): 4 KPIs —
      • Saldo Atual = projected.current_balance (cor: positivo>0 verde / ≤0 vermelho, D-02);
      • Runway (meses) = `burn_rate > 0 ? current_balance / burn_rate : null` (guard divisão por
        zero, D-03; formato com 2 casas + " meses"; cor neutra);
      • Saldo Mín 90d = projected.min_balance (D-04; vermelho se < alert_threshold, senão neutro)
        com subtítulo = dateFmt(treasury.min_balance_date) (D-05 — data do saldo mínimo, DISTINTA
        do alert_date);
      • Alerta (D-06): se treasury.alert_date != null → AlertTriangle + "Saldo abaixo de
        {currFmt(alert_threshold)} em {dateFmt(alert_date)}" em kpi-negative com border
        destructive; senão "Sem alerta" em kpi-positive.

    Faixa 2 — "Realizado" (header azul/neutro): 4 KPIs (janela 30d, D-07) —
      • Entrada Real = treasury.entrada_real_30d (sempre kpi-positive);
      • Saída Real = treasury.saida_real_30d (sempre kpi-negative);
      • Resultado = entrada_real_30d − saida_real_30d (derivado; positivo verde / ≤0 vermelho);
      • Burn Rate = treasury.burn_rate (D-08, média 3m; kpi-neutral informativo). Subtítulo do
        Burn Rate: "média mensal (3m)" — deixa explícito que NÃO é a janela de 30d.

    Faixa 3 — "Exposição a Fornecedor" (header laranja): 4 KPIs (todos kpi-negative) —
      Fornec 30d / 60d / 90d (treasury.fornec_30d/60d/90d, D-09 cumulativo) e Total Exposição
      (treasury.total_exposicao, D-09b).

    Grid por faixa: `grid grid-cols-2 md:grid-cols-4 gap-3`. Header de faixa com label uppercase
    colorido + régua (padrão do PATTERNS). NÃO inventar dados — todos os valores vêm dos hooks.
  </action>
  <verify>
    <automated>test -f src/components/financial/TreasuryPanel.tsx && grep -q "useTreasuryPanel" src/components/financial/TreasuryPanel.tsx && grep -q "useProjectedBalance" src/components/financial/TreasuryPanel.tsx && grep -q "AlertTriangle" src/components/financial/TreasuryPanel.tsx && grep -q "burn_rate > 0" src/components/financial/TreasuryPanel.tsx && npx tsc --noEmit 2>&1 | grep -q "TreasuryPanel" && echo "TS-ERR" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    Arquivo existe (≥80 linhas); 12 KPIs presentes em 3 faixas; usa useTreasuryPanel +
    useProjectedBalance(90) + useFinancialSettings; Runway com guard de divisão por zero; Alerta
    com AlertTriangle e label de alert_threshold; Saldo Mín 90d com subtítulo min_balance_date;
    Resultado derivado de entrada−saída; tsc sem erro no arquivo.
  </acceptance_criteria>
  <done>Painel de 12 KPIs renderiza com dados reais e cores corretas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CostCompositionChart + SupplierExposureChart (TESO-02)</name>
  <files>src/components/financial/CostCompositionChart.tsx, src/components/financial/SupplierExposureChart.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFinanceiro.tsx linhas ~486–578 (estrutura ResponsiveContainer/BarChart, CartesianGrid/XAxis/YAxis style, stackId, radius só na última Bar)
    - 51-PATTERNS.md seções CostCompositionChart.tsx (CATEGORY_COLORS, pivot long→wide com useMemo, BarChart empilhado) e SupplierExposureChart.tsx (truncate, BarChart agrupado 3 Bars sem stackId)
    - 51-RESEARCH.md §6 (shapes e Pitfall 4 NULL→"Outros", Pitfall 5 pivot)
  </read_first>
  <action>
    Dois componentes named export. Ambos: container Card + CardContent com título; estado de
    loading via Skeleton; empty state simples quando sem dados (texto "Sem dados de
    {custos|exposição}"). currFmt local (pt-BR/BRL).

    CostCompositionChart (D-12): consome useCostByMonth(9). Mapa CATEGORY_COLORS conforme
    PATTERNS (9 categorias + "Outros"). Pivot long→wide com useMemo (Pitfall 5): agrupar por
    month, label "2026-04"→"Abr/26", set row[category]=total; allCategories = set único de
    categorias; ordenar por _sort. Renderizar BarChart empilhado: uma `<Bar stackId="stack">`
    por categoria com fill=CATEGORY_COLORS[cat] ?? "#94a3b8", radius [3,3,0,0] só na última.
    XAxis dataKey="month", YAxis tickFormatter R$k. Categoria NULL já vem como "Outros" da RPC.

    SupplierExposureChart (D-13): consome useSupplierExposure(10). Sem pivot (a RPC já entrega
    wide). Helper truncate(s,12) para nomes longos no XAxis. BarChart agrupado: 3 `<Bar>` SEM
    stackId — amount_30d (name "≤ 30d", #3b82f6), amount_60d ("≤ 60d", #f59e0b), amount_90d
    ("≤ 90d", #ef4444), cada uma radius [3,3,0,0], maxBarSize 20, barGap 2. XAxis
    dataKey="supplier" com tickFormatter=truncate. Legend + Tooltip com currFmt.

    Importar recharts named (BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip as
    RechartsTooltip, Legend, ResponsiveContainer). NÃO tocar em CashFlowChart.
  </action>
  <verify>
    <automated>test -f src/components/financial/CostCompositionChart.tsx && test -f src/components/financial/SupplierExposureChart.tsx && grep -q 'stackId="stack"' src/components/financial/CostCompositionChart.tsx && grep -q "useCostByMonth" src/components/financial/CostCompositionChart.tsx && grep -q "useSupplierExposure" src/components/financial/SupplierExposureChart.tsx && test $(grep -c "<Bar" src/components/financial/SupplierExposureChart.tsx) -ge 3 && npx tsc --noEmit 2>&1 | grep -qE "CostCompositionChart|SupplierExposureChart" && echo "TS-ERR" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    Ambos os arquivos existem; CostCompositionChart usa pivot + Bar stackId="stack" por
    categoria + useCostByMonth; SupplierExposureChart tem 3 Bars sem stackId + truncate +
    useSupplierExposure; tsc sem erro nesses arquivos.
  </acceptance_criteria>
  <done>Os 2 gráficos novos renderizam com dados reais das RPCs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wiring em MLFluxoCaixa (remover 3 cards, inserir painel + gráficos)</name>
  <files>src/pages/mercadolivre/MLFluxoCaixa.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFluxoCaixa.tsx (arquivo inteiro; imports linhas 28–32, grid de cards linhas 228–249, CashFlowChart linhas 251–258, aba Simulador 261–264)
    - 51-PATTERNS.md seção MLFluxoCaixa.tsx (modify) — removals e inserções exatas
    - 51-RESEARCH.md §8 (touch points: o que remover, o que manter)
  </read_first>
  <action>
    Edições cirúrgicas na aba Caixa Real apenas (TabsContent value="real"):
    (1) REMOVER os 3 imports: TodayBalanceCard, ProjectedBalanceCard, CapacityCard (D-01).
    (2) ADICIONAR imports: TreasuryPanel, CostCompositionChart, SupplierExposureChart.
    (3) SUBSTITUIR o `<div className="grid grid-cols-1 md:grid-cols-3 gap-4">` com os 3 cards
        por `<TreasuryPanel />`. MANTER o wrapper `flex flex-col gap-4` e o botão owner-only
        "Ajustar saldo de hoje" (continua relevante — initial_balance persiste).
    (4) ADICIONAR, abaixo do bloco CashFlowChart (ainda dentro de TabsContent value="real"),
        uma grid `grid grid-cols-1 lg:grid-cols-2 gap-6` com `<CostCompositionChart />` e
        `<SupplierExposureChart />`.

    NÃO TOCAR: CashFlowChart e seu hook useCashFlowData (D-11), AdjustBalanceDialog, o header
    sticky, a TabsContent value="simulador" / CashFlowSimulator (D-TESO-05), nem qualquer import
    fora dos listados. Os arquivos TodayBalanceCard/ProjectedBalanceCard/CapacityCard NÃO são
    deletados do disco — só removidos da página.
  </action>
  <verify>
    <automated>test $(grep -cE "TodayBalanceCard|ProjectedBalanceCard|CapacityCard" src/pages/mercadolivre/MLFluxoCaixa.tsx) -eq 0 && grep -q "<TreasuryPanel" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "<CostCompositionChart" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "<SupplierExposureChart" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "CashFlowSimulator" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "CashFlowChart" src/pages/mercadolivre/MLFluxoCaixa.tsx && npx tsc --noEmit 2>&1 | grep -q "MLFluxoCaixa" && echo "TS-ERR" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    Zero referências a TodayBalanceCard/ProjectedBalanceCard/CapacityCard; TreasuryPanel +
    CostCompositionChart + SupplierExposureChart renderizados na aba Caixa Real; CashFlowChart e
    CashFlowSimulator ainda presentes (D-11, D-TESO-05); tsc sem erro na página.
  </acceptance_criteria>
  <done>Página reconfigurada; build e tsc limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [checkpoint] Aprovação visual do Wesley no preview Vercel</name>
  <files>src/pages/mercadolivre/MLFluxoCaixa.tsx (preview Vercel — sem merge para main)</files>
  <action>Garantir tsc/build limpos, publicar branch de preview no Vercel (NAO merge para main) e conduzir a verificacao visual com o Wesley descrita em <how-to-verify>, incluindo a confirmacao da definicao do Burn Rate (D-08: 3 meses vs 30 dias). Registrar a decisao do Burn Rate no SUMMARY.</action>
  <what-built>
    Painel de Tesouraria completo na aba Caixa Real: 12 KPIs em 3 faixas + Saldo Projetado
    (reuso) + Composição de Custos por Mês + Exposição por Fornecedor. Cards antigos removidos.
    Aba Simulador preservada.
  </what-built>
  <how-to-verify>
    1. Garantir `npx tsc --noEmit` e `npm run build` limpos.
    2. Publicar branch de preview no Vercel (padrão do projeto: branch + .env.production +
       share link fura-login — ver reference_vercel_preview_workflow). NÃO fazer merge para main.
    3. Wesley acessa /fluxo-de-caixa no preview, aba "Caixa Real", e confere:
       • 12 KPIs nas 3 faixas (Saúde / Realizado / Exposição), layout espelhando a referência;
       • valores de Exposição (Fornec 30/60/90d, Total) e Burn Rate batendo com a referência;
       • Alerta mostrando "Saldo abaixo de R$30.000 em DD/MM/AAAA" quando aplicável;
       • gráficos Composição de Custos (empilhado) e Exposição por Fornecedor (agrupado) corretos;
       • gráfico Saldo Projetado intacto; aba Simulador intacta.
    4. **CONFIRMAR COM WESLEY a definição do Burn Rate (D-08):** hoje implementado como média
       mensal das saídas dos ÚLTIMOS 3 MESES (subtítulo "média mensal (3m)"), deliberadamente
       NÃO a janela de 30d — para não duplicar a "Saída Real" 30d. Wesley havia marcado "30 dias"
       no bloco que incluía o Burn Rate. Perguntar: manter 3m (atual) ou trocar para 30d
       (aceitando Burn Rate == Saída Real). Se ele preferir 30d, abrir tarefa de ajuste na RPC.
  </how-to-verify>
  <acceptance_criteria>
    Wesley aprova o painel no preview OU lista ajustes. Decisão do Burn Rate (3m vs 30d)
    registrada. Sem merge para main antes do "approved".
  </acceptance_criteria>
  <verify>
    <automated>MANUAL — checkpoint visual: `npx tsc --noEmit` e `npm run build` limpos + aprovacao do Wesley no preview Vercel</automated>
  </verify>
  <done>Wesley aprova o painel no preview (ou lista ajustes); decisao Burn Rate 3m/30d registrada; sem merge antes do approved.</done>
  <resume-signal>Wesley digita "approved" ou descreve ajustes (incl. preferência de Burn Rate 3m/30d).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| componentes React → hooks → RPC | dados de tesouraria vêm de RPCs INVOKER; nenhum render de dado cross-tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07 | Information Disclosure | TreasuryPanel renderizando valores de outra org | mitigate | Hooks usam currentOrg + RPC INVOKER (RLS); sem org_id manual na UI |
| T-51-08 | Tampering | quebra acidental do Simulador/CashFlowChart (D-11/D-TESO-05) | mitigate | Edição cirúrgica restrita à TabsContent value=real; verify grep garante CashFlowSimulator/CashFlowChart presentes |
| T-51-09 | Repudiation | merge sem aprovação | mitigate | Checkpoint blocking-human antes de qualquer merge para main (regra do ROADMAP) |
| T-51-SC | Tampering | npm installs | n/a | Zero pacotes novos (recharts/lucide/tanstack já instalados) |
</threat_model>

<verification>
- `tsc --noEmit` + `npm run build` limpos
- Zero referências aos 3 cards antigos em MLFluxoCaixa.tsx
- TreasuryPanel + 2 gráficos novos renderizados na aba Caixa Real
- CashFlowChart + CashFlowSimulator preservados (grep)
- Aprovação visual do Wesley registrada
</verification>

<success_criteria>
12 KPIs em 3 faixas + 3 gráficos na aba Caixa Real; 3 cards antigos fora; Simulador e Saldo
Projetado intactos; Wesley aprova no preview (com decisão Burn Rate registrada). TESO-01,
TESO-02 e TESO-05 satisfeitos.
</success_criteria>

<output>
Create `.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-03-SUMMARY.md` when done
</output>
