---
phase: 51-painel-de-tesouraria-fluxo-de-caixa
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - src/hooks/useTreasuryPanel.ts
  - src/hooks/useCostByMonth.ts
  - src/hooks/useSupplierExposure.ts
  - src/hooks/useFinancialSettings.ts
autonomous: true
requirements: [TESO-01, TESO-02, TESO-03]
user_setup: []

must_haves:
  truths:
    - "useTreasuryPanel() retorna os 10 escalares do painel (burn_rate, alert_threshold, alert_date, min_balance_date, entrada_real_30d, saida_real_30d, fornec_30d/60d/90d, total_exposicao)"
    - "useCostByMonth(months) retorna linhas {month, category, total} para o gráfico empilhado"
    - "useSupplierExposure(topN) retorna linhas {supplier, amount_30d, amount_60d, amount_90d}"
    - "useFinancialSettings expõe alert_threshold (default 30000) no tipo, no select e no mapeamento"
  artifacts:
    - path: "src/hooks/useTreasuryPanel.ts"
      provides: "hook RPC get_treasury_panel + interface TreasuryPanelData"
      exports: ["useTreasuryPanel", "TreasuryPanelData"]
    - path: "src/hooks/useCostByMonth.ts"
      provides: "hook RPC get_cost_by_month + interface CostByMonthRaw"
      exports: ["useCostByMonth", "CostByMonthRaw"]
    - path: "src/hooks/useSupplierExposure.ts"
      provides: "hook RPC get_supplier_exposure + interface SupplierExposureRow"
      exports: ["useSupplierExposure", "SupplierExposureRow"]
  key_links:
    - from: "src/hooks/useTreasuryPanel.ts"
      to: "get_treasury_panel RPC"
      via: "supabase.rpc('get_treasury_panel', { p_org_id })"
      pattern: "get_treasury_panel"
    - from: "src/hooks/useFinancialSettings.ts"
      to: "financial_settings.alert_threshold"
      via: "select inclui alert_threshold"
      pattern: "alert_threshold"
---

<objective>
Criar a camada de hooks (TanStack Query v5) que consome as 3 RPCs novas e estender
`useFinancialSettings` com `alert_threshold`. São 3 hooks novos + 1 edição cirúrgica,
todos espelhando o padrão exato de `useProjectedBalance.ts` (queryKey `["cashflow", ...]`,
staleTime 3min, enabled !!orgId, mapeamento Number(... ?? 0)).

Purpose: Desacoplar o frontend de tesouraria da forma das RPCs — os componentes da
Wave 3 recebem dados tipados e prontos. Habilita TESO-01 (KPIs), TESO-02 (gráficos) e
completa a ponta de leitura de TESO-03 (alert_threshold).

Output: 3 hooks novos + useFinancialSettings estendido; `tsc --noEmit` limpo.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-PATTERNS.md
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-RESEARCH.md

# Analog exato a copiar (imports, queryKey, mapeamento):
@src/hooks/useProjectedBalance.ts
# Arquivo a editar (4 edições cirúrgicas):
@src/hooks/useFinancialSettings.ts
# Símbolos das RPCs (forma de retorno) produzidos pela Wave 1:
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-01-SUMMARY.md
</context>

<artifacts_produced>
| Símbolo | Tipo | Forma |
|---------|------|-------|
| `useTreasuryPanel()` + `TreasuryPanelData` | hook + interface | `{ burn_rate, alert_threshold, alert_date:string\|null, min_balance_date:string\|null, entrada_real_30d, saida_real_30d, fornec_30d, fornec_60d, fornec_90d, total_exposicao }` |
| `useCostByMonth(months?=9)` + `CostByMonthRaw` | hook + interface | linhas `{ month:string, category:string, total:number }` |
| `useSupplierExposure(topN?=10)` + `SupplierExposureRow` | hook + interface | linhas `{ supplier:string, amount_30d, amount_60d, amount_90d }` |
| `FinancialSettings.alert_threshold` | campo de interface existente | `number` (default 30000) |
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar os 3 hooks de tesouraria</name>
  <files>src/hooks/useTreasuryPanel.ts, src/hooks/useCostByMonth.ts, src/hooks/useSupplierExposure.ts</files>
  <read_first>
    - src/hooks/useProjectedBalance.ts (imports `useQuery`/`supabase`/`useOrganization`; padrão de queryKey `["cashflow", <key>, orgId]`; staleTime 3*60*1000; enabled !!orgId; `data?.[0]` para 1-row, mapeamento Number(... ?? 0))
    - 51-PATTERNS.md seções useTreasuryPanel / useCostByMonth / useSupplierExposure (código completo de cada hook, copiar verbatim)
  </read_first>
  <action>
    Criar os 3 arquivos seguindo VERBATIM os blocos de código em 51-PATTERNS.md (eles já
    espelham useProjectedBalance.ts). Named exports (convenção do projeto). Cada hook:
    importa useQuery/supabase/useOrganization; deriva orgId = currentOrg?.id ?? null;
    enabled !!orgId; staleTime 3*60*1000.

    useTreasuryPanel.ts: queryKey ["cashflow","treasury_panel",orgId]; chama
    supabase.rpc("get_treasury_panel",{ p_org_id: orgId }); resultado 1-row via data?.[0];
    mapeia os 10 campos da interface TreasuryPanelData — Number(... ?? 0) para numéricos,
    `r.alert_date ? String(r.alert_date) : null` e `r.min_balance_date ? String(r.min_balance_date) : null`
    para as datas. INCLUIR min_balance_date (a RPC da Wave 1 retorna esse campo).

    useCostByMonth.ts: assinatura `useCostByMonth(months: number = 9)`; queryKey
    ["cashflow","cost_by_month",orgId,months]; rpc("get_cost_by_month",{ p_org_id, p_months: months });
    retorna array (data ?? []).map → { month:String, category:String, total:Number(... ?? 0) };
    retorno [] quando !orgId.

    useSupplierExposure.ts: assinatura `useSupplierExposure(topN: number = 10)`; queryKey
    ["cashflow","supplier_exposure",orgId,topN]; rpc("get_supplier_exposure",{ p_org_id, p_top_n: topN });
    retorna array map → { supplier:String(... ?? ""), amount_30d/60d/90d:Number(... ?? 0) };
    retorno [] quando !orgId.

    NÃO fazer select direto em cash_outflows/cash_inflows (risco de truncamento PostgREST —
    sempre via RPC). Tipar os campos `(r: any)` no map como nos analogs.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "useTreasuryPanel|useCostByMonth|useSupplierExposure" | head; test -f src/hooks/useTreasuryPanel.ts && test -f src/hooks/useCostByMonth.ts && test -f src/hooks/useSupplierExposure.ts && grep -q "treasury_panel" src/hooks/useTreasuryPanel.ts && grep -q "min_balance_date" src/hooks/useTreasuryPanel.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    3 arquivos existem; cada um exporta o hook + a interface (named export); queryKeys sob
    namespace "cashflow"; useTreasuryPanel mapeia os 10 campos incluindo min_balance_date;
    useCostByMonth/useSupplierExposure retornam arrays mapeados; tsc sem erro nesses arquivos.
  </acceptance_criteria>
  <done>Hooks tipados e compiláveis, consumindo as 3 RPCs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Estender useFinancialSettings com alert_threshold (D-10)</name>
  <files>src/hooks/useFinancialSettings.ts</files>
  <read_first>
    - src/hooks/useFinancialSettings.ts (arquivo inteiro, 53 linhas — 4 pontos de edição)
    - 51-PATTERNS.md seção useFinancialSettings (modify) — as 4 edições exatas com números de linha
  </read_first>
  <action>
    4 edições cirúrgicas, sem tocar em staleTime/queryKey/enabled:
    (1) interface FinancialSettings: adicionar `alert_threshold: number;`.
    (2) const DEFAULTS: adicionar `alert_threshold: 30000,`.
    (3) string do .select(): trocar para
        `"initial_balance, operational_cost_rate, safety_margin, alert_threshold"`.
    (4) objeto de retorno mapeado: adicionar
        `alert_threshold: Number(data.alert_threshold ?? 30000),`.
    Nenhuma outra mudança.
  </action>
  <verify>
    <automated>grep -c "alert_threshold" src/hooks/useFinancialSettings.ts | grep -qE "^[34]$" && grep -q "safety_margin, alert_threshold" src/hooks/useFinancialSettings.ts && npx tsc --noEmit 2>&1 | grep -q "useFinancialSettings" && echo "TS-ERR" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    alert_threshold aparece na interface, em DEFAULTS, na string do select e no mapeamento de
    retorno; default 30000; tsc sem novos erros em useFinancialSettings.ts.
  </acceptance_criteria>
  <done>useFinancialSettings expõe alert_threshold com default 30000.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend hook → RPC autenticada | supabase.rpc carrega o JWT da sessão; org isolada via RLS server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05 | Information Disclosure | hooks lendo cash_* | mitigate | Hooks chamam apenas RPCs SECURITY INVOKER (Wave 1); zero select direto em cash_inflows/cash_outflows — RLS + sem truncamento PostgREST |
| T-51-06 | Tampering | mapeamento de tipos solto | accept | `Number(... ?? 0)` e guards de null evitam NaN/undefined propagar para a UI; baixo risco, sem dado cross-tenant |
| T-51-SC | Tampering | npm installs | n/a | Zero pacotes novos (RESEARCH §"Standard Stack": tudo já instalado) |
</threat_model>

<verification>
- `tsc --noEmit` limpo para os 4 arquivos
- 3 hooks com queryKey sob "cashflow"
- useTreasuryPanel inclui min_balance_date
- useFinancialSettings.select inclui alert_threshold
</verification>

<success_criteria>
4 arquivos de hook prontos e tipados; consumo das 3 RPCs e da coluna alert_threshold
funcionando no nível de tipos; nenhum select direto em tabelas cash_*. TESO-01/TESO-02
(camada de dados) e TESO-03 (leitura) habilitados para a Wave 3.
</success_criteria>

<output>
Create `.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-02-SUMMARY.md` when done
</output>
