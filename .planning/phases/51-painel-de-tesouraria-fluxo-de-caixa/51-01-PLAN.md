---
phase: 51-painel-de-tesouraria-fluxo-de-caixa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260650000000_treasury_panel.sql
autonomous: false
requirements: [TESO-03, TESO-04]
user_setup: []

must_haves:
  truths:
    - "Coluna alert_threshold existe em financial_settings (numeric, default 30000)"
    - "RPC get_treasury_panel(uuid) existe, SECURITY INVOKER, retorna burn_rate/alert_threshold/alert_date/min_balance_date/entrada_real_30d/saida_real_30d/fornec_30d/60d/90d/total_exposicao"
    - "RPC get_cost_by_month(uuid,int) existe, SECURITY INVOKER, retorna month/category/total agrupado"
    - "RPC get_supplier_exposure(uuid,int) existe, SECURITY INVOKER, retorna supplier/amount_30d/60d/90d top-N"
    - "Migration aplicada no projeto LIVE ckcdevcxgvueywivefgx — as 3 RPCs respondem via SQL"
  artifacts:
    - path: "supabase/migrations/20260650000000_treasury_panel.sql"
      provides: "ALTER financial_settings + 3 RPCs SECURITY INVOKER + REVOKE/GRANT"
      contains: "get_treasury_panel"
  key_links:
    - from: "get_treasury_panel"
      to: "cash_outflows / cash_inflows / financial_settings"
      via: "agregação por p_org_id (RLS org-first, SECURITY INVOKER)"
      pattern: "SECURITY INVOKER"
---

<objective>
Criar a fundação de backend do Painel de Tesouraria: uma migration com a coluna
`alert_threshold` em `financial_settings` (D-10) e 3 RPCs de agregação
(`get_treasury_panel`, `get_cost_by_month`, `get_supplier_exposure`), todas
SECURITY INVOKER + BRT timezone + REVOKE PUBLIC / GRANT authenticated. A migration
é então APLICADA no projeto Supabase LIVE `ckcdevcxgvueywivefgx` via MCP
apply_migration (sem isso, os hooks da Wave 2 retornam erro — as RPCs não existem
em runtime).

Purpose: As RPCs entregam os 9 valores escalares do painel (Burn Rate, alert_date,
Entrada/Saída/Resultado 30d, Fornec 30/60/90d, Total Exposição) + as 2 séries dos
gráficos novos, com isolamento de tenant via RLS (nunca DEFINER + org_id por param —
risco IDOR). Implementa TESO-03 e TESO-04.

Output: 1 migration aplicada em produção; 3 RPCs validadas via SQL contra os
valores-âncora de referência do Wesley.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-CONTEXT.md
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-RESEARCH.md
@.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-PATTERNS.md

# Analog migration (copiar padrão SECURITY INVOKER + BRT + REVOKE/GRANT):
@supabase/migrations/20260619020000_cashflow_brt_timezone.sql
@supabase/migrations/20260618120000_cash_flow_rpcs.sql
@supabase/migrations/20260645011000_consultor_rpcs_revoke_public_execute.sql
# Schema das tabelas-fonte:
@supabase/migrations/20260618100000_cash_flow_tables.sql
</context>

<artifacts_produced>
Símbolos novos criados por este plano (downstream consome na Wave 2):

| Símbolo | Tipo | Assinatura / Forma |
|---------|------|--------------------|
| `financial_settings.alert_threshold` | coluna | `numeric NOT NULL DEFAULT 30000` |
| `get_treasury_panel(p_org_id uuid)` | RPC (1 row) | retorna `burn_rate, alert_threshold, alert_date, min_balance_date, entrada_real_30d, saida_real_30d, fornec_30d, fornec_60d, fornec_90d, total_exposicao` |
| `get_cost_by_month(p_org_id uuid, p_months int DEFAULT 9)` | RPC (multi-row) | retorna `month text, category text, total numeric` |
| `get_supplier_exposure(p_org_id uuid, p_top_n int DEFAULT 10)` | RPC (multi-row) | retorna `supplier text, amount_30d numeric, amount_60d numeric, amount_90d numeric` |
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration treasury_panel (coluna + 3 RPCs)</name>
  <files>supabase/migrations/20260650000000_treasury_panel.sql</files>
  <read_first>
    - supabase/migrations/20260619020000_cashflow_brt_timezone.sql (padrão SECURITY INVOKER + `v_today DATE := (now() AT TIME ZONE 'America/Sao_Paulo')::date`)
    - supabase/migrations/20260645011000_consultor_rpcs_revoke_public_execute.sql (padrão REVOKE FROM PUBLIC, anon / GRANT TO authenticated)
    - supabase/migrations/20260618100000_cash_flow_tables.sql (colunas exatas de cash_inflows.net_amount/release_date, cash_outflows.amount/outflow_date/supplier/category/status, financial_settings)
    - 51-RESEARCH.md §5 (SQL completo das 3 RPCs) e §4 (derivação por KPI)
  </read_first>
  <action>
    Criar a migration com 4 blocos, seguindo VERBATIM o SQL documentado em 51-RESEARCH.md §5,
    com UM ajuste sobre o §5 (ver abaixo). Header da migration deve conter o comentário de
    projeto: aplicar via MCP apply_migration em ckcdevcxgvueywivefgx; NUNCA `supabase db push`
    (CLI linkado ao projeto ERRADO gionpsuunfkkzzjdubfy).

    Bloco A — Coluna (D-10, TESO-03): `ALTER TABLE public.financial_settings ADD COLUMN
    alert_threshold numeric NOT NULL DEFAULT 30000` dentro de bloco idempotente
    `DO $$ BEGIN ... EXCEPTION WHEN duplicate_column THEN NULL; END $$;`. Sem nova policy RLS
    (a coluna herda as policies SELECT/ALL existentes de financial_settings).

    Bloco B — `get_treasury_panel(p_org_id uuid)` (D-02..D-09b, TESO-04): LANGUAGE plpgsql,
    SECURITY INVOKER, SET search_path = 'public'. `v_today := (now() AT TIME ZONE
    'America/Sao_Paulo')::date`. Calcular: v_current (initial_balance + inflows hoje −
    outflows hoje); burn_rate = SUM(cash_outflows.amount WHERE outflow_date entre v_today-90 e
    v_today exclusivo) / 3.0 (D-08); entrada_real_30d = SUM(cash_inflows.net_amount entre
    v_today-30 e v_today) (D-07); saida_real_30d = SUM(cash_outflows.amount entre v_today-30 e
    v_today AND status='paid') (D-07 — "Saída REAL/realizada" = efetivamente pago, resolvendo
    Open Question Q2 do RESEARCH; validado empiricamente: na janela de 30d paid==todos os status,
    R$208.127,35 idêntico, pois contas pending têm outflow_date futuro; Resultado é derivado no
    frontend); fornec_30d/60d/90d = SUM(amount WHERE supplier IS NOT NULL AND status='pending'
    AND outflow_date <= v_today+N) cumulativo (D-09); total_exposicao = SUM(amount WHERE
    supplier IS NOT NULL AND status='pending') qualquer data (D-09b). Loop 1..90 acumulando
    v_bal (inflows.release_date − outflows.outflow_date por dia) detectando alert_date (1º dia
    v_bal < alert_threshold, D-06) E min_balance_date (dia do menor v_bal no horizonte).
    AJUSTE SOBRE O §5: o RETURNS TABLE do §5 omite `min_balance_date`; ADICIONAR
    `min_balance_date DATE` ao RETURNS TABLE e ao RETURN QUERY (a interface da Wave 2 já espera
    esse campo — ver 51-PATTERNS.md useTreasuryPanel; D-05 = data do saldo mínimo é DISTINTA do
    alert_date, ver Pitfall 1 do RESEARCH). Ordem das colunas no RETURN QUERY deve casar com o
    RETURNS TABLE.

    Bloco C — `get_cost_by_month(p_org_id uuid, p_months int DEFAULT 9)` (D-12): LANGUAGE sql,
    SECURITY INVOKER, SET search_path = 'public'. SELECT TO_CHAR(outflow_date,'YYYY-MM') AS
    month, COALESCE(NULLIF(TRIM(category),''),'Outros') AS category, SUM(amount) AS total —
    WHERE organization_id=p_org_id AND outflow_date >= (DATE_TRUNC('month', v_today_brt) −
    (p_months-1) meses) — GROUP BY 1,2 ORDER BY 1,3 DESC. Usar a expressão BRT inline conforme §5.

    Bloco D — `get_supplier_exposure(p_org_id uuid, p_top_n int DEFAULT 10)` (D-13): LANGUAGE
    plpgsql, SECURITY INVOKER, SET search_path='public', v_today BRT. RETURN QUERY com
    SUM(amount) FILTER (WHERE outflow_date <= v_today+30/60/90) por supplier, WHERE supplier IS
    NOT NULL AND status='pending' AND outflow_date <= v_today+90, GROUP BY supplier, ORDER BY
    amount_90d DESC, LIMIT p_top_n.

    Bloco E — REVOKE/GRANT para as 3 funções: `REVOKE EXECUTE ... FROM PUBLIC, anon;` +
    `GRANT EXECUTE ... TO authenticated;` com as assinaturas exatas
    (uuid), (uuid,int), (uuid,int).

    NÃO usar SECURITY DEFINER em nenhuma função. NÃO passar org_id como filtro de bypass — a
    RLS org-first das tabelas faz o isolamento sob SECURITY INVOKER.
  </action>
  <verify>
    <automated>grep -q "SECURITY INVOKER" supabase/migrations/20260650000000_treasury_panel.sql && test $(grep -v '^--' supabase/migrations/20260650000000_treasury_panel.sql | grep -c "SECURITY DEFINER") -eq 0 && grep -q "min_balance_date" supabase/migrations/20260650000000_treasury_panel.sql && grep -q "America/Sao_Paulo" supabase/migrations/20260650000000_treasury_panel.sql && grep -q "FROM PUBLIC, anon" supabase/migrations/20260650000000_treasury_panel.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    Arquivo existe; contém as 4 definições (1 ALTER idempotente + 3 CREATE OR REPLACE FUNCTION);
    todas SECURITY INVOKER (zero SECURITY DEFINER em linhas não-comentário); todas usam
    `(now() AT TIME ZONE 'America/Sao_Paulo')::date`; get_treasury_panel inclui min_balance_date
    no RETURNS TABLE e no RETURN QUERY; bloco REVOKE/GRANT presente para as 3 funções.
  </acceptance_criteria>
  <done>Migration escrita e idempotente, pronta para apply.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Aplicar a migration no projeto LIVE ckcdevcxgvueywivefgx</name>
  <files>supabase/migrations/20260650000000_treasury_panel.sql (aplicada via MCP)</files>
  <action>Aplicar a migration da Task 1 no projeto Supabase LIVE ckcdevcxgvueywivefgx via MCP apply_migration (nome: treasury_panel). Executado pelo orquestrador — o gsd-executor nao tem MCP/token de deploy Supabase. NUNCA usar `supabase db push` (CLI linkado ao projeto ERRADO gionpsuunfkkzzjdubfy). Apos aplicar, rodar os smokes de validacao descritos em <how-to-verify>.</action>
  <what-built>
    A migration supabase/migrations/20260650000000_treasury_panel.sql (coluna alert_threshold +
    3 RPCs). O gsd-executor NÃO tem MCP/token de deploy Supabase — o orquestrador aplica.
  </what-built>
  <how-to-verify>
    1. Aplicar via MCP Supabase `apply_migration` no projeto `ckcdevcxgvueywivefgx` (nome:
       `treasury_panel`, conteúdo = arquivo da Task 1). NUNCA `supabase db push`.
    2. Validar a coluna:
       `SELECT column_name FROM information_schema.columns WHERE table_name='financial_settings' AND column_name='alert_threshold';`
       → 1 linha.
    3. Validar que as 3 RPCs existem e são INVOKER:
       `SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('get_treasury_panel','get_cost_by_month','get_supplier_exposure');`
       → 3 linhas, todas com `prosecdef = false` (INVOKER).
    4. Smoke da RPC com a org Pé Vermeio (`7f615df7-...`, descobrir o uuid exato via
       `SELECT id FROM organizations WHERE ...` ou usar o conhecido da STATE.md):
       `SELECT * FROM get_treasury_panel('<org-id-pe-vermeio>');`
       Comparar contra os valores-âncora de referência (RESEARCH §"Reference Values", delta
       razoável pelos dias decorridos): Burn Rate ≈ 124.942; Fornec 30d ≈ 133k / 60d ≈ 226k /
       90d ≈ 311k; Total Exposição ≈ 671k. NOTA: Entrada/Saída Real 30d vão DIFERIR da imagem
       (a imagem reflete ~3m; D-07 fixa 30d) — isso é esperado, não é erro.
    5. Smoke das séries:
       `SELECT * FROM get_cost_by_month('<org-id>', 9) LIMIT 5;` (linhas month/category/total) e
       `SELECT * FROM get_supplier_exposure('<org-id>', 10);` (≤10 fornecedores, amount_90d desc).
  </how-to-verify>
  <acceptance_criteria>
    Coluna alert_threshold presente; 3 RPCs existem com prosecdef=false; get_treasury_panel
    retorna valores na ordem de grandeza dos âncora (Burn/Fornec/Total Exposição); as 2 RPCs de
    série retornam linhas. Sem isso, a Wave 2 falha em runtime (RPC inexistente).
  </acceptance_criteria>
  <verify>
    <automated>MANUAL — orquestrador valida via SQL: coluna alert_threshold em information_schema.columns; 3 RPCs com prosecdef=false em pg_proc; smoke get_treasury_panel bate ordem de grandeza dos ancora</automated>
  </verify>
  <done>Migration aplicada em ckcdevcxgvueywivefgx; coluna + 3 RPCs INVOKER respondendo via SQL.</done>
  <resume-signal>Digite "aplicada" após confirmar coluna + 3 RPCs INVOKER + smoke dos valores-âncora.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado → RPC | JWT do usuário cruza para o Postgres; org derivada via RLS, não por param confiável |
| Postgres role → tabelas cash_* | RLS org-first isola tenants; SECURITY INVOKER preserva o role do chamador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Information Disclosure (IDOR) | get_treasury_panel / get_cost_by_month / get_supplier_exposure | mitigate | SECURITY INVOKER + RLS org-first; org_id NÃO é trust-bypass param (a RLS das tabelas filtra). DEFINER seria IDOR CRITICAL (ver feedback_supabase_security_invoker) |
| T-51-02 | Elevation of Privilege | EXECUTE nas 3 RPCs | mitigate | REVOKE EXECUTE FROM PUBLIC, anon; GRANT EXECUTE TO authenticated — anon não executa |
| T-51-03 | Tampering | search_path hijack | mitigate | SET search_path = 'public' em todas as 3 funções |
| T-51-04 | Information Disclosure | timezone boundary (UTC vs BRT) expõe janela errada | accept→mitigate | `(now() AT TIME ZONE 'America/Sao_Paulo')::date` — sem dado cross-tenant, só correção de janela |
| T-51-SC | Tampering | npm/pip/cargo installs | n/a | Sem instalação de pacotes nesta fase (RESEARCH §"Package Legitimacy Audit": zero pacotes novos) |
</threat_model>

<verification>
- `grep -c "SECURITY DEFINER"` (linhas não-comentário) na migration = 0
- 3 RPCs com prosecdef=false em pg_proc no projeto LIVE
- Coluna alert_threshold em information_schema.columns
- Smoke get_treasury_panel bate ordem de grandeza dos âncora (Burn/Fornec/Total Expo)
</verification>

<success_criteria>
Migration aplicada em ckcdevcxgvueywivefgx; coluna alert_threshold + 3 RPCs SECURITY INVOKER
existindo e respondendo via SQL; valores-âncora de exposição/burn batem (delta razoável).
TESO-03 e TESO-04 satisfeitos no nível de backend.
</success_criteria>

<output>
Create `.planning/phases/51-painel-de-tesouraria-fluxo-de-caixa/51-01-SUMMARY.md` when done
</output>
