---
phase: 53-camada-llm-an-lise-inteligente
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - src/hooks/useConsultorInsights.ts
  - src/components/mercadolivre/ConsultorLLMSummary.tsx
  - src/components/mercadolivre/ConsultorCard.tsx
  - src/pages/MercadoLivre.tsx
autonomous: false
requirements: [LLM-01, LLM-02, LLM-03, LLM-04, LLM-06, LLM-07]

must_haves:
  truths:
    - "Lojista vê um resumo COO em prosa PT-BR no topo do painel /vendas (SC-3 / LLM-01, LLM-03)"
    - "Lojista clica 'Explicar' por insight e recebe explicação sob demanda, cacheada por insight/dia (SC-4 / LLM-02)"
    - "Reabrir o painel não re-gera nem re-cobra (cached); botão 'Atualizar análise' força regeneração respeitando o cap (LLM-04)"
    - "Indicador 'análise desatualizada — clique para atualizar' aparece quando o estado dos insights muda após a geração (SC-6 / LLM-06)"
    - "llm_enabled=false → UI mostra só o consultor determinístico puro, sem resumo COO (SC-7 / LLM-07)"
    - "Quando a EF retorna fallback:true, a UI mostra o texto determinístico do v1 (LLM-05)"
  artifacts:
    - path: "src/components/mercadolivre/ConsultorLLMSummary.tsx"
      provides: "Bloco de resumo COO: prosa + botão 'Atualizar análise' + badge 'análise desatualizada' + skeleton"
      min_lines: 50
    - path: "src/hooks/useConsultorInsights.ts"
      provides: "Query resumo COO + mutation 'Explicar' + flags disabled/stale/fallback/cap"
      exports: ["useConsultorInsights"]
  key_links:
    - from: "src/components/mercadolivre/ConsultorLLMSummary.tsx"
      to: "src/hooks/useConsultorInsights.ts"
      via: "consome o resumo COO + isStale + refresh + disabled do hook estendido"
      pattern: "useConsultorInsights"
    - from: "src/hooks/useConsultorInsights.ts"
      to: "EF consultor-llm (Plan 53-01)"
      via: "supabase.functions.invoke('consultor-llm', { mode })"
      pattern: "consultor-llm"
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/components/mercadolivre/ConsultorLLMSummary.tsx"
      via: "renderizado acima do <ConsultorCard> quando connected (linha ~694)"
      pattern: "ConsultorLLMSummary"
---

<objective>
Plugar a camada LLM no frontend: estender `useConsultorInsights` com a query do resumo COO e a mutation "Explicar", criar o componente `ConsultorLLMSummary` (prosa + botão "Atualizar análise" + badge "análise desatualizada"), renderizá-lo no topo do painel `/vendas` (MercadoLivre.tsx), e adicionar o botão "Explicar" por insight no `ConsultorCard`.

Tudo é aditivo: o consultor determinístico v1 continua intacto. Quando `llm_enabled=false` (kill-switch) ou a EF retorna `fallback:true`, a UI mostra só o v1. Nenhum pacote npm novo — usa shadcn/ui (Card/Button/Skeleton/Badge) + sonner + react-query já instalados.

Purpose: entregar a experiência visível ao lojista — narrativa causal estilo COO + explicação por insight, cacheada, desligável, com sinal de desatualização.
Output: `ConsultorLLMSummary.tsx`, extensão de `useConsultorInsights.ts`, "Explicar" no `ConsultorCard.tsx`, plug em `MercadoLivre.tsx`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-camada-llm-an-lise-inteligente/53-RESEARCH.md
@.planning/phases/53-camada-llm-an-lise-inteligente/53-01-PLAN.md

# Arquivos a estender (ler antes de codar):
@src/hooks/useConsultorInsights.ts
@src/components/mercadolivre/ConsultorCard.tsx
@src/pages/MercadoLivre.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender useConsultorInsights (resumo COO + Explicar + flags disabled/stale/fallback)</name>
  <read_first>
    - src/hooks/useConsultorInsights.ts inteiro (padrão de query/mutation/invoke linha 153-160)
    - 53-01-PLAN.md (contrato da EF: modes summary/explain, retornos {analysis_text,cached,stale}/{disabled}/{fallback})
    - 53-RESEARCH.md Open Question 1 (Explicar cacheado por rule_key) + Code Examples (invoke pattern)
  </read_first>
  <files>src/hooks/useConsultorInsights.ts</files>
  <action>Estender o hook existente SEM quebrar a API atual (insights/score/dismiss continuam). Adicionar: (1) uma query `summary` que invoca `supabase.functions.invoke('consultor-llm', { body: { mode: 'summary' } })` (JWT do usuário automático, igual o invoke da linha 153), com staleTime alto (mesmo dia), expondo `summaryText`, `summaryDisabled` (resp.disabled), `summaryFallback` (resp.fallback), `summaryStale` (resp.stale) e `summaryLoading`; (2) uma mutation `refreshSummary()` que invoca com `{ mode:'summary', force_refresh:true }` e invalida a query (botão "Atualizar análise", LLM-04 — o cap é aplicado server-side pela EF; se a EF responder erro de cap, exibir toast sonner); (3) uma mutation `explain(insightId)` que invoca `{ mode:'explain', insight_id }`, retornando o texto da explicação (cache por insight/dia é responsabilidade da EF via prompt_version). Quando `summaryFallback` ou `summaryDisabled` true, os consumidores devem cair para o v1 — o hook só expõe as flags, a decisão de render é da UI. Manter tipagem explícita (interfaces no topo do arquivo, padrão do projeto). Não logar respostas brutas.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Hook expõe summaryText/summaryDisabled/summaryFallback/summaryStale/summaryLoading + refreshSummary() + explain(); API antiga (insights/score/dismiss) intacta; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: ConsultorLLMSummary (prosa COO + Atualizar análise + badge desatualizada) e plug em /vendas</name>
  <read_first>
    - src/pages/MercadoLivre.tsx:694 (bloco {connected && <ConsultorCard.../>}) — ponto de plug
    - src/components/mercadolivre/ConsultorCard.tsx (estilo Card/Badge do consultor, tokens de cor)
    - 53-RESEARCH.md Code Examples (plug do resumo) + Don't Hand-Roll (render como <p>/split por \n, sem markdown renderer)
  </read_first>
  <files>src/components/mercadolivre/ConsultorLLMSummary.tsx, src/pages/MercadoLivre.tsx</files>
  <action>Criar `ConsultorLLMSummary.tsx`: consome o hook estendido. Estados: loading → Skeleton; `summaryDisabled` → não renderiza nada (kill-switch, LLM-07 — UI mostra só o v1 ConsultorCard); `summaryFallback` → não renderiza o bloco LLM (cai pro v1, LLM-05); caso normal → Card com a prosa COO (renderizar como parágrafos: split por `\n`, sem markdown renderer), um botão "Atualizar análise" (chama refreshSummary, com loading), e — quando `summaryStale` — um badge/linha "análise desatualizada — clique para atualizar" (LLM-06) que dispara o mesmo refresh. Erro de cap → toast sonner. Usar shadcn Card/Button/Skeleton/Badge + lucide icon, consistente com ConsultorCard. Em `MercadoLivre.tsx`, renderizar `{connected && <ConsultorLLMSummary />}` imediatamente ACIMA do `{connected && (<ConsultorCard .../>)}` (linha ~694) — mudança 100% aditiva, sem tocar o ConsultorCard existente nem os KPIs. Importar o componente no topo (padrão dos imports do arquivo).</action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "ConsultorLLMSummary" src/pages/MercadoLivre.tsx</automated>
  </verify>
  <done>ConsultorLLMSummary renderiza prosa COO + botão Atualizar + badge stale; respeita disabled/fallback (cai pro v1); plugado acima do ConsultorCard em /vendas; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 3: Botão "Explicar" por insight no ConsultorCard (cache insight/dia via EF)</name>
  <read_first>
    - src/components/mercadolivre/ConsultorCard.tsx (InsightItem, linha 67-93 — onde adicionar o botão)
    - 53-RESEARCH.md (Explicar por insight, mode:'explain', cache por insight/dia)
  </read_first>
  <files>src/components/mercadolivre/ConsultorCard.tsx</files>
  <action>Adicionar em `InsightItem` (ou no card) um botão "Explicar" por linha de insight que chama `explain(insight.id)` do hook (passado via prop ou consumido — manter o ConsultorCard recebendo a função por prop para não acoplar o card ao hook, seguindo o padrão de onDismiss). Estado por linha: enquanto carrega mostra spinner; ao resolver mostra a explicação (prosa) abaixo do título do insight; reabrir a mesma explicação no mesmo dia volta cacheada (a 2ª invoke retorna cached — responsabilidade da EF). Erro → toast sonner. Mudança aditiva: não alterar dismiss, score line, nem o "Ver todos". Adicionar a prop `onExplain?: (id:string)=>Promise<string>` à interface ConsultorCardProps e passá-la de MercadoLivre.tsx (e MLConsultor se aplicável) a partir do hook. Não embutir literais de instrução de prompt-injection.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "Explicar" src/components/mercadolivre/ConsultorCard.tsx</automated>
  </verify>
  <done>Cada insight tem botão "Explicar" que busca explicação sob demanda; spinner durante; explicação exibida; 2ª abertura same-day vem cacheada; resto do card intacto; tsc verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: validação visual do resumo COO + Explicar + stale + kill-switch</name>
  <what-built>Resumo COO no topo de /vendas + botão "Explicar" por insight + badge "análise desatualizada" + botão "Atualizar análise" + respeito a kill-switch/fallback.</what-built>
  <action>[BLOCKING] Owner valida visualmente o painel /vendas seguindo os passos abaixo (preview Vercel) antes de fechar a fase.</action>
  <how-to-verify>
    1. Abrir /vendas com uma org conectada e llm_enabled=true → ver o resumo COO em prosa causal acima do card de saúde (ex: "TACoS subiu, puxando a margem").
    2. Reabrir /vendas → resumo vem do cache (sem nova cobrança/geração).
    3. Clicar "Explicar" em um insight → explicação aparece; clicar de novo no mesmo dia → instantâneo (cached).
    4. Forçar mudança nos insights e recarregar → badge "análise desatualizada — clique para atualizar"; clicar "Atualizar análise" → regenera.
    5. Setar consultor_config.llm_enabled=false → o resumo COO some; o ConsultorCard v1 continua.
    6. Conferir que nenhum número do resumo diverge dos insights (se divergir, a EF deveria ter caído pro v1 — fallback).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva problemas (ex: resumo re-gera ao reabrir, badge stale não aparece, kill-switch não esconde, números divergentes).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → EF consultor-llm | invoke via supabase-js com JWT do usuário (sem header manual) |
| EF response → render | analysis_text/explicação é texto gerado por LLM exibido ao lojista |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-08 | Information Disclosure | render de texto LLM com dado de outra org | mitigate | a EF (53-01) já escopa por org; UI só renderiza o que a EF retorna para o JWT atual |
| T-53-09 | Integrity | exibir número alucinado ao lojista | mitigate | UI cai pro v1 quando summaryFallback=true (validação numérica é server-side em 53-01) |
| T-53-10 | DoS (custo) | botão Atualizar em loop / re-geração ao reabrir | mitigate | cache server-side (cached na 2ª invoke); cap diário aplicado pela EF; query com staleTime alto |
| T-53-11 | Tampering | XSS via texto LLM | mitigate | render como texto/`<p>` (React escapa por padrão); SEM dangerouslySetInnerHTML, sem markdown renderer |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote externo instalado (RESEARCH: zero npm); usa shadcn/sonner/react-query já presentes |
</threat_model>

<verification>
- `npx tsc --noEmit` — types verdes.
- `npm run build` — build verde (Vite).
- `grep -c "ConsultorLLMSummary" src/pages/MercadoLivre.tsx` — plug presente.
- Checkpoint visual do owner: resumo COO + Explicar + stale + kill-switch.
</verification>

<success_criteria>
Resumo COO em prosa no topo de /vendas (cacheado, com Atualizar análise + badge desatualizada); Explicar por insight cacheado; kill-switch e fallback caem pro v1; consultor determinístico intacto; build verde.
</success_criteria>

<output>
Create `.planning/phases/53-camada-llm-an-lise-inteligente/53-02-SUMMARY.md` when done.
</output>
