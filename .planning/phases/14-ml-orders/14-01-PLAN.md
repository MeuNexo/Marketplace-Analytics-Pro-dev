---
phase: 14-ml-orders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/mercado-libre-integration/index.ts
autonomous: true
requirements: [ORDERS-01, ORDERS-02]

must_haves:
  truths:
    - "Nenhuma referência a `orderRows` permanece em mercado-libre-integration/index.ts após o deploy"
    - "`fetchShipmentStates` retorna apenas `{ uf, state_name }` — sem `base_cost` nem `cidade`"
    - "A resposta da função `mercado-libre-integration` não é afetada: mesmos dados de daily/hourly cache, mesmas métricas"
  artifacts:
    - path: "supabase/functions/mercado-libre-integration/index.ts"
      provides: "Função sem o bloco fire-and-forget e com fetchShipmentStates limpa"
      contains: "fetchShipmentStates"
  key_links:
    - from: "supabase/functions/mercado-libre-integration/index.ts"
      to: "public.orders"
      via: "ausência de escrita direta — escrita passa a ser exclusiva de sync-ml-orders"
      pattern: "orderRows"
---

<objective>
Remover o bloco fire-and-forget de escrita em `public.orders` de `mercado-libre-integration` e reverter `fetchShipmentStates` ao tipo de retorno original.

Purpose: O bloco (linhas 786-866) escreve em `public.orders` sem calcular `custo_unit`, `tax_rate`, `tax_amount`, `receita_bruta` ou `receita_liquida`. Quando esse bloco executa antes de `sync-ml-orders`, os campos ficam nulos; quando executa depois, sobrescreve dados completos com dados incompletos. A escrita autorizada em `public.orders` é responsabilidade exclusiva de `sync-ml-orders`.
Output: `mercado-libre-integration/index.ts` sem o bloco fire-and-forget e com `fetchShipmentStates` revertida ao tipo `Map<string, { uf: string; state_name: string }>`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-ml-orders/14-RESEARCH.md

Arquivo principal: `supabase/functions/mercado-libre-integration/index.ts` (946 linhas)

**Bloco a remover — linhas 786-866 (exatas):**
- Linha 786: `// Orders upsert — fire-and-forget, não bloqueia o response`
- Linhas 787-841: loop que monta o array `orderRows`
- Linhas 844-866: IIFE `(async () => { ... })();` que faz o upsert em lote
- Linha 867 em diante: `await Promise.all(upsertPromises);` — DEVE PERMANECER

**`fetchShipmentStates` a reverter — linhas 46-109:**
Tipo de retorno atual (com expansão):
```typescript
Promise<Map<string, { uf: string; state_name: string; base_cost: number | null; cidade: string | null }>>
```
Tipo de retorno alvo (original):
```typescript
Promise<Map<string, { uf: string; state_name: string }>>
```

Campos extras a remover dentro do worker (linhas 89-97):
```typescript
const baseCostRaw = data?.base_cost ?? data?.cost?.gross ?? null;
const base_cost = baseCostRaw != null ? Number(baseCostRaw) : null;
const addrCity = data?.receiver_address?.city ?? null;
const cidade = addrCity
  ? (typeof addrCity === "object" ? (addrCity as any)?.name ?? null : String(addrCity))
  : null;
```
E a linha do `map.set` (linha 97) muda de:
```typescript
map.set(sid, { uf, state_name: stateName, base_cost, cidade });
```
para:
```typescript
map.set(sid, { uf, state_name: stateName });
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover bloco fire-and-forget (linhas 786-866)</name>
  <files>supabase/functions/mercado-libre-integration/index.ts</files>
  <action>
Abrir o arquivo. Localizar a linha com o comentário `// Orders upsert — fire-and-forget, não bloqueia o response` (linha 786). Remover do início dessa linha até o fechamento do bloco `if (orderRows.length > 0) { ... })(); }` na linha 866 (inclusive). A linha 868 (`await Promise.all(upsertPromises);`) deve permanecer intacta — ela é o continuation do código de daily/hourly cache, não faz parte do bloco removido.

Verificação de corte exato: após a remoção, a linha imediatamente após o último bloco de `upsertPromises.push(...)` deve ser `await Promise.all(upsertPromises);` sem nenhuma referência a `orderRows` entre elas.

Pitfall: NÃO remover `await Promise.all(upsertPromises)` — essa linha finaliza os upserts de daily/hourly/product/user cache que são o trabalho principal da função.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && grep -c "orderRows" supabase/functions/mercado-libre-integration/index.ts</automated>
  </verify>
  <done>O comando grep retorna 0. Nenhuma referência a `orderRows` permanece no arquivo.</done>
</task>

<task type="auto">
  <name>Task 2: Reverter `fetchShipmentStates` ao tipo original (linhas 46-109)</name>
  <files>supabase/functions/mercado-libre-integration/index.ts</files>
  <action>
Após a Task 1, editar a função `fetchShipmentStates`:

1. Alterar a assinatura (linha 49) de:
```typescript
Promise<Map<string, { uf: string; state_name: string; base_cost: number | null; cidade: string | null }>>
```
para:
```typescript
Promise<Map<string, { uf: string; state_name: string }>>
```

2. Alterar a declaração do Map (linha 50) de:
```typescript
const map = new Map<string, { uf: string; state_name: string; base_cost: number | null; cidade: string | null }>();
```
para:
```typescript
const map = new Map<string, { uf: string; state_name: string }>();
```

3. Remover as 5 linhas de extração de `base_cost` e `cidade` (linhas 89-94):
```typescript
const baseCostRaw = data?.base_cost ?? data?.cost?.gross ?? null;
const base_cost = baseCostRaw != null ? Number(baseCostRaw) : null;
const addrCity = data?.receiver_address?.city ?? null;
const cidade = addrCity
  ? (typeof addrCity === "object" ? (addrCity as any)?.name ?? null : String(addrCity))
  : null;
```

4. Alterar o `map.set` (linha 97) de:
```typescript
map.set(sid, { uf, state_name: stateName, base_cost, cidade });
```
para:
```typescript
map.set(sid, { uf, state_name: stateName });
```

Pitfall: O TypeScript no Deno usa verificação de tipos em runtime via `deno check`. Se qualquer chamada downstream de `fetchShipmentStates` acessar `.base_cost` ou `.cidade`, o deploy falhará. Após o Task 1 ter removido o fire-and-forget, não deve restar nenhuma referência a esses campos — confirmar com grep antes de continuar.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && grep -c "base_cost\|shipInfo?.cidade" supabase/functions/mercado-libre-integration/index.ts</automated>
  </verify>
  <done>grep retorna 0 para `base_cost` e `shipInfo?.cidade` no arquivo. O tipo de retorno de `fetchShipmentStates` é `{ uf: string; state_name: string }` sem campos extras.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy e smoke test</name>
  <files>supabase/functions/mercado-libre-integration/index.ts</files>
  <action>
Fazer deploy da função editada:

```bash
cd /root/garment-glow-test
npx supabase functions deploy mercado-libre-integration --project-ref gionpsuunfkkzzjdubfy
```

Após deploy bem-sucedido, invocar a função para um dia recente (smoke test):

```bash
# Obter token de acesso da sessão local — não é possível aqui; o teste visual deve ser feito via UI
# Alternativa: usar Supabase Dashboard > Edge Functions > mercado-libre-integration > "Invoke"
# Body: {"ml_user_id":"<id>","date_from":"2026-05-20","date_to":"2026-05-20","seller_id":null}
```

Confirmar nos logs da função que:
1. Não aparece a mensagem "Orders: X rows saved" (que era logada pelo bloco removido)
2. A função retorna `{"success":true, ...}` com os campos normais de daily/hourly cache
3. Não há TypeScript errors no deploy output

Pitfall: Se o deploy falhar com erro de tipo TypeScript (ex: "Property 'base_cost' does not exist"), significa que alguma referência residual ficou no arquivo. Verificar com `grep "base_cost\|cidade" supabase/functions/mercado-libre-integration/index.ts`.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx supabase functions deploy mercado-libre-integration --project-ref gionpsuunfkkzzjdubfy 2>&1 | tail -5</automated>
  </verify>
  <done>Deploy conclui sem erros. A função não escreve mais em `public.orders` diretamente.</done>
</task>

</tasks>

<verification>
Ao final das 3 tasks:
- `grep -c "orderRows" supabase/functions/mercado-libre-integration/index.ts` retorna 0
- `grep -c "base_cost" supabase/functions/mercado-libre-integration/index.ts` retorna 0
- Deploy conclui com sucesso
- `await Promise.all(upsertPromises)` permanece intacto (daily/hourly/product/user cache não são afetados)
</verification>

<success_criteria>
O bloco fire-and-forget foi removido e a função foi deployada. O Dashboard de Vendas continua funcionando (sem regressão nos KPIs de visitas/receita bruta). A tabela `public.orders` não recebe mais escrita de `mercado-libre-integration` — escrita exclusiva via `sync-ml-orders`.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-ml-orders/14-01-SUMMARY.md` com:
- O que foi removido (linhas exatas, motivo)
- Resultado do deploy
- Confirmação de que `await Promise.all(upsertPromises)` permanece intacto
</output>
