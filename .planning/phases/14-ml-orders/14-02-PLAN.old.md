# Plan 14-02: Frontend — Hook `useMLOrders` + `costSummary` real + ticket médio correto

## Goal

Ao final deste plano:
1. Existe `src/hooks/useMLOrders.ts` com o hook `useMLOrders(from, to)` que lê `public.orders` e retorna `{ total_comissao, total_frete, paid_orders_count, paid_revenue }`
2. `costSummary` em `MercadoLivre.tsx` usa `ordersSummary.total_comissao` e `ordersSummary.total_frete` em vez de `grossRevenue * 0.11` e `grossRevenue * 0.05`, com fallback para percentuais se `ordersSummary` for null/undefined
3. `avg_ticket` em `effectiveMetrics` usa `paid_revenue / paid_orders_count` (só pedidos pagos), com fallback para `total_revenue / total_orders` enquanto `ordersSummary` ainda carrega
4. Taxa de conversão não regride (usa `ml_daily_cache`, não `orders`)

---

## Context

### Decisões críticas do research

- **Hook lê a tabela diretamente** (Pattern 5 — direct Supabase client), sem edge function intermediária, igual a `useMLStateQuery`.
- **Query filtra por** `organization_id`, `ml_user_id IN (resolvedMLUserIds)`, `data_pedido BETWEEN from AND to`.
- **Fallback obrigatório:** se `ordersSummary` é null/undefined ou se `total_comissao === 0 && total_frete === 0 && paid_orders_count === 0`, usar percentuais hardcoded como fallback (evita regressão para períodos históricos sem dados em `orders`).
- **`avg_ticket` depende de `ordersSummary`** que é assíncrono — usar `?? m.total_orders` e `?? m.approved_revenue` como fallback durante loading.
- **Taxa de conversão (`KPIS-04`):** não muda nada — usa `unique_buyers / unique_visits` de `ml_daily_cache`. Sem risco de regressão.
- **`effectiveMetrics` é um `useMemo`** — `ordersSummary` (retorno de `useMLOrders`) precisa estar na dependency array do `useMemo` de `effectiveMetrics`.

### Arquivos relevantes

- `src/hooks/useMLQueries.ts` — padrão exato a seguir para novo hook (TanStack React Query v5)
- `src/pages/MercadoLivre.tsx` — linhas 251-302 são o alvo da modificação
- `src/contexts/MLStoreContext.tsx` — exporta `useMLStore` com `resolvedMLUserIds`
- `src/contexts/OrganizationContext.tsx` — exporta `useOrganization` com `currentOrg`

### Localização exata das linhas a modificar em `MercadoLivre.tsx`

```typescript
// Linha 264 — avg_ticket atual (a substituir):
if (m.total_orders > 0) m.avg_ticket = m.total_revenue / m.total_orders;

// Linhas 291-302 — costSummary atual (a substituir):
const costSummary = useMemo(() => {
  const grossRevenue = effectiveMetrics?.total_revenue ?? 0;
  const comissao = grossRevenue * 0.11;   // SUBSTITUIR
  const frete = grossRevenue * 0.05;      // SUBSTITUIR
  const ads = adsSummary.total_spend;
  const totalKnown = comissao + frete + ads;
  return {
    comissao, frete, publicidade: ads, custo_produto: 0, impostos: 0,
    total_known: totalKnown, gross_revenue: grossRevenue,
    pct_receita: grossRevenue > 0 ? Math.round((totalKnown / grossRevenue) * 10000) / 100 : 0,
  };
}, [effectiveMetrics, adsSummary]);
```

### Query key pattern do projeto

```typescript
// Padrão observado em useMLQueries.ts:
queryKey: ["ml", "orders-summary", orgId, resolvedMLUserIds, from, to],
```

---

## Tasks

### Task 1: Criar hook `useMLOrders`

**File(s):**
- `/root/garment-glow-test/src/hooks/useMLOrders.ts`

**What:** Criar o hook `useMLOrders(from: string, to: string)` que consulta `public.orders` e agrega comissão, frete e contagem de pedidos pagos para o período.

**How:** Criar o arquivo `src/hooks/useMLOrders.ts` com o seguinte conteúdo:

```typescript
/**
 * useMLOrders — aggregates real comissao/frete/paid-count from public.orders
 * Used by MercadoLivre.tsx to replace hardcoded 11%/5% cost estimates.
 */
import { useQuery } from "@tanstack/react-query";
import { useMLStore } from "@/contexts/MLStoreContext";
import { useOrganization } from "@/contexts/OrganizationContext";
import { supabase } from "@/integrations/supabase/client";

export interface MLOrderSummary {
  total_comissao: number;
  total_frete: number;
  paid_orders_count: number;
  paid_revenue: number;
}

/**
 * Returns null (no data signal) when orders table has no rows for the period —
 * callers fall back to hardcoded percentages in that case.
 */
export function useMLOrders(from: string, to: string) {
  const { resolvedMLUserIds } = useMLStore();
  const { currentOrg } = useOrganization();
  const orgId = currentOrg?.id ?? null;

  return useQuery<MLOrderSummary | null>({
    queryKey: ["ml", "orders-summary", orgId, resolvedMLUserIds, from, to],
    queryFn: async (): Promise<MLOrderSummary | null> => {
      if (!orgId || resolvedMLUserIds.length === 0) return null;

      const { data, error } = await supabase
        .from("orders")
        .select("comissao, frete, status, preco_unit, quantidade")
        .eq("organization_id", orgId)
        .in("ml_user_id", resolvedMLUserIds)
        .gte("data_pedido", from)
        .lte("data_pedido", to);

      if (error) throw error;

      const rows = data ?? [];

      // Return null (no-data signal) when table has no rows for this period.
      // Caller falls back to hardcoded percentages.
      if (rows.length === 0) return null;

      const total_comissao = rows.reduce((s, r) => s + (r.comissao ?? 0), 0);
      const total_frete = rows.reduce((s, r) => s + (r.frete ?? 0), 0);

      const paidRows = rows.filter((r) => r.status === "paid");
      const paid_orders_count = paidRows.length;
      const paid_revenue = paidRows.reduce(
        (s, r) => s + (r.preco_unit ?? 0) * (r.quantidade ?? 1),
        0,
      );

      return { total_comissao, total_frete, paid_orders_count, paid_revenue };
    },
    enabled: !!orgId && resolvedMLUserIds.length > 0 && !!from && !!to,
    staleTime: 5 * 60 * 1000,
  });
}
```

**Verify:**
- Arquivo existe em `src/hooks/useMLOrders.ts`
- `cd /root/garment-glow-test && npx tsc --noEmit` retorna 0 erros relativos ao novo arquivo

---

### Task 2: Atualizar `MercadoLivre.tsx` — `costSummary` real + `avg_ticket` correto

**File(s):**
- `/root/garment-glow-test/src/pages/MercadoLivre.tsx`

**What:** Três mudanças cirúrgicas em `MercadoLivre.tsx`:
1. Importar e chamar `useMLOrders`
2. Corrigir `avg_ticket` no `useMemo` de `effectiveMetrics`
3. Substituir `costSummary` hardcoded por valores reais com fallback

**How:**

**Mudança 1 — Import (no bloco de imports de hooks, após linha ~17):**

Adicionar ao bloco de imports de hooks existentes:
```typescript
import { useMLOrders } from "@/hooks/useMLOrders";
```

**Mudança 2 — Chamar o hook (após as chamadas de hooks existentes, antes de `effectiveMetrics` useMemo, por volta da linha ~248):**

Adicionar chamada do hook logo após as declarações de `allDaily`, `allHourly`, etc. — antes do `useMemo` de `effectiveMetrics`. O `currentFrom` e `currentTo` já estão em escopo neste ponto:

```typescript
const { data: ordersSummary } = useMLOrders(currentFrom, currentTo);
```

**Mudança 3 — Corrigir `avg_ticket` dentro do `useMemo` de `effectiveMetrics` (linha ~264):**

```typescript
// ANTES:
if (m.total_orders > 0) m.avg_ticket = m.total_revenue / m.total_orders;

// DEPOIS:
const paidCount = ordersSummary?.paid_orders_count ?? m.total_orders;
const paidRev   = ordersSummary?.paid_revenue     ?? m.approved_revenue;
if (paidCount > 0) {
  m.avg_ticket = paidRev / paidCount;
} else if (m.total_orders > 0) {
  m.avg_ticket = m.total_revenue / m.total_orders; // fallback completo
}
```

Adicionar `ordersSummary` na dependency array do `useMemo` de `effectiveMetrics`:
```typescript
// ANTES:
}, [effectiveDaily]);

// DEPOIS:
}, [effectiveDaily, ordersSummary]);
```

**Mudança 4 — Substituir `costSummary` (linhas 291-302):**

```typescript
// ANTES:
const costSummary = useMemo(() => {
  const grossRevenue = effectiveMetrics?.total_revenue ?? 0;
  const comissao = grossRevenue * 0.11;
  const frete = grossRevenue * 0.05;
  const ads = adsSummary.total_spend;
  const totalKnown = comissao + frete + ads;
  return {
    comissao, frete, publicidade: ads, custo_produto: 0, impostos: 0,
    total_known: totalKnown, gross_revenue: grossRevenue,
    pct_receita: grossRevenue > 0 ? Math.round((totalKnown / grossRevenue) * 10000) / 100 : 0,
  };
}, [effectiveMetrics, adsSummary]);

// DEPOIS:
const costSummary = useMemo(() => {
  const grossRevenue = effectiveMetrics?.total_revenue ?? 0;
  // Use real values from orders table; fall back to hardcoded percentages when
  // orders data is unavailable (historical period before sync, or still loading).
  const comissao = ordersSummary?.total_comissao ?? grossRevenue * 0.11;
  const frete    = ordersSummary?.total_frete    ?? grossRevenue * 0.05;
  const ads = adsSummary.total_spend;
  const totalKnown = comissao + frete + ads;
  return {
    comissao, frete, publicidade: ads, custo_produto: 0, impostos: 0,
    total_known: totalKnown, gross_revenue: grossRevenue,
    pct_receita: grossRevenue > 0 ? Math.round((totalKnown / grossRevenue) * 10000) / 100 : 0,
  };
}, [effectiveMetrics, adsSummary, ordersSummary]);
```

**Verify:**
- `cd /root/garment-glow-test && npx tsc --noEmit` retorna 0 erros
- `cd /root/garment-glow-test && npm run build` conclui sem erros
- No browser, navegar para `/` (Dashboard de Vendas):
  - Os KPIs de Comissão e Frete mostram valores monetários (não necessariamente 11%/5% de grossRevenue)
  - Se `orders` ainda não tem dados para o período: os valores de fallback aparecem (baseados em 11%/5%) — sem quebra
  - Console não mostra erros de TypeScript ou React Query
- Taxa de conversão continua aparecendo normalmente (não foi alterada)

---

## Commit message

```
feat(14-02): useMLOrders hook + costSummary real + avg_ticket correto

- Add useMLOrders hook reading public.orders for real comissao/frete sums
- Replace hardcoded 11%/5% in costSummary with real SUM from orders table
- Fix avg_ticket to use paid_revenue/paid_orders_count (excludes cancelled)
- Fallback to percentages when orders table has no data for the period
```
