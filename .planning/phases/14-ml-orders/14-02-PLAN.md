---
phase: 14-ml-orders
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sync-ml-orders/index.ts
autonomous: true
requirements: [ORDERS-03, ORDERS-04, KPIS-01]

must_haves:
  truths:
    - "Rows em `public.orders` após sync têm `receita_bruta` não nulo quando `preco_unit` não é nulo"
    - "`receita_liquida` = `preco_unit * quantidade - comissao - frete - tax_amount` (nulos tratados como 0)"
    - "Se `preco_unit` é null, ambos os campos ficam null (não zero)"
  artifacts:
    - path: "supabase/functions/sync-ml-orders/index.ts"
      provides: "expandOrder com receita_bruta e receita_liquida no payload de upsert"
      contains: "receita_bruta"
  key_links:
    - from: "supabase/functions/sync-ml-orders/index.ts"
      to: "public.orders"
      via: "upsert com onConflict ml_order_id,ml_user_id,item_id,variation_id"
      pattern: "receita_bruta.*receita_liquida"
---

<objective>
Adicionar os campos `receita_bruta` e `receita_liquida` ao objeto retornado por `expandOrder` em `sync-ml-orders/index.ts`.

Purpose: Esses campos existem na tabela `public.orders` e são consumidos por `MLPedidos.tsx`. Atualmente `expandOrder` não os calcula, então toda row fica com `NULL` nesses campos. O dashboard de Vendas usa `preco_unit * quantidade` aggregado diretamente, mas `MLPedidos.tsx` precisa de `receita_liquida` por row para exibir margem por pedido.
Output: `sync-ml-orders/index.ts` com `receita_bruta` e `receita_liquida` calculados e incluídos no `return` de `expandOrder`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-ml-orders/14-RESEARCH.md

Arquivo principal: `supabase/functions/sync-ml-orders/index.ts` (~520 linhas)

**Ponto de inserção — `expandOrder` return, linha 286:**

O return atual termina na linha 312 com `uf_origem: ufOrigem,`. Os campos devem ser adicionados ANTES do fechamento `};` do return.

**Variáveis locais disponíveis no escopo em que o return ocorre:**
- `precoUnit` — `item.unit_price != null ? Number(item.unit_price) : null`
- `quantidade` — `Number(item.quantity || 0)`
- `item.sale_fee` — comissão bruta do ML (já existe como `comissao: item.sale_fee != null ? Number(item.sale_fee) : null`)
- `frete` — já calculado acima no mesmo escopo
- `taxAmount` — já calculado acima no mesmo escopo

**Fórmulas exatas (da RESEARCH.md Q2):**
```typescript
receita_bruta:   precoUnit != null ? precoUnit * quantidade : null,
receita_liquida: precoUnit != null
  ? precoUnit * quantidade
    - (item.sale_fee != null ? Number(item.sale_fee) : 0)
    - (frete ?? 0)
    - (taxAmount ?? 0)
  : null,
```

Notas importantes:
- Usar `item.sale_fee` diretamente (não a variável `comissao` — ela não existe como variável local; o campo é calculado inline no return)
- `frete` e `taxAmount` já são variáveis locais no escopo de `expandOrder` — usar direto com `?? 0`
- Se `precoUnit` é null, ambos devem ser null (não 0)

**Estado atual do return (linhas 286-312):**
```typescript
return {
  ml_order_id:     String(order.id),
  ml_user_id:      mlUserId,
  seller_id:       sellerId,
  user_id:         userId,
  organization_id: organizationId,
  item_id:         itemId,
  variation_id:    prod.variation_id ? String(prod.variation_id) : "",
  sku:             prod.seller_custom_field ?? prod.seller_sku ?? null,
  titulo:          prod.title ?? null,
  listing_type,
  quantidade,
  preco_unit:      precoUnit,
  comissao:        item.sale_fee    != null ? Number(item.sale_fee)    : null,
  frete,
  status:          order.status ?? null,
  data_pedido:     datePedido,
  data_pagamento:  dataPagamento,
  estado,
  cidade,
  comprador,
  synced_at:       syncAt,
  custo_unit:      custoUnit,
  tax_rate:        taxRate,
  tax_amount:      taxAmount,
  uf_origem:       ufOrigem,
};
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar `receita_bruta` e `receita_liquida` ao return de `expandOrder`</name>
  <files>supabase/functions/sync-ml-orders/index.ts</files>
  <behavior>
    - Caso base: `precoUnit=100`, `quantidade=2`, `sale_fee=15`, `frete=10`, `taxAmount=5` → `receita_bruta=200`, `receita_liquida=170`
    - Nulos na comissao: `precoUnit=100`, `quantidade=1`, `sale_fee=null`, `frete=null`, `taxAmount=null` → `receita_bruta=100`, `receita_liquida=100`
    - preco_unit nulo: `precoUnit=null` → `receita_bruta=null`, `receita_liquida=null`
    - frete nulo (timeout shipment): tratado como 0, não propaga null para receita_liquida
  </behavior>
  <action>
No arquivo `supabase/functions/sync-ml-orders/index.ts`, localizar o `return {` da função `expandOrder` (linha ~286). Adicionar os dois campos ao final do objeto, antes do `};` de fechamento:

```typescript
      receita_bruta:   precoUnit != null ? precoUnit * quantidade : null,
      receita_liquida: precoUnit != null
        ? precoUnit * quantidade
          - (item.sale_fee != null ? Number(item.sale_fee) : 0)
          - (frete ?? 0)
          - (taxAmount ?? 0)
        : null,
```

Inserir após `uf_origem: ufOrigem,` e antes de `};`.

Pitfall 1: NÃO usar uma variável `comissao` — ela não existe como variável local. O campo `comissao` no return é calculado inline a partir de `item.sale_fee`. Usar `item.sale_fee` diretamente na fórmula de `receita_liquida`.

Pitfall 2: `frete` pode ser null quando o fetch de shipment falhou (timeout). Usar `?? 0` para tratar como zero — a receita fica ligeiramente superestimada mas não vai null.

Pitfall 3: NÃO duplicar a lógica de cálculo de `frete` ou `taxAmount` — usar as variáveis locais já existentes no escopo.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && grep -A2 "receita_bruta" supabase/functions/sync-ml-orders/index.ts</automated>
  </verify>
  <done>grep mostra as duas linhas `receita_bruta` e `receita_liquida` no arquivo com as fórmulas corretas.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy de `sync-ml-orders` e validação de rows</name>
  <files>supabase/functions/sync-ml-orders/index.ts</files>
  <action>
Deploy da função:

```bash
cd /root/garment-glow-test
npx supabase functions deploy sync-ml-orders --project-ref gionpsuunfkkzzjdubfy
```

Após deploy, invocar manualmente para verificar que rows aparecem com `receita_bruta` não nulo. Usar Supabase Dashboard > Edge Functions > sync-ml-orders > "Invoke" com:
```json
{
  "ml_user_id": "<id_da_conta_conectada>",
  "date_from": "2026-05-20",
  "date_to": "2026-05-20"
}
```
Authorization: Bearer `<token da sessão do usuário>`

Após invoke bem-sucedido, verificar via SQL no Supabase:
```sql
SELECT ml_order_id, preco_unit, quantidade, receita_bruta, receita_liquida, comissao, frete, tax_amount
FROM orders
WHERE data_pedido = '2026-05-20'
LIMIT 5;
```

Confirmar:
1. `receita_bruta = preco_unit * quantidade` para as rows retornadas
2. `receita_liquida` é negativa ou positiva mas não nula quando `preco_unit` não é nulo
3. Deploy output não contém erros TypeScript

Pitfall: Se o invoke retornar 401, o token expirou — fazer logout/login no app e copiar o token da sessão do localStorage (`supabase.auth.getSession()`).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx supabase functions deploy sync-ml-orders --project-ref gionpsuunfkkzzjdubfy 2>&1 | tail -5</automated>
  </verify>
  <done>Deploy conclui sem erros. Ao menos um row em `public.orders` tem `receita_bruta` não nulo após invoke manual.</done>
</task>

</tasks>

<verification>
Ao final das 2 tasks:
- `grep "receita_bruta" supabase/functions/sync-ml-orders/index.ts` retorna a linha com a fórmula
- `grep "receita_liquida" supabase/functions/sync-ml-orders/index.ts` retorna a linha com a fórmula
- Deploy conclui sem erros TypeScript
- SQL query em `public.orders` mostra `receita_bruta = preco_unit * quantidade` para rows existentes
</verification>

<success_criteria>
`expandOrder` retorna `receita_bruta` e `receita_liquida` em todos os rows. Após sync de qualquer período, rows em `public.orders` têm esses campos preenchidos (não nulos quando `preco_unit` não é nulo). `MLPedidos.tsx` passa a exibir valores reais de receita líquida por pedido.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-ml-orders/14-02-SUMMARY.md` com:
- Fórmulas exatas adicionadas
- Resultado do deploy
- Exemplo de row do SQL com `receita_bruta` e `receita_liquida` preenchidos
</output>
