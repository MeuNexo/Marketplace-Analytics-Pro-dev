---
phase: 59-fluxo-caixa-correcoes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sync-tiny-payables/index.ts
autonomous: false
requirements:
  - CASHFIX-02
user_setup:
  - service: supabase
    why: "Deploy da Edge Function sync-tiny-payables (requer SUPABASE_ACCESS_TOKEN do Wesley — gsd-executor não tem MCP/token)"
    dashboard_config:
      - task: "Deploy da EF sync-tiny-payables via CLI/MCP com SUPABASE_ACCESS_TOKEN"
        location: "Orquestrador — projeto ckcdevcxgvueywivefgx"

must_haves:
  truths:
    - "A EF sync-tiny-payables responde 202 em <200ms (EdgeRuntime.waitUntil) — o net.http_post do cron deixa de ser abortado aos 5s"
    - "A causa-raiz da não-persistência (silent-no-write) é PROVADA por observabilidade (nº de lojas, nº de contas do Tiny, nº de linhas pré-upsert, validade do token Tiny) antes do fix definitivo"
    - "Após o fix, cash_outflows volta a persistir: count(DISTINCT synced_at::date) cresce / max(synced_at) avança"
    - "A EF continua escopada por org/loja (loop por ml_tokens com Tiny conectado) e não expõe service_role; cadência 6h mantida"
  artifacts:
    - path: "supabase/functions/sync-tiny-payables/index.ts"
      provides: "EF com padrão EdgeRuntime.waitUntil (202 imediato + runSync em background com try/catch) + observabilidade (logs de lojas/contas/rows) + fix da causa-raiz identificada"
      contains: "EdgeRuntime.waitUntil"
  key_links:
    - from: "supabase/migrations/20260618115000_cash_outflows_tiny_cron.sql"
      to: "supabase/functions/sync-tiny-payables/index.ts"
      via: "cron sync-tiny-payables-6h dispara net.http_post → EF responde 202 (waitUntil desacopla o pg_net da duração de ~15,7s)"
      pattern: "sync-tiny-payables"
    - from: "supabase/functions/sync-tiny-payables/index.ts"
      to: "cash_outflows"
      via: "upsert onConflict (organization_id, tiny_payable_id) com synced_at/updated_at no set"
      pattern: "cash_outflows.*upsert"
---

<objective>
Acabar com o congelamento do contas a pagar (CASHFIX-02). Desde 18/06 `cash_outflows` não persiste: o cron `sync-tiny-payables-6h` dispara, a EF responde 200 em ~15,7s, mas (a) o `net.http_post` do pg_net abandona a conexão aos ~5s (timeout default) e (b) mesmo quando a EF completa, `synced_at` continua preso em 18/06 → há um segundo bug de silent-no-write dentro da EF.

Purpose: Restaurar a persistência ≥1x/dia mantendo a cadência atual de 6h. A estratégia é PRIMEIRO provar a causa-raiz com observabilidade (não confiar no status 200 como prova de sync), DEPOIS corrigir o bug específico e desacoplar o disparo da duração via `EdgeRuntime.waitUntil` (EF responde 202 imediato e processa em background — dispensa nova migration de cron e elimina o timeout do pg_net).

Output: EF `sync-tiny-payables` reescrita com waitUntil + observabilidade + fix da causa-raiz (1 dos 4 suspects do RESEARCH), deployada pelo orquestrador, validada por SQL em prod.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-fluxo-caixa-correcoes/59-CONTEXT.md
@.planning/phases/59-fluxo-caixa-correcoes/59-RESEARCH.md

# EF atual (alvo do fix) — ler linha a linha
@supabase/functions/sync-tiny-payables/index.ts

# Cron atual (NÃO recriar via SQL Editor; só consultar — waitUntil dispensa nova migration de cron)
@supabase/migrations/20260618115000_cash_outflows_tiny_cron.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever EF com waitUntil + observabilidade (debug-first)</name>
  <files>supabase/functions/sync-tiny-payables/index.ts</files>
  <read_first>
    - supabase/functions/sync-tiny-payables/index.ts (EF completa — handler `serve` ~linha 275, `processLoja` ~202, `fetchPayables` ~162, `getTinyToken` ~65, upsert ~257)
    - 59-RESEARCH.md seções "4 Suspects de Silent-No-Write" (file:line de cada suspect), "Estratégia de Debug", "Fix Recomendado: waitUntil Pattern para sync-tiny-payables", e "Common Pitfalls" (Pitfall 4 try/catch no background)
  </read_first>
  <action>
    Reescrever o handler `serve` da EF `sync-tiny-payables` para o padrão `EdgeRuntime.waitUntil` (per CASHFIX-02, recomendação HIGH do RESEARCH — desacopla o pg_net da duração e dispensa nova migration de cron):

    1. No `serve`: manter o tratamento de `OPTIONS` e o `requireServiceRole`. Depois, em vez de `await` da lógica de sync, chamar `EdgeRuntime.waitUntil(runSync())` (SEM await) e responder imediatamente `jsonResp({ ok: true, msg: "sync enqueued" }, 202)`. Isso faz o pg_net receber 202 em <200ms e nunca mais estourar "Timeout of 5000 ms reached".

    2. Mover TODA a lógica atual do try do handler (calcular janela dateFrom/dateTo, buscar tokenRows de ml_tokens com Tiny conectado, loop de `processLoja`) para uma função `async function runSync(): Promise<void>`. Embrulhar o corpo inteiro em try/catch com `console.error` completo (Pitfall 4 — exceções no background não propagam pro caller; sem try/catch o processo morre sem log).

    3. `EdgeRuntime` é global no runtime Supabase Edge (sem import). Para satisfazer o `deno check`/tsc, adicionar no topo do arquivo a declaração de tipo: uma `declare const EdgeRuntime` com a assinatura `waitUntil: (p: Promise<unknown>) => void` (premissa A2 do RESEARCH — se não fosse global, esta declaração é o fallback recomendado).

    4. OBSERVABILIDADE (debug-first — provar a causa-raiz, não confiar no 200): adicionar `console.log` em pontos-chave para revelar qual dos 4 suspects é o real:
       - Em `runSync`, após buscar `tokenRows`: logar `tokenRows.length` (nº de lojas com Tiny) e os `ml_user_id`. Se vazio, logar e abortar com mensagem clara (Suspect: query de lojas vazia).
       - Em `processLoja`, após `getTinyToken`: logar que o token foi obtido OK para o `ml_user_id` (Suspect 2: token expirado/refresh falhando — hoje o throw cai no catch da loja e a EF responde 200 com errors).
       - Em `fetchPayables` (offset 0): logar `Object.keys(data)` (raw keys da resposta Tiny) e `itens.length` — revela Suspect 3 (formato de resposta mudou: ex. `{ contas: [...] }` em vez de `{ itens: [...] }`).
       - Em `processLoja`, antes do upsert: logar `rows.length` (nº de linhas que serão upsertadas).
       - Manter/garantir o `console.error` do `upsertErr` (Suspect 4: upsert engolido) já existente.

    5. NÃO mudar a cadência (cron 6h fica como está — Wesley pediu manter). NÃO recriar/alterar o cron via SQL Editor. Com waitUntil, NÃO é necessária nova migration de cron — registrar isso no SUMMARY.

    6. Confirmar que o upsert mantém `synced_at` e `updated_at` no payload (já presentes nas linhas ~246-247) e o `onConflict: "organization_id,tiny_payable_id"` com `ignoreDuplicates: false` (faz UPDATE de todos os campos no conflito, incluindo synced_at — RESEARCH confirma que o upsert atualiza synced_at SE for atingido).

    NÃO aplicar ainda o fix definitivo de um suspect específico nesta task se a causa-raiz não estiver provada — esta versão é debug+waitUntil. O fix do suspect específico será concluído na Task 3 após a leitura dos logs (Task 2). Se a causa for óbvia pelo código (ex.: corrigir Suspect 1, passar dateFrom/dateTo ao tinyGet, é seguro e barato), pode incluir, documentando no SUMMARY.
  </action>
  <verify>
    <automated>grep -q "EdgeRuntime.waitUntil" supabase/functions/sync-tiny-payables/index.ts && grep -q "declare const EdgeRuntime" supabase/functions/sync-tiny-payables/index.ts && grep -q "async function runSync" supabase/functions/sync-tiny-payables/index.ts && (command -v deno >/dev/null 2>&1 && deno check supabase/functions/sync-tiny-payables/index.ts 2>&1 | tail -3 || echo "deno not available locally — deno check runs at deploy")</automated>
  </verify>
  <acceptance_criteria>
    - serve() responde 202 via EdgeRuntime.waitUntil(runSync()) sem await
    - runSync() existe, contém toda a lógica de sync, embrulhada em try/catch com console.error
    - declare const EdgeRuntime presente (tipagem para deno check)
    - Logs de observabilidade presentes: tokenRows.length, token OK, raw keys + itens.length, rows.length
    - Cadência inalterada; nenhuma migration de cron criada
  </acceptance_criteria>
  <done>EF reescrita com waitUntil + observabilidade; deno check (ou tsc) sem erro de tipo de EdgeRuntime.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Deploy da EF (debug) + invocar + ler logs p/ provar a causa-raiz (orquestrador)</name>
  <files>supabase/functions/sync-tiny-payables/index.ts</files>
  <action>Checkpoint do ORQUESTRADOR (não autônomo — gsd-executor não tem SUPABASE_ACCESS_TOKEN/MCP). Deployar a EF da Task 1 via CLI/MCP no `ckcdevcxgvueywivefgx`, invocar manualmente, e LER os logs para provar qual dos 4 suspects é a causa-raiz real (não confiar no status 200 como prova de sync). Registrar o suspect confirmado para destravar a Task 3.</action>
  <what-built>EF `sync-tiny-payables` com waitUntil + observabilidade (Task 1). O deploy de EF requer `SUPABASE_ACCESS_TOKEN` do Wesley (sbp_...) — o gsd-executor NÃO tem o MCP/token; quem deploya é o ORQUESTRADOR via CLI/MCP no projeto `ckcdevcxgvueywivefgx` (padrão das Phases 57/58).</what-built>
  <how-to-verify>
    1. Orquestrador deploya a EF `sync-tiny-payables` (CLI `supabase functions deploy` ou MCP) no `ckcdevcxgvueywivefgx` com o SUPABASE_ACCESS_TOKEN do Wesley.
    2. Smoke: `curl -X POST .../functions/v1/sync-tiny-payables -H "Authorization: Bearer <service_role_key>" -w "%{http_code} %{time_total}"` → esperar 202 em <0,5s (waitUntil responde imediato).
    3. Aguardar ~30s (o background runSync processa) e ler os logs da EF (Supabase dashboard ou MCP get_logs). Identificar qual suspect é o real pelos logs:
       - `tokenRows=0` → Suspect: nenhuma loja com Tiny conectado (query/coluna).
       - token NÃO obtido / erro de refresh → Suspect 2 (token Tiny expirado/refresh quebrado — mais provável).
       - `raw keys=...` sem `itens` e `itens=0` → Suspect 3 (formato de resposta Tiny mudou).
       - `rows para upsert=N` mas upsert error → Suspect 4 (upsert engolido / schema/key).
    4. Registrar no checkpoint qual suspect foi confirmado (é o que a Task 3 vai corrigir).
  </how-to-verify>
  <resume-signal>Type "approved" com a identificação do suspect real (ex.: "Suspect 2 — token Tiny expirado, refresh 401"). Isso destrava a Task 3 (fix definitivo). Se os logs forem inconclusivos, descreva o que apareceu.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Corrigir a causa-raiz identificada + revalidar persistência</name>
  <files>supabase/functions/sync-tiny-payables/index.ts</files>
  <read_first>
    - supabase/functions/sync-tiny-payables/index.ts (versão da Task 1)
    - 59-RESEARCH.md "4 Suspects" + "Ranking dos Suspects" (o fix concreto depende do suspect provado na Task 2)
    - Resultado do checkpoint da Task 2 (qual suspect foi confirmado)
  </read_first>
  <action>
    Aplicar o fix específico para o suspect PROVADO na Task 2 (per CASHFIX-02). Não chutar — corrigir o que os logs revelaram:

    - Se Suspect 2 (token Tiny expirado/refresh quebrado): a correção do token pode ser reconexão do Tiny pelo Wesley em /integracoes (ação humana — sinalizar no SUMMARY) e/ou ajuste do fluxo de refresh em `getTinyToken` se o bug for de código (ex.: tratamento de resposta do tiny-oauth). Garantir que a falha de token de UMA loja não impeça as demais (loop já isola por try/catch).
    - Se Suspect 3 (formato de resposta Tiny): estender o parsing em `fetchPayables` para cobrir os nomes reais observados nos logs (ex.: adicionar `data?.contas` / outro alias) além de `itens`/`data`.
    - Se Suspect 4 (upsert engolido): corrigir a causa do `upsertErr` (ex.: schema drift, key, service_role). Não mascarar — o upsert deve passar a gravar.
    - Se Suspect 1 (dateFrom/dateTo ignorados — baixo impacto): passar `dateFrom`/`dateTo` ao `tinyGet` em `fetchPayables` se relevante; é seguro incluir de qualquer forma.

    Manter o padrão waitUntil + observabilidade da Task 1. NÃO mudar cadência. NÃO recriar cron via SQL Editor. Se (e só se) o waitUntil sozinho não bastar (ex.: limite de background no free tier atingido — improvável), adicionar `timeout_milliseconds := 60000` ao `net.http_post` via NOVA migration versionada `20260659000100_*` (não SQL Editor) — decidir só se necessário e documentar.

    Após o fix de código, o deploy final + validação SQL acontece no checkpoint da Task 4.
  </action>
  <verify>
    <automated>(command -v deno >/dev/null 2>&1 && deno check supabase/functions/sync-tiny-payables/index.ts 2>&1 | tail -3 || echo "deno check at deploy") && grep -q "EdgeRuntime.waitUntil" supabase/functions/sync-tiny-payables/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - O fix corresponde ao suspect provado na Task 2 (rastreável no SUMMARY)
    - waitUntil + observabilidade preservados
    - deno check sem erro; cadência inalterada; sem drift via SQL Editor
  </acceptance_criteria>
  <done>Causa-raiz corrigida no código (ou ação humana de reconexão sinalizada se for token); EF pronta para redeploy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: [BLOCKING] Redeploy EF + provar persistência por SQL (orquestrador)</name>
  <files>supabase/functions/sync-tiny-payables/index.ts</files>
  <action>Checkpoint do ORQUESTRADOR (não autônomo). Registrar o baseline SQL ANTES, redeployar a EF com o fix da Task 3 no `ckcdevcxgvueywivefgx`, invocar, e provar por SQL que `max(synced_at)` avançou para hoje (persistência real) com logs mostrando rows>0 sem erro de upsert.</action>
  <what-built>EF `sync-tiny-payables` com o fix da causa-raiz (Task 3). Redeploy é checkpoint do orquestrador (SUPABASE_ACCESS_TOKEN do Wesley).</what-built>
  <how-to-verify>
    1. Baseline ANTES (registrar): `SELECT count(DISTINCT synced_at::date) AS dias_distintos, max(synced_at) AS ultimo_sync FROM cash_outflows WHERE organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7';` — esperado hoje: dias_distintos=1, ultimo_sync=2026-06-18.
    2. Orquestrador redeploya a EF (com o fix) no `ckcdevcxgvueywivefgx`.
    3. Invocar manualmente: `curl -X POST .../functions/v1/sync-tiny-payables -H "Authorization: Bearer <service_role_key>"` → 202 imediato; aguardar ~30s.
    4. Ler logs: confirmar nº de lojas, nº de contas do Tiny, nº de linhas upsertadas > 0 e sem upsert error.
    5. Provar persistência: rodar o SQL do passo 1 de novo → `max(synced_at)` deve ter avançado para HOJE e/ou `count(DISTINCT synced_at::date)` >= 2. Total/abertos devem refletir o Tiny ao vivo.
    6. (Opcional) Confirmar que o cron das próximas 6h também grava — `cron.job_run_details` + `net._http_response` sem mais "Timeout of 5000ms reached" (o 202 imediato elimina o timeout).
  </how-to-verify>
  <resume-signal>Type "approved" quando max(synced_at) tiver avançado para hoje (persistência provada) e os logs mostrarem rows>0 sem erro de upsert. Caso contrário, descreva o que os logs/SQL mostraram (volta pra Task 3).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron (Pattern B) → EF | net.http_post com Bearer service_role_key (vault) dispara a EF; EF valida via requireServiceRole() |
| EF → Tiny API | EF usa token Tiny por org (ml_tokens.tiny_access_token) — token escopado à loja, refresh via tiny-oauth |
| EF → cash_outflows | EF escreve com service_role (bypassa RLS) mas SEMPRE com organization_id da própria loja (loop por ml_tokens) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04 | Spoofing / Elevation | EF sync-tiny-payables (verify_jwt=false) | mitigate | `requireServiceRole()` mantido — só Bearer service_role_key passa; qualquer outro header → 401. waitUntil NÃO move a checagem de auth (ela continua ANTES do EdgeRuntime.waitUntil). |
| T-59-05 | Information Disclosure | service_role_key | mitigate | Key vem do env (SUPABASE_SERVICE_ROLE_KEY) e do vault no cron — nunca logada. Os logs de observabilidade logam ml_user_id, nº de contas, rows.length, raw keys — NÃO logar token nem service_role nem valores de contas sensíveis (só contagens/keys). |
| T-59-06 | Tampering / cross-org write | upsert em cash_outflows | mitigate | EF escopa por org: loop sobre ml_tokens com Tiny conectado; cada row carrega o organization_id da própria loja; onConflict (organization_id, tiny_payable_id) — sem escrita cross-org. Preservado no refactor waitUntil. |
| T-59-07 | Repudiation / silent failure | waitUntil background | mitigate | runSync() embrulhado em try/catch com console.error (Pitfall 4) — falha de background fica logada, não some. A observabilidade impede o "200 com 0 writes" mascarado (regra do CONTEXT: não confiar no 200 como prova). |
| T-59-SC | Tampering | npm/pip/cargo installs | mitigate | Sem novos pacotes (RESEARCH "zero novas dependências"); EdgeRuntime é global do runtime (sem import). Nada a auditar. |
</threat_model>

<verification>
- EF: EdgeRuntime.waitUntil(runSync()) responde 202; runSync em try/catch; declare const EdgeRuntime; observabilidade (lojas/contas/rows). deno check sem erro.
- Checkpoint Task 2 (orquestrador): deploy debug + invocação + logs provam qual suspect é o real.
- Task 3: fix do suspect provado (sem chute).
- Checkpoint Task 4 (orquestrador): redeploy + `SELECT count(DISTINCT synced_at::date), max(synced_at) FROM cash_outflows` mostra max(synced_at) avançando para hoje (persistência provada) e logs com rows>0 sem erro.
- Cadência 6h mantida; sem drift via SQL Editor.
</verification>

<success_criteria>
- CASHFIX-02 satisfeito: causa-raiz da não-persistência identificada por observabilidade e corrigida.
- `net.http_post` deixa de ser abortado aos 5s (EF responde 202 via waitUntil — desacoplado da duração de ~15,7s).
- `cash_outflows` volta a persistir: `count(DISTINCT synced_at::date)` cresce / `max(synced_at)` avança; total/abertos refletem o Tiny ao vivo.
- Cadência 6h mantida; EF segue escopada por org; service_role não exposto.
</success_criteria>

<output>
Create `.planning/phases/59-fluxo-caixa-correcoes/59-02-SUMMARY.md` when done
</output>
