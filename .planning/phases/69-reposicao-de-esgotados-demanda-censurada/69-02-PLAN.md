---
phase: 69-reposicao-de-esgotados-demanda-censurada
plan: 02
type: tdd
wave: 2
depends_on: ["69-01"]
files_modified:
  - src/lib/analysis/replenishmentUtils.ts
  - src/lib/analysis/replenishmentUtils.test.ts
  - src/integrations/supabase/types.ts
  - src/hooks/useReplenishmentBySku.ts
  - src/components/mercadolivre/ReplenishmentSkuTable.tsx
  - src/components/mercadolivre/ReplenishmentSkuFilters.tsx
  - src/pages/mercadolivre/MLCompras.tsx
autonomous: true
requirements: [ESGOT-01, ESGOT-02, ESGOT-03, ESGOT-04]
must_haves:
  truths:
    - "O espelho TS classifica status_esgotado (com_giro/repor_esgotado/revisar_esgotado/descontinuar) com os mesmos cortes 90/365 da RPC"
    - "O espelho TS estima a venda/dia pelo melhor ritmo 30d/180d com proteção anti-pico (≥2 dias distintos, senão média conservadora 90d)"
    - "A coluna 'O que fazer' mostra os 3 estados novos (repor/revisar/descontinuar) distintos do giro normal"
    - "Linhas com venda_dia_origem='historico_esgotado' exibem badge 'estoque zerado · demanda estimada pelo histórico' com a venda/dia estimada"
    - "O filtro Situação ganha as 3 opções novas e filtra corretamente"
    - "tsc, build e vitest (incluindo os testes novos) passam sem regressão das Phases 62–68"
  prohibitions:
    - "A demanda estimada NÃO pode ser exibida sem o badge que a distingue da demanda real"
    - "Os helpers/lógica de cálculo da RPC (hook/RPC) NÃO podem ser reimplementados no lado React — só espelhados em replenishmentUtils puro e lidos via status_esgotado/venda_dia_origem"
    - "As funções existentes de replenishmentUtils (calcReplenishment, resolveParams*, EWMA, etc.) NÃO podem ter assinatura alterada"
  artifacts:
    - path: "src/lib/analysis/replenishmentUtils.ts"
      provides: "StatusEsgotado type + classifyStatusEsgotado + estimateBestRate + isDemandaEstimada"
      contains: "StatusEsgotado"
    - path: "src/components/mercadolivre/ReplenishmentSkuTable.tsx"
      provides: "3 estados novos na coluna 'O que fazer' + badge demanda estimada"
      contains: "historico_esgotado"
    - path: "src/components/mercadolivre/ReplenishmentSkuFilters.tsx"
      provides: "3 opções novas no filtro Situação"
      contains: "repor_esgotado"
  key_links:
    - from: "src/hooks/useReplenishmentBySku.ts (mapRow)"
      to: "src/components/mercadolivre/ReplenishmentSkuTable.tsx (AcaoCell)"
      via: "campo status_esgotado mapeado da RPC e consumido pela célula 'O que fazer'"
      pattern: "status_esgotado"
---

<objective>
Expor na tela `/compras` o resultado do motor de esgotados do 69-01: os 3 estados
novos na coluna "O que fazer", o badge que distingue demanda **estimada** de **real**,
e as opções no filtro "Situação". Mais o espelho TS puro (`replenishmentUtils`) da
classificação + estimativa, com testes vitest (TDD RED→GREEN).

Purpose: Parte frontend de ESGOT-03 (transparência na tela) e ESGOT-04 (espelho TS +
testes + sem regressão). Reusa o padrão leigo do 63-05 e os badges do 67.
Output: `/compras` mostra os esgotados resgatados, sempre rotulando estimado vs real.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/69-reposicao-de-esgotados-demanda-censurada/69-CONTEXT.md
@.planning/phases/69-reposicao-de-esgotados-demanda-censurada/69-01-PLAN.md
@src/lib/analysis/replenishmentUtils.ts
@src/hooks/useReplenishmentBySku.ts
@src/components/mercadolivre/ReplenishmentSkuTable.tsx
@src/components/mercadolivre/ReplenishmentSkuFilters.tsx
@src/pages/mercadolivre/MLCompras.tsx
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Espelho TS da classificação + estimativa (RED→GREEN)</name>
  <files>src/lib/analysis/replenishmentUtils.ts, src/lib/analysis/replenishmentUtils.test.ts</files>
  <read_first>
    - src/lib/analysis/replenishmentUtils.ts — padrão de funções puras + JSDoc (espelha CTEs da RPC). NÃO alterar funções existentes; só ADICIONAR no final.
    - src/lib/analysis/replenishmentUtils.test.ts — padrão dos describe/it (cabeçalho importa os símbolos; helpers locais). Adicionar novos describe sem mexer nos 63 testes atuais.
    - .planning/phases/69-reposicao-de-esgotados-demanda-censurada/69-01-PLAN.md — seção <artifacts_this_phase_produces> e a Task 1 (cortes 90/365, janela 30d/180d, anti-pico ≥2 dias, conservadora 90d) para espelhar EXATAMENTE a RPC.
  </read_first>
  <behavior>
    - classifyStatusEsgotado({skuStock, venda30d, lastSaleDays}):
      - skuStock>0 OU venda30d>0 → 'com_giro'
      - esgotado (skuStock=0 && venda30d=0) e lastSaleDays ≤ 90 → 'repor_esgotado'
      - esgotado e lastSaleDays entre 91 e 365 → 'revisar_esgotado'
      - esgotado e lastSaleDays > 365 OU lastSaleDays=null → 'descontinuar'
    - estimateBestRate({dailySales, distinctSaleDays, sum90d}):
      - distinctSaleDays ≥ 2 e melhor janela 30d/180d > 0 → {vendaDia: best/30, origem:'historico_esgotado'}
      - senão (anti-pico) → {vendaDia: sum90d/90, origem:'conservador'}
      - melhor janela = maior soma de qty em qualquer 30 dias consecutivos dentro de 180d (espelha best_rate_by_sku)
    - isDemandaEstimada(vendaDiaOrigem): true sse 'historico_esgotado'
    - Casos de borda nos testes: exatamente 90 e 91 dias (limites dos baldes); 1 dia distinto com venda de atacado → cai na conservadora (anti-pico); melhor janela escolhe o pico de 30d, não a média de 180d.
  </behavior>
  <action>
RED primeiro: adicione em replenishmentUtils.test.ts novos `describe` para
classifyStatusEsgotado, estimateBestRate e isDemandaEstimada cobrindo os casos do
bloco <behavior> (incluindo os limites 90/91/365 e a proteção anti-pico). Commit do
teste falhando antes da implementação (padrão TDD das Phases 62/67).

GREEN: ADICIONE ao final de replenishmentUtils.ts (sem tocar no que existe):
- `export type StatusEsgotado = "com_giro" | "repor_esgotado" | "revisar_esgotado" | "descontinuar";`
- constantes nomeadas espelhando a RPC (ex.: RECENCY_REPOR_DAYS=90, RECENCY_REVISAR_DAYS=365, BEST_WINDOW_DAYS=30, BEST_LOOKBACK_DAYS=180, MIN_DISTINCT_SALE_DAYS=2, CONSERVATIVE_DAYS=90);
- `classifyStatusEsgotado(...)`, `estimateBestRate(...)` (retorna {vendaDia, origem:'historico_esgotado'|'conservador'}), `isDemandaEstimada(origem)`;
- estenda o type `VendaDiaOrigem` para incluir `'historico_esgotado'`.
Documente cada função com JSDoc citando ESGOT-01/ESGOT-02 e a CTE espelhada
(sales_history_by_sku / best_rate_by_sku). Mantenha tudo puro (sem React/Supabase/DOM).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run src/lib/analysis/replenishmentUtils.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Os 63 testes existentes continuam verdes + os novos describe passam.
    - `StatusEsgotado` exportado e `VendaDiaOrigem` inclui 'historico_esgotado'.
    - classifyStatusEsgotado retorna o balde certo nos limites 90/91/365 e null.
    - estimateBestRate cai na conservadora quando distinctSaleDays<2 (anti-pico) e usa o pico 30d quando ≥2.
    - Nenhuma função existente teve assinatura alterada.
  </acceptance_criteria>
  <done>vitest verde com os testes novos; espelho TS cobre classificação + estimativa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expor status_esgotado no tipo da RPC, types.ts e hook</name>
  <files>src/integrations/supabase/types.ts, src/hooks/useReplenishmentBySku.ts</files>
  <read_first>
    - src/hooks/useReplenishmentBySku.ts — interface ReplenishmentSkuRow + mapRow (acesso via Record<string,unknown> com fallback seguro, padrão Phase 67/68). Importa VendaDiaOrigem de replenishmentUtils.
    - src/integrations/supabase/types.ts (linhas ~2076–2107) — bloco Returns de get_replenishment_by_sku (manualmente mantido; NÃO regenerar).
    - src/lib/analysis/replenishmentUtils.ts — StatusEsgotado já adicionado na Task 1.
  </read_first>
  <behavior>
    - mapRow expõe row.status_esgotado tipado como StatusEsgotado, com fallback 'com_giro' quando ausente.
    - venda_dia_origem aceita 'historico_esgotado' (já refletido pelo type estendido na Task 1).
  </behavior>
  <action>
Em useReplenishmentBySku.ts: importe `StatusEsgotado` de replenishmentUtils; adicione
o campo `status_esgotado: StatusEsgotado` à interface `ReplenishmentSkuRow` (com JSDoc
"Phase 69 — balde de recência para esgotados"); mapeie em mapRow via
`((r.status_esgotado as string) ?? "com_giro") as StatusEsgotado` (padrão fallback
seguro do 67). Propague também `status_esgotado` para o agrupamento se necessário para
a linha mestre (ex.: derivar um status agregado priorizando repor>revisar>descontinuar);
mínimo: a linha filha (VariationRow) tem o campo.

Em types.ts: no bloco Returns de get_replenishment_by_sku, adicione
`status_esgotado: string` ao final. Acrescente também `p_smart?: boolean` ao bloco Args
(hoje o tipo está defasado — o hook já passa p_smart). NÃO regenerar o arquivo inteiro;
só editar esse bloco.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ReplenishmentSkuRow` tem `status_esgotado: StatusEsgotado` e mapRow o popula com fallback 'com_giro'.
    - types.ts Returns inclui `status_esgotado: string`; Args inclui `p_smart?: boolean`.
    - `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>tsc verde; o campo status_esgotado flui da RPC até ReplenishmentSkuRow.</done>
</task>

<task type="auto">
  <name>Task 3: UI — 3 estados na coluna "O que fazer", badge demanda estimada, filtro Situação</name>
  <files>src/components/mercadolivre/ReplenishmentSkuTable.tsx, src/components/mercadolivre/ReplenishmentSkuFilters.tsx, src/pages/mercadolivre/MLCompras.tsx</files>
  <read_first>
    - src/components/mercadolivre/ReplenishmentSkuTable.tsx — AcaoCell e MasterAcaoCell (ordem dos estados: sem_giro → gatilho+compra → custo_ausente → estoque ok); VendaDiaCell; padrão de Badge e emoji do 63-05.
    - src/components/mercadolivre/ReplenishmentSkuFilters.tsx — type FilterStatus + Select de Situação.
    - src/pages/mercadolivre/MLCompras.tsx — applyFilters (switch de filterStatus) + statusCounts + regroupRows.
    - src/lib/analysis/replenishmentUtils.ts — isDemandaEstimada (Task 1) p/ decidir o badge.
  </read_first>
  <action>
ReplenishmentSkuTable.tsx — AcaoCell (e o equivalente para a linha mestre/SKU único):
classifique PRIMEIRO por status_esgotado, antes da lógica atual:
  - 'descontinuar' → estado leigo tipo "⚫ Descontinuar" (sem quantidade), tom muted;
  - 'revisar_esgotado' → "⚠️ Revisar (parado)" (sem quantidade), tom warning;
  - 'repor_esgotado' → "🔴 Repor {compra_sugerida}" (tom destructive, como o "Comprar" atual)
    MAIS o badge "estoque zerado · demanda estimada pelo histórico" exibindo a venda/dia
    estimada (numFmt(row.venda_dia)/d) quando isDemandaEstimada(row.venda_dia_origem);
  - 'com_giro' → cai na lógica existente intocada (sem_giro/gatilho/custo/ok).
Rótulos/ícones exatos a seu critério, seguindo o padrão leigo do 63-05 (PT claro, sem
jargão). O badge deve ser visualmente distinto da venda real (use Badge outline/secondary
com texto curto + tooltip se ajudar). Para a linha mestre (MasterAcaoCell), derive um
estado agregado: se alguma variação é repor_esgotado → mostrar "Repor (esgotado)"; senão
revisar; senão descontinuar; senão lógica atual.

ReplenishmentSkuFilters.tsx — estenda `FilterStatus` para incluir 'repor_esgotado' |
'revisar_esgotado' | 'descontinuar' e adicione os 3 SelectItem com rótulos leigos (ex.:
"Repor esgotado", "Revisar parado", "Descontinuar").

MLCompras.tsx — em applyFilters, trate os 3 novos valores filtrando por
r.status_esgotado === <valor>. Atualize statusCounts se fizer sentido (ex.: contador de
"repor esgotado") seguindo o mini-resumo existente. Não precisa mudar regroupRows nem o
total de compra: a RPC já zera compra de revisar/descontinuar, então
total_compra_sugerida já exclui esses baldes (NÃO somar compra de revisar/descontinuar).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit && npx vitest run 2>&1 | tail -8 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - A coluna "O que fazer" mostra estados distintos para repor_esgotado / revisar_esgotado / descontinuar; com_giro inalterado.
    - Linhas com venda_dia_origem='historico_esgotado' exibem o badge "demanda estimada pelo histórico" com a venda/dia estimada.
    - O filtro Situação tem as 3 opções novas e filtra por status_esgotado.
    - O total de compra na tela NÃO inclui compra de revisar_esgotado/descontinuar (já 0 na RPC).
    - tsc 0 erros, vitest todos verdes, npm run build limpo (sem regressão das Phases 62–68).
  </acceptance_criteria>
  <done>/compras distingue visualmente demanda estimada de real e expõe os 3 baldes; build/tsc/vitest verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC → UI | Dados já filtrados por org pela RPC SECURITY INVOKER (69-01); UI só renderiza |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | Information Disclosure | UI /compras | accept | Sem novo dado sensível; isolamento de tenant é da RPC (69-01), UI só exibe linhas já escopadas |
| T-69-05 | Tampering | badge demanda estimada | mitigate | Badge derivado de venda_dia_origem='historico_esgotado' da RPC (fonte de verdade), não de heurística client-side |
</threat_model>

<verification>
- Task 1: vitest do replenishmentUtils.test.ts verde (testes novos + 63 existentes).
- Task 2: tsc --noEmit verde.
- Task 3: tsc + vitest (suite completa) + npm run build verdes.
- Checkpoint visual de Wesley em /compras (preview Vercel) é do orquestrador, pós-merge.
</verification>

<success_criteria>
- /compras mostra os 3 estados novos, badge "demanda estimada pelo histórico" com a venda/dia, e filtro Situação com as 3 opções; espelho TS testado; tsc/build/vitest sem regressão.
</success_criteria>

<artifacts_this_phase_produces>
Símbolos novos introduzidos por este plano:
- `StatusEsgotado` (type TS) + helpers `classifyStatusEsgotado`, `estimateBestRate`, `isDemandaEstimada` em replenishmentUtils.ts.
- Constantes TS: RECENCY_REPOR_DAYS, RECENCY_REVISAR_DAYS, BEST_WINDOW_DAYS, BEST_LOOKBACK_DAYS, MIN_DISTINCT_SALE_DAYS, CONSERVATIVE_DAYS.
- `VendaDiaOrigem` estendido com `'historico_esgotado'`.
- Campo `status_esgotado: StatusEsgotado` em `ReplenishmentSkuRow` (hook) + `status_esgotado: string` no Returns de types.ts + `p_smart?: boolean` no Args.
- Estados de UI novos na coluna "O que fazer": Repor (esgotado) / Revisar (parado) / Descontinuar; badge "estoque zerado · demanda estimada pelo histórico".
- `FilterStatus` estendido com 'repor_esgotado' | 'revisar_esgotado' | 'descontinuar' (filtro Situação).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/69-reposicao-de-esgotados-demanda-censurada/69-02-SUMMARY.md` when done
</output>
