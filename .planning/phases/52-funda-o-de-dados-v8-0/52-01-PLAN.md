---
phase: 52-funda-o-de-dados-v8-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260652000000_v8_action_tables.sql
  - supabase/migrations/20260652000100_v8_llm_cache.sql
  - supabase/migrations/20260652000200_v8_alter_existing.sql
  - supabase/migrations/20260652000300_v8_action_transition_rpc.sql
autonomous: false
requirements: ["LLM-04", "LLM-07", "ACT-02", "ACT-03", "ACT-04", "ACT-05", "ACT-06", "ACT-07", "SNZ-02", "STORE-01", "STORE-05", "TUNE-01"]

must_haves:
  truths:
    - "Existe llm_analysis_cache org-first com UNIQUE (organization_id, analysis_date, prompt_version) — 1 linha por org/dia/versão"
    - "Existe proposed_actions com 6 estados (proposed/approved/rejected/executing/done/failed) e índice de dedup parcial WHERE status IN ('proposed','approved','executing')"
    - "Existe action_audit_log append-only — só SELECT para authenticated, sem policy de UPDATE/DELETE"
    - "insights tem snoozed_until timestamptz; consultor_config tem llm_enabled boolean; consultor_health_snapshots tem ml_user_id_key"
    - "RPC claim_approved_action faz UPDATE ... WHERE status='approved' RETURNING * (SECURITY INVOKER) com EXECUTE revogado de PUBLIC/anon/authenticated"
    - "Owner só consegue mover proposed_actions para approved|rejected via RLS WITH CHECK — nunca para executing/done/failed"
  artifacts:
    - path: "supabase/migrations/20260652000000_v8_action_tables.sql"
      provides: "proposed_actions + action_audit_log + RLS org-first"
      contains: "CREATE TABLE IF NOT EXISTS public.proposed_actions"
    - path: "supabase/migrations/20260652000100_v8_llm_cache.sql"
      provides: "llm_analysis_cache org-first + RLS"
      contains: "CREATE TABLE IF NOT EXISTS public.llm_analysis_cache"
    - path: "supabase/migrations/20260652000200_v8_alter_existing.sql"
      provides: "ALTERs em insights / consultor_config / consultor_health_snapshots"
      contains: "ADD COLUMN IF NOT EXISTS"
    - path: "supabase/migrations/20260652000300_v8_action_transition_rpc.sql"
      provides: "RPC claim_approved_action atômica + REVOKE/GRANT"
      contains: "CREATE OR REPLACE FUNCTION public.claim_approved_action"
  key_links:
    - from: "supabase/migrations/20260652000000_v8_action_tables.sql"
      to: "public.organizations / public.insights"
      via: "FK organization_id REFERENCES organizations; insight_id REFERENCES insights"
      pattern: "REFERENCES public.(organizations|insights)"
    - from: "supabase/migrations/20260652000300_v8_action_transition_rpc.sql"
      to: "supabase/migrations/20260652000000_v8_action_tables.sql"
      via: "RPC opera sobre public.proposed_actions criada em M1"
      pattern: "UPDATE public.proposed_actions"
---

<objective>
Criar todo o schema de fundação do v8.0 (Consultor v2) em 4 migrations idempotentes: 3 tabelas novas (`proposed_actions`, `action_audit_log`, `llm_analysis_cache`), os ALTERs nas tabelas existentes (`insights`, `consultor_config`, `consultor_health_snapshots`) e a RPC de transição atômica `claim_approved_action`. Tudo com RLS org-first, state-machine de 6 estados via CHECK (não enum), dedup parcial e REVOKE de EXECUTE público.

Esta é uma fase de **dados pura** — sem UI, sem Edge Functions novas. O executor escreve os arquivos `.sql`; o **orquestrador aplica via MCP `apply_migration` em `ckcdevcxgvueywivefgx` e roda `get_advisors`** (gsd-executor não tem MCP/deploy Supabase).

Purpose: atende os 6 Success Criteria da Phase 52 (ROADMAP linhas 32–37) e desbloqueia as fases 53–56.
Output: 4 arquivos de migration commitados em `supabase/migrations/`, aplicados em produção, advisors sem erro crítico novo.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-funda-o-de-dados-v8-0/52-RESEARCH.md

# Padrões a replicar verbatim (org-first RLS, idempotência, REVOKE, INVOKER):
@supabase/migrations/20260645000000_consultor_tables.sql
@supabase/migrations/20260615120100_consultor_config_ads_cols.sql
@supabase/migrations/20260645011000_consultor_rpcs_revoke_public_execute.sql
@supabase/migrations/20260615120000_margin_with_ads_rpc.sql
</context>

<artifacts_this_phase_produces>
**Tabelas novas:**
- `public.proposed_actions` — fila de ações com state-machine de 6 estados
- `public.action_audit_log` — log de auditoria append-only/imutável
- `public.llm_analysis_cache` — cache de análise LLM org-first (1 linha/org/dia/prompt_version)

**Colunas novas (ALTER):**
- `insights.snoozed_until timestamptz NULL`
- `insights.snooze_count int NOT NULL DEFAULT 0` (discricionário — coluna informativa, baixo risco)
- `consultor_config.llm_enabled boolean NOT NULL DEFAULT true`
- `consultor_config.llm_model text NOT NULL DEFAULT 'claude-haiku-4-5'`
- `consultor_health_snapshots.ml_user_id_key text NOT NULL DEFAULT ''`

**Índices novos:**
- `proposed_actions_open_dedup` — UNIQUE PARCIAL `(organization_id, rule_key, target_ref) WHERE status IN ('proposed','approved','executing')`
- `proposed_actions_org_status_idx (organization_id, status, created_at DESC)`
- `audit_log_action_idx (action_id, created_at DESC)` / `audit_log_org_idx (organization_id, created_at DESC)`
- `llm_cache_org_date_idx (organization_id, analysis_date DESC)`

**Constraints novas:**
- `proposed_actions` CHECK status (6 estados) + CHECK action_type
- `llm_analysis_cache` UNIQUE `(organization_id, analysis_date, prompt_version)`
- `consultor_health_snapshots` UNIQUE `(organization_id, ml_user_id_key, snapshot_month)` (substitui `snapshots_org_month`)

**RPC nova:**
- `public.claim_approved_action(p_action_id uuid) RETURNS public.proposed_actions` — SECURITY INVOKER, REVOKE de PUBLIC/anon/authenticated, GRANT a service_role

(types.ts é entregue no plano 52-02.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migration M1 — proposed_actions + action_audit_log</name>
  <files>supabase/migrations/20260652000000_v8_action_tables.sql</files>
  <read_first>
    - `supabase/migrations/20260645000000_consultor_tables.sql:41-214` — padrão org-first: CREATE TABLE IF NOT EXISTS, CHECK via DO/EXCEPTION duplicate_object, índices IF NOT EXISTS, ENABLE RLS, DROP POLICY IF EXISTS antes de CREATE POLICY, helpers is_org_member/get_org_role.
    - `52-RESEARCH.md` seções "DDL: proposed_actions (M1)" (linhas ~122-160), "Immutability do action_audit_log" (linhas ~313-340) e "RLS & Security" (linhas ~217-264).
    - `.planning/research/ARCHITECTURE.md:96-155` (DDL completo) e `:280-313` (tabela de transições).
  </read_first>
  <action>
    Criar a migration M1 com header comentado no estilo de consultor_tables.sql (mencionar Supabase project ckcdevcxgvueywivefgx e idempotência). Conteúdo:

    TABELA proposed_actions: CREATE TABLE IF NOT EXISTS public.proposed_actions com colunas exatamente como em 52-RESEARCH.md DDL — id uuid PK gen_random_uuid; organization_id uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE; ml_user_id text NULL; insight_id uuid NULL REFERENCES public.insights(id) ON DELETE SET NULL; rule_key text NOT NULL; action_type text NOT NULL; target_ref text NOT NULL; current_value jsonb NULL; proposed_value jsonb NOT NULL; estimated_impact_brl numeric NULL; status text NOT NULL DEFAULT 'proposed'; proposed_by uuid NOT NULL; approved_by uuid NULL; approved_at timestamptz NULL; executed_at timestamptz NULL; result_summary text NULL; dry_run_preview jsonb NULL; created_at/updated_at timestamptz NOT NULL DEFAULT now(). As colunas de auditoria (approved_by/approved_at/executed_at/result_summary) entram AGORA, não em patch posterior (Pitfall 6 do research).

    CHECKs idempotentes (DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$, igual insights linhas 64-79): (a) status IN ('proposed','approved','rejected','executing','done','failed'); (b) action_type IN ('update_price','pause_ads_campaign','update_ads_budget','activate_listing','pause_listing'). NÃO usar enum nativo — text+CHECK (Pitfall P5).

    Índice de dedup PARCIAL (SC-2): CREATE UNIQUE INDEX IF NOT EXISTS proposed_actions_open_dedup ON public.proposed_actions (organization_id, rule_key, target_ref) WHERE status IN ('proposed','approved','executing'). E índice de query: proposed_actions_org_status_idx (organization_id, status, created_at DESC).

    RLS proposed_actions (replicar bloco de 52-RESEARCH.md "RLS & Security"): ENABLE ROW LEVEL SECURITY; DROP POLICY IF EXISTS para select/insert/update; POLICY select FOR SELECT TO authenticated USING is_org_member; POLICY insert FOR INSERT TO authenticated WITH CHECK (is_org_member AND proposed_by = auth.uid() AND status = 'proposed'); POLICY update FOR UPDATE TO authenticated USING (get_org_role = 'owner'::public.org_role) WITH CHECK (get_org_role = 'owner'::public.org_role AND status IN ('approved','rejected')). O WITH CHECK do UPDATE NUNCA pode permitir executing/done/failed (Anti-Pattern 3 / Pitfall P7) — esses estados são exclusivos do executor service_role. Sem policy de DELETE.

    TABELA action_audit_log: CREATE TABLE IF NOT EXISTS public.action_audit_log com id uuid PK; organization_id uuid NOT NULL REFERENCES organizations ON DELETE CASCADE; action_id uuid NOT NULL REFERENCES public.proposed_actions(id) ON DELETE CASCADE; actor_id uuid NULL; from_status text NOT NULL; to_status text NOT NULL; detail jsonb NULL; created_at timestamptz NOT NULL DEFAULT now(). Índices: audit_log_action_idx (action_id, created_at DESC) e audit_log_org_idx (organization_id, created_at DESC).

    RLS action_audit_log append-only: ENABLE ROW LEVEL SECURITY; DROP POLICY IF EXISTS "audit_log_select"; POLICY select FOR SELECT TO authenticated USING is_org_member. IMUTABILIDADE = AUSÊNCIA de policy de INSERT/UPDATE/DELETE para authenticated (default-deny). Escrita só via service_role (que ignora RLS). Adicionar comentário SQL explicando que o cap de ~4KB em detail é responsabilidade do executor (fase 54), não CHECK no schema (Q4 do research — um CHECK rígido faria o INSERT de auditoria falhar com resposta ML grande). NÃO adicionar trigger de imutabilidade (não exigido; padrão do projeto é ausência-de-grant).

    Idempotência obrigatória em todo o arquivo: IF NOT EXISTS / DO-EXCEPTION / DROP POLICY IF EXISTS — o orquestrador re-aplica via MCP sem medo.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260652000000_v8_action_tables.sql | grep -c "WHERE status IN ('proposed','approved','executing')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS public.proposed_actions"` == 1 e `... public.action_audit_log` == 1
    - O índice `proposed_actions_open_dedup` é UNIQUE e PARCIAL com `WHERE status IN ('proposed','approved','executing')` (grep acima retorna ≥1)
    - CHECK de status contém os 6 estados; CHECK de action_type contém os 5 tipos
    - Policy `proposed_actions_update` tem WITH CHECK com `status IN ('approved','rejected')` e NÃO menciona 'executing'/'done'/'failed'
    - `action_audit_log` tem APENAS policy de SELECT (nenhum CREATE POLICY de INSERT/UPDATE/DELETE para authenticated)
    - Arquivo é re-entrant: todos os CREATE TABLE/INDEX usam IF NOT EXISTS e policies usam DROP POLICY IF EXISTS
  </acceptance_criteria>
  <done>Migration M1 escrita, idempotente, com proposed_actions (state-machine 6 estados + dedup parcial) e action_audit_log (append-only) e RLS org-first.</done>
</task>

<task type="auto">
  <name>Task 2: Migration M2 — llm_analysis_cache (org-first)</name>
  <files>supabase/migrations/20260652000100_v8_llm_cache.sql</files>
  <read_first>
    - `52-RESEARCH.md` seção "DDL: llm_analysis_cache (M2)" (linhas ~164-186) e Pitfall P1/P2 (org-first na key; prompt_version na UNIQUE, divergindo do ARCHITECTURE que usava prompt_hash).
    - `supabase/migrations/20260645000000_consultor_tables.sql:90-114` — bloco de RLS SELECT-only org-first.
  </read_first>
  <action>
    Criar M2 com header comentado. CREATE TABLE IF NOT EXISTS public.llm_analysis_cache: id uuid PK gen_random_uuid; organization_id uuid NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE; analysis_date date NOT NULL; prompt_version text NOT NULL DEFAULT 'v1'; model_used text NOT NULL; prompt_hash text NULL (coluna de staleness para LLM-06, NÃO entra na key); analysis_text text NOT NULL; insight_count int NOT NULL DEFAULT 0; tokens_used int NULL; created_at timestamptz NOT NULL DEFAULT now().

    CONSTRAINT de unicidade org-first (SC-1, decisão sobre ARCHITECTURE no Pitfall P2): UNIQUE (organization_id, analysis_date, prompt_version) — organization_id DEVE liderar a key (anti cross-tenant leak, P1). Aplicar via CONSTRAINT inline ou ADD CONSTRAINT idempotente; nomear `llm_cache_org_date_version`. Índice de leitura: CREATE INDEX IF NOT EXISTS llm_cache_org_date_idx ON public.llm_analysis_cache (organization_id, analysis_date DESC).

    RLS: ENABLE ROW LEVEL SECURITY; DROP POLICY IF EXISTS "llm_cache_select"; POLICY select FOR SELECT TO authenticated USING is_org_member(auth.uid(), organization_id). INSERT/UPDATE só via service_role (sem policy de escrita para authenticated) — a EF da fase 53 grava o cache.

    Comentar no header POR QUE prompt_version está na key (isola caches quando o system prompt da fase 53 mudar, sem invalidar todas as orgs ao mesmo tempo — SC-1).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260652000100_v8_llm_cache.sql | grep -c "UNIQUE (organization_id, analysis_date, prompt_version)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS public.llm_analysis_cache"` == 1
    - A UNIQUE key é `(organization_id, analysis_date, prompt_version)` com organization_id como primeira coluna (grep acima ≥1)
    - Existe coluna `prompt_hash text NULL` separada da key (staleness, não dedup)
    - Existe APENAS policy de SELECT org-first (`is_org_member`); nenhuma policy de INSERT/UPDATE para authenticated
    - Arquivo re-entrant (IF NOT EXISTS / DROP POLICY IF EXISTS / constraint idempotente)
  </acceptance_criteria>
  <done>Migration M2 escrita: llm_analysis_cache org-first, key (org, date, prompt_version), RLS SELECT-only.</done>
</task>

<task type="auto">
  <name>Task 3: Migration M3 — ALTERs + Migration M4 — RPC de transição atômica</name>
  <files>supabase/migrations/20260652000200_v8_alter_existing.sql, supabase/migrations/20260652000300_v8_action_transition_rpc.sql</files>
  <read_first>
    - `supabase/migrations/20260615120100_consultor_config_ads_cols.sql` — padrão ALTER ... ADD COLUMN IF NOT EXISTS.
    - `52-RESEARCH.md` seções "DDL: ALTERs (M3)" (linhas ~188-213, incl. troca de UNIQUE idempotente / Pitfall P3) e "State-machine + transição atômica" (linhas ~284-309).
    - `supabase/migrations/20260615120000_margin_with_ads_rpc.sql` — SECURITY INVOKER + SET search_path + GRANT.
    - `supabase/migrations/20260645011000_consultor_rpcs_revoke_public_execute.sql` — REVOKE EXECUTE FROM PUBLIC, anon, authenticated + GRANT a service_role (Pitfall P4).
    - Nota P8: a policy `insights_dismiss` (UPDATE p/ qualquer membro, consultor_tables.sql:104-111) JÁ cobre escrita de snoozed_until — NÃO criar policy nova em insights.
  </read_first>
  <action>
    M3 (20260652000200_v8_alter_existing.sql) — header comentado, então 3 ALTERs idempotentes:
    1. ALTER TABLE public.insights ADD COLUMN IF NOT EXISTS snoozed_until timestamptz NULL, ADD COLUMN IF NOT EXISTS snooze_count int NOT NULL DEFAULT 0. NÃO re-adicionar ml_user_id nem ml_user_id_key (já existem — research P6/types.ts confirmam). NÃO criar policy nova (insights_dismiss já cobre — P8).
    2. ALTER TABLE public.consultor_config ADD COLUMN IF NOT EXISTS llm_enabled boolean NOT NULL DEFAULT true, ADD COLUMN IF NOT EXISTS llm_model text NOT NULL DEFAULT 'claude-haiku-4-5'. NÃO re-adicionar os 14 limiares (já existem). A policy owner-only consultor_config_write já cobre a escrita.
    3. consultor_health_snapshots: ADD COLUMN IF NOT EXISTS ml_user_id_key text NOT NULL DEFAULT ''; depois DROP CONSTRAINT IF EXISTS snapshots_org_month; depois ADD CONSTRAINT snapshots_org_store_month UNIQUE (organization_id, ml_user_id_key, snapshot_month) envolvido em DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$ (Pitfall P3 — a troca de UNIQUE precisa ser não-ingênua/re-entrant; linhas org-level antigas têm ml_user_id_key='' por default e não colidem).

    M4 (20260652000300_v8_action_transition_rpc.sql) — header comentado explicando POR QUE SECURITY INVOKER (a RLS org-first de proposed_actions enforça escopo; passar p_action_id de outra org não casa linha visível ao caller → anti-IDOR) e POR QUE o REVOKE (Postgres concede EXECUTE a PUBLIC por default → get_advisors acusa). Então:
    CREATE OR REPLACE FUNCTION public.claim_approved_action(p_action_id uuid) RETURNS public.proposed_actions LANGUAGE sql SECURITY INVOKER SET search_path = public AS $$ UPDATE public.proposed_actions SET status='executing', updated_at=now() WHERE id = p_action_id AND status = 'approved' RETURNING *; $$;
    REVOKE EXECUTE ON FUNCTION public.claim_approved_action(uuid) FROM PUBLIC, anon, authenticated;
    GRANT EXECUTE ON FUNCTION public.claim_approved_action(uuid) TO service_role;
    (Zero linhas retornadas = já reivindicada → executor da fase 54 aborta antes de tocar a API ML — anti-TOCTOU/dedup.)
  </action>
  <verify>
    <automated>grep -c "SECURITY INVOKER" supabase/migrations/20260652000300_v8_action_transition_rpc.sql; grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260652000200_v8_alter_existing.sql</automated>
  </verify>
  <acceptance_criteria>
    - M3: `grep -c "ADD COLUMN IF NOT EXISTS"` >= 5 (snoozed_until, snooze_count, llm_enabled, llm_model, ml_user_id_key)
    - M3 NÃO contém `ADD COLUMN IF NOT EXISTS ml_user_id` em insights nem re-adiciona limiares existentes em consultor_config
    - M3: troca de UNIQUE de snapshots usa `DROP CONSTRAINT IF EXISTS snapshots_org_month` e `ADD CONSTRAINT snapshots_org_store_month` dentro de DO/EXCEPTION duplicate_object
    - M4: a função é `SECURITY INVOKER` (não DEFINER), faz `UPDATE ... WHERE id = p_action_id AND status = 'approved' RETURNING *`
    - M4 contém `REVOKE EXECUTE ON FUNCTION public.claim_approved_action(uuid) FROM PUBLIC, anon, authenticated` e `GRANT EXECUTE ... TO service_role`
  </acceptance_criteria>
  <done>M3 (3 ALTERs idempotentes, troca de UNIQUE re-entrant) e M4 (RPC INVOKER atômica + REVOKE/GRANT) escritas.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4 [BLOCKING]: Orquestrador aplica as 4 migrations via MCP + get_advisors</name>
  <action>O ORQUESTRADOR (não o gsd-executor, que não tem MCP/deploy Supabase) aplica as 4 migrations via MCP `apply_migration` no project ckcdevcxgvueywivefgx, na ordem 20260652000000 → 100 → 200 → 300, rodando `get_advisors` (security) após cada apply. Depois valida via execute_sql/list_tables conforme how-to-verify. NUNCA usar `supabase db push` (CLI linkado no projeto errado gionpsuunfkkzzjdubfy).</action>
  <what-built>4 arquivos de migration escritos em supabase/migrations/ (20260652000000..300). NÃO foram aplicados — gsd-executor não tem MCP/deploy Supabase.</what-built>
  <how-to-verify>
    O ORQUESTRADOR (não o executor) executa, nesta ordem, via MCP `apply_migration` no project **ckcdevcxgvueywivefgx** — NUNCA `supabase db push` (CLI linkado no projeto errado gionpsuunfkkzzjdubfy):
    1. apply_migration 20260652000000_v8_action_tables
    2. apply_migration 20260652000100_v8_llm_cache
    3. apply_migration 20260652000200_v8_alter_existing
    4. apply_migration 20260652000300_v8_action_transition_rpc
    Após cada apply rodar `get_advisors` (security). Depois validar via `execute_sql`/`list_tables` em ckcdevcxgvueywivefgx:
    - `list_tables` mostra proposed_actions, action_audit_log, llm_analysis_cache com as colunas esperadas
    - SC-2: tentar 2 ações abertas mesmo (org, rule_key, target_ref) → 2ª viola `proposed_actions_open_dedup`
    - SC-3: como role authenticated, UPDATE/DELETE em action_audit_log → negado (default-deny)
    - SC-5: 2 chamadas de `claim_approved_action` no mesmo id 'approved' → 1 retorna a linha, a outra retorna 0 linhas; confirmar EXECUTE revogado de authenticated/anon
    - get_advisors: NENHUM erro crítico novo (especialmente nenhum `*_security_definer_function_executable` para claim_approved_action)
  </how-to-verify>
  <resume-signal>Digite "aplicado" quando as 4 migrations estiverem aplicadas em ckcdevcxgvueywivefgx e get_advisors estiver sem erro crítico novo; ou descreva o erro do advisor para correção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → proposed_actions/llm_analysis_cache/action_audit_log | Usuário autenticado de uma org tentando ler/escrever dados de outra org (cross-tenant) |
| authenticated owner → proposed_actions UPDATE | Owner tentando forjar estado de execução sem o executor real |
| anon/PUBLIC → claim_approved_action RPC | Default EXECUTE do Postgres expõe a RPC a chamadores não autorizados via /rest/v1/rpc |
| executor (service_role) → proposed_actions | Execução concorrente/duplicada da mesma ação aprovada (TOCTOU) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information Disclosure | llm_analysis_cache | mitigate | UNIQUE org-first `(organization_id, analysis_date, prompt_version)` + RLS SELECT `is_org_member`; organization_id lidera a key (anti cross-tenant leak, P1) |
| T-52-02 | Elevation/Tampering (IDOR) | proposed_actions / claim_approved_action | mitigate | RLS org-first `is_org_member`/`get_org_role`; RPC `claim_approved_action` SECURITY INVOKER (RLS enforça escopo mesmo com p_action_id arbitrário) |
| T-52-03 | Elevation | claim_approved_action (default EXECUTE a PUBLIC) | mitigate | `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated` + `GRANT TO service_role` (Pitfall P4; precedente 20260645011000) |
| T-52-04 | Tampering | proposed_actions UPDATE policy | mitigate | RLS WITH CHECK limita authenticated a `status IN ('approved','rejected')` — owner não forja 'done'/'executing' (Anti-Pattern 3 / P7) |
| T-52-05 | Repudiation | action_audit_log | mitigate | Append-only: SELECT-only policy, sem GRANT de UPDATE/DELETE → mutação negada por default-deny |
| T-52-06 | Tampering (duplicação) | proposed_actions / claim_approved_action | mitigate | Gate atômico `UPDATE ... WHERE status='approved' RETURNING *` (0 linhas = já reivindicada) + índice dedup parcial impede 2ª ação aberta |
| T-52-07 | Denial of Service | action_audit_log.detail jsonb | accept | Cap ~4KB aplicado pelo executor (fase 54), não no schema — CHECK rígido faria o INSERT de auditoria falhar com resposta ML grande (Q4) |
| T-52-SC | Tampering | migrations aplicadas no projeto errado | mitigate | Aplicar SOMENTE via MCP `apply_migration` em ckcdevcxgvueywivefgx; NUNCA `db push` (CLI no projeto errado); checkpoint blocking-human do orquestrador |

(Sem instalação de pacotes npm/pip/cargo nesta fase → sem Package Legitimacy Gate.)
</threat_model>

<verification>
- 4 migrations escritas em supabase/migrations/ com prefixo 20260652* (ordena após 20260650000400) e idempotentes.
- Aplicadas via MCP em ckcdevcxgvueywivefgx (checkpoint do orquestrador), advisors sem erro crítico novo.
- SC-1 (cache org-first), SC-2 (6 estados + dedup parcial), SC-3 (append-only), SC-4 (colunas), SC-5 (RPC atômica + REVOKE) confirmados via execute_sql/list_tables/get_advisors.
</verification>

<success_criteria>
- proposed_actions, action_audit_log, llm_analysis_cache existem em ckcdevcxgvueywivefgx com as colunas/índices/constraints especificados.
- insights.snoozed_until, consultor_config.llm_enabled, consultor_health_snapshots.ml_user_id_key presentes.
- claim_approved_action é SECURITY INVOKER com EXECUTE revogado de PUBLIC/anon/authenticated.
- get_advisors sem erro crítico novo (especialmente sem `*_security_definer_function_executable` para a RPC nova).
</success_criteria>

<output>
Create `.planning/phases/52-funda-o-de-dados-v8-0/52-01-SUMMARY.md` when done
</output>
