---
phase: 52-funda-o-de-dados-v8-0
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/integrations/supabase/types.ts
autonomous: true
requirements: ["LLM-04", "LLM-07", "ACT-02", "ACT-05", "SNZ-02", "STORE-05", "TUNE-01"]

must_haves:
  truths:
    - "types.ts tem os blocos Row/Insert/Update/Relationships das 3 tabelas novas (proposed_actions, action_audit_log, llm_analysis_cache)"
    - "Os blocos existentes de insights, consultor_config e consultor_health_snapshots têm as colunas novas com nullability correta"
    - "tsc --noEmit passa — os consumidores existentes continuam compilando com o novo types.ts"
  artifacts:
    - path: "src/integrations/supabase/types.ts"
      provides: "Tipos TypeScript do schema v8.0 (edição manual — CLI linkado no projeto errado)"
      contains: "proposed_actions"
  key_links:
    - from: "src/integrations/supabase/types.ts"
      to: "supabase/migrations/20260652000000_v8_action_tables.sql"
      via: "blocos de tipo refletem manualmente o DDL das migrations 20260652*"
      pattern: "proposed_actions|action_audit_log|llm_analysis_cache"
---

<objective>
Atualizar manualmente `src/integrations/supabase/types.ts` para refletir o schema v8.0 criado no plano 52-01: blocos de tipo para as 3 tabelas novas e os campos novos nas 3 tabelas alteradas. Garantir que o projeto compila (`tsc --noEmit`) e builda.

**Por que manual:** o Supabase CLI local está linkado no projeto errado (`gionpsuunfkkzzjdubfy`) — `supabase gen types` apontaria para o schema errado. types.ts é editado à mão neste projeto (precedente Phase 18-02, STATE.md:143).

Purpose: SC-6 (types.ts atualizado + build verde). Sem isso, as fases 53–56 não têm tipos para consumir as tabelas novas.
Output: `src/integrations/supabase/types.ts` editado, `tsc --noEmit` e `npm run build` verdes.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-funda-o-de-dados-v8-0/52-RESEARCH.md
@.planning/phases/52-funda-o-de-dados-v8-0/52-01-PLAN.md

# Estrutura exata dos blocos a replicar (Row/Insert/Update/Relationships):
@src/integrations/supabase/types.ts
# DDL fonte da verdade (as migrations escritas no plano 52-01):
@supabase/migrations/20260652000000_v8_action_tables.sql
@supabase/migrations/20260652000100_v8_llm_cache.sql
@supabase/migrations/20260652000200_v8_alter_existing.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campos novos às 3 tabelas existentes em types.ts</name>
  <files>src/integrations/supabase/types.ts</files>
  <read_first>
    - `src/integrations/supabase/types.ts:120-305` — blocos atuais de consultor_config (120), consultor_health_snapshots (185) e insights (238): formato Row/Insert/Update e regra de nullability (NOT NULL DEFAULT → obrigatório no Row, opcional `?` no Insert/Update; NULL → `| null`).
    - `52-RESEARCH.md` seção "types.ts — abordagem de atualização" (linhas ~344-353).
    - DDL fonte: `supabase/migrations/20260652000200_v8_alter_existing.sql` (do plano 52-01) para os nomes/tipos/defaults exatos.
  </read_first>
  <action>
    Editar em ordem alfabética dentro de cada bloco existente, mantendo o estilo idêntico (chaves ordenadas alfabeticamente como nos blocos atuais):

    consultor_config (bloco em ~:120): adicionar nos 3 sub-blocos Row/Insert/Update:
      - llm_enabled: boolean (Row obrigatório; Insert/Update opcional `llm_enabled?: boolean`)
      - llm_model: string (Row obrigatório; Insert/Update opcional `llm_model?: string`)

    consultor_health_snapshots (bloco em ~:185): adicionar:
      - ml_user_id_key: string (NOT NULL DEFAULT '' → Row obrigatório; Insert/Update opcional `ml_user_id_key?: string`)

    insights (bloco em ~:238): adicionar:
      - snoozed_until: string | null (timestamptz NULL → Row `string | null`; Insert/Update `snoozed_until?: string | null`)
      - snooze_count: number (NOT NULL DEFAULT 0 → Row obrigatório; Insert/Update `snooze_count?: number`)

    Não tocar nos Relationships desses blocos (FKs não mudaram).
  </action>
  <verify>
    <automated>grep -c "llm_enabled" src/integrations/supabase/types.ts; grep -c "snoozed_until" src/integrations/supabase/types.ts; grep -c "ml_user_id_key" src/integrations/supabase/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "llm_enabled"` >= 3 (Row + Insert + Update de consultor_config)
    - `grep -c "llm_model"` >= 3
    - `grep -c "snoozed_until"` >= 3 (Row + Insert + Update de insights)
    - `grep -c "snooze_count"` >= 3
    - `ml_user_id_key` aparece nos blocos de consultor_health_snapshots além do de insights já existente
    - Nullability: `snoozed_until: string | null` no Row; demais colunas NOT NULL DEFAULT são obrigatórias no Row e opcionais (`?`) no Insert/Update
  </acceptance_criteria>
  <done>consultor_config, consultor_health_snapshots e insights têm as colunas novas com nullability correta em Row/Insert/Update.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar blocos de tipo das 3 tabelas novas + verificar build</name>
  <files>src/integrations/supabase/types.ts</files>
  <read_first>
    - `src/integrations/supabase/types.ts:238-305` — bloco `insights` completo (Row/Insert/Update/Relationships) como template de tabela org-first com FK para organizations.
    - DDL fonte: `supabase/migrations/20260652000000_v8_action_tables.sql` e `20260652000100_v8_llm_cache.sql` (do plano 52-01) para colunas/tipos/nullability/FKs exatos.
  </read_first>
  <action>
    Inserir 3 novos blocos de tabela em ordem alfabética correta dentro de `Tables` (action_audit_log antes de commercial_*; llm_analysis_cache e proposed_actions na posição alfabética). Cada bloco com Row/Insert/Update/Relationships seguindo o template de `insights`.

    proposed_actions — Row: id string; organization_id string; ml_user_id string | null; insight_id string | null; rule_key string; action_type string; target_ref string; current_value Json | null; proposed_value Json; estimated_impact_brl number | null; status string; proposed_by string; approved_by string | null; approved_at string | null; executed_at string | null; result_summary string | null; dry_run_preview Json | null; created_at string; updated_at string. Insert: obrigatórios = organization_id, rule_key, action_type, target_ref, proposed_value, proposed_by; o resto opcional (id?, status?, created_at?, updated_at? têm default; os NULL são opcionais). Update: todos opcionais. Relationships: FK proposed_actions_organization_id_fkey → organizations(id); FK proposed_actions_insight_id_fkey → insights(id).

    action_audit_log — Row: id string; organization_id string; action_id string; actor_id string | null; from_status string; to_status string; detail Json | null; created_at string. Insert: obrigatórios organization_id, action_id, from_status, to_status; id?/created_at?/actor_id?/detail? opcionais. Update todos opcionais. Relationships: FK → organizations(id) e FK action_id → proposed_actions(id).

    llm_analysis_cache — Row: id string; organization_id string; analysis_date string; prompt_version string; model_used string; prompt_hash string | null; analysis_text string; insight_count number; tokens_used number | null; created_at string. Insert: obrigatórios organization_id, analysis_date, model_used, analysis_text; prompt_version?/insight_count?/id?/created_at? opcionais (têm default); prompt_hash?/tokens_used? opcionais (NULL). Update todos opcionais. Relationships: FK → organizations(id).

    Usar o tipo `Json` do próprio arquivo para colunas jsonb. Após editar, rodar a verificação de build.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "proposed_actions:"`, `action_audit_log:`, `llm_analysis_cache:` cada um >= 1 (bloco de tabela presente)
    - Colunas jsonb tipadas como `Json` / `Json | null`; colunas NULL como `... | null` no Row
    - `npx tsc --noEmit` passa sem erros
    - `npm run build` conclui com sucesso (types.ts novo compila com os consumidores existentes)
  </acceptance_criteria>
  <done>Os 3 blocos de tabela novos estão em types.ts na posição alfabética correta; tsc e build verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| types.ts (gerado manualmente) → consumidores frontend | Tipos incorretos (nullability errada / coluna faltando) mascarariam bugs de tenant/escopo nas fases 53–56 que consomem essas tabelas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08 | Tampering (drift de tipo) | src/integrations/supabase/types.ts | mitigate | Editar manualmente espelhando o DDL das migrations 20260652* (fonte da verdade); `tsc --noEmit` + `npm run build` verdes garantem consistência com os consumidores |
| T-52-09 | Information Disclosure (nullability incorreta) | colunas org-first em types.ts | accept | A segurança real é a RLS no banco (plano 52-01), não os tipos; types.ts errado é bug de DX, não de isolamento de tenant |

(Sem instalação de pacotes nesta fase → sem Package Legitimacy Gate.)
</threat_model>

<verification>
- types.ts contém os 3 blocos de tabela novos + os campos novos nas 3 tabelas alteradas, com nullability correta.
- `npx tsc --noEmit` e `npm run build` passam (SC-6 build verde).
</verification>

<success_criteria>
- src/integrations/supabase/types.ts reflete fielmente o schema v8.0 (3 tabelas novas + 5 colunas novas).
- Projeto compila e builda sem erros.
</success_criteria>

<output>
Create `.planning/phases/52-funda-o-de-dados-v8-0/52-02-SUMMARY.md` when done
</output>
