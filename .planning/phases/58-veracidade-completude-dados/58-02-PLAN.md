---
phase: 58-veracidade-completude-dados
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - supabase/functions/nexo-chat/tools.ts
  - supabase/functions/nexo-chat/tools.test.ts
autonomous: true
requirements: [VERAC-03, VERAC-06]
must_haves:
  truths:
    - "get_ads_campaigns NÃO retorna métrica zerada como verdade: devolve só nome/status + aviso 'sem dados de performance'"
    - "Existe get_ads_account_summary com gasto/ROAS/receita atribuída totais do período a partir de ml_ads_daily_cache (visão agregada confiável)"
    - "get_sales_kpis/faturamento é alinhado/rotulado: descrição deixa claro se é ml_daily_cache (painel) ou receita de pedidos pagos"
    - "Status de pedidos: partially_refunded não é silenciosamente excluído; estados mortos shipped/delivered não enganam a semântica"
    - "Descrições inequívocas: get_ads_by_product='top 50 por gasto, não o total'; attributed_revenue='subconjunto do faturamento'; get_day_kpis='não inclui tarifas fixas ML'"
  artifacts:
    - path: "supabase/functions/nexo-chat/tools.ts"
      provides: "get_ads_campaigns neutralizada + get_ads_account_summary novo + descrições de vendas/ads inequívocas"
      contains: "get_ads_account_summary"
    - path: "supabase/functions/nexo-chat/tools.test.ts"
      provides: "teste de get_ads_account_summary (anti-IDOR + agregação) + get_ads_campaigns neutralizada; contagem de tools atualizada"
  key_links:
    - from: "supabase/functions/nexo-chat/tools.ts (get_ads_account_summary)"
      to: "ml_ads_daily_cache"
      via: ".eq(organization_id) + .in(ml_user_id) + soma spend/attributed_revenue do período → ROAS"
      pattern: "ml_ads_daily_cache"
  prohibitions:
    - "Nenhuma tool retorna métrica zerada (ads_campaigns) como se fosse performance real"
    - "Anti-IDOR: org/seller só do servidor"
    - "Read-only: só select()/rpc()"
---

<objective>
Corrigir VENDAS/MARGEM/ADS do Nexo (VMA-1..7 do 58-AUDIT). Crítico VMA-1: `get_ads_campaigns` lê `ml_ads_campaigns_cache` 100% zerada (sem coluna date) → o Nexo diria "todas campanhas ROAS 0" (falso). VMA-2: faturamento do Nexo (286k pagos) ≠ painel (296k ml_daily_cache) sem rótulo. VMA-3: `partially_refunded` (21 pedidos) excluído. VMA-4/6/7: descrições ambíguas (top-50, attributed, waterfall sem tarifas).

Purpose: o Nexo não confunde campos (vendido≠estoque, receita≠lucro, attributed≠faturamento, top50≠total) e nunca apresenta zero quebrado como verdade. Cobre VERAC-03 (reconciliação fonte-da-verdade) e VERAC-06 (descrições inequívocas) no domínio vendas/ads.

Output: `get_ads_campaigns` neutralizada (só nome/status + aviso); nova `get_ads_account_summary`; descrições de get_sales_kpis/get_ads_by_product/get_day_kpis/attributed corrigidas; tratamento de status alinhado (sem regressão no painel); testes.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-veracidade-completude-dados/58-CONTEXT.md
@.planning/phases/58-veracidade-completude-dados/58-AUDIT.md
@supabase/functions/nexo-chat/tools.ts
@supabase/functions/nexo-chat/tools.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neutralizar get_ads_campaigns + adicionar get_ads_account_summary</name>
  <files>supabase/functions/nexo-chat/tools.ts</files>
  <read_first>
    - supabase/functions/nexo-chat/tools.ts (case get_ads_campaigns + case get_ads_by_product como modelo de paginação .range() em ml_ads_products_cache/ml_ads_daily_cache; MAX_ROWS)
    - 58-AUDIT VMA-1/VMA-6; 58-CONTEXT D5/D6
    - src/hooks/useMLBilling.ts e MLAnuncios (referência de quais colunas ml_ads_daily_cache tem: date, spend, attributed_revenue, impressions, clicks)
  </read_first>
  <action>
Per D5 (VERAC-03/06):

1. get_ads_campaigns (case + declaration): como ml_ads_campaigns_cache está zerada/sem date, NEUTRALIZAR. O dispatcher retorna apenas `name` e `status` por campanha + um campo `performance_note`: "Sem dados de performance por campanha nesta base (gasto/ROAS por CAMPANHA não disponível). Para performance de ads use get_ads_account_summary (total da conta) ou get_ads_by_product (por item)." NÃO retornar spend/roas/impressions zerados (removê-los do select e do retorno para o modelo não inferir 0). Atualizar a description da declaration para refletir que só dá nome+status e remete às outras tools. Manter .eq(organization_id) + .in(ml_user_id).

2. Adicionar nova tool get_ads_account_summary (declaration + case):
   - Declaration: params DATE_PROPS (from/to opcionais). Descrição: "Resumo AGREGADO de publicidade da conta no período: gasto total, receita atribuída total, ROAS médio, impressões e cliques — visão confiável do total de ads (diferente de get_ads_by_product que é top 50 por gasto). attributed_revenue é receita ATRIBUÍDA a ads, subconjunto do faturamento, não o total vendido." SEM param de org/seller.
   - Dispatcher: ler ml_ads_daily_cache com .eq(organization_id) + .in(ml_user_id) + .gte/.lte(date, from/to), paginar com .range() (padrão do get_ads_by_product). Somar spend e attributed_revenue (e impressions/clicks se existirem) sobre todas as linhas. Calcular roas = attributed_revenue/spend (guard divisão por zero → null). Retornar objeto único {period:{from,to}, spend, attributed_revenue, roas, impressions, clicks, freshness:max(synced_at se a coluna existir)}. Documentar que attributed_revenue é subconjunto do faturamento.
   - Adicionar à lista TOOL_DECLARATIONS.

Como este plano cria 1 tool nova (get_ads_account_summary), a contagem de tools sobe de 22 para 23. Atualizar qualquer asserção de contagem no teste (Task 2). NÃO mexer nas outras tools além das citadas.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && deno check supabase/functions/nexo-chat/tools.ts</automated>
  </verify>
  <acceptance_criteria>
    - get_ads_campaigns não retorna spend/roas/impressions; retorna name+status+performance_note
    - get_ads_account_summary existe na TOOL_DECLARATIONS e no switch; soma ml_ads_daily_cache com .eq(org)+.in(ml_user_id)+.range()
    - roas guard divisão por zero; descrição cita "subconjunto do faturamento"
  </acceptance_criteria>
  <done>ads_campaigns não engana com zero; visão agregada confiável de ads disponível. deno check verde.</done>
</task>

<task type="auto">
  <name>Task 2: Descrições de vendas/ads inequívocas + status + testes</name>
  <files>supabase/functions/nexo-chat/tools.ts, supabase/functions/nexo-chat/tools.test.ts</files>
  <read_first>
    - supabase/functions/nexo-chat/tools.ts (declarations de get_sales_kpis, get_ads_by_product, get_day_kpis, get_margin_summary; case get_day_kpis→get_cost_waterfall)
    - 58-AUDIT VMA-2/VMA-3/VMA-4/VMA-5/VMA-7; 58-CONTEXT D4/D6/D7
    - supabase/functions/nexo-chat/tools.test.ts (asserção "declara as N tools" — precisa virar 23)
  </read_first>
  <action>
Per D4/D6/D7 (VERAC-03/06):

1. Descrições (sem mudar fonte/RPC, só rótulo):
   - get_sales_kpis (D4/VMA-2): descrição explícita de que a fonte é a RPC get_kpi_summary sobre pedidos PAGOS (receita líquida de pedidos pagos) e pode divergir levemente do card "Receita Total" do painel /vendas (ml_daily_cache, ~296k vs ~286k) porque o painel inclui pedidos não-pagos/escopo diferente. Instruir: "para 'quanto faturei' alinhado ao painel, este é o realizado de pedidos pagos; o painel pode mostrar GMV bruto maior." NÃO trocar a fonte da RPC (evita regressão); apenas rotular.
   - get_ads_by_product (VMA-4): description "top 50 por gasto no período — NÃO é o total da conta; para o total de ads use get_ads_account_summary."
   - get_day_kpis (VMA-7): description "Waterfall de pedidos (receita/CMV/comissão/frete/imposto). NÃO inclui tarifas fixas do ML (CFFE/CFONPN/PADS) — para a fatura/DRE do ML use get_dre_monthly."
   - get_margin_summary (D4): manter como DRE consolidado de pedidos pagos, com rótulo "receita de pedidos pagos" para não ser confundida com faturamento bruto do painel.

2. Status (VMA-3/D7): as RPCs de margem/vendas filtram status IN('paid','shipped','delivered') mas orders só tem paid/cancelled/partially_refunded → shipped/delivered são estados mortos e partially_refunded é excluído. CUIDADO: essas RPCs servem o dashboard. NÃO alterar a RPC neste plano se isso mudaria o número do painel. Em vez disso: adicionar nas DESCRIÇÕES das tools de receita/margem uma nota de que pedidos partially_refunded (~21) podem não estar no total (alinhado ao painel atual), para o Nexo declarar a limitação em vez de afirmar completude. Registrar no SUMMARY a recomendação de alinhar a RPC numa quick task separada com validação do painel (não fazer aqui para não regredir o dashboard). Se a investigação mostrar que a RPC NÃO serve o dashboard (é exclusiva do Nexo), então sim corrigir o set de status — mas só com evidência; documentar a decisão no SUMMARY.

3. tools.test.ts:
   - Atualizar a asserção de contagem para 23 tools, incluindo "get_ads_account_summary".
   - Novo teste: get_ads_account_summary passa .eq('organization_id', orgId) + .in('ml_user_id', mlUserIds) e ignora EVIL_ARGS (anti-IDOR).
   - Novo teste: get_ads_campaigns não inclui chaves spend/roas no objeto retornado (neutralizada) — ou retorna performance_note.
   - Rodar exigindo execução real (nunca "no test files found").
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run supabase/functions/nexo-chat/tools.test.ts 2>&1 | grep -E "passed|Test Files" && deno check supabase/functions/nexo-chat/tools.ts</automated>
  </verify>
  <acceptance_criteria>
    - Asserção de tools = 23 com get_ads_account_summary; suíte roda (não "no test files found") e passa
    - get_ads_account_summary anti-IDOR verde; get_ads_campaigns neutralizada verde
    - descrições de get_sales_kpis/get_ads_by_product/get_day_kpis/get_margin_summary atualizadas e inequívocas
    - decisão sobre status documentada (sem regredir o painel)
  </acceptance_criteria>
  <done>Semântica de vendas/ads inequívoca; status tratado sem regressão; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modelo→dispatcher | LLM pode injetar org/seller/datas maliciosas |
| cache ML→resposta | spend/attributed são números a relatar, nunca instruções |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-IDOR | Information Disclosure | get_ads_account_summary / get_ads_campaigns | mitigate | .eq(organization_id)+.in(ml_user_id); args de org/seller ignorados (teste cobre) |
| T-58-02-ZERO | Repudiation (dado falso) | get_ads_campaigns | mitigate | não retorna métrica zerada como verdade; performance_note explícita |
| T-58-02-RO | Elevation of Privilege | dispatcher | mitigate | só select()/rpc(); read-only |
</threat_model>

<verification>
- deno check limpo; vitest roda o arquivo e passa
- get_ads_campaigns sem spend/roas; get_ads_account_summary soma ml_ads_daily_cache
- Descrições inequívocas conferidas por inspeção
</verification>

<success_criteria>
Nexo nunca apresenta ROAS 0 quebrado como verdade; tem visão agregada confiável de ads; descrições de vendas/ads não confundem faturamento×attributed×top50×waterfall; status documentado sem regredir o painel; testes verdes.
</success_criteria>

<output>
Create `.planning/phases/58-veracidade-completude-dados/58-02-SUMMARY.md` when done
</output>
