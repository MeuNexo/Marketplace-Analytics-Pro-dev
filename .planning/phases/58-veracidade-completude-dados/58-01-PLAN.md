---
phase: 58-veracidade-completude-dados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/nexo-chat/tools.ts
  - supabase/functions/nexo-chat/tools.test.ts
autonomous: true
requirements: [VERAC-01, VERAC-02, VERAC-04]
must_haves:
  truths:
    - "get_inventory sem search retorna por padrão anúncios ATIVOS (não soterra com pausados)"
    - "get_inventory retorna um resumo agregado (nº itens, total unidades Full, ativos, pausados, nº com tamanho esgotado) além da amostra"
    - "Quando has_variations=true, o resultado resume os tamanhos/cores com qty=0 ('X de N tamanhos esgotados') a partir do jsonb variations"
    - "Todo resultado de estoque é rotulado como 'estoque Full (fulfillment)' — fonte explícita"
    - "get_inventory expõe synced_at/recência do cache para o modelo sinalizar defasagem"
    - "get_coverage rotula 'ruptura no Full' e documenta a janela (30d fixo ou intervalo real)"
  artifacts:
    - path: "supabase/functions/nexo-chat/tools.ts"
      provides: "get_inventory com status=active default + agregado + variações + label Full + synced_at; get_coverage rótulo Full"
      contains: "logistic_type"
    - path: "supabase/functions/nexo-chat/tools.test.ts"
      provides: "testes do novo shape de get_inventory (active default, agregado, variações, anti-IDOR mantido)"
  key_links:
    - from: "supabase/functions/nexo-chat/tools.ts (get_inventory)"
      to: "ml_inventory_cache (jsonb variations + has_variations + logistic_type + synced_at)"
      via: "select inclui variations,has_variations,logistic_type,synced_at; .eq(organization_id) + .in(ml_user_id)"
      pattern: "variations"
  prohibitions:
    - "NENHUMA mutação no banco/ML (read-only) — só select() no dispatcher"
    - "Anti-IDOR: orgId e mlUserIds só do servidor; args.org_id/seller_id/ml_user_id sempre ignorados"
    - "Nenhuma tool retorna métrica como verdade absoluta sem rótulo de fonte/escopo (Full≠total)"
---

<objective>
Corrigir o domínio ESTOQUE do Nexo (achados EST-1..6 do 58-AUDIT). Hoje `get_inventory` sem busca ordena por `available_quantity ASC` + cap 50 sem filtrar status → devolve só anúncios PAUSADOS; expõe `available_quantity` item-level mascarando ruptura por variação; não traz agregado nem rótulo de que é só Full; não expõe frescura.

Purpose: o Nexo nunca afirma "0 em estoque / ruptura" como fato absoluto lendo só o Full, nem reporta número item-level como se fosse por variação. Cobre VERAC-01 (estoque Full rotulado, sem afirmar ruptura absoluta), VERAC-02 (estoque por variação) e VERAC-04 (frescura) no domínio estoque.

Output: `get_inventory` reescrito (default status=active, resumo agregado, resumo de variações esgotadas, rótulo "estoque Full (fulfillment)", `synced_at`); `get_coverage` com rótulos corretos; testes atualizados.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-veracidade-completude-dados/58-CONTEXT.md
@.planning/phases/58-veracidade-completude-dados/58-AUDIT.md
@supabase/functions/nexo-chat/tools.ts
@supabase/functions/nexo-chat/tools.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: get_inventory — status=active default, agregado, variações, rótulo Full e synced_at</name>
  <files>supabase/functions/nexo-chat/tools.ts</files>
  <read_first>
    - supabase/functions/nexo-chat/tools.ts (case "get_inventory" + declaration de get_inventory + helper cap/MAX_ROWS)
    - supabase/functions/ml-inventory/index.ts linhas ~146-190 (shape real de variations: cada var tem variation_id, available_quantity, size/cor em attributes; campos has_variations, logistic_type no cache)
    - 58-AUDIT.md seção 1 (EST-1..6) e 58-CONTEXT D1/D2/D3
  </read_first>
  <action>
Reescrever o case "get_inventory" no dispatcher e sua declaration (per D1/D2/D3, VERAC-01/02/04):

1. Declaration: aceitar, além de `search`, um parâmetro opcional `status` (enum textual "active"|"paused"|"all", default "active"). NÃO adicionar param de org/seller. Descrição deve deixar inequívoco: "Estoque FULL (fulfillment) por anúncio — NÃO é estoque total da empresa; não há saldo de CD/Tiny nesta base. Por padrão lista anúncios ATIVOS. Retorna um resumo agregado + amostra; quando o anúncio tem variações, resume os tamanhos/cores esgotados. Passe search para um produto específico." Deixar claro vendido≠estoque e Full≠total na descrição.

2. Dispatcher: adicionar ao select as colunas `variations`, `has_variations`, `logistic_type`, `synced_at` (além das já lidas). Filtrar status: se args.status === "paused" → .eq("status","paused"); se "all" → sem filtro; default (active ou ausente) → .eq("status","active"). Manter `search` sanitizado já existente. Manter .eq(organization_id) + .in(ml_user_id) (anti-IDOR intacto).

3. Em vez de ordenar só por available_quantity ASC, ordenar de forma que os relevantes (ativos com venda) apareçam: order por available_quantity ASC mas após o filtro de status — agora os campeões ativos entram. Manter cap MAX_ROWS na amostra.

4. Construir um objeto de retorno `{ label, freshness, summary, sample }` em vez de array cru:
   - `label`: "estoque Full (fulfillment) — não inclui CD/Tiny; não é estoque total".
   - `freshness`: o MAIOR synced_at das linhas lidas (ou null), como sinal de recência (VERAC-04).
   - `summary`: agregado calculado em memória sobre o conjunto lido para a org/mlUserIds (rodar uma 2ª query leve sem cap, ou agregar via count): total de itens (na visão filtrada), soma de available_quantity (total unidades Full), nº ativos, nº pausados, nº de itens com available_quantity=0, e nº de itens com has_variations=true que têm ≥1 variação com available_quantity=0. Para o agregado total NÃO aplicar o cap de 50 (é contagem/soma) — usar uma query separada selecionando só as colunas necessárias (status, available_quantity, has_variations, variations) com paginação .range() se passar de 1000 linhas, ou um count + soma. Documentar no código que o agregado é calculado sobre TODO o conjunto Full da org, não só a amostra.
   - `sample`: até MAX_ROWS itens, cada um enriquecido com um campo derivado `variations_out_of_stock` quando has_variations=true: extrair do jsonb `variations` os nomes (size/cor dos attributes) das variações com available_quantity=0 e o texto "X de N tamanhos esgotados". Itens sem variação não recebem esse campo.

Helper de extração de variações esgotadas: função pura no módulo (ex. summarizeVariations(variations: unknown): { total: number; out_of_stock: number; names: string[] }) — defensiva a jsonb null/array vazio. NÃO inline lógica complexa fora de função nomeada.

NÃO mudar nenhuma outra tool neste plano (vendas/ads/financeiro são planos seguintes que tocam o MESMO arquivo em waves posteriores).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && deno check supabase/functions/nexo-chat/tools.ts</automated>
  </verify>
  <acceptance_criteria>
    - get_inventory sem args retorna objeto com {label, freshness, summary, sample}; summary tem totalItems, totalUnits, active, paused, outOfStock, itemsWithSizeOutOfStock
    - Por padrão só anúncios status=active entram (paused/all sob demanda via args.status)
    - sample inclui variations_out_of_stock para itens com has_variations=true
    - label cita "Full (fulfillment)" e nega "total"; freshness = max(synced_at)
    - .eq(organization_id) + .in(ml_user_id) mantidos (anti-IDOR)
  </acceptance_criteria>
  <done>get_inventory devolve estoque Full ativo agregado + por variação + frescura, rotulado, sem afirmar ruptura absoluta. deno check verde.</done>
</task>

<task type="auto">
  <name>Task 2: get_coverage rótulos + testes do novo shape</name>
  <files>supabase/functions/nexo-chat/tools.ts, supabase/functions/nexo-chat/tools.test.ts</files>
  <read_first>
    - supabase/functions/nexo-chat/tools.ts (declaration + case get_coverage; get_consultor_coverage usa janela /30 fixo)
    - supabase/functions/nexo-chat/tools.test.ts (estrutura do stub encadeável; precisa suportar o novo objeto de retorno)
    - 58-AUDIT EST-4/EST-5; 58-CONTEXT D3
  </read_first>
  <action>
1. get_coverage: atualizar a DESCRIPTION da declaration (per D3/EST-4/EST-5) para: "Cobertura de estoque em dias e venda média diária por SKU, calculada sobre os últimos 30 dias (janela fixa). Ruptura aqui é ruptura no FULL — não no estoque total. sold_quantity de outras tools é total histórico do anúncio, não venda do período." Não alterar a RPC (compartilhada/ok); só o rótulo na tool e, se a RPC retornar sold_quantity, anexar nota no resultado de que é janela 30d fixa. Se for trivial anexar um campo `window: "30d-fixed"` ao retorno do dispatcher, fazer; caso contrário deixar só na descrição.

2. tools.test.ts: ajustar/expandir os testes para o novo shape de get_inventory:
   - Atualizar o stub Supabase encadeável se necessário (o objeto de retorno mudou de array para {summary, sample}); o teste de anti-IDOR de get_inventory deve continuar verificando .eq('organization_id', orgId) e .in('ml_user_id', mlUserIds).
   - Novo teste: get_inventory sem args aplica filtro status=active (verificar via call.eqs.status === 'active' ou ausência de filtro conforme implementação — adaptar o stub para registrar .eq('status',...)).
   - Novo teste unitário da função summarizeVariations: dado um jsonb com N variações e K com qty 0, retorna {total:N, out_of_stock:K, names:[...]}.
   - Manter o teste "declara as 22 tools" — NÃO mudou a contagem neste plano (get_inventory continua 1 tool). Garantir que o array esperado siga igual.
   Rodar a suíte exigindo execução real (nunca "no test files found").
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run supabase/functions/nexo-chat/tools.test.ts 2>&1 | grep -E "passed|Test Files" && deno check supabase/functions/nexo-chat/tools.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest roda o arquivo (não "no test files found") e todos os testes passam
    - get_coverage description cita "30 dias (janela fixa)" e "ruptura no FULL"
    - teste de summarizeVariations verde; anti-IDOR de get_inventory verde com novo shape
  </acceptance_criteria>
  <done>Cobertura rotulada + bateria de testes do estoque verde provando active-default, agregado, variações e anti-IDOR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modelo→dispatcher | o LLM pode tentar injetar args.org_id/seller_id/ml_user_id/status maliciosos |
| dados ML→prompt | títulos/SKUs/variations em jsonb são informação, nunca instrução |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01-IDOR | Information Disclosure | get_inventory dispatcher | mitigate | orgId/mlUserIds só do servidor; .eq(organization_id)+.in(ml_user_id); args.org_id/seller_id/ml_user_id ignorados (teste cobre) |
| T-58-01-INJ | Tampering | jsonb variations / title | mitigate | summarizeVariations trata variations como dado puro; search já sanitizado (remove %,()*\\) |
| T-58-01-RO | Elevation of Privilege | dispatcher | mitigate | só select(); nenhuma mutação; read-only |
| T-58-01-STATUS | Tampering | args.status do modelo | mitigate | enum allow-list (active/paused/all); valor fora do enum cai no default active |
</threat_model>

<verification>
- deno check supabase/functions/nexo-chat/tools.ts limpo
- npx vitest run supabase/functions/nexo-chat/tools.test.ts → todos verdes, arquivo executado
- Inspeção: get_inventory default não inclui pausados; objeto tem summary+sample+label+freshness
</verification>

<success_criteria>
get_inventory devolve estoque Full ATIVO agregado + por variação + frescura, rotulado "Full (fulfillment)", sem afirmar ruptura absoluta; get_coverage rotulado; testes verdes; anti-IDOR e read-only preservados.
</success_criteria>

<output>
Create `.planning/phases/58-veracidade-completude-dados/58-01-SUMMARY.md` when done
</output>
