---
phase: 58-veracidade-completude-dados
plan: 04
type: execute
wave: 4
depends_on: ["58-03"]
files_modified:
  - supabase/functions/nexo-chat/tools.ts
  - supabase/functions/nexo-chat/tools.test.ts
  - supabase/functions/nexo-chat/loop.ts
  - supabase/functions/nexo-chat/index.ts
autonomous: true
requirements: [VERAC-03, VERAC-05]
must_haves:
  truths:
    - "Existe get_reputation: chama a EF ml-reputation por ml_user_id (mesma fonte do /reputacao: nível/termômetro, claims_rate, cancellation_rate, power_seller)"
    - "Existe get_goals(period_month?): lê ml_targets escopado por seller_id ∈ mlUserIds (ml_targets NÃO tem organization_id → anti-IDOR via seller_id derivado server-side) e cruza meta × realizado"
    - "get_claims não expõe campos mortos (data_limite/solucao 100% null) e distingue status/tipo + contagem aberto×total"
    - "get_health_score ordena por snapshot_month DESC (casa com o dashboard), não created_at"
    - "get_open_questions alinhado a status='UNANSWERED' ou documentado"
  artifacts:
    - path: "supabase/functions/nexo-chat/tools.ts"
      provides: "get_reputation + get_goals novas; get_claims/get_health_score/get_open_questions corrigidas"
      contains: "get_reputation"
    - path: "supabase/functions/nexo-chat/tools.test.ts"
      provides: "testes anti-IDOR de get_goals (seller_id ∈ mlUserIds, sem org_id) + get_reputation (só mlUserIds do servidor); contagem de tools = 25"
  key_links:
    - from: "supabase/functions/nexo-chat/tools.ts (get_goals)"
      to: "ml_targets"
      via: ".in('seller_id', mlUserIds) — NUNCA org/seller do modelo; ml_targets não tem organization_id"
      pattern: "ml_targets"
    - from: "supabase/functions/nexo-chat/tools.ts (get_reputation)"
      to: "EF ml-reputation"
      via: "invoca ml-reputation por ml_user_id ∈ mlUserIds (server-side)"
      pattern: "ml-reputation"
  prohibitions:
    - "get_goals: seller_id SEMPRE de mlUserIds do servidor; ml_targets sem organization_id NÃO pode receber seller vindo do modelo (IDOR adaptado)"
    - "Read-only: get_reputation invoca a EF read-only; nenhuma mutação"
    - "Nenhuma tool retorna métrica zerada/vazia como verdade — declarar limitação"
---

<objective>
Fechar os GAPs do domínio OPERACIONAL & SAÚDE (OPS-1..5 do 58-AUDIT). OPS-1: reputação não tem tool (dashboard /reputacao busca ao vivo da EF ml-reputation). OPS-2: metas existem em ml_targets (jun R$583.000) mas sem tool; atenção: ml_targets é por user_id+seller_id, SEM organization_id → anti-IDOR adaptado. OPS-3: get_claims expõe data_limite/solucao 100% null. OPS-4: get_health_score ordena por created_at (dashboard usa snapshot_month). OPS-5: get_open_questions cosmético.

Purpose: o Nexo responde "qual meu termômetro/estou Verde?" e "como está minha meta do mês?" com a fonte-da-verdade, sem campos mortos e sem confundir ordenação. Cobre VERAC-03 (reconciliação) e VERAC-05 (declarar limitação) no domínio operacional/saúde.

Output: novas tools get_reputation e get_goals (anti-IDOR adaptado para ml_targets); get_claims/get_health_score/get_open_questions corrigidas; testes anti-IDOR.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-veracidade-completude-dados/58-CONTEXT.md
@.planning/phases/58-veracidade-completude-dados/58-AUDIT.md
@supabase/functions/nexo-chat/tools.ts
@supabase/functions/nexo-chat/tools.test.ts
@supabase/functions/ml-reputation/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Novas tools get_reputation + get_goals (anti-IDOR adaptado)</name>
  <files>supabase/functions/nexo-chat/tools.ts, supabase/functions/nexo-chat/loop.ts, supabase/functions/nexo-chat/index.ts</files>
  <read_first>
    - supabase/functions/ml-reputation/index.ts (contrato: recebe ?ml_user_id=, valida is_org_member via token da org do ml_user_id, retorna {seller_reputation:{level_id,...}, power_seller_status, nickname}); aceita Authorization Bearer JWT
    - supabase/functions/nexo-chat/index.ts (como mlUserIds é resolvido server-side de ml_tokens; sb é service_role)
    - supabase/functions/nexo-chat/tools.ts (padrão do dispatcher; helpers today/clampMonth; case get_sales_kpis para o "realizado" via get_kpi_summary)
    - 58-AUDIT OPS-1/OPS-2; 58-CONTEXT D12/D13
  </read_first>
  <action>
Adicionar 2 tools novas (declarations + cases), per D12/D13 (VERAC-03/05):

1. get_reputation (OPS-1):
   - Declaration: sem params (ou param vazio). Descrição: "Reputação do ML ao vivo (mesma fonte do /reputacao): nível/termômetro (level_id), claims_rate, cancellation_rate, delayed_handling, power_seller_status. Read-only." SEM param de org/seller.
   - Dispatcher: para cada ml_user_id em mlUserIds (server-side), invocar a EF ml-reputation. Como o dispatcher tem o sb service_role mas a EF ml-reputation exige Bearer JWT + valida is_org_member, a forma robusta é: chamar a EF via fetch para `${SUPABASE_URL}/functions/v1/ml-reputation?ml_user_id=<id>` com Authorization usando a SERVICE_ROLE key (a EF faz getUser — service_role NÃO é um user → 401). PORTANTO: passar o dispatcher a precisar do JWT do usuário NÃO é desejável (acopla). Em vez disso, dispatchTool deve receber, além de sb/orgId/mlUserIds, um parâmetro opcional já disponível no index (o token/JWT do request) OU o index resolve reputação e injeta. Decisão de implementação: estender a assinatura de dispatchTool com um campo de contexto `ctx` (ex. { userJwt?: string }) preenchido pelo index.ts a partir do Authorization do request, e usá-lo só em get_reputation para invocar a EF com o JWT real do usuário (mantém o is_org_member da EF como segunda barreira anti-IDOR). Atualizar loop.ts para repassar ctx ao dispatch (a assinatura de dispatchImpl muda). Documentar que ctx.userJwt é só para chamar EFs que exigem JWT — nunca exposto ao modelo.
   - Resultado: array por loja {ml_user_id, level_id, claims_rate, cancellation_rate, power_seller_status, nickname} + label "reputação ao vivo do ML". Se a EF falhar para uma loja, incluir {ml_user_id, error} em vez de inventar — declarar limitação (VERAC-05).
   - ALTERNATIVA mais simples se a mudança de assinatura for grande: se existir uma tabela persistida de reputação, ler dela com .eq(organization_id). Verificar no read_first; o 58-AUDIT diz que NÃO há fonte persistida → a invocação da EF com JWT é o caminho. Escolher o caminho e documentar no SUMMARY.

2. get_goals (OPS-2/D13) — anti-IDOR adaptado:
   - Declaration: param opcional period_month (YYYY-MM, default mês corrente). Descrição: "Meta do mês × realizado: meta de receita/lucro (ml_targets) vs realizado (get_kpi_summary) → % atingido. Metas são por loja." SEM param de org/seller.
   - Dispatcher: ml_targets NÃO tem organization_id. Anti-IDOR: filtrar SOMENTE por .in('seller_id', mlUserIds) — onde mlUserIds vem do servidor (derivado de ml_tokens WHERE organization_id). NUNCA aceitar seller do modelo. Ler ml_targets do period_month; cruzar com o realizado chamando get_kpi_summary(orgId, mlUserIds, mês-01, mês-fim) (ou reaproveitar a lógica de get_sales_kpis). Retornar {period_month, by_seller:[{seller_id, meta_receita, meta_lucro_pct, realizado_receita, pct_atingido}], label}. Se ml_targets vier vazio para o mês → declarar "sem meta cadastrada para <mês>" (VERAC-05), não inventar.
   - clampMonth já existe para validar period_month.

Adicionar ambas a TOOL_DECLARATIONS. Contagem de tools sobe de 23 para 25.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && deno check supabase/functions/nexo-chat/tools.ts && deno check supabase/functions/nexo-chat/loop.ts && deno check supabase/functions/nexo-chat/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - get_reputation e get_goals existem em TOOL_DECLARATIONS e no switch
    - get_goals filtra ml_targets SOMENTE por .in('seller_id', mlUserIds); nunca seller do modelo
    - get_reputation invoca ml-reputation por ml_user_id do servidor (ctx.userJwt repassado pelo index, nunca pelo modelo); falha vira {ml_user_id,error}, não número inventado
    - se a assinatura de dispatchTool mudou, loop.ts e index.ts repassam ctx e deno check passa nos três
  </acceptance_criteria>
  <done>Reputação e metas viram tools com a fonte-da-verdade e anti-IDOR adaptado; deno check verde nos 3 arquivos.</done>
</task>

<task type="auto">
  <name>Task 2: Limpar get_claims / get_health_score / get_open_questions + testes anti-IDOR das novas tools</name>
  <files>supabase/functions/nexo-chat/tools.ts, supabase/functions/nexo-chat/tools.test.ts</files>
  <read_first>
    - supabase/functions/nexo-chat/tools.ts (cases get_claims, get_health_score, get_open_questions)
    - supabase/functions/nexo-chat/tools.test.ts (stub; asserção de contagem; precisa suportar nova assinatura ctx se mudou)
    - 58-AUDIT OPS-3/OPS-4/OPS-5; 58-CONTEXT D14
  </read_first>
  <action>
Per D14 (VERAC-03/05):

1. get_claims (OPS-3): remover data_limite e solucao do select e do retorno (100% null — campos mortos que prometem "prazos" inexistentes). Adicionar ao retorno uma contagem agregada: total de claims e nº abertos (status='opened'), e a distribuição por tipo (mediations/returns/change/cancel_purchase). Atualizar a description: "Reclamações/devoluções/mediações: status e tipo + contagem aberto×total. NÃO há prazo/solução nesta base." Manter .eq(organization_id)+.in(ml_user_id).

2. get_health_score (OPS-4): trocar a ordenação de created_at para snapshot_month DESC (casar com o dashboard) — selecionar snapshot_month e ordenar por ele. Se a coluna snapshot_month existir no select; senão verificar o nome real no read_first. Opcional: anexar scoreDelta vs o snapshot anterior se trivial. Manter .eq(organization_id).

3. get_open_questions (OPS-5, cosmético): manter o filtro resposta IS NULL (coincide com UNANSWERED nesta base) mas atualizar a description para dizer "perguntas sem resposta (status UNANSWERED)". Se a coluna status existir, opcionalmente trocar para .eq('status','UNANSWERED') — só se não regredir (validar que bate com IS NULL). Documentar a escolha.

4. tools.test.ts:
   - Atualizar a asserção de contagem para 25 tools incluindo "get_reputation" e "get_goals".
   - Novo teste anti-IDOR get_goals: passa EVIL_ARGS (seller_id alheio) e verifica que o filtro usa .in('seller_id', mlUserIds) do SERVIDOR e NÃO o seller dos args; nunca consulta org alheia.
   - Novo teste get_reputation: verifica que só os mlUserIds do servidor são usados (não args.ml_user_id); se usar ctx.userJwt, o stub do dispatch deve aceitar a nova assinatura.
   - Ajustar TODAS as chamadas de dispatchTool nos testes existentes se a assinatura ganhou ctx (passar um ctx vazio/stub) — garantir que a suíte inteira continua compilando e passando.
   - Rodar exigindo execução real (nunca "no test files found").
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run supabase/functions/nexo-chat/tools.test.ts 2>&1 | grep -E "passed|Test Files" && deno check supabase/functions/nexo-chat/tools.ts</automated>
  </verify>
  <acceptance_criteria>
    - get_claims sem data_limite/solucao; retorna status/tipo + contagem aberto×total
    - get_health_score ordena por snapshot_month DESC
    - get_open_questions description cita UNANSWERED
    - asserção de tools = 25 com get_reputation+get_goals; suíte roda (não "no test files found") e passa
    - testes anti-IDOR de get_goals (seller_id ∈ mlUserIds) e get_reputation verdes
  </acceptance_criteria>
  <done>Operacional/saúde sem campos mortos, ordenação correta; reputação/metas com anti-IDOR provado; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modelo→dispatcher | LLM pode injetar seller_id/ml_user_id/period_month maliciosos |
| dispatcher→EF ml-reputation | invocação cross-EF precisa de JWT real do usuário (não service_role como user) |
| ml_targets sem organization_id | scope só por seller_id — risco de IDOR se seller vier do modelo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-GOALS-IDOR | Information Disclosure | get_goals (ml_targets sem org_id) | mitigate | filtro SOMENTE .in('seller_id', mlUserIds) do servidor; seller do modelo ignorado; teste anti-IDOR dedicado |
| T-58-04-REP-IDOR | Information Disclosure | get_reputation → ml-reputation | mitigate | ml_user_id só do servidor; ctx.userJwt do request real → EF revalida is_org_member (2ª barreira); ctx nunca exposto ao modelo |
| T-58-04-RO | Elevation of Privilege | dispatcher / EF call | mitigate | get_reputation invoca EF read-only; nenhuma mutação |
| T-58-04-LIMIT | Repudiation (dado falso) | get_goals/get_reputation vazios | mitigate | declara "sem meta cadastrada"/{error} em vez de inventar número (VERAC-05) |
</threat_model>

<verification>
- deno check limpo em tools.ts (+ loop.ts/index.ts se a assinatura ctx mudou)
- vitest roda o arquivo e passa; anti-IDOR de get_goals e get_reputation verdes
- get_claims sem campos mortos; get_health_score por snapshot_month
</verification>

<success_criteria>
Reputação e metas viram tools fiéis à fonte-da-verdade com anti-IDOR adaptado (ml_targets por seller_id, ml-reputation via JWT real); claims/health/questions limpos; declara limitação quando vazio; testes verdes.
</success_criteria>

<output>
Create `.planning/phases/58-veracidade-completude-dados/58-04-SUMMARY.md` when done
</output>