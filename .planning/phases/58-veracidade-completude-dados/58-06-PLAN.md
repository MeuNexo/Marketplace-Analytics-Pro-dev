---
phase: 58-veracidade-completude-dados
plan: 06
type: execute
wave: 6
depends_on: ["58-01", "58-02", "58-03", "58-04", "58-05"]
files_modified:
  - .planning/phases/58-veracidade-completude-dados/58-VERIFICATION.md
autonomous: false
requirements: [VERAC-07]
must_haves:
  truths:
    - "A EF nexo-chat é deployada em prod (ckcdevcxgvueywivefgx) pelo orquestrador (executor não tem SUPABASE_ACCESS_TOKEN)"
    - "A migration de cron de re-sync de ml_billing_daily é aplicada via MCP pelo orquestrador"
    - "A bateria de auditoria por domínio (VERAC-07) é RE-RODADA comparando cada tool vs a fonte-da-verdade com SQL real da Pé Vermeio (org 7f615df7-…) e cada domínio bate"
    - "Wesley valida no preview e aprova (aprovação humana obrigatória)"
  artifacts:
    - path: ".planning/phases/58-veracidade-completude-dados/58-VERIFICATION.md"
      provides: "registro do re-teste por domínio (tool vs fonte-da-verdade) com veredito por domínio"
      contains: "VERAC-07"
  key_links:
    - from: "tools deployadas (nexo-chat)"
      to: "fonte-da-verdade do dashboard (ml_daily_cache, ml_billing_daily, ml_inventory_cache, ml_ads_daily_cache, ml_targets, ml-reputation, RPCs de margem/caixa)"
      via: "comparação SQL real por domínio na org Pé Vermeio"
      pattern: "VERAC-07"
  prohibitions:
    - "Auto-aprovação proibida: checkpoint blocking-human (Wesley aprova explicitamente)"
    - "Deploy de EF e apply de migration são do ORQUESTRADOR (MCP/CLI com token), nunca do executor"
    - "Read-only mantido em todas as tools; anti-IDOR confirmado no re-teste"
---

<objective>
Fechar a Phase 58 (VERAC-07): deployar a EF nexo-chat com todas as correções dos planos 58-01..05, aplicar a migration de cron de re-sync do ml_billing_daily, e RE-RODAR a bateria de auditoria por domínio — comparando cada tool do Nexo com a fonte-da-verdade do dashboard usando SQL real da Pé Vermeio — registrando que cada domínio bate. Validação final humana de Wesley no preview.

Purpose: o critério de aceite da fase é a bateria por domínio batendo com a fonte-da-verdade (mesmo método do 58-AUDIT, agora pós-fix). Sem este re-teste a fase não está fechada.

Output: deploy + migration aplicados; 58-VERIFICATION.md com o re-teste por domínio e o veredito; aprovação de Wesley.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-veracidade-completude-dados/58-CONTEXT.md
@.planning/phases/58-veracidade-completude-dados/58-AUDIT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Deploy EF nexo-chat + aplicar migration de cron (ORQUESTRADOR)</name>
  <action>Checkpoint do orquestrador: aplicar a migration de cron via MCP apply_migration em ckcdevcxgvueywivefgx, deployar a EF nexo-chat via CLI com SUPABASE_ACCESS_TOKEN (verify_jwt=true, ACTIVE), confirmar o cron agendado e ml_billing_daily fresco, e publicar o preview Vercel. Executor NÃO executa (sem token).</action>
  <what-built>
    Os planos 58-01..05 alteraram supabase/functions/nexo-chat/tools.ts, prompt.ts, tools.test.ts e criaram a migration 20260655000000_phase58_cron_billing_daily_resync.sql. Tudo compila (deno check) e os testes passam — mas NADA está em prod até o deploy.
  </what-built>
  <how-to-verify>
    O ORQUESTRADOR executa (executor não tem SUPABASE_ACCESS_TOKEN / Supabase MCP de deploy):
    1. Aplicar a migration de cron via MCP apply_migration no projeto ckcdevcxgvueywivefgx (NUNCA db push; CLI local está linkado no projeto errado).
    2. Deployar a EF nexo-chat via CLI com o SUPABASE_ACCESS_TOKEN do Wesley (igual ao 57-04): verificar ACTIVE + verify_jwt=true. Smoke: 401 sem auth; 200 OPTIONS.
    3. Confirmar que o cron job aparece em cron.job e que uma execução manual do sync-ml-billing modo daily atualiza ml_billing_daily (coverage até ontem/hoje).
    4. Publicar o preview Vercel (branch da fase) para Wesley logar.
  </how-to-verify>
  <resume-signal>Orquestrador confirma: EF nexo-chat ACTIVE, migration aplicada, cron agendado, ml_billing_daily fresco, preview no ar.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Re-rodar bateria por domínio (VERAC-07) + aprovação de Wesley</name>
  <action>Checkpoint humano: o orquestrador re-roda a auditoria por domínio (mesmo método do 58-AUDIT) na org Pé Vermeio via SQL real, escreve 58-VERIFICATION.md com veredito por domínio, e Wesley valida no preview e aprova. Aprovação humana obrigatória (blocking).</action>
  <what-built>
    EF deployada com tools corrigidas (estoque Full agregado+variações+frescura; ads_campaigns neutralizada + get_ads_account_summary; faturamento/status/ads rotulados; DRE mês-calendário; cashflow saldo_hoje; get_reputation + get_goals; claims/health/questions limpos; prompt de veracidade/frescura).
  </what-built>
  <how-to-verify>
    O ORQUESTRADOR re-roda a auditoria por domínio (mesmo método do 58-AUDIT) na org Pé Vermeio (7f615df7-…, loja 1639558873, projeto ckcdevcxgvueywivefgx), comparando CADA tool com a fonte-da-verdade via SQL real, e escreve o resultado em 58-VERIFICATION.md. Domínios obrigatórios (cada um deve BATER):
    1. Estoque/cobertura: get_inventory default = ativos (não pausados); total Full ativo bate com soma de ml_inventory_cache (status=active); variações esgotadas conferidas num item com variação (ex. Coturno MLB4265421967 tamanho 38=0); rótulo "Full"; freshness presente.
    2. Vendas/faturamento: get_sales_kpis rotulado vs ml_daily_cache (~296k) e pedidos pagos (~286k) — divergência explicada, não silenciosa.
    3. Ads: get_ads_account_summary (gasto/ROAS/attributed) bate com ml_ads_daily_cache do período; get_ads_campaigns NÃO retorna zero como verdade (só nome/status+note); get_ads_by_product rotulado top-50.
    4. Margem ×4 (produto/marca/UF/dia): RPCs batem com o painel (sem regressão); status partially_refunded documentado.
    5. DRE/custos: get_dre_monthly (ml_billing_daily mês-calendário) bate com o card DRE do painel; coverage_until reflete o cron; get_costs_by_month descrito como fluxo de caixa.
    6. Caixa/tesouraria: get_cashflow saldo_hoje bate com Tesouraria; horizonte rotulado.
    7. Perguntas/devoluções: get_open_questions (UNANSWERED) e get_claims (sem campos mortos, contagem aberto×total) batem.
    8. Reputação: get_reputation bate com /reputacao (nível/termômetro) via ml-reputation.
    9. Metas: get_goals (ml_targets, jun R$583.000) cruza meta×realizado; anti-IDOR por seller_id confirmado.
    10. Score: get_health_score por snapshot_month bate com o dashboard.
    Anti-IDOR e read-only reconfirmados (nenhuma tool aceita org/seller do modelo; nenhuma mutação).
    Por fim: Wesley conversa com o Nexo no preview testando as perguntas que antes davam fato errado (estoque/ruptura, faturamento, DRE, reputação, meta) e aprova.
  </how-to-verify>
  <resume-signal>Wesley digita "aprovado" (ou descreve divergências a corrigir). 58-VERIFICATION.md registra cada domínio com veredito BATE/AJUSTAR.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy/migration | só o orquestrador com token aplica em prod |
| modelo→tools (live) | re-teste confirma anti-IDOR end-to-end na org real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06-IDOR | Information Disclosure | tools deployadas | mitigate | re-teste por domínio reconfirma org/seller só do servidor (incl. get_goals por seller_id e get_reputation via JWT) |
| T-58-06-FALSE | Repudiation (dado falso) | bateria VERAC-07 | mitigate | cada domínio comparado com SQL real da fonte-da-verdade; divergência = AJUSTAR antes de fechar |
| T-58-06-RO | Elevation of Privilege | EF live | mitigate | read-only reconfirmado; nenhuma mutação ML; ações continuam na Phase 54 com aprovação |
| T-58-06-HUMAN | Tampering | aprovação | mitigate | blocking-human: Wesley aprova explicitamente; auto-advance ignorado |
</threat_model>

<verification>
- EF nexo-chat ACTIVE + verify_jwt=true; migration de cron aplicada; ml_billing_daily fresco
- 58-VERIFICATION.md com os 10 domínios e veredito por domínio (todos BATE)
- Aprovação explícita de Wesley registrada
</verification>

<success_criteria>
Phase 58 fechada: tools/prompt em prod, cron de billing aplicado, bateria por domínio batendo com a fonte-da-verdade (VERAC-07), anti-IDOR e read-only confirmados, Wesley aprovou.
</success_criteria>

<output>
Create `.planning/phases/58-veracidade-completude-dados/58-VERIFICATION.md` (re-teste por domínio) e `.planning/phases/58-veracidade-completude-dados/58-06-SUMMARY.md` when done
</output>
