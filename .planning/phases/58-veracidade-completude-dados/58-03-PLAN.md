---
phase: 58-veracidade-completude-dados
plan: 03
type: execute
wave: 3
depends_on: ["58-02"]
files_modified:
  - supabase/functions/nexo-chat/tools.ts
  - supabase/functions/nexo-chat/tools.test.ts
  - supabase/migrations/20260655000000_phase58_cron_billing_daily_resync.sql
autonomous: true
requirements: [VERAC-03, VERAC-04, VERAC-06]
must_haves:
  truths:
    - "get_dre_monthly alinha ao card DRE do dashboard: agrega ml_billing_daily por mês-calendário (mesma lógica do useMLBillingDaily), não o ciclo de fatura 06→05"
    - "get_costs_by_month tem descrição correta: 'saídas de caixa por categoria/mês (pagas + a pagar)' — NÃO é CMV nem fatura ML"
    - "get_cashflow expõe saldo de HOJE como campo nomeado e cita o horizonte; não projeta saldo além da cobertura real de inflows sem rótulo"
    - "Existe migration de cron diário que re-sincroniza ml_billing_daily do mês corrente (corrige defasagem FIN-2)"
    - "get_dre_monthly expõe frescura (synced_at/cobertura até que dia) para o Nexo sinalizar defasagem"
  artifacts:
    - path: "supabase/functions/nexo-chat/tools.ts"
      provides: "get_dre_monthly via ml_billing_daily mês-calendário + saldo_hoje no cashflow + descrição costs corrigida"
      contains: "ml_billing_daily"
    - path: "supabase/migrations/20260655000000_phase58_cron_billing_daily_resync.sql"
      provides: "pg_cron diário re-sync ml_billing_daily mês corrente (aplicado pelo orquestrador via MCP no checkpoint 58-06)"
      contains: "cron.schedule"
  key_links:
    - from: "supabase/functions/nexo-chat/tools.ts (get_dre_monthly)"
      to: "ml_billing_daily"
      via: "agrega por mês-calendário .eq(organization_id)+.in(ml_user_id)+.gte/.lte(data, mês 01–fim)"
      pattern: "ml_billing_daily"
  prohibitions:
    - "NÃO aplicar a migration de cron pela mão do executor — é checkpoint do orquestrador (MCP). O executor só ESCREVE o arquivo."
    - "Anti-IDOR: org/seller só do servidor"
    - "Read-only no dispatcher (só select/rpc); a migration de cron é a única mudança de banco e é aplicada no checkpoint"
---

<objective>
Corrigir o domínio FINANCEIRO do Nexo (FIN-1..5 do 58-AUDIT). FIN-1: `get_dre_monthly` lê `ml_billing_monthly` (ciclo 06→05, jun R$43.869) ≠ card DRE do dashboard que usa `ml_billing_daily` mês-calendário (R$34.853). FIN-2: `ml_billing_daily` está DEFASADA (parou 06-12, sem cron). FIN-3: `get_cashflow` +90d projeta -734k vs Tesouraria 30d -182k (horizonte/cobertura). FIN-4: `get_costs_by_month` descrito como DRE/fatura mas é fluxo de caixa por categoria. FIN-5: `get_dre_monthly` chamado "DRE" mas só traz fatura ML.

Purpose: o Nexo bate com o dashboard no DRE/billing (mesma fonte/escopo), não confunde fluxo-de-caixa com fatura ML, e não projeta saldo além da cobertura real sem rótulo. Cobre VERAC-03 (reconciliação), VERAC-04 (frescura/cron) e VERAC-06 (descrições) no domínio financeiro.

Output: `get_dre_monthly` agregando `ml_billing_daily` por mês-calendário; `get_cashflow` com saldo_hoje + horizonte rotulado; descrição de `get_costs_by_month` corrigida; migration de cron diário de re-sync do `ml_billing_daily`; testes.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-veracidade-completude-dados/58-CONTEXT.md
@.planning/phases/58-veracidade-completude-dados/58-AUDIT.md
@supabase/functions/nexo-chat/tools.ts
@supabase/functions/nexo-chat/tools.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: get_dre_monthly por mês-calendário (ml_billing_daily) + descrições financeiras + cashflow saldo_hoje</name>
  <files>supabase/functions/nexo-chat/tools.ts</files>
  <read_first>
    - supabase/functions/nexo-chat/tools.ts (case get_dre_monthly→ml_billing_monthly; case get_cashflow→get_cashflow RPC; case get_costs_by_month; declarations correspondentes)
    - src/hooks/useMLBilling.ts (useMLBillingDaily: como agrega ml_billing_daily por mês-calendário 01–fim; colunas data/tipo/amount/synced_at)
    - 58-AUDIT FIN-1/FIN-3/FIN-4/FIN-5; 58-CONTEXT D8/D10/D11
  </read_first>
  <action>
Per D8/D10/D11 (VERAC-03/04/06):

1. get_dre_monthly (FIN-1/FIN-5): trocar a fonte de ml_billing_monthly para ml_billing_daily, agregando por MÊS-CALENDÁRIO igual ao useMLBillingDaily do dashboard. Dispatcher: dado period_month (YYYY-MM, default mês corrente via clampMonth existente), calcular o 1º e o último dia do mês; ler ml_billing_daily com .eq(organization_id)+.in(ml_user_id)+.gte(data, mês-01)+.lte(data, mês-fim), paginar .range() se >1000 linhas; agregar por tipo (CFFE/CFONPN/PADS/total) somando amount. Retornar {period_month, label:"fatura/tarifas ML do mês-calendário (01–fim), espelho do card DRE do painel — NÃO é DRE completo (sem CMV/impostos/lucro); para lucro use get_margin_summary", by_type:{...}, total, coverage_until: max(data lida), freshness: max(synced_at)}. coverage_until permite ao Nexo sinalizar defasagem (VERAC-04). Atualizar a description da declaration: "Fatura/tarifas do ML por MÊS-CALENDÁRIO (espelha o card DRE do painel). Não é DRE completo: só tarifas ML (CFFE/CFONPN/PADS), sem CMV/impostos/lucro."

2. get_costs_by_month (FIN-4): NÃO mudar a RPC (get_cost_by_month serve fluxo de caixa por categoria). Corrigir só a DESCRIPTION da declaration: "Saídas de caixa por categoria e por mês (Fornecedores, Salários, Empréstimo, etc., pagas + a pagar) — NÃO é CMV nem a fatura do ML. Para fatura ML use get_dre_monthly; para CMV/lucro use get_margin_summary."

3. get_cashflow (FIN-3/D10): manter a projeção futura (hoje→+90d default) mas:
   - Anexar ao retorno um campo nomeado `saldo_hoje` (o saldo do dia de hoje na série) e `horizon_label` explicando o horizonte e que o saldo após a última data com inflow real é projeção parcial (outflows vão até 2030, inflows só ~+27d → o tombo de 90d é em parte artefato). Se a série já traz saldo acumulado por dia, extrair o de hoje; senão expor o primeiro ponto.
   - Atualizar a description: "Projeção FUTURA de caixa (default hoje→+90d). saldo_hoje é o saldo atual. Atenção: inflows projetados existem só até ~+27d; saldo muito à frente é projeção parcial (sobrestima saída). Para o painel de Tesouraria use horizonte 30d (get_treasury_panel)."
   NÃO alterar a RPC get_cashflow (compartilhada com Tesouraria) — só enriquecer/rotular o retorno da tool e a descrição.

Não tocar outras tools além destas três.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && deno check supabase/functions/nexo-chat/tools.ts</automated>
  </verify>
  <acceptance_criteria>
    - get_dre_monthly lê ml_billing_daily agregando mês-calendário com .eq(org)+.in(ml_user_id); retorna by_type+total+coverage_until+freshness+label
    - get_costs_by_month description diz "saídas de caixa por categoria — não é CMV nem fatura ML"
    - get_cashflow retorna saldo_hoje + horizon_label; descrição rotula a cobertura parcial dos inflows
  </acceptance_criteria>
  <done>DRE do Nexo bate com o card do painel (mês-calendário); custos/cashflow rotulados sem confundir CMV/fatura/projeção. deno check verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migration de cron diário re-sync ml_billing_daily + testes do dispatcher financeiro</name>
  <files>supabase/migrations/20260655000000_phase58_cron_billing_daily_resync.sql, supabase/functions/nexo-chat/tools.test.ts</files>
  <read_first>
    - supabase/migrations/ (padrão de migrations cron já existentes — ex. treasury enqueue/tick crons; como invocar EF via net.http_post com token; STATE.md menciona EF sync-ml-billing v8 modo daily)
    - supabase/functions/sync-ml-billing/index.ts (modo daily: como re-sincroniza ml_billing_daily do mês corrente — payload esperado)
    - 58-AUDIT FIN-2; 58-CONTEXT D9
    - supabase/functions/nexo-chat/tools.test.ts (stub; asserções a ajustar para o novo shape de get_dre_monthly)
  </read_first>
  <action>
Per D9 (VERAC-04) e VERAC-03:

1. Criar a migration supabase/migrations/20260655000000_phase58_cron_billing_daily_resync.sql:
   - Agendar via cron.schedule um job DIÁRIO (ex. 06:40 UTC) que re-dispara a EF sync-ml-billing no modo daily para o MÊS CORRENTE de cada org/seller ativo — espelhando o padrão de invocação on-demand já usado (net.http_post para a URL da EF com Authorization service_role / token, payload modo daily + período mês corrente). Reutilizar o mecanismo de auth de cron já presente nas migrations existentes (sb_secret/service_role via get_app_secret ou vault — seguir o padrão do projeto, NÃO inventar). Idempotente: usar cron.unschedule do job homônimo antes de reagendar (guard se não existir).
   - Comentar no SQL: objetivo (FIN-2 — ml_billing_daily defasado), frequência, e que mês-calendário é a fonte do card DRE do painel e do get_dre_monthly do Nexo.
   - NÃO aplicar a migration aqui. O executor só escreve o arquivo; a aplicação via MCP é parte do checkpoint 58-06 (orquestrador). Anotar isso num comentário de cabeçalho.

2. tools.test.ts: ajustar o teste de get_dre_monthly:
   - A fonte mudou de ml_billing_monthly para ml_billing_daily → o teste de anti-IDOR de get_dre_monthly deve verificar .eq('organization_id', orgId) + .in('ml_user_id', mlUserIds) na tabela ml_billing_daily (atualizar o nome da tabela esperada no stub).
   - Manter a contagem de tools = 23 (este plano não cria tool nova).
   - Se get_cashflow ganhou shape de objeto com saldo_hoje, garantir que o teste existente (se houver) continue válido; do contrário não adicionar teste de saldo_hoje (depende de série real) — focar no anti-IDOR de get_dre_monthly via ml_billing_daily.
   - Rodar exigindo execução real (nunca "no test files found").
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run supabase/functions/nexo-chat/tools.test.ts 2>&1 | grep -E "passed|Test Files" && deno check supabase/functions/nexo-chat/tools.ts && test -f supabase/migrations/20260655000000_phase58_cron_billing_daily_resync.sql && grep -q "cron.schedule" supabase/migrations/20260655000000_phase58_cron_billing_daily_resync.sql && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - migration existe e contém cron.schedule re-sincronizando ml_billing_daily do mês corrente (idempotente)
    - migration NÃO é aplicada pelo executor (cabeçalho documenta que aplicação é checkpoint 58-06)
    - teste anti-IDOR de get_dre_monthly verde contra ml_billing_daily; suíte roda (não "no test files found")
  </acceptance_criteria>
  <done>Cron de re-sync escrito (a aplicar no checkpoint); DRE do Nexo lê a fonte fresca; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modelo→dispatcher | LLM pode injetar org/seller/period_month maliciosos |
| cron→EF sync-ml-billing | invocação automatizada precisa de auth service_role correta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03-IDOR | Information Disclosure | get_dre_monthly (ml_billing_daily) | mitigate | .eq(organization_id)+.in(ml_user_id); args de org/seller ignorados; clampMonth valida period_month |
| T-58-03-CRON | Spoofing | cron job → EF | mitigate | usa o mesmo mecanismo de auth de cron já presente no projeto (service_role/sb_secret), não credencial nova |
| T-58-03-RO | Elevation of Privilege | dispatcher | mitigate | só select()/rpc(); read-only. Única mudança de banco = migration de cron, aplicada no checkpoint |
</threat_model>

<verification>
- deno check limpo; vitest roda o arquivo e passa
- migration com cron.schedule presente (não aplicada pelo executor)
- get_dre_monthly agrega ml_billing_daily por mês-calendário; cashflow expõe saldo_hoje
</verification>

<success_criteria>
DRE/billing do Nexo bate com o card do painel (ml_billing_daily mês-calendário); custos e cashflow rotulados sem confundir CMV/fatura/projeção; cron de re-sync escrito p/ aplicar no checkpoint; anti-IDOR e read-only preservados; testes verdes.
</success_criteria>

<output>
Create `.planning/phases/58-veracidade-completude-dados/58-03-SUMMARY.md` when done
</output>