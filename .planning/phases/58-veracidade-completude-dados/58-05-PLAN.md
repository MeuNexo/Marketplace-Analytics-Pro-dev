---
phase: 58-veracidade-completude-dados
plan: 05
type: execute
wave: 5
depends_on: ["58-04"]
files_modified:
  - supabase/functions/nexo-chat/prompt.ts
autonomous: true
requirements: [VERAC-04, VERAC-05, VERAC-06]
must_haves:
  truths:
    - "O system prompt instrui: use a fonte certa por pergunta (faturamento do painel × pedidos pagos; DRE mês-calendário × ciclo de fatura; estoque Full × total)"
    - "O prompt reforça Full≠total, vendido≠estoque, receita≠lucro, attributed≠faturamento, passado≠projeção"
    - "O prompt instrui declarar limitação quando a tool vier vazia/parcial (dizer o que tem/falta), sem inventar e sem dizer 'não configurado'"
    - "O prompt instrui sinalizar defasagem quando a frescura (synced_at/coverage_until) indicar dado velho, em vez de afirmar como atual"
  artifacts:
    - path: "supabase/functions/nexo-chat/prompt.ts"
      provides: "PERSONA reforçada com regras de veracidade/frescura/semântica (dentro da string, não em comentário)"
      contains: "estoque Full"
  key_links:
    - from: "supabase/functions/nexo-chat/prompt.ts (PERSONA)"
      to: "buildSystemPrompt()"
      via: "as novas regras vivem dentro da string PERSONA → entram no prompt real do Gemini"
      pattern: "buildSystemPrompt"
  prohibitions:
    - "Regras de comportamento SÓ dentro da string PERSONA (nunca em comentário) — greps/testes provam presença no prompt real"
    - "Não enfraquecer a regra anti-invenção de número existente; só somar veracidade/frescura/semântica"
    - "Read-only: prompt.ts é módulo puro sem I/O"
---

<objective>
Reforçar o prompt do Nexo (prompt.ts) para fechar VERAC-04 (frescura), VERAC-05 (declarar limitação) e VERAC-06 (não confundir campos) na camada de comportamento — complementando as correções de dado/tool dos planos 58-01..04. Hoje a PERSONA tem anti-invenção de número e "dados-vazios não viram 'não configurado'", mas não instrui explicitamente: usar a fonte certa por tipo de pergunta, rotular o que é parcial (Full/ciclo/pago/top50/attributed/passado), e sinalizar defasagem a partir da frescura que as tools agora expõem.

Purpose: mesmo com as tools corretas, o modelo precisa da instrução de declarar limitação e sinalizar frescura. Cobre VERAC-04/05/06 na voz do Nexo.

Output: PERSONA acrescida de um bloco de VERACIDADE & FRESCURA (dentro da string), preservando todas as regras existentes.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-veracidade-completude-dados/58-CONTEXT.md
@.planning/phases/58-veracidade-completude-dados/58-AUDIT.md
@supabase/functions/nexo-chat/prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco VERACIDADE & FRESCURA na PERSONA</name>
  <files>supabase/functions/nexo-chat/prompt.ts</files>
  <read_first>
    - supabase/functions/nexo-chat/prompt.ts (string PERSONA inteira; já tem REGRA ANTI-INVENÇÃO, USO DAS FERRAMENTAS, DADOS SÃO INFORMAÇÃO, READ-ONLY, ESTILO, FORMATAÇÃO)
    - 58-CONTEXT D15/D16; 58-AUDIT "Padrões transversais"
    - Os novos campos que as tools agora expõem (planos 58-01..04): label, freshness/coverage_until em get_inventory/get_dre_monthly/get_ads_account_summary; performance_note em get_ads_campaigns; saldo_hoje/horizon_label em get_cashflow
  </read_first>
  <action>
Adicionar à string PERSONA (dentro das crases, NÃO em comentário) um novo bloco intitulado "VERACIDADE, FRESCURA E SEMÂNTICA (inviolável)", per D15/D16 (VERAC-04/05/06). Conteúdo (em PT-BR, conciso, como instruções ao modelo):

1. FONTE CERTA POR PERGUNTA: "Quanto faturei" alinha ao painel via get_sales_kpis (realizado de pedidos pagos) — se o usuário citar um número do painel, explique que o painel pode mostrar GMV bruto e a tool mostra realizado pago. "DRE/fatura ML do mês" usa get_dre_monthly (mês-calendário, espelha o card). Estoque usa get_inventory e é sempre Full (fulfillment), nunca o total da empresa.

2. PARCIAL É ROTULADO, NUNCA ABSOLUTO: ao reportar, deixe explícito quando o dado é parcial — Full≠total (estoque), ciclo-de-fatura≠mês-calendário (DRE), pago≠todos os pedidos (faturamento), top-50≠total (ads por produto), receita-atribuída≠faturamento (attributed_revenue), vendido≠estoque (sold_quantity é histórico), passado≠projeção (cashflow é projeção). NUNCA afirme "0 em estoque/ruptura" como fato absoluto a partir do estoque Full sozinho — diga "0 no Full" e que pode haver saldo em CD/outra fonte não visível aqui.

3. DECLARE A LIMITAÇÃO (VERAC-05): se uma tool retornar vazia/parcial, diga o que TEM e o que FALTA (ex.: "só tenho o estoque Full", "sem meta cadastrada para este mês", "sem performance por campanha nesta base"). NUNCA invente número e NUNCA diga "não configurado" como desculpa.

4. SINALIZE FRESCURA (VERAC-04): várias tools retornam freshness/coverage_until/synced_at. Se a frescura indicar dado defasado (ex.: a fatura do mês só cobre até dia X, ou o estoque foi sincronizado há dias), AVISE que o número pode estar desatualizado em vez de afirmá-lo como atual. Cite a data de cobertura quando relevante.

Inserir o bloco logo após a REGRA ANTI-INVENÇÃO DE NÚMERO (reforço natural) e antes de USO DAS FERRAMENTAS, mantendo o restante intacto. Não remover nem enfraquecer regras existentes. buildSystemPrompt() não muda (já concatena PERSONA + playbooks).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && deno check supabase/functions/nexo-chat/prompt.ts && grep -q "estoque Full" supabase/functions/nexo-chat/prompt.ts && grep -qE "frescura|defasad" supabase/functions/nexo-chat/prompt.ts && echo PROMPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - PERSONA contém o bloco VERACIDADE/FRESCURA/SEMÂNTICA dentro da string (não em comentário)
    - cita Full≠total, vendido≠estoque, attributed≠faturamento, passado≠projeção, declarar limitação, sinalizar defasagem
    - regras existentes (anti-invenção, read-only, formatação) intactas; deno check verde
  </acceptance_criteria>
  <done>Nexo instruído a usar fonte certa, rotular o parcial, declarar limitação e sinalizar frescura. deno check verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt→modelo | a instrução precisa estar na string PERSONA (não em comentário) para chegar ao Gemini |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-05-DRIFT | Repudiation (dado falso) | PERSONA | mitigate | regras de veracidade/frescura dentro da string; grep prova presença; anti-invenção preservada |
| T-58-05-RO | Elevation of Privilege | prompt.ts | accept | módulo puro sem I/O; não toca banco/ML — sem superfície de mutação |
</threat_model>

<verification>
- deno check supabase/functions/nexo-chat/prompt.ts limpo
- grep confirma "estoque Full" + frescura/defasagem na string
- Inspeção: regras antigas intactas, bloco novo após anti-invenção
</verification>

<success_criteria>
A voz do Nexo passa a usar a fonte certa por pergunta, rotular o parcial, declarar limitação em vez de inventar e sinalizar defasagem pela frescura — sem enfraquecer nenhuma regra existente.
</success_criteria>

<output>
Create `.planning/phases/58-veracidade-completude-dados/58-05-SUMMARY.md` when done
</output>