---
phase: 50-simulador-de-cenarios-de-caixa-e-se
plan: 03
type: execute
wave: 3
depends_on: ["50-01", "50-02"]
files_modified:
  - src/components/financial/CashFlowSimulator.tsx
  - src/pages/mercadolivre/MLFluxoCaixa.tsx
autonomous: false
requirements: [SIM-02, SIM-03]
must_haves:
  truths:
    - "A página /fluxo-de-caixa tem Tabs shadcn 'Caixa Real' | 'Simulador'"
    - "A aba 'Caixa Real' mostra exatamente o conteúdo atual da página (cards + CashFlowChart) — intocada"
    - "A aba 'Simulador' tem slider de recebimento extra/dia (−5000..+5000, step 100), slider de gasto extra/dia (0..+10000, step 100), até 2 eventos pontuais (valor/data/tipo) e botão Limpar"
    - "Arrastar sliders ou adicionar eventos atualiza o gráfico (3ª linha) e o veredito em tempo real"
    - "Recarregar a página volta ao real (estado só de sessão — useState, sem persistência)"
    - "Botão '+ Adicionar evento' some ao atingir 2 eventos; lixeira remove evento; date picker limita data a [hoje, hoje+120]"
  artifacts:
    - path: "src/components/financial/CashFlowSimulator.tsx"
      provides: "Aba Simulador: estado de sessão + controles + integra simulateCashflow/chart/verdict"
      min_lines: 90
      exports: ["CashFlowSimulator"]
    - path: "src/pages/mercadolivre/MLFluxoCaixa.tsx"
      provides: "Tabs Caixa Real | Simulador"
      contains: "Tabs"
  key_links:
    - from: "src/components/financial/CashFlowSimulator.tsx"
      to: "src/lib/cashflowSimulation.ts"
      via: "chama simulateCashflow(base, params)"
      pattern: "simulateCashflow"
    - from: "src/components/financial/CashFlowSimulator.tsx"
      to: "src/components/financial/CashFlowChart.tsx"
      via: "passa simulatedSeries (3ª linha)"
      pattern: "simulatedSeries"
    - from: "src/components/financial/CashFlowSimulator.tsx"
      to: "src/components/financial/SimulatorVerdictCard.tsx"
      via: "passa verdict"
      pattern: "SimulatorVerdictCard"
    - from: "src/pages/mercadolivre/MLFluxoCaixa.tsx"
      to: "src/components/financial/CashFlowSimulator.tsx"
      via: "renderiza dentro da TabsContent 'Simulador'"
      pattern: "CashFlowSimulator"
---

<objective>
Montar a aba Simulador e plugar tudo na página:
1. Criar `CashFlowSimulator` — componente da aba que detém o estado de sessão (`useState`: recebExtra, gastoExtra, eventos[]), renderiza os controles (2 sliders + lista de até 2 eventos pontuais + botão Limpar), reusa o baseline via `useCashFlowData`, chama `simulateCashflow` (plano 50-01) e injeta o resultado no `CashFlowChart` (prop `simulatedSeries`, plano 50-02) e no `SimulatorVerdictCard` (plano 50-02).
2. Envolver o conteúdo atual de `MLFluxoCaixa` em `Tabs` shadcn ("Caixa Real" | "Simulador"): a aba "Caixa Real" recebe o conteúdo atual SEM nenhuma alteração; a aba "Simulador" renderiza `CashFlowSimulator`.
3. Checkpoint visual do Wesley no preview Vercel antes de qualquer merge para main.

Purpose: Completa o Simulador end-to-end (SIM-02 Tabs, SIM-03 controles). É a única onda que toca a página alvo — a aba "Caixa Real" (já validada com a planilha DFC do Wesley) fica intocada, apenas movida para dentro de uma TabsContent.
Output: `CashFlowSimulator.tsx` novo + `MLFluxoCaixa.tsx` com Tabs.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Contrato da fase (UI §4 controles, edge cases §7, horizonte 120d)
@docs/superpowers/specs/2026-06-19-simulador-fluxo-caixa-design.md

# Decisões travadas (ranges/steps/eventos LOCKED)
@.planning/phases/50-simulador-de-cenarios-de-caixa-e-se/50-CONTEXT.md

# Página alvo (envolver em Tabs, NÃO mudar conteúdo da aba Caixa Real)
@src/pages/mercadolivre/MLFluxoCaixa.tsx

# Baseline + margem
@src/hooks/useCashFlowData.ts
@src/hooks/useFinancialSettings.ts

# Peças produzidas nas ondas 1 e 2
@src/lib/cashflowSimulation.ts
@src/components/financial/CashFlowChart.tsx
@src/components/financial/SimulatorVerdictCard.tsx

# shadcn disponíveis: slider, tabs, switch, popover, calendar, card, button, input, label
@src/components/ui/slider.tsx
@src/components/ui/tabs.tsx
@src/components/ui/calendar.tsx
@src/lib/formatters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar CashFlowSimulator (estado de sessão + controles + integração)</name>
  <files>src/components/financial/CashFlowSimulator.tsx</files>
  <read_first>
    - .planning/phases/50-simulador-de-cenarios-de-caixa-e-se/50-CONTEXT.md (ranges/steps/eventos LOCKED, botão Limpar, "+ Adicionar evento" some ao atingir 2)
    - docs/superpowers/specs/2026-06-19-simulador-fluxo-caixa-design.md §4 (layout controles ↔ resultado), §7 (baseline carregando = skeleton/controles desabilitados)
    - src/lib/cashflowSimulation.ts (simulateCashflow + tipos SimEvent/SimParams)
    - src/components/financial/CashFlowChart.tsx (prop simulatedSeries) e SimulatorVerdictCard.tsx (prop verdict)
    - src/hooks/useCashFlowData.ts (período hoje→hoje+120; usar mesmo padrão de MLFluxoCaixa) e useFinancialSettings.ts (safety_margin, default 10000)
    - src/components/ui/{slider,calendar,popover,switch,input,button,label}.tsx
  </read_first>
  <action>
    Criar `src/components/financial/CashFlowSimulator.tsx` (export nomeado `CashFlowSimulator`). Estado de sessão via `useState` (SEM persistência — recarregar volta ao real, decisão LOCKED):
    - `recebExtra: number` (default 0), `gastoExtra: number` (default 0), `eventos: SimEvent[]` (default []).
    Dados: chamar `useCashFlowData(hoje, hoje+120)` (mesmo período/horizonte de MLFluxoCaixa — 120d) e `useFinancialSettings()` para `margem = safety_margin ?? 10000`. Calcular `{ series, verdict } = simulateCashflow(baseline ?? [], { recebExtra, gastoExtra, eventos, margem })` (memoizar com useMemo dependente de baseline/recebExtra/gastoExtra/eventos/margem).
    Controles (valores LOCKED no CONTEXT.md):
    - Slider "Recebimento extra por dia": min −5000, max +5000, step 100, default 0; label mostra valor atual formatado (formatCurrency).
    - Slider "Gasto extra por dia": min 0, max +10000, step 100, default 0; label idem.
    - Lista de eventos pontuais (0 a 2): cada item = input de valor (R$), date picker (Calendar dentro de Popover) LIMITADO a [hoje, hoje+120] (disabled fora do intervalo — edge §7), toggle entrada/saída (Switch ou par de botões), botão lixeira p/ remover. Botão "+ Adicionar evento" que SOME quando `eventos.length === 2`.
    - Botão "Limpar": zera os 2 sliders e a lista de eventos.
    Estados de borda (edge §7): se baseline ainda carregando (isLoading) ou vazio → skeleton/aviso e controles desabilitados.
    Layout: coluna de controles (esquerda no desktop / topo no mobile) + coluna de resultado (direita): `SimulatorVerdictCard` no topo + `CashFlowChart data={baseline} simulatedSeries={series}` abaixo (a 3ª linha só aparece quando `verdict.ativa`, garantido pelo módulo + lógica do chart). Usar grid responsivo (ex.: `grid lg:grid-cols-[320px_1fr]`, decisão de layout = discretion). Cores SEMPRE `hsl(var(--token))`; formatação via `@/lib/formatters`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "simulateCashflow" src/components/financial/CashFlowSimulator.tsx && grep -q "simulatedSeries" src/components/financial/CashFlowSimulator.tsx && grep -q "SimulatorVerdictCard" src/components/financial/CashFlowSimulator.tsx</automated>
  </verify>
  <done>CashFlowSimulator renderiza 2 sliders (ranges/steps LOCKED), até 2 eventos pontuais com date picker limitado a [hoje,hoje+120], botão Limpar; chama simulateCashflow e alimenta CashFlowChart (simulatedSeries) + SimulatorVerdictCard (verdict); estado só de sessão; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Envolver MLFluxoCaixa em Tabs (Caixa Real | Simulador)</name>
  <files>src/pages/mercadolivre/MLFluxoCaixa.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFluxoCaixa.tsx (conteúdo atual: header sticky + grid de 3 cards + botão ajustar + gráfico/empty/dialog)
    - src/components/ui/tabs.tsx (Tabs/TabsList/TabsTrigger/TabsContent)
    - src/components/financial/CashFlowSimulator.tsx (criado na Task 1)
  </read_first>
  <action>
    Refatorar `MLFluxoCaixa` para introduzir `Tabs` shadcn (importar de `@/components/ui/tabs`) com duas TabsTrigger: "Caixa Real" (value="real") e "Simulador" (value="simulador"), defaultValue="real". A `TabsList` fica logo abaixo do header sticky.
    - `TabsContent value="real"`: MOVER, sem nenhuma alteração de conteúdo/comportamento, o bloco atual (grid de 3 cards + botão "Ajustar saldo de hoje" owner-only + gráfico/CashFlowEmptyState + AdjustBalanceDialog). A aba Caixa Real deve renderizar exatamente o que a página renderiza hoje (validada com a planilha DFC do Wesley — NÃO mexer na matemática nem nos componentes dela).
    - `TabsContent value="simulador"`: renderizar `<CashFlowSimulator />`.
    Manter o header sticky (`MLPageHeader title="Fluxo de Caixa"`) fora das Tabs (acima da TabsList) e o skeleton de page-loading inicial. Não duplicar a chamada de useCashFlowData da aba Real (o simulador tem a sua própria). tsc + build limpos.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Tabs" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "CashFlowSimulator" src/pages/mercadolivre/MLFluxoCaixa.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>MLFluxoCaixa tem Tabs "Caixa Real" | "Simulador"; aba Real = conteúdo atual intacto; aba Simulador = CashFlowSimulator; tsc e build limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Aba "Simulador" na página /fluxo-de-caixa: Tabs (Caixa Real | Simulador), 2 sliders (recebimento/gasto extra/dia), até 2 eventos pontuais com date picker, botão Limpar, gráfico com 3ª linha tracejada azul "Cenário simulado" e card de veredito (Saudável/Risco + folga/necessidade + menor saldo + data). Cálculo 100% frontend reusando get_cashflow, sem persistência.
  </what-built>
  <how-to-verify>
    Deploy de preview no Vercel (branch da fase, NÃO merge para main ainda) e validação visual do Wesley:
    1. Abrir o preview Vercel em /fluxo-de-caixa. Confirmar que a aba "Caixa Real" está IDÊNTICA à atual (3 cards + gráfico verde/âmbar) — nada mudou.
    2. Clicar na aba "Simulador". Arrastar o slider "Gasto extra por dia" para cima: a 3ª linha azul tracejada deve descer e o veredito virar "Risco" quando o saldo cruzar a margem (R$10k), mostrando a data crítica e o quanto precisa receber/dia.
    3. Arrastar "Recebimento extra por dia" para cima: linha azul sobe, veredito vira "Saudável" com "pode gastar até +R$X/dia".
    4. Adicionar 1 evento de saída numa data futura: o saldo simulado deve cair SÓ a partir daquela data (degrau no gráfico). Confirmar que "+ Adicionar evento" some após o 2º evento e que a lixeira remove.
    5. Clicar "Limpar": volta ao cenário projetado (linha azul some). Recarregar a página: volta ao real (sem persistência).
    6. Conferir mobile (responsivo) e dark/light.
  </how-to-verify>
  <resume-signal>Wesley digita "aprovado" (libera merge para main) ou descreve ajustes</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run build` limpos
- Aba "Caixa Real" renderiza o conteúdo atual sem regressão
- Estado do simulador some ao recarregar (sem persistência)
- Checkpoint visual do Wesley APROVADO no preview Vercel antes de qualquer merge para main
</verification>

<success_criteria>
SIM-02 (Tabs "Caixa Real" | "Simulador", aba Real intocada) e SIM-03 (sliders com ranges/steps LOCKED + até 2 eventos pontuais + botão Limpar, tudo reativo) satisfeitos; simulador integrado ao gráfico e ao veredito; aprovação visual do Wesley registrada.
</success_criteria>

<artifacts_this_phase_produces>
- `src/components/financial/CashFlowSimulator.tsx` — aba Simulador (estado de sessão + controles + integração)
- `src/pages/mercadolivre/MLFluxoCaixa.tsx` — Tabs Caixa Real | Simulador
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/50-simulador-de-cenarios-de-caixa-e-se/50-03-SUMMARY.md` when done
</output>
