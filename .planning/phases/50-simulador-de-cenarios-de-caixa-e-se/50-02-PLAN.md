---
phase: 50-simulador-de-cenarios-de-caixa-e-se
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/components/financial/CashFlowChart.tsx
  - src/components/financial/SimulatorVerdictCard.tsx
autonomous: true
requirements: [SIM-04, SIM-05]
must_haves:
  truths:
    - "CashFlowChart aceita prop opcional simulatedSeries; sem a prop, o gráfico renderiza idêntico ao uso atual da aba Caixa Real (retrocompatível 100%)"
    - "Quando simulatedSeries é passado, uma 3ª linha tracejada azul (hsl(var(--kpi-neutral))) 'Cenário simulado' aparece sobre o gráfico"
    - "SimulatorVerdictCard mostra selo Saudável (verde) ou Risco (vermelho) conforme verdict.status"
    - "SimulatorVerdictCard exibe a frase de folga (saudável) ou de necessidade (risco), o menor saldo e a data crítica"
  artifacts:
    - path: "src/components/financial/CashFlowChart.tsx"
      provides: "Gráfico estendido com prop opcional simulatedSeries (3ª linha)"
      contains: "simulatedSeries"
    - path: "src/components/financial/SimulatorVerdictCard.tsx"
      provides: "Card de veredito (status/folga/necessidade/menor saldo/data)"
      min_lines: 40
      exports: ["SimulatorVerdictCard"]
  key_links:
    - from: "src/components/financial/SimulatorVerdictCard.tsx"
      to: "src/lib/cashflowSimulation.ts"
      via: "consome o tipo SimVerdict"
      pattern: "SimVerdict"
    - from: "src/components/financial/CashFlowChart.tsx"
      to: "recharts Line dataKey simulado"
      via: "merge de simulatedSeries por fullDate na data do chart"
      pattern: "simulatedSeries"
---

<objective>
Construir os dois blocos de apresentação consumidos pela aba Simulador, sem ainda tocar na página:
1. Estender `CashFlowChart` com uma prop OPCIONAL `simulatedSeries` que adiciona uma 3ª linha tracejada azul ("Cenário simulado", `hsl(var(--kpi-neutral))`) — 100% retrocompatível: a aba "Caixa Real" continua chamando sem a prop e nada muda.
2. Criar `SimulatorVerdictCard`, card isolado que renderiza o veredito (selo Saudável/Risco + frase de folga/necessidade + menor saldo + data crítica) a partir do tipo `SimVerdict` do plano 50-01.

Purpose: Isolar a UI de resultado (gráfico + veredito) do estado/controles. Mantém o `CashFlowChart` retrocompatível (SIM-04) e dá um card de veredito testável visualmente sozinho (SIM-05). O plano 50-03 apenas monta os controles e injeta dados nestes componentes.
Output: `CashFlowChart.tsx` estendido + `SimulatorVerdictCard.tsx` novo.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Contrato da fase (UI §4/§6, cor da linha simulada §6)
@docs/superpowers/specs/2026-06-19-simulador-fluxo-caixa-design.md

# Decisões travadas
@.planning/phases/50-simulador-de-cenarios-de-caixa-e-se/50-CONTEXT.md

# Gráfico atual a estender (NÃO quebrar uso existente) + tipo CashFlowDataPoint
@src/components/financial/CashFlowChart.tsx
@src/hooks/useCashFlowData.ts

# Tipos SimVerdict/SimPoint do módulo puro (criados no 50-01)
@src/lib/cashflowSimulation.ts

# Formatters centralizados (usar formatCurrency em vez de toLocaleString ad-hoc)
@src/lib/formatters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender CashFlowChart com prop opcional simulatedSeries</name>
  <files>src/components/financial/CashFlowChart.tsx</files>
  <read_first>
    - src/components/financial/CashFlowChart.tsx (estrutura atual: ComposedChart, Lines accumulated_balance / accumulated_balance_sma, Legend formatter, CustomTooltip)
    - src/lib/cashflowSimulation.ts (tipo SimPoint: { fullDate, cenario })
    - docs/superpowers/specs/2026-06-19-simulador-fluxo-caixa-design.md §6 (cor da linha simulada = hsl(var(--kpi-neutral)), tracejada)
  </read_first>
  <action>
    Adicionar à interface `CashFlowChartProps` uma prop OPCIONAL `simulatedSeries?: SimPoint[]` (importar `SimPoint` de `@/lib/cashflowSimulation`). Comportamento:
    - Quando a prop é ausente/undefined/vazia: NÃO renderizar nenhuma linha nova; o gráfico fica idêntico ao atual (retrocompatibilidade SIM-04 — a aba Caixa Real chama sem a prop).
    - Quando presente e não-vazia: fazer merge do campo `cenario` na série do chart por `fullDate` (cada ponto de `data` recebe um campo `cenario` vindo do `simulatedSeries` com mesmo fullDate; pontos sem correspondência ficam sem o campo) e renderizar uma 3ª `<Line>` `dataKey="cenario"` com `stroke="hsl(var(--kpi-neutral))"`, `strokeDasharray` (tracejada, distinta da âmbar), `strokeWidth={2}`, `dot={false}`, `connectNulls`. Cores SEMPRE via `hsl(var(--token))` (regra CLAUDE.md — nunca var cru).
    - Estender o `Legend formatter` para mapear `"cenario"` → "Cenário simulado".
    - Opcional: incluir o valor do cenário no CustomTooltip quando presente (linha azul). Não obrigatório, mas não quebrar o tooltip atual.
    Não alterar a assinatura existente além de ADICIONAR a prop opcional. Garantir que o uso atual em MLFluxoCaixa (sem a prop) compila e renderiza igual.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "simulatedSeries" src/components/financial/CashFlowChart.tsx && grep -q "hsl(var(--kpi-neutral))" src/components/financial/CashFlowChart.tsx</automated>
  </verify>
  <done>CashFlowChart aceita `simulatedSeries?` opcional; sem a prop o render é idêntico ao atual; com a prop há uma 3ª linha tracejada azul "Cenário simulado"; tsc limpo; cor via hsl(var(--kpi-neutral)).</done>
</task>

<task type="auto">
  <name>Task 2: Criar SimulatorVerdictCard</name>
  <files>src/components/financial/SimulatorVerdictCard.tsx</files>
  <read_first>
    - src/lib/cashflowSimulation.ts (tipo SimVerdict)
    - src/lib/formatters.ts (formatCurrency)
    - docs/superpowers/specs/2026-06-19-simulador-fluxo-caixa-design.md §4 (painel de veredito) + CONTEXT.md bloco "Veredito (output)"
    - src/components/financial/CashFlowChart.tsx (padrão Card/CardContent shadcn + tokens kpi.positive/negative)
  </read_first>
  <action>
    Criar `src/components/financial/SimulatorVerdictCard.tsx` com export nomeado `SimulatorVerdictCard`. Props: `{ verdict: SimVerdict }` (importar tipo de `@/lib/cashflowSimulation`). Render (texto LOCKED no CONTEXT.md):
    - Selo de status: Saudável → verde (`text-kpi-positive` / badge com `bg-kpi-positive`); Risco → vermelho (`text-kpi-negative` / `bg-kpi-negative`). Ícone lucide adequado (ex.: CheckCircle2 / AlertTriangle).
    - Frase principal:
      - status "saudavel": "Você ainda pode gastar até +{formatCurrency(folgaGastoDia)}/dia mantendo o caixa seguro."
      - status "risco": "Caixa fica abaixo da margem em {valeDate formatado dd/MM}. Precisa de +{formatCurrency(necessidadeReceitaDia)}/dia de recebimento para equilibrar."
    - Sempre mostrar: "Menor saldo: {formatCurrency(menorSaldo)}" e a data crítica ({valeDate} formatada dd/MM via date-fns ou substring).
    - Quando `verdict.ativa === false` (nenhuma simulação): exibir o veredito do próprio baseline com um rótulo neutro indicando que reflete o cenário projetado atual (edge §7 "sem simulação ativa") — não esconder o card.
    Cores SEMPRE `hsl(var(--token))` via classes utilitárias de token (text-kpi-positive/negative). Usar `formatCurrency` de `@/lib/formatters` (não toLocaleString inline).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export function SimulatorVerdictCard\|export const SimulatorVerdictCard" src/components/financial/SimulatorVerdictCard.tsx && grep -qE "text-kpi-(positive|negative)" src/components/financial/SimulatorVerdictCard.tsx</automated>
  </verify>
  <done>SimulatorVerdictCard exporta um card que mostra selo Saudável/Risco, frase de folga/necessidade, menor saldo e data crítica a partir de SimVerdict; usa tokens kpi.positive/negative e formatCurrency; tsc limpo.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpo após ambas as tasks
- CashFlowChart sem a prop = render idêntico ao atual (a aba Caixa Real do plano 50-03 não passa a prop)
- Sem `var(` cru de cor nem hex literais novos (regra CLAUDE.md): cores via `hsl(var(--token))`
</verification>

<success_criteria>
SIM-04 (CashFlowChart estendido com simulatedSeries opcional, 3ª linha tracejada azul, 100% retrocompatível) e SIM-05 (SimulatorVerdictCard com selo + folga/necessidade + menor saldo + data) satisfeitos como componentes isolados, prontos para o plano 50-03 montar.
</success_criteria>

<artifacts_this_phase_produces>
- `src/components/financial/CashFlowChart.tsx` — estendido com prop opcional `simulatedSeries`
- `src/components/financial/SimulatorVerdictCard.tsx` — card de veredito (consumido pelo 50-03)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/50-simulador-de-cenarios-de-caixa-e-se/50-02-SUMMARY.md` when done
</output>
