---
phase: 50-simulador-de-cenarios-de-caixa-e-se
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/cashflowSimulation.ts
  - src/lib/cashflowSimulation.test.ts
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Sem deltas e sem eventos, a série simulada é igual ao baseline (accumulated_balance_sma)"
    - "Gasto extra/dia suficiente empurra o menor saldo abaixo da margem → status 'risco' com necessidadeReceitaDia > 0"
    - "Recebimento extra/dia suficiente mantém o menor saldo acima da margem → status 'saudavel' com folgaGastoDia > 0"
    - "Evento pontual de saída rebaixa o saldo a partir da data do evento (não antes)"
    - "Evento pontual de entrada eleva o saldo a partir da data do evento (não antes)"
    - "folga/necessidade são calculadas no índice do vale (argmin) correto, diasAteVale = valeIdx + 1"
  artifacts:
    - path: "src/lib/cashflowSimulation.ts"
      provides: "Função pura simulateCashflow(base, params) → { series, verdict }"
      min_lines: 60
      exports: ["simulateCashflow"]
    - path: "src/lib/cashflowSimulation.test.ts"
      provides: "Testes vitest cobrindo os 6 casos do spec §8"
      min_lines: 70
  key_links:
    - from: "src/lib/cashflowSimulation.ts"
      to: "src/hooks/useCashFlowData.ts"
      via: "consome o tipo CashFlowDataPoint (campos fullDate, accumulated_balance_sma)"
      pattern: "CashFlowDataPoint"
---

<objective>
Criar o módulo puro de cálculo `src/lib/cashflowSimulation.ts` (sem React, sem Supabase) que é o núcleo testável do Simulador de Cenários. Recebe a série base do `get_cashflow` (via tipo `CashFlowDataPoint`) + parâmetros de simulação (recebExtra/dia, gastoExtra/dia, até 2 eventos pontuais, margem) e produz a série simulada ponto a ponto + o veredito (status saudável/risco, menor saldo, data crítica, folga ou necessidade diária).

Implementado via TDD: os 6 testes vitest do spec §8 são escritos primeiro (RED), depois a implementação mínima passa (GREEN), depois refactor se necessário.

Purpose: Isolar a matemática do simulador num módulo puro testável. Toda a regra de negócio (SIM-01) vive aqui; os componentes React das ondas seguintes apenas chamam `simulateCashflow` e renderizam. Garante corretude antes de qualquer UI.
Output: `src/lib/cashflowSimulation.ts` + `src/lib/cashflowSimulation.test.ts`, com `npx vitest run src/lib/cashflowSimulation.test.ts` verde.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Contrato da fase (LER — arquitetura, matemática §5, edge cases §7, testes §8)
@docs/superpowers/specs/2026-06-19-simulador-fluxo-caixa-design.md

# Decisões travadas do Wesley
@.planning/phases/50-simulador-de-cenarios-de-caixa-e-se/50-CONTEXT.md

# Fonte do tipo baseline (reusar CashFlowDataPoint — campos fullDate + accumulated_balance_sma)
@src/hooks/useCashFlowData.ts

# Padrão de teste vitest do projeto (globals habilitados: describe/it/expect SEM import)
@src/lib/mco.test.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Definir contrato de tipos + testes RED do módulo puro</name>
  <files>src/lib/cashflowSimulation.ts, src/lib/cashflowSimulation.test.ts</files>
  <read_first>
    - docs/superpowers/specs/2026-06-19-simulador-fluxo-caixa-design.md §5 (matemática), §7 (edge cases), §8 (testes)
    - src/hooks/useCashFlowData.ts (interface CashFlowDataPoint — usar fullDate e accumulated_balance_sma)
    - src/lib/mco.test.ts (padrão vitest: describe/it/expect são globais, NÃO importar)
  </read_first>
  <behavior>
    Contrato exportado por cashflowSimulation.ts (definir tipos + assinatura, corpo pode lançar/retornar stub nesta task):
    - `SimEventType = "entrada" | "saida"`
    - `interface SimEvent { valor: number; data: string; tipo: SimEventType }` (data = yyyy-MM-dd)
    - `interface SimParams { recebExtra: number; gastoExtra: number; eventos: SimEvent[]; margem: number }`
    - `interface SimPoint { fullDate: string; cenario: number }` (cenário = saldo simulado acumulado do dia)
    - `interface SimVerdict { menorSaldo: number; valeIdx: number; diasAteVale: number; valeDate: string; status: "saudavel" | "risco"; folgaGastoDia: number; necessidadeReceitaDia: number; ativa: boolean }`
    - `function simulateCashflow(base: Pick<CashFlowDataPoint, "fullDate" | "accumulated_balance_sma">[], params: SimParams): { series: SimPoint[]; verdict: SimVerdict }`

    Testes (RED — devem falhar até a Task 2) cobrindo o spec §8. Usar um baseline fixo de ~10 pontos com fullDate sequencial (ex.: a partir de "2026-06-19") e accumulated_balance_sma conhecido para asserts determinísticos:
    - Test 1 (sem simulação): recebExtra=0, gastoExtra=0, eventos=[] → cada series[i].cenario === base[i].accumulated_balance_sma; verdict.ativa === false; menorSaldo === min(base.accumulated_balance_sma)
    - Test 2 (gasto → risco): gastoExtra alto o suficiente p/ menorSaldo < margem → verdict.status === "risco" e verdict.necessidadeReceitaDia > 0 e folgaGastoDia === 0
    - Test 3 (recebimento → folga): recebExtra alto, baseline acima da margem → verdict.status === "saudavel" e verdict.folgaGastoDia > 0 e necessidadeReceitaDia === 0
    - Test 4 (evento saída na data certa): 1 evento {tipo:"saida"} numa data do meio da série → series antes da data == baseline; a partir da data, cenario == baseline − valor
    - Test 5 (evento entrada na data certa): 1 evento {tipo:"entrada"} → a partir da data, cenario == baseline + valor; antes, inalterado
    - Test 6 (vale correto): montar baseline com mínimo num índice conhecido → verdict.valeIdx === esse índice, diasAteVale === valeIdx+1, valeDate === base[valeIdx].fullDate
  </behavior>
  <action>
    Criar `src/lib/cashflowSimulation.ts` exportando APENAS os tipos e a assinatura de `simulateCashflow` acima (corpo stub que retorna `{ series: [], verdict: ... }` ou lança — o suficiente p/ tsc compilar e os testes rodarem em RED). Criar `src/lib/cashflowSimulation.test.ts` com os 6 testes acima usando `describe`/`it`/`expect` como globais (NÃO importar — globals habilitados, ver mco.test.ts). Importar somente `simulateCashflow` e os tipos do módulo. Rodar e CONFIRMAR que os testes falham (RED). Commit: `test(50-01): add failing tests for cashflowSimulation`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/cashflowSimulation.test.ts 2>&1 | grep -qE "fail|FAIL|✗|×" && echo "RED OK (tests failing as expected)"</automated>
  </verify>
  <done>cashflowSimulation.ts exporta os tipos + assinatura; cashflowSimulation.test.ts tem os 6 testes do spec §8; os testes FALHAM (RED) porque a implementação é stub.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implementar simulateCashflow até GREEN + refactor</name>
  <files>src/lib/cashflowSimulation.ts</files>
  <read_first>
    - docs/superpowers/specs/2026-06-19-simulador-fluxo-caixa-design.md §5 (fórmulas exatas)
    - .planning/phases/50-simulador-de-cenarios-de-caixa-e-se/50-CONTEXT.md (bloco "Matematica")
  </read_first>
  <action>
    Implementar `simulateCashflow` exatamente conforme a matemática do spec §5 (LOCKED no CONTEXT.md):
    - Para cada ponto i (0-indexed, i=0=hoje), diasDecorridos = i+1.
    - `deltaMediaAcum(i) = (recebExtra − gastoExtra) * diasDecorridos`.
    - `eventosAcum(i) = Σ eventos cujo data <= base[i].fullDate` (entrada: +valor, saída: −valor). Comparar como strings yyyy-MM-dd (lexicográfico = cronológico).
    - `cenario(i) = base[i].accumulated_balance_sma + deltaMediaAcum(i) + eventosAcum(i)`.
    - Veredito: `menorSaldo = min(cenario)`, `valeIdx = argmin`, `diasAteVale = valeIdx + 1` (sempre ≥ 1 → sem divisão por zero, edge §7), `valeDate = base[valeIdx].fullDate`.
    - `status = menorSaldo >= margem ? "saudavel" : "risco"`.
    - `folgaGastoDia = max(0, (menorSaldo − margem) / diasAteVale)`; `necessidadeReceitaDia = max(0, (margem − menorSaldo) / diasAteVale)`.
    - `ativa = recebExtra !== 0 || gastoExtra !== 0 || eventos.length > 0` (controla render da 3ª linha — spec §5 nota).
    - Base vazia ([]): retornar series=[] e verdict neutro (menorSaldo=0, valeIdx=0, diasAteVale=1, status conforme margem, ativa=false) — não lançar (edge §7 "baseline vazio").
    NÃO inserir blocos de código no plano: implementar diretamente no arquivo. Rodar até GREEN. Refatorar se útil mantendo testes verdes. Commits: `feat(50-01): implement simulateCashflow` e, se houver, `refactor(50-01): ...`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/cashflowSimulation.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Os 6 testes do spec §8 passam (GREEN); `tsc --noEmit` limpo; base vazia não lança; matemática idêntica ao spec §5.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/cashflowSimulation.test.ts` → 6/6 passam
- `npx tsc --noEmit` limpo
- Sem import de React/Supabase em cashflowSimulation.ts (módulo puro): `grep -nE "react|supabase" src/lib/cashflowSimulation.ts` retorna vazio
</verification>

<success_criteria>
SIM-01 satisfeito: módulo puro `cashflowSimulation.ts` calcula série simulada + veredito a partir de baseline + deltas + eventos, com testes vitest cobrindo sem-simulação, gasto→risco, recebimento→folga e eventos pontuais entrada/saída na data certa, todos verdes.
</success_criteria>

<artifacts_this_phase_produces>
- `src/lib/cashflowSimulation.ts` — função pura `simulateCashflow` + tipos `SimEvent`/`SimParams`/`SimPoint`/`SimVerdict` (consumidos pelos planos 50-02 e 50-03)
- `src/lib/cashflowSimulation.test.ts` — suíte vitest
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/50-simulador-de-cenarios-de-caixa-e-se/50-01-SUMMARY.md` when done
</output>
