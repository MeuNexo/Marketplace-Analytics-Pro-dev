---
phase: 20-dashboard-personalizavel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mercadolivre/MLKPIGrid.tsx
  - src/hooks/useDashboardLayout.ts
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements:
  - RF-01
  - RF-02
  - RF-03

must_haves:
  truths:
    - "MLKPIGrid usa size='default' (não compact) e exibe ≥10 KPI cards"
    - "Hook useDashboardLayout gerencia ordem e visibilidade via localStorage"
    - "Botão Personalizar no header de MercadoLivre.tsx abre Sheet lateral"
    - "Sheet tem Switch por widget + botões ↑ ↓ + botão Restaurar padrão"
    - "Widgets ocultos não renderizam; ordem define sequência de renderização"
    - "Nenhuma nova dependência adicionada ao package.json"
  artifacts:
    - path: "src/hooks/useDashboardLayout.ts"
      provides: "estado de layout (ordem + visibilidade) com persistência localStorage"
    - path: "src/components/mercadolivre/MLKPIGrid.tsx"
      provides: "grid expandido com 10 KPI cards em tamanho default"
    - path: "src/pages/MercadoLivre.tsx"
      provides: "seções renderizadas em ordem configurável + botão Personalizar + Sheet"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useDashboardLayout.ts"
      via: "useDashboardLayout()"
      pattern: "hook → estado → renderização condicional por widget ID"
---

<objective>
Expandir os KPI cards do dashboard de Vendas e adicionar personalização por usuário.

Resultado esperado: o usuário vê todos os indicadores em tamanho legível, pode ocultar/mostrar seções e reordenar o dashboard — tudo salvo automaticamente no localStorage.

Sem novas dependências. Usa Sheet + Switch + botões já disponíveis no projeto.
</objective>

<execution_context>
@.planning/phases/20-dashboard-personalizavel/20-CONTEXT.md
</execution_context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hook useDashboardLayout — estado de layout com localStorage</name>
  <files>src/hooks/useDashboardLayout.ts</files>
  <action>
Criar `src/hooks/useDashboardLayout.ts` com a seguinte estrutura:

```typescript
import { useState, useCallback, useEffect } from "react";

export interface WidgetConfig {
  id: string;
  label: string;
  description: string;
  visible: boolean;
}

const DEFAULT_WIDGETS: WidgetConfig[] = [
  { id: "kpi_grid",       label: "KPIs de Vendas",      description: "Cards com receita, pedidos, ticket, visitas e mais",   visible: true },
  { id: "revenue_chart",  label: "Gráfico de Receita",  description: "Evolução diária ou horária da receita + metas",        visible: true },
  { id: "cost_waterfall", label: "Custos & Top Produtos", description: "Waterfall financeiro e produtos mais vendidos",       visible: true },
  { id: "brand_charts",   label: "Gráficos de Marca",   description: "Faturamento, markup e share por marca",               visible: true },
];

const STORAGE_KEY = "ml_dashboard_layout_v1";

function loadLayout(): WidgetConfig[] {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return DEFAULT_WIDGETS;
    const saved: WidgetConfig[] = JSON.parse(raw);
    // Merge: mantém novos widgets que ainda não existem no storage
    const savedIds = new Set(saved.map(w => w.id));
    const merged = [
      ...saved.filter(s => DEFAULT_WIDGETS.some(d => d.id === s.id)),
      ...DEFAULT_WIDGETS.filter(d => !savedIds.has(d.id)),
    ];
    return merged.length > 0 ? merged : DEFAULT_WIDGETS;
  } catch {
    return DEFAULT_WIDGETS;
  }
}

function saveLayout(widgets: WidgetConfig[]) {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(widgets));
  } catch { /* ignore */ }
}

export function useDashboardLayout() {
  const [widgets, setWidgets] = useState<WidgetConfig[]>(loadLayout);

  // Persiste sempre que muda
  useEffect(() => {
    saveLayout(widgets);
  }, [widgets]);

  const toggleWidget = useCallback((id: string) => {
    setWidgets(prev => prev.map(w => w.id === id ? { ...w, visible: !w.visible } : w));
  }, []);

  const moveUp = useCallback((id: string) => {
    setWidgets(prev => {
      const idx = prev.findIndex(w => w.id === id);
      if (idx <= 0) return prev;
      const next = [...prev];
      [next[idx - 1], next[idx]] = [next[idx], next[idx - 1]];
      return next;
    });
  }, []);

  const moveDown = useCallback((id: string) => {
    setWidgets(prev => {
      const idx = prev.findIndex(w => w.id === id);
      if (idx < 0 || idx >= prev.length - 1) return prev;
      const next = [...prev];
      [next[idx], next[idx + 1]] = [next[idx + 1], next[idx]];
      return next;
    });
  }, []);

  const resetLayout = useCallback(() => {
    setWidgets(DEFAULT_WIDGETS);
  }, []);

  const isVisible = useCallback((id: string) => {
    return widgets.find(w => w.id === id)?.visible ?? true;
  }, [widgets]);

  return { widgets, toggleWidget, moveUp, moveDown, resetLayout, isVisible };
}
```
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit 2>&1 | grep "useDashboardLayout\|error TS" | head -10</automated>
  </verify>
  <done>Hook exporta useDashboardLayout com widgets, toggleWidget, moveUp, moveDown, resetLayout, isVisible</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MLKPIGrid expandido — size default + 10 KPI cards</name>
  <files>src/components/mercadolivre/MLKPIGrid.tsx</files>
  <action>
Atualizar `MLKPIGrid.tsx` com as seguintes mudanças:

**1. Grid responsivo ampliado**
Trocar:
```tsx
<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-7 gap-3">
```
Por:
```tsx
<div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-3">
```

**2. Remover `size="compact"` de todos os KPICard** (usar o default)

**3. Adicionar 3 novos KPI cards** após os existentes:

```tsx
{/* Compradores Únicos */}
<KPICard
  title="Compradores"
  value={metrics ? metrics.unique_buyers.toLocaleString("pt-BR") : "—"}
  icon={<Users className="w-4 h-4" />}
  variant="minimal"
  iconClassName="bg-[hsl(200,80%,50%)]/10 text-[hsl(200,80%,50%)]"
  loading={loading}
  refreshing={refreshing}
  delta={metrics && previousMetrics ? calcDelta(metrics.unique_buyers, previousMetrics.unique_buyers) : undefined}
/>

{/* Unidades Vendidas */}
<KPICard
  title="Unidades"
  value={metrics ? metrics.units_sold.toLocaleString("pt-BR") : "—"}
  icon={<Package className="w-4 h-4" />}
  variant="minimal"
  iconClassName="bg-[hsl(280,70%,55%)]/10 text-[hsl(280,70%,55%)]"
  loading={loading}
  refreshing={refreshing}
  delta={metrics && previousMetrics ? calcDelta(metrics.units_sold, previousMetrics.units_sold) : undefined}
/>

{/* Impostos */}
<KPICard
  title="Impostos"
  value={
    kpiSummary?.has_tax_data
      ? currencyFmt(kpiSummary.total_tax)
      : "—"
  }
  subtitle={
    kpiSummary?.has_tax_data && kpiSummary.gross_revenue > 0
      ? `${((kpiSummary.total_tax / kpiSummary.gross_revenue) * 100).toFixed(1)}% da receita`
      : undefined
  }
  icon={<Receipt className="w-4 h-4" />}
  variant="minimal"
  iconClassName="bg-red-500/10 text-red-500"
  loading={summaryLoading}
  refreshing={refreshing}
  tooltip="Total de impostos (ICMS + PIS + COFINS) calculados sobre os pedidos do período."
/>
```

**4. Adicionar imports necessários no topo:**
```tsx
import { DollarSign, ShoppingCart, Tag, Eye, Percent, TrendingUp, Wallet, Users, Package, Receipt } from "lucide-react";
```

**Nota sobre `units_sold`:** verificar se está na interface `Metrics`. Se não estiver, adicionar ao interface local em MLKPIGrid.tsx:
```typescript
interface Metrics {
  total_revenue: number;
  units_sold: number;      // já deve existir
  avg_ticket: number;
  unique_visits: number;
  conversion_rate: number;
  total_orders: number;
  unique_buyers: number;   // verificar se existe; se não, pode ser null-safe
}
```

Se `unique_buyers` não existir em `MLKPISummary`, usar `metrics.unique_buyers` — verificar em `useMLKPISummary.ts`. Se não existir lá, buscar de `metrics` (da query diária) onde deve estar disponível como `unique_buyers`.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit 2>&1 | grep "MLKPIGrid\|error TS" | head -10</automated>
  </verify>
  <done>MLKPIGrid mostra ≥10 cards em size default; TypeScript limpo</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: MercadoLivre.tsx — botão Personalizar + Sheet + renderização por widget</name>
  <files>src/pages/MercadoLivre.tsx</files>
  <action>
Três mudanças em `MercadoLivre.tsx`:

---

**Mudança 1 — Imports**

Adicionar ao bloco de imports:
```tsx
import { useDashboardLayout } from "@/hooks/useDashboardLayout";
import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription } from "@/components/ui/sheet";
import { Switch } from "@/components/ui/switch";
import { Button } from "@/components/ui/button";
import { Settings2, ChevronUp, ChevronDown, RotateCcw } from "lucide-react";
```

---

**Mudança 2 — Hook e estado do Sheet**

Logo após os outros hooks (após `const impostosTotal = ...`), adicionar:
```tsx
const { widgets, toggleWidget, moveUp, moveDown, resetLayout, isVisible } = useDashboardLayout();
const [layoutOpen, setLayoutOpen] = useState(false);
```

---

**Mudança 3 — Botão "Personalizar" no header**

Localizar o bloco do header onde ficam os botões (Modo TV, Atualizar, etc.) e adicionar o botão Personalizar:
```tsx
<Button
  variant="outline"
  size="sm"
  className="h-8 gap-1.5 text-xs"
  onClick={() => setLayoutOpen(true)}
>
  <Settings2 className="w-3.5 h-3.5" />
  <span className="hidden sm:inline">Personalizar</span>
</Button>
```

---

**Mudança 4 — Sheet de personalização**

Adicionar antes do fechamento do return, logo após o conteúdo principal:
```tsx
<Sheet open={layoutOpen} onOpenChange={setLayoutOpen}>
  <SheetContent side="right" className="w-80">
    <SheetHeader>
      <SheetTitle className="text-base">Personalizar Dashboard</SheetTitle>
      <SheetDescription className="text-xs">
        Escolha quais seções exibir e em qual ordem.
      </SheetDescription>
    </SheetHeader>

    <div className="mt-6 space-y-3">
      {widgets.map((widget, idx) => (
        <div
          key={widget.id}
          className="flex items-center gap-3 p-3 rounded-lg border bg-card"
        >
          <Switch
            checked={widget.visible}
            onCheckedChange={() => toggleWidget(widget.id)}
            className="shrink-0"
          />
          <div className="flex-1 min-w-0">
            <p className="text-sm font-medium leading-none">{widget.label}</p>
            <p className="text-xs text-muted-foreground mt-0.5 truncate">{widget.description}</p>
          </div>
          <div className="flex flex-col gap-0.5 shrink-0">
            <button
              onClick={() => moveUp(widget.id)}
              disabled={idx === 0}
              className="p-0.5 rounded hover:bg-muted disabled:opacity-30 disabled:cursor-not-allowed"
            >
              <ChevronUp className="w-3.5 h-3.5" />
            </button>
            <button
              onClick={() => moveDown(widget.id)}
              disabled={idx === widgets.length - 1}
              className="p-0.5 rounded hover:bg-muted disabled:opacity-30 disabled:cursor-not-allowed"
            >
              <ChevronDown className="w-3.5 h-3.5" />
            </button>
          </div>
        </div>
      ))}
    </div>

    <div className="mt-6 pt-4 border-t">
      <Button
        variant="ghost"
        size="sm"
        className="w-full gap-2 text-xs text-muted-foreground"
        onClick={resetLayout}
      >
        <RotateCcw className="w-3 h-3" />
        Restaurar padrão
      </Button>
    </div>
  </SheetContent>
</Sheet>
```

---

**Mudança 5 — Renderização condicional e ordenada por widget**

Substituir o bloco de seções estático por renderização baseada na ordem dos widgets.

Localizar a área com as seções (após o banner de "sem dados") e substituir por:

```tsx
{widgets.map((widget) => {
  if (!widget.visible) return null;

  if (widget.id === "kpi_grid") return (
    <MLKPIGrid
      key="kpi_grid"
      metrics={effectiveMetrics}
      previousMetrics={previousMetrics}
      loading={effectiveLoading}
      syncing={effectiveSyncing}
      hasSyncProgress={!!syncProgress}
      kpiSummary={kpiSummary}
      kpiSummaryLoading={kpiSummaryLoading}
      adsTotalForPeriod={adsSummary.total_spend}
    />
  );

  if (widget.id === "revenue_chart") return (
    <div key="revenue_chart" className="grid grid-cols-1 lg:grid-cols-[1fr_320px] gap-3">
      <MLRevenueChart
        chartTitle={chartTitle}
        showHourlyChart={showHourlyChart}
        hasHourlyData={hasHourlyData}
        syncing={syncing}
        chartData={chartData}
        isAll={isAll}
        overlaidHourlyData={overlaidHourlyData}
        perMarketplaceHourly={perMarketplaceHourly}
      />
      <GoalsCard
        currentRevenue={monthlyMetrics?.total_revenue ?? 0}
        currentOrders={monthlyMetrics?.units_sold ?? 0}
        currentTicket={monthlyMetrics?.avg_ticket ?? 0}
        currentConversion={monthlyMetrics?.conversion_rate ?? 0}
        storeId={selectedStore !== "all" ? String(selectedStore) : (stores[0]?.ml_user_id ?? undefined)}
      />
    </div>
  );

  if (widget.id === "cost_waterfall") return (
    <div key="cost_waterfall" className="grid grid-cols-1 lg:grid-cols-6 gap-3">
      <MLCostCard
        gross_revenue={effectiveMetrics?.total_revenue ?? 0}
        cancelled_revenue={costWaterfall?.cancelled_revenue ?? 0}
        comissao={costWaterfall?.total_comissao ?? ordersSummary?.total_comissao ?? (effectiveMetrics?.total_revenue ?? 0) * 0.11}
        frete={costWaterfall?.total_frete ?? ordersSummary?.total_frete ?? (effectiveMetrics?.total_revenue ?? 0) * 0.05}
        publicidade={adsSummary.total_spend}
        cmv={costWaterfall?.has_cmv ? costWaterfall.cmv : null}
        impostos={impostosTotal}
        loading={costWaterfallLoading}
      />
      <MLTopProducts products={effectiveProducts} />
    </div>
  );

  if (widget.id === "brand_charts") return (
    <div key="brand_charts" className="space-y-3">
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-3">
        <BrandRevenueChart
          data={brandData?.brandRevenueSeries ?? []}
          topBrands={brandData?.topBrands ?? []}
          loading={brandLoading}
        />
        <BrandMarkupChart
          data={brandData?.brandMarkupSeries ?? []}
          topBrands={brandData?.topBrands ?? []}
          loading={brandLoading}
        />
      </div>
      <CustoOperacionalChart
        custoSeries={brandData?.custoSeries ?? []}
        adsDaily={adsDaily}
        loading={brandLoading}
      />
      <BrandSharePieChart
        brandAggregates={brandData?.brandAggregates ?? []}
        loading={brandLoading}
      />
    </div>
  );

  return null;
})}
```

---

**Atenção:** o bloco original das seções deve ser REMOVIDO (substituído pelo map acima). Garantir que o banner "sem dados" permanece ANTES do map de widgets.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit 2>&1 | grep "MercadoLivre\|error TS" | head -15</automated>
  </verify>
  <done>
- Botão Personalizar aparece no header
- Sheet abre com lista de widgets + toggles + setas
- Seções renderizadas em ordem definida pelo usuário
- Seções ocultas não renderizam
- TypeScript sem erros
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript limpo (zero erros)
cd /root/garment-glow-test && npx tsc --noEmit 2>&1 | tail -5

# Hook criado
ls src/hooks/useDashboardLayout.ts

# KPIGrid tem os novos cards
grep -n "Compradores\|Unidades\|Impostos\|Receipt\|Users\|Package" src/components/mercadolivre/MLKPIGrid.tsx

# MercadoLivre tem Sheet e botão
grep -n "Sheet\|Personalizar\|layoutOpen\|useDashboardLayout" src/pages/MercadoLivre.tsx | head -15
```
</verification>

<success_criteria>
1. `useDashboardLayout.ts` exporta hook com 5 funções + array `widgets`
2. MLKPIGrid exibe ≥10 KPI cards em `size="default"`
3. Grid responsivo: 2 cols mobile, 5 cols desktop
4. Botão "Personalizar" visível no header da página Vendas
5. Sheet abre com 4 widgets listados, cada um com Switch + setas ↑↓
6. Toggle de visibilidade oculta/mostra a seção correspondente na página
7. Reordenação com setas muda a ordem de renderização das seções
8. Preferências sobrevivem a reload (localStorage `ml_dashboard_layout_v1`)
9. Botão "Restaurar padrão" reseta para configuração default
10. TypeScript: 0 erros; testes existentes: 63/63 passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/20-dashboard-personalizavel/20-01-SUMMARY.md` com:
- Lista de arquivos modificados/criados
- Contagem de KPI cards antes e depois
- Confirmação de TypeScript limpo e testes passando
</output>
