---
phase: 96-dre-correcoes-linha-a-linha
plan: 05
type: execute
wave: 2
depends_on: ["96-01", "96-04"]
files_modified:
  - src/lib/dreMargem.ts
  - src/lib/dreMargem.test.ts
  - src/components/mercadolivre/MLCostCard.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements: ["C1", "C2/C5"]

must_haves:
  truths:
    - "O card exibe a receita BRUTA do mês e uma linha própria de Cancelamentos de vendas"
    - "O cancelamento (incluindo o reembolso) entra na FÓRMULA da margem, não só na tela — a margem de maio continua partindo de 247.216,12"
    - "Existe UMA fórmula de margem de contribuição no código, não duas"
    - "O Total de tarifas ML exibido no card exclui o parcelamento, e a linha do parcelamento continua visível como informativa"
  artifacts:
    - path: "src/lib/dreMargem.ts"
      provides: "computeMargemContribuicao — fonte única da fórmula, pura e testável"
      exports: ["computeMargemContribuicao", "MargemContribuicaoInput"]
    - path: "src/lib/dreMargem.test.ts"
      provides: "prova de que o cancelamento entra na fórmula e o bottom line não infla"
      min_lines: 50
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/lib/dreMargem.ts"
      via: "computeMargemContribuicao alimenta buildDreCascade e a prop do card"
      pattern: "computeMargemContribuicao"
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/hooks/useCancelledRevenue.ts"
      via: "useCancelledRevenue no mesmo eixo de mês do dreWaterfall"
      pattern: "useCancelledRevenue"
---

<objective>
[C1] Receita BRUTA + linha "Cancelamentos de vendas" (−), com o cancelamento **dentro da fórmula**. [C2/C5-frontend] Fazer o parcelamento sair do total exibido, mantendo a linha na tela como informativa.

Purpose: a ARMADILHA que o CONTEXT marca — se o cancelamento entrar só na tela e não na fórmula, a margem de maio infla R$14.450,29. E o RESEARCH achou que **a fórmula está DUPLICADA** (`MercadoLivre.tsx:364-367` e `MLCostCard.tsx:113-117`): mudar uma só faz o card exibir dois números incoerentes, em silêncio.
Output: `src/lib/dreMargem.ts` (fonte única da fórmula) + o card compondo bruto/cancelamentos + o total de tarifas respeitando a flag `excluded`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md
@src/lib/dreCascade.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [C1] computeMargemContribuicao — fonte única da fórmula (TDD)</name>

  <read_first>
    - `src/pages/MercadoLivre.tsx:364-367` — a fórmula #1 (`margemContribuicao`), que alimenta `buildDreCascade`.
    - `src/components/mercadolivre/MLCostCard.tsx:112-130` — a fórmula #2 (`lucro`), duplicada, que alimenta `lucroPositivo`, `margemPct` e o fallback `resultadoOperacionalVal ?? lucro`.
    - `src/lib/dreCascade.ts:94-127` — `round2` e como `margemContribuicao` entra na cascata. Este arquivo **não é tocado** neste plano.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md` seção `### [C1]` — a ARMADILHA e os números de maio.
  </read_first>

  <files>src/lib/dreMargem.ts (novo), src/lib/dreMargem.test.ts (novo)</files>

  <behavior>
    `computeMargemContribuicao(input: MargemContribuicaoInput): number` — input `{ receitaBruta, cancelamentosVendas, totalTarifas, cmvMes, impostosMes }`, os dois últimos nullable.
    - Test 1 (a ARMADILHA — prova de que o cancelamento está na fórmula): maio corrigido — `receitaBruta: 261666.41`, `cancelamentosVendas: 14450.29`, `totalTarifas: 63878.37`, `cmvMes: 126574.59`, `impostosMes: 4793.23` → **51969.93** (a margem corrigida do CONTEXT §2). Se alguém tirar o cancelamento da fórmula, o resultado vira 66420.22 — R$14.450,29 inflados — e este teste pega.
    - Test 2 (equivalência com a expressão legada / SC5): com os mesmos inputs, o resultado é igual a `247216.12 - 63878.37 - 126574.59 - 4793.23`. A base líquida e a base bruta-menos-cancelamento têm que dar o MESMO número — é o que garante que o C1 é apresentação, não mudança de resultado.
    - Test 3 (maio "se fechar hoje", antes das correções): `receitaBruta: 261666.41`, `cancelamentos: 14450.29`, `totalTarifas: 75127.33`, `cmv: 136462.51`, `impostos: 4793.23` → **30833.05** (a margem "hoje" do CONTEXT §2).
    - Test 4 (a identidade que protege o swing — reembolso entra nos DOIS lados): `receitaBruta − cancelamentosVendas === 247216.12` com os inputs do Test 1. O reembolso de R$386,39 soma na bruta **e** sai no cancelamento, então a líquida não se move. Se alguém adicionar o reembolso só num dos lados, este teste pega.
    - Test 5: `cmvMes: null` e `impostosMes: null` são tratados como 0 (mesma semântica de hoje: `(cmvMes ?? 0)`).
    - Test 6: `cancelamentosVendas: 0` → o resultado é `receitaBruta - totalTarifas - cmv - impostos` (mês sem cancelamento não muda nada).
    - Test 7: o retorno é arredondado a 2 casas (entradas com 3+ casas não vazam float sujo).
  </behavior>

  <action>
    RED primeiro: escreva `src/lib/dreMargem.test.ts` com os 7 casos, commit só do teste, confirme o vermelho, depois implemente.

    Crie `src/lib/dreMargem.ts` — módulo puro, **sem import de React nem de Supabase** (padrão de `dreCascade.ts`/`dreRegime.ts`):
    - `export interface MargemContribuicaoInput { receitaBruta: number; cancelamentosVendas: number; totalTarifas: number; cmvMes: number | null; impostosMes: number | null }`
    - `export function computeMargemContribuicao(input): number` — subtrai, nessa ordem, os cancelamentos de venda, o total de tarifas, o CMV e os impostos da receita bruta; nullish→0; `round2` no retorno.
    - Comentário de cabeçalho, registrando as duas coisas que fazem este módulo existir: (a) a fórmula estava **duplicada** em `MercadoLivre.tsx` e `MLCostCard.tsx`, e divergir as duas fazia o card se auto-contradizer em silêncio; (b) o cancelamento tem que entrar na FÓRMULA e não só na tela, senão a margem infla R$14.450,29 (a ARMADILHA do C1). Deixe explícito que a base bruta-menos-cancelamento é **algebricamente idêntica** à base líquida de hoje — o C1 muda o que se VÊ, não o resultado. Registre também a decisão do dono sobre o reembolso: *"reembolso fica de fora da receita e e considerado como cancelamento"* → ele entra na bruta E sai no cancelamento, mantendo a líquida em 247.216,12.
  </action>

  <verify>
    <automated>npx vitest run src/lib/dreMargem.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/lib/dreMargem.test.ts` → 7/7 passam.
    - `git log --oneline -2 -- src/lib/dreMargem.test.ts` mostra o commit do teste ANTES do da implementação (RED→GREEN).
    - `grep -rc 'react\|supabase' src/lib/dreMargem.ts` → 0 (módulo puro).
    - `npx tsc --noEmit` → 0 erros.
  </acceptance_criteria>

  <done>A fórmula da margem existe uma única vez, é pura, e um teste prova que o cancelamento de venda está dentro dela com o número real de maio.</done>
</task>

<task type="auto">
  <name>Task 2: [C1] MLCostCard — receita bruta, cancelamentos, margem por prop, parcelamento informativo</name>

  <read_first>
    - `src/components/mercadolivre/MLCostCard.tsx` **inteiro** (488 linhas) — props (`:27-80`), o `lucro` duplicado (`:112-130`), o bloco "Receita do mês" (`:249-258`), o `.map` dos grupos de tarifas (`:262-280`), o "Total de tarifas ML" (`:283-296`) e o subtotal "Margem de contribuição" (`:348-367`).
    - `src/components/mercadolivre/MLCostCard.tsx:16-17` — o helper `pct(v, base)`, usado em **todas** as linhas do card.
    - `src/hooks/useMLBilling.ts:20-24` — `BillingGroup`, que ganhou `excluded?: boolean` no plano 96-01.
  </read_first>

  <files>src/components/mercadolivre/MLCostCard.tsx</files>

  <action>
    **Props novas** (em `MLCostCardProps`, `:27-80`):
    - `receitaBruta: number` — o novo destaque do topo.
    - `cancelamentosVendas?: number` — default 0.
    - `margemContribuicao: number` — **obrigatória**. Vem calculada do caller.

    `receitaMes` **permanece** como prop e continua sendo a **base de todos os `pct()`**.
    🚨 **Isto é decisão de projeto, não descuido:** `pct(x, receitaMes)` alimenta as porcentagens de tarifas, CMV, impostos, blocos operacionais e resultado líquido. Trocar a base para a bruta mudaria TODOS os percentuais do card — inclusive no mês aberto (previsão), quebrando o SC5 — e faria o "%" da DRE deixar de bater com o resto do app. A base do % é a **receita líquida** (247.216,12 em maio); a bruta é exibição.

    **Deletar a fórmula duplicada** (`:113-117`): `lucro` sai; no lugar, use a prop `margemContribuicao`. Reaponte os 3 consumidores: `lucroPositivo` (`:118`), `margemPct` (`:119`) e o fallback `resultadoOperacionalVal = resultadoOperacional ?? lucro` (`:125`). Nenhuma aritmética de margem pode sobrar neste arquivo.

    **Bloco da receita** (`:249-258`): o rótulo "Receita do mês (vendas pagas)" descreve o valor antigo — atualize para refletir que o valor exibido agora é o bruto (ex.: "Receita bruta do mês"), e exiba `receitaBruta` no lugar de `receitaMes`.

    **Linha nova "Cancelamentos de vendas"**, logo abaixo do bloco da receita e **ANTES** dos grupos de tarifas — renderize só quando `cancelamentosVendas` for diferente de 0. Espelhe o estilo das linhas de dedução (`(−)` + label + `pct()` + `fmt()`).
    🚨 **NÃO** a coloque dentro do `.map` de `gruposTarifas` (`:262-280`): aquele `.map` renderiza tarifas e é seguido pelo subtotal "Total de tarifas ML". Cancelamento de **venda** não é tarifa — entrar ali contaminaria o subtotal de tarifas e quebraria o SC2.

    **Grupos excluídos** (o parcelamento, no `.map` de `:262-280`): quando `grupo.excluded` for verdadeiro, renderize a linha em modo **informativo** — sem o prefixo `(−)`, com texto atenuado, e um `Tooltip` (o componente já está importado, `:4-9`) explicando que a taxa é paga pelo comprador (acréscimo no preço dele) e por isso não é custo da loja nem entra no total. A linha continua visível: o Wesley quer ver o número, só não quer que ele seja subtraído.

    **Total de tarifas ML** (`:283-296`): continua exibindo a prop `totalTarifas` — que o caller já entrega sem o parcelamento. Não recalcule nada aqui.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'const lucro' src/components/mercadolivre/MLCostCard.tsx` → **0** (a fórmula duplicada morreu).
    - `grep -v '^\s*[/*]' src/components/mercadolivre/MLCostCard.tsx | grep -c 'receitaMes -'` → **0** (nenhuma aritmética de margem sobrou no componente).
    - `grep -c 'margemContribuicao' src/components/mercadolivre/MLCostCard.tsx` ≥ 3 (prop + tipo + consumo).
    - `grep -c 'receitaBruta' src/components/mercadolivre/MLCostCard.tsx` ≥ 3.
    - `grep -c 'excluded' src/components/mercadolivre/MLCostCard.tsx` ≥ 1 (a linha informativa existe).
    - `grep -c 'pct(' src/components/mercadolivre/MLCostCard.tsx` mantém a contagem de antes do plano, e **todas** as chamadas continuam usando `receitaMes` como 2º argumento: `grep -c 'pct(.*, receitaBruta)' <arquivo>` → **0**.
    - `npx tsc --noEmit` → 0 erros; `npm run build` limpo.
  </acceptance_criteria>

  <done>O card exibe receita bruta + cancelamentos, recebe a margem pronta, renderiza o parcelamento como informativo e não contém mais nenhuma fórmula de margem.</done>
</task>

<task type="auto">
  <name>Task 3: [C1/C2/C5] MercadoLivre.tsx — compor bruto, total de tarifas excluded-aware, fórmula única</name>

  <read_first>
    - `src/pages/MercadoLivre.tsx:225-275` — eixo de mês (`billingMonthFrom`/`billingMonthTo`/`monthlyFrom`/`monthlyTo`), `dreWaterfall` (`:246`) e `receitaMes` (`:266`).
    - `src/pages/MercadoLivre.tsx:252-255` — `groupBillingCharges` destructurado. 🚨 **Repare que `totalTarifas` é destructurado aqui e NUNCA usado.**
    - `src/pages/MercadoLivre.tsx:337-357` — `gruposTarifasEfetivos` (fallback estimado) e `totalTarifasEfetivo`. 🚨 **Este é o furo que o RESEARCH não pegou:** `totalTarifasEfetivo` **re-soma o array de grupos** e ignora o `totalTarifas` do hook. É ele que vai para o card (`:861`) e para a fórmula da margem (`:365`). **Corrigir só `groupBillingCharges` não muda NADA na tela** — a terceira duplicação da phase.
    - `src/pages/MercadoLivre.tsx:359-371` — `margemContribuicao` e `buildDreCascade`.
    - `src/pages/MercadoLivre.tsx:857-880` — as props passadas ao `MLCostCard`.
  </read_first>

  <files>src/pages/MercadoLivre.tsx</files>

  <action>
    1. **Receita cancelada:** chame `useCancelledRevenue` (plano 96-04) **no mesmo eixo de mês do `dreWaterfall`** — mês corrente → `monthlyFrom`/`monthlyTo`; mês navegado → `billingMonthFrom`/`billingMonthTo`, exatamente a mesma condicional de `:233-236`. Um eixo diferente faz bruto e líquido discordarem. Derive `cancelamentosVendas` e `receitaBrutaMes = receitaMes + cancelamentosVendas`.

    2. **Unificar grupos + total num único memo.** Hoje `gruposTarifasEfetivos` (`:337-352`) e `totalTarifasEfetivo` (`:354-357`) são dois memos, e o segundo re-soma o array — descartando a exclusão do parcelamento que o 96-01 implementou. Substitua os dois por **um** memo que devolva `{ groups, totalTarifas }`:
       - com billing real (daily ou mensal): devolva o objeto de `groupBillingCharges` **inteiro** — `groups` e `totalTarifas` juntos. O `totalTarifas` do hook já exclui o parcelamento; é ele que tem que chegar ao card, não uma re-soma.
       - sem billing (fallback estimado de orders): mantenha os 7 grupos de hoje (`:343-351`, incluindo a linha de parcelamento com `amount: 0`) e devolva `totalTarifas` = comissão + frete + ads. O parcelamento estimado é 0, então a exclusão é inócua aqui — mas o shape do retorno tem que ser o mesmo nos dois ramos.
       Mantenha os nomes `gruposTarifasEfetivos` / `totalTarifasEfetivo` como saídas desse memo, para não espalhar renomeação por um arquivo de 982 linhas.

    3. **Fórmula única:** substitua a expressão inline de `margemContribuicao` (`:364-367`) por uma chamada a `computeMargemContribuicao` (Task 1), passando `receitaBruta: receitaBrutaMes`, `cancelamentosVendas`, `totalTarifas: totalTarifasEfetivo`, `cmvMes`, `impostosMes`. Mantenha o `useMemo` e as dependências corretas. O valor **não pode mudar** — é algebricamente o mesmo de hoje; o Test 2 do 96-05 prova isso, e `buildDreCascade` (`:368-371`) continua recebendo o mesmo número.

    4. **Props do card** (`:857-880`): acrescente `receitaBruta={receitaBrutaMes}`, `cancelamentosVendas={cancelamentosVendas}` e `margemContribuicao={margemContribuicao}`. `receitaMes={receitaMes}` **continua** (é a base dos `pct()`).

    **Não mexa** em `currentGrossProfit` (`:373-385`), `mcoInput` (`:390-413`) nem em nada que leia `monthlyCostWaterfall` — eles consomem `paid_revenue` com o significado antigo, que esta phase preserva de propósito. O bruto é **exclusivo do card do DRE**.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'computeMargemContribuicao' src/pages/MercadoLivre.tsx` ≥ 2 (import + uso).
    - `grep -v '^\s*[/*]' src/pages/MercadoLivre.tsx | grep -c 'receitaMes - totalTarifasEfetivo'` → **0** (a expressão inline duplicada morreu).
    - `grep -c 'useCancelledRevenue' src/pages/MercadoLivre.tsx` ≥ 2 (import + uso).
    - `grep -c 'receitaBruta=' src/pages/MercadoLivre.tsx` = 1 e `grep -c 'margemContribuicao=' src/pages/MercadoLivre.tsx` = 1.
    - `grep -c 'gruposTarifasEfetivos.reduce' src/pages/MercadoLivre.tsx` → **0** (a re-soma que descartava a exclusão do parcelamento morreu).
    - `grep -c 'receitaMes={receitaMes}' src/pages/MercadoLivre.tsx` = 1 (a base dos percentuais está preservada).
    - `git diff main -- src/pages/MercadoLivre.tsx | grep -c '^[+-].*mcoInput\|^[+-].*currentGrossProfit'` → **0** (MCO e GoalsCard intocados).
    - `npx tsc --noEmit` 0 erros; `npx vitest run` verde; `npm run build` limpo.
  </acceptance_criteria>

  <done>O card recebe bruto, cancelamentos e a margem calculada pela fonte única; o total de tarifas que chega à tela e à fórmula é o do hook (sem parcelamento); MCO/GoalsCard/`/financeiro` não foram tocados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → RPC `get_cancelled_revenue` | já mitigado no 96-04 (INVOKER + anti-IDOR provado); aqui é só consumo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-15 | Tampering | fórmula da margem duplicada | mitigate | a duplicação é **eliminada**: `computeMargemContribuicao` vira a única fonte; gate de grep prova que não sobrou aritmética no card nem inline na página |
| T-96-16 | Information Disclosure | receita bruta exibida | accept | dado da própria org, no card do próprio owner/membro |
| T-96-17 | Repudiation | número do card divergir do resto do app | mitigate | base dos `pct()` presa em `receitaMes` (líquida); `mcoInput`/`currentGrossProfit` intocados — gate de diff prova |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | **nenhum pacote novo neste plano** — zero instalações |
</threat_model>

<verification>
- `npx vitest run` verde (baseline + 6 novos de `dreMargem`).
- `npx tsc --noEmit` 0 erros; `npm run build` limpo.
- Gates de grep provando as 3 duplicações eliminadas (fórmula no card, fórmula inline na página, re-soma do total de tarifas).
</verification>

<success_criteria>
- SC1/SC2 no frontend: o card de maio mostra bruto 261.666,41, cancelamentos 14.450,29 e Total de tarifas 63.878,37; a margem parte de 247.216,12.
- A ARMADILHA do C1 fechada: teste automatizado prova que o cancelamento está na fórmula.
- Uma única fórmula de margem no código; um único total de tarifas.
</success_criteria>

<artifacts_this_plan_produces>
| Símbolo | Tipo | Onde |
|---|---|---|
| `computeMargemContribuicao` | função pura (nova) | `src/lib/dreMargem.ts` |
| `MargemContribuicaoInput` | interface (nova) | `src/lib/dreMargem.ts` |
| `src/lib/dreMargem.test.ts` | arquivo de teste (novo) | — |
| `MLCostCardProps.receitaBruta` | prop (nova, obrigatória) | `src/components/mercadolivre/MLCostCard.tsx` |
| `MLCostCardProps.cancelamentosVendas` | prop (nova, opcional) | `src/components/mercadolivre/MLCostCard.tsx` |
| `MLCostCardProps.margemContribuicao` | prop (nova, obrigatória) | `src/components/mercadolivre/MLCostCard.tsx` |
| `MLCostCardProps.lucro` (local) | **removido** | `src/components/mercadolivre/MLCostCard.tsx` |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-05-SUMMARY.md` when done
</output>
