---
phase: 96-dre-correcoes-linha-a-linha
plan: 03
type: execute
wave: 2
depends_on: ["96-02"]
files_modified:
  - supabase/migrations/2026XXXXXXXXXX_cmv_cheio_puro_and_gaps.sql
  - src/hooks/useCmvCheioGate.ts
autonomous: false
requirements: ["C6"]

must_haves:
  truths:
    - "O CMV da apuração é o custo cheio puro — pedidos sem custo cheio somam zero, nunca caem no custo médio disfarçado"
    - "O sistema sabe listar exatamente quais SKUs do mês estão sem custo cheio, com receita e unidades"
    - "A previsão (CMV médio) continua idêntica — nenhuma outra coluna do waterfall muda"
    - "Um JWT de outra org não vê os gaps da Pé Vermeio"
  artifacts:
    - path: "supabase/migrations/2026XXXXXXXXXX_cmv_cheio_puro_and_gaps.sql"
      provides: "get_cost_waterfall com cmv_cheio puro (DROP+CREATE) + get_cmv_cheio_gaps (RPC nova)"
      contains: "get_cmv_cheio_gaps"
    - path: "src/hooks/useCmvCheioGate.ts"
      provides: "hook que lê get_cmv_cheio_gaps no mesmo eixo de mês do waterfall"
      exports: ["useCmvCheioGate"]
  key_links:
    - from: "src/hooks/useCmvCheioGate.ts"
      to: "src/lib/dreCloseGate.ts"
      via: "import do tipo CmvCheioGap (o tipo mora no módulo puro, o hook re-exporta)"
      pattern: "CmvCheioGap"
    - from: "src/hooks/useCmvCheioGate.ts"
      to: "get_cmv_cheio_gaps"
      via: "supabase.rpc com p_org_id/p_user_ids/p_from/p_to"
      pattern: "get_cmv_cheio_gaps"
---

<objective>
[C6-backend] Matar o `COALESCE(custo_unit_cheio, custo_unit)` **só na apuração** e criar a RPC que lista os SKUs sem custo cheio.

Purpose: `get_cost_waterfall.cmv_cheio` hoje mascara custo faltante sem avisar — maio retorna 136.462,51 quando o cheio puro é 126.574,59. O autor original documentou o COALESCE como decisão deliberada ("sem ele, linhas sem cost_full somariam 0 → subestimariam o CMV"). **O C6 reverte essa decisão de propósito:** Wesley prefere bloquear o fechamento a mascarar. Os R$9.887,92 de diferença são a 2ª maior parcela do swing de maio.
Output: 1 migration (DROP+CREATE do waterfall + RPC nova de gaps) + 1 hook.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md
@supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql
@src/hooks/useMLCostWaterfall.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — cmv_cheio puro + get_cmv_cheio_gaps</name>

  <read_first>
    - `supabase/migrations/20260612120000_fix_cost_waterfall_fallback_and_upsert_preserve.sql:21-58` — a versão do `get_cost_waterfall` **que está no repo** (6 colunas, sem `cmv_cheio`). ⚠️ **A versão VIVA em prod tem 7 colunas** (drift do worktree irmão, confirmado no ADENDO do RESEARCH). O repo está atrás do banco — **nunca escreva a migration a partir do arquivo do repo.**
    - `supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql:1-33` — o padrão de header deste repo para migration de reconciliação de drift, e a lição do `42P13`: quando `RETURNS TABLE` muda de forma, é `DROP FUNCTION` + `CREATE`, nunca `CREATE OR REPLACE`. **E os GRANTs não sobrevivem ao DROP** → re-GRANT obrigatório.
    - `supabase/migrations/20260682000000_orders_price_timeseries_sku.sql` — o padrão de RPC de tenant deste repo: `SECURITY INVOKER` + `SET search_path TO 'public'` + `REVOKE ... FROM PUBLIC, anon` + `GRANT ... TO authenticated`.
    - `src/hooks/useMLCostWaterfall.ts:6-29,52-76` — o contrato que o frontend espera do waterfall.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-RESEARCH.md` seção `## 🚨 C.1 — Blast radius de get_cost_waterfall` — a tabela de 6 consumidores. **`cmv_cheio` é seguro (1 leitor). `paid_revenue` NÃO é (6 leitores).**
  </read_first>

  <files>supabase/migrations/2026XXXXXXXXXX_cmv_cheio_puro_and_gaps.sql</files>

  <action>
    ⚠️ **A numeração do arquivo é definida na Task 2, com o `max(version)` vivo em mãos.** Escreva o arquivo com um nome provisório e renomeie após o checkpoint — o repo está ~19 dias ATRÁS do banco (a branch da Phase 95 já usa `20260713140000`; o vivo é maior). Nunca numere pelo `ls` do repo.

    **Bloco A — `get_cost_waterfall`, cmv_cheio puro:**
    A definição correta é a **VIVA em prod**, não a do repo. O orquestrador vai colar o `pg_get_functiondef` no checkpoint da Task 2; escreva a migration a partir dele. A mudança é **cirúrgica e única**: na expressão da coluna `cmv_cheio`, remova o COALESCE **interno** (o que troca o custo cheio pelo médio quando o cheio é nulo), preservando o COALESCE **externo** (que transforma `SUM` de conjunto vazio em zero). `SUM` já ignora NULL por definição — o efeito passa a ser: linha sem custo cheio contribui zero, que é exatamente o comportamento que o gate do C6 vai denunciar em vez de mascarar.

    🚨 **TUDO O MAIS fica byte-a-byte idêntico ao corpo vivo:** as 7 colunas na mesma ordem, `paid_revenue` com o fallback `receita_bruta → preco_unit * quantidade`, `cmv` (médio) intocado, `total_comissao`, `total_frete`, `total_tax`, `orders_count`, o `WHERE` com `status IN ('paid','shipped','delivered')` e o cast de `data_pedido`. Mudar `paid_revenue` vazaria para `/financeiro`, MCO, GoalsCard e o Nexo; mudar `cmv` vazaria para a previsão e mataria o SC5.

    `DROP FUNCTION IF EXISTS public.get_cost_waterfall(uuid, text[], date, date);` + `CREATE FUNCTION`. Declare `SECURITY INVOKER` **explicitamente** (a versão viva não declara — o default do Postgres já é INVOKER, então está correta hoje, mas ao reescrever, explicite). Re-`GRANT EXECUTE ... TO authenticated` + `REVOKE ... FROM PUBLIC, anon`.

    **Bloco B — `get_cmv_cheio_gaps` (RPC nova):**
    ```
    CREATE FUNCTION public.get_cmv_cheio_gaps(p_org_id uuid, p_user_ids text[], p_from date, p_to date)
    RETURNS TABLE (sku text, marca text, linhas bigint, unidades bigint, receita numeric, tem_custo_medio boolean)
    LANGUAGE sql STABLE SECURITY INVOKER SET search_path TO 'public'
    ```
    Corpo: `SELECT o.sku, MAX(o.marca), count(*)::bigint, SUM(o.quantidade)::bigint, COALESCE(SUM(o.receita_bruta),0), bool_or(o.custo_unit IS NOT NULL) FROM public.orders o WHERE ... AND o.custo_unit_cheio IS NULL GROUP BY o.sku ORDER BY COALESCE(SUM(o.receita_bruta),0) DESC`.

    🚨 **O `WHERE` desta RPC (fora a condição do custo cheio nulo) tem que ser IDÊNTICO ao do `get_cost_waterfall`** — mesmo filtro de org, mesmo `ml_user_id = ANY(p_user_ids)`, mesmo `status IN (...)`, **mesmo cast de `data_pedido`**. Se os dois conjuntos de pedidos divergirem, o gate e o CMV discordam e o número não fecha. Copie o predicado do corpo vivo, não reescreva de memória.

    `tem_custo_medio` distingue os dois casos na tela: "tem médio, falta o cheio" (34 SKUs de maio, resolvidos pelo backfill do 96-07) vs "não tem custo nenhum" (os 4 SKUs que o Wesley cadastra no Tiny manualmente).

    Agregação simples com `GROUP BY` — **sem subquery correlacionada** (essas estouram o `statement_timeout` de 8s do role `authenticated` em RPC INVOKER). `REVOKE ... FROM PUBLIC, anon` + `GRANT ... TO authenticated`.

    **Header do arquivo:** documente (a) que a versão viva do waterfall é drift e esta migration a reconcilia no repo, (b) que o COALESCE interno era deliberado e está sendo revertido por decisão do dono, (c) o alvo numérico de maio: `cmv_cheio` 136.462,51 → 126.574,59.
  </action>

  <verify>
    <automated>test -f supabase/migrations/*cmv_cheio_puro_and_gaps.sql && grep -q 'DROP FUNCTION IF EXISTS public.get_cost_waterfall' supabase/migrations/*cmv_cheio_puro_and_gaps.sql && grep -q 'SECURITY INVOKER' supabase/migrations/*cmv_cheio_puro_and_gaps.sql && grep -q 'GRANT EXECUTE' supabase/migrations/*cmv_cheio_puro_and_gaps.sql && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - O arquivo existe e contém `DROP FUNCTION IF EXISTS public.get_cost_waterfall(` seguido de `CREATE FUNCTION public.get_cost_waterfall(` — nunca `CREATE OR REPLACE` (evita `42P13`).
    - `grep -c 'GRANT EXECUTE' <arquivo>` ≥ 2 (re-GRANT do waterfall após o DROP + GRANT da RPC nova).
    - `grep -c 'REVOKE' <arquivo>` ≥ 1.
    - `grep -c 'SECURITY INVOKER' <arquivo>` = 2 (as duas funções declaram explicitamente).
    - `grep -c 'get_cmv_cheio_gaps' <arquivo>` ≥ 3 (CREATE + REVOKE + GRANT).
    - `grep -c 'orders_count' <arquivo>` ≥ 1 — as 7 colunas do waterfall foram preservadas.
    - As colunas `paid_revenue`, `cmv`, `total_comissao`, `total_frete`, `total_tax`, `orders_count` do bloco A são textualmente idênticas às do `pg_get_functiondef` colado no checkpoint (diff manual = só a linha do `cmv_cheio`).
  </acceptance_criteria>

  <done>Migration pronta em disco com os dois blocos, grants, revokes e header explicando o drift. Ainda NÃO aplicada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — orquestrador numera, aplica e prova a migration via MCP</name>
  <action>Checkpoint do orquestrador: aplicar a migration via MCP Supabase e rodar as 8 provas descritas em `<how-to-verify>`. Nenhum código é escrito nesta task.</action>
  <what-built>
    A migration `cmv_cheio_puro_and_gaps` está escrita em disco mas **não aplicada**. `gsd-executor` não aplica migration — quem aplica é o orquestrador, via MCP Supabase (`apply_migration`) no projeto **`ckcdevcxgvueywivefgx`** (NÃO o do CLAUDE.md). **Nunca `supabase db push`** — o CLI local está linkado no projeto errado (`gionpsuunfkkzzjdubfy`).
  </what-built>

  <how-to-verify>
    Cada consulta é **single-statement** — o `execute_sql` do MCP só devolve o 1º result set.

    **PASSO 0 — numerar (obrigatório ANTES de aplicar):**
    ```sql
    SELECT max(version) FROM supabase_migrations.schema_migrations;
    ```
    Renomeie o arquivo para um timestamp ACIMA do retornado (esperado ~`20260713xxxxxx`; use `20260715xxxxxx`). O repo está atrás do banco — numerar pelo `ls` do repo aplica a migration "no passado" e quebra a ordem.

    **PASSO 1 — capturar o corpo VIVO antes de mexer** (é a fonte da migration; cole no summary):
    ```sql
    SELECT pg_get_functiondef('public.get_cost_waterfall(uuid,text[],date,date)'::regprocedure);
    ```
    Confirme o COALESCE interno na coluna `cmv_cheio` e as 7 colunas. Se divergir do que a Task 1 assumiu, **corrija a migration antes de aplicar**.

    **PASSO 2 — baseline de maio ANTES (guarde os 7 números):**
    ```sql
    SELECT * FROM public.get_cost_waterfall('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date);
    ```
    Alvo: `cmv_cheio` = **136462.51**.

    **PASSO 3 — aplicar** via `apply_migration`.

    **PASSO 4 — prova do C6 (o número do SC1):**
    ```sql
    SELECT * FROM public.get_cost_waterfall('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date);
    ```
    - `cmv_cheio` = **126574.59** (era 136462.51 — delta **9887.92**, a 2ª parcela do swing).
    - `paid_revenue` = **247216.12** e as outras 5 colunas **idênticas ao PASSO 2**. Qualquer divergência aqui = vazamento para `/financeiro`/MCO/GoalsCard/Nexo → **reverter**.

    **PASSO 5 — prova do SC3 (39 SKUs):**
    ```sql
    SELECT count(*) AS skus, SUM(linhas) AS linhas, ROUND(SUM(receita),2) AS receita, count(*) FILTER (WHERE NOT tem_custo_medio) AS sem_custo_nenhum FROM public.get_cmv_cheio_gaps('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date);
    ```
    Alvo: **39 SKUs / 226 linhas / R$23.828,31 / 4 sem custo nenhum**. Se der 34 ou menos, o backfill do 96-07 já rodou fora de ordem — **pare** (o SC3 precisa ser medido antes do backfill).

    **PASSO 6 — coerência gate × waterfall** (o conjunto de pedidos tem que ser o mesmo):
    ```sql
    SELECT ROUND((SELECT cmv_cheio FROM public.get_cost_waterfall('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date)),2) AS cheio_puro, ROUND((SELECT SUM(receita) FROM public.get_cmv_cheio_gaps('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date)),2) AS receita_sem_cheio;
    ```
    Alvo: 126574.59 e 23828.31. Se `receita_sem_cheio` vier NULL ou 0, o predicado da RPC nova divergiu do waterfall.

    **PASSO 7 — anti-IDOR (obrigatório, RPC nova):** impersone o JWT da Pé Vermeio (`7f615df7-...`) e chame `get_cmv_cheio_gaps` com `p_org_id` da org **Thales** (`e4150d57-...`) → **0 linhas**. Use `SET LOCAL ROLE authenticated` + `set_config('request.jwt.claims', ..., true)` em transação com ROLLBACK (o `service_role` bypassa RLS — testar como postgres não prova nada). Single-statement via `LATERAL` (padrão da Phase 79). Prova de controle: a mesma impersonação com a própria org → 39 linhas.

    **PASSO 8 — advisors:** `get_advisors(type: "security")` — nenhum issue NOVO atribuível às 2 funções.
  </how-to-verify>

  <resume-signal>Cole no chat: o `max(version)` usado, os 7 números do PASSO 2 e do PASSO 4 lado a lado, o resultado do PASSO 5 e a prova anti-IDOR do PASSO 7. Depois digite "aplicada" ou descreva a divergência.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Hook useCmvCheioGate</name>

  <read_first>
    - `src/hooks/useMLCostWaterfall.ts:31-81` — o padrão exato de hook de RPC deste repo: `useMLStore().resolvedMLUserIds` + `useOrganization().currentOrg`, `supabase.rpc`, queryKey `["ml", <nome>, orgId, resolvedMLUserIds, from, to]`, `enabled` com guard de org+lojas. **Copie a forma daqui.**
    - `src/lib/dreCloseGate.ts` (criado no 96-02) — o tipo `CmvCheioGap` **mora lá**; este hook importa e re-exporta, mesmo padrão de `useDreOperational` com `DreOperationalRow` de `dreCascade.ts`.
    - `src/pages/MercadoLivre.tsx:225-249` — de onde vêm `billingMonthFrom`/`billingMonthTo`/`monthlyFrom`/`monthlyTo`. O hook recebe `from`/`to` prontos (o eixo de mês é resolvido pelo caller no 96-06), igual ao `useMLCostWaterfall`.
    - `src/hooks/useImpostoGuiaReal.ts:65` — o precedente de mapear a linha crua da RPC com `(r: any)`. Este repo **não regenera `types.ts`** para RPC nova (precedente aceito na Phase 90-04) — siga o padrão.
  </read_first>

  <files>src/hooks/useCmvCheioGate.ts (novo)</files>

  <action>
    Crie `src/hooks/useCmvCheioGate.ts` espelhando a forma de `useMLCostWaterfall`:

    - `export type { CmvCheioGap } from "@/lib/dreCloseGate";` (re-export — o tipo é do módulo puro).
    - `export function useCmvCheioGate(from: string, to: string)` → `useQuery<CmvCheioGap[]>`.
    - queryKey: `["ml", "cmv-cheio-gaps", orgId, resolvedMLUserIds, from, to]`.
    - `queryFn`: `supabase.rpc("get_cmv_cheio_gaps", { p_org_id: orgId, p_user_ids: resolvedMLUserIds, p_from: from.substring(0,10), p_to: to.substring(0,10) })` — o `substring(0,10)` é o mesmo tratamento de `useMLCostWaterfall.ts:44-45`. Mapeie `snake_case` → `camelCase` (`tem_custo_medio` → `temCustoMedio`), `Number()` nos numéricos, `marca` pode vir null.
    - **Sem o guard `if (paid_revenue === 0) return null`** de `useMLCostWaterfall.ts:62`: lista vazia aqui significa "nenhum gap" (mês liberado), que é um estado válido e diferente de "sem dado". Retorne `[]`, nunca `null`.
    - `enabled: !!orgId && resolvedMLUserIds.length > 0 && !!from && !!to`; `staleTime: 5 * 60 * 1000` (igual ao waterfall).
    - Comentário de cabeçalho: por que este hook existe (o gate do C6 não dá para derivar client-side — puxar todos os pedidos do mês esbarraria no truncamento de 1000 linhas do PostgREST) e que ele **nunca** alimenta `resolveDreRegime` — só o caminho de fechar o mês (Pitfall 7).

    **Não** consuma este hook em lugar nenhum ainda — a fiação é do 96-06.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` → 0 erros.
    - `grep -c 'get_cmv_cheio_gaps' src/hooks/useCmvCheioGate.ts` = 1.
    - `grep -c 'CmvCheioGap' src/hooks/useCmvCheioGate.ts` ≥ 2 (import/re-export + tipo do useQuery).
    - `grep -c 'return null' src/hooks/useCmvCheioGate.ts` → 0 (lista vazia ≠ ausência de dado).
    - `grep -rn 'useCmvCheioGate' src/pages src/components` → sem resultados (a fiação é do 96-06).
    - `npx vitest run` → suíte verde, sem regressão.
  </acceptance_criteria>

  <done>Hook tipado, org-scoped, alinhado ao eixo de mês do waterfall, devolvendo `CmvCheioGap[]` e nunca `null`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → RPC `get_cmv_cheio_gaps` | `p_org_id` vem do cliente — só a RLS de `orders` impede ler outra org |
| navegador → RPC `get_cost_waterfall` | idem; função reescrita nesta phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-06 | Elevation of Privilege / IDOR | `get_cmv_cheio_gaps` | mitigate | `SECURITY INVOKER` + `SET search_path TO 'public'`; DEFINER com param de org seria IDOR direto. Prova obrigatória no PASSO 7 (JWT Pé Vermeio × org Thales = 0 linhas) |
| T-96-07 | Elevation of Privilege | `get_cost_waterfall` reescrita | mitigate | o DROP apaga os GRANTs; re-GRANT só para `authenticated` + `REVOKE FROM PUBLIC, anon` — sem isso o default de EXECUTE reabre a função para `anon` |
| T-96-08 | Information Disclosure | `sku`/`marca`/`receita` na resposta | accept | dado da própria org, para o próprio owner/membro; RLS de `orders` já é o limite |
| T-96-09 | Denial of Service | timeout de 8s do role `authenticated` | mitigate | `GROUP BY` simples, sem subquery correlacionada; o cast de `data_pedido` (TEXT) cega índice — se estourar, pré-filtrar por comparação de string ISO antes do cast |
| T-96-10 | Tampering | DROP+CREATE em função de 6 consumidores | mitigate | só a expressão de `cmv_cheio` muda; PASSO 2 × PASSO 4 comparam as outras 6 colunas antes/depois. Divergência = reverter |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | **nenhum pacote novo neste plano** — zero instalações |
</threat_model>

<verification>
- Prova de que `cmv_cheio` de maio caiu 136.462,51 → 126.574,59 e que as outras 6 colunas não se moveram.
- Prova de 39 SKUs / 226 linhas / R$23.828,31 / 4 sem custo nenhum.
- Prova anti-IDOR como role real (`authenticated`), não como `postgres`.
- `npx tsc --noEmit` + `npx vitest run` verdes.
</verification>

<success_criteria>
- SC1 (parcela CMV): delta de 9.887,92 provado em prod.
- SC3: a RPC lista os 39 SKUs com receita e unidades, e distingue "sem custo cheio" de "sem custo nenhum".
- SC5: `cmv` (médio), `paid_revenue` e as demais colunas byte-a-byte iguais → previsão intacta.
</success_criteria>

<artifacts_this_plan_produces>
| Símbolo | Tipo | Onde |
|---|---|---|
| `get_cost_waterfall(uuid,text[],date,date)` | RPC **reescrita** (coluna `cmv_cheio` sem COALESCE interno) | prod `ckcdevcxgvueywivefgx` + migration |
| `get_cmv_cheio_gaps(uuid,text[],date,date)` | RPC **nova** | prod + migration |
| coluna `tem_custo_medio` | coluna de retorno (nova) | `get_cmv_cheio_gaps` |
| `useCmvCheioGate` | hook (novo) | `src/hooks/useCmvCheioGate.ts` |
| `2026XXXXXXXXXX_cmv_cheio_puro_and_gaps.sql` | migration (nova, numerada no checkpoint) | `supabase/migrations/` |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-03-SUMMARY.md` when done
</output>
