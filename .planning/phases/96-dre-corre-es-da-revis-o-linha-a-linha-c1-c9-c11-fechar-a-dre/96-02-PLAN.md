---
phase: 96-dre-correcoes-linha-a-linha
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dreCloseGate.ts
  - src/lib/dreCloseGate.test.ts
  - src/lib/dreRegime.ts
  - src/hooks/useImpostoGuiaReal.ts
  - src/lib/dreCascade.test.ts
autonomous: true
requirements: ["C7", "C11"]

must_haves:
  truths:
    - "O sistema só considera o imposto apurado quando as 3 guias da competência M+1 estão com status pago"
    - "Uma guia de R$0,01 paga NÃO bloqueia a apuração (0,01 = apurado, deu zero por crédito)"
    - "Um mês com qualquer guia pendente é rejeitado, por maior que seja o valor"
    - "O INSS continua no bloco Pessoal e nenhum valor dele migra para a linha de impostos"
  artifacts:
    - path: "src/lib/dreCloseGate.ts"
      provides: "canApurarImposto + resolveCloseGate + tipo CmvCheioGap — lógica pura do gate de fechamento"
      exports: ["canApurarImposto", "resolveCloseGate", "CmvCheioGap", "CloseGateResult"]
      min_lines: 60
    - path: "src/lib/dreCloseGate.test.ts"
      provides: "provas do SC4 (0,01 passa / pending rejeita / categoria duplicada rejeita)"
      min_lines: 70
  key_links:
    - from: "src/lib/dreCloseGate.ts"
      to: "src/lib/dreRegime.ts"
      via: "import de IMPOSTO_VENDA_CATEGORIES (fonte única — hoje há 2 cópias)"
      pattern: "IMPOSTO_VENDA_CATEGORIES"
---

<objective>
[C7] Gate de imposto pelo **status**, nunca pelo valor. [C11] Travar o INSS no bloco Pessoal com teste de regressão.

Purpose: Wesley confirmou que PIS/COFINS de maio geraram CRÉDITO — não houve guia a pagar. O `0,01` significa *"apurado, deu zero"*, não placeholder. Um gate que bloqueasse em `0,01` travaria maio à toa. O placeholder real é `status='pending'` (o Tiny repete PIS 716,19 + COFINS 3.298,87 idênticos todo mês de jul/2026 a abr/2027).
Output: `src/lib/dreCloseGate.ts` — toda a lógica pura do gate de fechamento (imposto + CMV cheio), testável sem banco. O plano 96-06 só a liga na tela.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [C7] canApurarImposto + resolveCloseGate (módulo puro, TDD)</name>

  <read_first>
    - `src/lib/dreRegime.ts:27-57` — `DreRegime`, a const local `IMPOSTO_VENDA_CATEGORIES` (`:30-34`, hoje **não exportada**), `monthPlusOne`, `GuiaRealCategoryTotal`.
    - `src/lib/dreRegime.ts:94-133` — `resolveDreRegime`. **Leia para NÃO tocar.** O branch `!isClosed` (`:111-117`) nunca lê `cmvCheio`/`guiaReal` — é essa propriedade estrutural que garante o SC5 (previsão byte-a-byte). O gate vive fora dele.
    - `src/hooks/useImpostoGuiaReal.ts:35-39` — a **segunda cópia** da mesma lista de categorias. Duas cópias divergindo é o risco que esta task fecha.
    - `src/hooks/useImpostoGuiaReal.ts:59-69` — a RPC `get_imposto_guia_by_competence` **já retorna `status`**. Nenhuma mudança de backend é necessária para o C7.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md` seção `### [C7]`.
  </read_first>

  <files>src/lib/dreCloseGate.ts (novo), src/lib/dreCloseGate.test.ts (novo), src/lib/dreRegime.ts, src/hooks/useImpostoGuiaReal.ts</files>

  <behavior>
    `canApurarImposto(guia: GuiaRealCategoryTotal[] | null): ImpostoGateResult`
    - Test 1 (maio PASSA — a prova de que o valor não é o sinal): `[{ICMS, 4793.21, "paid"}, {PIS, 0.01, "paid"}, {COFINS, 0.01, "paid"}]` → `{ ok: true, missing: [], pending: [] }`.
    - Test 2 (o placeholder real): `[{ICMS, 12000, "pending"}, {PIS, 716.19, "pending"}, {COFINS, 3298.87, "pending"}]` → `ok: false`, `pending` com as 3 categorias. Repare que os valores são ALTOS e mesmo assim rejeita — o sinal é o status.
    - Test 3 (categoria duplicada — a RPC agrupa por categoria×status): `[{ICMS, 4000, "paid"}, {ICMS, 793.21, "pending"}, {PIS, 0.01, "paid"}, {COFINS, 0.01, "paid"}]` → `ok: false`, `pending: ["Imposto Venda - ICMS"]`. Uma linha não-paga contamina a categoria inteira.
    - Test 4 (guia faltando): sem COFINS → `ok: false`, `missing: ["Imposto Venda - COFINS"]`.
    - Test 5: `null` e `[]` → `ok: false`, `missing` com as 3 categorias, `pending: []`.
    - Test 6 (blindagem contra a regressão do C7): guia toda paga com todos os totais em `0.01` → `ok: true`. Nenhum ramo da função pode olhar para `total`.

    `resolveCloseGate({ gaps, guia }): CloseGateResult`
    - Test 7 (maio hoje): `gaps` com 39 entradas somando `receita` 23828.31 + guia toda paga → `{ blocked: true, skusFaltando: 39, receitaFaltando: 23828.31 }` e `reasons` com exatamente 1 motivo (o do CMV).
    - Test 8 (os dois motivos): gaps não-vazio + guia pendente → `blocked: true`, `reasons.length === 2`.
    - Test 9 (liberado): `gaps: []` + guia toda paga → `{ blocked: false, reasons: [] }`.
    - Test 10 (dado ainda carregando): `gaps: null` → `blocked: true` (fail-closed: não libera fechar sem saber).
  </behavior>

  <action>
    RED primeiro: escreva `src/lib/dreCloseGate.test.ts` com os 10 casos, commit só do teste, rode e confirme o vermelho. Depois implemente.

    1. Em `src/lib/dreRegime.ts:30`, adicione a palavra-chave `export` à const das 3 categorias de imposto sobre venda. **É a ÚNICA mudança permitida neste arquivo neste plano** — zero alteração de lógica, zero alteração em `resolveDreRegime`. Os 18 testes de `dreRegime.test.ts` continuarem verdes é a prova barata do SC5.
    2. Em `src/hooks/useImpostoGuiaReal.ts`, apague a cópia local da lista (`:35-39`) e importe a const de `@/lib/dreRegime`. O arquivo já importa `monthPlusOne` de lá (`:30`) — só estenda o import. Uma fonte, não duas.
    3. Crie `src/lib/dreCloseGate.ts` — módulo puro, **sem import de React nem de Supabase** (padrão de `src/lib/dreCascade.ts` e `dreRegime.ts`). Exporte:
       - `interface ImpostoGateResult { ok: boolean; missing: string[]; pending: string[] }`
       - `function canApurarImposto(guia: GuiaRealCategoryTotal[] | null): ImpostoGateResult` — agrupe as linhas por `category`; `missing` = categorias da lista ausentes; `pending` = categorias presentes em que ALGUMA linha tem status diferente de `"paid"`; `ok` = `missing` e `pending` ambos vazios. **A função nunca lê o campo `total` de nenhuma linha** — o valor não é sinal de nada.
       - `interface CmvCheioGap { sku: string; marca: string | null; linhas: number; unidades: number; receita: number; temCustoMedio: boolean }` — o tipo mora aqui e é re-exportado pelo hook do plano 96-03, mesmo padrão de `DreOperationalRow` em `dreCascade.ts:17`.
       - `interface CloseGateResult { blocked: boolean; reasons: string[]; impostoMissing: string[]; impostoPending: string[]; skusFaltando: number; receitaFaltando: number }`
       - `function resolveCloseGate(input: { gaps: CmvCheioGap[] | null; guia: GuiaRealCategoryTotal[] | null }): CloseGateResult` — bloqueia se `gaps` é null (fail-closed), se `gaps.length > 0`, ou se `canApurarImposto` reprovar. `reasons` são frases pt-BR curtas e acionáveis prontas para tooltip (ex.: "N SKUs sem custo cheio (R$X da receita)" e "Guia de {categorias} ainda não paga na competência M+1").
       - `receitaFaltando` = soma de `gaps[].receita`, arredondada a 2 casas (helper `round2` local, igual a `dreCascade.ts:96`).
    4. Cabeçalho do arquivo: comente **por que** o gate mora aqui e não em `resolveDreRegime` (Pitfall 7 do RESEARCH — o gate dentro do resolver mudaria a previsão e quebraria o SC5) e **por que** o status é o sinal e não o valor (Pitfall 8).
  </action>

  <verify>
    <automated>npx vitest run src/lib/dreCloseGate.test.ts src/lib/dreRegime.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/lib/dreCloseGate.test.ts` → 10/10 passam.
    - `npx vitest run src/lib/dreRegime.test.ts` → **18/18 passam** (prova do SC5 — a lógica do resolver não foi tocada).
    - `git diff main -- src/lib/dreRegime.ts | grep -c '^[+-]' ` ≤ 2 — a única mudança no resolver é a palavra `export` (1 linha removida + 1 adicionada).
    - `grep -c 'Imposto Venda - ICMS' src/hooks/useImpostoGuiaReal.ts` → **0** (a cópia local morreu; o arquivo importa a lista).
    - `grep -rc 'react\|supabase' src/lib/dreCloseGate.ts` → 0 (módulo puro).
    - `npx tsc --noEmit` → 0 erros; `npx vitest run` → suíte completa verde.
  </acceptance_criteria>

  <done>`canApurarImposto` aprova maio (3 guias pagas, duas delas com R$0,01) e rejeita qualquer competência com linha pendente, sem jamais ler o valor. `resolveCloseGate` combina o gate de imposto com o de CMV cheio e falha fechado quando o dado ainda não chegou.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [C11] Teste de regressão — INSS fica no bloco Pessoal</name>

  <read_first>
    - `src/lib/dreCascade.ts:20-62` — `DreBloco`, `DreOperationalRow`, `OPERACIONAL_BLOCOS`, `BLOCO_LABELS`.
    - `src/lib/dreCascade.ts:105-127` — `buildDreCascade` e o guardrail SC-3 (`:110-112`) que filtra `impostos_venda`/`excluido` ANTES de somar.
    - `src/lib/dreCascade.test.ts:1-30` — helper `row()` e a convenção dos describes.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md` seção `### [C11]`.
  </read_first>

  <files>src/lib/dreCascade.test.ts</files>

  <behavior>
    Novo `describe` em `dreCascade.test.ts` — **C11 é uma NÃO-mudança**: nenhuma linha de `dreCascade.ts` muda. O teste é a trava que impede alguém de "consertar" o mapa depois. Os testes devem passar em VERDE já na primeira execução; se falharem, o C11 não é mais uma não-mudança e o plano precisa parar e reportar.
    - Test 1: linhas `{bloco:'pessoal', category:'Salários', total:24000}` + `{bloco:'pessoal', category:'Pessoal - INSS', total:3852.19}` → a linha agregada do bloco `pessoal` soma **27852.19** (o número real de maio/2026) e traz o label "Pessoal".
    - Test 2: a mesma fixture não produz nenhuma linha de imposto na cascata — `OPERACIONAL_BLOCOS` não contém `impostos_venda`, e nenhuma linha do resultado carrega esse bloco.
    - Test 3: uma linha `{bloco:'impostos_venda', category:'Imposto Venda - ICMS', total:16015.06}` misturada na fixture é filtrada pelo guardrail e **não** altera `resultadoOperacional` (compare o resultado com e sem ela — devem ser idênticos).
    - Test 4: o INSS entra no `resultadoOperacional` DEPOIS da margem de contribuição (é dedução operacional), não antes — com margem 51969.93 e só as 2 linhas de pessoal, `resultadoOperacional === 24117.74`.
  </behavior>

  <action>
    Adicione o describe ao final de `src/lib/dreCascade.test.ts` usando o helper `row()` já existente (ele aceita `category` em `opts`). Use o comentário do describe para registrar POR QUE o teste existe: o INSS é encargo de folha, não imposto sobre venda; movê-lo para a linha de impostos seria dupla contagem, porque a linha de impostos do card já consome a guia real de ICMS/PIS/COFINS da competência M+1.

    **Não edite `src/lib/dreCascade.ts`.** Se algum dos 4 testes falhar, pare e reporte — significa que o mapa do backend divergiu do esperado e o C11 deixou de ser uma não-mudança.
  </action>

  <verify>
    <automated>npx vitest run src/lib/dreCascade.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/lib/dreCascade.test.ts` → todos passam, incluindo os 4 novos, **sem nenhuma edição em `src/lib/dreCascade.ts`**.
    - `git diff --name-only main -- src/lib/dreCascade.ts` → saída vazia (o módulo não foi tocado).
    - `grep -c 'Pessoal - INSS' src/lib/dreCascade.test.ts` ≥ 1.
  </acceptance_criteria>

  <done>4 testes verdes travam o INSS no bloco Pessoal e provam que nenhum valor de `impostos_venda` entra nos subtotais — SC6 coberto por teste automatizado, sem tocar em código de produção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma | os dois módulos são puros; não fazem I/O, não recebem entrada de rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04 | Elevation of Privilege | `resolveCloseGate` | accept | o gate é **UX**, não segurança — a RLS de `dre_month_close` (owner-only INSERT) é a autoridade. Um owner com curl fecha o mês faltando custo. Decisão consciente registrada em 96-RESEARCH §C6, não esquecimento |
| T-96-05 | Tampering | `canApurarImposto` | mitigate | fail-closed em entrada nula/vazia (Test 5/10): ausência de dado nunca vira permissão |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | **nenhum pacote novo neste plano** — zero instalações |
</threat_model>

<verification>
- `npx vitest run` verde (baseline ~542 + 14 novos).
- `npx tsc --noEmit` 0 erros.
- `src/lib/dreRegime.ts` com diff de exatamente 1 palavra-chave; 18/18 testes verdes.
</verification>

<success_criteria>
- SC4 provado em teste puro: maio (3 guias pagas, duas em R$0,01) → aprova; competência com `pending` → rejeita; `0,01` nunca bloqueia.
- SC6 provado em teste puro: INSS soma 27.852,19 no bloco Pessoal; `impostos_venda` não entra em subtotal nenhum.
- `resolveDreRegime` intocado.
</success_criteria>

<artifacts_this_plan_produces>
| Símbolo | Tipo | Arquivo |
|---|---|---|
| `canApurarImposto` | função pura (nova) | `src/lib/dreCloseGate.ts` |
| `resolveCloseGate` | função pura (nova) | `src/lib/dreCloseGate.ts` |
| `ImpostoGateResult` | interface (nova) | `src/lib/dreCloseGate.ts` |
| `CloseGateResult` | interface (nova) | `src/lib/dreCloseGate.ts` |
| `CmvCheioGap` | interface (nova — re-exportada pelo hook do 96-03) | `src/lib/dreCloseGate.ts` |
| `IMPOSTO_VENDA_CATEGORIES` | const (passa a ser exportada) | `src/lib/dreRegime.ts` |
| `src/lib/dreCloseGate.test.ts` | arquivo de teste (novo) | — |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-02-SUMMARY.md` when done
</output>
