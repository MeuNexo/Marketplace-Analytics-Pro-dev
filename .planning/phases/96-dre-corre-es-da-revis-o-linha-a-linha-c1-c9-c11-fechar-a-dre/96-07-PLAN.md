---
phase: 96-dre-correcoes-linha-a-linha
plan: 07
type: execute
wave: 5
depends_on: ["96-06"]
files_modified:
  - supabase/migrations/2026XXXXXXXXXX_backfill_custo_unit_cheio_rerun.sql
  - supabase/functions/sync-tiny-costs/index.ts
  - supabase/functions/recalc-order-costs/index.ts
autonomous: false
requirements: ["C6"]

must_haves:
  truths:
    - "Os SKUs de maio que já têm custo cheio cadastrado no Tiny passam a ter custo cheio nos pedidos"
    - "O custo médio (custo_unit) não é tocado por nada neste plano"
    - "Pedidos novos passam a receber custo cheio na ingestão — a cobertura para de congelar"
  artifacts:
    - path: "supabase/migrations/2026XXXXXXXXXX_backfill_custo_unit_cheio_rerun.sql"
      provides: "backfill idempotente de orders.custo_unit_cheio a partir de ml_product_costs.cost_full"
      contains: "custo_unit_cheio IS NULL"
  key_links:
    - from: "orders.custo_unit_cheio"
      to: "ml_product_costs.cost_full"
      via: "join por SKU (o.sku = pc.seller_sku), escopado por organization_id"
      pattern: "seller_sku"
---

<objective>
[C6-backfill] Fechar 34 dos 39 SKUs de maio re-rodando o backfill do `custo_unit_cheio`, e parar o congelamento da cobertura na ingestão.

Purpose: sem o backfill, o gate do 96-06 bloqueia maio para sempre — o sistema para de mentir, mas também para de fechar. O ADENDO do RESEARCH verificou no banco vivo: dos 39 SKUs, **35 existem em `ml_product_costs` e 34 já têm `cost_full`** → o backfill (idempotente) fecha 34 na hora. Os 4 restantes são **exatamente** os 4 SKUs da tarefa manual já entregue ao Wesley no Tiny. O resíduo do backfill é precisamente o trabalho dele.

**Autorização (Wesley, 2026-07-15):** *"Pode confiar nos valores que estão na Tiny, no que está preço custo e o cheio, e o preço custo médio é o descontado automaticamente ou manualmente o ICMS, pis e cofins do produto."* Isso valida o Cenário A do ADENDO: a razão cheio/médio de 1,2522 em 261 produtos (`= 1/(1−0,2014)`) é consequência da estrutura tributária, **não** de um `médio × fator` no nosso código. O código já lê os dois campos separados do Tiny e o backfill só **copia** — a restrição "nunca médio×fator" **não é violada**.
Output: 2 EFs alinhadas ao que já roda em prod + 1 migration de backfill.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint — a cobertura congelou? as EFs vivas gravam o custo cheio?</name>
  <action>Checkpoint do orquestrador: rodar as 4 verificações de `<how-to-verify>` via MCP Supabase e decidir se a Task 2 é necessária ou no-op. Nenhum código é escrito nesta task.</action>
  <what-built>
    **Nada ainda — este checkpoint decide o tamanho do plano.** O RESEARCH deixou 2 lacunas abertas que só o banco vivo responde, e a resposta muda o que as Tasks 2/3 fazem. O orquestrador roda via MCP Supabase em `ckcdevcxgvueywivefgx`; `gsd-executor` não tem MCP.
  </what-built>

  <how-to-verify>
    Single-statement cada.

    **PASSO 1 — a cobertura congelou mesmo? (lacuna A.5 do RESEARCH):**
    ```sql
    SELECT date_trunc('month', o.data_pedido::timestamptz)::date AS mes, count(*) FILTER (WHERE o.custo_unit_cheio IS NULL) AS linhas_sem_cheio, count(*) AS linhas, ROUND(100.0 * count(*) FILTER (WHERE o.custo_unit_cheio IS NOT NULL) / NULLIF(count(*),0), 1) AS pct_cobertura FROM public.orders o WHERE o.organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid AND o.status IN ('paid','shipped','delivered') AND o.data_pedido::timestamptz >= '2026-01-01' GROUP BY 1 ORDER BY 1;
    ```
    **A leitura que importa:** se a cobertura **despencar nos meses recentes** (jun/jul perto de 0%), a EF `recalc-order-costs` viva **não escreve** `custo_unit_cheio` — o valor de hoje é um snapshot de um backfill antigo, e a Task 2 é obrigatória (senão o gate bloqueia junho e julho para sempre, e a phase entrega um sistema que nunca fecha). Se a cobertura for estável ~90% em todos os meses, a EF já escreve e a Task 2 vira no-op.

    **PASSO 2 — versão viva das EFs:** `list_edge_functions` e compare `sync-tiny-costs` e `recalc-order-costs` com o repo. O repo (`sync-tiny-costs/index.ts:161-163`) colapsa os dois preços num só e **nunca grava `cost_full`**; `recalc-order-costs/index.ts:97,111,143-147` **nunca grava `custo_unit_cheio`**. Mas o ADENDO provou que **634 de 635 linhas de `ml_product_costs` TÊM `cost_full` em prod** → a EF viva de `sync-tiny-costs` está à frente do repo (drift). Determine, para cada uma das 2 EFs: **viva à frente do repo, ou repo == vivo?**

    **PASSO 3 — `cost_full` disponível para os SKUs de maio (o alvo do backfill):**
    ```sql
    SELECT count(*) AS skus_gap, count(*) FILTER (WHERE pc.seller_sku IS NOT NULL) AS existem_em_costs, count(*) FILTER (WHERE pc.cost_full IS NOT NULL) AS tem_cost_full FROM public.get_cmv_cheio_gaps('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date) g LEFT JOIN public.ml_product_costs pc ON pc.seller_sku = g.sku AND (pc.organization_id IS NULL OR pc.organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid);
    ```
    Alvo (ADENDO): **39 / 35 / 34**. Se `tem_cost_full` < 34, o re-sync do `sync-tiny-costs` precisa rodar ANTES do backfill — senão o backfill vira **no-op silencioso** (0 rows, sem erro), exatamente o aviso do arquivo de referência (`20260690000200:15-17`).

    **PASSO 4 — `max(version)`:** `SELECT max(version) FROM supabase_migrations.schema_migrations;` (para numerar a Task 3 acima do vivo).
  </how-to-verify>

  <resume-signal>Cole a tabela de cobertura por mês (PASSO 1), o veredito de drift de cada EF (PASSO 2), os 3 números do PASSO 3 e o `max(version)`. Diga: "Task 2 necessária" ou "Task 2 é no-op" — e, se necessária, se o re-sync tem que preceder o backfill.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Alinhar as 2 EFs ao que grava custo cheio (port do worktree irmão)</name>

  <read_first>
    - `supabase/functions/sync-tiny-costs/index.ts:140-230` — a Fase 1 (listagem `/produtos`) e a Fase 2 (detalhe `/produtos/{id}`). Repare em `:161-163`: os dois preços do Tiny são **colapsados num só** (`precoCustoMedio ?? precoCusto`) e só `cost` é gravado — `cost_full` nunca.
    - `supabase/functions/recalc-order-costs/index.ts:90-150` — o `.select` de `ml_product_costs` (`:97`, sem `cost_full`), o `.select` de `orders` (`:111`) e a montagem do `patch` (`:143-147`, sem `custo_unit_cheio`).
    - **Referência prod-provada:** `/root/garment-glow-dre/supabase/functions/sync-tiny-costs/index.ts` e `.../recalc-order-costs/index.ts` (branch `gsd/phase-86-dre-competencia`, **não mergeada**). Rode `diff` contra os arquivos deste repo e porte só o delta. Esta é a implementação que já produziu os 634 `cost_full` vivos — **não redesenhe**.
  </read_first>

  <files>supabase/functions/sync-tiny-costs/index.ts, supabase/functions/recalc-order-costs/index.ts</files>

  <action>
    ⚠️ **Se o checkpoint da Task 1 disse "no-op" para uma das EFs, pule essa EF** e registre no summary que o repo foi alinhado ao vivo por inspeção. Se disse que a EF viva está à frente, **o objetivo desta task é trazer o repo para o estado do vivo** (reconciliação de drift), não inventar comportamento novo.

    **`sync-tiny-costs`:** separe os dois campos do Tiny em vez de colapsá-los — `precoCustoMedio` → `cost` (custo médio, o que já existe hoje) e `precoCusto` → `cost_full` (o custo cheio). Mantenha o fallback atual de `cost` intacto (`precoCustoMedio ?? precoCusto`) para não regredir os 139 produtos em que o Tiny devolve o mesmo valor nos dois campos. Grave `cost_full` só quando for > 0, senão null. **Aplique nos DOIS lugares**: Fase 1 (listagem, ~`:161-173`) e Fase 2 (detalhe, ~`:208-221`) — portar só um deixa metade dos produtos sem cheio.

    **`recalc-order-costs`:** o `.select` de `ml_product_costs` (`:97`) passa a trazer `cost_full`; o de `orders` (`:111`) passa a trazer `custo_unit_cheio`; monte um mapa `costFullBySku` por `seller_sku`; no `patch` (`:143-147`), grave `custo_unit_cheio` quando o valor do mapa não for nulo.

    🚨 **REGRA DURA: nada neste plano toca `orders.custo_unit`.** O custo médio alimenta MCO, precificação, `/analise-precos` e `/produtos-vendidos` (via `orders_price_timeseries` e `get_margin_with_ads_by_product`, que leem `custo_unit` direto). Mexer nele vaza para 4 telas e para a precificação. `custo_unit_cheio` é lido por **um** consumidor (`get_cost_waterfall.cmv_cheio` → o card do DRE) — é por isso que ele é seguro e o médio não é.

    ⚠️ **Não faça `precoCustoMedio × fator`** em lugar nenhum. A fonte do cheio é o campo `precoCusto` do Tiny, lido separadamente. Derivar o cheio do médio reintroduziria o C6 disfarçado — é a restrição explícita do dono.
  </action>

  <verify>
    <automated>npx deno check supabase/functions/sync-tiny-costs/index.ts supabase/functions/recalc-order-costs/index.ts 2>/dev/null || deno check supabase/functions/sync-tiny-costs/index.ts supabase/functions/recalc-order-costs/index.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'cost_full' supabase/functions/sync-tiny-costs/index.ts` ≥ 2 (Fase 1 **e** Fase 2).
    - `grep -c 'cost_full' supabase/functions/recalc-order-costs/index.ts` ≥ 1 e `grep -c 'custo_unit_cheio' supabase/functions/recalc-order-costs/index.ts` ≥ 2.
    - `git diff main -- supabase/functions/recalc-order-costs/index.ts | grep -c "^[+].*custo_unit[^_]"` → **0** (nenhuma linha adicionada escreve o custo médio).
    - `grep -c 'precoCusto' supabase/functions/sync-tiny-costs/index.ts` ≥ 2 (os dois campos do Tiny são lidos separadamente).
    - `deno check` limpo nas 2 EFs.
    - `diff` contra o worktree irmão mostra que o delta portado é o mesmo — sem invenção.
  </acceptance_criteria>

  <done>As 2 EFs no repo gravam `cost_full`/`custo_unit_cheio` e o repo deixa de estar atrás do que roda em prod. Deploy é do orquestrador (Task 4).</done>
</task>

<task type="auto">
  <name>Task 3: Migration de backfill (idempotente, só custo_unit_cheio)</name>

  <read_first>
    - **Referência:** `/root/garment-glow-dre/supabase/migrations/20260690000200_backfill_custo_unit_cheio.sql` — o UPDATE completo e o aviso do próprio arquivo (`:15-17`) de que rodar antes do re-sync vira **no-op silencioso**.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-RESEARCH.md` seção `## 🎯 O backfill fecharia 34 dos 39 SKUs de maio HOJE`.
  </read_first>

  <files>supabase/migrations/2026XXXXXXXXXX_backfill_custo_unit_cheio_rerun.sql</files>

  <action>
    Porte o UPDATE do arquivo de referência, renumerado acima do `max(version)` obtido no checkpoint da Task 1. O corpo é:

    `UPDATE public.orders o SET custo_unit_cheio = pc.cost_full FROM public.ml_product_costs pc WHERE o.sku = pc.seller_sku AND pc.cost_full IS NOT NULL AND o.custo_unit_cheio IS NULL AND (pc.organization_id IS NULL OR pc.organization_id = o.organization_id);`

    Propriedades que **não** podem mudar:
    - **Idempotente** — a condição de `custo_unit_cheio IS NULL` faz re-execução ser inócua. É o que permite rodar de novo depois que o Wesley cadastrar os 4 SKUs no Tiny.
    - **Só `custo_unit_cheio` no SET.** Uma única coluna. Nada de `custo_unit`, nada de `tax_*`.
    - **Escopo de org no join** — `pc.organization_id IS NULL OR = o.organization_id` evita casar SKU de uma org com custo de outra.
    - Sem filtro de período: fecha o histórico inteiro de uma vez.

    **Header:** registre (a) a autorização do Wesley com a citação, (b) que a fonte é `precos.precoCusto` do Tiny (campo lido separadamente, **não** derivado do médio), (c) o alvo: 34 dos 39 SKUs de maio, resíduo = os 4 SKUs da tarefa manual, (d) que a migration é idempotente e re-executável.

    ⚠️ **Achado de qualidade de dado (não bloqueia, não corrija aqui):** 7 SKUs da família `1156120*` têm médio > cheio (ex.: `1156120NATP` médio 157,00 × cheio 107,81), o que é impossível pela definição do Wesley (o médio é o cheio MENOS impostos) — a ficha do Tiny provavelmente tem os dois campos invertidos. Impacto desprezível: só 2 venderam em 2026, R$396 no total. **Registre no header como candidato ao alerta de dado ruim** (mesma família do C8: informar, nunca auto-corrigir) e reporte no summary. Não tente consertar no backfill.
  </action>

  <verify>
    <automated>bash -c 'f=$(ls supabase/migrations/*backfill_custo_unit_cheio_rerun.sql) && grep -q "custo_unit_cheio IS NULL" "$f" && grep -q "cost_full IS NOT NULL" "$f" && [ "$(grep -c "SET custo_unit" "$f")" = "1" ] && echo OK'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'custo_unit_cheio IS NULL' <arquivo>` = 1 (idempotência).
    - `grep -c 'SET custo_unit_cheio' <arquivo>` = 1 e `grep -cE 'SET custo_unit[^_]|,\s*custo_unit\s*=' <arquivo>` → **0** (o médio não é tocado).
    - `grep -c 'organization_id' <arquivo>` ≥ 1 (join escopado por org).
    - `grep -ci 'fator\|1.2522\|média \* \|medio \*' <arquivo>` → **0** (nenhuma derivação aritmética do médio).
  </acceptance_criteria>

  <done>Migration de backfill em disco, idempotente, monocoluna, escopada por org. Ainda NÃO aplicada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — deploy das EFs, re-sync e apply do backfill (ordem importa)</name>
  <action>Checkpoint do orquestrador: deployar as 2 EFs e aplicar o backfill via MCP, na ordem dos 8 passos de `<how-to-verify>`. Nenhum código é escrito nesta task.</action>
  <what-built>
    As 2 EFs estão alinhadas no repo e a migration de backfill está em disco. Falta o orquestrador: **deploy das EFs** (`deploy_edge_function` via MCP — o `gsd-executor` não deploya EF) e **apply da migration** (`apply_migration` em `ckcdevcxgvueywivefgx`).
  </what-built>

  <how-to-verify>
    ⚠️ **ORDEM IMPORTA** — inverter faz o backfill virar no-op silencioso (0 rows, sem erro).

    **PASSO 1 — deploy das EFs** que a Task 2 mudou (`deploy_edge_function`). `sync-tiny-costs` e `recalc-order-costs` são EFs de negócio → `verify_jwt=true` (confirmado no fechamento da Phase 47).

    **PASSO 2 — re-sync do `sync-tiny-costs`** (se o PASSO 3 do primeiro checkpoint indicou `tem_cost_full` < 34). Confirme depois:
    ```sql
    SELECT count(*) AS linhas, count(cost_full) AS com_cost_full FROM public.ml_product_costs WHERE organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid;
    ```
    Alvo do ADENDO: **635 / 634**.

    **PASSO 3 — baseline do médio ANTES do backfill (a prova da regra dura):**
    ```sql
    SELECT count(*) AS linhas, count(custo_unit) AS com_medio, ROUND(SUM(custo_unit * quantidade),2) AS cmv_medio_total, count(custo_unit_cheio) AS com_cheio FROM public.orders WHERE organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid AND status IN ('paid','shipped','delivered') AND data_pedido::date BETWEEN '2026-05-01' AND '2026-05-31';
    ```
    Guarde os 4 números.

    **PASSO 4 — aplicar o backfill** via `apply_migration`.

    **PASSO 5 — prova de que o médio NÃO foi tocado (a mais importante):** repita a query do PASSO 3. `com_medio` e `cmv_medio_total` têm que estar **idênticos**; só `com_cheio` sobe. Qualquer movimento em `custo_unit` = vazamento para MCO/precificação/`/analise-precos` → **reverter**.

    **PASSO 6 — prova do backfill (34 de 39):**
    ```sql
    SELECT count(*) AS skus_restantes, SUM(linhas) AS linhas_restantes, ROUND(SUM(receita),2) AS receita_restante, count(*) FILTER (WHERE NOT tem_custo_medio) AS sem_custo_nenhum FROM public.get_cmv_cheio_gaps('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date);
    ```
    Alvo: de 39 SKUs para **5 ou menos**, com `sem_custo_nenhum` = **4** (`K2CTXCB191380PTOBRANM`, `K2CTXCB191380PTOBRANGG`, `K2CTXCB191380PTOBRANP`, `180128333315NATP` — a tarefa manual do Wesley no Tiny). Se continuar 39, o backfill foi no-op → o re-sync do PASSO 2 não rodou.

    **PASSO 7 — o CMV cheio subiu:**
    ```sql
    SELECT ROUND(cmv_cheio,2) AS cmv_cheio, ROUND(cmv,2) AS cmv_medio, ROUND(paid_revenue,2) AS receita FROM public.get_cost_waterfall('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date);
    ```
    `cmv_cheio` sai de 126.574,59 e sobe (as 226 linhas que entravam com custo zero passam a ter custo). `cmv` (médio) e `paid_revenue` **idênticos** ao de antes. ⚠️ **O número novo vira a nova verdade de maio** — o `+9.072,94` do CONTEXT §2 era otimista justamente porque o cheio puro cobria só 90,4%; a estimativa do dono para o CMV real é ~138.633,69 e o resultado ~−R$2.986. **Cole o valor obtido** — o 96-08 reconcilia contra ele.

    **PASSO 8 — o gate na tela:** com maio ainda tendo 4 SKUs sem custo nenhum, o botão "Marcar mês como apurado" continua **desabilitado**, e a lista do C6 agora mostra 4 SKUs marcados como "sem custo nenhum — cadastrar no Tiny". É o comportamento correto: o resíduo do backfill é exatamente a tarefa do Wesley.
  </how-to-verify>

  <resume-signal>Cole: PASSO 3 × PASSO 5 lado a lado (prova de que `custo_unit` não se moveu), o PASSO 6 (SKUs restantes) e o PASSO 7 (novo `cmv_cheio`). Depois digite "backfill aplicado" ou descreva a divergência.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Tiny → `sync-tiny-costs` → `ml_product_costs` | dado de terceiro entra no sistema de custo |
| `ml_product_costs` → `orders.custo_unit_cheio` | UPDATE em massa em tabela de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-22 | Tampering | backfill escrever em `custo_unit` | mitigate | SET monocoluna; gate de grep na Task 3 + prova antes/depois no PASSO 3/5. Vazaria para MCO, precificação, `/analise-precos` e `/produtos-vendidos` |
| T-96-23 | Tampering | join por SKU casar org errada | mitigate | `pc.organization_id IS NULL OR = o.organization_id` no WHERE |
| T-96-24 | Repudiation | custo cheio ser o CADASTRADO hoje, não o da época da venda | accept | viés conhecido e aceito: uma venda de janeiro recebe o custo cadastrado hoje. Point-in-time (`cost_full_history`) resolveria, mas é problema ortogonal e phase própria. Registrar no summary |
| T-96-25 | Integrity | 7 SKUs da família `1156120*` com médio > cheio (campos invertidos no Tiny) | accept | impossível pela definição do dono; impacto R$396 em 2026. **Só informar** (mesma regra do C8) — não auto-corrigir |
| T-96-26 | Denial of Service | UPDATE sem filtro de período em `orders` | accept | tabela de ~15k linhas da org; UPDATE único, fora do caminho do usuário |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | **nenhum pacote novo** — o port das EFs não adiciona import externo; Deno usa os mesmos `esm.sh`/`deno.land/std` já presentes |
</threat_model>

<verification>
- `deno check` limpo nas 2 EFs.
- Prova antes/depois de que `custo_unit` e `paid_revenue` não se moveram.
- SKUs de maio sem custo cheio: 39 → ≤5, com os 4 "sem custo nenhum" identificados.
- Novo `cmv_cheio` de maio registrado (entrada do 96-08).
</verification>

<success_criteria>
- 34 dos 39 SKUs de maio fechados; resíduo = exatamente os 4 SKUs da tarefa manual do Wesley.
- `custo_unit` (médio) intocado — MCO, precificação e `/analise-precos` sem regressão.
- A cobertura para de congelar: pedidos novos recebem custo cheio na ingestão.
- Nenhuma derivação `médio × fator` em lugar nenhum.
</success_criteria>

<artifacts_this_plan_produces>
| Símbolo | Tipo | Onde |
|---|---|---|
| `2026XXXXXXXXXX_backfill_custo_unit_cheio_rerun.sql` | migration nova (numerada no checkpoint) | `supabase/migrations/` |
| `sync-tiny-costs` grava `ml_product_costs.cost_full` | comportamento de EF (reconciliado com prod) | `supabase/functions/sync-tiny-costs/index.ts` |
| `recalc-order-costs` grava `orders.custo_unit_cheio` | comportamento de EF (novo no repo) | `supabase/functions/recalc-order-costs/index.ts` |
| `costFullBySku` | Map interno (novo) | `supabase/functions/recalc-order-costs/index.ts` |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-07-SUMMARY.md` when done
</output>
