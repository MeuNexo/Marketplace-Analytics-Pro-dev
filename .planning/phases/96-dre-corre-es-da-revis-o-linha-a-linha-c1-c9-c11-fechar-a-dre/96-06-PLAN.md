---
phase: 96-dre-correcoes-linha-a-linha
plan: 06
type: execute
wave: 3
depends_on: ["96-02", "96-03", "96-04", "96-05"]
files_modified:
  - src/components/mercadolivre/MLCostCard.tsx
  - src/pages/MercadoLivre.tsx
autonomous: true
requirements: ["C6", "C7", "C8", "C9"]

must_haves:
  truths:
    - "O botão 'Marcar mês como apurado' fica desabilitado quando falta custo cheio ou guia paga, e o tooltip diz exatamente o que falta"
    - "O Wesley vê a lista dos SKUs sem custo cheio, distinguindo 'falta o cheio' de 'não tem custo nenhum'"
    - "Quando Não classificado > 0, o card lista os lançamentos para recategorizar no Tiny — nunca corrige sozinho"
    - "O alerta de double-count mostra o valor em risco, e o valor continua sendo somado na cascata"
    - "A previsão (mês aberto) não é afetada pelo gate — resolveDreRegime segue intocado"
  artifacts:
    - path: "src/pages/MercadoLivre.tsx"
      provides: "fiação de useCmvCheioGate + resolveCloseGate + useNaoClassificadoItems, e o early-return do handler"
      contains: "resolveCloseGate"
    - path: "src/components/mercadolivre/MLCostCard.tsx"
      provides: "botão bloqueado com motivo, lista de SKUs faltantes, lista de nao_classificado, alerta de double-count com valor"
      contains: "closeBlocked"
  key_links:
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/lib/dreCloseGate.ts"
      via: "resolveCloseGate({ gaps, guia }) → closeBlocked + reasons"
      pattern: "resolveCloseGate"
    - from: "src/pages/MercadoLivre.tsx"
      to: "src/lib/dreRegime.ts"
      via: "NENHUMA mudança — resolveDreRegime continua recebendo os mesmos inputs de hoje"
      pattern: "resolveDreRegime"
---

<objective>
[C6/C7] Gate no botão "Marcar mês como apurado". [C8] Listar o Não classificado. [C9] Elevar o alerta de double-count.

Purpose: a apuração nunca pode fechar com dado faltando. Hoje `get_cost_waterfall` mascarava o custo faltante com o médio (corrigido no 96-03) — sem o gate, o sistema apenas trocaria uma mentira (CMV inflado) por outra (CMV subestimado, custo zero para 226 linhas). **O gate é o que faz o sistema parar de mentir:** ele bloqueia e diz exatamente o que preencher.
Output: fiação dos 3 hooks + a lógica pura do 96-02 na tela. Toda a aritmética já foi provada em teste; aqui é wiring.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md
@src/lib/dreRegime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: [C6/C7/C8] MercadoLivre.tsx — fiar os gates e o early-return</name>

  <read_first>
    - `src/pages/MercadoLivre.tsx:266-303` — `dreSaleMonth`, `useDreMonthClose`, `useImpostoGuiaReal`, `useImpostoGuiaNudge`, `resolveDreRegime` (`:279-288`), `nudgeClose` (`:295-298`), `canClose` (`:299`), `guiaCompetenceLabel` (`:300-303`).
    - `src/pages/MercadoLivre.tsx:305-323` — `handleCloseDreMonth` / `handleReopenDreMonth`, com o padrão de `toast.error`.
    - `src/lib/dreCloseGate.ts` (do 96-02) — `resolveCloseGate`, `canApurarImposto`, `CloseGateResult`. **Toda a lógica já está aqui e testada; esta task só chama.**
    - `src/hooks/useCmvCheioGate.ts` (do 96-03) e `src/hooks/useNaoClassificadoItems.ts` (do 96-04) — as assinaturas.
    - `src/lib/dreRegime.ts:99-133` — `resolveDreRegime`. **Leia para NÃO tocar.** Pitfall 7: gate dentro do resolver = previsão muda = SC5 quebrado.
    - `src/lib/dreRegime.ts:170-203` — `shouldNudgeClose`. Entenda a contradição documentada (Pitfall 10) antes de escrever o tooltip.
  </read_first>

  <files>src/pages/MercadoLivre.tsx</files>

  <action>
    1. **Gate de CMV:** chame `useCmvCheioGate` **no mesmo eixo de mês do `dreWaterfall`** — a mesma condicional `billingMonthIsCurrentMonth ? monthlyFrom/monthlyTo : billingMonthFrom/billingMonthTo` de `:233-236`. Eixo diferente = o gate olha um mês e o CMV outro.

    2. **Gate combinado:** `const closeGate = resolveCloseGate({ gaps: cmvGate.data ?? null, guia: guiaReal.data ?? null })`. Derive `closeBlocked = !monthClose.isClosed && closeGate.blocked`.
       🚨 **O gate só vale para FECHAR.** Um mês já fechado (`isClosed`) tem que continuar reabrível — se o `closeBlocked` alcançasse o botão de reabrir, um mês fechado com dado faltando ficaria preso para sempre.

    3. **Lista do não classificado:** `useNaoClassificadoItems(dreSaleMonth)` — `dreSaleMonth` já é `"YYYY-MM-01"` (`:271`) e é o mês de **venda**, não o M+1 da guia (o M+1 é exclusivo do imposto).

    4. **Defesa em profundidade** em `handleCloseDreMonth` (`:305-313`): early-return antes de `monthClose.close()` quando `closeBlocked`, com `toast.error` listando `closeGate.reasons`. O `disabled` do botão é UX; isto é a segunda barreira. (A RLS de `dre_month_close` só checa owner — ela **não** conhece o gate, e um owner com curl ainda fecha o mês. É decisão consciente, registrada no threat model, não esquecimento.)

    5. **Props novas para o card:** `closeBlocked`, `closeBlockReasons={closeGate.reasons}`, `cmvGaps={cmvGate.data ?? []}`, `naoClassificadoItems={naoClassificadoItems.data ?? []}`.

    🚨 **`resolveDreRegime` (`:279-288`) NÃO muda** — nem os inputs, nem a chamada. Nenhum valor do gate pode alcançar o resolver. É a garantia mais barata do SC5.

    ⚠️ **Não** tente alinhar `shouldNudgeClose` ao gate. O sinal "vencimento≠21" (`dreRegime.ts:185-189`) dispara ANTES do pagamento, então o empurrãozinho 🟢 vai aparecer em meses que o gate barra — botão verde e desabilitado ao mesmo tempo. Isso é **esperado**: a decisão da Phase 94 (nudge cedo) está travada, e o gate é a autoridade. A Task 2 resolve a contradição explicando o "porquê não" no tooltip — não mexendo no nudge.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'resolveCloseGate' src/pages/MercadoLivre.tsx` ≥ 2 (import + uso).
    - `grep -c 'useCmvCheioGate' src/pages/MercadoLivre.tsx` ≥ 2 e `grep -c 'useNaoClassificadoItems' src/pages/MercadoLivre.tsx` ≥ 2.
    - `git diff main -- src/lib/dreRegime.ts | grep -c 'resolveDreRegime'` → **0** (o resolver não foi tocado).
    - `git diff main -- src/pages/MercadoLivre.tsx | grep -c '^[+-].*resolveDreRegime({'` → **0** (a chamada e os inputs do resolver estão intactos).
    - `npx vitest run src/lib/dreRegime.test.ts` → **18/18** (prova do SC5).
    - `closeBlocked` é derivado com a condição de mês não-fechado: `grep -c 'isClosed && closeGate.blocked' src/pages/MercadoLivre.tsx` ≥ 1.
    - `npx tsc --noEmit` 0 erros; `npm run build` limpo.
  </acceptance_criteria>

  <done>A página calcula `closeBlocked` a partir da lógica pura já testada, bloqueia o handler de fechar, alimenta o card com gaps/motivos/lançamentos — e o resolver de regime segue byte-a-byte igual.</done>
</task>

<task type="auto">
  <name>Task 2: [C6/C7/C8/C9] MLCostCard — botão bloqueado com motivo, listas e alerta com valor</name>

  <read_first>
    - `src/components/mercadolivre/MLCostCard.tsx:205-237` — o bloco do empurrãozinho + o botão de marcar/reabrir (`:225-235`), incluindo o `disabled` atual (`:229`).
    - `src/components/mercadolivre/MLCostCard.tsx:369-405` — o `.map` dos blocos operacionais, onde mora a linha "Não classificado" e o ícone `HelpCircle` do double-count (`:381-394`).
    - `src/components/mercadolivre/MLCostCard.tsx:428-457` — o mesmo alerta no bloco financeiro (`:433-446`).
    - `src/lib/dreCascade.ts:64-76` — `DreCascadeBlocoLine` (tem `bloco`, `label`, `total`, `doubleCountRisk`) — é como o card identifica a linha do não classificado.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md` seções `### [C8]` e `### [C9]`.
  </read_first>

  <files>src/components/mercadolivre/MLCostCard.tsx</files>

  <action>
    **Props novas:** `closeBlocked?: boolean`, `closeBlockReasons?: string[]`, `cmvGaps?: CmvCheioGap[]` (tipo importado de `@/lib/dreCloseGate`), `naoClassificadoItems?: NaoClassificadoItem[]` (tipo de `@/hooks/useNaoClassificadoItems`).

    **[C6/C7] Botão** (`:225-235`): acrescente `closeBlocked` à condição de `disabled` (`:229`). 🚨 Só quando **não** está fechado — o botão "Reabrir mês" nunca pode ser bloqueado pelo gate, senão um mês fechado com dado faltando fica preso.
    Envolva o botão num `Tooltip` que, quando bloqueado, lista `closeBlockReasons`. É isto que resolve a contradição do Pitfall 10: o 🟢 "Guias parecem lançadas" pode estar aceso ao lado de um botão desabilitado — o tooltip explica o porquê. Um botão desabilitado sem motivo visível é pior que o bug original.
    ⚠️ `Tooltip` do Radix não dispara em toque. O card já usa `TooltipProvider`/`Tooltip` para o nudge e para o double-count — siga o mesmo padrão do arquivo por consistência, mas saiba que no mobile o motivo pode não aparecer no hover. Se o alvo for tap-friendly, `Popover` é o padrão do repo para isso (decisão da Phase 46: *"Radix Tooltip não dispara no touch; Popover com open controlado é confiável no iOS/Android"*) — use `Popover` nas **listas** (C6/C8), que são o conteúdo que o Wesley precisa mesmo ler no celular.

    **[C6] Lista dos SKUs faltantes:** quando `cmvGaps` não for vazio, exiba perto do botão um gatilho discreto (ex.: "N SKUs sem custo cheio") abrindo um `Popover` com os top ~10 por receita: SKU, marca, unidades, receita. **Distinga os dois casos** com `temCustoMedio`: `false` → "sem custo nenhum — cadastrar no Tiny" (são os 4 SKUs da tarefa manual do Wesley); `true` → "falta o custo cheio" (os 34 que o backfill do 96-07 resolve). Mostre o total de receita afetada (R$23.828,31 em maio). Esta lista é o "diga exatamente o que preencher" do goal da phase — não a esconda atrás de um ícone de 12px.

    **[C8] Lista do não classificado:** na linha do bloco `nao_classificado` do `.map` (`:373-405`), quando `total > 0`, acrescente um alerta (ícone de atenção, não o `HelpCircle` neutro) abrindo um `Popover` com `naoClassificadoItems`: fornecedor, descrição, valor, data. Texto do cabeçalho deixando claro que a ação é **recategorizar no Tiny** — o sistema **informa e nunca corrige sozinho** (decisão explícita do Wesley). Não altere o valor da linha, não subtraia, não esconda.

    **[C9] Double-count com valor:** o tooltip de `:389-391` hoje diz "Pode conter fatura ML já contabilizada na margem" — sem número, é fácil de ignorar. Inclua o **valor da linha** no texto (ex.: em maio, R$22.752,76 no bloco Operacional, dos quais R$20.550,13 são billing ML já contado em tarifas). Faça o mesmo no bloco financeiro (`:441-443`).
    🚨 **Nada de auto-netting.** A cascata continua somando o valor cheio — é decisão do dono: o Wesley corrige na fonte (passa a lançar no Tiny só a parte não-ML e ajusta as 6 faturas históricas). A flag **sinaliza**, não desconta.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'closeBlocked' src/components/mercadolivre/MLCostCard.tsx` ≥ 3 (prop + tipo + disabled).
    - `grep -c 'closeBlockReasons' src/components/mercadolivre/MLCostCard.tsx` ≥ 2 (o motivo é exibido, não só usado).
    - `grep -c 'cmvGaps' src/components/mercadolivre/MLCostCard.tsx` ≥ 2 e `grep -c 'temCustoMedio' src/components/mercadolivre/MLCostCard.tsx` ≥ 1 (os dois casos são distinguidos).
    - `grep -c 'naoClassificadoItems' src/components/mercadolivre/MLCostCard.tsx` ≥ 2.
    - `grep -c 'Popover' src/components/mercadolivre/MLCostCard.tsx` ≥ 2 (as listas são tap-friendly).
    - O `disabled` do botão só considera o gate no caminho de fechar: `grep -c 'mesClosed .*closeBlocked\|closeBlocked && !mesClosed\|!mesClosed && closeBlocked' <arquivo>` ≥ 1.
    - `npx tsc --noEmit` 0 erros; `npx vitest run` verde; `npm run build` limpo.
  </acceptance_criteria>

  <done>O botão bloqueia com motivo legível, as duas listas (SKUs sem custo cheio / lançamentos não classificados) são acessíveis no toque, e o alerta de double-count mostra o valor em risco sem descontar nada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `dre_month_close` (INSERT/DELETE) | RLS owner-only é a única autoridade real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-18 | Elevation of Privilege | gate de fechamento é só UX | accept | as policies de `dre_month_close` (`20260694000000:54-57`) só checam `get_org_role = 'owner'` — um owner com curl fecha o mês faltando custo. **Decisão consciente:** o gate previne erro honesto, não ataque; trigger `BEFORE INSERT` foi avaliada e recusada (acoplaria a RPC de custo à tabela de close e complicaria o reopen) |
| T-96-19 | Denial of Service | mês fechado ficar preso por gate no reopen | mitigate | `closeBlocked` só se aplica quando `!mesClosed`; gate de grep na acceptance prova |
| T-96-20 | Information Disclosure | listas expõem SKU/fornecedor/valor no card | accept | dado da própria org, para owner/membro; RLS já é o limite (provado no 96-03/96-04) |
| T-96-21 | Repudiation | double-count sinalizado mas somado | accept | **intencional** (C9) — o dono corrige na fonte; auto-netting seria alterar o número sem o dono pedir |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | **nenhum pacote novo neste plano** — Popover/Tooltip já são shadcn/ui instalados |
</threat_model>

<verification>
- `npx vitest run` verde, incluindo os **18/18** de `dreRegime.test.ts` (SC5).
- `npx tsc --noEmit` 0 erros; `npm run build` limpo.
- `git diff main -- src/lib/dreRegime.ts` sem nenhuma mudança de lógica.
</verification>

<success_criteria>
- SC3: o gate impede o fechamento de maio e lista os SKUs faltantes, com receita e a distinção "sem cheio" × "sem custo nenhum".
- SC4: o gate de imposto aceita maio (3 guias pagas) e rejeita competência com `pending`; `0,01` não bloqueia (provado no 96-02, aqui é consumo).
- SC5: `resolveDreRegime` intocado; previsão inalterada.
- C8/C9: informar e listar; nunca auto-corrigir, nunca auto-netting.
</success_criteria>

<artifacts_this_plan_produces>
| Símbolo | Tipo | Onde |
|---|---|---|
| `MLCostCardProps.closeBlocked` | prop (nova) | `src/components/mercadolivre/MLCostCard.tsx` |
| `MLCostCardProps.closeBlockReasons` | prop (nova) | `src/components/mercadolivre/MLCostCard.tsx` |
| `MLCostCardProps.cmvGaps` | prop (nova) | `src/components/mercadolivre/MLCostCard.tsx` |
| `MLCostCardProps.naoClassificadoItems` | prop (nova) | `src/components/mercadolivre/MLCostCard.tsx` |
| `closeGate` / `closeBlocked` | derivações (novas) | `src/pages/MercadoLivre.tsx` |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-06-SUMMARY.md` when done
</output>
