---
phase: 96-dre-correcoes-linha-a-linha
plan: 08
type: execute
wave: 5
depends_on: ["96-01", "96-02", "96-03", "96-04", "96-05", "96-06", "96-07"]
files_modified:
  - .planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-RECONCILIACAO.md
autonomous: false
requirements: ["C1", "C2/C5", "C4", "C6", "C7", "C8", "C9", "C11"]

must_haves:
  truths:
    - "A cascata de maio reconcilia o swing de R$52.496,21 contra dado vivo, sem sobra"
    - "O Wesley confirma na tela que a DRE de maio parou de mentir"
    - "A distinção SC2 × SC5 está documentada — tarifas mudam no mês aberto de propósito, e isso não é regressão"
  artifacts:
    - path: ".planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-RECONCILIACAO.md"
      provides: "prova numérica dos 6 Success Criteria contra prod, lado a lado com o CONTEXT §2"
      min_lines: 40
  key_links:
    - from: "96-RECONCILIACAO.md"
      to: ".planning/ROADMAP.md (Phase 96 Success Criteria)"
      via: "cada SC com a query que o prova e o número obtido"
      pattern: "SC[1-6]"
---

<objective>
Provar os 6 Success Criteria contra o banco vivo e fechar a phase com o dono.

Purpose: os 8 itens (C1..C11) foram corrigidos em planos separados; nenhum deles prova o **número da phase**. O SC1 é a prova de que as 4 correções somam exatamente o buraco de maio — `tarifas 11.248,96 + CMV 9.887,92 + cartão 20.550,13 + não classificado 10.809,20 = 52.496,21`, sem sobra. Uma peça errada e a soma não fecha; foi assim que o Wesley achou os furos.
Output: `96-RECONCILIACAO.md` (a prova) + a validação do dono na tela.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provas automatizadas (SC2/SC4/SC5/SC6) + esqueleto da reconciliação</name>

  <read_first>
    - `.planning/ROADMAP.md` seção `### Phase 96` — os 6 Success Criteria, literais.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md` seção `## 2. A cascata de maio` — a tabela de 2 colunas ("Se fechar hoje" × "Corrigida"), que é o gabarito.
    - Os `96-0*-SUMMARY.md` dos planos anteriores — de onde saem os números já provados (em especial o `cmv_cheio` pós-backfill do 96-07 PASSO 7, que **substitui** o 126.574,59 do CONTEXT).
  </read_first>

  <files>.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-RECONCILIACAO.md</files>

  <action>
    Rode as provas que **não** precisam de MCP e monte o documento com os resultados. Para cada SC: o critério literal do ROADMAP, o comando/query, o número obtido, e PASS/FAIL.

    **SC2 (tarifas 63.878,37)** — já provado em teste puro no 96-01: `npx vitest run src/hooks/useMLBilling.test.ts`. Cole o nome do teste e o resultado.
    **SC4 (gate de imposto por status)** — já provado no 96-02: `npx vitest run src/lib/dreCloseGate.test.ts`. Destaque os dois casos que importam: maio com duas guias de `0,01` **passa**; competência com `pending` de R$3.298,87 **rejeita**.
    **SC5 (previsão byte-a-byte)** — `npx vitest run src/lib/dreRegime.test.ts` → **18/18**, com `git diff main -- src/lib/dreRegime.ts` mostrando só a palavra-chave `export`. Some `git diff main --stat -- src/lib/dreCascade.ts` → **vazio**.

    🚨 **Documente a distinção SC2 × SC5 numa seção própria** — é a leitura errada mais provável de quem auditar esta phase depois:
    > **SC5 não significa "o mês aberto não muda em nada".** C2/C4/C5 mudam as tarifas **também no mês aberto** — e isso é **intencional**: taxa de parcelamento não é custo da loja em mês nenhum, aberto ou fechado, e competência é a régua certa em ambos. O mesmo vale para o C1 (receita bruta + cancelamentos aparecem nos dois regimes).
    > **SC5 é sobre `resolveDreRegime`** — o resolver escolhe a base de **CMV e imposto** conforme o regime, e **nunca lê tarifas nem receita**. O que a Phase 94 travou e esta phase preserva é: mês aberto → CMV **médio** + imposto **estimado**; mês fechado → CMV **cheio** + **guia real**. Nenhuma das 8 correções toca essa escolha. Os 18 testes verdes + o diff de 1 palavra em `dreRegime.ts` são a prova.
    > Em uma frase: **SC5 protege a escolha da base, não os números da tela.**
    **SC6 (INSS no bloco Pessoal)** — `npx vitest run src/lib/dreCascade.test.ts`, os 4 testes do C11.
    **Suíte + build:** `npx vitest run` (baseline ~542 + os novos), `npx tsc --noEmit`, `npm run build`.

    Monte a tabela da cascata de maio com **3 colunas**: "CONTEXT — se fechar hoje", "CONTEXT — corrigida" e "**Medido em prod**". Deixe a 3ª coluna com marcadores `<preencher no checkpoint>` nas linhas que dependem de query viva (Receita, Tarifas, CMV, Impostos, blocos, Resultado). As linhas provadas por teste puro já vêm preenchidas.

    ⚠️ **Registre a divergência esperada, com destaque:** o CONTEXT §2 projeta resultado corrigido de **+9.072,94**, mas o próprio CONTEXT avisa que esse número é **otimista** — o cheio puro cobria só 90,4% da receita e as 226 linhas sem custo entravam com **custo zero**. Depois do backfill do 96-07, o CMV sobe e o resultado cai. A estimativa do dono é CMV ≈ **138.633,69** e resultado ≈ **−R$2.986**. **O SC1 se prova pelo SWING (52.496,21), não pelo resultado final** — o resultado final depende do backfill e dos 4 SKUs que o Wesley cadastra no Tiny. Deixe isso explícito no documento para não parecer que a phase errou o alvo.
  </action>

  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run build && test -f .planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-RECONCILIACAO.md</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run` → suíte verde; contagem ≥ baseline (~542) + os testes novos das phases 96-01/02/05.
    - `npx vitest run src/lib/dreRegime.test.ts` → **18/18** (SC5).
    - `npx tsc --noEmit` → 0 erros; `npm run build` → limpo.
    - `96-RECONCILIACAO.md` existe e contém uma seção por SC (`grep -c 'SC[1-6]' <arquivo>` ≥ 6).
    - A tabela da cascata tem as 3 colunas e as linhas pendentes marcadas para o checkpoint.
    - O documento registra explicitamente que o SC1 se prova pelo swing, não pelo resultado final.
    - `grep -c 'SC5 protege a escolha da base' <arquivo>` ≥ 1 — a distinção SC2 × SC5 está documentada.
  </acceptance_criteria>

  <done>SC2, SC4, SC5 e SC6 provados por teste automatizado e documentados; o esqueleto da reconciliação está pronto para os números vivos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — prova do SC1 em prod + validação do Wesley na tela</name>
  <action>Checkpoint do orquestrador + do dono: rodar as provas de SC1/SC3 via MCP, publicar o preview e conduzir a validação visual descrita em `<how-to-verify>`. Nenhum código é escrito nesta task.</action>
  <what-built>
    Todas as 8 correções estão aplicadas (código + prod). Falta a prova do **SC1** contra o banco vivo — que só o orquestrador roda, via MCP Supabase em `ckcdevcxgvueywivefgx`.
  </what-built>

  <how-to-verify>
    Single-statement cada. Org Pé Vermeio `7f615df7-7bac-45e5-8a93-827fb9ddeec7`, loja `1639558873`, maio/2026.

    **SC1 — a reconciliação do swing (a prova da phase):**

    1. **Receita** — `get_cost_waterfall` → `paid_revenue` = **247.216,12**; `get_cancelled_revenue` → **14.450,29** (cancelled 14.063,90 + reembolsado 386,39); bruto = **261.666,41**; líquida = `261.666,41 − 14.450,29` = **247.216,12**, idêntica à de hoje. **A receita não contribui com nada para o swing** — o C1 é simetria de apresentação, não correção de valor. Se a líquida se moveu, o C1 quebrou o bottom line.
    2. **Tarifas** — some `ml_billing_daily` de maio por `competence_date`, total = **74.704,19**; menos CFONPN (12.187,14) e BFONPN (−1.361,32) = **63.878,37**. Delta vs. os 75.127,33 de hoje (por `charge_date`, com parcelamento): **11.248,96**. ✅ 1ª parcela.
    3. **CMV** — `cmv_cheio` **pós-backfill** (o número que o 96-07 PASSO 7 registrou). ⚠️ O delta de **9.887,92** do SC1 é `136.462,51 − 126.574,59` = o efeito **de tirar o COALESCE**, medido no 96-03 ANTES do backfill. **É essa a 2ª parcela do swing.** O backfill é um efeito posterior e separado — não o misture na conta do swing, ou a soma não fecha. ✅ 2ª parcela.
    4. **Cartão** — `get_dre_operational_by_competence` de maio: a linha `Cartão de crédito` com `double_count_risk = true`, total **22.752,76**, dos quais **20.550,13** são billing ML. ⚠️ **Esta parcela do swing NÃO é entregue por código** — o Wesley corrige na fonte (Tiny), lançando só a parte não-ML a partir de agora e ajustando as 6 faturas históricas em julho. O escopo de código foi **expor o alerta com o valor** (C9). Registre como "pendente do dono", não como FAIL. ✅ 3ª parcela (identificada).
    5. **Não classificado** — `get_dre_nao_classificado_items` de maio soma **10.809,20**, 20/20 lançamentos são nota de fornecedor. ⚠️ Igual ao cartão: a correção é do Wesley no Tiny (recategorizar 7 notas Outros→Fornecedores); o código **informa e lista** (C8). ✅ 4ª parcela (identificada).
    6. **A soma:** `11.248,96 + 9.887,92 + 20.550,13 + 10.809,20` = **52.496,21**. Bate exato, sem sobra. Se não bater, alguma peça está errada — **não arredonde para fechar**.

    **SC3 — o gate impede:** confirme via `get_cmv_cheio_gaps` que maio ainda tem os 4 SKUs sem custo nenhum e que o botão continua desabilitado. Registre a evolução 39 → 4 (o backfill fez a parte dele; o resíduo é do Wesley).

    **Validação do Wesley — preview Vercel** (branch `gsd/phase-96-dre-correcoes-linha-a-linha`, apontando para o Supabase de prod; padrão de `reference_vercel_preview_workflow`). Peça para ele navegar até maio/2026 em `/vendas` e conferir:
    1. **Receita bruta R$261.666,41** no topo + linha "(−) Cancelamentos de vendas R$14.450,29" logo abaixo (o reembolso de R$386,39 está dentro do cancelamento, por decisão dele).
    2. **"Taxas de parcelamento" R$10.825,82** aparece como linha **informativa** (cinza, sem o `(−)`), e **Total de tarifas ML = R$63.878,37** (não a soma com ela).
    3. **Margem de contribuição** parte de 247.216,12 — o número **não inflou** R$14.450,29 por causa do cancelamento na tela.
    4. Botão **"Marcar mês como apurado" desabilitado**, com tooltip dizendo o que falta; a lista mostra os **4 SKUs "sem custo nenhum — cadastrar no Tiny"**.
    5. Linha **"Não classificado" com alerta** listando Textile Xtra 4.627,04 + Pralana 3.329,39 + Pralana 2.852,77.
    6. Alerta de **double-count no bloco Operacional mostrando o valor** (R$22.752,76 / R$20.550,13 de billing ML).
    7. Navegue para um mês **aberto** (previsão) e confirme que CMV e Impostos continuam iguais ao de antes — SC5 na prática.

    **Follow-ups a registrar** (não bloqueiam):
    - Índice em `ml_billing_daily (organization_id, ml_user_id, competence_date)` — o filtro novo do C4 pode não ter índice (a coluna é drift).
    - `orders.data_pedido` TEXT → timestamptz (pendência antiga do STATE).
    - 7 SKUs da família `1156120*` com médio > cheio (campos invertidos no Tiny) — candidato ao alerta de dado ruim.
    - `custo_unit_cheio` é o custo **cadastrado hoje**, não o da época da venda (point-in-time seria phase própria).
  </how-to-verify>

  <resume-signal>Cole a soma do SC1 (as 4 parcelas fechando em 52.496,21) e a identidade da receita (bruta − cancelamentos = 247.216,12). Depois digite "aprovado" ou descreva o que está errado na tela.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | plano de verificação; só leitura e validação humana |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-27 | Repudiation | número "fechar" por arredondamento em vez de por correção | mitigate | o SC1 exige as 4 parcelas batendo **exatas** contra query viva, sem sobra; arredondar para fechar é explicitamente proibido |
| T-96-28 | Information Disclosure | preview Vercel aponta para o Supabase de PROD | accept | padrão já estabelecido no repo (`reference_vercel_preview_workflow`); exige login real, RLS ativa |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | **nenhum pacote novo neste plano** — zero instalações |
</threat_model>

<verification>
- Suíte completa verde; `tsc --noEmit` 0; `npm run build` limpo.
- SC1 provado contra prod, com as 4 parcelas fechando em 52.496,21.
- SC3 provado na tela (gate desabilita o botão e lista os 4 SKUs).
- Wesley aprovou a tela; identidade da receita provada (bruta − cancelamentos = líquida de hoje).
</verification>

<success_criteria>
- Os 6 Success Criteria do ROADMAP com PASS documentado e o número obtido lado a lado com o gabarito do CONTEXT.
- As 2 parcelas que dependem do Wesley (cartão 20.550,13 / não classificado 10.809,20) registradas como **identificadas e expostas pelo sistema**, com a correção na fonte pendente — não como falha da phase.
- Follow-ups registrados no STATE.
</success_criteria>

<artifacts_this_plan_produces>
| Símbolo | Tipo | Onde |
|---|---|---|
| `96-RECONCILIACAO.md` | documento de prova (novo) | `.planning/phases/96-.../` |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-08-SUMMARY.md` when done
</output>
