---
phase: 96-dre-correcoes-linha-a-linha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useMLBilling.ts
  - src/hooks/useMLBilling.test.ts
autonomous: true
requirements: ["C2/C5", "C4"]

must_haves:
  truths:
    - "As taxas de parcelamento (CFONPN + BFONPN) deixam de ser somadas ao Total de tarifas ML"
    - "O cancelamento do parcelamento (BFONPN) para de cair na linha Cancelamentos de tarifas"
    - "As tarifas do mês passam a ser lidas pela competência da venda, não pela data da cobrança"
    - "Tarifas de maio/2026 por competência sem parcelamento = R$63.878,37"
  artifacts:
    - path: "src/hooks/useMLBilling.ts"
      provides: "groupBillingCharges com grupo excluded + useMLBillingDaily por competence_date"
      contains: "excluded"
    - path: "src/hooks/useMLBilling.test.ts"
      provides: "TDD de groupBillingCharges — primeiro teste do arquivo (não existia)"
      min_lines: 60
  key_links:
    - from: "src/hooks/useMLBilling.ts"
      to: "ml_billing_daily.competence_date"
      via: "filtro de range da query PostgREST"
      pattern: "competence_date"
---

<objective>
[C2/C5] Tirar o parcelamento (CFONPN + BFONPN) do Total de tarifas ML, sem sumir com a linha da tela.
[C4] Ler `ml_billing_daily` por **competência da venda** em vez de data de cobrança.

Purpose: são os dois furos que valem R$11.248,96 do swing de maio (75.127,33 → 63.878,37). Wesley: taxa de parcelamento não é custo nosso — quem paga é o comprador, e ela não está na `receita_bruta`.
Output: `groupBillingCharges` com flag `excluded` + `useMLBillingDaily` por `competence_date` + o primeiro arquivo de teste de `useMLBilling.ts`.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [C2/C5] Blacklist do parcelamento em groupBillingCharges (TDD)</name>

  <read_first>
    - `src/hooks/useMLBilling.ts:20-143` — `BillingGroup`, `GroupedBillingResult`, `BILLING_GROUP_MAP`, `groupBillingCharges`. **Leia inteiro antes de editar** — a armadilha está na ordem das condições dentro do loop.
    - `src/lib/dreCascade.test.ts:1-30` — convenção de teste puro do repo (vitest, helper `row()` de fixture, describe por guardrail).
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md` seção `### [C2/C5]` — a lista fechada do que ENTRA e do que SAI.
  </read_first>

  <files>src/hooks/useMLBilling.test.ts (novo), src/hooks/useMLBilling.ts</files>

  <behavior>
    Fixtures obrigatórias no teste (valores reais de maio/2026, org Pé Vermeio, já reconciliados no CONTEXT):
    - Test 1 (a armadilha): charges `[{CFONPN, 12187.14}, {BFONPN, -1361.32}]` → o grupo `parcelamento` soma **10825.82** e o grupo `cancelamentos` fica em **0**. Hoje BFONPN é capturado pelo teste de prefixo "B" ANTES do mapa de grupos e cai em Cancelamentos — este teste falha em RED por esse motivo exato.
    - Test 2: o grupo `parcelamento` volta com `excluded: true`; todos os outros grupos vêm sem a flag (ou `false`).
    - Test 3: `totalTarifas` NÃO inclui o valor do grupo `parcelamento`. Fixture: `[{CFONPN,12187.14},{BFONPN,-1361.32},{CVVML,1000}]` → `totalTarifas === 1000`.
    - Test 4 (prova do SC2): fixture do mês de maio somando **74704.19** no total bruto por competência (inclua CVVML, CFFE, PADS, CDIFAL, CESM, CVAF, BVVML e o par CFONPN/BFONPN, com os demais valores fechando a conta) → `totalTarifas === 63878.37`.
    - Test 5 (não-regressão): os demais `B*` (ex.: BVVML −500, BFFE −200) continuam somando em `cancelamentos` (−700) e continuam DENTRO de `totalTarifas`.
    - Test 6 (não-regressão): `CESM` (Minha Página) e `CDSDB` (devolução) continuam caindo no bucket combinado `afiliados_outras` e continuam DENTRO de `totalTarifas` — nenhum charge_type é dropado.
  </behavior>

  <action>
    RED primeiro: crie `src/hooks/useMLBilling.test.ts` com as 6 fixtures acima, commit só do teste, rode e confirme que Test 1/2/3/4 falham. Depois implemente.

    Implementação em `src/hooks/useMLBilling.ts`:

    1. `BillingGroup` (`:20-24`) ganha `excluded?: boolean` — opcional, para não quebrar o objeto de fallback montado à mão em `MercadoLivre.tsx:337-352` (esse arquivo é do plano 96-05; **não o toque aqui**).
    2. Declare um Set de módulo com os dois charge_types do parcelamento — a cobrança (`CFONPN`) e o estorno dela (`BFONPN`) — e um flag `excluded: true` na entrada `parcelamento` do `BILLING_GROUP_MAP` (`:46-50`).
    3. 🚨 **A armadilha (Pitfall 2 do RESEARCH):** dentro do loop de `groupBillingCharges` (`:97-109`) o teste de prefixo "B" roda ANTES da busca no mapa e engole TODO charge_type iniciado por B. Insira o teste do Set do parcelamento **ANTES** do teste de prefixo, acumulando no grupo `parcelamento`. Sem isso o `BFONPN` (−1.361,32) continua em Cancelamentos e o total dá 65.239,69 em vez de 63.878,37.
    4. Propague `excluded` para os objetos montados em `:112-116`.
    5. `totalTarifas` (`:140`) passa a filtrar os grupos marcados como excluídos antes do reduce. `groups` continua devolvendo TODOS os grupos (incluindo o parcelamento) — a tela precisa exibir a linha como informativa (isso é do 96-05). Não drope linhas.
    6. Atualize o comentário de bloco `:78-87` para descrever a nova regra (parcelamento fora do total, resto tudo dentro).

    **NÃO** mexa em nenhum outro charge_type. A lista do CONTEXT é fechada: o único type removido do total é o parcelamento. Comissão, envios, devolução, ads, Full, DIFAL, Minha Página, afiliados, MP e todos os demais B* continuam entrando.
  </action>

  <verify>
    <automated>npx vitest run src/hooks/useMLBilling.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/hooks/useMLBilling.test.ts` → 6/6 passam.
    - `git log --oneline -2 -- src/hooks/useMLBilling.test.ts` mostra o commit do teste ANTES do commit da implementação (RED→GREEN).
    - `grep -c 'BFONPN' src/hooks/useMLBilling.ts` ≥ 1 (o type do estorno do parcelamento é tratado explicitamente, não por prefixo).
    - `npx vitest run` → suíte completa verde, sem regressão da baseline (~542 testes).
    - `npx tsc --noEmit` → 0 erros.
  </acceptance_criteria>

  <done>Fixture de maio (74.704,19 bruto por competência) → `totalTarifas === 63878.37` e `groups` ainda contém a linha `parcelamento` com `amount === 10825.82` e `excluded === true`.</done>
</task>

<task type="auto">
  <name>Task 2: [C4] useMLBillingDaily filtra por competence_date</name>

  <read_first>
    - `src/hooks/useMLBilling.ts:257-323` — `DailyBillingResult`, `lastDayOfMonth`, `useMLBillingDaily`. O comentário de `:272-275` já afirma "competência" — ele mente hoje; é exatamente este bug.
    - `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-RESEARCH.md` seção `# ADENDO` → a coluna `competence_date` **existe e está populada em prod** (verificado no banco vivo). **Não precisa de DDL.** É drift (não está em migration do repo) — não tente "consertar" isso aqui.
  </read_first>

  <files>src/hooks/useMLBilling.ts</files>

  <action>
    Em `useMLBillingDaily` (`:277-323`):

    1. O tipo `Row` (`:290`) e o `.select` (`:296`) passam a trazer **as duas** colunas de data: a de cobrança e a de competência.
    2. Os dois filtros de range (`.gte` / `.lte`, hoje em `:299-300`) trocam de coluna: passam a apontar para `competence_date`. O custo cai no mês da VENDA que o gerou, não no mês em que o ML cobrou.
    3. **`coverageTo` (`:315`) continua usando a data de cobrança** — ele é indicador de "até quando sincronizamos", não de competência. Por isso o `.select` precisa das duas colunas. Não troque essa linha.
    4. Bump da `queryKey` (`:283`) — a semântica do resultado mudou para o mesmo par (org, período), então quem já tem a página aberta serviria cache velho. Troque o literal do segundo segmento da key para uma versão nova (ex.: `"billing-daily-v2"`).
    5. Corrija o comentário de bloco (`:272-275`) para dizer a verdade: o range é por competência da venda; a cobertura é pela data de cobrança.

    Não toque em `useMLBillingDailyWithSync` (`:330-369`) nem na EF `sync-ml-billing`.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'competence_date' src/hooks/useMLBilling.ts` ≥ 3 (tipo Row + select + os dois filtros de range).
    - `grep -v '^\s*[/*]' src/hooks/useMLBilling.ts | grep -c '\.gte("charge_date"'` → **0** (nenhum filtro de range por data de cobrança sobrou fora de comentário).
    - `grep -c 'charge_date' src/hooks/useMLBilling.ts` ≥ 1 — a coluna continua existindo, no `.select` e no cálculo de `coverageTo`.
    - `grep -c 'billing-daily-v2' src/hooks/useMLBilling.ts` = 1 (queryKey bumpada).
    - `npx tsc --noEmit` → 0 erros; `npx vitest run` → suíte verde.
  </acceptance_criteria>

  <done>A query de `useMLBillingDaily` seleciona as duas colunas de data, filtra o range por `competence_date`, deriva `coverageTo` da data de cobrança e usa uma queryKey nova.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → PostgREST (`ml_billing_daily`) | leitura org-scoped; RLS é a autoridade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Information Disclosure | `useMLBillingDaily` | mitigate | filtros `organization_id` + `ml_user_id` preservados byte-a-byte; só a coluna do range de data muda |
| T-96-02 | Tampering | `groupBillingCharges` | accept | função pura sobre dado já carregado; sem entrada não confiável |
| T-96-03 | Denial of Service | filtro por `competence_date` sem índice | accept | pode não haver índice em `(organization_id, ml_user_id, competence_date)` — volume é ~1 mês de tarifas de 1 org, seq scan tolerável. Se o card ficar lento, 96-08 registra o índice como follow-up |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | **nenhum pacote novo neste plano** — zero instalações |
</threat_model>

<verification>
- `npx vitest run` verde (baseline ~542 + 6 novos).
- `npx tsc --noEmit` 0 erros.
- `npm run build` limpo.
</verification>

<success_criteria>
- Prova do SC2 em teste puro: fixture de maio 74.704,19 → `totalTarifas === 63878.37`.
- `BFONPN` fora de "Cancelamentos de tarifas".
- Range de `ml_billing_daily` por `competence_date`; `coverageTo` por data de cobrança.
- Nenhum charge_type dropado da lista `groups`.
</success_criteria>

<artifacts_this_plan_produces>
| Símbolo | Tipo | Arquivo |
|---|---|---|
| `BillingGroup.excluded?: boolean` | campo de interface (novo) | `src/hooks/useMLBilling.ts` |
| Set de módulo dos charge_types do parcelamento | const (novo) | `src/hooks/useMLBilling.ts` |
| `src/hooks/useMLBilling.test.ts` | arquivo de teste (novo — não existia) | — |
| queryKey `"billing-daily-v2"` | literal (novo) | `src/hooks/useMLBilling.ts` |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-01-SUMMARY.md` when done
</output>