---
phase: 96-dre-correcoes-linha-a-linha
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - supabase/migrations/2026XXXXXXXXXX_dre_cancelled_revenue_and_nao_classificado.sql
  - src/hooks/useCancelledRevenue.ts
  - src/hooks/useNaoClassificadoItems.ts
autonomous: false
requirements: ["C1", "C8"]

must_haves:
  truths:
    - "O sistema sabe quanto foi cancelado de vendas no mês, sem alterar o significado de paid_revenue"
    - "O sistema sabe listar os lançamentos que caíram no bloco Não classificado, para o Wesley recategorizar no Tiny"
    - "Nenhum consumidor de get_cost_waterfall muda de comportamento"
  artifacts:
    - path: "supabase/migrations/2026XXXXXXXXXX_dre_cancelled_revenue_and_nao_classificado.sql"
      provides: "get_cancelled_revenue + dre_bloco_for_category + get_dre_nao_classificado_items"
      contains: "get_cancelled_revenue"
    - path: "src/hooks/useCancelledRevenue.ts"
      provides: "receita cancelada do mes no mesmo eixo do waterfall"
      exports: ["useCancelledRevenue"]
    - path: "src/hooks/useNaoClassificadoItems.ts"
      provides: "lancamentos crus do bloco nao_classificado da competencia"
      exports: ["useNaoClassificadoItems", "NaoClassificadoItem"]
  key_links:
    - from: "get_dre_nao_classificado_items"
      to: "get_dre_operational_by_competence"
      via: "helper dre_bloco_for_category — o mapa categoria→bloco continua tendo UMA fonte no backend"
      pattern: "dre_bloco_for_category"
---

<!-- planner-discipline-allow: 'cancelled' -->
<!-- planner-discipline-allow: partially_refunded -->

<objective>
[C1-backend] RPC isolada de receita cancelada. [C8-backend] RPC que lista os lançamentos do bloco Não classificado.

Purpose:
- **C1:** a fatura ML é líquida (tem "Cancelamentos de tarifas"), mas a receita do card é só `paid` — réguas diferentes para o mesmo evento. Exibir bruto + "Cancelamentos de vendas" resolve a assimetria. 🚨 **`paid_revenue` tem 6 consumidores** (`/financeiro`, MCO, GoalsCard, Nexo, useAutoRecalc e o próprio card) — mudar o significado dela vazaria para todos. Por isso: **RPC nova, isolada, e a composição acontece no card.** É o mesmo padrão que a Phase 94 escolheu ao criar `get_imposto_guia_by_competence` em vez de inchar a RPC grande.
- **C8:** o valor (R$10.809,20 em maio) já aparece na cascata; o que falta é a LISTA. Decisão de Wesley: **só informar, nunca auto-corrigir** — ele recategoriza no Tiny.
Output: 1 migration (2 RPCs novas + 1 helper, tudo aditivo) + 2 hooks. Nenhuma RPC existente é tocada.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-CONTEXT.md
@supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql
@src/lib/dreCascade.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — cancelled_revenue + helper de bloco + itens do nao_classificado</name>

  <read_first>
    - `supabase/migrations/20260692000000_dre_operational_reconcile_context_map.sql` **inteiro** — é a RPC 87 (`get_dre_operational_by_competence`). Preste atenção em: o `CASE` que mapeia categoria→bloco (`:51-76`), o ramo `ELSE` que produz `nao_classificado` (`:71`), a flag `double_count_risk` (`:76`) e o `COALESCE(competence_date, date_trunc('month', outflow_date))` nas **duas bordas** do range (`:79-82`). **Esta RPC NÃO é modificada por este plano.**
    - `supabase/migrations/20260618100000_cash_flow_tables.sql:130-199` — colunas de `cash_outflows` (`description`, `supplier`, `category`, `amount`, `outflow_date`, `document_number`, `competence_date`) e a RLS org-scoped.
    - `supabase/migrations/20260612120000_fix_cost_waterfall_fallback_and_upsert_preserve.sql:21-58` — o predicado de período/status e o fallback de receita do waterfall. É a régua que a RPC de cancelados tem que espelhar. ⚠️ Lembre que a versão VIVA tem 7 colunas (drift) — mas o predicado do `WHERE` e o fallback de `paid_revenue` são os deste arquivo; confirme no checkpoint.
    - `src/lib/dreCascade.ts:1-18` — a regra explícita do repo: *"O campo `bloco` é consumido DIRETAMENTE — NUNCA re-derivado de `category`"*. É por isso que o filtro de "não classificado" fica no backend.
  </read_first>

  <files>supabase/migrations/2026XXXXXXXXXX_dre_cancelled_revenue_and_nao_classificado.sql</files>

  <action>
    ⚠️ Nome provisório; a numeração sai no checkpoint da Task 2, com o `max(version)` vivo em mãos. O repo está ~19 dias atrás do banco — nunca numere pelo `ls` do repo.

    **Bloco A — `get_cancelled_revenue`:** assinatura `(p_org_id uuid, p_user_ids text[], p_from date, p_to date)`, `RETURNS TABLE (cancelled_revenue numeric, cancelled_orders bigint)`, `LANGUAGE sql STABLE SECURITY INVOKER SET search_path TO 'public'`.
    Corpo: soma da receita com o **mesmo fallback do waterfall** (`COALESCE(o.receita_bruta, o.preco_unit * o.quantidade, 0)`) e `count(*)`, sobre `public.orders`, filtrando org + `o.ml_user_id = ANY(p_user_ids)` + **o mesmo cast de `data_pedido` do waterfall** + `o.status = 'cancelled'`.

    🚨 **A régua tem que ser espelho do `get_cost_waterfall`.** O waterfall soma `status IN ('paid','shipped','delivered')`; esta RPC soma o complemento cancelado, no mesmo eixo de data e com o mesmo fallback de receita. Se as réguas divergirem, o bruto exibido não bate com `paid + cancelled` e o C1 mente. Alvo de maio: **14.063,90**, para que `247.216,12 + 14.063,90 = 261.280,02`.

    **`partially_refunded` FICA DE FORA dos dois lados** — é o status quo de hoje (nem receita, nem cancelamento). É 1 pedido / R$386,39 em maio, e mantê-lo fora é o que preserva a reconciliação exata do swing de 52.496,21. ⚠️ **É a única decisão do CONTEXT que ficou ABERTA**; está registrada como pergunta ao Wesley no checkpoint do 96-08. Não invente uma regra aqui.

    **Bloco B — helper `dre_bloco_for_category(p_category text) RETURNS text`, `LANGUAGE sql IMMUTABLE SET search_path TO 'public'`:**
    Corpo = **cópia literal do `CASE` da RPC 87** (`20260692000000:51-76`), incluindo o ramo final que devolve `nao_classificado`. Não simplifique, não reordene, não "melhore" — a equivalência com a RPC 87 é provada por query no checkpoint. Este helper **não** é usado pela RPC 87 (que fica intocada, protegendo SC5/SC6); ele existe para que o C8 não re-derive o mapa no cliente.

    **Bloco C — `get_dre_nao_classificado_items(p_org_id uuid, p_month date)`**, `RETURNS TABLE (outflow_date date, competence_month date, description text, supplier text, category text, amount numeric, document_number text)`, `LANGUAGE sql STABLE SECURITY INVOKER SET search_path TO 'public'`.
    Corpo: de `public.cash_outflows co`, filtrando org + `public.dre_bloco_for_category(co.category) = 'nao_classificado'` + o range de competência, `ORDER BY co.amount DESC`.

    🚨 **Pitfall 9 do RESEARCH — o erro que produz lista vazia com a cascata mostrando R$10.809,20:** todas as linhas de maio em `nao_classificado` têm `competence_date` **NULL** (resíduo dos 8,7% que o backfill da Phase 86 não pegou). A RPC 87 as captura via `COALESCE(competence_date, date_trunc('month', outflow_date))`. Replique o **mesmo COALESCE nas duas bordas** do range (`>= p_month` e `< p_month + interval '1 month'`) **e** na coluna `competence_month` projetada. Sem isso a lista vem vazia e o alerta vira mentira.

    Ambas as RPCs: `REVOKE ... FROM PUBLIC, anon` + `GRANT EXECUTE ... TO authenticated`. `SECURITY INVOKER` explícito — a RLS de `orders`/`cash_outflows` é quem isola a org; DEFINER com param de org seria IDOR.

    **Sem `DROP FUNCTION` neste arquivo.** As três funções são novas; a migration é 100% aditiva. Se você sentir vontade de dropar algo, pare — não é este plano.

    **Header:** documente que a migration é aditiva, o alvo de maio (cancelado 14.063,90 / não classificado 10.809,20), e que o helper é cópia deliberada do CASE da RPC 87 com equivalência provada por query.
  </action>

  <verify>
    <automated>bash -c 'f=$(ls supabase/migrations/*dre_cancelled_revenue_and_nao_classificado.sql) && grep -q get_cancelled_revenue "$f" && grep -q get_dre_nao_classificado_items "$f" && grep -q dre_bloco_for_category "$f" && ! grep -qi "DROP FUNCTION" "$f" && echo OK'</automated>
  </verify>

  <acceptance_criteria>
    - O arquivo existe e contém as 3 funções (`get_cancelled_revenue`, `dre_bloco_for_category`, `get_dre_nao_classificado_items`).
    - `grep -ci 'DROP FUNCTION' <arquivo>` → **0** (migration aditiva; nenhuma função existente é dropada).
    - `grep -c 'get_dre_operational_by_competence' <arquivo>` → **0** (a RPC 87 não é redefinida aqui).
    - `grep -c 'SECURITY INVOKER' <arquivo>` = 2 (as duas RPCs de tenant; o helper é IMMUTABLE puro sobre um texto).
    - `grep -c 'GRANT EXECUTE' <arquivo>` ≥ 2 e `grep -c 'REVOKE' <arquivo>` ≥ 2.
    - `grep -c "COALESCE(co.competence_date" <arquivo>` ≥ 3 (as duas bordas do range + a coluna projetada).
    - `grep -c "'cancelled'" <arquivo>` = 1 e `grep -ci 'partially_refunded' <arquivo>` = 0 (fica fora, por decisão explícita).
  </acceptance_criteria>

  <done>Migration aditiva em disco com as 3 funções, grants, revokes e header. Ainda NÃO aplicada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — orquestrador numera, aplica e prova a migration via MCP</name>
  <action>Checkpoint do orquestrador: aplicar a migration via MCP Supabase e rodar as 8 provas descritas em `<how-to-verify>`. Nenhum código é escrito nesta task.</action>
  <what-built>
    A migration `dre_cancelled_revenue_and_nao_classificado` está escrita em disco mas **não aplicada**. `gsd-executor` não aplica migration — quem aplica é o orquestrador via MCP Supabase (`apply_migration`) no projeto **`ckcdevcxgvueywivefgx`** (NÃO o do CLAUDE.md). **Nunca `supabase db push`** — o CLI local está linkado no projeto errado (`gionpsuunfkkzzjdubfy`).
  </what-built>

  <how-to-verify>
    Cada consulta é **single-statement** — o `execute_sql` do MCP só devolve o 1º result set.

    **PASSO 0 — numerar:** `SELECT max(version) FROM supabase_migrations.schema_migrations;` → renomeie o arquivo para um timestamp ACIMA do retornado (esperado ~`20260713xxxxxx`; use `20260715xxxxxx`, coordenado com o do plano 96-03 para não colidir).

    **PASSO 1 — censo de status ANTES de aplicar (valida a régua do Bloco A):**
    ```sql
    SELECT o.status, count(*) AS pedidos, ROUND(SUM(COALESCE(o.receita_bruta, o.preco_unit * o.quantidade, 0)),2) AS receita FROM public.orders o WHERE o.organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid AND o.ml_user_id = '1639558873' AND o.data_pedido::date BETWEEN '2026-05-01' AND '2026-05-31' GROUP BY 1 ORDER BY 3 DESC;
    ```
    Espere: `paid` (+`shipped`/`delivered` se existirem) somando **247.216,12**; `cancelled` somando **14.063,90**; `partially_refunded` com **1 pedido / R$386,39**. ⚠️ **Se `cancelled` não der 14.063,90, a régua do Bloco A está errada — pare e corrija a migration antes de aplicar.** Se aparecer algum status inesperado (ex.: `returned`), reporte antes de seguir.

    **PASSO 2 — aplicar** via `apply_migration`.

    **PASSO 3 — prova do C1:**
    ```sql
    SELECT * FROM public.get_cancelled_revenue('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, ARRAY['1639558873'], '2026-05-01'::date, '2026-05-31'::date);
    ```
    Alvo: `cancelled_revenue` = **14063.90**. Some com o `paid_revenue` do waterfall → **261280.02** (o bruto de maio do CONTEXT).

    **PASSO 4 — equivalência do helper com a RPC 87 (a prova anti-drift):**
    ```sql
    SELECT count(*) AS categorias_divergentes FROM (SELECT DISTINCT co.category FROM public.cash_outflows co WHERE co.organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid) c JOIN LATERAL (SELECT r.bloco FROM public.get_dre_operational_by_competence('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, '2026-05-01'::date) r WHERE r.category = c.category LIMIT 1) rpc ON true WHERE public.dre_bloco_for_category(c.category) IS DISTINCT FROM rpc.bloco;
    ```
    Alvo: **0**. Qualquer valor > 0 = o helper divergiu do CASE da RPC 87 → corrija a cópia.

    **PASSO 5 — prova do C8:**
    ```sql
    SELECT count(*) AS lancamentos, ROUND(SUM(amount),2) AS total FROM public.get_dre_nao_classificado_items('7f615df7-7bac-45e5-8a93-827fb9ddeec7'::uuid, '2026-05-01'::date);
    ```
    Alvo: `total` = **10809.20** — o mesmo valor da linha "Não classificado" da cascata. ⚠️ **Se vier 0 linhas, o COALESCE de `competence_date` nas bordas do range foi esquecido** (Pitfall 9). Confirme também que os 3 maiores são Textile Xtra 4.627,04 + Pralana 3.329,39 + Pralana 2.852,77.

    **PASSO 6 — anti-IDOR (obrigatório, 2 RPCs novas):** impersone o JWT da Pé Vermeio (`7f615df7-...`) e chame **cada uma** das 2 RPCs com `p_org_id` da org **Thales** (`e4150d57-...`) → **0 linhas / soma zero**. `SET LOCAL ROLE authenticated` + `set_config('request.jwt.claims', ..., true)` em transação com ROLLBACK — o `service_role` bypassa RLS, testar como `postgres` não prova nada. Single-statement via `LATERAL` (padrão da Phase 79). Prova de controle: a mesma impersonação contra a própria org devolve os números dos PASSOS 3 e 5.

    **PASSO 7 — não-regressão do waterfall:** confirme que `get_cost_waterfall` de maio continua devolvendo `paid_revenue` = **247216.12**. Esta migration não deveria tocá-lo; se o número mudou, algo saiu do escopo.

    **PASSO 8 — advisors:** `get_advisors(type: "security")` — nenhum issue NOVO atribuível às 3 funções.
  </how-to-verify>

  <resume-signal>Cole no chat: o `max(version)` usado, a tabela do censo de status (PASSO 1), os números dos PASSOS 3/4/5 e a prova anti-IDOR do PASSO 6. Depois digite "aplicada" ou descreva a divergência.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Hooks useCancelledRevenue e useNaoClassificadoItems</name>

  <read_first>
    - `src/hooks/useMLCostWaterfall.ts:31-81` — o padrão de hook de RPC com escopo de lojas: `useMLStore().resolvedMLUserIds` + `useOrganization().currentOrg`, queryKey `["ml", <nome>, orgId, resolvedMLUserIds, from, to]`, `.substring(0,10)` nas datas, `enabled` com guard. É a forma do `useCancelledRevenue`.
    - `src/hooks/useImpostoGuiaReal.ts:47-72` — o padrão de hook de RPC **por competência** (só org + mês, sem lojas), queryKey `["dre", <nome>, orgId, mes]`, mapeamento com `(r: any)`. É a forma do `useNaoClassificadoItems`.
    - `src/hooks/useDreOperational.ts` — o padrão de "tipo mora no módulo puro, hook re-exporta" e o footgun do `p_month` sempre em `"YYYY-MM-01"`, nunca `"YYYY-MM"`.
  </read_first>

  <files>src/hooks/useCancelledRevenue.ts (novo), src/hooks/useNaoClassificadoItems.ts (novo)</files>

  <action>
    **`src/hooks/useCancelledRevenue.ts`** — espelhe `useMLCostWaterfall`:
    - `export function useCancelledRevenue(from: string, to: string)` → `useQuery<{ cancelledRevenue: number; cancelledOrders: number }>`.
    - queryKey `["ml", "cancelled-revenue", orgId, resolvedMLUserIds, from, to]`; `supabase.rpc("get_cancelled_revenue", { p_org_id, p_user_ids, p_from: from.substring(0,10), p_to: to.substring(0,10) })`.
    - Devolva **sempre um objeto** com `cancelledRevenue: 0` quando não houver linha — nunca `null`. Um mês sem cancelamento é um estado válido, e o card precisa somar 0, não travar. **Não** copie o guard `if (paid_revenue === 0) return null` de `useMLCostWaterfall.ts:62`.
    - `enabled: !!orgId && resolvedMLUserIds.length > 0 && !!from && !!to`; `staleTime: 5 * 60 * 1000`.

    **`src/hooks/useNaoClassificadoItems.ts`** — espelhe `useImpostoGuiaReal`:
    - `export interface NaoClassificadoItem { outflowDate: string; competenceMonth: string; description: string; supplier: string | null; category: string; amount: number; documentNumber: string | null }`.
    - `export function useNaoClassificadoItems(saleMonth: string)` → `useQuery<NaoClassificadoItem[]>`, queryKey `["dre", "nao-classificado-items", orgId, saleMonth]`.
    - `supabase.rpc("get_dre_nao_classificado_items", { p_org_id: orgId, p_month: saleMonth })`.
    - ⚠️ **`p_month` tem que ser `"YYYY-MM-01"`** (primeiro dia do mês), nunca `"YYYY-MM"` — o mesmo footgun documentado em `useDreOperational.ts`. O caller (96-06) passa `dreSaleMonth`, que já é o mês de VENDA (não o M+1 da guia — o M+1 é só do imposto).
    - Devolva `[]` quando vazio, nunca `null`.

    **Nenhum dos dois hooks é consumido neste plano** — a fiação é do 96-05 (cancelados) e do 96-06 (não classificado). Este repo **não regenera `types.ts`** para RPC nova (precedente aceito na Phase 90-04): mapeie a linha crua com `(r: any)`, como `useImpostoGuiaReal.ts:65`.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` → 0 erros; `npx vitest run` → suíte verde (baseline preservada).
    - `grep -c 'get_cancelled_revenue' src/hooks/useCancelledRevenue.ts` = 1.
    - `grep -c 'get_dre_nao_classificado_items' src/hooks/useNaoClassificadoItems.ts` = 1.
    - `grep -c 'return null' src/hooks/useCancelledRevenue.ts src/hooks/useNaoClassificadoItems.ts` → 0 (ausência de dado é 0/[], não null).
    - `grep -rn 'useCancelledRevenue\|useNaoClassificadoItems' src/pages src/components` → sem resultados (a fiação é dos planos seguintes).
  </acceptance_criteria>

  <done>Dois hooks tipados e org-scoped, alinhados aos eixos de mês corretos (lojas+range para cancelados; competência para não classificado), nenhum ainda consumido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → RPC `get_cancelled_revenue` | `p_org_id` vem do cliente — só a RLS de `orders` impede ler outra org |
| navegador → RPC `get_dre_nao_classificado_items` | idem, sobre `cash_outflows` (dado financeiro cru: fornecedor, valor, nº do documento) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-11 | Elevation of Privilege / IDOR | as 2 RPCs novas | mitigate | `SECURITY INVOKER` + `SET search_path TO 'public'`; prova obrigatória no PASSO 6 (JWT Pé Vermeio × org Thales = 0 linhas), como role `authenticated` |
| T-96-12 | Information Disclosure | `get_dre_nao_classificado_items` expõe fornecedor/descrição/nº de documento | mitigate | `REVOKE FROM PUBLIC, anon` + `GRANT TO authenticated`; RLS org-scoped de `cash_outflows` já provada nas Phases 43/87. Sem o REVOKE, o default de EXECUTE exporia contas a pagar para `anon` |
| T-96-13 | Tampering | `dre_bloco_for_category` divergir do CASE da RPC 87 | mitigate | PASSO 4 prova equivalência sobre TODAS as categorias vivas da org; divergência > 0 bloqueia o plano |
| T-96-14 | Repudiation | flag `double_count_risk` continua só sinalizando (o valor É somado) | accept | **intencional** — decisão do dono (C9): Wesley corrige na fonte (Tiny). Nada de auto-netting |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | **nenhum pacote novo neste plano** — zero instalações |
</threat_model>

<verification>
- Censo de status de maio bate com o CONTEXT (paid 247.216,12 / cancelled 14.063,90 / partially_refunded 1 pedido).
- `get_cancelled_revenue` de maio = 14.063,90; bruto composto = 261.280,02.
- `get_dre_nao_classificado_items` de maio soma 10.809,20 (igual à linha da cascata).
- Helper equivalente à RPC 87 em 100% das categorias vivas.
- Anti-IDOR provado nas 2 RPCs como role real.
- `npx tsc --noEmit` + `npx vitest run` verdes.
</verification>

<success_criteria>
- SC1 (parcela não classificado): os 10.809,20 ficam identificados e listados, prontos para o Wesley recategorizar.
- C1 destravado no backend sem tocar em `paid_revenue`: `/financeiro`, MCO, GoalsCard e Nexo continuam idênticos.
- Nenhuma função existente dropada ou alterada.
</success_criteria>

<artifacts_this_plan_produces>
| Símbolo | Tipo | Onde |
|---|---|---|
| `get_cancelled_revenue(uuid,text[],date,date)` | RPC nova | prod `ckcdevcxgvueywivefgx` + migration |
| `dre_bloco_for_category(text)` | função helper nova (IMMUTABLE) | prod + migration |
| `get_dre_nao_classificado_items(uuid,date)` | RPC nova | prod + migration |
| `useCancelledRevenue` | hook novo | `src/hooks/useCancelledRevenue.ts` |
| `useNaoClassificadoItems` | hook novo | `src/hooks/useNaoClassificadoItems.ts` |
| `NaoClassificadoItem` | interface nova | `src/hooks/useNaoClassificadoItems.ts` |
| `2026XXXXXXXXXX_dre_cancelled_revenue_and_nao_classificado.sql` | migration nova (numerada no checkpoint) | `supabase/migrations/` |
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/96-dre-corre-es-da-revis-o-linha-a-linha-c1-c9-c11-fechar-a-dre/96-04-SUMMARY.md` when done
</output>
