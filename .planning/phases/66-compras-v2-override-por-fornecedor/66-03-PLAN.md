---
phase: 66-compras-v2-override-por-fornecedor
plan: 03
type: execute
wave: 3
depends_on: ["66-02"]
files_modified:
  - src/lib/analysis/replenishmentUtils.ts
  - src/lib/analysis/replenishmentUtils.test.ts
  - src/hooks/usePurchaseOrderSuppliers.ts
  - src/hooks/useReplenishmentBySku.ts
  - src/components/mercadolivre/ReplenishmentParamsDialog.tsx
autonomous: true
requirements: [FORN-04, FORN-05]
user_setup: []

must_haves:
  truths:
    - "resolveParamsBySku reflete a precedência de 4 níveis SKU > fornecedor > marca > global, com testes cobrindo todos os fallbacks (incluindo SKU sem fornecedor → pula para marca/global)"
    - "param_origem no TS inclui 'fornecedor' (hook não descarta o valor)"
    - "O diálogo de Regras de Compra permite escolher escopo 'Por Fornecedor' (owner/admin) com o fornecedor selecionado por dropdown dos fornecedores distintos das OCs — não texto livre"
    - "A suíte vitest passa inteira (208 existentes + novos casos), tsc 0 e build ok — sem regressão"
  artifacts:
    - path: "src/lib/analysis/replenishmentUtils.ts"
      provides: "resolveParamsBySku com 4º nível fornecedor (FORN-05)"
      contains: "fornecedorRow"
    - path: "src/hooks/usePurchaseOrderSuppliers.ts"
      provides: "Hook que lê get_purchase_order_suppliers para o dropdown (FORN-04)"
      contains: "get_purchase_order_suppliers"
    - path: "src/components/mercadolivre/ReplenishmentParamsDialog.tsx"
      provides: "CRUD de params com escopo 'fornecedor' + dropdown (FORN-04)"
      contains: "fornecedor"
  key_links:
    - from: "src/components/mercadolivre/ReplenishmentParamsDialog.tsx"
      to: "src/hooks/usePurchaseOrderSuppliers.ts"
      via: "dropdown de fornecedor alimentado pelo hook quando scope==='fornecedor'"
      pattern: "usePurchaseOrderSuppliers"
    - from: "src/hooks/usePurchaseOrderSuppliers.ts"
      to: "get_purchase_order_suppliers (RPC)"
      via: "supabase.rpc('get_purchase_order_suppliers', { p_org_id })"
      pattern: "get_purchase_order_suppliers"
---

<objective>
Estender o frontend `/compras` e o módulo puro espelho para o quarto nível de parametrização (fornecedor): `resolveParamsBySku` ganha o argumento `fornecedorRow` e a precedência de 4 níveis; um novo hook `usePurchaseOrderSuppliers` alimenta o dropdown; o diálogo "Regras de Compra" ganha o escopo "Por Fornecedor" com seletor por dropdown (não texto livre).

Purpose: Entregar a UI de edição (D-09/D-10/D-11) e o espelho testável da RPC (D-08), fechando a precedência observável de ponta a ponta. O dropdown garante match exato com `purchase_orders.fornecedor`, evitando override que não casa por divergência de digitação (Pitfall 1).

Output: Módulo + testes atualizados, hook novo, hook de tipo corrigido e diálogo estendido. Implementa FORN-04 e a metade frontend de FORN-05 (testes + sem regressão).

GATE: Depende do 66-02 — a RPC `get_purchase_order_suppliers` e o `param_origem='fornecedor'` precisam existir em prod para o hook e o type baterem com o banco vivo.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-compras-v2-override-por-fornecedor/66-CONTEXT.md
@.planning/phases/66-compras-v2-override-por-fornecedor/66-RESEARCH.md
@src/lib/analysis/replenishmentUtils.ts
@src/lib/analysis/replenishmentUtils.test.ts
@src/hooks/useReplenishmentBySku.ts
@src/components/mercadolivre/ReplenishmentParamsDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveParamsBySku ganha 4º nível 'fornecedor' + atualizar e ampliar testes</name>
  <read_first>
    - src/lib/analysis/replenishmentUtils.ts (assinatura atual de `resolveParamsBySku(skuRow, marcaRow, globalRow, defaults)`; tipo `ReplenishmentResult.paramOrigem`)
    - src/lib/analysis/replenishmentUtils.test.ts (linhas 142-211 — 5 chamadas existentes de `resolveParamsBySku` com 3 args posicionais que QUEBRAM ao inserir `fornecedorRow` na 2ª posição)
    - .planning/phases/66-compras-v2-override-por-fornecedor/66-RESEARCH.md (Pattern 4 — nova assinatura; "Code Examples" — os 5 novos casos de teste prontos)
    - .planning/phases/66-compras-v2-override-por-fornecedor/66-CONTEXT.md (D-08 — espelho de 4 níveis com todos os fallbacks)
  </read_first>
  <behavior>
    - skuRow presente vence tudo → origem 'sku'
    - skuRow=null, fornecedorRow presente → origem 'fornecedor' (usa params do fornecedor)
    - skuRow=null, fornecedorRow=null, marcaRow presente → origem 'marca' (pula fornecedor: SKU sem OC)
    - fornecedorRow presente, marcaRow=null → fornecedor vence global → origem 'fornecedor'
    - todos null → defaults 30/60/7/1/1, origem 'global'
    - Regressão: os 5 casos existentes continuam válidos após passar `null` na nova posição de fornecedor
  </behavior>
  <action>
    Em `replenishmentUtils.ts`: alterar a assinatura de `resolveParamsBySku` para inserir `fornecedorRow: Partial<ReplenishmentParams> | null` como 2º parâmetro (entre `skuRow` e `marcaRow`): `(skuRow, fornecedorRow, marcaRow, globalRow, defaults)`. Atualizar o tipo de retorno e a variável `origem` para a união `"sku" | "fornecedor" | "marca" | "global"`. Inserir o ramo `else if (fornecedorRow != null) { origem = "fornecedor"; source = fornecedorRow; }` ENTRE o ramo de `skuRow` e o de `marcaRow`. Atualizar também `ReplenishmentResult.paramOrigem` para incluir `"fornecedor"`. Em `replenishmentUtils.test.ts`: atualizar as 5 chamadas existentes de `resolveParamsBySku(...)` inserindo `null` na 2ª posição (ex.: `resolveParamsBySku(skuRow, null, marcaRow, globalRow)`), preservando as asserções. Adicionar os 5 novos casos da seção "Code Examples" do RESEARCH (fornecedor presente vence global; sku vence fornecedor; fornecedor=null pula para marca; fornecedor>global; todos null). Implementa FORN-05 (espelho + casos). NÃO tocar `calcReplenishment`, `resolveParams` (Phase 62) nem `compraUtils` (legado).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run src/lib/analysis/replenishmentUtils.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `resolveParamsBySku` tem 5 parâmetros com `fornecedorRow` em 2º e retorna `origem` ∈ {sku, fornecedor, marca, global}.
    - As 5 chamadas antigas foram atualizadas para a nova aridade; os 5 casos novos passam.
    - `npx vitest run src/lib/analysis/replenishmentUtils.test.ts` verde, sem nenhum teste falhando.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Hook usePurchaseOrderSuppliers + corrigir tipo param_origem em useReplenishmentBySku</name>
  <read_first>
    - src/hooks/useReplenishmentBySku.ts (interface `ReplenishmentSkuRow.param_origem` linha 44 e o cast em `mapRow` linha 129 — ambos `"sku" | "marca" | "global"`, faltando 'fornecedor' — Pitfall 5)
    - .planning/phases/66-compras-v2-override-por-fornecedor/66-RESEARCH.md (Pattern 6 — código completo do hook usePurchaseOrderSuppliers)
    - src/contexts/OrganizationContext (useOrganization → currentOrg.id) e src/integrations/supabase/client (supabase)
  </read_first>
  <action>
    Criar `src/hooks/usePurchaseOrderSuppliers.ts` (named export `usePurchaseOrderSuppliers`) que usa `useQuery` com `queryKey: ["get_purchase_order_suppliers", currentOrg?.id]`, `enabled: !!currentOrg?.id`, `staleTime: 5*60*1000`, e `queryFn` que chama `supabase.rpc("get_purchase_order_suppliers", { p_org_id: currentOrg.id })`, lança em erro, e retorna `(data ?? []).map((r: { fornecedor: string }) => r.fornecedor)` (`Promise<string[]>`). Em `useReplenishmentBySku.ts`: atualizar a interface `ReplenishmentSkuRow.param_origem` para `"sku" | "fornecedor" | "marca" | "global"` e o cast correspondente em `mapRow` (incluir 'fornecedor' na união). Implementa o backend de UI de FORN-04 e evita descartar 'fornecedor' (Pitfall 5).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && grep -q "get_purchase_order_suppliers" src/hooks/usePurchaseOrderSuppliers.ts && grep -q '"sku" | "fornecedor" | "marca" | "global"' src/hooks/useReplenishmentBySku.ts && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `usePurchaseOrderSuppliers` existe, retorna `string[]`, escopado por `currentOrg.id`.
    - `param_origem` (interface + cast) inclui 'fornecedor' em `useReplenishmentBySku.ts`.
    - `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ReplenishmentParamsDialog — escopo 'fornecedor' com dropdown</name>
  <read_first>
    - src/components/mercadolivre/ReplenishmentParamsDialog.tsx (type `Scope` linha 34; `paramsSchema` linha 49; `SCOPE_LABELS` linha 71; `scopeValueLabel` linha 77; o Select de escopo linha 250-260; o campo `scope_value` Input linha 268-280; o payload com `scope === "global" ? ""` linha 205)
    - .planning/phases/66-compras-v2-override-por-fornecedor/66-RESEARCH.md (Pattern 5 — pontos de extensão; D-10 dropdown obrigatório; D-11 só fornecedor com OC)
    - src/hooks/usePurchaseOrderSuppliers.ts (criado na Task 2)
  </read_first>
  <action>
    Estender o diálogo: (1) adicionar `"fornecedor"` ao type `Scope` e ao `z.enum([...])` do `paramsSchema`. (2) adicionar `fornecedor: "Por Fornecedor"` em `SCOPE_LABELS` e o caso correspondente em `scopeValueLabel` (ex.: "Fornecedor (das ordens de compra)"). (3) adicionar `<SelectItem value="fornecedor">Por Fornecedor</SelectItem>` ao Select de escopo. (4) Quando `scope === "fornecedor"`, renderizar um `<Select>` (não `<Input>`) para `scope_value`, com opções vindas de `usePurchaseOrderSuppliers()`: usar `Controller`/`setValue` para gravar o valor selecionado em `scope_value`; durante o load mostrar `<Skeleton>`; se a lista vier vazia, mostrar mensagem orientando a sincronizar as OCs primeiro (D-11). Manter o `<Input>` livre para `scope === "marca"` e `scope === "sku"`. (5) garantir que o payload de insert/update grava `scope_value` = valor exato do dropdown (match com `purchase_orders.fornecedor`). (6) ajustar o texto de precedência visível (linhas ~263 e ~482) para "Por SKU supera Por Fornecedor, que supera Por Marca, que supera Global". Reusar o RLS write owner/admin existente (`canEdit`) — sem mudança de permissão. Implementa FORN-04.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && grep -q "usePurchaseOrderSuppliers" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && grep -q '"fornecedor"' src/components/mercadolivre/ReplenishmentParamsDialog.tsx && npx tsc --noEmit 2>&1 | tail -10 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - O escopo "Por Fornecedor" aparece no Select; ao escolhê-lo, o campo de valor é um dropdown alimentado por `usePurchaseOrderSuppliers` (nunca texto livre).
    - Estado de loading (Skeleton) e lista vazia (mensagem orientando re-sync) tratados.
    - O texto de precedência reflete SKU > Fornecedor > Marca > Global.
    - `tsc --noEmit` 0 + `npm run build` ok.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente (dialog) → replenishment_params (PostgREST) | INSERT/UPDATE de params; RLS `rp_write` exige owner/admin |
| Cliente → get_purchase_order_suppliers (RPC) | leitura org-scoped via SECURITY INVOKER |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-07 | Elevation of Privilege | escrita de param por viewer/member | mitigate | `canEdit = owner/admin` no client + RLS `rp_write` no servidor (padrão Phase 63, reusado sem mudança); toast 42501 já tratado |
| T-66-08 | Tampering (match quebrado) | scope_value de fornecedor divergente de purchase_orders.fornecedor | mitigate | Dropdown obrigatório (D-10) — grava o valor exato da coluna; sem `<Input>` livre para fornecedor (Pitfall 1) |
| T-66-09 | Information Disclosure | hook lê fornecedores | mitigate | RPC SECURITY INVOKER (66-02) + escopo `currentOrg.id`; cross-org já = 0 (validado no 66-02) |
| T-66-SC | Tampering (supply chain) | npm installs | accept | Nenhuma dependência nova — só extensão de código com libs já em prod (RESEARCH: "Nenhuma dependência nova") |
</threat_model>

<verification>
- `npx vitest run` (suíte inteira): 208 existentes + novos casos verdes.
- `npx tsc --noEmit`: 0 erros.
- `npm run build`: ok.
- Inspeção: dropdown de fornecedor presente quando escopo='fornecedor'; texto de precedência de 4 níveis.
</verification>

<success_criteria>
- FORN-04: CRUD de params por fornecedor (owner/admin) com dropdown dos fornecedores distintos das OCs; precedência visível na UI.
- FORN-05 (frontend): `resolveParamsBySku` de 4 níveis testado (todos os fallbacks) + suíte/tsc/build sem regressão.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plano 66-03)

| Símbolo / Artefato | Tipo | Estado ao fim do plano |
|--------------------|------|------------------------|
| `resolveParamsBySku(skuRow, fornecedorRow, marcaRow, globalRow, defaults)` | função TS | assinatura de 4 níveis |
| `ReplenishmentResult.paramOrigem` inclui 'fornecedor' | tipo TS | atualizado |
| 5 novos casos de teste de fornecedor | testes vitest | adicionados (+ 5 antigos migrados p/ nova aridade) |
| `usePurchaseOrderSuppliers` | hook React | criado |
| `ReplenishmentSkuRow.param_origem` inclui 'fornecedor' | tipo TS | corrigido (hook não descarta valor) |
| Escopo "Por Fornecedor" + dropdown no diálogo | UI | criado |
</artifacts>

<output>
Create `.planning/phases/66-compras-v2-override-por-fornecedor/66-03-SUMMARY.md` when done.
</output>
