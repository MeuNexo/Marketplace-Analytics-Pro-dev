---
phase: 66-compras-v2-override-por-fornecedor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260666000000_fornecedor_scope.sql
  - supabase/functions/sync-tiny-purchase-orders/index.ts
autonomous: false
requirements: [FORN-01, FORN-02]
user_setup: []

must_haves:
  truths:
    - "A migration 20260666000000 (coluna purchase_orders.fornecedor + scope='fornecedor') está versionada no git na branch da Phase 66 (não mais untracked)"
    - "A EF sync-tiny-purchase-orders que grava fornecedor = contato.nome está commitada e deployada em prod"
    - "Após o re-sync, purchase_orders.fornecedor está populado (> 0 OCs com fornecedor) na org Pé Vermeio"
    - "A lista de fornecedores distintos foi inspecionada e os nomes estão limpos/consistentes (sem duplicatas por casing/espaço) — gate D-12/D-13 aprovado antes de a precedência entrar"
  artifacts:
    - path: "supabase/migrations/20260666000000_fornecedor_scope.sql"
      provides: "Fundação de schema: coluna fornecedor + scope fornecedor na constraint (FORN-01/FORN-02)"
      contains: "fornecedor"
    - path: "supabase/functions/sync-tiny-purchase-orders/index.ts"
      provides: "EF grava fornecedor = contato.nome no upsert de purchase_orders (FORN-01)"
      contains: "fornecedor"
  key_links:
    - from: "supabase/functions/sync-tiny-purchase-orders/index.ts"
      to: "purchase_orders.fornecedor"
      via: "upsert grava o campo fornecedor derivado de det.contato.nome"
      pattern: "fornecedor"
---

<objective>
Commitar e ativar a fundação de dados da Phase 66 (já parcialmente em prod, mas untracked/não-deployada — D-14), e travar o gate de qualidade dos nomes de fornecedor (D-12/D-13) ANTES de qualquer precedência entrar em produção.

Purpose: A precedência por fornecedor (plano 66-02) só pode casar corretamente se `purchase_orders.fornecedor` estiver populado E os nomes estiverem limpos. Ligar o override sobre dados sujos casaria errado e passaria despercebido. Este plano garante dado bom antes da lógica.

Output: Migration `20260666000000` versionada na branch; EF commitada + deployada; `purchase_orders.fornecedor` populado via re-sync; lista de fornecedores validada (checkpoint bloqueante). Implementa FORN-01 e FORN-02.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/66-compras-v2-override-por-fornecedor/66-CONTEXT.md
@.planning/phases/66-compras-v2-override-por-fornecedor/66-RESEARCH.md
@supabase/migrations/20260666000000_fornecedor_scope.sql
@supabase/functions/sync-tiny-purchase-orders/index.ts
@supabase/migrations/20260665000000_purchase_orders.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commitar fundação (migration untracked + EF local) na branch da Phase 66</name>
  <read_first>
    - supabase/migrations/20260666000000_fornecedor_scope.sql (untracked, já aplicado em prod — D-14)
    - supabase/functions/sync-tiny-purchase-orders/index.ts (alteração local não commitada: linha ~118 grava `fornecedor`, linha ~136 inclui no upsert)
    - .planning/phases/66-compras-v2-override-por-fornecedor/66-CONTEXT.md (D-14: commitar na branch própria gsd/phase-66-override-fornecedor, NÃO na gsd/phase-65)
  </read_first>
  <action>
    Confirmar que a branch atual é `gsd/phase-66-override-fornecedor` (já é, per STATE.md). NÃO editar o conteúdo dos arquivos — eles já estão corretos: a migration adiciona `purchase_orders.fornecedor TEXT` com `IF NOT EXISTS` (idempotente) e troca a constraint `replenishment_params_scope_check` para incluir `'fornecedor'`; a EF deriva `fornecedor` de `det.contato.nome` (fallback `h.contato.nome`) com `.trim().slice(0, 200)` e `|| null`, e inclui o campo `fornecedor` no objeto de upsert de `purchase_orders`. Apenas versionar ambos no git: `git add` da migration `20260666000000_fornecedor_scope.sql` (untracked) e da EF `sync-tiny-purchase-orders/index.ts` (modified), e commitar com mensagem referenciando FORN-01/FORN-02 e D-14. Implementa FORN-01 (coluna + EF grava fornecedor) e FORN-02 (scope='fornecedor' na constraint).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && git status --porcelain supabase/migrations/20260666000000_fornecedor_scope.sql supabase/functions/sync-tiny-purchase-orders/index.ts | grep -c '^' | grep -qx 0 && grep -c 'fornecedor' supabase/migrations/20260666000000_fornecedor_scope.sql</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` para os dois arquivos retorna vazio (ambos commitados, working tree limpo para eles).
    - `git log -1 --name-only` lista a migration e a EF no commit, na branch `gsd/phase-66-override-fornecedor`.
    - A migration contém `ADD COLUMN IF NOT EXISTS fornecedor` e a constraint com `'fornecedor'`; a EF contém a derivação `det?.contato?.nome` e o campo `fornecedor` no upsert.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy da EF + validar registro da migration + disparar re-sync (orquestrador via MCP)</name>
  <action>Orquestrador/Wesley executa via MCP Supabase: valida o registro da migration `20260666000000` (sem reaplicar), faz `deploy_edge_function` de `sync-tiny-purchase-orders` e dispara o re-sync (`net.http_post`) para popular `purchase_orders.fornecedor`. Detalhes em how-to-verify.</action>
  <what-built>
    Fundação commitada (Task 1). Falta deployar a EF e popular `purchase_orders.fornecedor` em prod — operações de MCP que o orquestrador/Wesley executa (o gsd-executor NÃO deploya EF nem aplica/dispara via MCP).
  </what-built>
  <how-to-verify>
    Executar, no projeto Supabase `ckcdevcxgvueywivefgx`, via MCP:
    1. VALIDAR REGISTRO DA MIGRATION (NÃO reaplicar — D-14/Pitfall 3): `execute_sql` → `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '20260666000000';`. Se retornar 1 linha → migration já registrada, NÃO chamar `apply_migration` para ela. Se retornar 0 linhas → `apply_migration` com o conteúdo de `20260666000000_fornecedor_scope.sql` (é idempotente via IF NOT EXISTS).
    2. DEPLOY DA EF: `deploy_edge_function` para `sync-tiny-purchase-orders` com o código atual (já commitado). Mantém `EdgeRuntime.waitUntil` (retorna 202).
    3. RE-SYNC: `execute_sql` → `SELECT net.http_post(url := 'https://ckcdevcxgvueywivefgx.supabase.co/functions/v1/sync-tiny-purchase-orders', headers := jsonb_build_object('Authorization','Bearer '||(SELECT get_app_secret('SUPABASE_SERVICE_ROLE_KEY')),'Content-Type','application/json'), body := '{"ml_user_id":"1639558873"}'::jsonb);`
    4. Aguardar ~3 min e confirmar população: `execute_sql` → `SELECT COUNT(*) FROM purchase_orders WHERE organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7' AND fornecedor IS NOT NULL;` — esperado > 0 (era 0 antes do deploy).
  </how-to-verify>
  <resume-signal>Digite "approved" quando a EF estiver deployada e o COUNT de OCs com fornecedor for > 0. Se o COUNT continuar 0, descreva o erro (token Tiny vazio? secret do vault com nome diferente? — ver Assumptions A3).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate D-12/D-13 — validar nomes de fornecedor limpos + predominante coerente</name>
  <action>Orquestrador/Wesley inspeciona via MCP `execute_sql` a lista de fornecedores distintos e o preview do predominante por SKU, confirmando nomes limpos/consistentes antes de liberar o plano 66-02. Detalhes em how-to-verify.</action>
  <what-built>
    `purchase_orders.fornecedor` populado. Este é o gate de qualidade D-12/D-13: validar que os nomes de fornecedor estão limpos e que o "predominante" faz sentido ANTES de o plano 66-02 ligar a precedência sobre eles.
  </what-built>
  <how-to-verify>
    Executar via MCP `execute_sql` no projeto `ckcdevcxgvueywivefgx` e inspecionar visualmente:
    1. LISTA DE FORNECEDORES DISTINTOS (qualidade dos nomes): `SELECT fornecedor, COUNT(DISTINCT sku) AS skus_distintos, SUM(quantidade) AS qtd_total FROM purchase_orders WHERE organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7' AND fornecedor IS NOT NULL GROUP BY fornecedor ORDER BY qtd_total DESC;` — confirmar: sem duplicatas por casing/espaço (ex.: "Pralana" vs "Pralana "), nomes legíveis e consistentes.
    2. PREVIEW DO PREDOMINANTE POR SKU (lógica do 66-02): `SELECT DISTINCT ON (sku_code) sku_code, fornecedor, total_qty, ultima_data FROM (SELECT sku AS sku_code, fornecedor, SUM(quantidade) AS total_qty, MAX(COALESCE(data_entrega, data_pedido)) AS ultima_data FROM purchase_orders WHERE organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7' AND fornecedor IS NOT NULL GROUP BY sku, fornecedor) t ORDER BY sku_code, total_qty DESC, ultima_data DESC NULLS LAST LIMIT 20;` — confirmar por amostragem que o fornecedor predominante de cada SKU faz sentido (D-01/D-02).
    3. Confirmar preenchimento de `data_entrega` vs `data_pedido` (desempate, A2/Q3): `SELECT COUNT(*) FILTER (WHERE data_entrega IS NOT NULL) AS com_entrega, COUNT(*) FILTER (WHERE data_pedido IS NOT NULL) AS com_pedido, COUNT(*) AS total FROM purchase_orders WHERE organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7';` — se `data_entrega` for rara, o `COALESCE(data_entrega, data_pedido)` do 66-02 já cobre.
  </how-to-verify>
  <resume-signal>Digite "approved" se os nomes estão limpos e o predominante faz sentido → libera o plano 66-02. Se houver nomes sujos/duplicados, NÃO prossiga: descreva o caso para decidir limpeza (na EF ou nos dados) antes de ligar a precedência.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tiny ERP API → EF (Deno) | `det.contato.nome` é dado externo não confiável que entra em `purchase_orders.fornecedor` |
| EF (service_role) → purchase_orders | escrita privilegiada; re-sync disparado por `service_role_key` do vault |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering/Injection | EF grava `contato.nome` em `fornecedor` | mitigate | Valor é coerced com `String(...).trim().slice(0,200)` e `|| null`; upsert parametrizado pelo supabase-js (sem SQL string concat) |
| T-66-02 | Information Disclosure | Re-sync via `net.http_post` com service_role | mitigate | Disparo só via MCP `execute_sql` (orquestrador/Wesley); secret lido do vault `get_app_secret`, nunca hardcoded no repo |
| T-66-03 | Elevation/Scope | EF resolve `organization_id` via `ml_tokens` por `ml_user_id` | accept | EF já aborta se `organization_id` ausente (linha 83); padrão existente Phase 65, sem mudança |
| T-66-SC | Tampering (supply chain) | npm/deno installs | accept | Phase 66 não instala nenhum pacote novo (RESEARCH: "Não aplicável"); EF usa imports já fixados |
</threat_model>

<verification>
- `git status --porcelain` limpo para a migration e a EF (commitados na branch da Phase 66).
- `SELECT version FROM supabase_migrations.schema_migrations WHERE version='20260666000000'` retorna 1 (registrada; não reaplicar).
- `SELECT COUNT(*) FROM purchase_orders WHERE organization_id='7f615df7-...' AND fornecedor IS NOT NULL` > 0 após re-sync.
- Checkpoint D-12/D-13 aprovado: nomes de fornecedor limpos + predominante coerente.
</verification>

<success_criteria>
- FORN-01: coluna `purchase_orders.fornecedor` em prod + EF deployada gravando `contato.nome` (provado por COUNT > 0).
- FORN-02: constraint `replenishment_params_scope_check` aceita `'fornecedor'` (migration registrada).
- Gate de qualidade dos nomes aprovado — pré-requisito bloqueante para 66-02.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plano 66-01)

| Símbolo / Artefato | Tipo | Estado ao fim do plano |
|--------------------|------|------------------------|
| `purchase_orders.fornecedor` (coluna TEXT) | schema | versionada no git + em prod + populada via re-sync |
| `scope='fornecedor'` em `replenishment_params_scope_check` | constraint | versionada + em prod |
| `sync-tiny-purchase-orders` grava `fornecedor=contato.nome` | edge function | commitada + deployada |
</artifacts>

<output>
Create `.planning/phases/66-compras-v2-override-por-fornecedor/66-01-SUMMARY.md` when done.
</output>
