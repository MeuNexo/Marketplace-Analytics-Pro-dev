---
phase: 67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - src/lib/analysis/replenishmentUtils.ts
  - src/lib/analysis/replenishmentUtils.test.ts
autonomous: true
requirements: [SMART-01, SMART-02, SMART-03, SMART-04]
must_haves:
  truths:
    - "calcEwmaDaily retorna a média ponderada por decaimento (alpha=0.3) ÷7 e null quando <2 semanas"
    - "calcSeasonalFactor retorna fator clampado [0.5,2.5] e active=true só com >=12 meses; senão {factor:1.0, active:false}"
    - "calcTrend retorna ↑/↓/~ pelo threshold de 20% recente vs antigo"
    - "resolveSmartLeadTime usa a mediana real quando ocCount>=2; senão cai no param (precedência Phase 66)"
    - "Os testes existentes (calcReplenishment, resolveParams, resolveParamsBySku) continuam verdes — sem regressão das Phases 62-66"
  artifacts:
    - path: "src/lib/analysis/replenishmentUtils.ts"
      provides: "Funções puras espelho da RPC esperta (EWMA, sazonal, tendência, lead time)"
      contains: "calcEwmaDaily"
    - path: "src/lib/analysis/replenishmentUtils.test.ts"
      provides: "Testes vitest das novas funções + casos de fallback por dimensão"
      contains: "calcSeasonalFactor"
  key_links:
    - from: "replenishmentUtils (constantes alpha/clamp/limiares)"
      to: "migration 20260667000100 (RPC esperta)"
      via: "mesmos valores (alpha=0.3, clamp 0.5/2.5, 12 meses, K=2 OCs, threshold 0.20)"
      pattern: "0.3"
---

<objective>
Estender o módulo puro `replenishmentUtils.ts` (espelho testável da RPC) com as funções da camada esperta, espelhando EXATAMENTE as fórmulas e limiares da migration `20260667000100` (Plano 67-01): EWMA diária, fator sazonal por marca/mês, tendência (badge), e resolução de lead time real com fallback no param. Cobrir cada dimensão e seus fallbacks com testes vitest, sem quebrar os testes existentes.

Purpose: Travar as fórmulas espertas em código puro auditável (SMART-04 "espelho TS testável"), garantir paridade com o SQL e provar os limiares de fallback (SMART-03/D-08/D-09). É a rede de segurança contra regressão das Phases 62-66.

Output: `replenishmentUtils.ts` + `replenishmentUtils.test.ts` estendidos; suíte vitest verde; tsc limpo.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-CONTEXT.md
@.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-RESEARCH.md
@src/lib/analysis/replenishmentUtils.ts
@src/lib/analysis/replenishmentUtils.test.ts
@supabase/migrations/20260667000100_get_replenishment_by_sku_smart.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Funções puras de velocidade esperta (EWMA, fator sazonal, tendência) + testes</name>
  <files>src/lib/analysis/replenishmentUtils.ts, src/lib/analysis/replenishmentUtils.test.ts</files>
  <read_first>
    - 67-RESEARCH.md seção "5. replenishmentUtils.ts — espelho puro testável" (assinaturas exatas de calcEwmaDaily, calcSeasonalFactor, calcTrend) e "Fórmulas Concretas" 1/2/4 (constantes)
    - src/lib/analysis/replenishmentUtils.ts (estilo: funções puras exportadas, JSDoc, sem deps React/Supabase)
    - supabase/migrations/20260667000100_get_replenishment_by_sku_smart.sql (constantes finais pós-checkpoint — alpha, clamp, limiares devem casar)
  </read_first>
  <behavior>
    - calcEwmaDaily([{weekOffset,qty}...], alpha=0.3): com >=2 semanas, retorna SUM(qty·decay^offset)/SUM(decay^offset)/7; com <2 semanas retorna null (fallback à média plana)
    - calcEwmaDaily: semana mais recente (offset 0) pesa mais que offset 3 (decay=0.7); espaçamento real preservado (offset, não rank)
    - calcSeasonalFactor(map<mes,avg>, targetMonth): com <12 meses → {factor:1.0, active:false}; com >=12 meses → factor = targetAvg/globalAvg clampado [0.5,2.5], active:true
    - calcSeasonalFactor: globalAvg=0 ou mês ausente → {factor:1.0, active:false}
    - calcTrend(recent, older, 0.20): recent>older·1.20 → '↑'; recent<older·0.80 → '↓'; entre → '~'; null/zero → '~'
  </behavior>
  <action>
    Acrescentar a `replenishmentUtils.ts` as três funções puras com as assinaturas EXATAS do 67-RESEARCH seção 5: `calcEwmaDaily(weeklyObs, alpha=0.3)`, `calcSeasonalFactor(monthlyAvgs, targetMonth, clampMin=0.5, clampMax=2.5)` e `calcTrend(ewmaRecentDaily, ewmaOlderDaily, threshold=0.20)`. Constantes (alpha 0.3 / decay 0.7, clamp 0.5–2.5, limiar 12 meses, threshold 0.20, mínimo 2 semanas) DEVEM ser idênticas às da migration 67-01 (D-02/D-03/D-09) — se o checkpoint do 67-01 tiver ajustado algum valor, usar o valor final da migration. Adicionar os testes correspondentes em `replenishmentUtils.test.ts` em novos blocos `describe`, cobrindo os casos do bloco <behavior> (inclusive os fallbacks por limiar). NÃO modificar as funções/testes existentes (calcReplenishment, resolveParams, resolveParamsBySku).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run src/lib/analysis/replenishmentUtils.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - calcEwmaDaily, calcSeasonalFactor, calcTrend exportadas com assinaturas do RESEARCH
    - Constantes idênticas às da migration 67-01
    - Novos testes cobrem caminho esperto E fallback (null/1.0/'~') de cada função
    - Testes existentes continuam verdes; tsc limpo
  </acceptance_criteria>
  <done>Funções de velocidade esperta + testes verdes; paridade de constantes com a RPC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolução de lead time real com fallback transparente + origem por dimensão + testes</name>
  <files>src/lib/analysis/replenishmentUtils.ts, src/lib/analysis/replenishmentUtils.test.ts</files>
  <read_first>
    - 67-RESEARCH.md seção "Fórmula 3" + "Fallback chain para lead time" + "Limiares de Fallback Concretos" (origem 'fornecedor_real' vs 'param', K=2)
    - src/lib/analysis/replenishmentUtils.ts (padrão de resolveParamsBySku — origem como discriminador)
  </read_first>
  <behavior>
    - resolveSmartLeadTime({medianLeadDays, ocCount}, paramLeadTime, smart): smart=true & ocCount>=2 & medianLeadDays>0 → {leadTime: medianLeadDays, origem:'fornecedor_real'}
    - resolveSmartLeadTime: smart=false OU ocCount<2 OU sem mediana → {leadTime: paramLeadTime, origem:'param'} (precedência Phase 66 intocada)
    - resolveVendaDiaOrigem(weeksWithSales, sazonalAtiva, smart): smart & weeks>=2 & sazonal → 'ewma_sazonal'; smart & weeks>=2 → 'ewma'; senão → 'simples'
  </behavior>
  <action>
    Adicionar a `replenishmentUtils.ts` os helpers puros que espelham a resolução de origem da RPC (SMART-02/SMART-03/D-07/D-08): `resolveSmartLeadTime(...)` retornando `{ leadTime, origem: 'fornecedor_real' | 'param' }` e `resolveVendaDiaOrigem(...)` retornando `'ewma_sazonal' | 'ewma' | 'simples'`, com os limiares K>=2 OCs e >=2 semanas. Tipar as origens como union types exportados (espelhando a interface ReplenishmentSkuRow do hook). Adicionar testes vitest cobrindo cada ramo de fallback do bloco <behavior>, incluindo o caso "modo simples" (sem base → 'simples'/'param'). Não tocar nas funções existentes.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx vitest run src/lib/analysis/replenishmentUtils.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - resolveSmartLeadTime e resolveVendaDiaOrigem exportadas com union types de origem
    - Limiares (K>=2, semanas>=2) idênticos à migration 67-01
    - Testes cobrem fornecedor_real, param, ewma_sazonal, ewma e simples
    - Suíte completa verde; tsc limpo
  </acceptance_criteria>
  <done>Helpers de fallback transparente + testes verdes; paridade com a RPC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | módulo puro sem I/O, rede, DOM ou banco — não cruza fronteira de confiança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05 | Tampering | divergência espelho-TS vs RPC | mitigate | constantes/limiares idênticos à migration 67-01; testes provam cada limiar; revisão cruzada no SUMMARY |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo (só TypeScript puro com libs já instaladas) |
</threat_model>

<verification>
- vitest da suíte de replenishmentUtils (novos + existentes) verde; tsc --noEmit limpo.
</verification>

<success_criteria>
- 5 funções/helpers novos espelham a RPC esperta com constantes idênticas; cada dimensão e seu fallback têm teste; zero regressão dos testes das Phases 62-66.
</success_criteria>

<output>
Create `.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-02-SUMMARY.md` when done
</output>
