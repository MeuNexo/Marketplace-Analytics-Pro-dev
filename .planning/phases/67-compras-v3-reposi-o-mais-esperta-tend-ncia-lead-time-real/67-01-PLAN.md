---
phase: 67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260667000100_get_replenishment_by_sku_smart.sql
autonomous: false
requirements: [SMART-01, SMART-02, SMART-03, SMART-04]
must_haves:
  truths:
    - "Com p_smart=TRUE, venda_dia reflete EWMA × fator sazonal (quando há base suficiente); p_smart=FALSE reproduz EXATAMENTE o avg_daily da Phase 66 (sem regressão)"
    - "lead_time_dias usa a mediana real por fornecedor quando oc_count>=2; senão cai na precedência de params SKU>fornecedor>marca>global da Phase 66"
    - "A RPC retorna 5 colunas novas de transparência: venda_dia_origem, lead_time_origem, tendencia, fator_sazonal, lead_time_real"
    - "Cada camada esperta liga independentemente por limiar (EWMA>=2 semanas, sazonal>=12 meses, lead time>=2 OCs); sem base, a dimensão cai no simples"
    - "A RPC permanece SECURITY INVOKER; consulta com JWT de org alheia retorna 0 linhas (anti-IDOR)"
  artifacts:
    - path: "supabase/migrations/20260667000100_get_replenishment_by_sku_smart.sql"
      provides: "RPC get_replenishment_by_sku v7 com p_smart + 3 CTEs novas + 5 colunas"
      contains: "ewma_sales"
  key_links:
    - from: "get_replenishment_by_sku (ewma_sales/seasonal_index)"
      to: "orders + ml_inventory_cache"
      via: "organization_id = p_org_id em CADA CTE nova (anti-IDOR paramétrico)"
      pattern: "organization_id = p_org_id"
    - from: "get_replenishment_by_sku (lead_time_by_fornecedor)"
      to: "purchase_orders"
      via: "percentile_cont(0.5) WITHIN GROUP por fornecedor + JOIN fornecedor_by_sku"
      pattern: "percentile_cont"
---

<objective>
Reescrever a RPC `get_replenishment_by_sku` (CREATE OR REPLACE, migration nova) trocando os dois insumos "burros" — velocidade de venda (média plana) e lead time (param fixo) — por sinais espertos com fallback transparente por dimensão. Acrescenta o 4º parâmetro `p_smart BOOLEAN DEFAULT TRUE`, três CTEs novas (`ewma_sales`, `seasonal_index`, `lead_time_by_fornecedor`) e 5 colunas de saída para alimentar badges. Toda a fundação das Phases 62/63/65/66 (CTEs `inventory_by_sku`, `sales_by_sku`, `incoming_by_sku`, `fornecedor_by_sku`, fórmula de ponto/alvo/compra, params por escopo) fica INTOCADA.

Purpose: Tornar ponto de reposição e alvo mais precisos (tendência + sazonalidade de rodeios + prazo real do fornecedor) sem perder a auditabilidade — e sem regressão quando o toggle está OFF. Implementa SMART-01 (EWMA+sazonal), SMART-02 (lead time real), SMART-03 (fallback por dimensão), SMART-04 (toggle p_smart + anti-IDOR + sem regressão).

Output: Migration `20260667000100_get_replenishment_by_sku_smart.sql` + aplicação/validação em prod via checkpoint do orquestrador.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-CONTEXT.md
@.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-RESEARCH.md
@supabase/migrations/20260666000100_get_replenishment_by_sku_fornecedor.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration da RPC esperta (p_smart + 3 CTEs novas + 5 colunas)</name>
  <files>supabase/migrations/20260667000100_get_replenishment_by_sku_smart.sql</files>
  <read_first>
    - supabase/migrations/20260666000100_get_replenishment_by_sku_fornecedor.sql (RPC ATUAL — copiar verbatim as CTEs inventory_by_sku, sales_by_sku, incoming_by_sku, fornecedor_by_sku, base e o SELECT final; só estender o que está descrito abaixo)
    - 67-RESEARCH.md seções "Fórmulas Concretas", "Ponto de Inserção na RPC Existente", "Limiares de Fallback Concretos", "Code Examples" (SQL concreto de cada CTE nova, JOINs, substituições e GRANT/REVOKE)
  </read_first>
  <action>
    Criar `supabase/migrations/20260667000100_get_replenishment_by_sku_smart.sql` com um único `CREATE OR REPLACE FUNCTION public.get_replenishment_by_sku`, partindo de cópia verbatim da migration da Phase 66. Mudanças cirúrgicas (D-01..D-11), exatamente como o 67-RESEARCH detalha:

    1. Assinatura: acrescentar 4º parâmetro `p_smart BOOLEAN DEFAULT TRUE` (D-10). No corpo, normalizar com uma variável local derivada de `COALESCE(p_smart, TRUE)` e usá-la em todos os ramos (Pitfall 1 — proteção contra NULL).

    2. CTE nova `ewma_sales` (D-02, SMART-01): média ponderada por decaimento exponencial sobre as últimas 12 semanas (84 dias) de `orders` (status='paid'), peso `POWER(0.7, week_offset)` com `week_offset` = offset REAL de semanas via `DATE_TRUNC('week', ...)` (não rank). Expor `ewma_daily`, `weeks_with_sales`, `ewma_recent_daily` (week_offset<4) e `ewma_older_daily` (week_offset 4..11). Filtro `organization_id = p_org_id` E `p_smart = TRUE` dentro da subquery de orders (short-circuit + anti-IDOR, Pitfall 4).

    3. CTE nova `seasonal_index` (D-03, SMART-01): índice sazonal ratio-to-average no nível MARCA (`ml_inventory_cache.brand`), só o mês corrente, lookback 2 anos. Limiar `months_covered >= 12` senão `fator_sazonal = 1.0`; clamp `GREATEST(0.5, LEAST(2.5, ...))`; expor `brand`, `fator_sazonal`, `sazonal_ativa`. Filtro `organization_id = p_org_id` E `p_smart = TRUE` nas subqueries de `ml_inventory_cache` e `orders`.

    4. CTE nova `lead_time_by_fornecedor` (D-05, SMART-02): `percentile_cont(0.5) WITHIN GROUP (ORDER BY (po.data_entrega - po.data_pedido))` por `fornecedor`, com `ROUND(...)::INTEGER`, `COUNT(*) AS oc_count`, guard `data_entrega >= data_pedido` (Pitfall 3). Sem filtro de `situacao` (a tabela já é snapshot só de OCs em trânsito, D-06). Filtro `organization_id = p_org_id` E `p_smart = TRUE`.

    5. CTE `params` (estendida, SMART-02/D-07): adicionar `LEFT JOIN lead_time_by_fornecedor lf ON lf.fornecedor = forn.fornecedor` (reusa `fornecedor_by_sku` da Phase 66). No COALESCE de `lead_time_dias`, anteceder um ramo `CASE WHEN p_smart AND lf.median_lead_days IS NOT NULL AND lf.oc_count >= 2 THEN lf.median_lead_days ELSE NULL END` ANTES dos níveis de param existentes (que permanecem idênticos). As demais colunas de params (meta_cobertura, safety, moq, pack, param_origem) ficam INTOCADAS.

    6. CTE `base` (estendida, SMART-01/D-04): JOIN `ewma_sales es` e `seasonal_index si` (via brand). Substituir o cálculo de `venda_dia` por um CASE: EWMA×sazonal quando `p_smart AND weeks_with_sales>=2 AND sazonal_ativa`; só EWMA quando `p_smart AND weeks_with_sales>=2`; senão `COALESCE(s.avg_daily,0)` (a média plana atual, fallback — Pitfall 1). `cobertura_atual`, `ponto_reposicao`, `alvo`, `compra_sugerida`, `gatilho_ativo` continuam usando a MESMA fórmula, apenas alimentada pelo novo `venda_dia` e pelo `lead_time_dias` real.

    7. RETURNS TABLE + SELECT final (estendido, SMART-03/D-08/D-11): manter TODAS as colunas atuais na MESMA ordem e acrescentar ao FINAL as 5 novas: `venda_dia_origem TEXT` ('ewma_sazonal'|'ewma'|'simples'), `lead_time_origem TEXT` ('fornecedor_real'|'param'), `tendencia TEXT` ('↑'|'↓'|'~' por threshold 20% recente vs antigo), `fator_sazonal NUMERIC` (NULL se não aplicado), `lead_time_real INTEGER` (NULL se oc_count<2). Lógica de cada coluna conforme 67-RESEARCH "Novas colunas no RETURNS TABLE e SELECT final".

    8. SECURITY INVOKER + SET search_path='public' mantidos. GRANT/REVOKE (Pitfall 2): `REVOKE` da assinatura antiga de 3 args E da nova de 4 args para PUBLIC/anon; `GRANT EXECUTE` da assinatura de 4 args (UUID, INTEGER, NUMERIC, BOOLEAN) para authenticated.

    NÃO aplicar a migration (executor não tem MCP/token Supabase) — a aplicação é a Task 2 (checkpoint do orquestrador).
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && f=$(ls supabase/migrations/20260667000100_get_replenishment_by_sku_smart.sql) && grep -q "p_smart" "$f" && grep -q "ewma_sales" "$f" && grep -q "seasonal_index" "$f" && grep -q "lead_time_by_fornecedor" "$f" && grep -q "percentile_cont" "$f" && grep -q "SECURITY INVOKER" "$f" && grep -q "UUID, INTEGER, NUMERIC, BOOLEAN" "$f" && [ "$(grep -c 'organization_id = p_org_id' "$f")" -ge 6 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration existe e contém p_smart, as 3 CTEs novas e percentile_cont
    - SECURITY INVOKER preservado; GRANT da assinatura de 4 args presente
    - Toda CTE nova que lê orders/purchase_orders/ml_inventory_cache filtra organization_id = p_org_id
    - As 5 colunas novas estão no RETURNS TABLE e no SELECT final, ao FINAL, sem reordenar as existentes
    - Nenhuma alteração na fórmula de ponto/alvo/compra/gatilho nem nos níveis de params existentes
  </acceptance_criteria>
  <done>Migration escrita, gates de grep passam; pronta para aplicação via MCP no checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Aplicar e validar a migration via MCP (checkpoint do orquestrador)</name>
  <action>O ORQUESTRADOR (não o executor) aplica a migration via MCP apply_migration e valida via execute_sql em prod, seguindo os passos de how-to-verify. Executor não tem MCP/token Supabase.</action>
  <what-built>
    Migration `20260667000100_get_replenishment_by_sku_smart.sql` — RPC esperta com p_smart, EWMA, índice sazonal, lead time real e 5 colunas de transparência.
  </what-built>
  <how-to-verify>
    O ORQUESTRADOR (não o executor) aplica e valida em prod `ckcdevcxgvueywivefgx` (org Pé Vermeio `7f615df7-7bac-45e5-8a93-827fb9ddeec7`):

    1. Aplicar via MCP `apply_migration` (nome `get_replenishment_by_sku_smart`).
    2. NÃO-REGRESSÃO (Pitfall 1, crítico): `execute_sql` chamando a RPC com `p_smart=false` e comparar `venda_dia` de uma amostra de SKUs com o `avg_daily` esperado (SUM(orders.quantidade na janela)/dias) — devem ser idênticos. Provar ao SKU.
    3. EWMA ON: chamar com `p_smart=true` e confirmar que `venda_dia`/`venda_dia_origem` mudam para SKUs com >=2 semanas de venda (origem 'ewma' ou 'ewma_sazonal').
    4. Open question A (sazonal): `SELECT i.brand, COUNT(DISTINCT to_char(o.data_pedido::date,'YYYY-MM')) FROM orders o JOIN ml_inventory_cache i ON i.item_id=o.item_id WHERE o.organization_id='7f615df7-7bac-45e5-8a93-827fb9ddeec7' AND o.status='paid' GROUP BY i.brand ORDER BY 2 DESC LIMIT 20` — confirmar quantas marcas têm >=12 meses (provável fallback fator=1.0 hoje; feature correta para o futuro).
    5. Open question B (tipo): `SELECT pg_typeof(data_entrega - data_pedido) FROM purchase_orders LIMIT 1` — se for INTERVAL (não INTEGER), trocar para `EXTRACT(DAY FROM ...)` na CTE e reaplicar.
    6. Open question C (lead time): contar fornecedores com `oc_count >= 2` e datas válidas — confirmar se `lead_time_real` liga para algum (provável fallback para param hoje, infra pronta).
    7. ANTI-IDOR: chamar a RPC com `p_org_id` de org alheia sob JWT da Pé Vermeio (impersonação `SET LOCAL ROLE authenticated` + claims) → 0 linhas.
    8. `get_advisors` (security + performance) sem novos ERROR/WARN atribuíveis à RPC; checar custo das CTEs novas com EXPLAIN se a RPC ficar lenta (Assumption A3).
  </how-to-verify>
  <resume-signal>Digite "aprovado" quando p_smart=FALSE provar não-regressão, p_smart=TRUE provar EWMA, anti-IDOR=0 e advisors limpos; ou descreva os desvios (ex.: data_entrega-data_pedido é INTERVAL) para correção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser JWT) → RPC | usuário autenticado chama a RPC com p_org_id; org real vem da RLS, não do argumento |
| RPC → orders/purchase_orders/ml_inventory_cache | leitura de dados multi-tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Information Disclosure / Elevation | CTEs novas (ewma_sales, seasonal_index, lead_time_by_fornecedor) | mitigate | `organization_id = p_org_id` explícito em CADA CTE nova + SECURITY INVOKER + RLS org-first; provado por teste de org alheia=0 linhas no checkpoint (Pitfall 4) |
| T-67-02 | Tampering | parâmetro p_smart | mitigate | BOOLEAN tipado nativamente (sem concatenação SQL); `COALESCE(p_smart, TRUE)` protege contra NULL |
| T-67-03 | Denial of Service | custo das 3 CTEs novas (EWMA/percentile sobre orders/purchase_orders) | accept | curto-circuito `p_smart = TRUE` nas subqueries quando OFF; validar custo com EXPLAIN no checkpoint; dataset Pé Vermeio é pequeno (~22 OCs, ~464 SKUs) |
| T-67-04 | Tampering | regressão silenciosa com p_smart=FALSE | mitigate | teste de não-regressão obrigatório no checkpoint (venda_dia == avg_daily ao SKU) |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo (RESEARCH Package Legitimacy Audit = SKIPPED; só SQL puro) |
</threat_model>

<verification>
- Gate de grep na migration (Task 1).
- Checkpoint do orquestrador: não-regressão (p_smart=FALSE==atual), EWMA ON, 3 open questions, anti-IDOR=0, advisors limpos.
</verification>

<success_criteria>
- RPC v7 aplicada em prod; p_smart=FALSE idêntico à Phase 66; p_smart=TRUE liga EWMA/sazonal/lead-time por limiar; 5 colunas de transparência retornadas; SECURITY INVOKER + anti-IDOR provados.
</success_criteria>

<output>
Create `.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-01-SUMMARY.md` when done
</output>
