---
phase: 67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real
plan: 03
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - src/hooks/useReplenishmentBySku.ts
  - src/pages/mercadolivre/MLCompras.tsx
  - src/components/mercadolivre/ReplenishmentSkuTable.tsx
autonomous: false
requirements: [SMART-03, SMART-04]
must_haves:
  truths:
    - "A /compras tem um toggle Calculo esperto (ON por padrao); OFF reconsulta a RPC com p_smart=false e volta ao calculo simples"
    - "O hook passa p_smart EXPLICITAMENTE (nunca undefined) e inclui smartMode na queryKey"
    - "Cada SKU mostra, em badges/tooltip, a tendencia (seta cima/baixo/estavel), o ajuste sazonal, o lead time real vs param e o sinal modo simples quando alguma dimensao caiu no fallback"
    - "tsc, build e a suite vitest existente continuam verdes (sem regressao das Phases 62-66)"
  artifacts:
    - path: "src/hooks/useReplenishmentBySku.ts"
      provides: "param smartMode + 5 campos novos na interface e no mapRow"
      contains: "p_smart"
    - path: "src/components/mercadolivre/ReplenishmentSkuTable.tsx"
      provides: "badges de transparencia (tendencia/sazonal/lead time/modo simples) no ParamsTooltip"
      contains: "venda_dia_origem"
  key_links:
    - from: "MLCompras (Switch smartMode)"
      to: "useReplenishmentBySku rpc p_smart"
      via: "estado local useState(true) propagado como 3o argumento do hook"
      pattern: "p_smart"
    - from: "ReplenishmentSkuTable (ParamsTooltip)"
      to: "ReplenishmentSkuRow (5 campos novos)"
      via: "render condicional de badges por venda_dia_origem/lead_time_origem/tendencia/fator_sazonal"
      pattern: "lead_time_origem"
---

<objective>
Ligar a camada esperta na /compras: o hook useReplenishmentBySku ganha o parametro smartMode (default true) que vira p_smart na chamada RPC, a interface ReplenishmentSkuRow/mapRow ganham os 5 campos de transparencia, a pagina MLCompras ganha o toggle Calculo esperto (ON por padrao, espelhando o toggle do /fluxo-de-caixa), e a tabela mostra os badges de transparencia por SKU no ParamsTooltip.

Purpose: Dar ao Wesley o controle (comparar esperto vs simples) e a auditabilidade (por que cada SKU tem aquela sugestao) — SMART-04 (toggle + badges) e SMART-03 (sinal modo simples). Sem regressao da fundacao Phases 62-66.

Output: hook + pagina + tabela atualizados; tsc/build/vitest verdes; checkpoint visual do Wesley em preview.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-CONTEXT.md
@.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-RESEARCH.md
@src/hooks/useReplenishmentBySku.ts
@src/pages/mercadolivre/MLCompras.tsx
@src/components/mercadolivre/ReplenishmentSkuTable.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook smartMode + 5 campos novos na interface e mapRow</name>
  <files>src/hooks/useReplenishmentBySku.ts</files>
  <read_first>
    - 67-RESEARCH.md secoes 1 (Hook adicionar smartMode), 2 (Interface ReplenishmentSkuRow 5 novos campos), Pitfall 1, Pitfall 5
    - src/hooks/useReplenishmentBySku.ts (interface ReplenishmentSkuRow, mapRow, queryKey, chamada supabase.rpc)
    - src/hooks/useCashFlowData.ts (padrao do parametro de toggle propagado a RPC)
  </read_first>
  <behavior>
    - mapRow popula os 5 campos novos com fallback seguro: venda_dia_origem default simples, lead_time_origem default param, tendencia default estavel, fator_sazonal e lead_time_real null-safe via Number
    - useReplenishmentBySku(30, 1.0, smartMode=true) inclui smartMode na queryKey e envia p_smart explicitamente (nunca undefined)
  </behavior>
  <action>
    Em useReplenishmentBySku.ts: (1) acrescentar a interface ReplenishmentSkuRow os 5 campos com os union types do 67-RESEARCH secao 2 (venda_dia_origem, lead_time_origem, tendencia, fator_sazonal number|null, lead_time_real number|null); (2) no mapRow, mapear os 5 com fallback seguro (Pitfall 5 — campos acessados por string em Record<string,unknown>): origens com default simples/param, tendencia com default estavel, numericos com guarda null-safe via Number; (3) adicionar 3o parametro smartMode = true a assinatura do hook (D-10), inclui-lo na queryKey e passa-lo como p_smart: smartMode na chamada supabase.rpc — EXPLICITAMENTE, nunca undefined (Pitfall 1). Nao alterar groupByItem nem a logica de agrupamento.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit && grep -q "p_smart" src/hooks/useReplenishmentBySku.ts && grep -q "venda_dia_origem" src/hooks/useReplenishmentBySku.ts && grep -q "smartMode" src/hooks/useReplenishmentBySku.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Interface ReplenishmentSkuRow tem os 5 campos novos com union types
    - mapRow popula os 5 com fallback seguro (sem quebrar tipos)
    - Hook tem 3o parametro smartMode (default true), na queryKey e enviado como p_smart explicito
    - tsc --noEmit limpo
  </acceptance_criteria>
  <done>Hook e interface estendidos; p_smart propagado explicitamente; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Toggle Calculo esperto na pagina + badges de transparencia na tabela</name>
  <files>src/pages/mercadolivre/MLCompras.tsx, src/components/mercadolivre/ReplenishmentSkuTable.tsx</files>
  <read_first>
    - 67-RESEARCH.md secoes 3 (Pagina MLCompras toggle state e propagacao), 4 (ReplenishmentSkuTable badges de transparencia D-11)
    - src/pages/mercadolivre/MLFluxoCaixa.tsx (padrao do Switch + Label do toggle aprovado na Phase 60)
    - src/pages/mercadolivre/MLCompras.tsx (header sticky onde o toggle entra; chamada useReplenishmentBySku)
    - src/components/mercadolivre/ReplenishmentSkuTable.tsx (funcao ParamsTooltip — onde os badges entram; padrao de Badge ja usado)
  </read_first>
  <behavior>
    - O toggle inicia ligado (D-10); ligar/desligar troca a fonte dos dados (p_smart) e a tabela re-renderiza
    - Badges aparecem por SKU: tendencia (seta cima verde / baixo vermelho / estavel cinza), origem da velocidade (EWMA+saz / EWMA / Simples), fator sazonal quando aplicado, lead time real vs param, e marcador modo simples quando venda_dia_origem=simples
  </behavior>
  <action>
    Em MLCompras.tsx: adicionar const [smartMode, setSmartMode] = useState(true) (D-10) e passar os defaults atuais do hook (30, 1.0) explicitamente mais smartMode em useReplenishmentBySku. Renderizar o toggle no header sticky com Switch + Label Calculo esperto, espelhando exatamente o padrao do MLFluxoCaixa.tsx (id, checked, onCheckedChange, title explicativo curto). Em ReplenishmentSkuTable.tsx, estender a funcao ParamsTooltip (D-11/SMART-03): adicionar uma linha de badges derivados de row.venda_dia_origem (rotulo EWMA + saz / EWMA / Simples), row.tendencia (cor por direcao), row.fator_sazonal (quando nao-null: Sazonal vezes n), e row.lead_time_origem/row.lead_time_real (Prazo real n d vs Prazo fixo param_lead_time d). Quando venda_dia_origem for simples, exibir um marcador discreto de modo simples. Reusar o componente Badge ja importado; manter o tooltip compacto (nao poluir a tabela principal). Nao alterar a logica de filtros/export/agrupamento.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit && npx vitest run && grep -q "smartMode" src/pages/mercadolivre/MLCompras.tsx && grep -q "venda_dia_origem" src/components/mercadolivre/ReplenishmentSkuTable.tsx && npm run build 1>/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - Toggle Calculo esperto visivel no header, ON por padrao, controla p_smart via smartMode
    - ParamsTooltip mostra tendencia, origem da velocidade, fator sazonal (quando aplicado), lead time real vs param e marcador modo simples
    - tsc limpo, suite vitest existente verde, npm run build limpo
  </acceptance_criteria>
  <done>Toggle + badges entregues; sem regressao (tsc/build/vitest verdes).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual do Wesley em preview (/compras)</name>
  <action>O orquestrador publica o preview Vercel e o Wesley valida o toggle + badges conforme how-to-verify. Sem código a escrever pelo executor.</action>
  <what-built>
    Toggle Calculo esperto (ON por padrao) na /compras + badges de transparencia por SKU no tooltip (tendencia, EWMA/sazonal, lead time real vs param, modo simples).
  </what-built>
  <how-to-verify>
    Em preview Vercel (branch gsd/phase-67-calculo-esperto, apontando para Supabase prod ckcdevcxgvueywivefgx), logado como Pe Vermeio:
    1. Abrir /compras: confirmar que o toggle Calculo esperto aparece no header e inicia LIGADO.
    2. Abrir o tooltip (icone de regras) de alguns SKUs: confirmar os badges — tendencia (seta), origem da velocidade (EWMA / EWMA + saz / Simples), lead time real vs param.
    3. DESLIGAR o toggle: a tabela deve recarregar e as sugestoes voltarem ao calculo simples (badges passam a Simples / Prazo fixo); LIGAR de novo restaura o esperto. Comparar lado a lado faz sentido.
    4. Confirmar que SKUs sem base suficiente mostram modo simples (esperado hoje para sazonal/lead-time, conforme open questions do 67-01).
  </how-to-verify>
  <resume-signal>Digite aprovado se o toggle compara esperto vs simples e os badges explicam cada SKU; ou descreva ajustes de UX.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) -> RPC | hook envia p_smart + p_org_id; org real vem da RLS, nao do argumento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06 | Tampering | regressao via p_smart=undefined no hook | mitigate | hook sempre envia p_smart explicito (Pitfall 1); RPC tem COALESCE(p_smart,TRUE) (Plano 67-01) |
| T-67-07 | Information Disclosure | badges expoem dados de outra org | accept | dados vem da RPC SECURITY INVOKER org-scoped (67-01); frontend so renderiza o que a RPC retorna para a org do JWT |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo (Switch/Badge ja instalados) |
</threat_model>

<verification>
- tsc --noEmit limpo; npx vitest run verde (sem regressao Phases 62-66); npm run build limpo.
- Checkpoint visual do Wesley em preview: toggle ON por padrao, comparacao esperto/simples, badges de transparencia.
</verification>

<success_criteria>
- Toggle Calculo esperto (ON default) controla p_smart; badges mostram tendencia/sazonal/lead-time/modo-simples por SKU; zero regressao de tsc/build/suite.
</success_criteria>

<output>
Create `.planning/phases/67-compras-v3-reposi-o-mais-esperta-tend-ncia-lead-time-real/67-03-SUMMARY.md` when done
</output>
