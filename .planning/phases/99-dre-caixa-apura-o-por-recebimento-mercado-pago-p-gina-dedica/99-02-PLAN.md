---
phase: 99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dreCashCascade.ts
  - src/lib/dreCashCascade.test.ts
  - src/hooks/useDreCash.ts
  - src/hooks/useDreCashItems.ts
  - src/hooks/useDreCashHistory.ts
  - src/hooks/useCashFreshness.ts
autonomous: true
requirements: [DREC-04, DREC-05, DREC-06]
must_haves:
  truths:
    - "buildDreCashCascade transforma linhas cruas da RPC na cascata completa (entradas → resultado operacional → financeiro → resultado de caixa) com badge-resposta"
    - "Seções entrada/previsao e bloco excluido nunca contaminam a soma de saídas (guardrail testado)"
    - "Previsão de imposto null produz estado exibível como traço, com desvio null"
    - "Mês sem movimentação produz badge neutro"
    - "Hooks chamam get_dre_cash/get_dre_cash_items/get_dre_cash_history com p_month no formato YYYY-MM-01; items só dispara com bloco selecionado"
    - "useCashFreshness expõe max(synced_at) de cash_inflows E de cash_outflows separadamente"
  artifacts:
    - path: "src/lib/dreCashCascade.ts"
      provides: "Lib pura da cascata de caixa + badge (espelho de dreCascade.ts)"
      exports: ["buildDreCashCascade"]
      min_lines: 80
    - path: "src/lib/dreCashCascade.test.ts"
      provides: "Testes vitest da cascata (guardrail, matemática, mês vazio, previsão null, badge)"
      min_lines: 80
    - path: "src/hooks/useDreCash.ts"
      provides: "Hook TanStack Query da RPC get_dre_cash"
      exports: ["useDreCash"]
    - path: "src/hooks/useDreCashItems.ts"
      provides: "Hook do drill-down sob demanda"
      exports: ["useDreCashItems"]
    - path: "src/hooks/useDreCashHistory.ts"
      provides: "Hook da série de 12 meses"
      exports: ["useDreCashHistory"]
    - path: "src/hooks/useCashFreshness.ts"
      provides: "Frescor de cash_inflows/cash_outflows para o banner dado-velho"
      exports: ["useCashFreshness"]
  key_links:
    - from: "src/hooks/useDreCash.ts"
      to: "RPC get_dre_cash (plano 99-01)"
      via: "supabase.rpc com p_org_id + p_month"
      pattern: "rpc\\(\"get_dre_cash\""
    - from: "src/lib/dreCashCascade.ts"
      to: "linhas (secao, bloco, categoria, total, n) da RPC"
      via: "tipo DreCashRow como contrato de entrada"
      pattern: "DreCashRow"
    - from: "src/hooks/useCashFreshness.ts"
      to: "cash_inflows / cash_outflows"
      via: "query RLS direta select synced_at order desc limit 1"
      pattern: "synced_at"
---

<objective>
Criar a camada frontend não-visual da DRE Caixa: a lib pura `dreCashCascade.ts` (cascata + badge, espelho de `dreCascade.ts`, TDD com vitest) e os 4 hooks de dados (`useDreCash`, `useDreCashItems`, `useDreCashHistory`, `useCashFreshness`).

Purpose: a cascata e o badge-resposta são O produto da phase (DREC-04, DREC-05); a lib pura testada é prova obrigatória de fechamento (gate #1 do spec). Os hooks isolam a página (99-03) de qualquer detalhe de RPC. Roda em paralelo com 99-01 (zero arquivos em comum; o contrato das RPCs está travado no CONTEXT/spec).
Output: 6 arquivos novos, `npx vitest run src/lib/dreCashCascade.test.ts` verde, tsc limpo.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica/99-CONTEXT.md
@docs/superpowers/specs/2026-07-16-dre-caixa-design.md
@src/lib/dreCascade.ts
@src/lib/dreCascade.test.ts
@src/hooks/useDreOperational.ts
@src/hooks/useMLLastSync.ts
</context>

## Artifacts this phase produces (visão da phase inteira)

**Backend (plano 99-01):** migration `20260717000000_dre_cash_rpcs.sql` com RPCs `get_dre_cash`, `get_dre_cash_items`, `get_dre_cash_history`.

**Este plano (99-02):**
- `src/lib/dreCashCascade.ts` — tipos `DreCashRow`, `DreCashCascade`, `DreCashBadge`, `DreCashPrevisao`; funções `buildDreCashCascade`, `computePrevisaoDesvio`
- `src/lib/dreCashCascade.test.ts` — suíte vitest da lib
- `src/hooks/useDreCash.ts` (`useDreCash`), `src/hooks/useDreCashItems.ts` (`useDreCashItems`), `src/hooks/useDreCashHistory.ts` (`useDreCashHistory`), `src/hooks/useCashFreshness.ts` (`useCashFreshness`)

**Página e wiring (plano 99-03):** `src/pages/mercadolivre/MLDreCaixa.tsx`, rota `/dre-caixa` em `src/App.tsx`, entradas em `roleAccess.ts`/`routeMeta.ts`/`ApiSidebar.tsx`/`ApiMobileSidebar.tsx`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lib pura dreCashCascade.ts com testes vitest (RED→GREEN)</name>
  <files>src/lib/dreCashCascade.ts, src/lib/dreCashCascade.test.ts</files>
  <read_first>
    - src/lib/dreCascade.ts (molde estrutural: módulo 100% puro sem imports de React/Supabase, round2, guardrail de filtragem na entrada)
    - src/lib/dreCascade.test.ts (molde dos testes: guardrail, matemática, casos vazios)
    - .planning/phases/99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica/99-CONTEXT.md (seção "Cascata (LOCKED)" — ordem e composição exatas)
    - docs/superpowers/specs/2026-07-16-dre-caixa-design.md (seções 4 e 7 — cascata e edge cases)
  </read_first>
  <behavior>
    - Test 1 (guardrail): linhas com secao 'entrada' ou 'previsao' e linhas de saida com bloco 'excluido' NUNCA entram na soma de saídas nem no resultado — input com essas linhas produz os mesmos totais de saída que input sem elas
    - Test 2 (matemática da cascata): dado um conjunto de linhas conhecido, resultadoOperacional = liquido − soma dos blocos (impostos_venda, pessoal, estrutura, servicos, operacional, nao_classificado); resultadoCaixa = resultadoOperacional − financeiro; valores com round2
    - Test 3 (badge positivo): resultadoCaixa > 0 → tone 'positive' e texto contendo "sobrou" com o valor
    - Test 4 (badge negativo): resultadoCaixa < 0 → tone 'negative' e texto contendo "Faltou" com o valor absoluto
    - Test 5 (mês vazio): rows [] → todos os totais 0, badge tone 'neutral' texto "sem movimentação" (hasMovimento false)
    - Test 6 (previsão null): linha imposto_previsto com total null → previsao.impostoPrevisto null e previsao.desvioPct null (frontend renderiza "—")
    - Test 7 (desvio de previsão): computePrevisaoDesvio(previsto=1000, guiaPaga=1300) → desvioPct 30 e alert true (limiar 20%); (previsto=1000, guiaPaga=1050) → alert false; previsto 0 ou null → desvioPct null, alert false
    - Test 8 (nao_classificado gate): rows com bloco 'nao_classificado' total > 0 → flag naoClassificadoTotal > 0 exposta para o gate visual
  </behavior>
  <action>
Ciclo TDD: commit 1 = `src/lib/dreCashCascade.test.ts` falhando (test(99-02): ...), commit 2 = implementação verde (feat(99-02): ...).

Criar `src/lib/dreCashCascade.ts` como módulo 100% puro (zero imports de React/Supabase/rede — mesmo padrão de `dreCascade.ts`), implementando a cascata LOCKED do 99-CONTEXT:

- `export interface DreCashRow { secao: string; bloco: string | null; categoria: string; total: number | null; n: number }` — contrato 1:1 com o retorno de `get_dre_cash`.
- `export interface DreCashBadge { tone: "positive" | "negative" | "neutral"; valor: number; texto: string }`.
- `export interface DreCashPrevisao { impostoPrevisto: number | null; impostoGuiaPaga: number; faturamentoMes: number; desvioPct: number | null; alert: boolean }`.
- `export interface DreCashCascade` com: `entradas { bruto; descontosFonte; refunds; liquido; aLiberar }` (das categorias 'bruto'/'descontos_fonte'/'refunds'/'liquido'/'a_liberar' da seção entrada); `saidas: Array<{ bloco: string; label: string; total: number; n: number; categorias: Array<{ categoria: string; total: number; n: number }> }>` na ordem fixa impostos_venda → pessoal → estrutura → servicos → operacional → nao_classificado (blocos sem linhas aparecem com total 0 — a cascata sempre mostra as 6 linhas); `resultadoOperacional: number`; `financeiro: number` (bloco financeiro, fora do operacional); `resultadoCaixa: number`; `previsao: DreCashPrevisao`; `badge: DreCashBadge`; `naoClassificadoTotal: number`; `hasMovimento: boolean`.
- `export function buildDreCashCascade(rows: DreCashRow[]): DreCashCascade` — guardrail NA ENTRADA (espelho de dreCascade.ts): particionar rows por secao; dentro de saida, separar bloco 'excluido' (nunca somado) e 'financeiro' (somado só após o resultado operacional). `resultadoOperacional = liquido − Σ(6 blocos operacionais)`; `resultadoCaixa = resultadoOperacional − financeiro`. Usar `const round2 = (v: number) => Math.round(v * 100) / 100` idêntico ao de dreCascade.ts. `hasMovimento = liquido !== 0 || bruto !== 0 || Σsaidas !== 0 || financeiro !== 0`.
- Badge (LOCKED "Cascata"): `resultadoCaixa > 0` → tone positive, texto `As entradas do mês pagaram as contas — sobrou R$ {valor formatado pt-BR}`; `< 0` → tone negative, texto `Faltou R$ {abs formatado} — esse dinheiro saiu de outro lugar`; `hasMovimento === false` → tone neutral, texto `Sem movimentação no mês`. Formatação pt-BR via `toLocaleString("pt-BR", { minimumFractionDigits: 2 })` (sem depender de Intl no teste além do jsdom padrão). O selo "mês em andamento" é responsabilidade da página (depende da data corrente), NÃO da lib — a lib é determinística.
- Previsão (LOCKED "Imposto", DREC-05): ler categorias 'imposto_previsto', 'imposto_guia_paga', 'faturamento_mes' da seção previsao; `export function computePrevisaoDesvio(previsto: number | null, guiaPaga: number): { desvioPct: number | null; alert: boolean }` — `desvioPct = round2((guiaPaga − previsto) / previsto * 100)` quando `previsto` é número > 0; caso contrário null/false. Limiar de alerta: `|desvioPct| > 20` (constante exportada `DESVIO_ALERT_PCT = 20`; valor a critério de Claude conforme CONTEXT "Claude's Discretion" — documentar em comentário JSDoc de 1 linha).

Testes: espelhar a organização de `dreCascade.test.ts`, cobrindo os 8 comportamentos do bloco behavior com fixtures de DreCashRow explícitas.

<!-- planner-discipline-allow: supabase -->
  </action>
  <verify>
    <automated>npx vitest run src/lib/dreCashCascade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/dreCashCascade.test.ts` verde com >= 8 testes
    - `grep -c "from \"react\"" src/lib/dreCashCascade.ts` == 0 e `grep -c "supabase" src/lib/dreCashCascade.ts` == 0 (módulo puro)
    - `grep -c "export function buildDreCashCascade" src/lib/dreCashCascade.ts` == 1; `grep -c "export function computePrevisaoDesvio" src/lib/dreCashCascade.ts` == 1
    - Histórico git mostra o commit do teste ANTES do commit da implementação (RED→GREEN)
    - `npx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Lib pura com cascata na ordem travada, badge com os 3 tons e textos do CONTEXT, previsão com null-safety e desvio, guardrail provado por teste — gate #1 do spec satisfeito.</done>
</task>

<task type="auto">
  <name>Task 2: Hooks useDreCash / useDreCashItems / useDreCashHistory / useCashFreshness</name>
  <files>src/hooks/useDreCash.ts, src/hooks/useDreCashItems.ts, src/hooks/useDreCashHistory.ts, src/hooks/useCashFreshness.ts</files>
  <read_first>
    - src/hooks/useDreOperational.ts (molde 1:1: useOrganization + useQuery + supabase.rpc + p_month "YYYY-MM-01" + staleTime 5min + map (r: any))
    - src/hooks/useMLLastSync.ts (molde do padrão de frescor: select synced_at, order desc, limit 1, maybeSingle)
    - src/lib/dreCashCascade.ts (tipo DreCashRow criado na Task 1 — o hook devolve DreCashRow[])
  </read_first>
  <action>
Criar 4 hooks clonando o padrão de `useDreOperational.ts` (useOrganization → orgId, queryKey com orgId, enabled, staleTime 5 * 60 * 1000, queryFn com supabase.rpc, throw em error, map defensivo):

- `useDreCash(pMonth: string)` → `useQuery<DreCashRow[]>` com queryKey `["dre-cash", orgId, pMonth]`, rpc `get_dre_cash` com `{ p_org_id: orgId, p_month: pMonth }`. `pMonth` SEMPRE "YYYY-MM-01" (nunca "YYYY-MM" — cast date do Postgres falha; mesma regra de useDreOperational). Import do tipo: `import type { DreCashRow } from "@/lib/dreCashCascade"`. Mapear `total: r.total === null ? null : Number(r.total)` (numeric do Postgres chega como string em alguns drivers — normalizar).
- `useDreCashItems(pMonth: string, bloco: string | null)` → rpc `get_dre_cash_items` com `{ p_org_id: orgId, p_month: pMonth, p_bloco: bloco }`, queryKey `["dre-cash-items", orgId, pMonth, bloco]`, `enabled: !!orgId && !!pMonth && !!bloco` — só dispara sob clique no drill-down (spec seção 6.3). Tipo de retorno local `DreCashItem { outflow_date: string; supplier: string | null; category: string | null; amount: number; document_number: string | null }`.
- `useDreCashHistory(months: number = 12)` → rpc `get_dre_cash_history` com `{ p_org_id: orgId, p_months: months }`, queryKey `["dre-cash-history", orgId, months]`. Tipo local `DreCashHistoryRow { mes: string; entradas: number; saidas: number; resultado: number }` com Number() nos 3 numéricos.
- `useCashFreshness()` (DREC-06) → UMA query (queryKey `["cash-freshness", orgId]`) cujo queryFn roda em `Promise.all` duas leituras RLS diretas (padrão useMLLastSync — sem RPC nova, recomendação do RESEARCH): `supabase.from("cash_inflows").select("synced_at").eq("organization_id", orgId).order("synced_at", { ascending: false }).limit(1).maybeSingle()` e o equivalente para `cash_outflows`; retorna `{ inflowsSyncedAt: string | null; outflowsSyncedAt: string | null }`. Expor os DOIS separadamente — lição 2026-07-13: o token Tiny já morreu congelando só outflows; o banner precisa acusar cada fonte por si.

Nenhum hook escreve em tabela (RLS de escrita é service-role-only nas duas tabelas — Pitfall 5). Não usar `.from()` direto para dados financeiros agregados (truncamento PostgREST 1000 — Pitfall 6); as únicas queries diretas são as duas de frescor (LIMIT 1).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/dreCashCascade.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 4 arquivos criados; `grep -c 'rpc("get_dre_cash"' src/hooks/useDreCash.ts` == 1; `grep -c 'rpc("get_dre_cash_items"' src/hooks/useDreCashItems.ts` == 1; `grep -c 'rpc("get_dre_cash_history"' src/hooks/useDreCashHistory.ts` == 1
    - `grep -c 'enabled: !!orgId && !!pMonth && !!bloco' src/hooks/useDreCashItems.ts` == 1 (drill-down sob demanda)
    - `grep -c 'maybeSingle' src/hooks/useCashFreshness.ts` == 2 (inflows e outflows separados)
    - `grep -rc 'get_cashflow\|get_treasury_panel\|financial_settings\|initial_balance' src/hooks/useDreCash.ts src/hooks/useDreCashItems.ts src/hooks/useDreCashHistory.ts src/hooks/useCashFreshness.ts | grep -vc ':0'` == 0 (separação do Fluxo de Caixa — LOCKED)
    - `npx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>4 hooks no padrão TanStack Query do projeto, items lazy por bloco, frescor duplo inflows/outflows, zero acoplamento com Fluxo de Caixa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PostgREST/RPC | hooks passam p_org_id do contexto da org corrente; RLS decide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-05 | Information Disclosure | useCashFreshness lê cash_inflows/cash_outflows direto | mitigate | RLS is_org_member já filtra; query LIMIT 1 de 1 coluna (synced_at), sem dado financeiro |
| T-99-06 | Tampering | orgId manipulado no cliente | mitigate | RLS server-side é a autoridade (SECURITY INVOKER nas RPCs); frontend é só conveniência |
| T-99-SC | Tampering | npm installs | accept | nenhum pacote novo (audit do RESEARCH vazio) |
</threat_model>

<verification>
- `npx vitest run src/lib/dreCashCascade.test.ts` verde (>= 8 testes)
- `npx tsc --noEmit` limpo
- Nenhum arquivo da DRE de faturamento nem do Fluxo de Caixa modificado (`git diff --name-only` só mostra os 6 arquivos novos)
</verification>

<success_criteria>
- DREC-04 (parte não-visual): lib pura testada + 3 hooks RPC prontos para a página
- DREC-05 (parte frontend): previsão null-safe + desvio com limiar de alerta calculados em código puro testado
- DREC-06 (parte dados): frescor de inflows/outflows disponível para o banner
</success_criteria>

<output>
Create `.planning/phases/99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica/99-02-SUMMARY.md` when done
</output>
