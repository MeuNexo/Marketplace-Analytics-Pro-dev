---
phase: 99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica
plan: 03
type: execute
wave: 2
depends_on: [99-01, 99-02]
files_modified:
  - src/pages/mercadolivre/MLDreCaixa.tsx
  - src/App.tsx
  - src/config/roleAccess.ts
  - src/components/layout/routeMeta.ts
  - src/components/layout/ApiSidebar.tsx
  - src/components/layout/ApiMobileSidebar.tsx
autonomous: false
requirements: [DREC-04, DREC-05, DREC-06]
must_haves:
  truths:
    - "Usuário owner/admin/member acessa /dre-caixa pelo menu (desktop E mobile) e vê a página completa"
    - "Badge-resposta no topo: verde 'sobrou R$ X' / vermelho 'Faltou R$ X' / neutro 'sem movimentação'; mês corrente exibe selo 'mês em andamento'"
    - "Cascata com drill-down: clicar num bloco expande categorias; clicar em categoria/bloco carrega lançamentos individuais sob demanda"
    - "4 KPI tiles: Entradas líquidas · Saídas pagas · Resultado · Previsão imposto × guia (com alerta de desvio)"
    - "Gráfico de evolução 12m (entradas × saídas × resultado) + tabela de histórico mensal"
    - "Banner dado-velho aparece quando max(synced_at) de inflows OU outflows > 6h, acusando cada fonte separadamente"
    - "nao_classificado > 0 exibe gate visual de aviso"
    - "Reconciliação de mês fechado × extrato MP conferida pelo Wesley + ok visual final"
  artifacts:
    - path: "src/pages/mercadolivre/MLDreCaixa.tsx"
      provides: "Página /dre-caixa completa (header+badge, KPIs, cascata drill-down, evolução 12m, histórico, banner)"
      min_lines: 200
    - path: "src/App.tsx"
      provides: "Rota lazy /dre-caixa com RoleRoute + ErrorBoundary"
      contains: "dre-caixa"
    - path: "src/config/roleAccess.ts"
      provides: "Acesso OPERATIONAL para /dre-caixa"
      contains: "/dre-caixa"
    - path: "src/components/layout/routeMeta.ts"
      provides: "Título/subtítulo do header para /dre-caixa"
      contains: "/dre-caixa"
    - path: "src/components/layout/ApiSidebar.tsx"
      provides: "Item de menu DRE Caixa (desktop)"
      contains: "/dre-caixa"
    - path: "src/components/layout/ApiMobileSidebar.tsx"
      provides: "Item de menu DRE Caixa (mobile)"
      contains: "/dre-caixa"
  key_links:
    - from: "src/pages/mercadolivre/MLDreCaixa.tsx"
      to: "src/lib/dreCashCascade.ts"
      via: "buildDreCashCascade sobre as linhas de useDreCash"
      pattern: "buildDreCashCascade"
    - from: "src/pages/mercadolivre/MLDreCaixa.tsx"
      to: "src/hooks/useDreCashItems.ts"
      via: "drill-down sob demanda com bloco selecionado"
      pattern: "useDreCashItems"
    - from: "src/App.tsx"
      to: "src/pages/mercadolivre/MLDreCaixa.tsx"
      via: "React.lazy import + Route path /dre-caixa"
      pattern: "MLDreCaixa"
---

<objective>
Construir a página `/dre-caixa` (layout LOCKED do 99-CONTEXT: header+badge → KPI row → cascata com drill-down → evolução 12m → histórico → banner dado-velho), ligá-la em rota/role/menu/meta (desktop E mobile), e fechar a phase com os dois checkpoints humanos: reconciliação de mês fechado × extrato MP e ok visual do Wesley.

Purpose: entrega o produto visível da phase — a resposta "o que entrou no mês pagou as contas do mês?" legível para leigo (DREC-04), com previsão de imposto × guia e alerta de desvio (DREC-05) e banner de frescor (DREC-06).
Output: página em produção de código (branch da phase), gates #4 e #5 do spec cumpridos.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica/99-CONTEXT.md
@docs/superpowers/specs/2026-07-16-dre-caixa-design.md
@src/pages/mercadolivre/MLFluxoCaixa.tsx
@src/components/dashboard/KPICard.tsx
@src/components/financial/CashFlowChart.tsx
@src/App.tsx
@src/config/roleAccess.ts
@src/components/layout/routeMeta.ts
</context>

## Artifacts this phase produces (visão da phase inteira)

**Backend (plano 99-01):** migration `20260717000000_dre_cash_rpcs.sql` com RPCs `get_dre_cash`, `get_dre_cash_items`, `get_dre_cash_history`.

**Lib/hooks (plano 99-02):** `src/lib/dreCashCascade.ts` (+ `.test.ts`), `src/hooks/useDreCash.ts`, `useDreCashItems.ts`, `useDreCashHistory.ts`, `useCashFreshness.ts`.

**Este plano (99-03):**
- Página `src/pages/mercadolivre/MLDreCaixa.tsx` (componentes internos: badge-resposta, cascata drill-down, gráfico 12m, tabela histórico, banner frescor)
- Rota lazy `/dre-caixa` em `src/App.tsx`
- `"/dre-caixa": OPERATIONAL` em `src/config/roleAccess.ts`
- Entrada `/dre-caixa` em `src/components/layout/routeMeta.ts`
- Item de menu em `src/components/layout/ApiSidebar.tsx` e `src/components/layout/ApiMobileSidebar.tsx`

<tasks>

<task type="auto">
  <name>Task 1: Página MLDreCaixa.tsx completa</name>
  <files>src/pages/mercadolivre/MLDreCaixa.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFluxoCaixa.tsx (molde de página financeira: estrutura, seletor de datas com date-fns, estados loading/empty)
    - src/components/dashboard/KPICard.tsx (props reais: title, value, subtitle/subtitleNode, delta, icon, loading, variant, tooltip — usar 4x, não recriar tile)
    - src/components/financial/CashFlowChart.tsx (molde Recharts: ComposedChart, CustomTooltip, currFmt/tickFmt, ResponsiveContainer)
    - src/lib/dreCashCascade.ts (contrato DreCashCascade/DreCashBadge/DreCashPrevisao produzido no 99-02)
    - src/hooks/useDreCash.ts, src/hooks/useDreCashItems.ts, src/hooks/useDreCashHistory.ts, src/hooks/useCashFreshness.ts (assinaturas reais dos hooks do 99-02)
    - .planning/phases/99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica/99-CONTEXT.md (seção "Frontend (LOCKED)" — layout topo→baixo obrigatório)
  </read_first>
  <action>
Criar `src/pages/mercadolivre/MLDreCaixa.tsx` (export default, padrão das páginas lazy) com o layout LOCKED topo→baixo. Estado central: `selectedMonth` (Date, default = mês corrente) → `pMonth = format(startOfMonth(selectedMonth), "yyyy-MM-01")` na prática `format(..., "yyyy-MM-dd")` do primeiro dia (date-fns, molde MLFluxoCaixa); `cascade = buildDreCashCascade(rows)` memoizado sobre `useDreCash(pMonth).data ?? []`.

1. **Header**: seletor de mês (botões prev/next + rótulo "julho de 2026" via `format(selectedMonth, "MMMM 'de' yyyy", { locale: ptBR })`, molde do seletor de MLFluxoCaixa; bloquear next além do mês corrente) + **badge-resposta** grande a partir de `cascade.badge`: tone positive → visual verde (tokens kpi.positive/success do design system), negative → vermelho (destructive), neutral → cinza. Texto do badge vem PRONTO da lib (não re-montar frase na página). Se `selectedMonth` é o mês corrente, exibir selo "mês em andamento" ao lado do badge e nunca apresentar o resultado parcial como final (microcopy secundária: "entradas contam só até hoje; 'a liberar' mostrado à parte" — linguagem para leigos).
2. **KPI row** (4x KPICard, grid responsivo 2x2 mobile / 4 col desktop): "Entradas líquidas MP" (`cascade.entradas.liquido`, subtitle com bruto e "a liberar R$ X"), "Saídas pagas" (Σ blocos operacionais + financeiro, subtitle nº de lançamentos), "Resultado do mês" (`cascade.resultadoCaixa`, variant conforme sinal), "Previsão imposto × guia" (guia paga como value; subtitleNode com "previsto R$ X" ou "—" quando `previsao.impostoPrevisto === null`; quando `previsao.alert` true, ícone/texto de alerta de desvio com `desvioPct` — DREC-05).
3. **Cascata com drill-down** (Card com linhas, estilo tabela DRE): linhas informativas de entrada (bruto, descontos na fonte com sub-linha "dos quais devoluções", A LIBERAR) visualmente atenuadas (muted) + linha forte RECEBIMENTO LÍQUIDO MP; depois as 6 linhas de bloco na ordem da lib (rótulos leigos: Impostos (guias pagas) / Pessoal / Estrutura / Serviços / Operacional / Não classificado), subtotal RESULTADO OPERACIONAL DE CAIXA, linha Financeiro, total RESULTADO DE CAIXA DO MÊS. Cada bloco com total > 0 é clicável (Collapsible, padrão /compras): expande as categorias do bloco (já vêm em `cascade.saidas[].categorias`) e, ao expandir, dispara `useDreCashItems(pMonth, blocoSelecionado)` exibindo os lançamentos (data dd/MM, supplier, category, valor pt-BR, document_number quando houver) — "nada de número opaco". Se `cascade.naoClassificadoTotal > 0`, exibir o gate visual de aviso (mesmo padrão de alerta da DRE atual em /vendas: Alert destructive/warning pedindo categorização no Tiny).
4. **Evolução 12 meses**: Recharts ComposedChart (molde CashFlowChart) com dados de `useDreCashHistory(12)`: 2 Bar (entradas, saidas) + 1 Line (resultado) + ReferenceLine y=0, tooltip com valores pt-BR, eixo X com mês abreviado ("jan", "fev"). Cores: usar tokens de gráfico existentes do design system (padrão dataviz do projeto — não hex hardcoded novos).
5. **Tabela histórico mensal**: mesma fonte do gráfico, colunas Mês / Entradas / Saídas / Resultado (resultado colorido por sinal), mês corrente marcado "em andamento".
6. **Banner dado-velho** (DREC-06): com `useCashFreshness()`, se `inflowsSyncedAt` OU `outflowsSyncedAt` for null ou mais velho que 6h (comparar com Date.now(), constante `STALE_MS = 6 * 60 * 60 * 1000`), exibir Alert no topo da página acusando CADA fonte separadamente ("Entradas MP sincronizadas há Xh" / "Contas do Tiny sincronizadas há Xh" — lição 2026-07-13: o token Tiny morre silencioso sem afetar o MP). Fora da janela de 6h, exibir discretamente o frescor no rodapé.

Estados: loading (skeletons nos KPICards + Card), erro (mensagem com retry padrão), mês sem dados (badge neutro já cobre; cascata zerada visível). Proibido importar qualquer hook/RPC do Fluxo de Caixa (get_cashflow/get_treasury_panel) ou da DRE de faturamento — fontes exclusivas: os 4 hooks do 99-02 (LOCKED "Separações"). Componentes internos podem ser declarados no próprio arquivo (página autocontida, padrão MLFluxoCaixa); mover para src/components/financial/ apenas se ultrapassar ~400 linhas.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'buildDreCashCascade' src/pages/mercadolivre/MLDreCaixa.tsx` >= 1; `grep -c 'useDreCashItems' src/pages/mercadolivre/MLDreCaixa.tsx` >= 1; `grep -c 'useDreCashHistory' src/pages/mercadolivre/MLDreCaixa.tsx` >= 1; `grep -c 'useCashFreshness' src/pages/mercadolivre/MLDreCaixa.tsx` >= 1
    - `grep -c 'KPICard' src/pages/mercadolivre/MLDreCaixa.tsx` >= 4
    - `grep -c 'mês em andamento' src/pages/mercadolivre/MLDreCaixa.tsx` >= 1
    - `grep -c 'get_cashflow\|get_treasury_panel' src/pages/mercadolivre/MLDreCaixa.tsx` == 0 (separação LOCKED do Fluxo de Caixa)
    - `npx tsc --noEmit` sem erros e `npx vitest run` com a suíte completa verde
  </acceptance_criteria>
  <done>Página completa com os 6 blocos do layout LOCKED, drill-down sob demanda, previsão × guia com alerta, banner de frescor duplo, sem nenhum acoplamento com Fluxo de Caixa ou DRE de faturamento.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring — rota, role, meta e menu (desktop + mobile)</name>
  <files>src/App.tsx, src/config/roleAccess.ts, src/components/layout/routeMeta.ts, src/components/layout/ApiSidebar.tsx, src/components/layout/ApiMobileSidebar.tsx</files>
  <read_first>
    - src/App.tsx (linha ~145 — rota /fluxo-de-caixa como molde exato de Route lazy + RoleRoute + ErrorBoundary; bloco de imports React.lazy no topo)
    - src/config/roleAccess.ts (constante OPERATIONAL linha ~6 e mapa de rotas)
    - src/components/layout/routeMeta.ts (formato das entradas title/subtitle)
    - src/components/layout/ApiSidebar.tsx (grupo Operações, item Fluxo de Caixa linha ~56)
    - src/components/layout/ApiMobileSidebar.tsx (lista separada do mobile — lição de paridade dual-layout da Phase 71)
  </read_first>
  <action>
Cinco edits cirúrgicos (nenhuma rota/menu existente alterada):

1. `src/App.tsx`: adicionar `const MLDreCaixa = React.lazy(() => import("./pages/mercadolivre/MLDreCaixa"));` junto aos demais lazy imports; adicionar `<Route path="/dre-caixa" element={<RoleRoute><ErrorBoundary fallbackTitle="Erro na DRE Caixa"><MLDreCaixa /></ErrorBoundary></RoleRoute>} />` imediatamente após a rota `/fluxo-de-caixa` (mesmo bloco protegido do HeaderScopeProvider).
2. `src/config/roleAccess.ts`: adicionar `"/dre-caixa": OPERATIONAL,` no mapa (mesma linha de política de /fluxo-de-caixa e /compras — dado financeiro sensível; NÃO adicionar em VIEWER_ELIGIBLE_ROUTES, recomendação do RESEARCH).
3. `src/components/layout/routeMeta.ts`: adicionar `"/dre-caixa": { title: "DRE Caixa", subtitle: "O que entrou no mês pagou as contas do mês?" },`.
4. `src/components/layout/ApiSidebar.tsx`: no grupo "Operações", logo após o item Fluxo de Caixa, adicionar `{ icon: FileBarChart, label: "DRE Caixa", path: "/dre-caixa" }` (import de `FileBarChart` de lucide-react — ícone a critério de Claude, A1 do RESEARCH).
5. `src/components/layout/ApiMobileSidebar.tsx`: replicar o MESMO item na lista mobile (lista é separada — esquecer o mobile é o risco documentado; paridade dual-layout obrigatória).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && test $(grep -c '/dre-caixa' src/App.tsx) -ge 1 && test $(grep -c '"/dre-caixa"' src/config/roleAccess.ts) -eq 1 && test $(grep -c '/dre-caixa' src/components/layout/ApiSidebar.tsx) -ge 1 && test $(grep -c '/dre-caixa' src/components/layout/ApiMobileSidebar.tsx) -ge 1 && test $(grep -c '/dre-caixa' src/components/layout/routeMeta.ts) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `/dre-caixa` presente nos 5 arquivos (rota, role OPERATIONAL, meta, sidebar desktop, sidebar mobile)
    - `grep -c 'VIEWER_ELIGIBLE' src/config/roleAccess.ts` inalterado em relação ao HEAD anterior (viewer não ganha a rota)
    - `npm run build` limpo (chunk lazy da página gerado)
    - `git diff src/App.tsx` mostra apenas 2 hunks (lazy import + Route) — nenhuma rota existente tocada
  </acceptance_criteria>
  <done>Página acessível pelo menu em desktop e mobile para owner/admin/member, com título/subtítulo no header e build de produção limpo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reconciliação mês fechado × extrato MP + ok visual do Wesley</name>
  <files>src/pages/mercadolivre/MLDreCaixa.tsx</files>
  <read_first>
    - .planning/phases/99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica/99-CONTEXT.md (seção &lt;specifics&gt; — gates #4 e #5 de fechamento)
    - docs/superpowers/specs/2026-07-16-dre-caixa-design.md (seção 8 — testes e provas)
  </read_first>
  <action>Parar e devolver ao orquestrador: preparar o acesso do Wesley (preview Vercel da branch apontando pro Supabase prod, padrão reference_vercel_preview_workflow, OU merge após aprovação — decisão do orquestrador com Wesley) e conduzir os 7 passos do how-to-verify. Ajustes pedidos voltam como fix na Task 1 deste plano.</action>
  <what-built>Página /dre-caixa completa e ligada (rota+menu+role, desktop+mobile), backend em prod (99-01), lib pura testada (99-02), tsc/build/vitest verdes.</what-built>
  <how-to-verify>
    Wesley, na página /dre-caixa (org Pé Vermeio):
    1. Selecionar **junho/2026** (mês fechado): conferir o total "Entradas líquidas MP" contra o extrato/painel do Mercado Pago do mesmo período (liberações do mês). Deve bater (gate #4 — reconciliação). Conferir também que a linha "dos quais devoluções" faz sentido com os refunds do período.
    2. Badge-resposta: cor e frase coerentes com o resultado do mês ("sobrou R$ X" verde ou "Faltou R$ X" vermelho).
    3. Mês corrente (julho): selo "mês em andamento" visível; "a liberar no mês" separado das entradas; resultado não apresentado como final.
    4. Drill-down: clicar em "Operacional" → categorias → lançamentos individuais com fornecedor/data/valor (conferir 1 lançamento conhecido contra o Tiny).
    5. Previsão de imposto: tile mostra previsto × guia paga; se desvio > 20%, alerta visível; mês sem histórico mostra "—".
    6. Gráfico + tabela 12 meses coerentes entre si; banner de dado-velho ausente com syncs em dia (ou presente e acusando a fonte certa, se algum sync estiver parado).
    7. Menu: item "DRE Caixa" visível no desktop e no mobile; usuária viewer NÃO vê a rota.
  </how-to-verify>
  <acceptance_criteria>
    - Gate #4: entradas líquidas de junho/2026 reconciliadas com o extrato MP, confirmado por Wesley (diferenças explicadas linha a linha, se houver)
    - Gate #5: ok visual explícito do Wesley na página (badge, cascata, drill-down, gráfico, banner)
    - Itens 3, 5 e 7 verificados sem ressalva
  </acceptance_criteria>
  <verify>Os 7 passos do how-to-verify executados por Wesley na página real (org Pé Vermeio), com o resultado da reconciliação de junho registrado no SUMMARY.</verify>
  <done>Gates #4 (reconciliação mês fechado × extrato MP) e #5 (ok visual) do spec aprovados por Wesley — Success Criteria 1-5 da phase confirmados na prática.</done>
  <resume-signal>Wesley digita "aprovado" (fecha a phase) ou descreve os ajustes (voltam como fix na Task 1)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → rota /dre-caixa | dado financeiro sensível; viewer não pode acessar |
| browser → RPCs | valores de mês/bloco escolhidos pelo usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-07 | Information Disclosure | rota /dre-caixa para role viewer | mitigate | `"/dre-caixa": OPERATIONAL` em roleAccess.ts, fora de VIEWER_ELIGIBLE_ROUTES; RoleRoute na rota |
| T-99-08 | Spoofing/XSS | render de supplier/category vindos do Tiny | mitigate | render como texto React puro (sem dangerouslySetInnerHTML), padrão do projeto |
| T-99-09 | Information Disclosure | acesso direto à URL sem passar pelo menu | mitigate | RoleRoute + RLS server-side (defesa em profundidade — o menu é só descoberta) |
| T-99-SC | Tampering | npm installs | accept | nenhum pacote novo |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run build` e `npx vitest run` (suíte completa) verdes
- Rota/role/meta/menu presentes nos 5 arquivos de wiring; paridade desktop/mobile
- Checkpoint humano aprovado (reconciliação MP + ok visual)
- `git diff --name-only` da branch mostra apenas os arquivos desta phase (nenhum arquivo da DRE de faturamento/Fluxo de Caixa)
</verification>

<success_criteria>
- DREC-04: página /dre-caixa completa com lib pura testada e hooks — Success Criteria 1, 3, 4 da phase
- DREC-05: previsão × guia com alerta de desvio e "—" para null — Success Criteria 5
- DREC-06: banner dado-velho duplo (inflows/outflows) — lição 2026-07-13 coberta
- Success Criteria 2 da phase: reconciliação de mês fechado confirmada pelo Wesley
</success_criteria>

<output>
Create `.planning/phases/99-dre-caixa-apura-o-por-recebimento-mercado-pago-p-gina-dedica/99-03-SUMMARY.md` when done
</output>
