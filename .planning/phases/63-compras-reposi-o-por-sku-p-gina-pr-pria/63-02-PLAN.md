---
phase: 63-compras-reposi-o-por-sku-p-gina-pr-pria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql
  - src/integrations/supabase/types.ts
  - src/lib/analysis/replenishmentUtils.ts
  - src/lib/analysis/replenishmentUtils.test.ts
  - src/hooks/useReplenishmentBySku.ts
autonomous: false
requirements:
  - CMP-02
  - CMP-03
  - CMP-04
  - CMP-05
  - CMP-09
user_setup:
  - service: supabase (ckcdevcxgvueywivefgx)
    why: "Apply da RPC get_replenishment_by_sku via MCP apply_migration (db push proibido; CLI local linkado no projeto errado). gsd-executor nao tem MCP Supabase nem SUPABASE_ACCESS_TOKEN — e checkpoint do orquestrador."
    dashboard_config:
      - task: "apply_migration 20260663000100 (RPC get_replenishment_by_sku) no projeto ckcdevcxgvueywivefgx + validar por execute_sql (linhas por SKU, anti-IDOR, sem-variacao como SKU unico)"
        location: "MCP Supabase apply_migration / execute_sql"

must_haves:
  truths:
    - "A reposicao passa a ser por SKU/variacao: existe a RPC get_replenishment_by_sku (SECURITY INVOKER) que retorna UMA linha por variacao (Cor/Tamanho) e UMA linha por anuncio sem variacao tratado como SKU unico (CMP-03, D-01, D-06)"
    - "A velocidade por SKU vem de venda real em ml_orders por (item_id, variation_id) na janela, nao rateada (CMP-02, D-04); anuncio sem variacao usa velocidade por item normal casando com variation_id vazio (D-05)"
    - "O custo casa por SKU via ml_product_costs.seller_sku igual ao seller_custom_field da variacao (CMP-04); custo nulo gera custo_ausente true e valor_estimado NULL, mantendo a regra da Phase 62 (D-03)"
    - "A precedencia de parametros e SKU maior que marca maior que global maior que hardcoded (30/60/7/1/1) e param_origem reflete a origem (CMP-05, D-08)"
    - "Passar p_org_id de outra org retorna 0 linhas (anti-IDOR por construcao, SECURITY INVOKER + RLS, CMP-09/D-07)"
    - "replenishmentUtils tem resolveParamsBySku com precedencia SKU maior que marca maior que global e testes vitest verdes (CMP-09)"
  artifacts:
    - path: "supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql"
      provides: "RPC get_replenishment_by_sku SECURITY INVOKER + REVOKE/GRANT"
      contains: "CREATE OR REPLACE FUNCTION public.get_replenishment_by_sku"
    - path: "src/lib/analysis/replenishmentUtils.ts"
      provides: "resolveParamsBySku (SKU>marca>global) + tipo ReplenishmentSkuInput"
      contains: "resolveParamsBySku"
    - path: "src/hooks/useReplenishmentBySku.ts"
      provides: "Hook React Query que chama a RPC e agrupa por anuncio para o drill"
      contains: "get_replenishment_by_sku"
    - path: "src/integrations/supabase/types.ts"
      provides: "Tipos da funcao get_replenishment_by_sku (Functions)"
      contains: "get_replenishment_by_sku"
  key_links:
    - from: "supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql"
      to: "ml_inventory_cache.variations (jsonb) / ml_orders / ml_product_costs / replenishment_params"
      via: "jsonb_to_recordset LATERAL (unnest) + JOIN por variation_id + LATERAL de custo por seller_sku + COALESCE de params sku>marca>global"
      pattern: "jsonb_to_recordset"
    - from: "src/hooks/useReplenishmentBySku.ts"
      to: "supabase.rpc('get_replenishment_by_sku')"
      via: "useQuery escopado por currentOrg.id; mapeia e agrupa linhas por item_id"
      pattern: "get_replenishment_by_sku"
  prohibitions:
    - "get_replenishment_by_sku NUNCA usa SECURITY DEFINER (seria IDOR) — sempre INVOKER + SET search_path = 'public'"
    - "Nenhum GRANT EXECUTE a PUBLIC/anon"
    - "NAO alterar a RPC get_replenishment (Phase 62) — a nova RPC nasce ao lado para nao quebrar /estoque ate a validacao"
    - "NAO alterar o schema de ml_product_daily_cache (velocidade por SKU sai de ml_orders — abordagem aprovada no RESEARCH)"
    - "NAO mexer em compraUtils.ts (feature legada de /precificacao)"
---

<objective>
Construir o motor de reposicao por SKU: a RPC `get_replenishment_by_sku` (SECURITY INVOKER) que faz unnest do jsonb `variations` (uma linha por variacao; anuncio sem variacao = SKU unico), deriva velocidade por SKU de `ml_orders` (venda real, nao rateada), casa custo por `seller_sku` da variacao, aplica params com precedencia SKU > marca > global e mantem a formula da Phase 62 (ponto/alvo/gatilho/MOQ/pack/sem-giro/custo-nulo). Estender o modulo puro `replenishmentUtils.ts` com `resolveParamsBySku` + testes, atualizar `types.ts` e criar o hook `useReplenishmentBySku` (agrupando por anuncio para o drill). Aplicar a RPC via MCP (checkpoint) e provar anti-IDOR + linhas por SKU.

Purpose: E o coracao da fase. Independente do Plan 63-01 em arquivos (wave 1 paralelo); a correcao real do "custo ausente" e validada no 63-04 apos o sync do 63-01 popular os SKUs.
Output: RPC viva e validada, modulo puro + testes verdes, hook e types prontos para a UI (63-03).
</objective>

<artifacts_produced>
- RPC `get_replenishment_by_sku(p_org_id, p_sales_window_days, p_demand_multiplier)` (SECURITY INVOKER) com colunas por SKU
- `resolveParamsBySku` + tipo `ReplenishmentSkuInput` em `replenishmentUtils.ts` + casos vitest
- Hook `useReplenishmentBySku` (linhas por SKU + agrupamento por anuncio)
- Tipos de `get_replenishment_by_sku` em `types.ts`
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-CONTEXT.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md
@supabase/migrations/20260662000100_get_replenishment_rpc.sql
@src/lib/analysis/replenishmentUtils.ts
@src/hooks/useReplenishment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration RPC get_replenishment_by_sku (CMP-02/03/04/05-backend)</name>
  <files>supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql</files>
  <read_first>
    - supabase/migrations/20260662000100_get_replenishment_rpc.sql (RPC base por anuncio — copiar a formula de ponto/alvo/gatilho/MOQ/pack/custo-nulo/sem-giro e o bloco REVOKE/GRANT; so muda a granularidade para SKU)
    - .planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md (CMP-02, CMP-03 assinatura + unnest UNION ALL, CMP-04 LATERAL de custo, CMP-05 COALESCE de params)
  </read_first>
  <behavior>
    - Item COM variacoes (has_variations e jsonb_array_length(variations) maior que 0): uma linha por variacao; sku_stock = available_quantity da variacao; sku_code = seller_custom_field da variacao; attribute_combinations preenchido
    - Item SEM variacoes (NOT has_variations OU jsonb_array_length = 0): uma linha; sku_stock = item.available_quantity; sku_code = item.seller_custom_field; variation_id NULL; attribute_combinations NULL
    - venda_dia por SKU = SUM(ml_orders.quantidade)/janela para (item_id, variation_id) com status IN (paid, confirmed) na janela; anuncio sem variacao casa com o.variation_id vazio (Pitfall 2)
    - custo casa por seller_sku igual a sku_code; fallback item_id quando sku_code NULL; custo nulo gera custo_ausente true e valor_estimado NULL
    - params COALESCE: sku(scope_value = sku_code) maior que marca(scope_value = brand) maior que global maior que hardcoded; param_origem em {sku, marca, global}
    - compra_sugerida 0 se venda_dia 0 (sem_giro) ou estoque maior que ponto; senao GREATEST(CEIL(necessidade/pack)*pack, moq)
    - p_org_id de outra org retorna 0 linhas (RLS)
  </behavior>
  <action>
    Criar a RPC `public.get_replenishment_by_sku(p_org_id UUID, p_sales_window_days INTEGER DEFAULT 30, p_demand_multiplier NUMERIC DEFAULT 1.0)` RETURNS TABLE com as colunas da assinatura do RESEARCH (item_id, variation_id, title, brand, sku_code, attribute_combinations, logistic_type, sku_stock, venda_dia, cobertura_atual, ponto_reposicao, alvo, compra_sugerida, valor_estimado, custo_ausente, sem_giro, gatilho_ativo, param_lead_time, param_cobertura, param_safety, param_moq, param_pack, param_origem). LANGUAGE plpgsql, SECURITY INVOKER, SET search_path = 'public'. CTEs:
    (1) inventory_by_sku = UNION ALL. Ramo COM variacoes via `CROSS JOIN LATERAL jsonb_to_recordset(i.variations) AS v(variation_id TEXT, attribute_combinations JSONB, available_quantity INTEGER, sold_quantity INTEGER, seller_custom_field TEXT)` filtrando `i.organization_id = p_org_id AND i.status = 'active' AND i.has_variations = TRUE AND jsonb_array_length(i.variations) > 0`. Ramo SEM variacoes usando colunas item-level e `(i.has_variations = FALSE OR jsonb_array_length(i.variations) = 0)`.
    (2) sales_by_sku = LEFT JOIN `ml_orders o` por `o.organization_id = p_org_id AND o.item_id = inv.item_id AND o.data_pedido >= CURRENT_DATE - p_sales_window_days AND o.status IN ('paid','confirmed')` com casamento de variacao `(o.variation_id = inv.variation_id OR (inv.variation_id IS NULL AND o.variation_id = ''))`; `avg_daily = COALESCE(SUM(o.quantidade),0)::NUMERIC / NULLIF(p_sales_window_days,0)` (Pitfall 2).
    (3) params = COALESCE com 4 niveis por campo (lead_time_dias, meta_cobertura_dias, safety_days, moq, pack_multiple): subquery scope='sku' AND scope_value = COALESCE(inv.sku_code,''); depois scope='marca' AND scope_value = COALESCE(inv.brand,''); depois scope='global'; depois hardcoded (30/60/7/1/1). param_origem via CASE: 'sku' se EXISTS linha sku, senao 'marca' se EXISTS linha marca, senao 'global'.
    (4) base + SELECT final: reproduzir a formula EXATA da RPC 62 (ponto = venda_dia*(lead+safety); alvo = venda_dia*(cobertura+safety); gatilho_ativo = estoque <= ponto; sem_giro = venda_dia = 0 AND estoque > 0; compra_sugerida com GREATEST/CEIL/pack/moq e NULLIF(pack,0); custo via LATERAL `SELECT c.cost FROM ml_product_costs c WHERE c.organization_id = p_org_id AND (c.seller_sku = inv.sku_code OR (inv.sku_code IS NULL AND c.item_id = inv.item_id)) ORDER BY c.updated_at DESC NULLS LAST LIMIT 1`; valor_estimado NULL se custo NULL). ORDER BY compra_sugerida DESC NULLS LAST, item_id, variation_id.
    Fechar com `REVOKE EXECUTE ON FUNCTION public.get_replenishment_by_sku(UUID, INTEGER, NUMERIC) FROM PUBLIC, anon;` e `GRANT EXECUTE ON FUNCTION public.get_replenishment_by_sku(UUID, INTEGER, NUMERIC) TO authenticated;`. Cabecalho cita ckcdevcxgvueywivefgx, CMP-02/03/04/05, D-06/D-07 e que get_replenishment (Phase 62) permanece intocada.
  </action>
  <verify>
    <automated>grep -q "CREATE OR REPLACE FUNCTION public.get_replenishment_by_sku" supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql && grep -q "SECURITY INVOKER" supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql && grep -q "jsonb_to_recordset" supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql && grep -q "REVOKE EXECUTE" supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Funcao declarada SECURITY INVOKER + SET search_path = 'public'; sem SECURITY DEFINER
    - CTE de inventario usa jsonb_to_recordset com UNION ALL (ramo com variacao + ramo sem variacao)
    - sales_by_sku sai de ml_orders e trata variation_id vazio para anuncio sem variacao
    - params com COALESCE sku > marca > global > 30/60/7/1/1 e param_origem correto
    - Custo via LATERAL por seller_sku com fallback item_id; valor_estimado NULL se custo NULL
    - REVOKE de PUBLIC/anon + GRANT a authenticated presentes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: replenishmentUtils resolveParamsBySku + testes + types + hook (CMP-05/CMP-09)</name>
  <files>src/lib/analysis/replenishmentUtils.ts, src/lib/analysis/replenishmentUtils.test.ts, src/integrations/supabase/types.ts, src/hooks/useReplenishmentBySku.ts</files>
  <read_first>
    - src/lib/analysis/replenishmentUtils.ts (modulo puro atual: resolveParams marca>global; calcReplenishment travado da Phase 62 — REUSAR calcReplenishment como esta)
    - src/lib/analysis/replenishmentUtils.test.ts (suite vitest existente — espelhar o estilo dos casos)
    - src/hooks/useReplenishment.ts (hook base — copiar estrutura useQuery + mapeamento de linhas)
    - src/integrations/supabase/types.ts (secao Functions onde get_replenishment foi adicionado na Phase 62 — adicionar get_replenishment_by_sku ao lado)
    - .planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md (secoes "replenishmentUtils.ts — extensao para nivel SKU" e "React Query hook")
  </read_first>
  <behavior>
    - resolveParamsBySku(skuRow, marcaRow, globalRow, defaults): retorna params + origem 'sku' quando skuRow existe; 'marca' quando so marcaRow; 'global' caso contrario (espelha o COALESCE da RPC)
    - Caso: skuRow presente vence marcaRow e globalRow (origem 'sku')
    - Caso: skuRow ausente, marcaRow presente vence globalRow (origem 'marca')
    - Caso: so globalRow (origem 'global'); nenhum (origem 'global', valores defaults)
    - calcReplenishment permanece identico (sem alterar testes da Phase 62)
  </behavior>
  <action>
    Em `replenishmentUtils.ts`: adicionar (sem remover nada existente) o tipo `ReplenishmentSkuInput` com `variationId: string | null`, `skuCode: string | null`, `attributeCombinations: Array<{id: string; name: string; value: string}> | null` (alem dos campos ja usados: estoque, vendaDia, cost). Adicionar `resolveParamsBySku(skuRow, marcaRow, globalRow, defaults = REPLENISHMENT_DEFAULTS)` que espelha a precedencia SKU > marca > global da RPC e retorna `{ params, origem: 'sku' | 'marca' | 'global' }`. Ampliar o tipo de `paramOrigem` do `ReplenishmentResult` para `'sku' | 'marca' | 'global'`. NAO alterar `calcReplenishment` (reusar como esta).
    Em `replenishmentUtils.test.ts`: acrescentar um describe para `resolveParamsBySku` cobrindo os 4 casos de precedencia do bloco behavior + um caso que usa os params resolvidos por SKU em `calcReplenishment` (ex.: SKU override muda ponto/alvo). Manter todos os testes existentes intactos.
    Em `types.ts`: adicionar a assinatura de `get_replenishment_by_sku` na secao Functions (Args: p_org_id string, p_sales_window_days number, p_demand_multiplier number; Returns: array das linhas por SKU com as colunas da RPC), espelhando como `get_replenishment` foi tipado na Phase 62.
    Criar `src/hooks/useReplenishmentBySku.ts`: exportar a interface `ReplenishmentSkuRow` (colunas da RPC, com `variation_id: string | null`, `attribute_combinations`, `param_origem: 'sku'|'marca'|'global'`, mais `attribute_combinations_label: string`) e a interface `GroupedReplenishmentRow` (`item_id`, `title`, `brand`, `skus: ReplenishmentSkuRow[]` + agregados, ex. soma de compra_sugerida e contagem de gatilho ativo). Implementar `useReplenishmentBySku(salesWindowDays = 30, demandMultiplier = 1.0)` via useQuery escopado por `currentOrg.id` chamando `supabase.rpc('get_replenishment_by_sku', { p_org_id, p_sales_window_days, p_demand_multiplier })`, com mapeamento defensivo (Number/String/Boolean como no useReplenishment) e expondo `rows` (plano) e `grouped` (por item_id) para o drill. Derivar `attribute_combinations_label` a partir de attribute_combinations (ex. "Natural / G").
  </action>
  <verify>
    <automated>npx vitest run src/lib/analysis/replenishmentUtils.test.ts && grep -q "resolveParamsBySku" src/lib/analysis/replenishmentUtils.ts && grep -q "get_replenishment_by_sku" src/integrations/supabase/types.ts && grep -q "get_replenishment_by_sku" src/hooks/useReplenishmentBySku.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - vitest verde para replenishmentUtils.test.ts (casos novos de resolveParamsBySku + reuso de calcReplenishment) sem quebrar os 203 existentes
    - resolveParamsBySku retorna origem correta nos 4 casos (sku/marca/global/defaults)
    - types.ts contem a funcao get_replenishment_by_sku na secao Functions
    - useReplenishmentBySku expoe rows e grouped, escopado por currentOrg.id, sem org vinda de fora do contexto
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply RPC via MCP + validar linhas por SKU e anti-IDOR (checkpoint do orquestrador)</name>
  <action>
    Checkpoint do ORQUESTRADOR. O executor ja escreveu a RPC (Task 1) e o modulo/hook/types (Task 2); aqui o orquestrador aplica a migration da RPC via MCP `apply_migration` no projeto ckcdevcxgvueywivefgx e prova por SQL que a RPC retorna linhas por SKU e que org alheia retorna 0 linhas. Sem este passo build/types passam mas o banco live nao tem a RPC (falso-positivo).
  </action>
  <what-built>
    Migration da RPC get_replenishment_by_sku + modulo puro estendido + hook + types escritos no repo, RPC ainda NAO aplicada no banco live. gsd-executor nao tem MCP Supabase / SUPABASE_ACCESS_TOKEN.
  </what-built>
  <how-to-verify>
    [ORQUESTRADOR via MCP Supabase no projeto ckcdevcxgvueywivefgx — Org Pe Vermeio = 7f615df7-7bac-45e5-8a93-827fb9ddeec7; Org Thales = e4150d57-1349-48c9-9a89-82b1774857b0]
    1. apply_migration com o conteudo de supabase/migrations/20260663000100_get_replenishment_by_sku_rpc.sql
    2. Linhas por SKU (estrutura) — execute_sql:
       SELECT count(*) AS linhas,
              count(*) FILTER (WHERE variation_id IS NOT NULL) AS por_variacao,
              count(*) FILTER (WHERE variation_id IS NULL)     AS sem_variacao_sku_unico,
              count(*) FILTER (WHERE compra_sugerida > 0)      AS sugeridos,
              count(*) FILTER (WHERE custo_ausente)            AS sem_custo,
              count(*) FILTER (WHERE param_origem = 'sku')     AS params_por_sku
       FROM get_replenishment_by_sku('7f615df7-7bac-45e5-8a93-827fb9ddeec7', 30, 1.0);
       Esperado: linhas > 0; por_variacao > 0 (anuncios com variacao desaninhados); sem_variacao_sku_unico contabiliza os anuncios sem variacao; sem erro de divisao por zero.
    3. Anti-IDOR (CMP-09) — execute_sql chamando get_replenishment_by_sku com a org Thales sob impersonacao de um usuario Pe Vermeio (SET LOCAL ROLE authenticated + request.jwt.claims do usuario Pe Vermeio, em transacao ROLLBACK) — deve retornar 0 linhas. Registrar no SUMMARY.
    4. get_advisors (security) apos apply — sem novo ERROR de rls_disabled/security_definer para get_replenishment_by_sku.
    Observacao: a queda do "custo ausente" vs 44/116 e a velocidade real por SKU dependem do sync do 63-01 ter populado os seller_custom_field e do variation_id em ml_orders — essa prova final fica no 63-04.
  </how-to-verify>
  <acceptance_criteria>
    - apply_migration sem erro
    - SELECT retorna linhas > 0, por_variacao > 0 e o ramo sem-variacao presente; sem divisao por zero
    - Chamada cross-org (Thales sob JWT Pe Vermeio) retorna 0 linhas
    - get_advisors sem novo ERROR de security para get_replenishment_by_sku
  </acceptance_criteria>
  <resume-signal>Digite "aplicado" com o output do SELECT de estrutura e o resultado do teste cross-org, ou descreva o erro do apply_migration</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (authenticated user) -> supabase.rpc('get_replenishment_by_sku') | JWT do usuario cruza para o banco; p_org_id vem do cliente |
| RPC -> tabelas base (ml_inventory_cache, ml_orders, ml_product_costs, replenishment_params) | RLS org-first e o gate real de isolamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04 | Information Disclosure / Tampering | get_replenishment_by_sku recebendo p_org_id de outra org | mitigate | SECURITY INVOKER + RLS is_org_member nas 4 tabelas base; p_org_id alheio -> 0 linhas. Provado no checkpoint (cross-org = 0) |
| T-63-05 | Elevation of Privilege | EXECUTE concedido a PUBLIC/anon por default do Postgres | mitigate | REVOKE EXECUTE FROM PUBLIC, anon + GRANT so a authenticated (padrao get_cashflow/get_replenishment) |
| T-63-06 | Denial of Service | pack_multiple = 0 -> divisao por zero | mitigate | CHECK pack_multiple >= 1 na tabela (Phase 62) + NULLIF(pack,0) na RPC |
| T-63-07 | Information Disclosure | join de custo casando SKU de outra org | mitigate | LATERAL de custo filtra c.organization_id = p_org_id sob RLS de ml_product_costs (INVOKER) |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- grep confirma get_replenishment_by_sku SECURITY INVOKER + jsonb_to_recordset + REVOKE EXECUTE na migration
- npx vitest run da replenishmentUtils.test.ts verde (novos casos + 203 existentes)
- grep confirma get_replenishment_by_sku em types.ts e no hook
- Checkpoint do orquestrador: RPC aplicada, linhas por SKU > 0, por_variacao > 0, cross-org = 0
</verification>

<success_criteria>
get_replenishment_by_sku viva em ckcdevcxgvueywivefgx (SECURITY INVOKER, REVOKE/GRANT corretos), retornando linhas por SKU para a Pe Vermeio e nada para org alheia; replenishmentUtils com resolveParamsBySku testado; hook e types prontos. CMP-02/03/04/05-backend e a parte de motor do CMP-09 satisfeitos.
</success_criteria>

<output>
Create `.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-02-SUMMARY.md` when done
</output>
