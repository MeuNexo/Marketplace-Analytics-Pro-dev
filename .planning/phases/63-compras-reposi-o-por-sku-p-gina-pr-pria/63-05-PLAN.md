---
phase: 63-compras-reposi-o-por-sku-p-gina-pr-pria
plan: 05
type: execute
wave: 4
depends_on:
  - "63-03"
files_modified:
  - src/components/mercadolivre/ReplenishmentSkuTable.tsx
  - src/components/mercadolivre/ReplenishmentSkuFilters.tsx
  - src/components/mercadolivre/ReplenishmentParamsDialog.tsx
  - src/pages/mercadolivre/MLCompras.tsx
autonomous: true
requirements:
  - CMP-05
  - CMP-06
  - CMP-08

must_haves:
  truths:
    - "Todos os cabeçalhos de coluna usam linguagem leiga — Produto, Estoque, Vende por dia, Dura quanto, Comprar, Custo estimado — cada um com ícone de ajuda e tooltip explicativo de 1 frase"
    - "A coluna 'O que fazer' exibe 4 estados com emoji e cor: Comprar N (vermelho, gatilho ativo), Estoque ok (verde, sem gatilho), Sem vendas (cinza, sem giro), Falta custo (amarelo de aviso)"
    - "Os parâmetros brutos deixaram de ser coluna visível; os dados de regras aparecem somente via ícone-tooltip discreto (SlidersHorizontal) ao final de cada linha de variação ou SKU único"
    - "O diálogo de parâmetros exibe título 'Regras de Compra', rótulos leigos e 1 linha de ajuda + exemplo por campo (Aplicar a / Tempo de entrega / Estoque desejado / Folga de segurança / Pedido mínimo / Múltiplo de caixa)"
    - "Mini-resumo acima da tabela mostra contagem de SKUs nos estados 'para comprar', 'ok' e 'sem giro' derivada dos filteredRows já calculados"
    - "Filtro de status usa rótulo 'Situação' e opções 'Precisa comprar' / 'Sem vendas'; xlsx exporta com headers leigos (Vende por dia, Dura quanto (dias), Comprar (qtd), Custo estimado)"
    - "vitest passa, tsc --noEmit sem novos erros; hook useReplenishmentBySku, RPC get_replenishment_by_sku e replenishmentUtils intocados"
  artifacts:
    - path: "src/components/mercadolivre/ReplenishmentSkuTable.tsx"
      provides: "Tabela de 8 colunas em PT leigo com tooltips de cabeçalho, AcaoCell, MasterAcaoCell e ParamsTooltip"
      contains: "O que fazer"
    - path: "src/components/mercadolivre/ReplenishmentParamsDialog.tsx"
      provides: "Dialog com rótulos leigos, ajuda inline e exemplo por campo"
      contains: "Tempo de entrega do fornecedor"
    - path: "src/pages/mercadolivre/MLCompras.tsx"
      provides: "Mini-resumo de status acima da tabela; xlsx exportado com headers leigos"
      contains: "para comprar"
    - path: "src/components/mercadolivre/ReplenishmentSkuFilters.tsx"
      provides: "Filtros com rótulos coerentes com a nova linguagem da tabela"
      contains: "Precisa comprar"
  key_links:
    - from: "src/components/mercadolivre/ReplenishmentSkuTable.tsx"
      to: "src/hooks/useReplenishmentBySku.ts"
      via: "AcaoCell lê gatilho_ativo / sem_giro / custo_ausente / compra_sugerida; ParamsTooltip lê param_* e ponto_reposicao — sem alterar o hook"
      pattern: "AcaoCell|ParamsTooltip"
    - from: "src/pages/mercadolivre/MLCompras.tsx"
      to: "src/components/mercadolivre/ReplenishmentSkuTable.tsx"
      via: "statusCounts derivado dos filteredRows e renderizado como mini-resumo; contrato de props da tabela (grouped, isLoading, error) inalterado"
      pattern: "statusCounts|parComprar"
prohibitions:
  - "NAO alterar src/hooks/useReplenishmentBySku.ts nem a RPC get_replenishment_by_sku — logica de calculo intocada"
  - "NAO tocar src/lib/analysis/replenishmentUtils.ts, CompraRecomendadaPanel.tsx nem ReplenishmentPanel.tsx"
  - "NAO criar modo simples/avancado nem cards de acao — YAGNI (abordagens B/C descartadas no brainstorming de 2026-06-26)"
  - "NAO adicionar nenhuma dependencia npm nova — usar apenas shadcn/ui Tooltip + icones lucide-react ja presentes no projeto"
  - "NAO alterar a interface de props de ReplenishmentSkuTable (grouped/isLoading/error) nem de ReplenishmentSkuFilters — mudancas sao internas a apresentacao"
---

<objective>
Refinar a camada de apresentacao da tela /compras com linguagem leiga e clareza operacional, sem tocar logica de calculo, RPC ou banco.

Mudancas:
- ReplenishmentSkuTable: renomear 6 colunas, adicionar ícone "?" com tooltip de 1 frase em cada cabecalho, substituir a coluna de flags pela coluna "O que fazer" (4 estados com emoji/cor), recolher os parametros brutos em um icone-tooltip discreto (SlidersHorizontal) por linha — de 9 para 8 colunas.
- ReplenishmentParamsDialog: substituir todos os rotulos tecnicos por PT leigo com ajuda inline e exemplo por campo; titulo passa a "Regras de Compra".
- MLCompras: exibir mini-resumo de contagem por status acima da tabela; atualizar headers do xlsx exportado para os novos nomes leigos.
- ReplenishmentSkuFilters: atualizar rotulos do filtro de situacao para coerencia com a nova linguagem.

Purpose: Eliminar jargao de supply-chain da visao padrao — qualquer usuario deve entender o que fazer ao abrir a tela /compras.
Output: 4 arquivos de apresentacao modificados; hook, RPC, logica e banco intocados.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-05-UX-CONTEXT.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReplenishmentSkuTable — 8 colunas leigo, tooltips de cabecalho, "O que fazer", icone-tooltip de regras</name>
  <files>src/components/mercadolivre/ReplenishmentSkuTable.tsx</files>
  <read_first>
    - src/components/mercadolivre/ReplenishmentSkuTable.tsx — versao atual completa (ler antes de qualquer edicao; o arquivo tem 9 colunas e as funcoes FlagsCell/ParamsCell que serao substituidas)
    - src/hooks/useReplenishmentBySku.ts — campos de ReplenishmentSkuRow (gatilho_ativo, sem_giro, custo_ausente, compra_sugerida, ponto_reposicao, param_*) e GroupedReplenishmentRow (any_gatilho_ativo, any_custo_ausente, total_compra_sugerida, skus[])
  </read_first>
  <action>
    Adicione ao bloco de imports: `HelpCircle, SlidersHorizontal` de `lucide-react`; `Tooltip, TooltipContent, TooltipProvider, TooltipTrigger` de `@/components/ui/tooltip`. Mantenha os imports ja existentes (ChevronRight, Info, Badge, Skeleton, Collapsible etc.).

    Crie o helper `HeaderWithTip({ label, tip }: { label: string; tip: string })` que renderiza o label seguido de um icone HelpCircle de 3px (w-3 h-3 text-muted-foreground/60) envolto em TooltipProvider > Tooltip > TooltipTrigger (asChild, cursor-default) + TooltipContent (max-w-[200px] text-xs text-center).

    Crie o helper `AcaoCell({ row }: { row: ReplenishmentSkuRow })` com prioridade top-down:
    1. row.sem_giro verdadeiro: retornar span "Sem vendas" com emoji de circulo branco na frente, className="text-muted-foreground text-[11px]".
    2. row.gatilho_ativo true E row.compra_sugerida maior que zero: retornar span "Comprar {row.compra_sugerida}" com emoji de circulo vermelho, className="text-destructive text-[11px] font-semibold".
    3. row.custo_ausente true: retornar Badge variant="outline" com emoji de triangulo amarelo e texto "Falta custo", className="text-[10px] text-warning border-warning/40".
    4. default: retornar span "Estoque ok" com emoji de circulo verde, style={{ color: "var(--kpi-positive)" }} className="text-[11px]".

    Crie o helper `MasterAcaoCell({ group }: { group: GroupedReplenishmentRow })` com a mesma prioridade mas operando sobre campos de grupo:
    1. group.skus.every(s => s.sem_giro): "Sem vendas" (text-muted-foreground).
    2. group.any_gatilho_ativo: "Comprar {group.total_compra_sugerida}" (text-destructive font-semibold).
    3. group.any_custo_ausente: Badge "Falta custo" (warning).
    4. default: "Estoque ok" (kpi-positive).

    Crie o helper `ParamsTooltip({ row }: { row: ReplenishmentSkuRow })`: renderiza um botao com icone SlidersHorizontal (w-3.5 h-3.5 text-muted-foreground hover:text-foreground) envolto em TooltipProvider > Tooltip > TooltipTrigger (asChild) + TooltipContent (text-[11px] space-y-1). Dentro do TooltipContent mostrar tres linhas:
    - Linha 1: badge de origem (reutilizar a logica de cores do ParamsCell existente para sku/marca/global) + "Ponto {row.ponto_reposicao}un".
    - Linha 2: "Entrega {row.param_lead_time}d · Cob {row.param_cobertura}d · Folga {row.param_safety}d".
    - Linha 3: "Min {row.param_moq} · Caixa {row.param_pack}".

    Atualize o TableHeader para 8 TableHead usando HeaderWithTip nos seis cabecalhos textuais:
    - Col 1 "Produto" / tip "Anuncio no ML; clique para ver os tamanhos e cores disponíveis" (min-w-[220px]).
    - Col 2 "Estoque" / tip "Unidades disponíveis hoje no ML (nao desconta compras a chegar)" (text-right).
    - Col 3 "Vende por dia" / tip "Media de unidades vendidas por dia na janela analisada" (text-right).
    - Col 4 "Dura quanto" / tip "Quantos dias o estoque atual dura no ritmo de venda" (text-right).
    - Col 5 "Comprar" / tip "Quantidade sugerida de compra para atingir o estoque desejado" (text-right).
    - Col 6 "Custo estimado" / tip "Quanto essa compra deve custar (qtd x custo unitario)" (text-right).
    - Col 7 TableHead com texto "O que fazer" sem tooltip (auto-explicativo pelo emoji).
    - Col 8 TableHead vazio (cabecalho do icone de regras), sem tooltip.

    Atualize `VariationRow` para 8 TableCell: mantenha o conteudo das celulas de Produto (col 1), Estoque (col 2), Venda por dia (col 3) e CoberturaCell (col 4) inalterados; REMOVA a celula de ponto de reposicao existente; mantenha a celula de compra_sugerida (col 5) e ValorEstimadoCell (col 6) inalterados; substitua a celula de flags por `<AcaoCell row={sku} />` (col 7); substitua a celula de parametros por `<ParamsTooltip row={sku} />` (col 8).

    Atualize `MasterRow` para 8 TableCell: o mesmo mapeamento — remova a celula do ponto de reposicao; substitua a celula de flags por `<MasterAcaoCell group={group} />`; na col 8 renderize TableCell vazia (as regras sao por variacao, ficam visíveis quando o grupo e expandido).

    Atualize o bloco de SKU unico (single-sku sem expand, dentro do map em grouped.map) para 8 celulas seguindo o mesmo padrao de VariationRow: remova ponto de reposicao, use AcaoCell e ParamsTooltip.

    Remova as funcoes `FlagsCell` e `ParamsCell` do arquivo (nao sao mais usadas como colunas). Mantenha `CoberturaCell` e `ValorEstimadoCell` inalteradas. Mantenha `LoadingSkeleton` inalterado.
  </action>
  <verify>
    <automated>grep -q "O que fazer" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "Vende por dia" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "Dura quanto" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "Custo estimado" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "TooltipContent" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "AcaoCell" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "MasterAcaoCell" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "ParamsTooltip" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "SlidersHorizontal" src/components/mercadolivre/ReplenishmentSkuTable.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Tabela tem 8 colunas: Produto, Estoque, Vende por dia, Dura quanto, Comprar, Custo estimado, O que fazer, coluna de icone de regras.
    - Cada cabecalho textual (colunas 1 a 6) tem icone HelpCircle e tooltip de 1 frase ao hover.
    - AcaoCell exibe os 4 estados com emoji para linhas de variacao e linhas de SKU unico; MasterAcaoCell exibe estado agregado para a linha mestre de grupo.
    - ParamsTooltip (icone SlidersHorizontal) exibe ao hover: origem, ponto de reposicao e todos os parametros — renderizado apenas em linhas de variacao e linhas de SKU unico; linha mestre deixa a celula vazia.
    - Funcoes FlagsCell e ParamsCell removidas do arquivo; CoberturaCell e ValorEstimadoCell intocadas.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ReplenishmentParamsDialog — rotulos leigos + ajuda inline + exemplos por campo</name>
  <files>src/components/mercadolivre/ReplenishmentParamsDialog.tsx</files>
  <read_first>
    - src/components/mercadolivre/ReplenishmentParamsDialog.tsx — versao atual completa (ler antes de editar para nao regredir o CRUD zod/RLS/toast)
  </read_first>
  <action>
    Atualize o `DialogTitle` de "Parametros de Reposicao" para "Regras de Compra".

    Atualize o `DialogDescription` para: "Define as regras que calculam quanto comprar de cada produto. O mais especifico prevalece: Por SKU supera Por Marca, que supera Global (todos os produtos)." Mantenha a nota " (somente owner/admin podem editar)" ao final quando !canEdit.

    No componente `ParamRow`, atualize o paragrafo de resumo dos parametros para usar nomes leigos sem abreviacoes de supply-chain: "Entrega {param.lead_time_dias}d · Est. desejado {param.meta_cobertura_dias}d · Folga {param.safety_days}d · Min. {param.moq}un · Caixa {param.pack_multiple}".

    No componente `ParamForm`, para cada campo adicione Label com rotulo leigo e um paragrafo de ajuda imediatamente apos o Input (e apos o bloco de erro de validacao, se houver). Use `<p className="text-[10px] text-muted-foreground leading-tight mt-0.5">` para cada texto de ajuda. Nao altere os schemas zod, os atributos type/min/max dos inputs, nem a logica de submit.

    Campo de escopo (Select "Aplicar a"): Label muda para "Aplicar a". Opcoes do Select: "Global (todos os produtos)", "Por Marca", "Por SKU". Adicione paragrafo de ajuda abaixo do Select: "O mais especifico prevalece — Por SKU substitui Marca, que substitui Global."

    Campo lead_time_dias: Label "Tempo de entrega do fornecedor". Ajuda: "Dias entre fazer o pedido e a mercadoria chegar. Ex.: 30 dias para fornecedor nacional."

    Campo meta_cobertura_dias: Label "Estoque desejado (dias)". Ajuda: "Para quantos dias de venda voce quer ter estoque disponivel. Ex.: 60 dias equivalem a 2 meses."

    Campo safety_days: Label "Folga de seguranca (dias)". Ajuda: "Dias extras de estoque para imprevistos de atraso ou pico de vendas. Ex.: 7 dias."

    Campo moq: Label "Pedido minimo do fornecedor". Ajuda: "Quantidade minima por pedido imposta pelo fornecedor. Use 1 se nao ha minimo."

    Campo pack_multiple: Label "Multiplo de caixa". Ajuda: "Fornecedor entrega em caixas fechadas de X unidades. Use 1 para compra avulsa."

    Atualize o texto do empty state (quando params.length === 0) para: "Nenhuma regra configurada. Os valores padrao serao usados: Entrega 30d · Estoque desejado 60d · Folga 7d · Min. 1 · Caixa 1."

    Nao altere `scopeValueLabel`, os placeholders dos inputs de scope_value (ex: Pralana, 020491CA35GRX), a logica de submit/delete/CRUD, as constantes DEFAULTS, SCOPE_LABELS, os schemas zod, nem a verificacao canEdit/toast.
  </action>
  <verify>
    <automated>grep -q "Tempo de entrega do fornecedor" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && grep -q "Estoque desejado" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && grep -q "Folga de seguranca\|Folga de segurança" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && grep -q "Pedido minimo do fornecedor\|Pedido mínimo do fornecedor" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && grep -q "Multiplo de caixa\|Múltiplo de caixa" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && grep -q "Regras de Compra" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && grep -q "Aplicar a" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - DialogTitle exibe "Regras de Compra".
    - DialogDescription descreve a precedencia em PT leigo (Por SKU supera Por Marca, que supera Global).
    - Cada um dos 6 campos do formulario tem Label com rotulo leigo e paragrafo de ajuda com exemplo: Aplicar a, Tempo de entrega do fornecedor, Estoque desejado (dias), Folga de seguranca (dias), Pedido minimo do fornecedor, Multiplo de caixa.
    - ParamRow exibe resumo legivel sem abreviacoes tecnicas de supply-chain.
    - CRUD (create/update/delete), validacao zod, canEdit, toast de erro de permissao e RLS intocados.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: MLCompras mini-resumo + filtros leigos + xlsx headers leigos</name>
  <files>src/pages/mercadolivre/MLCompras.tsx, src/components/mercadolivre/ReplenishmentSkuFilters.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLCompras.tsx — versao atual completa (ver a estrutura do CardHeader e a funcao exportToXlsx para editar com precisao)
    - src/components/mercadolivre/ReplenishmentSkuFilters.tsx — versao atual completa (ver os types FilterStatus/FilterCusto exportados e o layout dos filtros)
  </read_first>
  <action>
    Em MLCompras.tsx:

    Derive `statusCounts` via `useMemo` a partir de `filteredRows` (ja calculado na pagina via useMemo). O objeto tem tres chaves: `parComprar` (contagem de rows com gatilho_ativo === true), `ok` (contagem de rows com gatilho_ativo === false e sem_giro === false) e `semGiro` (contagem de rows com sem_giro === true). Adicione o useMemo logo apos o useMemo existente de `filteredGrouped`, usando `filteredRows` como dependencia.

    Adicione o mini-resumo dentro do `CardHeader`, posicionado entre o `CardTitle` e o `ReplenishmentSkuFilters`. Renderize somente quando `!isLoading && allRows.length > 0`. O elemento e uma `<div className="flex flex-wrap gap-x-4 gap-y-1 text-[12px]">` com tres spans:
    - Span para parComprar: prefixo emoji de circulo vermelho + " {statusCounts.parComprar} para comprar"; adicionar `className="text-destructive font-medium"` quando `statusCounts.parComprar > 0`, `className="text-muted-foreground"` caso contrario.
    - Span para ok: prefixo emoji de circulo verde + " {statusCounts.ok} ok"; adicionar `style={{ color: "var(--kpi-positive)" }}` e `className="font-medium"` quando ok > 0, `className="text-muted-foreground"` caso contrario.
    - Span para semGiro: prefixo emoji de circulo branco + " {statusCounts.semGiro} sem giro"; sempre `className="text-muted-foreground"`.

    Na funcao `exportToXlsx`, atualize somente os nomes das chaves do objeto de dados que foram renomeadas na tabela:
    - Renomear "Venda/dia" para "Vende por dia" (mantenha `r.venda_dia` como valor).
    - Renomear "Cobertura(d)" para "Dura quanto (dias)" (mantenha `r.cobertura_atual ?? ""` como valor).
    - Renomear "Ponto Rep." para "Ponto de recompra" (mantenha `r.ponto_reposicao` como valor).
    - Renomear "Sugestao" para "Comprar (qtd)" (mantenha `r.compra_sugerida` como valor; a chave atual e "Sugestao" com acento — ajuste se necessario para o texto atual exato).
    - Renomear "Valor Est." para "Custo estimado" (mantenha `r.valor_estimado ?? ""` como valor).
    As demais colunas ("Item ID", "Anuncio", "Marca", "SKU", "Cor/Tamanho", "Estoque", "Custo ausente", "Sem giro", "Params") permanecem inalteradas.

    Em ReplenishmentSkuFilters.tsx:

    Atualize o Label do filtro de status de "Status" para "Situacao" (pode usar "Situação" com cedilha se o projeto usa UTF-8 consistentemente).

    Atualize as duas opcoes do Select de status que tem rotulos de jargao:
    - A opcao com `value="gatilho"` muda o texto exibido de "Gatilho ativo" para "Precisa comprar" (o value interno permanece "gatilho" para nao quebrar a logica de applyFilters em MLCompras).
    - A opcao com `value="sem_giro"` muda o texto exibido de "Sem giro" para "Sem vendas" (o value interno permanece "sem_giro").
    A opcao "Todos" permanece inalterada.

    Nao altere os tipos exportados FilterStatus e FilterCusto, a interface ReplenishmentSkuFiltersProps, nem nenhum outro elemento do componente (busca, filtro de marca, filtro de custo, contador de SKUs).
  </action>
  <verify>
    <automated>grep -q "parComprar\|statusCounts" src/pages/mercadolivre/MLCompras.tsx && grep -q "para comprar" src/pages/mercadolivre/MLCompras.tsx && grep -q "Dura quanto" src/pages/mercadolivre/MLCompras.tsx && grep -q "Custo estimado" src/pages/mercadolivre/MLCompras.tsx && grep -q "Precisa comprar" src/components/mercadolivre/ReplenishmentSkuFilters.tsx && grep -q "Sem vendas" src/components/mercadolivre/ReplenishmentSkuFilters.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - MLCompras exibe mini-resumo com contagem de SKUs nos 3 estados quando ha dados carregados; mini-resumo oculto durante loading e quando allRows esta vazio.
    - xlsx exportado usa os 5 headers leigos renomeados; demais colunas intactas.
    - ReplenishmentSkuFilters: Label "Situacao" no filtro de status; opcao de gatilho exibe "Precisa comprar"; opcao sem giro exibe "Sem vendas"; values internos "gatilho" e "sem_giro" e tipos FilterStatus inalterados.
    - Logica de filtro em applyFilters (MLCompras.tsx) intocada.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> ReplenishmentSkuTable / Filters / ParamsDialog | Camada de apresentacao pura — nenhuma nova superficie de entrada; dados fluem do hook existente e do supabase client guardado por RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-11 | Information Disclosure | ParamsTooltip exibe param_origem e valores de regras na UI | accept | Dados de configuracao operacional ja visiveis na coluna de parametros anterior; usuarios autenticados e escopados por org via RLS; sem PII exposto |
| T-63-SC | Tampering | npm installs | accept | Nenhum pacote novo — Tooltip e SlidersHorizontal ja presentes no projeto (shadcn/ui + lucide-react) |
</threat_model>

<verification>
Consolidado apos as 3 tasks:
- `npx tsc --noEmit` executado na raiz do projeto — zero novos erros de TypeScript.
- `npx vitest run` — todos os testes existentes passando sem regressao.
- Greps de acceptance das 3 tasks passando.
- Confirmar que src/hooks/useReplenishmentBySku.ts, src/lib/analysis/replenishmentUtils.ts e src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx nao foram alterados (git diff --name-only deve listar apenas os 4 arquivos de files_modified).
</verification>

<success_criteria>
Tela /compras totalmente em linguagem leiga: 8 colunas renomeadas com tooltip explicativo em cada cabecalho, coluna "O que fazer" com 4 estados operacionais claros, icone discreto de regras por linha. Dialogo "Regras de Compra" com ajuda clara por campo. Mini-resumo de status visivel acima da tabela. Filtros e xlsx com rotulos coerentes. CMP-05 / CMP-06 / CMP-08 satisfeitos na dimensao de clareza. Sem regressao de tsc / vitest / logica de calculo.
</success_criteria>

<output>
Create `.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-05-SUMMARY.md` when done
</output>
