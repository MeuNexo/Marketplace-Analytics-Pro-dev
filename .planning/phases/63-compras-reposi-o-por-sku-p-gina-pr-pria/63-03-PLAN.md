---
phase: 63-compras-reposi-o-por-sku-p-gina-pr-pria
plan: 03
type: execute
wave: 2
depends_on:
  - "63-01"
  - "63-02"
files_modified:
  - src/pages/mercadolivre/MLCompras.tsx
  - src/components/mercadolivre/ReplenishmentSkuTable.tsx
  - src/components/mercadolivre/ReplenishmentSkuFilters.tsx
  - src/components/mercadolivre/ReplenishmentParamsDialog.tsx
  - src/App.tsx
  - src/config/roleAccess.ts
  - src/components/layout/ApiSidebar.tsx
  - src/contexts/MenuVisibilityContext.tsx
  - src/pages/mercadolivre/MLEstoque.tsx
autonomous: true
requirements:
  - CMP-05
  - CMP-06
  - CMP-07
  - CMP-08

must_haves:
  truths:
    - "Existe a rota /compras no menu (secao Operacoes) com a Compra Recomendada por SKU; acesso owner/admin/member (CMP-07, D-10)"
    - "A aba 'Compra Recomendada' foi removida de /estoque (MLEstoque.tsx); o compraUtils legado de /precificacao permanece intocado (CMP-07, D-10)"
    - "A tela mostra linhas por SKU com drill anuncio->variacoes (Collapsible) e exportacao xlsx (CMP-08, D-11)"
    - "Ha filtros: marca, status/gatilho ativo, sem giro, com/sem custo e busca por titulo/SKU/tamanho (CMP-06, D-11)"
    - "Ha CRUD de parametros (global, marca, sku) com precedencia visivel; o botao de salvar so funciona para owner/admin (CMP-05, D-08/D-09)"
  artifacts:
    - path: "src/pages/mercadolivre/MLCompras.tsx"
      provides: "Pagina /compras compondo filtros + tabela por SKU + export + dialog de params"
      contains: "useReplenishmentBySku"
    - path: "src/components/mercadolivre/ReplenishmentSkuTable.tsx"
      provides: "Tabela agrupada por anuncio com drill por variacao"
      contains: "Collapsible"
    - path: "src/components/mercadolivre/ReplenishmentSkuFilters.tsx"
      provides: "Barra de filtros (marca, status, custo, busca)"
      contains: "Select"
    - path: "src/components/mercadolivre/ReplenishmentParamsDialog.tsx"
      provides: "CRUD de replenishment_params (global/marca/sku) write owner/admin"
      contains: "replenishment_params"
    - path: "src/config/roleAccess.ts"
      provides: "Rota /compras em OPERATIONAL"
      contains: "/compras"
  key_links:
    - from: "src/pages/mercadolivre/MLCompras.tsx"
      to: "src/hooks/useReplenishmentBySku.ts"
      via: "consome rows/grouped da RPC get_replenishment_by_sku"
      pattern: "useReplenishmentBySku"
    - from: "src/App.tsx"
      to: "src/pages/mercadolivre/MLCompras.tsx"
      via: "Route path=/compras com RoleRoute + ErrorBoundary + React.lazy"
      pattern: "/compras"
  prohibitions:
    - "NAO tocar src/lib/analysis/compraUtils.ts nem CompraRecomendadaPanel.tsx (feature legada de /precificacao) — Pitfall 6"
    - "NAO remover o ReplenishmentPanel.tsx em si (so remover a aba/uso em MLEstoque.tsx); ele e a base reusada pela nova tela"
    - "O write em replenishment_params depende da RLS owner/admin existente — NAO criar bypass; o dialog so dispara o insert/update e trata o erro de permissao"
---

<objective>
Entregar a pagina propria /compras com a Compra Recomendada por SKU: filtros (CMP-06), tabela com drill anuncio->variacoes e exportacao xlsx (CMP-08), CRUD de parametros global/marca/sku com write owner/admin (CMP-05), rota+navegacao (CMP-07) e remocao da aba de /estoque. Reaproveita o hook e os tipos do 63-02 e os padroes de tabela/shadcn ja usados no projeto.

Purpose: Separar Compras de Estoque (D-10) e dar ao lojista uma tela operacional de compra por SKU com filtros e parametros editaveis — sem cálculo no front (a fonte e a RPC do 63-02).
Output: 4 arquivos novos (pagina + 3 componentes) + 5 edicoes de roteamento/nav/estoque.
</objective>

<artifacts_produced>
- Rota `/compras` (App.tsx + roleAccess.ts OPERATIONAL + ApiSidebar.tsx + MenuVisibilityContext.tsx)
- `MLCompras.tsx` (pagina) + `ReplenishmentSkuTable.tsx` (drill) + `ReplenishmentSkuFilters.tsx` (filtros) + `ReplenishmentParamsDialog.tsx` (CRUD params)
- Aba "Compra Recomendada" removida de `MLEstoque.tsx`
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-CONTEXT.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md
@src/components/mercadolivre/ReplenishmentPanel.tsx
@src/config/roleAccess.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabela por SKU com drill + barra de filtros (CMP-06/CMP-08)</name>
  <files>src/components/mercadolivre/ReplenishmentSkuTable.tsx, src/components/mercadolivre/ReplenishmentSkuFilters.tsx</files>
  <read_first>
    - src/components/mercadolivre/ReplenishmentPanel.tsx (formatadores currencyFmt/numFmt, celulas CoberturaCell/ValorEstimadoCell/FlagsCell/ParamsCell, Table shadcn, badge custo ausente — reusar o estilo)
    - src/hooks/useReplenishmentBySku.ts (tipos ReplenishmentSkuRow e GroupedReplenishmentRow — vindos do 63-02)
    - .planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md (CMP-06 filtros via useMemo; CMP-08 drill com Radix Collapsible)
  </read_first>
  <action>
    Criar `ReplenishmentSkuFilters.tsx`: componente controlado que recebe props de valores e setters para filterBrand (Select com as marcas distintas), filterStatus (all | gatilho | sem_giro), filterCusto (all | com | sem), filterSemGiro (toggle) e searchText (Input). Usar shadcn Select/Input/Switch/Label e os tokens de tabela ja existentes. Receber a lista de marcas via prop `brands: string[]`.
    Criar `ReplenishmentSkuTable.tsx`: recebe `grouped: GroupedReplenishmentRow[]` (ja filtrado pela pagina) e renderiza uma tabela onde cada anuncio e uma linha mestre (Radix Collapsible: CollapsibleTrigger com ChevronRight rotacionado, titulo, marca, e agregados — soma de compra_sugerida, contagem de variacoes com gatilho) e cada variacao e uma linha filha (CollapsibleContent) com colunas: SKU (sku_code), Cor/Tamanho (attribute_combinations_label), Estoque, Venda/dia, Cobertura, Ponto, Sugestao, Valor Est., Flags (sem giro/custo ausente), Parametros (origem sku/marca/global). Reusar as celulas/badges de ReplenishmentPanel. Estado de expandido por Set<string> de item_id. Para anuncio sem variacao (skus de comprimento 1 com variation_id null), renderizar a linha mestre como SKU unico (sem necessidade de expand, ou expand trivial). Empty state e loading skeleton no padrao do ReplenishmentPanel.
  </action>
  <verify>
    <automated>grep -q "Collapsible" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "attribute_combinations_label\|sku_code" src/components/mercadolivre/ReplenishmentSkuTable.tsx && grep -q "Select" src/components/mercadolivre/ReplenishmentSkuFilters.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ReplenishmentSkuTable usa Radix Collapsible com linha mestre por anuncio e linhas filha por variacao (SKU + Cor/Tamanho)
    - ReplenishmentSkuFilters expoe marca, status (gatilho/sem giro), custo (com/sem) e busca textual
    - Reuso dos formatadores/celulas do ReplenishmentPanel; sem calculo de reposicao no componente
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pagina MLCompras + export xlsx + dialog de params (CMP-05/CMP-08)</name>
  <files>src/pages/mercadolivre/MLCompras.tsx, src/components/mercadolivre/ReplenishmentParamsDialog.tsx</files>
  <read_first>
    - src/components/mercadolivre/ReplenishmentPanel.tsx (aviso REPL-09 "a chegar", header do card, switch "apenas gatilho ativo")
    - src/hooks/useReplenishmentBySku.ts (rows/grouped + tipos)
    - src/config/roleAccess.ts (helper canAccess para checar owner/admin no botao de salvar params)
    - .planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md (CMP-08 export xlsx padrao TopSellingProducts; CMP-05 schema zod dos params + write owner/admin)
    - src/components/mercadolivre/analise/CompraRecomendadaPanel.tsx (NAO editar — apenas confirmar que NAO sera importado por engano)
  </read_first>
  <action>
    Criar `ReplenishmentParamsDialog.tsx`: Dialog (shadcn) acionado por botao "Editar parametros". Lista os escopos configurados em `replenishment_params` (SELECT direto via supabase client, escopado por org pela RLS) com scope global/marca/sku e form de criar/editar (react-hook-form + zod: scope enum global|marca|sku, scope_value string, lead_time_dias int 1..365, meta_cobertura_dias int 1..730, safety_days int 0..60, moq int >=1, pack_multiple int >=1). Para scope='marca' o scope_value e a marca; para 'sku' e o seller_custom_field da variacao. O insert/update vai pela tabela direto (a RLS owner/admin enforca a escrita); desabilitar o submit quando o role atual nao for owner/admin (usar canAccess/role do contexto) e, em erro de permissao do Supabase, exibir toast (sonner) explicando que so owner/admin editam. Invalida o query de useReplenishmentBySku ao salvar.
    Criar `MLCompras.tsx`: pagina que (a) exibe o aviso fixo REPL-09 ("a chegar nao descontado", copiar do ReplenishmentPanel); (b) chama useReplenishmentBySku; (c) deriva a lista de marcas e mantem o estado dos filtros (filterBrand/filterStatus/filterCusto/filterSemGiro/searchText) aplicando-os via useMemo sobre rows (e reagrupando por item_id para o grouped filtrado) — logica CMP-06 do RESEARCH; (d) renderiza ReplenishmentSkuFilters + ReplenishmentSkuTable; (e) botao "Exportar xlsx" que serializa as linhas filtradas (flat por SKU) com XLSX.utils.json_to_sheet + writeFile, padrao TopSellingProducts (colunas: Item ID, Anuncio, Marca, SKU, Cor/Tamanho, Estoque, Venda/dia, Cobertura, Ponto, Sugestao, Valor Est., Custo ausente, Sem giro, Params); (f) botao que abre o ReplenishmentParamsDialog. Header/empty/loading no padrao do ReplenishmentPanel.
  </action>
  <verify>
    <automated>grep -q "useReplenishmentBySku" src/pages/mercadolivre/MLCompras.tsx && grep -q "XLSX\|writeFile" src/pages/mercadolivre/MLCompras.tsx && grep -q "ReplenishmentParamsDialog" src/pages/mercadolivre/MLCompras.tsx && grep -q "replenishment_params" src/components/mercadolivre/ReplenishmentParamsDialog.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - MLCompras consome useReplenishmentBySku, aplica filtros via useMemo e renderiza filtros + tabela com drill
    - Botao Exportar gera xlsx das linhas filtradas (flat por SKU)
    - ReplenishmentParamsDialog faz CRUD em replenishment_params com zod; submit desabilitado para nao-owner/admin + toast em erro de RLS
    - Aviso REPL-09 ("a chegar nao descontado") presente na pagina
    - Nenhum import de compraUtils.ts ou CompraRecomendadaPanel.tsx
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rota + navegacao /compras e remocao da aba de /estoque (CMP-07)</name>
  <files>src/App.tsx, src/config/roleAccess.ts, src/components/layout/ApiSidebar.tsx, src/contexts/MenuVisibilityContext.tsx, src/pages/mercadolivre/MLEstoque.tsx</files>
  <read_first>
    - src/App.tsx (padrao de Route com React.lazy + RoleRoute + ErrorBoundary + Suspense PageLoader das outras paginas ML)
    - src/config/roleAccess.ts (mapa roleAccess; OPERATIONAL = owner/admin/member)
    - src/components/layout/ApiSidebar.tsx (secao "Operacoes" — itens Anuncios/Estoque/Pedidos/Precificacao/Fluxo de Caixa; icones lucide-react)
    - src/contexts/MenuVisibilityContext.tsx (MENU_SECTIONS, secao "Operacoes")
    - src/pages/mercadolivre/MLEstoque.tsx (import e uso de ReplenishmentPanel: import no topo, TabsTrigger value="compra" "Compra Recomendada", TabsContent value="compra")
  </read_first>
  <action>
    Em `App.tsx`: adicionar `const MLCompras = React.lazy(() => import("./pages/mercadolivre/MLCompras"));` e uma Route `path="/compras"` dentro do bloco protegido, no mesmo padrao das demais (RoleRoute + ErrorBoundary fallbackTitle="Erro em Compras" + Suspense). Atencao: MLCompras usa export default (garantir default export na pagina criada na Task 2).
    Em `roleAccess.ts`: adicionar `"/compras": OPERATIONAL` ao mapa roleAccess (owner/admin/member, D-10). Avaliar inclusao em VIEWER_ELIGIBLE_ROUTES — NAO incluir (D-10 limita a owner/admin/member; viewer fica default-deny).
    Em `ApiSidebar.tsx`: adicionar sob a secao "Operacoes", apos "Estoque", o item `{ icon: ShoppingCart, label: "Compras", path: "/compras" }` (importar ShoppingCart de lucide-react se ainda nao importado).
    Em `MenuVisibilityContext.tsx`: adicionar em MENU_SECTIONS na secao "Operacoes" o item `{ label: "Compras", path: "/compras" }`.
    Em `MLEstoque.tsx`: REMOVER o import de ReplenishmentPanel, o TabsTrigger value="compra" ("Compra Recomendada") e o TabsContent value="compra". Remover tambem o import do icone usado so por essa aba se ele ficar sem uso (verificar). NAO remover o arquivo ReplenishmentPanel.tsx (continua existindo, so deixa de ser usado por /estoque). NAO tocar nada de /precificacao.
  </action>
  <verify>
    <automated>grep -q '"/compras"' src/config/roleAccess.ts && grep -q "/compras" src/App.tsx && grep -q "Compras" src/components/layout/ApiSidebar.tsx && grep -q "Compras" src/contexts/MenuVisibilityContext.tsx && ! grep -q "ReplenishmentPanel" src/pages/mercadolivre/MLEstoque.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx tem a Route /compras (lazy + RoleRoute + ErrorBoundary)
    - roleAccess["/compras"] = OPERATIONAL (owner/admin/member); nao adicionado a VIEWER_ELIGIBLE_ROUTES
    - ApiSidebar e MenuVisibilityContext mostram "Compras" na secao Operacoes
    - MLEstoque.tsx NAO importa nem renderiza ReplenishmentPanel (aba removida); ReplenishmentPanel.tsx continua existindo
    - compraUtils.ts e CompraRecomendadaPanel.tsx intocados
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (role do usuario) -> RoleRoute /compras | acesso a rota gateado por canAccessWithViewer/roleAccess (owner/admin/member) |
| ReplenishmentParamsDialog -> INSERT/UPDATE replenishment_params | RLS owner/admin (Phase 62) e o gate real; o front so dispara e trata erro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Elevation of Privilege | member/viewer escrevendo params pela UI | mitigate | RLS rp_write (owner/admin) enforca no banco; o dialog desabilita submit para nao-owner/admin e exibe toast em erro de permissao (defesa em profundidade, nao substituta da RLS) |
| T-63-09 | Information Disclosure | viewer acessando /compras | mitigate | roleAccess["/compras"]=OPERATIONAL + viewer NAO em VIEWER_ELIGIBLE_ROUTES (default-deny) |
| T-63-10 | Tampering | edicao acidental do compraUtils legado | accept | Prohibition explicita + acceptance grep; arquivos de /precificacao fora do escopo |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta phase (xlsx/react-hook-form/zod/shadcn ja no projeto) |
</threat_model>

<verification>
- npx tsc --noEmit sem novos erros (validado de forma consolidada no 63-04)
- greps de acceptance por tarefa passando (rota/nav/aba removida/components)
- /compras renderiza com filtros, drill e export; aba sumiu de /estoque
</verification>

<success_criteria>
Pagina /compras no menu (Operacoes, owner/admin/member) com Compra Recomendada por SKU: filtros, drill anuncio->variacoes, export xlsx e CRUD de params (write owner/admin). Aba removida de /estoque; compraUtils legado intocado. CMP-05(UI)/CMP-06/CMP-07/CMP-08 satisfeitos.
</success_criteria>

<output>
Create `.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-03-SUMMARY.md` when done
</output>
