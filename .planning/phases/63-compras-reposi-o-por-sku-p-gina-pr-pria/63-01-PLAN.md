---
phase: 63-compras-reposi-o-por-sku-p-gina-pr-pria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/sync-ml-inventory/index.ts
  - supabase/migrations/20260663000000_replenishment_params_add_sku_scope.sql
autonomous: false
requirements:
  - CMP-01
  - CMP-05
user_setup:
  - service: supabase (ckcdevcxgvueywivefgx)
    why: "gsd-executor não tem MCP Supabase nem SUPABASE_ACCESS_TOKEN e NÃO deploya edge function. O deploy da EF sync-ml-inventory e o apply da migration de CHECK são checkpoint do orquestrador (MCP deploy_edge_function + apply_migration), seguido de validação por execute_sql."
    dashboard_config:
      - task: "deploy_edge_function sync-ml-inventory + apply_migration 20260663000000 no projeto ckcdevcxgvueywivefgx; disparar um sync e validar seller_custom_field por variação via execute_sql"
        location: "MCP Supabase deploy_edge_function / apply_migration / execute_sql"

must_haves:
  truths:
    - "sync-ml-inventory faz um segundo-passe por variação (/items/{id}/variations/{vid}) e grava seller_custom_field por variação em ml_inventory_cache.variations (D-02 / CMP-01)"
    - "Após um sync, as variações de anúncios cujo SKU foi cadastrado no ML deixam de ter seller_custom_field nulo (corrige o gap da amostra MLB3818741753)"
    - "replenishment_params.scope passa a aceitar 'sku' além de 'global'/'marca' (CHECK atualizado, D-08 / CMP-05)"
  artifacts:
    - path: "supabase/functions/sync-ml-inventory/index.ts"
      provides: "Segundo-passe per-variação que enriquece seller_custom_field antes do upsert"
      contains: "variations/"
    - path: "supabase/migrations/20260663000000_replenishment_params_add_sku_scope.sql"
      provides: "ALTER do CHECK de scope para incluir 'sku'"
      contains: "ALTER TABLE public.replenishment_params"
  key_links:
    - from: "supabase/functions/sync-ml-inventory/index.ts"
      to: "ML API /items/{id}/variations/{variationId}"
      via: "mlFetch por variação com concorrência limitada, espelhando ml-inventory/index.ts"
      pattern: "variations/"
  prohibitions:
    - "NÃO remover nem alterar o resolveSku item-level existente (seller_custom_field do item continua igual)"
    - "NÃO abortar o sync inteiro se uma chamada de variação falhar — try/catch por variação (padrão ml-inventory, Assumption A3)"
    - "NÃO tocar a EF ml-inventory (exibição) — só sync-ml-inventory grava no banco"
---

<objective>
Fechar a fundação de dados que falta para a reposição por SKU: (1) `sync-ml-inventory` passa a fazer o segundo-passe por variação (`/items/{id}/variations/{variationId}`) e gravar `seller_custom_field` por variação em `ml_inventory_cache.variations` — hoje nulo (CMP-01, D-02); (2) `replenishment_params` ganha o escopo `'sku'` no CHECK (CMP-05, D-08). Deploy da EF + apply da migration via MCP (checkpoint do orquestrador) com validação por SQL de que os SKUs por variação passaram a preencher.

Purpose: Sem o `seller_custom_field` por variação, o join de custo por SKU (Plan 63-02) não casa e o "custo ausente" não cai. Esta é a pré-condição de dados para o motor por SKU.
Output: EF sync-ml-inventory com segundo-passe, migration de CHECK aplicada, e prova SQL de que as variações estão com SKU preenchido.
</objective>

<artifacts_produced>
- `supabase/functions/sync-ml-inventory/index.ts` — segundo-passe per-variação (enriquece `seller_custom_field`)
- `supabase/migrations/20260663000000_replenishment_params_add_sku_scope.sql` — CHECK `scope IN ('global','marca','sku')`
- Dado vivo: `ml_inventory_cache.variations[].seller_custom_field` populado para anúncios com SKU por variação no ML
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-CONTEXT.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Segundo-passe per-variação no sync-ml-inventory (CMP-01)</name>
  <files>supabase/functions/sync-ml-inventory/index.ts</files>
  <read_first>
    - supabase/functions/sync-ml-inventory/index.ts (estado atual: monta `rows` com `variations` mas grava `seller_custom_field: resolveSku(v)` = null porque o multi-get não retorna isso por variação — linhas ~199-238)
    - supabase/functions/ml-inventory/index.ts (EF de exibição: já tem o segundo-passe — chama `/items/${item.id}/variations/${variation.variation_id}` com concorrência limitada e usa `resolveSku(fullVar) ?? variation.seller_custom_field`; espelhar EXATAMENTE o padrão de concorrência e try/catch)
    - .planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md (seção "CMP-01: Análise do Bloqueante" e "Fix para CMP-01" — código de referência do segundo-passe)
  </read_first>
  <action>
    Em `sync-ml-inventory/index.ts`, DEPOIS de montar o array `rows` (o loop de multi-get que constrói cada `row.variations`) e ANTES do loop de upsert em `ml_inventory_cache`, adicionar um segundo-passe que enriquece `seller_custom_field` por variação. Para cada `row` cujo array `variations` tenha comprimento maior que zero, e para cada variação desse array, chamar `mlFetch(\`/items/${row.item_id}/variations/${variation.variation_id}\`, access_token)` e atribuir `variation.seller_custom_field = resolveSku(fullVar) ?? variation.seller_custom_field`. Reutilizar a função `resolveSku` já existente no arquivo (cobre `seller_custom_field` e o atributo `SELLER_SKU`). Limitar a concorrência a no máximo 20 chamadas simultâneas (fatiar a lista de pares row/variation em batches de 20 e usar `Promise.all` por batch — mesmo padrão de `ml-inventory`). Envolver cada chamada de variação em try/catch que apenas registra `console.warn` com `row.item_id` e `variation.variation_id` e segue — uma falha de variação NÃO pode abortar o sync (Assumption A3). Iterar sobre o array `variations` independentemente do flag `has_variations` (um item com exatamente 1 variação tem `has_variations=false` mas ainda possui `variations[0]` a enriquecer). Não alterar nenhum outro campo do upsert nem o `resolveSku(b)` item-level.
  </action>
  <verify>
    <automated>grep -c "variations/\${variation.variation_id}\|variations/\` + \|/variations/" supabase/functions/sync-ml-inventory/index.ts | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo contém uma chamada `mlFetch` ao endpoint `/items/{item_id}/variations/{variation_id}` posicionada após a montagem de `rows` e antes do upsert
    - A atribuição usa `resolveSku(fullVar) ?? variation.seller_custom_field` (não sobrescreve com null)
    - Concorrência limitada a 20 por batch com `Promise.all`
    - try/catch por variação com `console.warn` (não lança); o upsert e o resolveSku item-level permanecem inalterados
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migration — replenishment_params aceita scope 'sku' (CMP-05)</name>
  <files>supabase/migrations/20260663000000_replenishment_params_add_sku_scope.sql</files>
  <read_first>
    - supabase/migrations/20260662000000_replenishment_params.sql (CHECK inline atual: `scope TEXT NOT NULL DEFAULT 'global' CHECK (scope IN ('global', 'marca'))` — o Postgres auto-nomeia essa constraint inline como `replenishment_params_scope_check`)
    - .planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md (seção "CMP-05: Mudança de Schema")
  </read_first>
  <action>
    Criar a migration que substitui o CHECK de `scope`. Primeiro `ALTER TABLE public.replenishment_params DROP CONSTRAINT IF EXISTS replenishment_params_scope_check;` (nome auto-gerado pela constraint inline da migration 62). Depois `ALTER TABLE public.replenishment_params ADD CONSTRAINT replenishment_params_scope_check CHECK (scope IN ('global', 'marca', 'sku'));`. Para escopo `sku`, `scope_value` recebe o `seller_custom_field` da variação (formato Tiny, ex. `020491CA35GRX`) — a UNIQUE existente `(organization_id, scope, scope_value)` já cobre o novo escopo sem alteração (Pitfall 7). Cabeçalho da migration deve citar Projeto `ckcdevcxgvueywivefgx`, requisito CMP-05 e a decisão D-08 (precedência SKU > marca > global). NÃO alterar RLS, índice ou colunas — só o CHECK.
  </action>
  <verify>
    <automated>grep -q "ADD CONSTRAINT replenishment_params_scope_check" supabase/migrations/20260663000000_replenishment_params_add_sku_scope.sql && grep -q "'sku'" supabase/migrations/20260663000000_replenishment_params_add_sku_scope.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A migration faz DROP CONSTRAINT IF EXISTS seguido de ADD CONSTRAINT com `scope IN ('global', 'marca', 'sku')`
    - Não há ALTER de RLS, índice ou outras colunas
    - Cabeçalho cita ckcdevcxgvueywivefgx, CMP-05 e D-08
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Deploy EF + apply migration via MCP + validar SKUs por variação (checkpoint do orquestrador)</name>
  <action>
    Checkpoint do ORQUESTRADOR. O executor já escreveu o segundo-passe (Task 1) e a migration de CHECK (Task 2); aqui o orquestrador faz o deploy da EF e aplica a migration via MCP Supabase no projeto `ckcdevcxgvueywivefgx`, dispara um sync e prova por SQL que as variações passaram a preencher `seller_custom_field`. Sem este passo a verificação dá falso-positivo (o repo muda mas o banco/EF live não).
  </action>
  <what-built>
    sync-ml-inventory com segundo-passe per-variação e migration de CHECK escritos no repo, ainda NÃO deployados/aplicados no projeto live. O gsd-executor não deploya EF nem aplica migration (sem MCP Supabase / SUPABASE_ACCESS_TOKEN).
  </what-built>
  <how-to-verify>
    [ORQUESTRADOR via MCP Supabase no projeto ckcdevcxgvueywivefgx — Org Pé Vermeio = 7f615df7-7bac-45e5-8a93-827fb9ddeec7]
    1. apply_migration com o conteúdo de supabase/migrations/20260663000000_replenishment_params_add_sku_scope.sql
    2. Confirmar o CHECK: execute_sql inserindo (em transação ROLLBACK) uma linha com scope='sku' — deve passar; e uma com scope='xpto' — deve violar o CHECK.
    3. deploy_edge_function sync-ml-inventory (script completo do repo).
    4. Disparar um sync para a loja do Pé Vermeio (invoke sync-ml-inventory com o ml_user_id do Pé Vermeio, Bearer service-role) e aguardar concluir.
    5. Open Question 1 (prova do CMP-01) — execute_sql:
       SELECT count(*) AS variacoes_total,
              count(*) FILTER (WHERE sku IS NOT NULL AND sku <> '') AS variacoes_com_sku
       FROM ml_inventory_cache i
       CROSS JOIN LATERAL jsonb_to_recordset(i.variations) AS v(seller_custom_field TEXT)
       , LATERAL (SELECT v.seller_custom_field AS sku) s
       WHERE i.organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7'
         AND i.has_variations = true;
       Esperado: variacoes_com_sku > 0 (se Wesley cadastrou SKU nas variações — Assumption A1/A4). Se = 0, PARAR e escalar: provável SKU não cadastrado no ML → ver fallback diferido (ponte via Tiny no CONTEXT.md).
    6. Open Question 2 (cobertura de venda por SKU, insumo do 63-02) — execute_sql:
       SELECT count(*) AS pedidos, count(NULLIF(variation_id,'')) AS com_variation
       FROM ml_orders
       WHERE organization_id = '7f615df7-7bac-45e5-8a93-827fb9ddeec7'
         AND data_pedido >= CURRENT_DATE - 60;
       Registrar a razão com_variation/pedidos no SUMMARY (insumo p/ validar velocidade por SKU em 63-04).
    7. get_advisors (security) — sem novo ERROR relacionado a replenishment_params.
  </how-to-verify>
  <acceptance_criteria>
    - apply_migration sem erro; CHECK aceita 'sku' e rejeita valor inválido (prova em transação ROLLBACK)
    - deploy_edge_function sync-ml-inventory concluído; sync executado sem erro fatal
    - SELECT de variações retorna variacoes_com_sku > 0 (ou, se 0, decisão de escalonamento registrada no SUMMARY)
    - Cobertura de ml_orders.variation_id registrada no SUMMARY
    - get_advisors sem novo ERROR de security
  </acceptance_criteria>
  <resume-signal>Digite "aplicado" com o output dos SELECTs de validação (variações com SKU + cobertura de variation_id), ou descreva o erro/escalonamento</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync-ml-inventory (service role) → ML API | Token ML por loja; segundo-passe acrescenta ~N chamadas por variação |
| sync-ml-inventory → ml_inventory_cache | EF é o único escritor; escreve por organization_id/ml_user_id do token |
| authenticated → INSERT replenishment_params (scope='sku') | RLS de escrita owner/admin (inalterada) é o gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Information Disclosure | Segundo-passe vazando dados entre orgs (token errado por loja) | mitigate | A EF resolve `access_token`/`organization_id` por `ml_user_id` no `ml_tokens` e só grava sob esse organization_id; o segundo-passe usa o mesmo `access_token` da loja em sync — sem cruzamento de org |
| T-63-02 | Denial of Service | Catálogo grande → muitas chamadas `/variations/{vid}` estourando timeout da EF | mitigate | Concorrência limitada a 20 + try/catch por variação; para >500 variações, considerar EdgeRuntime.waitUntil (lição Phase 59, registrado como follow-up) |
| T-63-03 | Tampering / EoP | member tentando escrever scope='sku' em replenishment_params | mitigate | RLS `rp_write` (owner/admin) da Phase 62 permanece intocada; o CHECK só amplia o domínio de scope |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- grep confirma o segundo-passe `/items/{id}/variations/{vid}` no sync-ml-inventory
- grep confirma o ADD CONSTRAINT com 'sku' na migration
- Checkpoint do orquestrador: EF deployada, migration aplicada, `variacoes_com_sku > 0` provado por SQL (ou escalonamento registrado), cobertura de variation_id registrada
</verification>

<success_criteria>
sync-ml-inventory grava `seller_custom_field` por variação em produção; `replenishment_params` aceita scope `sku`; prova SQL de SKUs por variação preenchidos. CMP-01 e a parte de schema do CMP-05 satisfeitos. Fundação de dados pronta para o motor por SKU (63-02) casar o custo.
</success_criteria>

<output>
Create `.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-01-SUMMARY.md` when done
</output>
