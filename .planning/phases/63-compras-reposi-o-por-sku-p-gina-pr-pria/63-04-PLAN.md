---
phase: 63-compras-reposi-o-por-sku-p-gina-pr-pria
plan: 04
type: execute
wave: 3
depends_on:
  - "63-01"
  - "63-02"
  - "63-03"
files_modified: []
autonomous: false
requirements:
  - CMP-09
user_setup:
  - service: supabase (ckcdevcxgvueywivefgx)
    why: "Prova final por execute_sql na base live (queda do custo ausente por SKU + anti-IDOR + linhas por variacao) — depende do sync do 63-01 ter populado seller_custom_field e da RPC do 63-02 estar aplicada. gsd-executor nao tem MCP Supabase; e checkpoint do orquestrador."
    dashboard_config:
      - task: "execute_sql de validacao final (custo_ausente vs 44/116, anti-IDOR, por_variacao) no projeto ckcdevcxgvueywivefgx"
        location: "MCP Supabase execute_sql"

must_haves:
  truths:
    - "A suite vitest inteira e o build/tsc passam sem regressao (CMP-09)"
    - "Na base live, get_replenishment_by_sku retorna linhas por variacao e a taxa de custo_ausente caiu vs a Phase 62 (44/116 por anuncio) — corrige o custo ausente por SKU (CMP-04/CMP-09)"
    - "Org alheia (Thales) sob JWT Pe Vermeio retorna 0 linhas na RPC por SKU (anti-IDOR, CMP-09/D-07)"
    - "Wesley confirma visualmente /compras: filtros, drill anuncio->variacoes, export xlsx, CRUD de params (owner edita, member nao), aba removida de /estoque (CMP-05/06/07/08)"
  artifacts: []
  key_links: []
  prohibitions:
    - "NAO marcar a fase como verificada se custo_ausente por SKU nao cair vs 44/116 sem registrar a causa (provavel SKU nao cadastrado no ML — Assumption A1/A4) e decisao do Wesley"
    - "NAO pular o teste cross-org — anti-IDOR e criterio de sucesso travado (block on high)"
---

<objective>
Verificacao final da fase: provar sem regressao (testes + build), provar na base live que a reposicao por SKU corrige o custo ausente (queda vs 44/116 da Phase 62) e mantem o anti-IDOR, e obter o ok visual do Wesley em /compras (filtros, drill, export, CRUD de params, aba removida de /estoque).

Purpose: Fechar CMP-09 e garantir que os blocos A/B/C entregaram o resultado observavel — reposicao por SKU com custo casado, sem quebrar nada.
Output: Suite verde, prova SQL da queda do custo ausente + anti-IDOR, e aprovacao visual do Wesley.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-CONTEXT.md
@.planning/phases/62-reposicao-server-side/62-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite de testes + build sem regressao (CMP-09)</name>
  <files>(nenhum — apenas executa)</files>
  <read_first>
    - .planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-RESEARCH.md (CMP-09: replenishmentUtils com 203 testes + casos por SKU)
  </read_first>
  <action>
    Rodar a suite vitest inteira e a checagem de tipos/build. Se algo quebrar, corrigir o que for regressao introduzida pela fase (sem reabrir escopo). Registrar no SUMMARY a contagem de testes verdes e o status do build.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run: todos os testes verdes (203 da Phase 62 + novos casos por SKU do 63-02)
    - npx tsc --noEmit: sem erros
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Prova SQL na base live — custo ausente cai + anti-IDOR (checkpoint do orquestrador)</name>
  <action>
    Checkpoint do ORQUESTRADOR via MCP Supabase. Provar na base live que a reposicao por SKU corrige o custo ausente e mantem o anti-IDOR. Depende do sync do 63-01 ter populado seller_custom_field por variacao e da RPC do 63-02 estar aplicada.
  </action>
  <what-built>
    Sync por variacao (63-01) + RPC por SKU (63-02) aplicados em prod. Falta a prova quantitativa de que o custo ausente caiu vs a Phase 62 e que o anti-IDOR vale por SKU.
  </what-built>
  <how-to-verify>
    [ORQUESTRADOR via MCP Supabase no projeto ckcdevcxgvueywivefgx — Org Pe Vermeio = 7f615df7-7bac-45e5-8a93-827fb9ddeec7; Thales = e4150d57-1349-48c9-9a89-82b1774857b0]
    1. Custo ausente por SKU vs Phase 62 — execute_sql:
       SELECT count(*) AS skus,
              count(*) FILTER (WHERE custo_ausente)     AS skus_sem_custo,
              count(*) FILTER (WHERE variation_id IS NOT NULL) AS por_variacao,
              count(*) FILTER (WHERE NOT custo_ausente) AS skus_com_custo
       FROM get_replenishment_by_sku('7f615df7-7bac-45e5-8a93-827fb9ddeec7', 30, 1.0);
       Comparar com a Phase 62 (44/116 anuncios sem custo). Esperado: a fracao skus_sem_custo/skus por SKU MENOR que a fracao por anuncio da Phase 62 (o custo passou a casar por SKU). Se NAO cair, registrar a causa (provavel SKU nao cadastrado no ML nas variacoes — Assumption A1/A4) e levar a decisao do Wesley antes de fechar a fase.
    2. Anti-IDOR por SKU — execute_sql chamando get_replenishment_by_sku com a org Thales sob impersonacao de usuario Pe Vermeio (SET LOCAL ROLE authenticated + request.jwt.claims, transacao ROLLBACK) — deve retornar 0 linhas.
    3. Sanidade do drill: por_variacao > 0 (anuncios com variacao desaninhados) e existe pelo menos 1 anuncio sem variacao como SKU unico.
    Registrar todos os numeros no SUMMARY e no 63-VERIFICATION.md.
  </how-to-verify>
  <acceptance_criteria>
    - skus_sem_custo/skus (por SKU) menor que 44/116 (por anuncio) OU causa registrada + decisao do Wesley
    - Chamada cross-org (Thales sob JWT Pe Vermeio) retorna 0 linhas
    - por_variacao > 0 e ao menos 1 SKU unico (sem variacao) presente
  </acceptance_criteria>
  <resume-signal>Digite "verificado" com os numeros (skus, skus_sem_custo, por_variacao, cross-org), ou descreva a causa da nao-queda do custo ausente</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Ok visual do Wesley em /compras (CMP-05/06/07/08)</name>
  <action>
    Checkpoint visual/comportamental. Wesley navega /compras logado e confirma os requisitos de UI da fase.
  </action>
  <what-built>
    Pagina /compras (filtros + drill + export + CRUD de params) no ar; aba "Compra Recomendada" removida de /estoque.
  </what-built>
  <how-to-verify>
    [WESLEY logado no app, projeto live]
    1. Abrir /compras pelo menu (secao Operacoes). A tela lista linhas por SKU; expandir um anuncio com variacoes (drill) mostra as variacoes Cor/Tamanho com estoque/venda/cobertura/ponto/sugestao proprios.
    2. Filtros: filtrar por marca, por "gatilho ativo", por "sem giro", por "sem custo" e buscar por SKU/tamanho/titulo — a lista responde.
    3. Exportar xlsx — o arquivo baixa com as colunas por SKU.
    4. "Editar parametros": como owner, criar/editar um param scope='sku' (ex. um SKU especifico) e ver a sugestao recalcular; confirmar que a precedencia SKU>marca>global aparece (param_origem). (Opcional) confirmar que um usuario member nao consegue salvar (botao desabilitado/toast).
    5. Abrir /estoque e confirmar que a aba "Compra Recomendada" NAO existe mais.
    6. Abrir /precificacao e confirmar que a analise de compra legada continua funcionando (intocada).
  </how-to-verify>
  <acceptance_criteria>
    - /compras visivel no menu; drill anuncio->variacoes funcional
    - Filtros (marca/status/custo/busca) e export xlsx funcionam
    - CRUD de params com precedencia SKU>marca>global; write so owner/admin
    - Aba "Compra Recomendada" ausente de /estoque; /precificacao intocado
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados em /compras</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verificacao -> RPC get_replenishment_by_sku (cross-org) | prova de anti-IDOR sob impersonacao em transacao ROLLBACK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-11 | Information Disclosure | regressao de anti-IDOR na RPC por SKU passar despercebida | mitigate | Teste cross-org explicito (Thales sob JWT Pe Vermeio = 0 linhas) e criterio bloqueante desta fase |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta phase |
</threat_model>

<verification>
- vitest run + tsc --noEmit verdes
- SQL: skus_sem_custo por SKU menor que 44/116; cross-org = 0; por_variacao > 0
- Wesley aprova /compras (filtros, drill, export, params) + aba removida de /estoque
</verification>

<success_criteria>
Fase 63 verificada: sem regressao de testes/build; reposicao por SKU com custo casado (custo ausente caiu vs Phase 62) e anti-IDOR provado; /compras aprovada visualmente. CMP-09 satisfeito; CMP-01..08 confirmados de ponta a ponta.
</success_criteria>

<output>
Create `.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-04-SUMMARY.md` and `.planning/phases/63-compras-reposi-o-por-sku-p-gina-pr-pria/63-VERIFICATION.md` when done
</output>
