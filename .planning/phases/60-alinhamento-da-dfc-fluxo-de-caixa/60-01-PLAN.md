---
phase: 60-alinhamento-da-dfc-fluxo-de-caixa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260660000000_cashflow_dfc_alignment.sql
autonomous: false
requirements:
  - CASHFIX-05
  - CASHFIX-06
user_setup: []

must_haves:
  truths:
    - "get_cashflow passa a ter 4 args (UUID,DATE,DATE,BOOLEAN p_include_purchase_forecasts DEFAULT false); a assinatura antiga de 3 args foi DROPada antes do CREATE (sem ambiguidade) — per CASHFIX-06"
    - "Com toggle OFF (default false), a CTE exp filtra status='pending' E COALESCE(category,'')<>'Previsões de compra' — per CASHFIX-06"
    - "No dia 8+ (datas > hoje(BRT)+7), accumulated_balance_sma usa GREATEST(d.inc, v_sma) — a média de 15d vira PISO — per CASHFIX-05"
    - "No dia 8+, daily_projection = GREATEST(0, v_sma - d.inc) — per CASHFIX-05"
    - "Dias 1-7 (datas <= hoje(BRT)+7) permanecem confirmado-only (d.inc) — regra travada na Phase 59, INTACTA"
    - "A fórmula da linha accumulated_balance NÃO muda; o filtro de previsões na CTE exp afeta ambas as linhas (previsão não é caixa real)"
    - "Reconciliação provada: soma das saídas em aberto 05-12/07 = R$87.105,79 com OFF e R$99.495,58 com ON; OC 383 aparece 1x com OFF"
    - "SECURITY INVOKER, search_path='public', REVOKE de PUBLIC/anon e GRANT authenticated preservados na nova assinatura de 4 args"
  artifacts:
    - path: "supabase/migrations/20260660000000_cashflow_dfc_alignment.sql"
      provides: "DROP da get_cashflow(UUID,DATE,DATE) + CREATE da versão de 4 args com GREATEST (entrada piso) e filtro de Previsões de compra controlável por p_include_purchase_forecasts"
      contains: "CREATE OR REPLACE FUNCTION public.get_cashflow"
  key_links:
    - from: "supabase/migrations/20260660000000_cashflow_dfc_alignment.sql"
      to: "cash_outflows"
      via: "CTE exp filtra co.status='pending' e (p_include_purchase_forecasts OR COALESCE(co.category,'')<>'Previsões de compra')"
      pattern: "Previs"
---

<objective>
Estender a RPC `public.get_cashflow` (projeto Supabase `ckcdevcxgvueywivefgx`) com as duas correções LOCKED pelo Wesley para alinhar a projeção do `/caixa` à DFC/Tiny: (1) ENTRADA — no dia 8+ a média de 15d (`v_sma`) vira PISO via `GREATEST(d.inc, v_sma)` para a cauda de recebimentos minúsculos do MP parar de suprimir a média (CASHFIX-05); (2) SAÍDA — novo 4º parâmetro `p_include_purchase_forecasts BOOLEAN DEFAULT false` que, quando `false`, exclui `category='Previsões de compra'` da CTE `exp` (CASHFIX-06), reconciliando o caixa ao centavo com o Tiny e resolvendo a OC 383 contada 2x.

Purpose: O diagnóstico está 100% fechado (dados live + decisões do owner no 60-CONTEXT.md). Esta migration é cirúrgica sobre a versão atual `20260659000300_cashflow_outflows_pending_only.sql` — só muda a CTE `exp` (filtro de previsões) e o ramo dia 8+ de `accumulated_balance_sma`/`daily_projection` (GREATEST). A linha confirmada `accumulated_balance` (reconciliada ao centavo na Phase 49/59) NÃO tem a fórmula alterada — embora o filtro de previsões na CTE `exp` reduza as saídas de AMBAS as linhas, que é o comportamento desejado (previsão de compra não é caixa real).

Output: 1 migration versionada que faz `DROP FUNCTION public.get_cashflow(UUID,DATE,DATE)` antes do `CREATE` da versão de 4 args. A aplicação em produção (MCP `apply_migration`) e a prova de reconciliação são executadas pelo orquestrador no checkpoint (o executor GSD não aplica migration).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-alinhamento-da-dfc-fluxo-de-caixa/60-CONTEXT.md

# Versão ATUAL da RPC — base EXATA a estender (CASHFIX-04, status='pending')
@supabase/migrations/20260659000300_cashflow_outflows_pending_only.sql

# Regra de projeção 7d (CASHFIX-01) — comentários do CASE de accumulated_balance_sma e daily_projection
@supabase/migrations/20260659000000_cashflow_projection_7d_rule.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration get_cashflow 4-arg — entrada piso (GREATEST) + filtro de Previsões de compra</name>
  <files>supabase/migrations/20260660000000_cashflow_dfc_alignment.sql</files>
  <read_first>
    - supabase/migrations/20260659000300_cashflow_outflows_pending_only.sql (VERSÃO BASE — copiar integralmente e aplicar SOMENTE as mudanças abaixo)
    - 60-CONTEXT.md seção `<decisions>` (decisões LOCKED: ENTRADA Opção A, SAÍDA toggle, cuidado de migração da assinatura)
  </read_first>
  <action>
    Criar a migration versionada `20260660000000_cashflow_dfc_alignment.sql` partindo de uma cópia EXATA de `20260659000300_cashflow_outflows_pending_only.sql`. Começar o arquivo com `DROP FUNCTION IF EXISTS public.get_cashflow(UUID,DATE,DATE);` ANTES do `CREATE OR REPLACE` — obrigatório, pois trocar a assinatura para 4 args com default causaria erro de função não-única quando chamada com 3 args (per 60-CONTEXT decisão "Assinatura da função").

    Mudar a assinatura para `public.get_cashflow(p_org_id UUID, p_start_date DATE, p_end_date DATE, p_include_purchase_forecasts BOOLEAN DEFAULT false)`. Manter o RETURNS TABLE de 7 colunas idêntico, `LANGUAGE plpgsql SECURITY INVOKER SET search_path = 'public'`, o bloco DECLARE, `v_today := (now() AT TIME ZONE 'America/Sao_Paulo')::date`, `v_start := GREATEST(p_start_date, v_today)`, o cálculo de `v_sma`, e os CTEs `days`/`inc`/`daily` exatamente como na base.

    Aplicar APENAS estas três mudanças (per CASHFIX-05 e CASHFIX-06):
    1. CTE `exp`: ao lado do `AND co.status = 'pending'` já existente, adicionar `AND (p_include_purchase_forecasts OR COALESCE(co.category, '') <> 'Previsões de compra')`. Quando o parâmetro é false (default), exclui as ordens de compra não faturadas; quando true, soma-as.
    2. `daily_projection` (3ª coluna do SELECT): manter o ramo dos dias 1-7 retornando 0 (`WHEN d.d_date <= v_today + 7 THEN 0::NUMERIC`); no ramo dia 8+ (ELSE), trocar a lógica `WHEN d.inc > 0 THEN 0 ELSE v_sma` por `GREATEST(0, v_sma - d.inc)::NUMERIC` (o quanto a média acrescenta acima do confirmado).
    3. `accumulated_balance_sma` (7ª coluna): manter o ramo dos dias 1-7 usando `d.inc` (`WHEN d.d_date <= v_today + 7 THEN d.inc`); no ramo dia 8+ (ELSE), trocar `WHEN d.inc > 0 THEN d.inc ELSE v_sma` por `GREATEST(d.inc, v_sma)`. A subtração de `d.exp` e o `SUM(...) OVER (ORDER BY d.d_date ASC)` permanecem.

    NÃO alterar a fórmula da coluna `accumulated_balance` (linha confirmada `v_initial + SUM(d.inc - d.exp) OVER ...`) — ela muda de VALOR apenas porque a CTE `exp` agora exclui previsões, o que é o comportamento desejado, mas a EXPRESSÃO não muda.

    Ao final, ajustar o REVOKE/GRANT para a NOVA assinatura de 4 args: `REVOKE EXECUTE ON FUNCTION public.get_cashflow(UUID,DATE,DATE,BOOLEAN) FROM PUBLIC, anon;` e `GRANT EXECUTE ON FUNCTION public.get_cashflow(UUID,DATE,DATE,BOOLEAN) TO authenticated;`. Atualizar o comentário de cabeçalho descrevendo as mudanças (entrada piso GREATEST + 4º param de previsões) e o projeto `ckcdevcxgvueywivefgx`.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260660000000_cashflow_dfc_alignment.sql && grep -q 'p_include_purchase_forecasts BOOLEAN DEFAULT false' supabase/migrations/20260660000000_cashflow_dfc_alignment.sql && grep -q 'DROP FUNCTION IF EXISTS public.get_cashflow(UUID,DATE,DATE)' supabase/migrations/20260660000000_cashflow_dfc_alignment.sql && grep -q 'GREATEST(d.inc, v_sma)' supabase/migrations/20260660000000_cashflow_dfc_alignment.sql && grep -q 'GREATEST(0, v_sma - d.inc)' supabase/migrations/20260660000000_cashflow_dfc_alignment.sql && grep -q "get_cashflow(UUID,DATE,DATE,BOOLEAN) TO authenticated" supabase/migrations/20260660000000_cashflow_dfc_alignment.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260660000000_cashflow_dfc_alignment.sql` existe.
    - Faz `DROP FUNCTION IF EXISTS public.get_cashflow(UUID,DATE,DATE)` antes do CREATE.
    - Assinatura `(UUID,DATE,DATE,BOOLEAN DEFAULT false)`; RETURNS TABLE de 7 colunas preservado.
    - CTE `exp` contém `co.status = 'pending'` E o filtro `(p_include_purchase_forecasts OR COALESCE(co.category,'') <> 'Previsões de compra')`.
    - Dia 8+: `accumulated_balance_sma` usa `GREATEST(d.inc, v_sma)`; `daily_projection` usa `GREATEST(0, v_sma - d.inc)`. Dias 1-7 intactos (d.inc / 0).
    - `SECURITY INVOKER`, `SET search_path='public'`, REVOKE de PUBLIC/anon e GRANT authenticated na assinatura de 4 args.
  </acceptance_criteria>
  <done>Migration escrita e validada por grep; pronta para apply via MCP no checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Aplicar migration via MCP apply_migration + provar reconciliação (orquestrador)</name>
  <action>Checkpoint do ORQUESTRADOR (não autônomo — gsd-executor não tem MCP/token). Aplicar a migration da Task 1 via MCP `apply_migration` no projeto `ckcdevcxgvueywivefgx` (NUNCA `db push` local — projeto local linkado no errado; NUNCA via SQL Editor — regra feedback_no_drift_via_sql_editor), depois rodar os 3 SELECTs de prova abaixo e obter aprovação do Wesley.</action>
  <what-built>Migration `20260660000000_cashflow_dfc_alignment.sql` com a get_cashflow de 4 args (entrada piso GREATEST + filtro de Previsões de compra).</what-built>
  <how-to-verify>
    O ORQUESTRADOR aplica a migration em produção via MCP Supabase `apply_migration` (projeto `ckcdevcxgvueywivefgx`) — o executor GSD não aplica migration (padrão do projeto: deploy via MCP/CLI pelo orquestrador). Depois, rodar as provas de reconciliação via MCP `execute_sql` (org Pé Vermeio `7f615df7-7bac-45e5-8a93-827fb9ddeec7`):

    1. Saídas em aberto 05-12/07 com toggle OFF devem somar R$87.105,79:
       SELECT SUM(daily_expense) FROM get_cashflow('7f615df7-7bac-45e5-8a93-827fb9ddeec7','2026-07-05','2026-07-12', false);
       Esperado: 87105.79

    2. Saídas em aberto 05-12/07 com toggle ON (inclui previsões) devem somar R$99.495,58:
       SELECT SUM(daily_expense) FROM get_cashflow('7f615df7-7bac-45e5-8a93-827fb9ddeec7','2026-07-05','2026-07-12', true);
       Esperado: 99495.58 (diferença = R$12.389,79 das previsões OC 383 + OC 410)

    3. Prova de que a OC 383 some da cópia "previsão" (09/07) com OFF: a saída de 09/07 com OFF não inclui os R$10.390,28 da OC 383 que aparecem com ON; a conta real de 11/07 permanece nas duas chamadas.

    4. Chamada de 3 args deve falhar (assinatura antiga DROPada) OU resolver para o default — confirmar que não há ambiguidade ("function is not unique").
  </how-to-verify>
  <resume-signal>Digite "approved" se as três somas baterem (87.105,79 OFF / 99.495,58 ON / OC383 1x), ou descreva a divergência.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado → RPC get_cashflow | usuário do app chama a RPC com org_id; entrada não confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Information Disclosure | get_cashflow(org_id) cross-tenant | mitigate | SECURITY INVOKER + RLS por organization_id nas tabelas cash_inflows/cash_outflows/orders/financial_settings (sem SECURITY DEFINER — evita IDOR, per feedback_supabase_security_invoker) |
| T-60-02 | Elevation of Privilege | EXECUTE da função por anon | mitigate | REVOKE EXECUTE FROM PUBLIC, anon; GRANT só a authenticated, na assinatura de 4 args |
| T-60-03 | Tampering | search_path hijack | mitigate | SET search_path='public' fixo na função |
</threat_model>

<verification>
- `grep` confirma DROP de 3 args, assinatura de 4 args, GREATEST nas duas colunas, filtro de previsões e GRANT/REVOKE na nova assinatura.
- Checkpoint (orquestrador via MCP): apply_migration + 3 provas de reconciliação (87.105,79 / 99.495,58 / OC383 1x).
</verification>

<success_criteria>
- Migration aplicada em prod; somas reconciliadas batem ao centavo; assinatura de 3 args removida sem ambiguidade; SECURITY INVOKER/REVOKE/GRANT preservados.
</success_criteria>

<artifacts_this_phase_produces>
- **Nova assinatura:** `public.get_cashflow(p_org_id UUID, p_start_date DATE, p_end_date DATE, p_include_purchase_forecasts BOOLEAN DEFAULT false)` — RETURNS TABLE de 7 colunas inalterado.
- **Migration:** `supabase/migrations/20260660000000_cashflow_dfc_alignment.sql` (DROP 3-arg + CREATE 4-arg, GREATEST + filtro de previsões).
- Consumido pelo PLANO 60-02 (frontend) que passa o 4º argumento.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/60-alinhamento-da-dfc-fluxo-de-caixa/60-01-SUMMARY.md` when done
</output>
