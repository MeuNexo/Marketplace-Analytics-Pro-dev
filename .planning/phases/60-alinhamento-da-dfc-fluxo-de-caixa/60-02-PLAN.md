---
phase: 60-alinhamento-da-dfc-fluxo-de-caixa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useCashFlowData.ts
  - src/pages/mercadolivre/MLFluxoCaixa.tsx
autonomous: false
requirements:
  - CASHFIX-06
user_setup: []

must_haves:
  truths:
    - "A página /fluxo-de-caixa (rota /fluxo-de-caixa, componente MLFluxoCaixa) tem um Switch shadcn 'Incluir previsões de compra', DESLIGADO por padrão — per CASHFIX-06"
    - "O estado do toggle propaga o 4º argumento p_include_purchase_forecasts na chamada supabase.rpc('get_cashflow', ...) via useCashFlowData"
    - "Com o toggle OFF (default) a UX atual fica intacta; alternar o toggle ao vivo muda a curva do gráfico (refetch da query)"
    - "Build verde (vite/tsc) sem erros de tipo"
  artifacts:
    - path: "src/hooks/useCashFlowData.ts"
      provides: "useCashFlowData aceita includePurchaseForecasts (default false), inclui-o na queryKey e passa p_include_purchase_forecasts no rpc('get_cashflow')"
      contains: "p_include_purchase_forecasts"
    - path: "src/pages/mercadolivre/MLFluxoCaixa.tsx"
      provides: "Estado React do toggle + Switch shadcn 'Incluir previsões de compra' (off por padrão), passado a useCashFlowData"
      contains: "includePurchaseForecasts"
  key_links:
    - from: "src/pages/mercadolivre/MLFluxoCaixa.tsx"
      to: "src/hooks/useCashFlowData.ts"
      via: "useCashFlowData(startDate, endDate, includePurchaseForecasts) — estado do Switch propagado"
      pattern: "includePurchaseForecasts"
    - from: "src/hooks/useCashFlowData.ts"
      to: "get_cashflow (RPC)"
      via: "supabase.rpc('get_cashflow', { ..., p_include_purchase_forecasts })"
      pattern: "p_include_purchase_forecasts"
---

<objective>
Adicionar o toggle "Incluir previsões de compra" na página `/fluxo-de-caixa` (CASHFIX-06), desligado por padrão, propagando o 4º argumento `p_include_purchase_forecasts` para a RPC `get_cashflow` (estendida no PLANO 60-01). Com OFF, o caixa exibido bate com a DFC/Tiny do Wesley (sem ordens de compra não faturadas); com ON, o lojista vê a visão "e se eu já comprometer as compras".

Purpose: Decisão LOCKED do Wesley — controlar a inclusão das "Previsões de compra" na própria UI. O default `false` mantém a página exatamente como está hoje, então a mudança é aditiva e não-disruptiva.

Output: `useCashFlowData` ganha um parâmetro booleano (default false) que entra na queryKey e no payload do RPC; `MLFluxoCaixa` ganha estado local + Switch shadcn na aba "Caixa Real", acima do gráfico. Push → Vercel auto-deploy.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-alinhamento-da-dfc-fluxo-de-caixa/60-CONTEXT.md

# Hook que chama a RPC (adicionar o 4º param)
@src/hooks/useCashFlowData.ts

# Página /fluxo-de-caixa que consome o hook (adicionar estado + Switch)
@src/pages/mercadolivre/MLFluxoCaixa.tsx

# Padrões do projeto (Switch shadcn, TanStack Query, design tokens)
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useCashFlowData aceita includePurchaseForecasts e propaga p_include_purchase_forecasts no RPC</name>
  <files>src/hooks/useCashFlowData.ts</files>
  <read_first>
    - src/hooks/useCashFlowData.ts (assinatura atual: useCashFlowData(startDate, endDate); chama rpc('get_cashflow', { p_org_id, p_start_date, p_end_date }))
    - 60-CONTEXT.md seção `<decisions>` FRONTEND (toggle off por padrão; propaga 4º arg)
  </read_first>
  <action>
    Estender a assinatura para `useCashFlowData(startDate: string, endDate: string, includePurchaseForecasts: boolean = false)`. Adicionar `includePurchaseForecasts` à `queryKey` (ex.: `["cashflow", "series", orgId, startDate, endDate, includePurchaseForecasts]`) para que alternar o toggle dispare refetch. Na chamada `supabase.rpc("get_cashflow", {...})`, adicionar `p_include_purchase_forecasts: includePurchaseForecasts` ao objeto de parâmetros (per CASHFIX-06). Não mudar o shape de retorno nem o mapeamento das linhas — apenas o input. O default `false` mantém o comportamento atual para qualquer chamador que não passe o argumento.
  </action>
  <verify>
    <automated>grep -q "includePurchaseForecasts" src/hooks/useCashFlowData.ts && grep -q "p_include_purchase_forecasts" src/hooks/useCashFlowData.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `useCashFlowData` aceita `includePurchaseForecasts` com default `false`.
    - O argumento entra na `queryKey` (refetch ao alternar) e no payload `p_include_purchase_forecasts` do `rpc('get_cashflow')`.
    - Shape de retorno (CashFlowDataPoint) inalterado; `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>Hook propaga o 4º param; build de tipos verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Switch "Incluir previsões de compra" (off por padrão) na página /fluxo-de-caixa</name>
  <files>src/pages/mercadolivre/MLFluxoCaixa.tsx</files>
  <read_first>
    - src/pages/mercadolivre/MLFluxoCaixa.tsx (aba "Caixa Real"; chama useCashFlowData(startDate, endDate); gráfico CashFlowChart logo abaixo do TreasuryPanel)
    - src/components/ui/switch.tsx (componente Switch shadcn já presente)
    - CLAUDE.md (Switch shadcn, design tokens, Label)
  </read_first>
  <action>
    Adicionar estado local `const [includePurchaseForecasts, setIncludePurchaseForecasts] = useState(false)` na página. Passar o estado para o hook: `useCashFlowData(startDate, endDate, includePurchaseForecasts)`. Renderizar um `Switch` do shadcn (import de `@/components/ui/switch`) com `Label` "Incluir previsões de compra", desligado por padrão, na aba "Caixa Real" — posicionar acima do `CashFlowChart` (ex.: numa linha à direita, junto ou abaixo do botão "Ajustar saldo de hoje"), usando os design tokens/utilitários Tailwind do projeto (texto `text-xs text-muted-foreground`, espaçamento consistente com o restante da página). Adicionar um `tooltip`/texto curto opcional explicando que inclui ordens de compra ainda não faturadas (discrição de UI dentro do padrão shadcn). Não alterar a aba "Simulador" nem o restante do layout. Com OFF (estado inicial) a página fica idêntica à atual.
  </action>
  <verify>
    <automated>grep -q "includePurchaseForecasts" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "@/components/ui/switch" src/pages/mercadolivre/MLFluxoCaixa.tsx && grep -q "Incluir previsões de compra" src/pages/mercadolivre/MLFluxoCaixa.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Estado local `includePurchaseForecasts` (default `false`) na página, passado a `useCashFlowData`.
    - `Switch` shadcn (import de `@/components/ui/switch`) com label "Incluir previsões de compra", desligado por padrão, na aba "Caixa Real" acima do gráfico.
    - Aba "Simulador" e layout existente inalterados; `npm run build` verde.
  </acceptance_criteria>
  <done>Toggle renderizado (off por padrão), propagado ao hook; build de produção verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Push → Vercel + validação visual do toggle na /fluxo-de-caixa (Wesley)</name>
  <action>Checkpoint do ORQUESTRADOR. Confirmar que a migration do 60-01 já está aplicada em prod (pré-requisito do RPC de 4 args), fazer push da branch → Vercel auto-deploy, e obter aprovação visual do Wesley conforme os passos abaixo. Validação final de alinhamento com a DFC é do Wesley (pendência aceita explícita).</action>
  <what-built>Toggle "Incluir previsões de compra" na página /fluxo-de-caixa (off por padrão) propagando o 4º arg da get_cashflow.</what-built>
  <how-to-verify>
    Pré-requisito: a migration do PLANO 60-01 já deve estar aplicada em produção (senão o RPC de 4 args falha). O orquestrador faz push da branch → Vercel auto-deploy. Então, logado como Wesley:
    1. Acessar `/fluxo-de-caixa`, aba "Caixa Real". O toggle "Incluir previsões de compra" aparece DESLIGADO.
    2. Com OFF: a curva do gráfico deve refletir as saídas reconciliadas com a DFC/Tiny (vira negativa por volta de 11/07, conforme tabela de referência do 60-CONTEXT — 11/07 ≈ −5.892, 15/07 ≈ −27.836).
    3. Ligar o toggle: a curva deve mudar ao vivo (saídas maiores, inclui OC 383 09/07 + OC 410 10/07 = +R$12.389,79 no período).
    4. Validação final de alinhamento com a DFC é do Wesley (pendência aceita explícita).
  </how-to-verify>
  <resume-signal>Digite "approved" se o toggle alterna a curva ao vivo e a curva OFF bate com a DFC do Wesley, ou descreva a divergência.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → RPC get_cashflow | o front passa um booleano controlado pelo usuário como 4º arg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering | p_include_purchase_forecasts vindo do cliente | accept | É só um filtro de visualização sobre dados já escopados por org (RLS/SECURITY INVOKER no backend); nenhum dado novo é exposto — alternar o booleano só inclui/exclui linhas que o usuário já pode ver |
| T-60-05 | Information Disclosure | dados de caixa de outra org | mitigate | Isolamento garantido no backend (60-01: SECURITY INVOKER + RLS por organization_id); o front só passa org da sessão via useOrganization |
</threat_model>

<verification>
- `grep` confirma `includePurchaseForecasts` no hook e na página, import do Switch e a label.
- `npx tsc --noEmit` (Task 1) e `npm run build` (Task 2) verdes.
- Checkpoint: toggle alterna a curva ao vivo; curva OFF reconciliada com a DFC (validação visual do Wesley — pendência aceita).
</verification>

<success_criteria>
- Toggle off-por-padrão na /fluxo-de-caixa propaga o 4º arg; UX atual intacta com OFF; build de produção verde; curva muda ao alternar.
</success_criteria>

<artifacts_this_phase_produces>
- **Estado do toggle:** `includePurchaseForecasts` (useState, default `false`) em `src/pages/mercadolivre/MLFluxoCaixa.tsx`.
- **Componente:** `Switch` shadcn "Incluir previsões de compra" na aba "Caixa Real".
- **Hook atualizado:** `useCashFlowData(startDate, endDate, includePurchaseForecasts=false)` em `src/hooks/useCashFlowData.ts` — inclui o param na queryKey e em `p_include_purchase_forecasts` do RPC.
- Consome a assinatura de 4 args criada no PLANO 60-01.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/60-alinhamento-da-dfc-fluxo-de-caixa/60-02-SUMMARY.md` when done
</output>