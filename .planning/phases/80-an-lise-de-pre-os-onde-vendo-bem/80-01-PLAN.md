---
phase: 80-an-lise-de-pre-os-onde-vendo-bem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/precoFaixas.ts
  - src/lib/precoFaixas.test.ts
autonomous: true
requirements: ["APF-UTIL", "APF-VEREDICTO"]

must_haves:
  truths:
    - "computePrecoFaixas reagrupa POR FAIXA DE PREÇO os pontos diários já reconciliados de computePrecoMcoSeries (consome McoSeriesPoint; não recalcula custo/imposto/comissão)"
    - "niceStep snapa a largura de bucket para a série 1/2/5 × 10^n e nunca retorna 0 ou negativo"
    - "a bucketização centra em ~90% das vendas (p05–p95 ponderados por unidades) e agrega outliers de preço alto numa única barra +R$X (sem cascata de faixas vazias)"
    - "faixaOtima é a faixa de maior altura conforme o modo: 'unidades' = mais unidades, 'lucro' = maior MCO R$ total"
    - "a faixa que contém o preço recente (ponto de bucket/data máximo) é marcada isPrecoAtual e sua margem vira margemRecentePct"
    - "computeVeredicto classifica saúde por MCO_SAUDAVEL_PCT (prejuízo <0, apertada [0,thr), saudável ≥thr) e a frase ótima acompanha o modo (unidades cita volume; lucro cita R$)"
    - "entrada vazia ou sem variação de preço degrada com transparência, nunca quebra (faixas=[], faixaOtima=null)"
  artifacts:
    - path: "src/lib/precoFaixas.ts"
      provides: "Util puro: bucketização por faixa de preço + veredito determinístico"
      exports: ["niceStep", "computePrecoFaixas", "classificarSaude", "computeVeredicto", "MCO_SAUDAVEL_PCT", "FaixaMode", "FaixaPreco", "FaixasResult", "Veredicto"]
      min_lines: 120
    - path: "src/lib/precoFaixas.test.ts"
      provides: "Testes vitest do util (niceStep, agregação por faixa, faixaOtima por modo, outlier, preço recente, veredito, defensivo)"
      min_lines: 80
  key_links:
    - from: "src/lib/precoFaixas.ts"
      to: "src/lib/precoMcoSeries.ts"
      via: "import type { McoSeriesPoint } — consome os pontos diários reconciliados; NÃO duplica o cálculo de MCO"
      pattern: "McoSeriesPoint"
---

# Plano 80-01 — Util `precoFaixas.ts` (bucketização por faixa + veredito)

> Código completo, passo a passo (TDD), em:
> `docs/superpowers/plans/2026-07-02-analise-precos-onde-vendo-bem.md` → **Task 1** e **Task 2**.
> Este plano GSD embute os mesmos passos. Siga o TDD: teste → falha → implementação → passa → commit.

## Constraints globais
- Sem novas dependências. Util 100% puro (sem React, sem I/O).
- Consome `McoSeriesPoint` de `src/lib/precoMcoSeries.ts` (entrada = pontos **diários**). NÃO recalcula custo/imposto — só reagrupa por preço.
- Veredito determinístico (sem LLM). `MCO_SAUDAVEL_PCT` = constante nomeada (default 5).
- Named exports; `camelCase` helpers; interfaces `PascalCase`.

## Task 1: bucketização e agregação por faixa

**Files:** Create `src/lib/precoFaixas.ts`, `src/lib/precoFaixas.test.ts`

- [ ] **Step 1:** Escrever teste de `niceStep` (ver plano doc Task 1 Step 1).
- [ ] **Step 2:** Rodar e ver falhar — `npx vitest run src/lib/precoFaixas.test.ts`.
- [ ] **Step 3:** Implementar `niceStep` + esqueleto do módulo (plano doc Task 1 Step 3).
- [ ] **Step 4:** Rodar e ver passar.
- [ ] **Step 5:** Escrever testes de `computePrecoFaixas` — agregação por faixa, `faixaOtima` por modo, outlier agregado, preço recente, entrada vazia (plano doc Task 1 Step 5).
- [ ] **Step 6:** Rodar e ver falhar.
- [ ] **Step 7:** Implementar `computePrecoFaixas` + interfaces `FaixaPreco`/`FaixasResult`/`FaixaMode` + `weightedPercentile` (plano doc Task 1 Step 7).
- [ ] **Step 8:** Rodar e ver passar.
- [ ] **Step 9:** Commit — `feat(analise-precos): util precoFaixas — reagrupa pontos diarios por faixa de preco`.

## Task 2: veredito determinístico

**Files:** Modify `src/lib/precoFaixas.ts`, `src/lib/precoFaixas.test.ts`

- [ ] **Step 1:** Escrever testes de `classificarSaude` e `computeVeredicto` (plano doc Task 2 Step 1).
- [ ] **Step 2:** Rodar e ver falhar.
- [ ] **Step 3:** Implementar `MCO_SAUDAVEL_PCT`, `classificarSaude`, `computeVeredicto` + tipos `SaudePreco`/`Veredicto` (plano doc Task 2 Step 3).
- [ ] **Step 4:** Rodar e ver passar — `npx vitest run src/lib/precoFaixas.test.ts`.
- [ ] **Step 5:** Commit — `feat(analise-precos): veredito deterministico de preco (saude + faixa otima)`.

## Self-check
- [ ] `npx tsc --noEmit` limpo.
- [ ] `npx vitest run src/lib/precoFaixas.test.ts` verde.
- [ ] Exports batem com o frontmatter (`niceStep`, `computePrecoFaixas`, `classificarSaude`, `computeVeredicto`, `MCO_SAUDAVEL_PCT`, tipos).
