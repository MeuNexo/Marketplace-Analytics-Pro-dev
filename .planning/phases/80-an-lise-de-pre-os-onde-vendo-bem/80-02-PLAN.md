---
phase: 80-an-lise-de-pre-os-onde-vendo-bem
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx
  - src/index.css
autonomous: true
requirements: ["APF-UI", "APF-KPIS", "APF-TEMPORAL", "APF-CVD"]

must_haves:
  truths:
    - "a visão principal de /analise-precos passa a ser um BarChart por faixa de preço (eixo X = faixas), não mais a série temporal"
    - "existe um toggle Unidades↔Lucro R$ que troca a altura das barras (unidades vs MCO R$ total) e o texto do veredito acompanha o modo"
    - "cada barra tem cor de margem (verde/âmbar/vermelho por classificarSaude) E rótulo de margem % no topo — cor nunca é o único sinal"
    - "a faixa do preço recente é destacada e há um cartão-veredito (2 frases) acima do gráfico"
    - "os KPIs são reduzidos a 4 (Preço recente · Margem recente % · Faixa campeã · Unidades no período) mantendo o comparativo vs período anterior onde há delta"
    - "o gráfico de linha temporal atual (Phase 79) é PRESERVADO dentro de uma aba/accordion secundária recolhida 'Evolução no tempo' — o fetch rows+granularity existente continua servindo só essa aba"
    - "o histograma consome pontos DIÁRIOS: novo fetch da RPC com _granularity='day' independente do toggle de granularidade"
    - "a paleta das barras é validada CVD-safe (light+dark) antes de fechar"
  artifacts:
    - path: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      provides: "Tela refeita: veredito + histograma de faixas com toggle + 4 KPIs + aba temporal secundária"
      contains: "computePrecoFaixas"
      min_lines: 300
  key_links:
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/lib/precoFaixas.ts"
      via: "import { computePrecoFaixas, computeVeredicto, classificarSaude } — usa o util do 80-01"
      pattern: "computePrecoFaixas"
    - from: "src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx"
      to: "src/lib/precoMcoSeries.ts"
      via: "import { computePrecoMcoSeries } — gera os pontos diários que alimentam as faixas"
      pattern: "computePrecoMcoSeries"
---

# Plano 80-02 — Componente: histograma de faixas + toggle + KPIs + aba temporal

> Passo a passo detalhado em:
> `docs/superpowers/plans/2026-07-02-analise-precos-onde-vendo-bem.md` → **Task 3, Task 4, Task 5**.

## Constraints globais
- Sem novas dependências (recharts/shadcn/Tailwind já presentes).
- NÃO tocar na RPC nem em `computeMco`/`computePrecoMcoSeries`. O histograma consome os pontos diários que já saem reconciliados ao centavo.
- Fonte de dados reconciliada: a aba secundária "Evolução no tempo" preserva o gráfico da Phase 79 intacto.
- Acessibilidade: rótulo de margem % em TODA barra; paleta CVD-safe (skill `dataviz`, light+dark).

## Task 3: histograma de faixas com toggle + veredito

**Files:** Modify `src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx`

- [ ] **Step 1:** Adicionar fetch diário dedicado (`dailyRows`, `_granularity:"day"`, deps sem `granularity`) — plano doc Task 3 Step 1.
- [ ] **Step 2:** Derivar `dailyPoints` (computePrecoMcoSeries day) → `faixasResult` (computePrecoFaixas) → `veredicto` (computeVeredicto), com estado `faixaMode` — plano doc Task 3 Step 2.
- [ ] **Step 3:** Renderizar cartão-veredito (2 frases, realce de cor por `veredicto.saude`) acima do gráfico — plano doc Task 3 Step 3.
- [ ] **Step 4:** Substituir a visão principal por `ToggleGroup` (Unidades/Lucro R$) + `BarChart` de faixas: `Cell` colorido por `classificarSaude(f.mcoPctMedio)`, `LabelList` de margem %, destaque da faixa `isPrecoAtual`, tooltip custom (preço médio, unidades, margem %, MCO R$, receita) — plano doc Task 3 Step 4.
- [ ] **Step 5:** `npx tsc --noEmit && npx vitest run && npm run build` — tudo verde.
- [ ] **Step 6:** Commit — `feat(analise-precos): histograma de faixas de preco com toggle unidades/lucro + veredito`.

## Task 4: 4 KPIs + aba secundária "Evolução no tempo"

**Files:** Modify `src/components/mercadolivre/anuncios/PrecoPraticadoReport.tsx`

- [ ] **Step 1:** Reduzir a grade de KPIs para 4 (Preço recente · Margem recente % · Faixa campeã · Unidades no período) mantendo deltas onde existem — plano doc Task 4 Step 1.
- [ ] **Step 2:** Mover o `ComposedChart` temporal atual para um `Accordion`/`Tabs` recolhido "Evolução no tempo"; o toggle de granularidade e o fetch `rows` passam a servir só essa aba (intactos) — plano doc Task 4 Step 2.
- [ ] **Step 3:** Atualizar o rodapé de transparência para a nova visão (faixas, toggle, cor=margem, outliers "+R$X") mantendo avisos de custo/imposto ausente — plano doc Task 4 Step 3.
- [ ] **Step 4:** `npx tsc --noEmit && npx vitest run && npm run build`.
- [ ] **Step 5:** Commit — `feat(analise-precos): 4 KPIs focados + evolucao no tempo como aba secundaria`.

## Task 5: cores acessíveis (CVD) + verificação

**Files:** Modify `src/index.css` (se faltar token), `PrecoPraticadoReport.tsx` (ajuste fino)

- [ ] **Step 1:** Validar a paleta verde/âmbar/vermelho no validador CVD/contraste da skill `dataviz` (light+dark); ajustar tokens até PASS — plano doc Task 5 Step 1.
- [ ] **Step 2:** Confirmar `LabelList` de margem % em toda barra (inclui outlier).
- [ ] **Step 3:** Verificação end-to-end com a skill `verify` em `/analise-precos` (anúncio com variação, ex. MLB4113792113): veredito, toggle Unidades↔Lucro, aba temporal, light+dark.
- [ ] **Step 4:** `npx tsc --noEmit && npx vitest run && npm run build` finais.
- [ ] **Step 5:** Commit — `style(analise-precos): valida paleta CVD-safe das barras de margem (light+dark)`.

## Self-check
- [ ] Visão principal = faixas; temporal preservada em aba secundária.
- [ ] Toggle troca altura + veredito; cor + rótulo % em toda barra.
- [ ] 4 KPIs com comparativo.
- [ ] Suíte verde; build ok; paleta CVD PASS light+dark.
