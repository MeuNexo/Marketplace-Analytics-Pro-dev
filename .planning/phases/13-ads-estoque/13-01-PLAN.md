---
phase: 13-ads-estoque
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/ml-ads/index.ts
autonomous: true

must_haves:
  truths:
    - ml-ads aceita ml_user_id via POST body (não apenas URL params)
    - Função deployada no projeto dev ckcdevcxgvueywivefgx
    - ml_ads_daily_cache tem linhas para ml_user_id=1639558873 após sync
    - ml_ads_campaigns_cache tem linhas para ml_user_id=1639558873
  artifacts:
    - ml-ads v2 deployado
    - Dados reais em ml_ads_daily_cache
  key_links:
    - useMLAds.syncNow() → supabase.functions.invoke("ml-ads", {body:{ml_user_id,force:true}}) → ml-ads POST body bug → 400 error
    - Fix: ml-ads deve ler ml_user_id de URL params OU de POST body
---

<objective>
Corrigir bug no ml-ads edge function onde ml_user_id é lido apenas de URL params,
mas o frontend envia via POST body. Após fix, deployar e disparar o primeiro sync
de publicidade da Pé Vermeio.

Bug: useMLAds.syncNow() chama supabase.functions.invoke("ml-ads", {body:{ml_user_id,force:true}})
mas a função lê: url.searchParams.get("ml_user_id") — body não é lido → 400 error.

Purpose: Habilitar sync de ads e popular ml_ads_daily_cache, ml_ads_campaigns_cache, ml_ads_products_cache.
Output: Tabelas de ads populadas com dados reais da Pé Vermeio.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix ml-ads — ler params do POST body quando ausentes na URL</name>
  <action>
    Editar supabase/functions/ml-ads/index.ts:

    Na função serve(), após parsear a URL e antes do `if (!mlUserId)`:
    - Se req.method === "POST", tentar req.json() para ler body
    - Usar body.ml_user_id como fallback quando url.searchParams.get("ml_user_id") for null
    - Mesmo para force, date_from, date_to

    Mudança cirúrgica no bloco de leitura de params (linhas ~327-333):

    DE:
    ```typescript
    const url      = new URL(req.url);
    const mlUserId = url.searchParams.get("ml_user_id");
    const dateFrom = url.searchParams.get("date_from") ?? subDaysStr(29);
    const dateTo   = url.searchParams.get("date_to")   ?? todayStr();
    const force    = url.searchParams.get("force") === "true";
    ```

    PARA:
    ```typescript
    const url  = new URL(req.url);
    let body: Record<string, unknown> = {};
    if (req.method !== "GET") {
      try { body = await req.json(); } catch { /* ignore */ }
    }
    const mlUserId = (url.searchParams.get("ml_user_id") ?? String(body.ml_user_id ?? "")) || null;
    const dateFrom = url.searchParams.get("date_from") ?? String(body.date_from ?? subDaysStr(29));
    const dateTo   = url.searchParams.get("date_to")   ?? String(body.date_to   ?? todayStr());
    const force    = url.searchParams.get("force") === "true" || body.force === true;
    ```

    Garantir que mlUserId seja null (não "") quando não informado, para o check `if (!mlUserId)` funcionar.
  </action>
  <done>Arquivo salvo com a correção</done>
</task>

<task type="auto">
  <name>Task 2: Deploy ml-ads no projeto dev</name>
  <action>
    Usar mcp__claude_ai_Supabase__deploy_edge_function para deployar ml-ads
    no projeto ckcdevcxgvueywivefgx.
  </action>
  <done>Deploy confirmado sem erro</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Disparar sync de publicidade pelo browser</name>
  <action>
    VERIFICAÇÃO MANUAL — sync requer sessão autenticada no browser.

    1. Acesse https://marketplace-analytics-pro-dev.vercel.app/publicidade
    2. Faça login como wesleysantos@pevermeio.com se necessário
    3. Clique no botão "Sincronizar ML" (ícone de reload com label)
    4. Aguarde até 60 segundos — um spinner deve aparecer

    Se aparecer erro no toast, informe a mensagem.
    Se funcionar, informe quantas campanhas aparecem (se houver).

    Nota: a Pé Vermeio pode não ter campanhas ativas de PADS.
    Nesse caso a tela mostrará "Nenhum produto patrocinado encontrado."
    O que importa é que o sync completou sem erro 400.
  </action>
  <verify>
    Após confirmação do usuário, verificar no banco:
    ```sql
    SELECT COUNT(*) as rows, MAX(synced_at) as last_sync
    FROM ml_ads_daily_cache
    WHERE ml_user_id = '1639558873';
    ```
    Resultado esperado: rows >= 0 (pode ser 0 se conta sem PADS), sem erro 400.
  </verify>
  <done>
    Sync completou sem erro — com ou sem dados (PADS pode não estar ativo).
    ml_ads_daily_cache verificado.
  </done>
</task>

</tasks>

<verification>
1. ml-ads aceita POST body com ml_user_id
2. Deploy confirmado
3. Sync disparado pelo browser sem erro 400
4. ml_ads_daily_cache verificado
</verification>

<success_criteria>
- ADS-01: ml-ads processou requisição via POST body sem retornar 400
- ADS-02: Cache de ads verificado (com ou sem dados — Pé Vermeio pode não ter PADS ativo)
</success_criteria>

<output>
Criar .planning/phases/13-ads-estoque/13-01-SUMMARY.md
</output>
