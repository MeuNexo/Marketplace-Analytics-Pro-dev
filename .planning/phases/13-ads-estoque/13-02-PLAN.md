---
phase: 13-ads-estoque
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - ml_inventory_cache tem itens para ml_user_id=1639558873 (count > 0)
    - /estoque exibe produtos reais com título, preço e status
    - /anuncios exibe anúncios reais da Pé Vermeio
    - ADS-03 e INV-01 a INV-03 satisfeitos
  artifacts:
    - Linhas em ml_inventory_cache com ml_user_id=1639558873
  key_links:
    - MLInventoryContext.syncNow() → sync-ml-inventory → ml_inventory_cache → MLEstoque/MLAnuncios
---

<objective>
Disparar o primeiro sync de estoque/inventário da Pé Vermeio e verificar que
/estoque e /anuncios exibem produtos reais.

O sync é acionado manualmente pelo usuário clicando o botão na página /estoque.
A edge function sync-ml-inventory busca todos os items active+paused da API ML
e salva em ml_inventory_cache.

Purpose: Validar INV-01, INV-02, INV-03 do milestone v4.0.
Output: ml_inventory_cache populado + telas /estoque e /anuncios com dados reais.
</objective>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Disparar sync de estoque pelo browser</name>
  <action>
    VERIFICAÇÃO MANUAL — sync requer sessão autenticada.

    1. Acesse https://marketplace-analytics-pro-dev.vercel.app/estoque
    2. Certifique-se que a loja "Pé Vermeio" está selecionada no header
    3. Clique no botão de sync (ícone de reload / "Sincronizar")
    4. Aguarde — pode levar 30-60 segundos (busca 81+ anúncios da API ML)

    Informe: quantos itens aparecem? Há produtos com título e preço visíveis?
    Se der erro no toast, copie a mensagem.
  </action>
  <verify>
    ```sql
    SELECT COUNT(*) as total_itens,
           SUM(available_quantity) as estoque_total,
           COUNT(CASE WHEN status = 'active' THEN 1 END) as ativos
    FROM ml_inventory_cache
    WHERE ml_user_id = '1639558873';
    ```
    Esperado: total_itens > 0 (a Pé Vermeio tem 81 anúncios confirmados na Phase 2).
  </verify>
  <done>
    ml_inventory_cache tem itens para 1639558873.
    /estoque mostra produtos reais.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verificar /anuncios com dados reais</name>
  <action>
    VERIFICAÇÃO MANUAL

    1. Acesse https://marketplace-analytics-pro-dev.vercel.app/anuncios
    2. Verifique que a tabela/lista mostra produtos reais com título e preço
    3. Confirme que não está vazia ou mostrando "nenhum item"

    Informe: quantos produtos aparecem em /anuncios?
  </action>
  <done>
    /anuncios exibe produtos reais da Pé Vermeio.
  </done>
</task>

</tasks>

<verification>
1. ml_inventory_cache tem count > 0 para ml_user_id=1639558873
2. /estoque mostra produtos com título, preço e status reais
3. /anuncios mostra anúncios reais
</verification>

<success_criteria>
- INV-01: sync-ml-inventory populou ml_inventory_cache com itens da Pé Vermeio
- INV-02: /estoque exibe produtos reais (não vazio)
- INV-03: /anuncios exibe anúncios reais
- ADS-03: /publicidade mostra dados reais (ou estado correto quando PADS não ativo)
</success_criteria>

<output>
Criar .planning/phases/13-ads-estoque/13-02-SUMMARY.md com:
- Total de itens em ml_inventory_cache
- Status de ADS-01 a ADS-03 e INV-01 a INV-03
</output>
