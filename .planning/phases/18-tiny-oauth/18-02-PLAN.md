---
phase: 18-tiny-oauth
plan: "02"
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/pages/Integrations.tsx
  - supabase/functions/sync-tiny-costs/index.ts
autonomous: true
requirements:
  - TINY-OAUTH-02

must_haves:
  truths:
    - "TinyIntegrationSection mostra botão 'Conectar Tiny ERP' (igual ao ML) quando desconectado"
    - "TinyIntegrationSection mostra status 'Conectado' + botão 'Sincronizar Custos' quando conectado"
    - "handleTinyOAuthCallback() detectado via state=tiny-* no ?code= callback"
    - "sync-tiny-costs usa tiny_access_token armazenado + refresh via tiny-oauth, sem client_credentials"
    - "Zero erros TypeScript"
  artifacts:
    - path: "src/pages/Integrations.tsx"
      provides: "Tiny ERP OAuth connect flow + sync button"
    - path: "supabase/functions/sync-tiny-costs/index.ts"
      provides: "CMV sync usando tokens OAuth armazenados"
---

<objective>
Atualizar a UI de integração do Tiny ERP para usar o fluxo OAuth completo (igual ao ML), e atualizar sync-tiny-costs para usar os tokens armazenados via OAuth em vez de client_credentials.
</objective>

<context>
@.planning/phases/18-tiny-oauth/18-CONTEXT.md

Referência de UX: como a integração ML funciona em src/pages/Integrations.tsx
- Botão "Conectar" → chama ml-oauth get_auth_url → redireciona para provider
- Callback detectado via ?code= na URL ao retornar
- useEffect monitora searchParams.get("code") + verifica se é ML (sem state prefixo) ou Tiny (state=tiny-*)

Referência de sync: supabase/functions/sync-tiny-costs/index.ts
- getTinyToken() atual usa client_credentials → substituir por token em banco
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar TinyIntegrationSection em Integrations.tsx</name>
  <files>src/pages/Integrations.tsx</files>
  <action>
Ler o arquivo completo src/pages/Integrations.tsx.

**Objetivo:** Transformar `TinyIntegrationSection` de "só sync" para "conectar OAuth + sync" igual ao ML.

**Mudanças na TinyIntegrationSection:**

1. Adicionar estado de conexão lendo ml_tokens do Supabase:
```typescript
// No início da função TinyIntegrationSection, adicionar:
const [tinyConnected, setTinyConnected] = useState(false);
const [tinyConnecting, setTinyConnecting] = useState(false);
const [tinyOAuthStep, setTinyOAuthStep] = useState<"idle" | "redirecting" | "verifying" | "saving" | "done" | "error">("idle");

// Verificar conexão existente ao montar
useEffect(() => {
  if (!effectiveMlUserId) return;
  supabase
    .from("ml_tokens")
    .select("tiny_access_token")
    .eq("ml_user_id", effectiveMlUserId)
    .maybeSingle()
    .then(({ data }) => setTinyConnected(!!data?.tiny_access_token));
}, [effectiveMlUserId]);
```

2. Adicionar handleTinyConnect (inicia OAuth):
```typescript
const handleTinyConnect = async () => {
  if (!effectiveMlUserId) {
    toast({ title: "Selecione uma loja ML primeiro", variant: "destructive" });
    return;
  }
  setTinyConnecting(true);
  setTinyOAuthStep("redirecting");
  try {
    // Salva ml_user_id para usar após o redirect
    localStorage.setItem("tiny_oauth_ml_user_id", effectiveMlUserId);

    const redirectUri = `${window.location.origin}/integracoes`;
    const { data, error } = await supabase.functions.invoke("tiny-oauth", {
      body: { action: "get_auth_url", redirect_uri: redirectUri },
    });
    if (error || !data?.auth_url) throw error || new Error("Sem URL de autorização");

    // Salva state para validar no retorno
    localStorage.setItem("tiny_oauth_state", data.state);
    window.location.href = data.auth_url;
  } catch (err: unknown) {
    const message = err instanceof Error ? err.message : String(err);
    toast({ title: "Erro ao iniciar conexão Tiny", description: message, variant: "destructive" });
    setTinyConnecting(false);
    setTinyOAuthStep("idle");
  }
};
```

3. Adicionar handleTinyDisconnect:
```typescript
const handleTinyDisconnect = async () => {
  if (!effectiveMlUserId) return;
  await supabase.functions.invoke("tiny-oauth", {
    body: { action: "disconnect", ml_user_id: effectiveMlUserId },
  });
  setTinyConnected(false);
  toast({ title: "Tiny ERP desconectado" });
};
```

4. No componente pai (Integrations), no useEffect que monitora `?code=`, adicionar detecção do callback Tiny:
```typescript
// Localizar o useEffect que detecta ?code= em Integrations (onde está handleMlOAuthCallback)
// ADICIONAR após a lógica ML:

const state = searchParams.get("state");
const code = searchParams.get("code");

if (code && state?.startsWith("tiny-")) {
  // É callback do Tiny OAuth
  handleTinyOAuthCallback(code, state);
}
```

5. Adicionar handleTinyOAuthCallback na função Integrations:
```typescript
const handleTinyOAuthCallback = async (code: string, state: string) => {
  // Validar state
  const savedState = localStorage.getItem("tiny_oauth_state");
  localStorage.removeItem("tiny_oauth_state");
  const mlUserId = localStorage.getItem("tiny_oauth_ml_user_id");
  localStorage.removeItem("tiny_oauth_ml_user_id");

  if (!savedState || savedState !== state) {
    toast({ title: "Erro de segurança", description: "State OAuth inválido.", variant: "destructive" });
    return;
  }
  if (!mlUserId) {
    toast({ title: "Erro", description: "ml_user_id não encontrado no retorno OAuth.", variant: "destructive" });
    return;
  }

  // Trocar code por token
  const redirectUri = `${window.location.origin}/integracoes`;
  const { data, error } = await supabase.functions.invoke("tiny-oauth", {
    body: { action: "exchange_code", code, redirect_uri: redirectUri, ml_user_id: mlUserId },
  });

  if (error || !data?.success) {
    toast({ title: "Erro ao conectar Tiny ERP", description: data?.error || error?.message, variant: "destructive" });
    return;
  }

  toast({ title: "Tiny ERP conectado!", description: "Agora você pode sincronizar os custos." });
  // Remove code/state da URL
  window.history.replaceState({}, "", "/integracoes");
};
```

**UI da TinyIntegrationSection — substituir o CardContent atual:**

Quando desconectado (`!tinyConnected`):
- Badge "Não conectado" (vermelho/outline)
- Botão "Conectar Tiny ERP" com ícone Link2 → chama handleTinyConnect

Quando conectado (`tinyConnected`):
- Badge "Conectado" (verde)
- Seção de sync existente (store selector + botão "Sincronizar Custos")
- Botão "Desconectar" (ghost, pequeno) → handleTinyDisconnect

**ATENÇÃO:** `handleTinyOAuthCallback` precisa estar no escopo da função `Integrations` (não dentro de `TinyIntegrationSection`), pois precisa ser registrado no useEffect que monitora `searchParams`. Passar `handleTinyOAuthCallback` como prop para `TinyIntegrationSection` OU registrar o estado `tinyOAuthConnecting` globalmente.

A solução mais limpa: elevar o estado de conexão Tiny para o componente pai `Integrations` e passar via props para `TinyIntegrationSection`. Semelhante a como `mlOAuthConnecting`/`mlOAuthStep` estão no componente pai.
  </action>
  <verify>
    <automated>cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep "Integrations\|TinyIntegration" | head -10</automated>
  </verify>
  <done>
- Badge de status (conectado/desconectado) visível
- Botão "Conectar Tiny ERP" quando desconectado
- Botão "Sincronizar Custos" + "Desconectar" quando conectado
- handleTinyOAuthCallback registrado no useEffect do ?code= callback
- State validation usando localStorage
- Zero erros TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar sync-tiny-costs para usar tokens OAuth armazenados</name>
  <files>supabase/functions/sync-tiny-costs/index.ts</files>
  <action>
Ler supabase/functions/sync-tiny-costs/index.ts completo.

**Objetivo:** Substituir `getTinyToken()` que usa `client_credentials` por leitura do `tiny_access_token` armazenado via OAuth, com refresh automático via `tiny-oauth`.

**Substituir a função getTinyToken() atual por:**

```typescript
async function getTinyToken(mlUserId: string): Promise<string> {
  const { data: tok, error } = await sb
    .from("ml_tokens")
    .select("tiny_access_token, tiny_refresh_token, tiny_expires_at")
    .eq("ml_user_id", mlUserId)
    .maybeSingle();

  if (error || !tok) {
    throw new Error(`Conta ML ${mlUserId} não encontrada em ml_tokens`);
  }

  if (!tok.tiny_access_token) {
    throw new Error(`Tiny ERP não conectado para a loja ${mlUserId}. Conecte em /integracoes.`);
  }

  const now = Math.floor(Date.now() / 1000);
  // Token válido por mais de 5 minutos
  if (tok.tiny_expires_at && tok.tiny_expires_at - now > 300) {
    return tok.tiny_access_token;
  }

  // Precisa renovar
  if (!tok.tiny_refresh_token) {
    throw new Error(`Token Tiny expirado e sem refresh_token para ${mlUserId}. Reconecte em /integracoes.`);
  }

  // Chamar tiny-oauth refresh_token
  const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;
  const refreshResp = await fetch(`${SUPABASE_URL}/functions/v1/tiny-oauth`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "Authorization": `Bearer ${Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")}`,
    },
    body: JSON.stringify({
      action: "refresh_token",
      refresh_token: tok.tiny_refresh_token,
      ml_user_id: mlUserId,
    }),
  });

  const refreshData = await refreshResp.json();
  if (!refreshResp.ok || !refreshData.success) {
    throw new Error(`Falha ao renovar token Tiny: ${refreshData.error || "desconhecido"}`);
  }

  return refreshData.access_token;
}
```

**Remover** as linhas de `TINY_CLIENT_ID` e `TINY_CLIENT_SECRET` do topo do arquivo (não são mais necessárias).

Manter o restante da função inalterado (fetchAllProducts, loop de upsert, rate limiting).

Deploiar a edge function atualizada no Supabase ckcdevcxgvueywivefgx.
  </action>
  <verify>
    <automated>grep -c "client_credentials" /root/garment-glow-test/supabase/functions/sync-tiny-costs/index.ts || echo "0 (OK: client_credentials removido)"</automated>
  </verify>
  <done>
- getTinyToken() lê tiny_access_token de ml_tokens
- Refresh automático via tiny-oauth quando expirado
- TINY_CLIENT_ID / TINY_CLIENT_SECRET removidos
- Edge function re-deployada
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript limpo
cd /root/garment-glow-test && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -5

# client_credentials removido do sync
grep "client_credentials" /root/garment-glow-test/supabase/functions/sync-tiny-costs/index.ts && echo "ERRO: ainda presente" || echo "OK: removido"

# Callback Tiny detectado na Integrations
grep "tiny-" /root/garment-glow-test/src/pages/Integrations.tsx | grep "state\|startsWith" | head -3

# tiny-oauth invocado
grep "tiny-oauth" /root/garment-glow-test/src/pages/Integrations.tsx | head -3
```
</verification>

<success_criteria>
1. "Conectar Tiny ERP" abre o OAuth do Tiny (redirect para contas Tiny)
2. Ao retornar com ?code=...&state=tiny-*, o callback é processado e token salvo
3. Botão "Sincronizar Custos" disponível apenas quando conectado
4. sync-tiny-costs usa token OAuth armazenado, não client_credentials
5. Zero erros TypeScript
</success_criteria>

<output>
Criar .planning/phases/18-tiny-oauth/18-02-SUMMARY.md com status de cada task e resultado do TypeScript.
</output>
